---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/resources/db/migration/V2__create_branch_member_admin.sql
  - src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt
  - src/main/kotlin/com/goldwrestling/branch/Branch.kt
  - src/main/kotlin/com/goldwrestling/member/Member.kt
  - src/main/kotlin/com/goldwrestling/member/MemberStatus.kt
  - src/main/kotlin/com/goldwrestling/admin/Admin.kt
  - src/main/kotlin/com/goldwrestling/admin/AdminBranch.kt
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "빈 DB에서 마이그레이션을 재생하면 branch/member/admin/admin_branch 4개 테이블이 생성된다"
    - "송파점 Branch 1건이 Flyway 시드 마이그레이션으로 존재한다 (D-09)"
    - "member 테이블에 branch_id NOT NULL FK가 있어 이후 지점 확장 전제를 만족한다"
    - "member의 name·phone_number 컬럼은 nullable이다 — 온보딩(Phase 2)에서 입력된다 (D-05)"
    - "admin 테이블에 로그인 자격(비밀번호·로그인 ID) 컬럼이 없다 — 인증 컬럼은 Phase 2에서 V+1 마이그레이션으로 추가된다 (D-04 최소 스키마)"
    - "JPA 엔티티 매핑이 실제 스키마와 일치해 ddl-auto=validate로 기동한다"
  artifacts:
    - path: "src/main/resources/db/migration/V2__create_branch_member_admin.sql"
      provides: "Branch/Member/Admin/AdminBranch 초기 스키마 + 송파점 시드"
      contains: "CREATE TABLE admin_branch"
      min_lines: 35
    - path: "src/main/kotlin/com/goldwrestling/member/Member.kt"
      provides: "Member 엔티티 (branch FK, nullable name/phoneNumber, status)"
      contains: "MemberStatus"
    - path: "src/main/kotlin/com/goldwrestling/member/MemberStatus.kt"
      provides: "회원 상태 enum (PENDING/ACTIVE/ON_LEAVE/INACTIVE)"
      contains: "ON_LEAVE"
    - path: "src/main/kotlin/com/goldwrestling/branch/Branch.kt"
      provides: "Branch 엔티티"
      contains: "@Table(name = \"branch\")"
    - path: "src/main/kotlin/com/goldwrestling/admin/AdminBranch.kt"
      provides: "Admin↔Branch 다대다 조인 엔티티"
      contains: "FetchType.LAZY"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/member/Member.kt"
      to: "src/main/kotlin/com/goldwrestling/branch/Branch.kt"
      via: "@ManyToOne(fetch = FetchType.LAZY) + @JoinColumn(name = \"branch_id\", nullable = false)"
      pattern: "@JoinColumn\\(name = \"branch_id\", nullable = false\\)"
    - from: "src/main/resources/db/migration/V2__create_branch_member_admin.sql"
      to: "src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt"
      via: "flyway_schema_history 버전 \"2\" + 송파점 시드 단언"
      pattern: "송파점"
    - from: "src/main/kotlin/com/goldwrestling/member/Member.kt"
      to: "member.status 컬럼"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "@Enumerated\\(EnumType.STRING\\)"
---

<objective>
FOUND-02: `Branch`·`Member`·`Admin`·`AdminBranch` 초기 스키마를 Flyway V2 마이그레이션으로 만들고,
같은 작업에서 JPA 엔티티를 매핑해 스키마와 코드가 실제로 일치함을 `ddl-auto=validate`로 증명한다.
송파점 Branch 1건은 시드 마이그레이션으로 넣는다(D-09 — 지점 관리 API는 스코프 밖이라 다른 주입 경로가 없다).

Purpose: Phase 2(인증·회원)부터 Phase 6(운영)까지 모든 테이블이 이 4개 테이블을 FK로 참조한다.
`member.branch_id`를 지금 넣지 않으면 회원 데이터가 쌓인 뒤 `NOT NULL` 컬럼을 추가하는 마이그레이션을 짜야 한다(RESEARCH Pitfall 4).
Output: V2 마이그레이션, 엔티티 4종 + `MemberStatus` enum, Flyway 통합테스트 케이스 4건.

**범위 제약(D-04 최소 스키마):** 이번 phase는 **확실한 정체성 컬럼만** 만든다.
카카오 식별자(`kakao_id`), 관리자 로그인 자격(`login_id`/`password_hash`), 역할 컬럼은 **넣지 않는다** — Phase 2가 V3 이후로 추가한다.
Pass·Reservation·ClassSchedule 등 다른 도메인 테이블도 이번 스코프가 아니다.

**실행 순서:** D-10(플랜 단위 브랜치 → dev PR → 머지 → 다음 작업)에 따라 플랜 01 완료·머지 후 시작한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/requirements.md
@docs/policies.md
@docs/glossary.md
@docs/conventions.md
@docs/decisions.md
@.claude/skills/add-migration/SKILL.md
@.claude/skills/verify-boot4-api/SKILL.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

**문서 우선순위(CLAUDE.md):** `docs/policies.md` > `docs/requirements.md` > `docs/glossary.md`·`docs/decisions.md`·`docs/conventions.md` > `.planning/**` > 코드.

**커밋 금지(CLAUDE.md 커밋 규칙, GSD 자동 커밋보다 우선):** `git commit`·`git push`를 실행하지 않는다.
작업 종료 시 `git status --short`를 SUMMARY에 적고 멈춘다.
</context>

<interfaces>
<!-- 기존 코드에서 이미 확인된 사실. 재탐색 불필요. -->

- 현재 마이그레이션은 `src/main/resources/db/migration/V1__baseline.sql`(no-op `SELECT 1;`) **하나뿐**이다 → 다음 번호는 반드시 **V2**. V1은 이미 커밋되어 있으므로 절대 수정하지 않는다.
- `application.yml`: `spring.jpa.hibernate.ddl-auto: validate`, `spring.flyway.baseline-on-migrate: false`, `validate-on-migrate: true`. 이 값들을 바꾸지 않는다.
- `build.gradle.kts`의 `allOpen` 블록이 `jakarta.persistence.Entity`/`MappedSuperclass`/`Embeddable`을 이미 다룬다 → 엔티티 클래스에 `open` 키워드를 직접 붙이지 않는다. Kotlin JPA 플러그인(`kotlin("plugin.jpa")`)이 no-arg 생성자도 만들어 준다.
- `src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt`는 `@SpringBootTest` + `@Import(TestcontainersConfiguration::class)` 조합에 `JdbcClient`를 주입받아 쓴다. 기존 테스트 2건(`모든 마이그레이션이 성공 상태로 기록된다`, `DB 세션 시간대가 Asia_Seoul 이다`)이 있고 첫 테스트는 `contains("1")`을 단언한다.
- `TestcontainersConfiguration`은 `postgres:18.4-alpine` 컨테이너를 `@ServiceConnection`으로 붙인다 → 통합테스트가 빈 DB에서 전 마이그레이션을 재생한다.
- glossary 확정 네이밍: `Branch` / `Member` / `Admin` / `AdminBranch`, `MemberStatus` = `PENDING`·`ACTIVE`·`ON_LEAVE`·`INACTIVE`. 금지어: `Ticket`, `Voucher`, `Coupon`, `Booking`, `Course`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V2 마이그레이션 (4개 테이블 + 송파점 시드) + Flyway 통합테스트 케이스</name>
  <files>
    src/main/resources/db/migration/V2__create_branch_member_admin.sql,
    src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt
  </files>
  <read_first>
    - .claude/skills/add-migration/SKILL.md 전체 (§1 파일명·번호, §2 컬럼 규약, §3 동시성 제약, §4 검증, "절대 금지" 3항목)
    - src/main/resources/db/migration/V1__baseline.sql (헤더 주석 스타일 — 파일 최상단에 목적을 한글로 명시)
    - src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt (기존 테스트 2건과 JdbcClient 사용법 — 애노테이션 조합 유지)
    - docs/policies.md §5·§5.1 (회원 상태 4종, 온보딩은 별도 상태가 아니라 프로필 입력 여부로 판정 → name/phone_number nullable 근거)
    - docs/requirements.md §1 (모든 핵심 엔티티에 branch_id, 관리자 권한은 관리자↔지점 다대다)
    - docs/glossary.md (Branch/Member/Admin/AdminBranch·MemberStatus 네이밍, 금지어)
    - docs/conventions.md §5(시간 타입 표 — timestamptz), §9(Flyway 규칙)
    - .planning/phases/01-foundation/01-CONTEXT.md D-04·D-05·D-09
  </read_first>
  <action>
    `src/main/resources/db/migration/V2__create_branch_member_admin.sql`을 새로 만든다. 파일 최상단에 V1과 같은 스타일로 목적을 한글 주석 2~3줄(무엇을 만드는지 + D-04 최소 스키마 원칙 + 인증 컬럼은 Phase 2에서 추가된다는 사실)로 남긴다.

    테이블은 아래 4개, 컬럼은 정확히 이 목록으로 만든다. 테이블명은 단수 `snake_case`, PK는 전부 `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`(add-migration §2).

    `branch`
    | 컬럼 | 타입/제약 |
    |---|---|
    | `id` | PK identity |
    | `name` | `VARCHAR(100) NOT NULL`, `CONSTRAINT uq_branch_name UNIQUE (name)` |
    | `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |

    `member`
    | 컬럼 | 타입/제약 |
    |---|---|
    | `id` | PK identity |
    | `branch_id` | `BIGINT NOT NULL`, `CONSTRAINT fk_member_branch FOREIGN KEY (branch_id) REFERENCES branch (id)` |
    | `name` | `VARCHAR(50)` — **nullable** (온보딩 전 값 없음, D-05 / policies §5.1) |
    | `phone_number` | `VARCHAR(20)` — **nullable** (동일 사유) |
    | `status` | `VARCHAR(20) NOT NULL DEFAULT 'PENDING'` (MemberStatus, `@Enumerated(STRING)` 대응) |
    | `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |
    | 인덱스 | `CREATE INDEX idx_member_branch ON member (branch_id)` |

    `admin`
    | 컬럼 | 타입/제약 |
    |---|---|
    | `id` | PK identity |
    | `name` | `VARCHAR(50) NOT NULL` |
    | `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |

    `admin_branch`
    | 컬럼 | 타입/제약 |
    |---|---|
    | `id` | PK identity |
    | `admin_id` | `BIGINT NOT NULL`, `CONSTRAINT fk_admin_branch_admin FOREIGN KEY (admin_id) REFERENCES admin (id)` |
    | `branch_id` | `BIGINT NOT NULL`, `CONSTRAINT fk_admin_branch_branch FOREIGN KEY (branch_id) REFERENCES branch (id)` |
    | `created_at` | `TIMESTAMPTZ NOT NULL DEFAULT now()` |
    | 제약·인덱스 | `CONSTRAINT uq_admin_branch UNIQUE (admin_id, branch_id)`, `CREATE INDEX idx_admin_branch_branch ON admin_branch (branch_id)` |

    시드(D-09): `INSERT INTO branch (name) VALUES ('송파점');` 를 `branch` 생성 직후에 넣는다. `id`를 명시하지 않는다(identity 시퀀스가 어긋난다). `admin`·`member`에는 시드 행을 넣지 않는다 — 관리자 계정 시드는 로그인 자격 컬럼이 생기는 Phase 2(AUTH-03)의 일이다.

    **넣지 않을 것(D-04):** `kakao_id`·`login_id`·`password_hash`·`role`·`updated_at`·`deleted_at`·`Pass`/`Reservation` 관련 테이블. `admin_branch`에 정원·권한 레벨 같은 부가 컬럼도 넣지 않는다(MVP는 1:1 매핑, requirements §1).

    `admin_branch`의 서로게이트 PK + `UNIQUE(admin_id, branch_id)` 조합은 add-migration §2의 "PK는 항상 id" 관례와의 일관성을 위한 선택이다(복합 PK 대신). 이 이유를 SQL 주석 한 줄로 남긴다.

    이어서 `FlywayMigrationIntegrationTest.kt`에 케이스를 추가한다(기존 2건은 그대로 두고, 첫 테스트의 `contains("1")`을 `contains("1", "2")`로 확장):
    - `송파점 지점이 시드로 한 건 존재한다` — `SELECT count(*) FROM branch WHERE name = '송파점'` 이 1
    - `member 테이블은 branch_id NOT NULL FK 를 갖는다` — `information_schema.columns`에서 `member.branch_id`의 `is_nullable`이 `NO`이고, `information_schema.table_constraints`에 `constraint_name = 'fk_member_branch'`, `constraint_type = 'FOREIGN KEY'` 행이 1건
    - `admin 테이블에는 아직 로그인 자격 컬럼이 없다` — `information_schema.columns`에서 `table_name = 'admin'`의 컬럼 이름 집합에 `password`/`login_id`/`password_hash`가 없음(D-04 보증 + 자격증명 스키마를 Phase 2가 의도적으로 설계하게 만드는 가드)

    테스트 메서드명은 백틱 한국어 서술문(conventions §10.2), 한 테스트는 한 가지만 검증한다. 애노테이션 조합은 기존 클래스 그대로 유지한다(컨텍스트 캐시 — conventions §10.1).

    이 태스크는 엔티티를 만들지 않으므로 `ddl-auto=validate`가 아직 검증할 매핑이 없다 — 그건 Task 2에서 닫는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"` 가 exit 0이고 테스트 5건(기존 2 + 신규 3)이 실행된다.
    - `ls src/main/resources/db/migration/` 출력이 `V1__baseline.sql`, `V2__create_branch_member_admin.sql` 2개뿐이다(번호 건너뜀 없음).
    - `git diff src/main/resources/db/migration/V1__baseline.sql` 가 빈 출력 — 이미 커밋된 마이그레이션을 수정하지 않았다.
    - V2 파일에서 4개 `CREATE TABLE`이 각각 확인된다: `grep -c '^CREATE TABLE' src/main/resources/db/migration/V2__create_branch_member_admin.sql` 결과가 4.
    - `grep -v '^--' src/main/resources/db/migration/V2__create_branch_member_admin.sql | grep -c "INSERT INTO branch"` 결과가 1이고, 같은 파일에 `member`·`admin` INSERT가 없다(`grep -c "INSERT INTO member\|INSERT INTO admin"` 결과 0).
    - `grep -v '^--' src/main/resources/db/migration/V2__create_branch_member_admin.sql | grep -ciE 'password|login_id|kakao|role '` 결과가 0 — D-04 최소 스키마를 넘지 않았다.
    - V2 파일에 FK 4개 제약명(`fk_member_branch`, `fk_admin_branch_admin`, `fk_admin_branch_branch`)과 `uq_branch_name`, `uq_admin_branch`, `idx_member_branch`, `idx_admin_branch_branch`가 모두 존재한다.
    - `grep -c 'TIMESTAMP\b' src/main/resources/db/migration/V2__create_branch_member_admin.sql` 결과가 0 — 시간대 없는 `timestamp`를 쓰지 않았다(모두 `TIMESTAMPTZ`).
    - `grep -q 'contains("1", "2")' src/test/kotlin/com/goldwrestling/db/FlywayMigrationIntegrationTest.kt` 성공.
  </acceptance_criteria>
  <done>빈 DB에서 V1→V2가 재생되어 4개 테이블·제약·인덱스가 만들어지고, 송파점 시드 1건·`member.branch_id` NOT NULL FK·`admin`에 자격 컬럼 없음이 통합테스트로 단언된다.</done>
</task>

<task type="auto">
  <name>Task 2: JPA 엔티티 4종 + MemberStatus enum (ddl-auto=validate 정합)</name>
  <files>
    src/main/kotlin/com/goldwrestling/branch/Branch.kt,
    src/main/kotlin/com/goldwrestling/member/MemberStatus.kt,
    src/main/kotlin/com/goldwrestling/member/Member.kt,
    src/main/kotlin/com/goldwrestling/admin/Admin.kt,
    src/main/kotlin/com/goldwrestling/admin/AdminBranch.kt
  </files>
  <read_first>
    - src/main/resources/db/migration/V2__create_branch_member_admin.sql (Task 1 산출물 — 컬럼명·nullable·길이를 그대로 옮겨야 한다)
    - docs/conventions.md §1(패키지 레이아웃: 기능별 패키지 안에 엔티티), §3(엔티티 규약 + `SessionPass` 표준 템플릿), §5(시간 타입)
    - docs/glossary.md (클래스명·enum 값·금지어)
    - docs/policies.md §5 (MemberStatus 4종의 의미)
    - .planning/phases/01-foundation/01-PATTERNS.md 엔티티 절 (conventions §3 템플릿 인용 + D-05 nullable 주의)
    - .claude/skills/verify-boot4-api/SKILL.md (낯선 애노테이션·임포트 경로 확인 절차)
    - build.gradle.kts 의 `allOpen` 블록 (엔티티에 open 키워드 불필요한 이유)
  </read_first>
  <action>
    V2 스키마에 대응하는 JPA 엔티티를 기능별 패키지(conventions §1)에 만든다. 엔티티를 이번 phase에서 함께 만드는 이유: `ddl-auto=validate`는 **매핑된 것만** 검증하므로, 엔티티가 없으면 FOUND-02는 "SQL 파일이 있다"까지만 증명된다. 엔티티를 매핑하면 통합테스트 기동만으로 스키마↔코드 일치가 검증된다.

    conventions §3 템플릿을 그대로 따른다: `data class` 금지 / 일반 `class` + 생성자 파라미터에 컬럼 매핑 / `id`는 클래스 본문에 `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null` / `@ManyToOne`은 `fetch = FetchType.LAZY` 명시 / enum은 `@Enumerated(EnumType.STRING)`.

    - `branch/Branch.kt` — `@Entity @Table(name = "branch")`. `@Column(nullable = false, length = 100) var name: String`.
    - `member/MemberStatus.kt` — `enum class MemberStatus { PENDING, ACTIVE, ON_LEAVE, INACTIVE }`. 각 값에 policies §5의 한국어 의미를 KDoc 또는 줄 주석으로 한 줄씩(승인대기/활성/휴회/비활성).
    - `member/Member.kt` — `@Entity @Table(name = "member")`. `@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "branch_id", nullable = false) val branch: Branch`; `@Column(length = 50) var name: String?`; `@Column(name = "phone_number", length = 20) var phoneNumber: String?`; `@Enumerated(EnumType.STRING) @Column(nullable = false, length = 20) var status: MemberStatus = MemberStatus.PENDING`. `name`/`phoneNumber`는 **Kotlin 타입도 nullable**이어야 한다 — 컬럼 nullable과 Kotlin non-null의 불일치는 `validate`가 잡지 못하는 종류의 버그다(conventions §3 마지막 항목). KDoc에 "온보딩(Phase 2, policies §5.1) 완료 판정은 이 두 필드의 입력 여부로 한다"를 한 줄 남긴다.
    - `admin/Admin.kt` — `@Entity @Table(name = "admin")`. `@Column(nullable = false, length = 50) var name: String`. KDoc에 "로그인 자격(ID/PW)은 Phase 2에서 V3 마이그레이션과 함께 추가된다(D-04)".
    - `admin/AdminBranch.kt` — `@Entity @Table(name = "admin_branch")`. `admin`·`branch` 둘 다 `@ManyToOne(fetch = FetchType.LAZY)` + `@JoinColumn(..., nullable = false)`. 양방향 연관을 만들지 않는다(conventions §3: 기본은 단방향).

    **`created_at` 매핑은 이번 phase에서 하지 않는다.** 이유를 각 엔티티 KDoc에 한 줄로 남긴다: 값은 DB `DEFAULT now()`가 소유하고, 애플리케이션이 이 값을 **읽거나 쓰는 코드 경로가 Phase 1엔 없다**. 지금 `OffsetDateTime.now()`를 필드 기본값으로 박으면 "현재 시각은 `Clock` 빈을 주입해서 쓴다"는 conventions §5 규칙을 어기게 되므로, 감사 시각 매핑 전략(`Clock` 빈 + JPA auditing 또는 `insertable = false` 읽기 전용 매핑)은 **첫 INSERT 경로가 생기는 Phase 2에서 결정**한다. Hibernate `validate`는 매핑되지 않은 추가 컬럼을 문제 삼지 않으므로 기동에는 영향이 없다.

    리포지토리·서비스·컨트롤러·DTO는 만들지 않는다. 이번 phase에 소비자가 없고, 엔티티를 API로 노출하지 않는다는 D-019는 Phase 2가 DTO로 감쌀 것을 전제한다.

    **테스트에 대하여(conventions §10.0):** 이 엔티티들은 동작(메서드)이 없는 매핑 선언이라 §10.0의 면제 항목("필드만 있는 DTO", "getter/setter 수준")에 해당한다. 매핑 정합성은 Task 1에서 확장한 `FlywayMigrationIntegrationTest`가 스프링 컨텍스트를 띄울 때 `ddl-auto=validate`로 검증한다 — 즉 이 태스크의 검증은 별도 테스트 신규 작성이 아니라 **기존 통합테스트 통과**다. 이 판단과 사유를 SUMMARY에 한 줄로 남긴다(CLAUDE.md 규칙 10). 엔티티에 도메인 메서드를 하나라도 추가했다면 그 순간 단위테스트 대상이 되므로, 이번 phase에서는 메서드를 추가하지 않는다.

    낯선 애노테이션·임포트 경로(`jakarta.persistence.*`)가 Boot 4에서 다른지 의심되면 추측하지 말고 `verify-boot4-api` 절차(§1 context7 → §5 `./gradlew compileKotlin`)를 밟는다.

    마지막으로 `./gradlew ktlintFormat` → `./gradlew build` 순서로 실행한다(conventions §11.5). 커밋하지 않는다 — `git status --short`를 SUMMARY에 적고 멈춘다. D-10에 따른 브랜치명 권장값은 `feat/p1-initial-schema`이며, 브랜치·PR 생성도 사용자 확인 후 진행한다.
  </action>
  <verify>
    <automated>./gradlew ktlintFormat && ./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` 가 exit 0 — `ddl-auto=validate`가 통과했다는 뜻이며, 매핑↔스키마 불일치가 없다.
    - 엔티티 5개 파일이 정확히 다음 경로에 존재한다: `branch/Branch.kt`, `member/Member.kt`, `member/MemberStatus.kt`, `admin/Admin.kt`, `admin/AdminBranch.kt`.
    - `grep -rc 'data class' src/main/kotlin/com/goldwrestling/{branch,member,admin}` 결과가 모두 0 — `data class` 금지 준수.
    - `grep -rc '@ManyToOne' src/main/kotlin/com/goldwrestling/{member,admin}` 합계와 `grep -rc 'FetchType.LAZY' src/main/kotlin/com/goldwrestling/{member,admin}` 합계가 같다 — 모든 `@ManyToOne`에 LAZY가 명시됐다(총 3개: Member→Branch, AdminBranch→Admin, AdminBranch→Branch).
    - `grep -rq 'EnumType.ORDINAL' src/main/kotlin` 이 no match이고, `grep -q '@Enumerated(EnumType.STRING)' src/main/kotlin/com/goldwrestling/member/Member.kt` 성공.
    - `grep -q 'var name: String?' src/main/kotlin/com/goldwrestling/member/Member.kt` 와 `grep -q 'var phoneNumber: String?' src/main/kotlin/com/goldwrestling/member/Member.kt` 둘 다 성공 — nullable 정합(D-05).
    - `grep -rq 'createdAt' src/main/kotlin/com/goldwrestling` 이 no match — `created_at`을 매핑하지 않았고 `OffsetDateTime.now()`를 필드 기본값으로 박지도 않았다.
    - `grep -rq 'MemberStatus' src/main/kotlin/com/goldwrestling/member/MemberStatus.kt` 성공하고 `PENDING`·`ACTIVE`·`ON_LEAVE`·`INACTIVE` 4개 값이 모두 존재하며 그 외 값은 없다.
    - `grep -rEq 'Ticket|Voucher|Coupon|Booking|Course' src/main/kotlin/com/goldwrestling` 이 no match — glossary 금지어 미사용.
    - `ls src/main/kotlin/com/goldwrestling/{branch,member,admin}` 출력에 `*Repository.kt`·`*Service.kt`·`*Controller.kt`·`dto/`가 없다 — 스코프를 넘지 않았다.
    - `git log --oneline -1` 이 이 플랜 실행 전과 동일 — 커밋이 생성되지 않았다.
  </acceptance_criteria>
  <done>엔티티 4종 + `MemberStatus`가 V2 스키마와 일치하게 매핑되어 `./gradlew build`(= `validate` 통과 + 전체 테스트)가 성공한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway 마이그레이션 → 운영 DB | 마이그레이션 내용이 곧 운영 스키마다. 잘못된 제약·누락된 FK는 애플리케이션 코드로 되돌릴 수 없다 |
| 시드 데이터 → 커밋된 저장소 | 마이그레이션 파일은 git에 그대로 남는다. 실값 시크릿이 들어가면 영구 노출이다 |
| JPA 엔티티 → (Phase 2의) API 응답 | 엔티티 필드가 곧 API 노출 후보가 된다. 이 phase에서 노출 경로를 만들지 않는 것이 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `V2__create_branch_member_admin.sql` 시드 데이터 | mitigate | 시드는 `branch` 1건(`'송파점'`)뿐이며 자격증명·개인정보·토큰을 포함하지 않는다. `admin`·`member` INSERT가 없음을 acceptance에서 grep으로 검증. 관리자 계정 시드는 비밀번호 해시 처리와 함께 Phase 2에서 다룬다 |
| T-01-07 | Tampering | `admin` 테이블에 자격증명 컬럼을 임의 추가 | mitigate | D-04에 따라 `password`/`login_id`/`kakao`/`role` 문자열이 V2에 없음을 grep으로 강제하고, `admin 테이블에는 아직 로그인 자격 컬럼이 없다` 통합테스트로 회귀를 막는다. 해시 알고리즘·저장 형식을 검증 없이 지금 굳히는 것을 방지 |
| T-01-08 | Repudiation | Admin↔Branch 매핑 중복 행 | mitigate | `uq_admin_branch UNIQUE (admin_id, branch_id)` 제약으로 중복 권한 부여를 DB가 거부한다. 권한 감사 시 "누가 어느 지점 담당인가"가 한 행으로 확정된다 |
| T-01-09 | Tampering | 참조 무결성 없는 회원 데이터 | mitigate | `member.branch_id NOT NULL` + `fk_member_branch` FK로 지점 없는 회원 행이 생길 수 없게 한다(requirements §1 확장 전제). 통합테스트가 `is_nullable = 'NO'`와 FK 존재를 단언 |
| T-01-10 | Information Disclosure | 엔티티가 그대로 API로 노출 | accept | 이번 phase는 컨트롤러·리포지토리·서비스를 만들지 않으므로 노출 경로 자체가 없다(acceptance에서 파일 부재로 검증). Phase 2가 D-019에 따라 DTO로 감싼다 |
| T-01-SC | Tampering | 의존성 설치 | accept | 신규 외부 의존성 없음(RESEARCH "신규 패키지 없음"). `build.gradle.kts`를 이 플랜에서 변경하지 않는다 |
</threat_model>

<verification>
- `./gradlew build` 통과 = ktlintCheck + compileKotlin + 전체 테스트 + `ddl-auto=validate` 정합
- `./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"` 5건 통과
- 로컬 DB 확인(add-migration §4): `docker compose up -d` 후 `./gradlew bootRun` 으로 기동되고 Flyway가 V2를 적용한다. 로컬 DB가 이미 어긋나 체크섬 오류가 나면 `docker compose down -v && docker compose up -d`로 초기화하되, **데이터가 지워진다는 사실을 사용자에게 먼저 알리고 확인받는다**
- 플랜 01의 `GlobalExceptionHandlerTest` 회귀 없음
- `git log --oneline -1`이 실행 전과 동일 — 자동 커밋 없음
</verification>

<success_criteria>
1. `branch`·`member`·`admin`·`admin_branch` 4개 테이블이 V2 마이그레이션으로 생성되고, 빈 DB 재생이 통합테스트로 검증된다
2. 송파점 Branch 1건이 시드로 존재한다 (D-09)
3. `member.branch_id`가 `NOT NULL` FK로 존재해 지점 확장 전제를 만족한다 (requirements §1)
4. JPA 엔티티 4종 + `MemberStatus`가 스키마와 일치해 `ddl-auto=validate`로 기동한다
5. 인증 관련 컬럼(카카오 식별자·관리자 자격·역할)이 스키마에 없다 — Phase 2가 V3로 추가한다 (D-04)
6. V1 마이그레이션이 수정되지 않았고 번호 건너뜀이 없다
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.

SUMMARY에 반드시 포함:
- `git status --short` 출력 (커밋하지 않았음을 함께 명시)
- 엔티티에 별도 단위테스트를 만들지 않은 사유 (conventions §10.0 면제 판단 — 매핑 정합은 `validate` + 통합테스트로 검증)
- `created_at`을 매핑하지 않은 결정과 Phase 2로 넘긴 이유 (감사 시각 전략은 `Clock` 빈이 필요한 첫 INSERT 경로에서 결정)
- 로컬 DB(`docker compose` + `bootRun`) 적용 확인 결과. DB 초기화가 필요했다면 사용자에게 먼저 알렸는지 여부
- **이번에 쓴 기술** 섹션 (CLAUDE.md 학습 모드): 이번 작업에 실제로 등장한 것만 3~6항목, 항목마다 ①이름·한 줄 정의 ②이 코드에서 왜 필요했는가(우리 도메인 상황으로) ③안 썼으면 뭐가 깨지는가. 처음 등장한 개념은 `★` 표시. 후보: Flyway 버전 마이그레이션과 체크섬, `ddl-auto=validate`가 검증하는 것과 못 하는 것, JPA `@ManyToOne` 지연 로딩(왜 EAGER가 기본인데 LAZY를 강제하는가), `@Enumerated(STRING)` vs ORDINAL, 외래키·유니크 제약이 애플리케이션 검증과 다른 층에서 하는 일, Testcontainers가 빈 DB를 매번 재생하는 것의 의미
</output>
