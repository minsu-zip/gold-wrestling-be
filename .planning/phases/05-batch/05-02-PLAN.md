---
phase: 05-batch
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql
  - src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt
  - src/main/kotlin/com/goldwrestling/batch/BatchTrigger.kt
  - src/main/kotlin/com/goldwrestling/batch/BatchExecutionStatus.kt
  - src/main/kotlin/com/goldwrestling/batch/BatchExecutionRepository.kt
  - src/main/kotlin/com/goldwrestling/member/Member.kt
  - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
  - src/test/kotlin/com/goldwrestling/batch/BatchExecutionRepositoryTest.kt
  - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt
  - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
autonomous: true
requirements: [BATCH-01, BATCH-04]

must_haves:
  truths:
    - "배치가 주체 없는(admin·member 둘 다 NULL) PassTransaction을 저장할 수 있다"
    - "관리자가 회원을 ON_LEAVE에서 ACTIVE로 되돌리면 그 시각이 member.returned_from_leave_at에 남는다"
    - "PENDING→ACTIVE·INACTIVE→ACTIVE·ACTIVE→ON_LEAVE 전이는 복귀 시각을 남기지 않는다"
    - "배치 실행 1건을 시각·처리 건수·결과·트리거 종류와 함께 저장할 수 있다"
  artifacts:
    - path: "src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql"
      provides: "CHECK 완화 + 복귀 시각 컬럼 + batch_execution 테이블"
      contains: "ck_pass_transaction_subject"
    - path: "src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt"
      provides: "배치 실행 이력 엔티티(append-only)"
      contains: "class BatchExecution"
    - path: "src/main/kotlin/com/goldwrestling/member/Member.kt"
      provides: "휴회 복귀 시각 필드"
      contains: "returnedFromLeaveAt"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      to: "Member.returnedFromLeaveAt"
      via: "ON_LEAVE→ACTIVE 전이에서만 Clock 기반 시각 대입"
      pattern: "returnedFromLeaveAt"
    - from: "src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql"
      to: "pass_transaction 시스템 주체 행"
      via: "at-most-one CHECK 재정의"
      pattern: "NOT \\(admin_id IS NOT NULL AND member_id IS NOT NULL\\)"
---

<objective>
Phase 5의 **스키마 선결 과제 3건**을 V9 마이그레이션 하나로 해결하고, 그 스키마를 쓰는 첫 코드
(엔티티·복귀 시각 기록)를 같은 플랜에서 함께 넣는다.

Purpose: 배치는 admin도 member도 아닌 **시스템 주체**로 원장을 쓰는데 V8 CHECK가 "정확히 하나"를
요구해 저장 자체가 불가능하다. 기준일 후보 ③(휴회 복귀일)은 기록하는 곳이 아예 없다. 실행 이력
테이블도 없다 — 세 가지가 모두 05-05·05-06의 전제라 여기서 먼저 만든다. 마이그레이션만 따로 머지하면
쓰이지 않는 컬럼이 dev에 남으므로(deliver-phase-chunk §1) 복귀 시각을 실제로 기록하는
`AdminMemberService` 변경까지 이 플랜에 포함한다.

Output: `V9__relax_pass_transaction_subject_and_add_batch_execution.sql`, `batch/` 패키지 4파일
(엔티티·enum 2종·리포지토리), `Member.returnedFromLeaveAt`, `AdminMemberService.changeStatus` 확장,
테스트 3파일 갱신·신설.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@docs/glossary.md
@.claude/skills/add-migration/SKILL.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-RESEARCH.md
@.planning/phases/05-batch/05-PATTERNS.md
@.planning/phases/05-batch/05-01-SUMMARY.md
</context>

<interfaces>
`src/main/resources/db/migration/` 현재 최신은 **V8**이다 → 새 파일은 **V9**. 커밋된 V1~V8은 수정 금지.

V8이 만든 완화 대상 제약:
```sql
ALTER TABLE pass_transaction ADD CONSTRAINT ck_pass_transaction_subject CHECK (
    (admin_id IS NOT NULL AND member_id IS NULL) OR
    (admin_id IS NULL AND member_id IS NOT NULL)
);
```

`pass/PassTransaction.kt` 생성자 시그니처(변경하지 않는다 — `admin`·`member`가 이미 nullable):
`PassTransaction(pass: Pass, amount: BigDecimal, reason: TransactionReason, note: String?, admin: Admin?, member: Member?, occurredAt: OffsetDateTime)`
클래스 KDoc이 "**정확히 하나**만 채워진다"고 적고 있다 — 이 문장을 시스템 주체(둘 다 null) 허용으로 고쳐야 한다.

`member/Member.kt`: nullable 필드 선언 관례는 `var rejectionReason: String? = null` (본문 생성자 파라미터).
`member/AdminMemberService.kt`: 생성자는 현재 `(memberRepository: MemberRepository, tokenService: TokenService)` —
`Clock` 주입이 없다. `changeStatus(memberId, newStatus)`가 `member.status = newStatus` 대입을 갖고 있다.
`config/ClockConfig.kt`가 `Clock` 빈을 제공한다.

`admin/Admin.kt`: `batch_execution.triggered_by_admin_id`의 FK 대상.

기존 엔티티 관례(conventions §3): `data class` 금지 / `@ManyToOne(fetch = FetchType.LAZY)` 명시 /
`@Enumerated(EnumType.STRING)` 필수 / `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null`.

테스트 애노테이션 조합(conventions §10.1 — 컨텍스트 캐시 재사용):
`@SpringBootTest` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)` (+ 필요 시 `@Transactional`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V9 마이그레이션 — CHECK 완화 + 복귀 시각 컬럼 + batch_execution 테이블</name>
  <files>src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql, src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt</files>
  <read_first>
    - .claude/skills/add-migration/SKILL.md (절차·금지사항 전체)
    - src/main/resources/db/migration/V8__extend_pass_transaction_subject.sql (CHECK 재정의 관례와 헤더 주석 형식)
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (신규 테이블 생성 관례 — 서로게이트 PK·TIMESTAMPTZ·명명된 FK·CHECK)
    - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt (기존 CHECK 관련 단언이 있는지 확인 — 있으면 함께 갱신)
    - docs/decisions.md (05-01이 기록한 결정 A·결정 D)
  </read_first>
  <action>
`src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql`을
새로 만든다. 헤더 주석에 05-01이 부여한 **결정 A·D의 실제 번호**와 D-105·D-106·D-108을 인용하고,
"커밋된 V4·V8은 수정하지 않는다"를 명시한다(V8 헤더 주석 형식을 그대로 따른다).

내용 3덩어리:

(1) `pass_transaction` 주체 CHECK 완화 — 기존 `ck_pass_transaction_subject`를 DROP하고
`CHECK (NOT (admin_id IS NOT NULL AND member_id IS NOT NULL))`로 재추가한다. 주석에 "배치
(`INACTIVITY`)는 admin도 member도 아닌 시스템 주체라 둘 다 NULL이다. 기존 행은 전부 한쪽만
채워져 있어 백필이 필요 없다"를 남긴다.

(2) `ALTER TABLE member ADD COLUMN returned_from_leave_at TIMESTAMPTZ;` — nullable.
주석에 "ON_LEAVE → ACTIVE 전이에서만 기록한다(D-105 기준일 후보 ③ 전용). 과거 복귀 이력은 데이터가
없어 후보에서 자연 제외되며 이는 의도된 동작"을 남긴다.

(3) `CREATE TABLE batch_execution` — 컬럼:
`id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`,
`trigger_type VARCHAR(20) NOT NULL`,
`triggered_by_admin_id BIGINT`,
`started_at TIMESTAMPTZ NOT NULL`,
`finished_at TIMESTAMPTZ NOT NULL`,
`processed_member_count INT NOT NULL`,
`deducted_count INT NOT NULL`,
`skipped_count INT NOT NULL`,
`status VARCHAR(20) NOT NULL`,
`error_summary VARCHAR(1000)`.
제약: `CONSTRAINT fk_batch_execution_admin FOREIGN KEY (triggered_by_admin_id) REFERENCES admin (id)`,
`CONSTRAINT ck_batch_execution_trigger CHECK ((trigger_type = 'MANUAL' AND triggered_by_admin_id IS NOT NULL) OR (trigger_type = 'SCHEDULED' AND triggered_by_admin_id IS NULL))`.
인덱스: `CREATE INDEX idx_batch_execution_started_at ON batch_execution (started_at);`
주석에 "실행 이력은 관측·복구 판단용이며 멱등성 판단에 쓰지 않는다(D-106·D-108)"를 남긴다.

`PassRepositoryTest`(또는 기존에 주체 CHECK를 단언하는 테스트 파일)에 **시스템 주체 행 저장이
성공한다**는 케이스를 추가한다 — `PassTransaction(admin = null, member = null,
reason = TransactionReason.INACTIVITY, ...)` 저장 후 `flush()`가 예외 없이 끝나고 조회 시 두 주체가
모두 null이다. 반대로 admin·member를 **둘 다 채운** 행 저장은 여전히 DB 제약 위반으로 실패한다는
케이스도 함께 둔다(완화가 과하게 열리지 않았음을 고정).

`PassTransaction.kt`의 클래스 KDoc에서 "**정확히 하나**만 채워진다"는 문장을 "관리자 조작은 [admin],
회원 셀프 조작은 [member], **배치(시스템 주체)는 둘 다 null**이다 — 둘 다 채우는 것만 금지된다
(V9에서 `ck_pass_transaction_subject`를 at-most-one으로 완화)"로 고친다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest" --tests "com.goldwrestling.pass.PassRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest" --tests "com.goldwrestling.pass.PassRepositoryTest"` 종료 코드 0
- `ls src/main/resources/db/migration/`에 `V9__relax_pass_transaction_subject_and_add_batch_execution.sql`이 있고 V1~V8 파일은 `git diff`에 나타나지 않는다
- `grep -c "NOT (admin_id IS NOT NULL AND member_id IS NOT NULL)" src/main/resources/db/migration/V9__*.sql`이 1 이상
- `grep -c "ck_batch_execution_trigger" src/main/resources/db/migration/V9__*.sql`이 1 이상
- V9 파일에 `TIMESTAMP ` (without time zone) 타입이 없다 — 타임스탬프는 전부 `TIMESTAMPTZ`
- 테스트가 "시스템 주체(둘 다 null) 저장 성공"과 "둘 다 채운 행 저장 실패"를 각각 단언한다
  </acceptance_criteria>
  <done>배치가 시스템 주체 원장을 남길 수 있고, 복귀 시각·실행 이력 저장 자리가 DB에 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: batch 패키지 엔티티·enum·리포지토리 + Member 복귀 시각 필드</name>
  <files>src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt, src/main/kotlin/com/goldwrestling/batch/BatchTrigger.kt, src/main/kotlin/com/goldwrestling/batch/BatchExecutionStatus.kt, src/main/kotlin/com/goldwrestling/batch/BatchExecutionRepository.kt, src/main/kotlin/com/goldwrestling/member/Member.kt, src/test/kotlin/com/goldwrestling/batch/BatchExecutionRepositoryTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (append-only 엔티티 골격 — 전 필드 val, id 선언 위치)
    - src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt (enum 상수별 KDoc 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassTransactionRepository.kt (리포지토리 선언 관례)
    - src/main/kotlin/com/goldwrestling/member/Member.kt (전체 — nullable 필드 선언 위치와 KDoc 관례)
    - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt (Testcontainers 리포지토리 테스트 골격·픽스처 생성 방식)
    - docs/glossary.md `## 배치 (Phase 5)` (05-01이 등록한 이름 — 이 이름을 그대로 쓴다)
  </read_first>
  <behavior>
    - `BatchExecutionRepository.save(...)` 후 `findById`로 읽으면 트리거 종류·시각·건수 3종·상태가 그대로 돌아온다
    - `trigger = MANUAL`인데 `triggeredBy = null`이면 저장이 DB 제약(`ck_batch_execution_trigger`) 위반으로 실패한다
    - `trigger = SCHEDULED`인데 `triggeredBy`가 채워져 있으면 저장이 실패한다
    - `trigger = SCHEDULED`, `triggeredBy = null`, `status = SUCCESS` 조합은 저장에 성공한다
    - `errorSummary = null`(정상 실행)도 저장에 성공한다
  </behavior>
  <action>
`src/main/kotlin/com/goldwrestling/batch/` 패키지를 새로 만들고 아래 4파일을 추가한다(D-018 기능별 패키지).

- `BatchTrigger.kt` — `enum class BatchTrigger { SCHEDULED, MANUAL }`. 상수마다 한 줄 KDoc
  (`TransactionReason.kt` 관례).
- `BatchExecutionStatus.kt` — `enum class BatchExecutionStatus { SUCCESS, PARTIAL_FAILURE }`.
  `SUCCESS` KDoc에 "경쟁 패배·대상 소진으로 인한 스킵은 정상 경로라 여기에 포함된다 — 스킵 건수는
  `skippedCount`가 따로 센다"를 명시한다(05-01 결정 D).
- `BatchExecution.kt` — `@Entity @Table(name = "batch_execution")`, append-only(전 필드 `val`, setter 없음).
  필드: `trigger: BatchTrigger`(`@Enumerated(STRING)`, `@Column(name = "trigger_type", nullable = false, length = 20)`),
  `triggeredBy: Admin?`(`@ManyToOne(fetch = LAZY)`, `@JoinColumn(name = "triggered_by_admin_id")`),
  `startedAt: OffsetDateTime`, `finishedAt: OffsetDateTime`,
  `processedMemberCount: Int`, `deductedCount: Int`, `skippedCount: Int`,
  `status: BatchExecutionStatus`(`@Enumerated(STRING)`, length = 20), `errorSummary: String?`(length = 1000).
  클래스 KDoc에 "관측·복구 판단용이며 멱등성의 근거가 아니다(D-108) — 이 테이블을 '오늘 이미 실행했나'
  판단에 쓰면 D-106 설계가 무너진다"를 남긴다.
- `BatchExecutionRepository.kt` — `interface BatchExecutionRepository : JpaRepository<BatchExecution, Long>`.
  이번 phase는 조회 API가 없으므로 커스텀 쿼리·`JpaSpecificationExecutor`를 넣지 않는다.

`member/Member.kt`에 `@Column(name = "returned_from_leave_at") var returnedFromLeaveAt: OffsetDateTime? = null`을
본문 생성자 파라미터 **맨 끝**에 추가한다(기존 파라미터 순서를 바꾸면 호출부가 전부 깨진다).
KDoc에 "`ON_LEAVE` → `ACTIVE` 전이에서만 갱신한다 — 다른 상태 전이는 이 값을 건드리지 않는다
(D-105 기준일 후보 ③). `Reservation.canceledAt`과 같은 단일 목적 시각 컬럼이다"를 남긴다.

테스트 `src/test/kotlin/com/goldwrestling/batch/BatchExecutionRepositoryTest.kt`를 `<behavior>`의
5개 케이스로 먼저 작성(RED — 엔티티가 없어 컴파일 실패)한 뒤 구현한다. 제약 위반 케이스는
`assertThatThrownBy { ...saveAndFlush(...) }`로 단언한다(`flush` 없이는 제약 위반이 트랜잭션 종료
시점까지 드러나지 않는다). 테스트 메서드명은 백틱 한국어 서술문(conventions §2).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.batch.BatchExecutionRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.batch.BatchExecutionRepositoryTest"` 종료 코드 0이고 5개 이상 테스트가 실행된다
- `grep -c "data class" src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt`가 0 (엔티티 data class 금지)
- `grep -c "fetch = FetchType.LAZY" src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt`가 1 이상
- `grep -c "@Enumerated(EnumType.STRING)" src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt`가 2
- `grep -c "var " src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt`가 0 (append-only)
- `grep -n "returnedFromLeaveAt" src/main/kotlin/com/goldwrestling/member/Member.kt`가 1행 이상 반환한다
- `./gradlew build`가 통과한다 (`ddl-auto=validate`가 V9 스키마와 엔티티 매핑 일치를 검증)
  </acceptance_criteria>
  <done>실행 이력을 저장할 엔티티·리포지토리가 존재하고 회원 엔티티가 복귀 시각을 담을 수 있다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ON_LEAVE→ACTIVE 복귀 시각 기록</name>
  <files>src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt, src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (전체 — `changeStatus`·`approve`의 현재 분기와 생성자)
    - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt (전체 — 애노테이션 조합·픽스처·MutableTestClock 조작 방식)
    - src/main/kotlin/com/goldwrestling/config/ClockConfig.kt (Clock 빈)
    - src/test/kotlin/com/goldwrestling/support/MutableTestClock.kt (시각 이동 API)
    - docs/decisions.md (05-01이 기록한 결정 B)
  </read_first>
  <behavior>
    - `ON_LEAVE` 회원을 `ACTIVE`로 바꾸면 `returnedFromLeaveAt`이 `Clock` 기준 현재 시각으로 채워진다
    - `PENDING` 회원을 `ACTIVE`로 바꾸면 `returnedFromLeaveAt`은 여전히 null이다
    - `INACTIVE` 회원을 `ACTIVE`로 바꿔도 `returnedFromLeaveAt`은 여전히 null이다
    - `ACTIVE` 회원을 `ON_LEAVE`로 바꾸면 `returnedFromLeaveAt`은 갱신되지 않는다(휴회 시작은 복귀가 아니다)
    - 두 번째 휴회 후 다시 복귀하면 `returnedFromLeaveAt`이 **더 최근 시각으로 덮어써진다**
    - `approve`(PENDING→ACTIVE)는 `returnedFromLeaveAt`을 건드리지 않는다
  </behavior>
  <action>
`AdminMemberService` 생성자에 `private val clock: Clock`을 추가한다(`ReservationLedgerSupport`와
동일한 주입 방식 — `OffsetDateTime.now()` 직접 호출 금지, conventions §5).

`changeStatus`에서 `member.status = newStatus` 대입 **직전에** 이전 상태를 지역 변수로 보관하고
(`val previousStatus = member.status`), 대입 직후에
`if (previousStatus == MemberStatus.ON_LEAVE && newStatus == MemberStatus.ACTIVE) { member.returnedFromLeaveAt = OffsetDateTime.now(clock) }`
분기를 추가한다. 기존 검사(`ACTIVE` 전환 시 온보딩 완료 요구)와 `rejectionReason` 초기화,
`revokeAllForMember` 호출 순서는 **바꾸지 않는다**.

`approve`는 `PENDING` → `ACTIVE` 전용이라 복귀가 아니다 — 손대지 않는다.

메서드 KDoc에 "휴회 복귀 시각은 여기서만 기록된다. 이 값은 2주 미사용 차감의 기준일 후보 ③이며
(D-105), 복귀하면 유예 2주가 새로 시작한다 — 기록이 누락되면 복귀 직후 회원이 밀린 주기만큼 소급
차감된다"를 남긴다.

`MemberStatusChangeTest`에 `<behavior>` 6종을 백틱 한국어 서술문 테스트로 추가한다. 시각 단언은
`(clock as MutableTestClock).setTo(...)`로 고정한 값과 `isEqualTo` 비교로 한다. 두 번째 복귀 케이스는
시각을 이동시킨 뒤 다시 복귀시켜 값이 **더 최근으로 바뀌는지** 확인한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.member.MemberStatusChangeTest" --tests "com.goldwrestling.member.MemberApprovalTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.member.MemberStatusChangeTest" --tests "com.goldwrestling.member.MemberApprovalTest"` 종료 코드 0
- `MemberStatusChangeTest`에 `returnedFromLeaveAt`을 단언하는 테스트가 6개 이상 있다
- `grep -c "OffsetDateTime.now()" src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt`가 0
  (반드시 `OffsetDateTime.now(clock)`)
- `AdminMemberService.changeStatus`가 `previousStatus`(또는 동등한 이전 상태 보관 변수)를 `member.status`
  대입 **전에** 읽는다 — 대입 후에 읽으면 조건이 영원히 거짓이다
- `approve` 메서드 본문에 `returnedFromLeaveAt`이 등장하지 않는다
- `./gradlew ktlintFormat` 후 `./gradlew build` 통과
  </acceptance_criteria>
  <done>기준일 후보 ③(휴회 복귀일)이 실제 데이터로 쌓이기 시작한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 → `pass_transaction` 원장 | CHECK 완화는 원장 무결성 규칙을 넓히는 변경이다 — 과하게 열면 주체 없는 사람 조작이 섞인다 |
| 관리자 상태 변경 API → 배치 기준일 | 복귀 시각이 잘못 기록되면 회원이 부당하게 차감되거나 영구히 차감되지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | CHECK 완화가 과해져 사람 조작 이력의 주체가 누락됨 | mitigate | at-most-one으로만 완화(둘 다 채우는 것은 여전히 금지). 기존 경로는 admin/member를 계속 채우고, 주체 없는 저장은 배치 코드에서만 발생 — Task 1이 "둘 다 채운 행은 실패"를 테스트로 고정 |
| T-05-04 | Repudiation | 배치 차감과 관리자 차감이 원장에서 구분되지 않음 | mitigate | 시스템 주체를 별도 계정으로 위장하지 않고 둘 다 NULL + `reason = INACTIVITY`로 표현 — `admin_id`의 의미를 오염시키지 않는다 |
| T-05-05 | Tampering | 복귀 시각이 다른 상태 전이에서도 갱신돼 차감 시계가 부당하게 리셋됨 | mitigate | `previousStatus == ON_LEAVE && newStatus == ACTIVE` 단일 분기. PENDING·INACTIVE→ACTIVE 케이스를 테스트로 고정 |
| T-05-06 | Denial of Service | `batch_execution`이 무한 증가 | accept | 하루 1~수 건 규모(연 400건 미만)라 보관 비용이 무의미하다. 정리 정책은 필요가 관찰되면 도입 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과 (Testcontainers가 빈 DB에서 V1~V9를 재생하고 엔티티 매핑을 validate)
- Phase 2~4 기존 테스트 전부 그린 — 특히 `PassLedgerInvariantTest`·`MemberApprovalTest`(CHECK 완화·Member 필드 추가의 회귀 확인)
</verification>

<success_criteria>
- 05-05가 `PassTransaction(admin = null, member = null)`을 저장할 때 DB 제약에 막히지 않는다
- 05-06이 `member.returnedFromLeaveAt`을 기준일 후보로 조회할 수 있는 데이터 경로가 존재한다
- `batch_execution`에 실행 1건을 기록할 수 있고, MANUAL 실행은 관리자 없이 저장될 수 없다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-02-SUMMARY.md` when done.
청크 A(`feature/phase-05a-batch-foundation`) 진행 중 — 이 플랜에서는 PR을 내지 않는다.
</output>
