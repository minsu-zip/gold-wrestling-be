---
phase: 05-batch
plan: 12
type: execute
wave: 12
gap_closure: true
depends_on: [05-11]
files_modified:
  - src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt
  - src/main/resources/application.yml
  - .env.example
  - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
  - src/main/kotlin/com/goldwrestling/batch/BatchExceptions.kt
  - src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt
  - src/test/kotlin/com/goldwrestling/batch/BatchExecutionRecorderTest.kt
  - docs/error-codes.md
  - docs/decisions.md
  - docs/glossary.md
autonomous: true
requirements: [BATCH-04]
must_haves:
  truths:
    - "실행 시작은 RUNNING 행 1건을 즉시 커밋하고, 이미 실행 중이면 BatchAlreadyRunningException으로 거부된다"
    - "거부는 RFC 9457 ProblemDetail 409 + BATCH_ALREADY_RUNNING 코드로 표현된다"
    - "앱이 죽어 남은 RUNNING 행은 임계 시간(기본 30분) 이후 FAILED(STALE)로 정리되고 새 실행이 시작된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt"
      provides: "실행 시작/종료를 러너 본문과 분리된 트랜잭션으로 기록"
      contains: "Propagation.REQUIRES_NEW"
    - path: "src/main/kotlin/com/goldwrestling/batch/BatchExceptions.kt"
      provides: "BatchAlreadyRunningException (409)"
      contains: "BatchAlreadyRunningException"
    - path: "src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt"
      provides: "정책 시행일·1회 실행 상한·stale 임계 시간 설정"
      contains: "policyEffectiveDate"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt"
      to: "BatchExecutionRepository.markStaleRunningAsFailed"
      via: "start()에서 stale 정리 후 RUNNING 삽입"
      pattern: "markStaleRunningAsFailed"
    - from: "src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt"
      to: "BatchAlreadyRunningException"
      via: "DataIntegrityViolationException 변환"
      pattern: "DataIntegrityViolationException"
---

<objective>
실행 직렬화의 **실행 지점**을 만든다 — `RUNNING` 행 삽입/확정을 러너 본문과 **별개의 트랜잭션**으로
수행하는 `BatchExecutionRecorder`와, 유니크 위반을 409로 바꾸는 도메인 예외·에러코드다.
같은 작업에서 배치 설정 3종(`policy-effective-date`·`max-deductions-per-run`·`stale-run-timeout`)을
`@ConfigurationProperties`로 묶는다 — `stale-run-timeout`은 이 플랜의 레코더가 바로 쓰고,
나머지 둘은 05-14가 쓴다.

Purpose: CR-01(동시 실행 이중 차감)의 거부 경로를 만든다. 러너에 `@Transactional`을 붙일 수 없고
(D-112 실패 격리) 같은 클래스 내부 호출은 프록시를 우회하므로, 기록자는 **별도 스프링 빈**이어야
한다(`InactivityDeductionService`가 이미 같은 이유로 분리돼 있다 — 05-01 결정 C).
Output: 프로퍼티 클래스·설정 키 3종, `BatchAlreadyRunningException` + `BATCH_ALREADY_RUNNING`(409),
레코더와 그 통합테스트, D-118 기록 + D-114 "새 에러코드는 추가하지 않는다" 정정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@docs/error-codes.md
@.planning/phases/05-batch/05-GAP-CONTEXT.md
@.claude/skills/add-domain-test/SKILL.md
@.claude/skills/verify-boot4-api/SKILL.md

**확정 설계(재검토 금지):** `05-GAP-CONTEXT.md` §2.1 — 시작·종료 UPDATE는 러너 본문과 별개
트랜잭션, 어떤 종료 경로에서도 행이 `RUNNING`으로 남지 않게 한다. stale 임계 기본 30분.
거부는 409 + `ProblemDetail`. D-114의 "새 에러코드는 추가하지 않는다"는 **정정 대상**이다 —
"거부할 요청이 없다"는 전제가 CR-01로 깨졌다.

**D-116 키를 옮기지 않는다.** `goldwrestling.batch.inactivity-scheduler-enabled`는
`@ConditionalOnProperty`가 **원시 프로퍼티 이름**으로 읽고 `build.gradle.kts` 테스트 태스크가
같은 이름의 시스템 프로퍼티로 고정한다 — 새 프로퍼티 클래스로 옮기면 킬 스위치가 조용히 깨진다.
새 키는 그 형제로 `goldwrestling.batch.inactivity.*` 아래에 둔다.

<interfaces>
05-11이 만든 것(이 플랜의 입력):
- `BatchExecutionStatus`: `RUNNING`, `SUCCESS`, `PARTIAL_FAILURE`, `FAILED`
- `BatchExecution(trigger, triggeredByAdminId: Long?, startedAt, status = RUNNING, ...)`,
  `fun finish(status, finishedAt, processedMemberCount, deductedCount, skippedCount, errorSummary)`
- `BatchExecutionRepository.findFirstByStatus(status)`,
  `markStaleRunningAsFailed(threshold, finishedAt, errorSummary): Int`,
  `findAllByOrderByStartedAtDesc(pageable)`
- V10: `uq_batch_execution_running` 부분 유니크 인덱스, `finished_at` nullable

기존 관례:
- `DomainException(errorCode, message, status = errorCode.defaultStatus)` — `common/error`
- `ErrorCode` enum에 `defaultStatus`를 함께 선언(예: `INSUFFICIENT_PASS_COUNT`)
- `GlobalExceptionHandler`가 `DomainException`을 `ProblemDetail`로 변환하고 `code` 필드에
  enum name을 넣는다 — 컨트롤러에서 `try-catch`를 쓰지 않는다(D-017)
- `@ConfigurationPropertiesScan`이 `GoldWrestlingApplication`에 이미 붙어 있다 —
  프로퍼티 클래스에 별도 등록 애노테이션이 필요 없다
- 기존 프로퍼티 클래스 선례: `config/JwtProperties.kt`, `config/AdminSeedProperties.kt`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 배치 설정 3종을 @ConfigurationProperties로 묶고 환경변수 키를 연다</name>
  <files>
    src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt,
    src/main/resources/application.yml,
    .env.example
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/config/JwtProperties.kt,
    src/main/kotlin/com/goldwrestling/config/AdminSeedProperties.kt,
    src/main/resources/application.yml,
    .env.example,
    src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt
  </read_first>
  <action>
    `src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt`를 새로 만든다.
    `@ConfigurationProperties(prefix = "goldwrestling.batch.inactivity")`를 붙인 `data class`이며
    필드는 정확히 3개, 모두 기본값을 갖는다(기본값이 있어야 로컬·테스트가 설정 없이 돈다):
    - `policyEffectiveDate: LocalDate = LocalDate.of(2026, 9, 1)` — 정책 시행일 하한.
      이 날짜 이전의 미사용은 차감 부채로 치지 않는다(05-14가 소비한다)
    - `maxDeductionsPerRun: Int = 1` — 배치 1회 실행에서 회원 1명당 최대 차감 횟수(05-14가 소비한다)
    - `staleRunTimeout: Duration = Duration.ofMinutes(30)` — 이 시간을 넘긴 `RUNNING` 행은 죽은
      실행으로 보고 정리한다(Task 3이 소비한다)
    클래스 KDoc에 각 값이 왜 필요한지(소급 차감 방지·사고 피해 상한·앱 급종료 시 영구 차단 방지)와
    **D-116의 `inactivity-scheduler-enabled`는 이 클래스로 옮기지 않는다**는 사실·이유를 적는다.

    `src/main/resources/application.yml`의 `goldwrestling.batch` 아래에 `inactivity:` 블록을 추가한다
    (기존 `inactivity-scheduler-enabled` 키는 **그대로 둔다**). 값은 전부 환경변수 오버라이드 형태로:
    `policy-effective-date: ${BATCH_INACTIVITY_POLICY_EFFECTIVE_DATE:2026-09-01}`,
    `max-deductions-per-run: ${BATCH_INACTIVITY_MAX_DEDUCTIONS_PER_RUN:1}`,
    `stale-run-timeout: ${BATCH_INACTIVITY_STALE_RUN_TIMEOUT:30m}`.
    각 키 위에 한 줄 주석으로 의미와 결정 번호(D-118·D-119)를 적는다.

    `.env.example`의 "--- 배치 (D-116) ---" 절에 위 세 환경변수 키를 값 없이(또는 기본값 주석과
    함께) 추가한다 — CLAUDE.md 시크릿 규칙("새 환경변수 추가 시 `.env.example`에 키 이름을 함께
    추가한다"). 실값처럼 보이는 문자열을 넣지 않는다.

    이 태스크는 `@ConfigurationProperties` 데이터 클래스 + yml + 문서만 건드리므로
    conventions §10.0의 **테스트 면제** 항목에 해당한다 — 테스트를 새로 만들지 않고,
    그 사실을 SUMMARY에 한 줄로 밝힌다.
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'goldwrestling.batch.inactivity"' src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt` 가 1
    - `grep -c 'policyEffectiveDate\|maxDeductionsPerRun\|staleRunTimeout' src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt` 가 3 이상
    - `grep -c 'inactivity-scheduler-enabled' src/main/resources/application.yml` 가 1 (D-116 키가 살아 있다)
    - `grep -c 'BATCH_INACTIVITY_POLICY_EFFECTIVE_DATE\|BATCH_INACTIVITY_MAX_DEDUCTIONS_PER_RUN\|BATCH_INACTIVITY_STALE_RUN_TIMEOUT' .env.example` 가 3
    - `./gradlew compileKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>배치 정책 값 3종이 코드 상수가 아니라 설정으로 존재하고, 재배포 없이 환경변수로 바꿀 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 중복 실행 거부 예외와 409 에러코드를 만든다</name>
  <files>
    src/main/kotlin/com/goldwrestling/batch/BatchExceptions.kt,
    src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt,
    docs/error-codes.md,
    docs/decisions.md
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt,
    src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt,
    src/main/kotlin/com/goldwrestling/common/error/DomainException.kt,
    src/main/kotlin/com/goldwrestling/common/error/GlobalExceptionHandler.kt,
    docs/error-codes.md,
    docs/decisions.md
  </read_first>
  <action>
    `src/main/kotlin/com/goldwrestling/batch/BatchExceptions.kt`를 새로 만든다.
    `class BatchAlreadyRunningException : DomainException(ErrorCode.BATCH_ALREADY_RUNNING, "...")` —
    사용자 대면 문구는 내부 정보 없이 "이미 실행 중인 배치가 있습니다. 실행이 끝난 뒤 다시
    시도하세요." 수준으로 쓴다(conventions §8 — 스택·SQL·제약조건명 금지). KDoc에 이 예외가
    나오는 조건(`uq_batch_execution_running` 위반)과 **정상 경로**라는 사실(관리자 더블클릭·cron과
    수동 실행 겹침은 예상된 상황이고, 거부가 곧 안전한 결과다)을 적는다.

    `ErrorCode`에 `BATCH_ALREADY_RUNNING(HttpStatus.CONFLICT)`을 추가한다 — enum 마지막에 두고
    KDoc 한 줄("배치가 이미 실행 중이어서 새 실행을 거부 (CR-01, D-118)")을 붙인다.
    conventions §8의 상태 매핑("정원·중복·잔여부족 같은 상태 충돌 409")과 일치한다.

    `docs/error-codes.md`에 "## 배치 코드 (Phase 5)" 절을 추가하고 기존 표 형식
    (`| 코드 | HTTP 상태 | 의미 | 발생 지점 |`)으로 한 행을 적는다. 발생 지점은
    `BatchExecutionRecorder`다.

    `docs/decisions.md`에 **D-118**을 추가한다 — 제목은 "배치 중복 실행은 409
    `BATCH_ALREADY_RUNNING`으로 거부한다". 내용: 결정(새 `ErrorCode` 추가 + `ProblemDetail` 409),
    이유(D-114가 "새 에러코드는 추가하지 않는다"고 한 근거는 "상태 기반 설계가 중복 실행을 흡수해
    거부할 요청이 없다"였는데 그 전제가 CR-01로 깨졌다 — 동시 호출은 흡수되지 않는다),
    기각 대안(무거부 재시도 안내만 — 관리자가 실제로 재시도하면 이중 차감이 그대로 난다 /
    202만 반환하고 조용히 스킵 — 관리자가 "실행됐다"고 오해한다).
    같은 편집에서 **D-114에 정정 항목**을 덧붙인다: "새 에러코드는 추가하지 않는다"는 문장이
    2026-08-16부로 철회됐고 `BATCH_ALREADY_RUNNING`이 추가됐다는 것, 근거는 D-118.

    **테스트 시차를 SUMMARY에 명시한다.** `conventions.md` §10.0은 "에러 응답 변경 → 통합테스트로
    상태코드·본문 형태 확인"을 요구하지만, 이 태스크 시점에는 409를 반환할 HTTP 경로가 아직
    존재하지 않는다(컨트롤러는 05-15에서 바뀐다). 따라서 HTTP 409 + `ProblemDetail` 본문 검증은
    05-15의 `AdminBatchControllerTest`·`AdminBatchRunConcurrencyTest`가 담당한다 —
    이 태스크는 예외·에러코드 선언까지만 한다. 이 시차와 "어느 플랜이 실제로 계약을 검증하는가"를
    `05-12-SUMMARY.md`에 한 줄로 밝힌다(CLAUDE.md 규칙 10 — 테스트를 쓰지 않기로 판단했으면
    완료 보고에 이유를 밝힌다).
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BATCH_ALREADY_RUNNING(HttpStatus.CONFLICT)' src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt` 가 1
    - `grep -c 'class BatchAlreadyRunningException' src/main/kotlin/com/goldwrestling/batch/BatchExceptions.kt` 가 1
    - `grep -c 'BATCH_ALREADY_RUNNING' docs/error-codes.md` 가 1
    - `grep -c '^## D-118' docs/decisions.md` 가 1
    - D-114 절에 2026-08-16 정정 항목이 있다 (`awk '/^## D-114/,/^## D-115/' docs/decisions.md | grep -c '2026-08-16'` 가 1 이상)
    - `./gradlew compileKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>중복 실행 거부가 이 저장소의 에러 규약(RFC 9457 + `code` 필드) 안에서 표현되고, D-114의 잘못된 전제가 문서에서 정정됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BatchExecutionRecorder — 시작·종료·stale 정리를 별도 트랜잭션으로</name>
  <files>
    src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt,
    src/test/kotlin/com/goldwrestling/batch/BatchExecutionRecorderTest.kt,
    docs/glossary.md
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt,
    src/main/kotlin/com/goldwrestling/batch/BatchExecutionRepository.kt,
    src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt,
    src/test/kotlin/com/goldwrestling/batch/BatchExecutionRepositoryTest.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityDeductionConcurrencyTest.kt,
    docs/glossary.md,
    docs/conventions.md
  </read_first>
  <behavior>
    - `start(trigger, triggeredByAdminId)`는 `RUNNING` 행 1건을 **즉시 커밋**하고 그 엔티티를 반환한다
      (호출부가 트랜잭션을 열지 않은 상태에서도, 다른 커넥션에서 바로 보인다)
    - 이미 `RUNNING` 행이 있으면 `start`는 `BatchAlreadyRunningException`을 던지고 새 행을 만들지 않는다
      (`batch_execution` 행 수가 증가하지 않는다)
    - `finish(...)` 후에는 같은 `start`가 다시 성공한다
    - `startedAt`이 `staleRunTimeout`보다 오래된 `RUNNING` 행은 `FAILED` + `errorSummary = "STALE"`로
      정리되고 새 `RUNNING` 행이 만들어진다
    - `staleRunTimeout` 이내의 `RUNNING` 행은 정리되지 않아 `start`가 거부된다
    - `finish(...)`는 `finishedAt`·집계 3종·`status`·`errorSummary`를 확정하고 `RUNNING`을 남기지 않는다
  </behavior>
  <action>
    `src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt`를 새로 만든다.
    `@Service`이며 생성자로 `BatchExecutionRepository`, `InactivityBatchProperties`, `Clock`을 받는다.

    `start(trigger: BatchTrigger, triggeredByAdminId: Long?): BatchExecution` —
    `@Transactional(propagation = Propagation.REQUIRES_NEW)`:
    1. `val now = OffsetDateTime.now(clock)`
    2. `markStaleRunningAsFailed(now.minus(properties.staleRunTimeout), now, "STALE")`를 먼저 호출하고,
       정리된 행이 1건 이상이면 `logger.warn`으로 남긴다(운영자가 앱 비정상 종료를 알아채야 한다)
    3. `batchExecutionRepository.saveAndFlush(BatchExecution(trigger = ..., triggeredByAdminId = ...,
       startedAt = now, status = BatchExecutionStatus.RUNNING))`
       — `save`가 아니라 **`saveAndFlush`**여야 유니크 위반이 이 메서드 안에서 드러난다
    4. `DataIntegrityViolationException`을 잡아 `BatchAlreadyRunningException`으로 바꿔 던진다.
       **잡은 뒤 정상 반환하면 안 된다** — 트랜잭션이 이미 rollback-only로 표시돼 커밋 시점에
       `UnexpectedRollbackException`이 난다. 반드시 예외를 던져 이 트랜잭션이 롤백되게 한다.
       이 사실을 KDoc에 남긴다.

    `finish(executionId: Long, status: BatchExecutionStatus, processedMemberCount: Int,
    deductedCount: Int, skippedCount: Int, errorSummary: String?): BatchExecution` —
    `@Transactional(propagation = Propagation.REQUIRES_NEW)`:
    행을 `findById(...).orElseThrow { IllegalStateException("...") }`로 읽어
    `execution.finish(status, OffsetDateTime.now(clock), ...)`를 호출하고(더티 체킹으로 반영)
    **확정된 엔티티를 반환한다** — 05-13의 러너와 05-15의 수동 실행 API가 이 반환값을 그대로
    응답·집계에 쓰므로 재조회를 한 번 더 하지 않게 한다.

    클래스 KDoc에 네 가지를 적는다:
    - **왜 별도 빈인가** — 러너에는 `@Transactional`을 붙일 수 없고(D-112 실패 격리),
      같은 클래스 내부 호출은 프록시를 우회해 트랜잭션 경계가 생기지 않는다
      (`InactivityDeductionService`가 이미 같은 이유로 분리돼 있다, 05-01 결정 C)
    - **왜 `REQUIRES_NEW`인가** — 호출부가 (읽기 전용) 트랜잭션을 열고 있어도 `RUNNING` 행이
      즉시 커밋돼야 다른 요청이 그 행을 보고 409를 받는다. 참여(REQUIRED)로 두면 호출부가 끝날
      때까지 커밋되지 않아 직렬화가 성립하지 않는다
    - **FK 위반과 유니크 위반의 구분** — `triggeredByAdminId`가 존재하지 않는 관리자면 같은
      `DataIntegrityViolationException`이 난다. 그래서 관리자 존재 검증은 이 메서드에 오기 전
      (`InactivityBatchRunner.resolveTriggeredByAdminId`)에서 끝난다는 전제를 명시한다
    - **이 테이블은 여전히 부족분 계산의 근거가 아니다**(D-106) — 중복 실행 거부에만 쓴다

    테스트 `src/test/kotlin/com/goldwrestling/batch/BatchExecutionRecorderTest.kt`를 만든다.
    `@SpringBootTest` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`
    (배치 테스트와 같은 조합 — 컨텍스트 캐시 공유). **클래스 레벨 `@Transactional`을 붙이지 않는다**
    — `REQUIRES_NEW` 커밋 동작을 관찰해야 한다. `@AfterEach`에서 이 클래스가 만든 `batch_execution`
    행을 반드시 지운다(`RUNNING` 행이 남으면 다른 테스트의 배치 실행이 전부 409로 막힌다 —
    이 사실을 KDoc에 남긴다). 시각 이동은 `(clock as MutableTestClock).setTo(...)`로 한다.
    위 `<behavior>` 6가지를 백틱 한국어 테스트로 각각 검증한다. `MANUAL` 트리거 테스트에는
    `ck_batch_execution_trigger` 때문에 실제 `Admin` 행이 필요하다(`BatchFixtures.admin` 사용,
    로그인 아이디 접두어는 이 클래스 전용으로 `admin-batch-recorder-`).

    `docs/glossary.md` 배치 절에 두 용어를 추가한다 — "배치 실행 기록자 `BatchExecutionRecorder`"
    (실행 시작/종료를 러너 본문과 분리된 트랜잭션으로 남기는 컴포넌트)와
    "정체된 실행 `stale run`"(임계 시간을 넘도록 끝나지 않은 `RUNNING` 행. 앱 비정상 종료의 흔적이며
    다음 시작이 `FAILED`로 정리한다).
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew cleanTest test --tests 'com.goldwrestling.batch.BatchExecutionRecorderTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Propagation.REQUIRES_NEW' src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt` 가 2
    - `grep -c 'saveAndFlush' src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt` 가 1
    - `grep -c 'BatchAlreadyRunningException' src/main/kotlin/com/goldwrestling/batch/BatchExecutionRecorder.kt` 가 1 이상
    - `grep -c '@Transactional' src/test/kotlin/com/goldwrestling/batch/BatchExecutionRecorderTest.kt` 가 0
    - `grep -c 'fun \`' src/test/kotlin/com/goldwrestling/batch/BatchExecutionRecorderTest.kt` 가 6 이상
    - `grep -c 'BatchExecutionRecorder' docs/glossary.md` 가 1 이상
    - 위 gradle 명령이 BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>실행 시작이 DB 레벨에서 직렬화되고, 앱이 죽어 남은 실행 중 행이 배치를 영구 차단하지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 HTTP 요청 → 실행 시작 | 인증된 관리자가 반복·동시 실행을 요청할 수 있다 |
| 운영 환경변수 → 배치 정책 값 | 배포 환경의 환경변수가 차감 정책(시행일·상한·stale 임계)을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05D-12-01 | Denial of Service | 반복 실행 요청 | mitigate | `uq_batch_execution_running` 위반 → `BatchAlreadyRunningException` → 409. 요청은 배치 본문을 시작조차 하지 못한다 |
| T-05D-12-02 | Denial of Service | 잔존 `RUNNING`으로 인한 영구 차단 | mitigate | `staleRunTimeout`(기본 30분) 초과분을 `start()`가 `FAILED`(STALE)로 정리한 뒤 새 행을 넣는다. 정리와 삽입이 같은 트랜잭션이라 경쟁 시 진 쪽은 409를 받는다(안전한 결과) |
| T-05D-12-03 | Tampering | `policy-effective-date` 환경변수 | accept | 값을 과거로 바꾸면 소급 차감 범위가 늘어난다. 배포 환경변수는 서버 운영자만 바꿀 수 있고, 실제 차감은 여전히 `max-deductions-per-run`(기본 1)이 하루 1회로 묶는다. `.env.example`에 키만 노출하고 실값은 커밋하지 않는다 |
| T-05D-12-04 | Information disclosure | 409 응답 본문 | mitigate | `DomainException`의 사용자 대면 문구만 나가고 제약조건명(`uq_batch_execution_running`)·SQL은 담지 않는다(conventions §8) |
| T-05D-12-SC | Tampering | 패키지 설치 | n/a | 새 의존성 없음 (npm/pip/cargo 설치 없음) |
</threat_model>

<verification>
- `./gradlew cleanTest test --tests 'com.goldwrestling.batch.*'` 통과
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `grep -rn "새 에러코드는 추가하지 않는다" docs/decisions.md` 결과가 정정 항목과 함께만 나타난다
</verification>

<success_criteria>
- 두 번째 `start()` 호출이 `BatchAlreadyRunningException`을 던지고 `batch_execution` 행이 늘지 않는다
- stale `RUNNING` 정리가 임계 시간 기준으로 동작한다(경계 양쪽 모두 테스트됨)
- `BATCH_ALREADY_RUNNING`이 `ErrorCode`·`docs/error-codes.md`·D-118에 모두 존재한다
- 배치 설정 3종이 환경변수로 오버라이드 가능하고 `.env.example`에 키가 있다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-12-SUMMARY.md` when done.
완료 보고에 `이번에 쓴 기술` 섹션을 넣는다 — 트랜잭션 전파(`REQUIRES_NEW`)가 왜 필요한지,
같은 클래스 내부 호출이 왜 트랜잭션 경계를 만들지 못하는지(프록시 우회)를 이 코드의 상황으로
설명한다. Task 1이 테스트 면제인 이유도 한 줄로 밝힌다.
</output>
