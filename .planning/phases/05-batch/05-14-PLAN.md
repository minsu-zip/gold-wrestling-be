---
phase: 05-batch
plan: 14
type: execute
wave: 14
gap_closure: true
depends_on: [05-13]
files_modified:
  - src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityDueDateCalculatorTest.kt
  - src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityBatchPolicyLimitTest.kt
  - build.gradle.kts
  - docs/policies.md
  - docs/decisions.md
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "정책 시행일 이전의 미사용 기간은 차감 부채로 계산되지 않는다"
    - "한 번의 배치 실행이 회원 1명에게서 차감할 수 있는 횟수는 최대 1회다"
    - "상한으로 잘린 부족분은 다음 실행이 상태 기반으로 이어받는다(캐치업 유지)"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt"
      provides: "기준일에 정책 시행일 하한 적용"
      contains: "policyEffectiveDate"
    - path: "src/test/kotlin/com/goldwrestling/batch/InactivityBatchPolicyLimitTest.kt"
      provides: "소급 차감 방지·1회 상한 실증"
      min_lines: 130
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt"
      to: "InactivityBatchProperties.maxDeductionsPerRun"
      via: "부족분에 1회 실행 상한 적용"
      pattern: "maxDeductionsPerRun"
    - from: "src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt"
      to: "InactivityDueDateCalculator.resolveDueDate"
      via: "정책 시행일을 파라미터로 전달"
      pattern: "resolveDueDate\\([^)]*policyEffectiveDate"
---

<objective>
CR-02를 닫는다 — `expectedDeductionCount`에 상한이 없고 기준일 후보에 하한이 없어, 배포 후 첫
실행이 "배치가 존재하지 않았던 과거 전체 기간"까지 밀린 주기로 계산한다. 200일 전 등록되고 한 번도
쓰이지 않은 `SESSION_PASS`는 한 번의 실행으로 잔여가 0이 될 수 있다.

두 가지를 넣는다(05-GAP-CONTEXT §2.2 확정):
1. **정책 시행일 하한** `2026-09-01` — 기준일 후보의 max가 시행일보다 이르면 시행일로 끌어올린다.
   "배치가 아예 없었던 기간"과 "배치가 며칠 죽었던 기간"을 코드가 구분하게 만드는 유일한 장치다.
2. **1회 실행 상한** 회원 1명당 1회 — 매일 04:00 배치에서 정상 부족분은 0 또는 1이다. 2 이상은
   사고이므로 하루 1회로 묶어 관리자가 킬 스위치를 켤 시간을 만든다. 밀린 주기는 다음 날들이
   이어받는다(D-106 상태 기반 캐치업이 그대로 작동한다).

Purpose: BATCH-01("정책대로 차감된다")이 실패로 판정된 두 경로 중 소급 차감 경로를 닫는다.
(CR-03 출석일 후보 부재는 범위 밖 — ROADMAP Phase 5 Note의 설계 결정이며 D-116 킬 스위치로 대응한다.)
Output: 계산기 시그니처 확장, 러너 상한 배선, 테스트 기본 시행일 고정, 실증 테스트, policies §4.3
명문화, D-119 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@.planning/phases/05-batch/05-GAP-CONTEXT.md
@.claude/skills/add-domain-test/SKILL.md

**확정 설계(재검토 금지):** 상한은 **회원당 1회**, 시행일은 **2026-09-01**(프로퍼티 기본값 +
환경변수 오버라이드). 다른 상한 값(3회 등)이나 다른 하한 방식(배치 도입일 자동 감지 등)을
제안하지 말 것.

**범위 밖:** CR-03(출석일 후보 부재로 저녁반 전용 회원 부당 차감). 운영 배포 시 cron을 꺼 둔 채
올린다(D-116)는 사실을 D-119에 남기는 것으로 갈음한다.

<interfaces>
현재 계산기 계약:
- `InactivityDueDateCalculator.resolveDueDate(candidates: InactivityDueDateCandidates): LocalDate?`
  — 후보 5종 중 non-null의 max, 전부 null이면 null
- `expectedDeductionCount(dueDate, today): Int` — `floor(경과일 / 14)`, 경과일 < 14면 0
- `shortfall(dueDate, today, inactivityEventDates): Int` — 기대치 − 기준일 당일 포함 이후 이력 수,
  하한 0
- 순수 계산이다 — Spring·DB·`Clock`에 의존하지 않고 `today`를 파라미터로 받는다(conventions §5)

05-12가 만든 것:
- `InactivityBatchProperties(policyEffectiveDate: LocalDate = 2026-09-01,
  maxDeductionsPerRun: Int = 1, staleRunTimeout: Duration = 30m)`,
  prefix `goldwrestling.batch.inactivity`

호출부: `InactivityBatchRunner.runStarted()`의 회원 루프 —
`resolveDueDate(candidates) ?: continue` → `shortfall(...)` → 부족분만큼 `deductOnce` 반복

**테스트 시각 상수:** `BatchFixtures.FIXED_TODAY = 2026-08-02`, `FIXED_TIME = 2026-08-02T10:00+09:00`.
기본 시행일(2026-09-01)은 이 시각보다 **미래**라, 하한을 그대로 적용하면 기존 배치 테스트가 전부
"차감 0"이 되어 무의미해진다 — 그래서 테스트 태스크가 시행일 기본값을 과거로 고정한다(Task 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기준일에 정책 시행일 하한을 적용한다 (순수 계산)</name>
  <files>
    src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityDueDateCalculatorTest.kt
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityDueDateCalculatorTest.kt,
    docs/policies.md,
    docs/conventions.md
  </read_first>
  <behavior>
    - 후보 max가 시행일보다 **이르면** 기준일은 시행일이다
    - 후보 max가 시행일보다 **늦으면** 기준일은 후보 max 그대로다
    - 후보 max가 시행일과 **같으면** 그대로다 (경계)
    - 후보가 **전부 null이면 여전히 null**이다 — 시행일로 대체하지 않는다(판정 대상 자체가 아니다)
    - 시행일이 미래면 기준일도 미래가 되어 `expectedDeductionCount`가 0이다
      (배포 전 데이터가 소급 차감되지 않는다)
    - 200일 전 등록 + 시행일이 30일 전 → 기대 차감 수가 14가 아니라 2다
  </behavior>
  <action>
    `resolveDueDate`의 시그니처를
    `resolveDueDate(candidates: InactivityDueDateCandidates, policyEffectiveDate: LocalDate): LocalDate?`
    로 바꾼다. 구현은 기존 `listOfNotNull(...).maxOrNull()` 뒤에 `?.coerceAtLeast(policyEffectiveDate)`를
    붙이는 것이다 — **null이면 null을 유지**해야 하므로 안전 호출 위치가 중요하다.
    시행일을 파라미터로 받는 이유(순수 계산 유지, Spring 의존 금지 — conventions §5)를 KDoc에 적는다.

    KDoc에 하한의 의미를 명확히 쓴다: 이 값은 "배치가 존재하기 시작한 날"이며, 그 이전의 미사용은
    차감 부채로 치지 않는다. 이것이 없으면 D-106의 캐치업("배치가 며칠 밀리면 다음 실행이 몰아서
    차감한다")이 "배치가 아예 없었던 과거"까지 몰아서 차감하는 결과가 된다(CR-02).

    `InactivityDueDateCalculatorTest`의 기존 `resolveDueDate` 테스트 호출부를 새 시그니처로 갱신하고
    (기존 계약은 그대로 유지 — 회귀 방어), 위 `<behavior>` 6가지를 백틱 한국어 테스트로 추가한다.
    이 테스트는 스프링 컨텍스트 없는 순수 단위테스트다(`ReservationPassPolicyTest` 관례).
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew cleanTest test --tests 'com.goldwrestling.batch.InactivityDueDateCalculatorTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'policyEffectiveDate: LocalDate' src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt` 가 1 이상
    - `grep -c 'coerceAtLeast(policyEffectiveDate)' src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt` 가 1
    - `grep -c 'import org.springframework' src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt` 가 0 (순수 계산 유지)
    - `grep -c '시행일' src/test/kotlin/com/goldwrestling/batch/InactivityDueDateCalculatorTest.kt` 가 4 이상
    - 위 gradle 명령이 BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>기준일이 정책 시행일보다 이를 수 없고, 그 경계 동작이 순수 함수 단위테스트로 고정됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 러너에 시행일·1회 상한을 배선하고 소급 차감 부재를 실증한다</name>
  <files>
    src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityBatchPolicyLimitTest.kt,
    build.gradle.kts,
    docs/policies.md,
    docs/decisions.md
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt,
    src/main/kotlin/com/goldwrestling/config/InactivityBatchProperties.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt,
    src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt,
    build.gradle.kts,
    docs/policies.md,
    docs/decisions.md
  </read_first>
  <behavior>
    - 200일 방치된 `SESSION_PASS`(잔여 5.0) 회원이 있어도 한 번의 실행에서 차감은 1회다
    - 하루 뒤 다시 실행하면 또 1회만 차감된다(캐치업이 유지된다)
    - 정책 시행일이 오늘−13일이면 부족분이 0이라 차감이 없다(시행일 이전 기간은 부채가 아니다)
    - 정책 시행일이 오늘−15일이면 정확히 1회 차감된다(경계 반대편)
    - 상한으로 잘린 부족분은 `skippedCount`에 세지 않는다 — `skippedCount`는 대상 소진·경쟁 패배
      전용이다(D-113)
  </behavior>
  <action>
    (a) `InactivityBatchRunner` 생성자에 `InactivityBatchProperties`를 주입하고 회원 루프를 바꾼다:
    - `InactivityDueDateCalculator.resolveDueDate(candidates, properties.policyEffectiveDate)`
    - 부족분 계산 뒤 `val deductionTarget = minOf(shortfallCount, properties.maxDeductionsPerRun)`로
      묶고, 반복 횟수를 `deductionTarget`으로 쓴다.
    - `shortfallCount > deductionTarget`이면 `logger.info`로 "회원 {id}의 부족분 {n} 중 {m}회만
      차감(1회 실행 상한) — 나머지는 다음 실행이 이어받는다"를 남긴다. **`skippedCount`를 올리지
      않는다** — 그 필드는 대상 소진·경쟁 패배 전용이고(D-113), 상한 적용은 정상 예정 동작이다.
    - KDoc에 두 값의 의미와 근거(CR-02, D-119)를 적는다. 특히 "하루 1회로 묶으면 사고가 나도 피해가
      하루 1회씩만 누적돼 관리자가 킬 스위치(D-116)를 켤 시간이 생긴다"는 이유를 남긴다.

    (b) `build.gradle.kts`의 `tasks.withType<Test>` 블록에
    `systemProperty("goldwrestling.batch.inactivity.policy-effective-date", "2000-01-01")`을 추가한다.
    **이유를 반드시 주석으로 남긴다**: 기본 시행일(2026-09-01)은 `BatchFixtures.FIXED_TODAY`
    (2026-08-02)보다 미래라, 고정하지 않으면 모든 배치 테스트의 기준일이 미래로 끌어올려져
    차감이 0이 되고 기존 멱등·캐치업 테스트가 아무것도 검증하지 못하게 된다.
    시행일 자체를 검증하는 테스트는 클래스별로 이 값을 덮어쓴다. 기존
    `goldwrestling.batch.inactivity-scheduler-enabled` 시스템 프로퍼티 줄은 그대로 둔다(D-116).

    (c) `src/test/kotlin/com/goldwrestling/batch/InactivityBatchPolicyLimitTest.kt`를 만든다.
    `@SpringBootTest(properties = ["goldwrestling.batch.inactivity.policy-effective-date=..."])`로
    클래스별 시행일을 지정하고(경계 검증용으로 두 클래스 또는
    `@Nested` + 별도 `@SpringBootTest` 조합 대신 **테스트별 상대 날짜 대신 클래스 상수**를 쓴다),
    나머지 애노테이션 조합은 다른 배치 통합테스트와 동일하게 맞춘다
    (`@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`, 클래스 레벨
    `@Transactional` 금지, `@AfterEach` 정리). 전용 픽스처 대역:
    `KAKAO_ID_BASE = 9_750_000_000L`, `KAKAO_ID_MAX = KAKAO_ID_BASE + 10_000L`,
    `ADMIN_LOGIN_PREFIX = "admin-inactivity-policy-limit-"`. 정리 쿼리는 범위로 좁힌다(WR-07).
    `batch_execution` 행도 반드시 정리한다.
    시행일 경계 두 케이스(오늘−13일 / 오늘−15일)는 `@SpringBootTest(properties = ...)`가 클래스
    단위라, 고정 날짜 프로퍼티를 쓰고 **clock을 그 날짜 기준으로 이동**시켜 두 경우를 만든다
    (`MutableTestClock.setTo`) — 프로퍼티를 테스트마다 바꾸려 하지 말 것.

    (d) `docs/policies.md` §4.3에 두 규칙을 명문화한다(도메인 규칙이므로 `.planning/`이 아니라
    policies에 쓴다):
    - "**정책 시행일 이전의 미사용은 차감하지 않는다** — 기준일이 정책 시행일보다 이르면 시행일을
      기준일로 본다. 시행일은 설정값이며 기본 `2026-09-01`이다."
    - "**한 번의 배치 실행에서 회원 1명당 최대 1회만 차감한다** — 밀린 주기는 다음 실행들이
      이어받는다(캐치업 유지)."
    두 문장은 스펙 추가이므로 SUMMARY 완료 보고에 "policies.md §4.3에 규칙 2개를 추가했다"는
    사실을 밝힌다(CLAUDE.md 문서 우선순위).

    (e) `docs/decisions.md`에 **D-119**를 추가한다 — 제목 "미사용 차감에 정책 시행일 하한과 1회 실행
    상한을 둔다". 결정(값·설정 키·기본값), 이유(D-106 캐치업은 "배치가 이미 돌고 있었다"는 전제
    위의 설계라 도입 이전 기간과 중단 기간을 구분하지 못한다 / 상한은 사고 피해를 하루 1회로 묶어
    킬 스위치를 켤 시간을 만든다), 기각 대안(상한 3회 — 첫 실행 피해가 여전히 크다 / 배치 도입일
    자동 감지 — `batch_execution` 최초 행에 의존하면 이력을 지우는 순간 소급 차감이 되살아난다 /
    하한 없이 kill switch만 — 켜는 순간 같은 사고가 난다).
    같은 항목에 **CR-03이 범위 밖**이라는 사실과 그 운영 대응(출석 후보가 생기는 Phase 6 전까지는
    `BATCH_INACTIVITY_SCHEDULER_ENABLED=false`로 cron을 꺼 둔 채 배포한다)을 함께 적는다.
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew cleanTest test --tests 'com.goldwrestling.batch.InactivityBatchPolicyLimitTest' --tests 'com.goldwrestling.batch.InactivityBatchIdempotencyTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'maxDeductionsPerRun' src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt` 가 1 이상
    - `grep -c 'policyEffectiveDate' src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt` 가 1 이상
    - `grep -c 'policy-effective-date' build.gradle.kts` 가 1
    - `grep -c 'inactivity-scheduler-enabled' build.gradle.kts` 가 1 (D-116 고정이 살아 있다)
    - `grep -v '^#' docs/policies.md | grep -c '정책 시행일'` 가 1 이상
    - `grep -c '^## D-119' docs/decisions.md` 가 1
    - `awk '/^## D-119/,/^## D-120/' docs/decisions.md | grep -c 'CR-03'` 가 1 이상
    - 위 gradle 명령이 BUILD SUCCESSFUL이고 `InactivityBatchIdempotencyTest`의 캐치업 테스트
      (`배치가 6주 밀리면 다음 실행이 밀린 3회를 몰아서 차감한다`)가 새 상한과 모순되지 않게
      갱신됐거나 여전히 통과한다
  </acceptance_criteria>
  <done>배포 첫 실행이 과거 전체를 소급 차감할 수 없고, 한 실행이 회원 1명에게서 1회를 넘게 깎지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 환경변수 → 차감 정책 값 | `policy-effective-date`·`max-deductions-per-run`이 회원 잔여 차감량을 직접 결정한다 |
| 배치 → 회원 잔여 | 사람 개입 없이 잔여를 깎는 유일한 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05D-14-01 | Denial of Service | 배포 첫 실행의 대량 소진 | mitigate | 시행일 하한 + 1회 실행 상한. 최악의 경우에도 하루 1회씩만 누적되며 그 사이 D-116 킬 스위치로 멈출 수 있다 |
| T-05D-14-02 | Tampering | 시행일 환경변수 조작 | accept | 과거로 바꾸면 소급 범위가 늘어난다. 서버 운영자만 접근 가능하고, 상한(1회/실행)이 피해 속도를 제한한다. `.env.example`에는 키 이름만 둔다 |
| T-05D-14-03 | Repudiation | 상한으로 잘린 부족분의 무기록 | accept | `skippedCount`를 오염시키지 않기 위해 로그로만 남긴다. 원장(`PassTransaction`)과 기준일로 언제든 재계산 가능하므로 정보가 사라지지 않는다 |
| T-05D-14-SC | Tampering | 패키지 설치 | n/a | 새 의존성 없음 (npm/pip/cargo 설치 없음) |
</threat_model>

<verification>
- `./gradlew cleanTest test --tests 'com.goldwrestling.batch.*'` 전체 통과 (특히 멱등·캐치업 기존 테스트)
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `grep -rn "상한 없이" docs/decisions.md` 결과가 D-119 이전 서술과 모순되지 않는다
</verification>

<success_criteria>
- 200일 방치 회원이 한 실행에서 1회만 차감된다
- 시행일 이전 기간이 부족분에 포함되지 않는다(경계 양쪽 테스트됨)
- policies §4.3에 두 규칙이 명문화되고 D-119가 기록됐다
- 기존 멱등·캐치업 테스트가 새 상한과 일관되게 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-14-SUMMARY.md` when done.
`이번에 쓴 기술` 섹션에 (1) 외부 설정(@ConfigurationProperties)으로 도메인 정책 값을 빼는 것이
왜 상수보다 안전한지, (2) 순수 함수가 시각·설정을 파라미터로 받아야 하는 이유를 설명한다.
`docs/policies.md`를 고쳤다는 사실도 함께 보고한다.
</output>
