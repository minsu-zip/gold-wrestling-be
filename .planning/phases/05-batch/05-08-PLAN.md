---
phase: 05-batch
plan: 08
type: execute
wave: 8
depends_on: ["05-07"]
files_modified:
  - src/main/kotlin/com/goldwrestling/config/SchedulingConfig.kt
  - src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt
  - src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt
  - src/main/kotlin/com/goldwrestling/batch/dto/BatchExecutionResponse.kt
  - src/test/kotlin/com/goldwrestling/batch/AdminBatchControllerTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [BATCH-01, BATCH-04]

must_haves:
  truths:
    - "애플리케이션이 뜨면 매일 새벽 4시(Asia/Seoul)에 미사용 차감 배치가 자동 실행된다"
    - "관리자가 POST /api/admin/batch/inactivity-runs로 배치를 즉시 실행하고 결과 요약을 받는다"
    - "회원 토큰·무인증 요청은 이 엔드포인트에 접근할 수 없다"
    - "수동 실행 기록에는 실행한 관리자가 남는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt"
      provides: "cron 트리거 (로직 없음)"
      contains: "@Scheduled"
    - path: "src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt"
      provides: "관리자 수동 실행 API"
      contains: "/batch/inactivity-runs"
    - path: "docs/api/openapi.yaml"
      provides: "FE 계약 갱신"
      contains: "inactivity-runs"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt"
      to: "InactivityBatchRunner.run(SCHEDULED, null)"
      via: "cron 트리거가 러너를 호출만 한다"
      pattern: "runner.run"
    - from: "src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt"
      to: "InactivityBatchRunner.run(MANUAL, adminId)"
      via: "수동 실행 경로가 같은 러너를 재사용"
      pattern: "BatchTrigger.MANUAL"
---

<objective>
배치를 **실제로 굴린다** — 앱 내 `@Scheduled` cron 트리거와 관리자 수동 실행 API를 붙이고
`openapi.yaml`을 재생성한다.

Purpose: 05-06까지는 "호출하면 동작하는 배치"였고 이 플랜이 그것을 **매일 새벽 자동 실행 + 사람이
필요할 때 즉시 실행**으로 바꾼다(D-108). 스케줄링 인프라는 이 저장소 최초 도입이라 `verify-boot4-api`
절차로 API를 확인하고 넘어간다. 두 트리거가 **같은 러너**를 호출해야 중복 구현이 생기지 않고, D-106
상태 기반 설계 덕에 수동 실행이 중복 차감을 만들지 않는다(05-07이 실증 완료).

Output: `config/SchedulingConfig.kt`, `batch/InactivityBatchScheduler.kt`, `batch/AdminBatchController.kt`,
`batch/dto/BatchExecutionResponse.kt`, 컨트롤러 통합테스트, 재생성된 `docs/api/openapi.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@docs/error-codes.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/verify-boot4-api/SKILL.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-RESEARCH.md
@.planning/phases/05-batch/05-PATTERNS.md
@.planning/phases/05-batch/05-06-SUMMARY.md
</context>

<interfaces>
`InactivityBatchRunner.run(trigger: BatchTrigger, triggeredByAdminId: Long?): BatchExecution` (05-06)
`BatchExecution` 필드: `id`, `trigger`, `triggeredBy: Admin?`, `startedAt`, `finishedAt`,
`processedMemberCount`, `deductedCount`, `skippedCount`, `status`, `errorSummary` (05-02)

`SEOUL_ZONE_ID` 상수는 `com.goldwrestling.SEOUL_ZONE_ID`(`GoldWrestlingApplication.kt` 최상위)에 이미 있다 —
새로 만들지 말고 import한다.

컨트롤러 관례(`schedule/AdminScheduleController.kt`):
- `@RestController` + `@RequestMapping("/api/admin")` + `@Tag(name = ..., description = ...)`
- 인가는 URL 규칙(`SecurityConfig`의 `/api/admin/**` → `hasRole("ADMIN")`)이 담당한다 —
  **컨트롤러에 권한 애노테이션·`@Transactional`·`try-catch`를 붙이지 않는다**(D-040, D-017)
- 인증 주체는 `@AuthenticationPrincipal principal: AuthenticatedPrincipal` + `principal.requireAdminId()`
- 메서드에 `@Operation(summary = ..., description = ...)`

DTO 관례(`pass/dto/PassResponse.kt`): `data class` + `@Schema(description = ...)` + `companion object { fun from(...) }`.
변환은 **트랜잭션 안 또는 LAZY 연관을 건드리지 않는 범위**에서만 — `BatchExecution.triggeredBy`는
LAZY `@ManyToOne`이라 컨트롤러에서 접근하면 `LazyInitializationException` 위험이 있다.
응답에는 `triggeredByAdminId`(연관 객체가 아니라 id)만 담아 이 문제를 원천 차단한다.

Boot 4 확인 대상(추측 금지, CLAUDE.md 규칙 9): `@EnableScheduling`·`@Scheduled`의 패키지 경로와
`zone` 속성, cron 표현식 필드 수(6필드: 초 분 시 일 월 요일).

기존 관리자 컨트롤러 테스트 골격: `schedule/AdminScheduleControllerTest.kt`
(`@SpringBootTest` + `@AutoConfigureMockMvc` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)` +
`TokenService`로 발급한 실제 토큰을 `HttpHeaders.AUTHORIZATION`에 실어 호출).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 스케줄링 인프라 + cron 트리거</name>
  <files>src/main/kotlin/com/goldwrestling/config/SchedulingConfig.kt, src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt</files>
  <read_first>
    - .claude/skills/verify-boot4-api/SKILL.md (①context7 ②maven-metadata ③compileKotlin)
    - src/main/kotlin/com/goldwrestling/config/ClockConfig.kt (단일 책임 @Configuration 관례)
    - src/main/kotlin/com/goldwrestling/GoldWrestlingApplication.kt (SEOUL_ZONE_ID, TimeZone 고정)
    - src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt (05-06 — run 시그니처)
    - .planning/phases/05-batch/05-RESEARCH.md Pattern 1 (Context7로 확인된 zone 속성 예제)
    - docs/decisions.md (05-01이 기록한 결정 E — cron 시각)
  </read_first>
  <action>
**(1) Boot 4 확인** — `verify-boot4-api` 절차로 `@EnableScheduling`/`@Scheduled`의 import 경로와
`zone` 속성 존재를 확인한다(context7 → 필요 시 maven-metadata → `./gradlew compileKotlin`).
확인 결과를 SUMMARY에 한 줄로 남긴다. 새 의존성은 추가하지 않는다 — 스케줄링은 이미 클래스패스에 있는
`spring-context` 기능이다.

**(2) `config/SchedulingConfig.kt`** — `@Configuration @EnableScheduling class SchedulingConfig`
(빈 없음). KDoc에 "이 저장소 최초의 스케줄링 도입 지점(D-108). 기본 스케줄러 풀 크기는 1이고 배치가
하루 1회·수 분 이내이므로 조정하지 않는다 — 스케줄 작업이 늘어나면 `spring.task.scheduling.pool.size`를
검토한다"를 남긴다.

**(3) `batch/InactivityBatchScheduler.kt`** — `@Component`,
`@Scheduled(cron = "0 0 4 * * *", zone = SEOUL_ZONE_ID)`가 붙은 `fun runDaily()` **하나만** 둔다.
본문은 `runner.run(trigger = BatchTrigger.SCHEDULED, triggeredByAdminId = null)` 호출뿐 —
조건문·쿼리·트랜잭션·예외 처리를 넣지 않는다(RESEARCH Pattern 1: 스케줄러는 트리거만).
KDoc에 "cron 6필드(초 분 시 일 월 요일). JVM 기본 시간대가 이미 `Asia/Seoul`이지만(main) `zone`을
명시하는 것이 conventions §5('시간대는 Asia/Seoul 명시')와 일치한다. **분산 락(ShedLock)을 쓰지
않는다** — 단일 EC2 인스턴스 전제이고, 중복 실행은 D-106 상태 기반 계산이 이미 안전하게 만든다(D-108)"를 남긴다.

**테스트 면제**: 두 파일 모두 conventions §10.0의 면제 항목(`config/` 설정 클래스, 위임만 하는 트리거)이다 —
스케줄 발화 자체는 프레임워크 기능이라 테스트로 검증하지 않는다. **이 면제 사유를 SUMMARY에 한 줄로 밝힌다**
(§10.5). 트리거가 호출하는 로직은 05-06 러너 테스트가 이미 덮는다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin && grep -c "@Scheduled(cron = \"0 0 4 \* \* \*\", zone = SEOUL_ZONE_ID)" src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew compileKotlin` 종료 코드 0
- `InactivityBatchScheduler.kt`에 `@Scheduled` 메서드가 정확히 1개이고 cron이 `0 0 4 * * *`, `zone`이 `SEOUL_ZONE_ID`다
- 스케줄러 메서드 본문이 `runner.run(...)` 호출 1줄이다 (`if`·`try`·리포지토리 호출 없음)
- `grep -c "const val SEOUL_ZONE_ID" src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt`가 0
  (상수를 재선언하지 않고 import한다)
- `git diff build.gradle.kts`가 변경 없음 (새 의존성 없음)
- `./gradlew build` 통과 — `@EnableScheduling`이 켜진 상태로 기존 테스트 컨텍스트가 정상 기동한다
- SUMMARY에 Boot 4 확인 결과와 테스트 면제 사유가 적혀 있다
  </acceptance_criteria>
  <done>앱이 뜨면 매일 새벽 4시에 배치가 자동으로 돈다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 관리자 수동 실행 API + 응답 DTO</name>
  <files>src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt, src/main/kotlin/com/goldwrestling/batch/dto/BatchExecutionResponse.kt, src/test/kotlin/com/goldwrestling/batch/AdminBatchControllerTest.kt</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md (0~7 전 절차)
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt (전체 — 트리거형 POST 관례·KDoc·@Tag)
    - src/main/kotlin/com/goldwrestling/pass/dto/PassResponse.kt (응답 DTO + from 관례)
    - src/test/kotlin/com/goldwrestling/schedule/AdminScheduleControllerTest.kt (토큰 발급·MockMvc 호출·인가 단언 방식)
    - src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt (`/api/admin/**` 규칙 — 수정하지 않는다)
    - src/main/kotlin/com/goldwrestling/batch/BatchExecution.kt (05-02 — 필드)
  </read_first>
  <behavior>
    - 관리자 토큰으로 `POST /api/admin/batch/inactivity-runs` 호출 → 200, 본문에
      `batchExecutionId`·`trigger("MANUAL")`·`triggeredByAdminId`·`startedAt`·`finishedAt`·
      `processedMemberCount`·`deductedCount`·`skippedCount`·`status` 필드가 있다
    - 차감 대상이 있는 상태에서 호출하면 실제로 잔여가 줄고 `deductedCount`가 1 이상이다
    - **같은 요청을 연속 2회** 보내면 두 번째 응답의 `deductedCount`가 0이다 (D-106 — 중복 실행 안전)
    - 회원 토큰으로 호출 → 403 이고 응답이 `application/problem+json`, `code`가 `ACCESS_DENIED`
    - 토큰 없이 호출 → 401 이고 `code`가 `UNAUTHENTICATED`
    - 응답 본문에 회원 개인정보(이름·전화번호)나 회원 id 목록이 포함되지 않는다 (집계 수치만)
  </behavior>
  <action>
**(1) `batch/dto/BatchExecutionResponse.kt`** — `data class`, 필드:
`batchExecutionId: Long`, `trigger: BatchTrigger`, `triggeredByAdminId: Long?`,
`startedAt: OffsetDateTime`, `finishedAt: OffsetDateTime`, `processedMemberCount: Int`,
`deductedCount: Int`, `skippedCount: Int`, `status: BatchExecutionStatus`, `errorSummary: String?`.
`@Schema(description = ...)`를 클래스와 주요 필드에 붙인다(FE가 이 스펙만 본다).
`companion object { fun from(execution: BatchExecution): BatchExecutionResponse }` —
`triggeredBy`(LAZY `@ManyToOne`) 대신 **id만** 꺼내고, KDoc에 "LAZY 연관 객체를 응답에 담지 않는다 —
컨트롤러(트랜잭션 밖)에서 접근하면 `LazyInitializationException`이 난다"를 남긴다.
`triggeredBy` id 접근이 프록시 초기화를 유발하지 않도록 `BatchExecution`이 이미 갖고 있는 방식
(연관의 `id`만 읽기)을 쓰고, 그래도 프록시가 초기화되면 러너가 반환하는 값에서 id를 함께 넘기는 방식으로
바꾼다 — 실제로 실행해 확인한다(추측하지 않는다).

**(2) `batch/AdminBatchController.kt`** — `@RestController @RequestMapping("/api/admin")`,
`@Tag(name = "admin-batch", description = "관리자 배치 수동 실행")`.
`@PostMapping("/batch/inactivity-runs")` 메서드 1개: 요청 본문 없음,
`@AuthenticationPrincipal principal: AuthenticatedPrincipal`를 받아
`runner.run(BatchTrigger.MANUAL, principal.requireAdminId())`를 호출하고 `BatchExecutionResponse.from(...)`을 반환한다.
`@Operation(summary = "2주 미사용 자동 차감 배치를 즉시 실행한다", description = ...)`의 description에
"중복 실행해도 안전하다 — 상태 기반 부족분 계산(D-106)이 이미 존재하는 차감을 다시 만들지 않는다.
배치가 며칠 중단됐다면 이 호출 한 번이 밀린 주기를 몰아서 보정한다"를 적는다.
경로가 복수형 `inactivity-runs`인 이유(실행 이력 리소스 생성)를 클래스 KDoc에 한 줄로 남긴다.
**SecurityConfig·ErrorCode·error-codes.md를 수정하지 않는다** — 인가는 기존 `/api/admin/**` 규칙을
상속하고 도메인 거부 경로가 없다(05-01 결정 E). 새 코드가 정말 필요해지면 그때 `ErrorCode`와
`docs/error-codes.md`를 같은 커밋에서 함께 갱신한다.

> **[필수 확인 — PR #14 리뷰 Info 이월]** 이 컨트롤러를 붙이는 순간
> `InactivityBatchRunner.resolveTriggeredBy`의 두 예외가 처음으로 HTTP 경로에 노출된다:
> `requireNotNull(triggeredByAdminId)` → `IllegalArgumentException`,
> 관리자 조회 실패 → `IllegalStateException`. 둘 다 에러코드가 없어 전역 핸들러가
> `ProblemDetail`로 매핑할 근거가 없고 스프링 기본 500이 나갈 수 있다(D-017 위반).
> **`principal.requireAdminId()`가 두 분기를 정말로 도달 불가로 만드는지 코드로 확인하고**,
> - 도달 불가면: 그 근거를 `resolveTriggeredBy` KDoc에 한 줄로 남긴다(다음 사람이 다시 의심하지 않게)
> - 도달 가능하면(예: 삭제된 관리자의 유효 토큰): 도메인 예외 + `ErrorCode` + `docs/error-codes.md`를
>   같은 커밋에서 함께 추가한다. 이때는 위의 "ErrorCode를 수정하지 않는다"가 해제된다
>
> 판정 근거가 되는 테스트는 이미 있다 — `InactivityBatchFailureIsolationTest`의
> `MANUAL 트리거인데 관리자 id가 없으면…` / `…관리자 id가 존재하지 않으면…` 2종.

**(3) `AdminBatchControllerTest`** — `<behavior>` 6종을 `AdminScheduleControllerTest`와 **동일한
애노테이션 조합**으로 작성한다(컨텍스트 캐시 재사용). 차감이 실제로 일어나는 케이스는
`BatchFixtures`로 기준일 14일 전 회원을 만들어 검증하고, 그 케이스는 클래스 레벨 `@Transactional`이
없어야 커밋이 보인다 — 인가 케이스(401/403)와 차감 케이스가 한 클래스에서 충돌하면 차감 케이스를
`@AfterEach` 정리 방식으로 통일한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.batch.AdminBatchControllerTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.batch.AdminBatchControllerTest"` 종료 코드 0이고 6개 이상 테스트가 실행된다
- 401·403 케이스가 각각 `application/problem+json`과 `code` 값(`UNAUTHENTICATED`/`ACCESS_DENIED`)을 단언한다
- "연속 2회 호출 시 두 번째 `deductedCount`가 0" 테스트가 있다
- `grep -c "@Transactional\|try {\|PreAuthorize" src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt`가 0
- `git diff src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt`가 변경 없음
- 응답 DTO에 `Admin` 엔티티 타입 필드가 없다 (`triggeredByAdminId: Long?`만)
- `./gradlew ktlintFormat` 후 `./gradlew build` 통과
  </acceptance_criteria>
  <done>관리자가 배치를 즉시 실행하고 결과 수치를 확인할 수 있다</done>
</task>

<task type="auto">
  <name>Task 3: openapi.yaml 재생성</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md §6 (재생성 절차와 확인 항목)
    - docs/conventions.md §6 (API 계약 규약)
    - docs/api/openapi.yaml (현재 `/api/admin/**` 경로들이 어떻게 표현돼 있는지 — 형태 비교용)
  </read_first>
  <action>
`docker compose up -d`로 Postgres를 띄우고 `./gradlew generateApiDocs`를 실행해 `docs/api/openapi.yaml`을
재생성한다(앱 기동 포함이라 최대 1분가량 걸린다 — 정상).

`git diff docs/api/openapi.yaml`으로 **의도한 변경만** 들어갔는지 확인한다:
- `/api/admin/batch/inactivity-runs` POST 경로 1개 추가
- `BatchExecutionResponse` 스키마 추가(+ `BatchTrigger`·`BatchExecutionStatus` enum)
- `servers:`가 `/`로 유지된다(환경 정보가 박히면 안 된다)
- 기존 경로·스키마의 무관한 변경이 없다 — 있으면 원인을 확인하고 SUMMARY에 기록한다

`docs/api/openapi.yaml`은 FE가 타입을 생성하는 원천이므로, 이 커밋에 코드와 함께 포함한다(규칙 4).
  </action>
  <verify>
    <automated>grep -c "inactivity-runs" docs/api/openapi.yaml</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "/api/admin/batch/inactivity-runs" docs/api/openapi.yaml`이 1 이상
- `grep -c "BatchExecutionResponse" docs/api/openapi.yaml`이 1 이상
- `grep -n "servers:" -A2 docs/api/openapi.yaml`의 url이 `/`다
- `git diff --stat docs/api/openapi.yaml`의 변경이 이번 엔드포인트·스키마 범위로 한정된다 (무관한
  대량 변경이 있으면 SUMMARY에 원인이 기록돼 있다)
  </acceptance_criteria>
  <done>FE가 새 관리자 API를 계약 파일로 확인할 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `/api/admin/batch/inactivity-runs` | 이 엔드포인트 호출 하나가 전 회원 원장을 건드린다 |
| cron 트리거 → 러너 | 사람의 개입 없이 매일 실행된다 — 잘못된 cron/시간대는 조용히 어긋난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Elevation of Privilege | 회원·비인증 사용자가 배치를 실행 | mitigate | 기존 `/api/admin/**` → `hasRole("ADMIN")` 규칙 상속(D-040). 401·403 케이스를 컨트롤러 테스트로 고정 |
| T-05-28 | Repudiation | 누가 수동 실행했는지 추적 불가 | mitigate | `principal.requireAdminId()`를 `batch_execution.triggered_by_admin_id`에 기록(V9 CHECK가 MANUAL의 non-null을 강제) |
| T-05-29 | Denial of Service | 관리자가 수동 실행을 반복 호출해 부하 유발 | accept | 부족분이 0이면 조회만 하고 끝나는 저비용 호출이고 관리자 전용이다. rate limit은 이번 범위 밖(RESEARCH Security Domain) |
| T-05-30 | Tampering | cron 시간대가 UTC로 해석돼 엉뚱한 시각에 실행 | mitigate | `zone = SEOUL_ZONE_ID` 명시 + JVM 기본 시간대 고정(main). acceptance가 cron 문자열·zone을 검사 |
| T-05-31 | Information Disclosure | 배치 응답에 회원 개인정보가 노출 | mitigate | 응답은 집계 수치와 관리자 id만 — 회원 id 목록·이름을 담지 않는다(테스트로 단언) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `docker compose up -d && ./gradlew generateApiDocs` 성공, `git diff docs/api/openapi.yaml`이 의도 범위
- 기존 관리자 컨트롤러 테스트(`AdminScheduleControllerTest`·`AdminPassControllerTest`) 그린
  (`@EnableScheduling` 도입이 컨텍스트에 회귀를 일으키지 않았음을 확인)
</verification>

<success_criteria>
- 배치가 자동(매일 새벽)·수동(관리자 API) 두 경로로 실행되고 둘 다 같은 러너를 쓴다
- 중복 실행이 안전하다는 것이 API 레벨 테스트로도 증명된다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-08-SUMMARY.md` when done.
청크 B(`feature/phase-05b-inactivity-batch`) 진행 중 — 다음 플랜(05-09)이 청크 B의 마지막이다.
</output>
