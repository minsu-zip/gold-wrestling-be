---
phase: 06-operations
plan: 05
type: tdd
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceExceptions.kt
  - src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt
  - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt
  - src/test/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicyTest.kt
  - src/test/kotlin/com/goldwrestling/pass/PassRepositoryEveningMembershipTest.kt
autonomous: true
requirements: [ATTEND-01, ATTEND-02]
must_haves:
  truths:
    - "수업날 기준 유효한 저녁반 회비를 보유한 회원은 저녁반 참여로 차감되지 않는다"
    - "회비가 없으면 만료가 가장 임박한 SESSION_PASS 한 장에서 0.5회가 차감 대상으로 선택된다"
    - "회비도 없고 잔여 0.5 미만이면 EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE(409)로 거부된다"
    - "유효기간 판정 기준일은 오늘이 아니라 수업날이다 — 소급 입력에서 그날 유효했는지로 판정한다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt"
      provides: "저녁반 0.5회 차감 판정(순수)"
      contains: "HALF_SESSION"
    - path: "src/main/kotlin/com/goldwrestling/pass/PassRepository.kt"
      provides: "수업날 기준 유효 회비 존재 조회"
      exports: ["existsActiveEveningMembership"]
    - path: "src/main/kotlin/com/goldwrestling/attendance/AttendanceExceptions.kt"
      provides: "출석 도메인 예외 5종"
      contains: "EveningAttendanceDeductionUnavailableException"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt"
      to: "reservation.ReservationPassPolicy"
      via: "차감 후보 선택 재사용(D-091, 재구현 금지)"
      pattern: "ReservationPassPolicy"
---

<objective>
저녁반 0.5회 차감(ATTEND-02)의 **판정부**를 TDD로 만든다 — 회비 우선, 만료 임박순 한 장,
수업날 기준 유효기간, 불가 시 거부. 저장·차감 실행은 06-07이 붙인다.

목적: 이 판정은 입력→출력이 완전히 명확한 도메인 로직이라 CLAUDE.md("입력→출력이 명확한 도메인
로직은 TDD 플랜")의 정확한 대상이다. 실행부와 섞으면 회비 우선/만료 임박순/수업날 기준 세 규칙이
Testcontainers 통합테스트 안에서만 검증돼 회귀 신호가 느려진다.

RED→GREEN→REFACTOR: 각 태스크는 실패하는 테스트를 먼저 커밋(`test(06-05): ...`)하고,
최소 구현으로 통과시킨 뒤(`feat(06-05): ...`) 정리한다.

산출물: 도메인 예외 5종, 판정 object 1, 리포지토리 쿼리 1, 단위테스트 1 + 통합테스트 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@.claude/skills/add-domain-test/SKILL.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AttendanceExceptions 5종을 DomainException 관례로 만든다</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/AttendanceExceptions.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt (404·409 예외 관례, `@Suppress("UNUSED_PARAMETER")`, 메시지에 id 미보간)
    - src/main/kotlin/com/goldwrestling/common/error/DomainException.kt (기반 클래스 시그니처)
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (06-01이 추가한 출석 코드 5종)
  </read_first>
  <action>
`attendance/AttendanceExceptions.kt` 한 파일에 5개 예외를 선언한다(`PassExceptions.kt`처럼 파일 하나에 모은다):
- `AttendanceNotFoundException(attendanceId: Long?)` → `ErrorCode.ATTENDANCE_NOT_FOUND`, 메시지 "출석 기록을 찾을 수 없습니다."
- `AttendanceMemberNotReservedException` → `ErrorCode.ATTENDANCE_MEMBER_NOT_RESERVED`, "해당 수업의 예약자가 아닙니다."
- `AttendanceClassTypeMismatchException` → `ErrorCode.ATTENDANCE_CLASS_TYPE_MISMATCH`, "이 수업 종류에는 사용할 수 없는 출석 처리입니다."
- `DuplicateAttendanceException` → `ErrorCode.DUPLICATE_ATTENDANCE`, "이미 출석이 기록된 회원입니다."
- `EveningAttendanceDeductionUnavailableException` → `ErrorCode.EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`,
  "유효한 저녁반 회비가 없고 잔여 횟수도 0.5회 미만이라 저녁반 출석을 기록할 수 없습니다."

모든 메시지에 회원 id·이름·이용권 id를 보간하지 않는다(정보 노출 방지, conventions §8).
`EveningAttendanceDeductionUnavailableException`의 KDoc에 D-133(왜 `InsufficientPassCountException`을
재사용하지 않는지)을 한 줄로 인용한다.

이 태스크 자체는 테스트를 만들지 않는다 — 예외 클래스는 분기 없는 선언이고, 각 예외가 실제로
던져지는 경로의 테스트는 Task 2·3과 06-06~06-08이 담당한다(conventions §10.0 면제 판단). 이 근거를
SUMMARY에 한 줄로 남긴다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -c "DomainException" src/main/kotlin/com/goldwrestling/attendance/AttendanceExceptions.kt</automated>
  </verify>
  <acceptance_criteria>
    - `AttendanceExceptions.kt`에 5개 클래스가 모두 존재하고 각각 `DomainException`을 상속한다
    - 파일 안에 `$` 문자열 템플릿을 통한 id 보간이 없다 (`grep -c '\$' 결과가 0`)
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>출석 경로의 모든 실패가 ProblemDetail로 변환 가능한 도메인 예외로 표현된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [TDD] EveningHalfDeductionPolicy — 회비 우선·만료 임박순·거부 판정</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt, src/test/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicyTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/ReservationPassPolicy.kt (자매 object 전문 — `DEDUCTION_AMOUNT` 상수, `selectCandidate`가 비면 예외를 던지는 계약, 패키지 위치 결정 KDoc)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt 90-200행 (`HALF_SESSION` 사용례, `compareTo` 비교 관례 — `equals` 금지)
    - docs/policies.md §4.2 (06-01이 보강한 저녁반 규칙)
    - docs/decisions.md D-091 (만료 임박순·합산 금지·수업날 기준)
    - src/test/kotlin/com/goldwrestling/reservation/PassDeductionCandidateTest.kt 또는 동등한 기존 정책 단위테스트 (없으면 add-domain-test 스킬 §1 골격을 따른다)
  </read_first>
  <behavior>
    - `HALF_SESSION` 상수가 `BigDecimal("0.5")`이다 (Double 생성자 금지)
    - `resolveDeduction(hasValidMembership = true, candidates = 무엇이든)` → 차감 없음을 뜻하는 결과를 반환한다
    - `resolveDeduction(hasValidMembership = false, candidates = 빈 리스트)` → `EveningAttendanceDeductionUnavailableException`
    - `resolveDeduction(hasValidMembership = false, candidates = [만료 늦은 장, 만료 임박한 장])` → 만료 임박한 장을 선택한다(리스트 정렬은 쿼리가 보장하므로 첫 원소를 취한다)
    - 잔여가 정확히 0.5인 장도 후보가 될 수 있다(0.5 이상이면 가능 — policies §4.2 "잔여 0.5회면 저녁반 0.5회 참여만 가능")
  </behavior>
  <action>
RED 먼저: `EveningHalfDeductionPolicyTest`를 스프링 없이 순수 Kotlin 단위테스트로 작성한다.
테스트 메서드명은 policies §4.2 문장을 그대로 쓴 백틱 한국어 서술문으로 한다
(예: `` `유효한 저녁반 회비가 있으면 차감하지 않는다` ``).
`Pass` 인스턴스는 테스트 내 헬퍼 함수로 만들되 `remainingCount`/`endDate`만 의미를 갖게 한다.

GREEN: `attendance/EveningHalfDeductionPolicy.kt`를 `object`로 만든다. **`attendance` 패키지에 둔다** —
`ReservationPassPolicy`가 `reservation`에 있는 것과 같은 이유로, `schedule`이나 `pass`가 `attendance`를
참조하지 않게 의존 방향을 attendance→pass 한 갈래로 유지한다(D-018). 이 근거를 클래스 KDoc에 적는다.

구성:
- `val HALF_SESSION: BigDecimal = BigDecimal("0.5")` (문자열 생성자 필수, D-016)
- `fun requireEveningSession(classType: ClassType)` — `ClassType.EVENING`이 아니면
  `AttendanceClassTypeMismatchException`을 던진다
- `fun selectCandidate(candidates: List<Pass>): Pass` — **정렬을 다시 하지 않는다.**
  `candidates.firstOrNull()`을 취하고 비어 있으면 `EveningAttendanceDeductionUnavailableException`을 던진다.
  KDoc에 "정렬(만료 임박순)은 `PassRepository.findDeductionCandidates`의 `order by`가 보장한다 —
  서비스·정책이 다시 정렬하면 규칙이 두 곳으로 갈라진다"를 적는다.
  `ReservationPassPolicy.selectCandidate`를 그대로 호출하지 않는 이유(던지는 예외 타입이
  `InsufficientPassCountException`이라 D-133이 요구한 저녁반 전용 코드가 나오지 않는다)를 KDoc에 남긴다.
- 잔여 비교가 필요한 곳에서는 `compareTo`/`signum`만 쓴다 — `equals` 금지(D-016).

REFACTOR: 중복 제거 후 `./gradlew ktlintFormat`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.EveningHalfDeductionPolicyTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.EveningHalfDeductionPolicyTest"` 가 0 failures로 끝난다
    - `EveningHalfDeductionPolicy.kt`에 `BigDecimal("0.5")`가 존재하고 `BigDecimal(0.5)`(Double 생성자)는 존재하지 않는다
    - 테스트 파일에 `sortedBy`·`sortBy` 문자열이 없다(정렬은 쿼리 책임임을 테스트도 전제한다)
    - 테스트가 스프링 컨텍스트를 띄우지 않는다 (`@SpringBootTest` 문자열 없음)
  </acceptance_criteria>
  <done>저녁반 차감 판정 3규칙(회비 우선·임박순 한 장·거부)이 밀리초 단위 단위테스트로 고정됐다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [TDD] PassRepository.existsActiveEveningMembership — 수업날 기준 회비 유효성 조회</name>
  <files>src/main/kotlin/com/goldwrestling/pass/PassRepository.kt, src/test/kotlin/com/goldwrestling/pass/PassRepositoryEveningMembershipTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt 115-145행 (`findDeductionCandidates` 쿼리와 KDoc — `endDate >= :classDate`가 D-066 종료일 포함 판정과 같은 비교축임을 확인)
    - src/main/kotlin/com/goldwrestling/pass/PassType.kt, PassStatus.kt (enum 상수명)
    - docs/policies.md §1 (종료일 당일까지 사용 가능, `!today.isAfter(endDate)`)
    - src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt (Pass 픽스처 생성 관례)
  </read_first>
  <behavior>
    - 수업날이 회비 기간 안(시작일 ≤ 수업날 ≤ 종료일)이면 true
    - 수업날이 **종료일 당일**이면 true (D-066 종료일 포함)
    - 수업날이 종료일 다음날이면 false
    - 수업날이 시작일 이전이면 false
    - 취소된(`CANCELED`) 회비는 false
    - `SESSION_PASS`만 있고 `EVENING_MEMBERSHIP`이 없으면 false
    - 오늘 기준으로는 만료됐지만 **수업날 기준으로는 유효한** 회비는 true (소급 입력)
  </behavior>
  <action>
RED 먼저: `PassRepositoryEveningMembershipTest`를 Testcontainers 통합테스트로 만든다
(`@SpringBootTest` + `@Import(TestcontainersConfiguration::class)`, 기존 pass 테스트와 동일 조합).
**기존 `PassRepositoryTest`를 수정하지 않고 새 파일로 만든다** — 06-05와 다른 플랜이 같은 파일을
동시에 건드리지 않게 하기 위해서다. 위 behavior 7개를 각각 한 테스트로 쓴다. 마지막 케이스는
`Clock`을 고정해 "오늘"과 "수업날"이 다른 상황을 만들고, 넘긴 파라미터가 수업날임을 단언한다.

GREEN: `PassRepository`에 메서드 하나를 추가한다 —
`existsActiveEveningMembership(memberId: Long, classDate: LocalDate): Boolean`.
`@Query`는 `findDeductionCandidates` 관례를 그대로 따라 JPQL 문자열로 쓰고, 조건은
`p.member.id = :memberId`, `p.type = com.goldwrestling.pass.PassType.EVENING_MEMBERSHIP`,
`p.status = com.goldwrestling.pass.PassStatus.ACTIVE`, `p.startDate <= :classDate`,
`p.endDate >= :classDate`이며 `select count(p) > 0` 형태로 반환한다.
KDoc에 **"이 쿼리의 `classDate`는 오늘이 아니라 수업날이다"**(D-128·Pitfall 1)와
`endDate >= :classDate`가 D-066 종료일 포함 판정과 같은 비교축이라는 점을 적는다.

REFACTOR 후 `./gradlew ktlintFormat`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.pass.PassRepositoryEveningMembershipTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.pass.PassRepositoryEveningMembershipTest"` 가 0 failures로 끝난다
    - `PassRepository.kt`에 `existsActiveEveningMembership`가 존재하고 쿼리 문자열에 `EVENING_MEMBERSHIP`과 `endDate >= :classDate`가 포함된다
    - 테스트에 `종료일 당일`과 `소급` 문자열을 포함한 메서드가 각각 존재한다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>회비 우선 판정의 데이터 근거가 수업날 기준으로 정확히 동작함이 실제 DB에서 검증됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 판정 계층 → 잔여 횟수 | 이 플랜은 "얼마를 어디서 깎을지"만 정한다. 실제 차감은 06-07이 조건부 UPDATE로 수행 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | 유효기간 판정에 오늘 날짜를 써서 소급 입력이 잘못된 이용권을 차감 | mitigate | `existsActiveEveningMembership`/`findDeductionCandidates` 파라미터를 수업날로 고정하고 "소급" 테스트로 실증 |
| T-06-14 | Tampering | 정책이 후보를 재정렬해 만료 임박순(D-091)이 깨짐 | mitigate | `selectCandidate`가 `first`만 취하고 정렬 금지를 KDoc·테스트로 고정 |
| T-06-15 | Information Disclosure | 차감 거부 응답에 이용권 id·잔여가 노출 | mitigate | 예외 메시지에 식별자·수치를 보간하지 않는다(Task 1 acceptance) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.goldwrestling.attendance.*" --tests "com.goldwrestling.pass.*"` 0 failures
- 커밋이 RED/GREEN 2~3개로 나뉘어 있다 (`test(06-05):` → `feat(06-05):`)
</verification>

<success_criteria>
- 회비 우선·만료 임박순·거부 3규칙이 단위테스트로 검증된다
- 수업날 기준 회비 유효성 조회가 종료일 경계와 소급 케이스에서 정확하다
- 출석 도메인 예외 5종이 존재한다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-05-SUMMARY.md` when done
</output>
