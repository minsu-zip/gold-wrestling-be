---
phase: 06-operations
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06"]
files_modified:
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt
  - src/test/kotlin/com/goldwrestling/attendance/AttendanceEveningHalfTest.kt
autonomous: true
requirements: [ATTEND-02]
must_haves:
  truths:
    - "저녁반 출석 추가와 0.5회 차감이 한 트랜잭션에서 함께 끝난다 — 차감만 되고 출석이 없는 상태가 존재하지 않는다"
    - "수업날 기준 유효한 저녁반 회비가 있으면 출석만 기록되고 차감이 일어나지 않는다"
    - "회비가 없으면 만료 임박순 SESSION_PASS 한 장에서 0.5회가 차감되고 EVENING_HALF 이력이 남는다"
    - "회비도 없고 잔여 0.5 미만이면 출석 추가 자체가 409로 거부되고 아무 것도 저장되지 않는다"
    - "저녁반 출석을 삭제하면 0.5회가 복구되고 EVENING_HALF_REFUND 이력이 남는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt"
      provides: "저녁반 출석 추가·삭제 + 차감·복구"
      exports: ["addEveningAttendance", "delete"]
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt"
      to: "pass.PassRepository.adjustRemainingCount"
      via: "조건부 원자 갱신(0행이면 도메인 예외)"
      pattern: "adjustRemainingCount"
    - from: "src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt"
      to: "pass.PassTransactionRepository"
      via: "EVENING_HALF / EVENING_HALF_REFUND 원장 기록"
      pattern: "TransactionReason.EVENING_HALF"
---

<objective>
저녁반 0.5회 차감(ATTEND-02)의 **실행부**를 만든다 — 출석 추가와 차감을 한 트랜잭션으로 묶고,
삭제 시 연결된 차감을 `EVENING_HALF_REFUND`로 복구한다(D-128).

목적: 이 경로가 이 phase에서 유일하게 회원 잔여 횟수를 바꾼다. Core Value("회원이 보는 잔여 횟수는
항상 실제 사용 가능 횟수와 일치한다")가 직접 걸려 있어, `ReservationLedgerSupport.createReservation`이
확립한 "판정 → 조건부 UPDATE(0행이면 예외) → 재조회 → INSERT → 원장 기록" 구조를 그대로 이식한다.
새 락 메커니즘이나 새 차감 경로를 발명하지 않는다.

산출물: 서비스 2 메서드 추가 + 통합테스트 1(신규 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: addEveningAttendance — 회비 우선 판정 → 조건부 0.5 차감 → 출석 INSERT → EVENING_HALF 원장</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/ReservationLedgerSupport.kt 62-155행 (이식할 구조 전문 — 조건부 UPDATE 이후 준영속화되므로 INSERT에 쓸 엔티티를 **재조회**하는 이유 포함)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt 40-60행·115-145행 (`adjustRemainingCount`의 `@Modifying(flushAutomatically, clearAutomatically)` 관례와 `findDeductionCandidates` 시그니처)
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (주체 컬럼 계약 — 관리자 주체면 `admin` non-null, `member` null)
    - src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt (06-05: `HALF_SESSION`, `requireEveningSession`, `selectCandidate`)
    - src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt (06-06이 만든 현재 상태 — 생성자 주입 목록과 `check`의 세션 확보 흐름)
    - docs/policies.md §4.2 (06-01이 보강한 규칙)
  </read_first>
  <behavior>
    - 세션 `classType`이 `EVENING`이 아니면 `AttendanceClassTypeMismatchException`
    - 이미 그 세션에 출석 기록이 있으면 `DuplicateAttendanceException`(사전 검사) — 경쟁으로 뚫리면 유니크 위반을 잡아 같은 예외로 변환
    - 수업날 기준 유효 회비 보유 시 `Attendance.passTransaction`이 null이고 `pass_transaction` 건수가 늘지 않는다
    - 회비 미보유 시 만료가 가장 임박한 `SESSION_PASS`의 `remainingCount`가 0.5 줄고 `EVENING_HALF` 이력 1건이 생긴다
    - 후보가 없으면 `EveningAttendanceDeductionUnavailableException`이 나고 `attendance` 행도 생기지 않는다
    - 조건부 UPDATE가 0행이면 `EveningAttendanceDeductionUnavailableException`(경쟁 패배)
    - `PassTransaction.admin`이 요청 관리자이고 `member`는 null이다
  </behavior>
  <action>
`AttendanceService` 생성자에 `passRepository`, `passTransactionRepository`를 추가한다(06-06이 남긴
주입 목록 뒤에 붙인다).

`@Transactional fun addEveningAttendance(adminId: Long, request: AddEveningAttendanceRequest): AttendanceResponse`를
아래 순서로 구현한다 — 순서를 바꾸지 않는다:
1. 시간표 조회(없으면 `ClassScheduleNotFoundException`) → `classSessionService.getOrCreate(schedule, classDate)`
2. `EveningHalfDeductionPolicy.requireEveningSession(session.classType)` — `EVENING`이 아니면 409
3. `attendanceRepository.existsByClassSessionIdAndMemberId(sessionId, memberId)`가 true면
   `DuplicateAttendanceException` (친절한 사전 검사 — 실제 방어선은 `uq_attendance_member_session`)
4. **회비 우선 판정**: `passRepository.existsActiveEveningMembership(memberId, session.classDate)`.
   **`LocalDate.now(clock)`을 넘기지 않는다** — 인자는 반드시 `session.classDate`(수업날)다(D-128, Pitfall 1)
5. 회비가 있으면 `passTransaction = null`로 두고 6단계를 건너뛴다
6. 회비가 없으면: `passRepository.findDeductionCandidates(memberId, PassType.SESSION_PASS, session.classDate, EveningHalfDeductionPolicy.HALF_SESSION)`
   → `EveningHalfDeductionPolicy.selectCandidate(...)`(비면 409) →
   `passRepository.adjustRemainingCount(passId, HALF_SESSION.negate())`가 0을 반환하면
   `EveningAttendanceDeductionUnavailableException`(경쟁 패배)
7. 조건부 UPDATE 이후 영속성 컨텍스트가 clear됐으므로 INSERT에 쓸 `Member`·`ClassSession`·`Admin`·`Pass`를
   **모두 재조회**한다(`ReservationLedgerSupport` 관례)
8. `PassTransaction(pass = refreshedPass, amount = HALF_SESSION.negate(), reason = TransactionReason.EVENING_HALF,
   note = null, admin = refreshedAdmin, member = null, occurredAt = now)`를 `save`해 id를 확보한다
9. `Attendance(member, classSession, status = ATTENDED, passTransaction = 위 이력 또는 null,
   checkedBy = admin, checkedAt = now, createdAt = now)`를 `saveAndFlush`한다.
   `DataIntegrityViolationException`을 잡아 `DuplicateAttendanceException`으로 변환한다
   (`ReservationLedgerSupport`가 `DuplicateReservationException`으로 변환하는 것과 동일 관례) —
   이때 트랜잭션이 롤백되므로 8단계의 차감·이력도 함께 사라진다
10. `AttendanceResponse.from(...)` 반환

`ClassType.EVENING` 세션은 `capacity`가 null이라 정원 조건부 UPDATE(`incrementReservedCountIfCapacityAvailable`)를
호출하지 않는다 — 저녁반은 예약 대상이 아니므로 `reservedCount`를 건드리지 않는다. 이 판단을 KDoc에 남긴다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "TransactionReason.EVENING_HALF" src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt && ! grep -q "LocalDate.now(clock)" src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AttendanceService.kt`에 `existsActiveEveningMembership(` 호출이 있고 그 인자에 `classDate`가 쓰인다 (`LocalDate.now(` 문자열이 파일 전체에 없다)
    - `adjustRemainingCount` 반환값이 0인 분기에서 예외를 던지는 코드가 존재한다
    - `DataIntegrityViolationException`을 잡아 `DuplicateAttendanceException`으로 변환하는 catch 블록이 존재한다
    - `incrementReservedCountIfCapacityAvailable` 문자열이 파일에 존재하지 않는다
  </acceptance_criteria>
  <done>저녁반 출석 추가가 회비 우선 판정과 조건부 차감·원장 기록을 한 트랜잭션으로 완결한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: delete — 저녁반 출석 삭제 시 0.5회 복구 + EVENING_HALF_REFUND 이력</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/ReservationLedgerSupport.kt 200-240행 (`restorePassAfterCancellation`의 reason 파라미터화 관례와 복구 실패 시 `IllegalStateException`)
    - src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt (Task 1이 만든 상태)
    - docs/policies.md §4.2 (06-01이 추가한 "저녁반 출석 삭제 시 자동 복구")
  </read_first>
  <behavior>
    - 없는 id로 삭제하면 `AttendanceNotFoundException`
    - `passTransaction`이 null인 출석(예약제/1:1 또는 회비 보유 저녁반)은 잔여·이력 변화 없이 행만 삭제된다
    - `passTransaction`이 있는 출석은 그 이력이 가리키는 `Pass`의 잔여가 0.5 늘고 `EVENING_HALF_REFUND` 이력 1건이 추가된다
    - 삭제 후 같은 회원·세션에 다시 출석 추가가 가능하다(유니크가 해제된다)
    - 복구 대상 `Pass`가 취소(`CANCELED`) 상태라 조건부 UPDATE가 0행이면 복구를 시도했다는 사실이 드러나는 예외가 난다
  </behavior>
  <action>
`@Transactional fun delete(adminId: Long, attendanceId: Long)`를 구현한다:
1. `attendanceRepository.findById(attendanceId)`가 없으면 `AttendanceNotFoundException(attendanceId)`
2. 연결된 `passTransaction`이 null이면 `attendanceRepository.delete(attendance)`로 끝낸다
   (예약제/1:1 출석과 회비 보유 저녁반 출석은 복구할 차감이 없다)
3. 연결된 이력이 있으면 그 이력의 `pass.id`를 꺼내 **먼저 출석 행을 삭제하고 flush**한 뒤
   (`attendance.pass_transaction_id` FK가 남아 있으면 이력 해석이 모호해지므로 삭제를 선행한다),
   `passRepository.adjustRemainingCount(passId, EveningHalfDeductionPolicy.HALF_SESSION)`를 호출한다.
   0행이면 `IllegalStateException("복구 대상 이용권(id=...)이 판정 이후 상태가 바뀌어 복구를 반영하지 못했습니다.")` —
   `ReservationLedgerSupport.restorePassAfterCancellation`과 동일한 문구·동일한 실패 처리다
   (조용히 넘어가면 잔여와 원장이 갈라진다)
4. `Pass`를 재조회한 뒤 `PassTransaction(pass = refreshedPass, amount = HALF_SESSION(양수),
   reason = TransactionReason.EVENING_HALF_REFUND, note = null, admin = refreshedAdmin, member = null,
   occurredAt = OffsetDateTime.now(clock))`를 저장한다
5. **원래 차감 이력(`EVENING_HALF`) 행은 삭제하지 않는다** — 원장은 append-only이고 상쇄 이력으로
   표현한다(CLAUDE.md 규칙 6). 이 원칙을 KDoc에 적는다.

`ReservationLedgerSupport`를 재사용하지 않는 이유를 KDoc에 남긴다 — 그 컴포넌트의 복구 금액이
`ReservationPassPolicy.DEDUCTION_AMOUNT`(1.0) 상수로 고정돼 있어 0.5 복구에 쓸 수 없다(06-PATTERNS 분석).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "TransactionReason.EVENING_HALF_REFUND" src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `delete`가 `EVENING_HALF_REFUND` 사유로 `PassTransaction`을 저장한다
    - `passTransactionRepository.delete` 호출이 파일 전체에 존재하지 않는다(원장 append-only)
    - `adjustRemainingCount` 0행 분기에서 예외를 던진다
  </acceptance_criteria>
  <done>저녁반 출석 삭제가 잔여를 복구하고 그 사실이 예약 복구와 구분되는 사유로 원장에 남는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AttendanceEveningHalfTest — 회비 우선·0.5 차감·409 거부·삭제 복구·소급 판정</name>
  <files>src/test/kotlin/com/goldwrestling/attendance/AttendanceEveningHalfTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/attendance/AttendanceServiceTest.kt (06-06이 만든 통합테스트의 애노테이션 조합·정리 순서 — 동일하게 맞춘다)
    - src/test/kotlin/com/goldwrestling/attendance/AttendanceFixtures.kt
    - src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt (`sessionPass` 픽스처 — 잔여 0.5/0인 장 만들 때 `INITIAL_GRANT` 이력 CHECK 제약 주의사항이 05-05 SUMMARY에 기록돼 있다)
    - docs/policies.md §4.2
  </read_first>
  <behavior>
    - `유효한 저녁반 회비가 있으면 차감하지 않는다`: 잔여 불변 + `pass_transaction` 건수 불변 + `attendance.pass_transaction_id`가 null
    - `회비가 없으면 만료가 가장 임박한 횟수권에서 0.5회를 차감한다`: 두 장 중 `endDate`가 이른 장만 줄어든다
    - `잔여 0.5회면 저녁반 0.5회 참여만 가능하다`: 잔여 0.5인 장에서 성공하고 잔여가 0이 된다
    - `회비도 없고 잔여가 0.5 미만이면 저녁반 출석을 기록할 수 없다`: 409 예외 + `attendance` 행 0건 + 잔여 불변
    - `저녁반 출석을 삭제하면 0.5회가 복구된다`: 잔여 +0.5, `EVENING_HALF_REFUND` 이력 1건, `EVENING_HALF` 이력은 그대로 남는다
    - `유효기간 판정 기준일은 오늘이 아니라 수업날이다`: 오늘 기준 만료된 이용권이지만 수업날 기준 유효하면 차감에 성공한다
    - `같은 회원을 같은 저녁반 수업에 두 번 추가할 수 없다`: 두 번째 호출이 409이고 잔여가 한 번만 줄었다
  </behavior>
  <action>
`AttendanceEveningHalfTest`를 06-06의 `AttendanceServiceTest`와 **동일한 애노테이션 조합**으로 만든다
(컨텍스트 캐시 재사용). 시각 고정이 필요하므로 `TestClockConfiguration`/`MutableTestClock`을 쓴다.

위 behavior 7개를 각각 하나의 테스트로 작성하고, 메서드명은 policies §4.2 문장을 그대로 쓴다.
모든 차감·복구 검증은 **잔여 값과 `PassTransaction` 건수·사유를 함께** 단언한다 — 잔여만 보면
이력 누락(조용한 버그)을 놓친다(add-domain-test §4).

픽스처 주의:
- 잔여 0인 `SESSION_PASS`는 `INITIAL_GRANT` 이력을 만들지 않는다(`ck_pass_transaction_amount_nonzero`가
  금액 0 이력을 거부한다 — 05-05 선례)
- `EVENING_MEMBERSHIP` 픽스처는 `remainingCount`가 null인 기간제다
- 정리 순서는 attendance → pass_transaction → pass → class_session → member/admin/branch

`BigDecimal` 비교는 전부 `compareTo`/`isEqualByComparingTo`로 한다 — `isEqualTo`는 `0.5 != 0.50`으로
실패한다(D-016).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.AttendanceEveningHalfTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.AttendanceEveningHalfTest"` 가 0 failures로 끝난다
    - 테스트 파일에 `수업날` 문자열을 포함한 소급 판정 테스트가 존재한다
    - 테스트 파일에 `isEqualTo(BigDecimal` 패턴이 없다(BigDecimal 비교는 `compareTo` 계열만 사용)
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>저녁반 차감·복구·거부의 모든 분기가 실제 DB에서 잔여와 원장 양쪽으로 검증된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 요청 → 회원 잔여 횟수 | 이 phase에서 유일하게 잔여를 변경하는 경로 |
| 애플리케이션 → PostgreSQL | 이중 차감의 최종 방어선은 `uq_attendance_member_session`과 조건부 UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Tampering | 동시 요청으로 같은 회원·세션에 이중 차감 | mitigate | 회원×세션 유니크 위반 시 트랜잭션 롤백으로 차감도 함께 취소 + 조건부 `adjustRemainingCount` 0행 거부. 06-08이 동시성 테스트로 실증 |
| T-06-21 | Tampering | 잔여가 음수가 되는 차감 | mitigate | `adjustRemainingCount`의 `remainingCount + :amount >= 0` 조건이 DB에서 거부하고 0행이면 409로 변환 |
| T-06-22 | Repudiation | 잔여가 바뀌었는데 원장에 근거가 없음 | mitigate | 차감·복구 모두 같은 트랜잭션에서 `PassTransaction`을 저장(CLAUDE.md 규칙 6), 테스트가 건수·사유를 단언 |
| T-06-23 | Elevation of Privilege | 회원이 자가 차감·자가 복구 호출 | mitigate | 이 서비스는 `/api/admin/**` 컨트롤러(06-08)에서만 노출되며 `PassTransaction.admin`이 항상 채워지고 `member`는 null |
| T-06-24 | Tampering | 소급 입력에서 오늘 기준 판정을 써 잘못된 이용권 차감 | mitigate | 회비·후보 조회 인자를 `session.classDate`로 고정하고 grep acceptance + 소급 테스트로 이중 방어 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.goldwrestling.attendance.*"` 0 failures
- `AttendanceService.kt`에 `LocalDate.now(` 문자열이 없다
</verification>

<success_criteria>
- 저녁반 출석 추가가 회비 우선·0.5 차감·409 거부 세 분기를 모두 정확히 처리한다
- 삭제가 `EVENING_HALF_REFUND`로 복구하고 원 이력은 남긴다
- 잔여 변경이 전부 원장에 근거를 남긴다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-07-SUMMARY.md` when done
</output>
