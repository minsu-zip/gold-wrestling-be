---
phase: 03-pass
plan: 06
type: execute
wave: 5
depends_on: [03-04, 03-02, 03-01]
files_modified:
  - src/main/kotlin/com/goldwrestling/pass/dto/RegisterPassRequest.kt
  - src/main/kotlin/com/goldwrestling/pass/dto/PassResponse.kt
  - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt
  - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt
  - src/main/kotlin/com/goldwrestling/auth/AuthenticatedPrincipal.kt
  - src/test/kotlin/com/goldwrestling/auth/AuthenticatedPrincipalTest.kt
  - src/test/kotlin/com/goldwrestling/pass/AdminPassControllerTest.kt
  - docs/api/openapi.yaml
  - docs/decisions.md
autonomous: true
requirements: [PASS-01, PASS-02]

must_haves:
  truths:
    - "관리자가 회원에게 저녁반 회비·예약제 횟수권·1:1 레슨권을 등록할 수 있다"
    - "시작일을 지정하지 않으면 오늘로, 지정하면 과거 날짜라도 그대로 등록된다"
    - "횟수권 등록 시 초기 횟수 부여가 INITIAL_GRANT PassTransaction 이력으로 남는다"
    - "관리자 토큰 없이 또는 회원 토큰으로 등록을 시도하면 401/403 ProblemDetail이 반환된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt"
      provides: "이용권 등록 유스케이스 (Pass 생성 + INITIAL_GRANT 이력)"
      contains: "TransactionReason.INITIAL_GRANT"
    - path: "src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt"
      provides: "POST /api/admin/members/{memberId}/passes"
      contains: "@PostMapping"
    - path: "src/main/kotlin/com/goldwrestling/pass/dto/PassResponse.kt"
      provides: "이용권 응답 DTO (displayStatus 계산 포함)"
      contains: "fun from("
    - path: "docs/api/openapi.yaml"
      provides: "FE 계약 재생성 결과"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/Pass.kt"
      via: "Pass.register(...) 팩토리 호출"
      pattern: "Pass\\.register\\("
    - from: "src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/PassTransactionRepository.kt"
      via: "같은 트랜잭션에서 INITIAL_GRANT 이력 저장"
      pattern: "passTransactionRepository\\.save"
---

<objective>
관리자 이용권 등록 API(`POST /api/admin/members/{memberId}/passes`)를 만든다.
등록과 `INITIAL_GRANT` 이력 기록이 **같은 트랜잭션**에서 일어나 "이력 없는 잔여 변경"이 원천적으로 불가능하게 한다.

Purpose: 이 phase에서 이용권이 처음 생기는 경로다. 이후 모든 플랜(가감·기간수정·취소·조회)의 통합테스트가
이 등록 경로로 픽스처를 만든다. `PassResponse`도 여기서 확정되어 나머지 플랜이 재사용한다.

Output: 요청·응답 DTO 2종, `AdminPassService.register`, `AdminPassController`, 통합테스트, 재생성된 `openapi.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/add-domain-test/SKILL.md
@docs/policies.md
@docs/conventions.md
@docs/decisions.md
@.planning/phases/03-pass/03-PATTERNS.md
@.planning/phases/03-pass/03-RESEARCH.md
@.planning/phases/03-pass/03-CONTEXT.md
</context>

<interfaces>
<!-- 이미 존재하는 계약. 이 값들로 충분하다. -->

`pass/Pass.kt` (03-04 산출물):
- `companion object { fun register(member: Member, branch: Branch, type: PassType, startDate: LocalDate, term: EveningMembershipTerm?, initialCount: BigDecimal?, registeredBy: Admin, now: OffsetDateTime): Pass }`
- `fun displayStatus(today: LocalDate): PassDisplayStatus`

`pass/PassTransaction.kt` (03-02 산출물) — 전 필드 `val` 생성자:
- `PassTransaction(pass, amount, reason, note, admin, occurredAt)`

`auth/AuthenticatedPrincipal.kt`:
- `data class AuthenticatedPrincipal(val principalType: PrincipalType, val principalId: Long, val memberStatus: MemberStatus?, val onboardingCompleted: Boolean)`
- `fun requireMemberId(): Long` — `check(principalType == PrincipalType.MEMBER)` 후 `principalId` 반환

`member/dto/MemberDetailResponse.kt` — 응답 DTO 형식의 원본 (`companion object { fun from(entity) }`, `@field:Schema`)

`member/AdminMemberController.kt` — 관리자 컨트롤러 형식의 원본:
- `@RestController @RequestMapping("/api/admin/members")`, 권한 애노테이션 없음(D-040), `@Transactional` 없음(D-020), `@Operation(summary = ...)`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 요청·응답 DTO 2종 + AuthenticatedPrincipal.requireAdminId</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/dto/RegisterPassRequest.kt,
    src/main/kotlin/com/goldwrestling/pass/dto/PassResponse.kt,
    src/main/kotlin/com/goldwrestling/auth/AuthenticatedPrincipal.kt,
    src/test/kotlin/com/goldwrestling/auth/AuthenticatedPrincipalTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/dto/RejectMemberRequest.kt (요청 DTO 형식의 원본 — `@field:NotBlank`, `@field:Schema`)
    - src/main/kotlin/com/goldwrestling/member/dto/MemberDetailResponse.kt (응답 DTO 형식의 원본 — `companion object fun from`, `requireNotNull(entity.id)`)
    - src/main/kotlin/com/goldwrestling/auth/AuthenticatedPrincipal.kt (전체 — `requireMemberId`를 대칭으로 본뜬다)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt, PassType.kt, PassDisplayStatus.kt, EveningMembershipTerm.kt
    - docs/conventions.md §6(DTO와 API), §10.0 표(DTO 변환에 계산·분기가 있으면 단위테스트 대상)
    - .planning/phases/03-pass/03-PATTERNS.md "요청/응답 DTO 4종"
  </read_first>
  <behavior>
    - `AuthenticatedPrincipal(ADMIN, 7).requireAdminId()` 는 7을 반환한다
    - `AuthenticatedPrincipal(MEMBER, 7).requireAdminId()` 는 `IllegalStateException`을 던진다
    - `PassResponse.from(pass, today)`의 `displayStatus`는 `pass.displayStatus(today)`와 같다 (변환 계층에서 규칙을 다시 쓰지 않는다)
  </behavior>
  <action>
    (1) `RegisterPassRequest` — `type: PassType`(`@field:NotNull`), `startDate: LocalDate? = null`,
    `term: EveningMembershipTerm? = null`, `initialCount: BigDecimal? = null`.
    검증은 **형식만** 건다 — `initialCount`에 `@field:Digits(integer = 3, fraction = 1)`(DECIMAL(4,1) 한계)만 두고,
    "0.5의 배수", "양수", "타입별 필수 조합"은 **DTO에 넣지 않는다**(conventions §6) — `Pass.register`가 이미 강제한다.
    각 필드에 `@field:Schema(description = ..., example = ...)`를 붙인다. FE가 이 문구만 보고 작업한다.
    `startDate`가 null이면 오늘로 채워진다는 사실(D-055)을 스키마 설명에 적는다.

    (2) `PassResponse` — 필드: `passId`, `memberId`, `type`, `displayStatus`, `startDate`, `endDate`,
    `remainingCount: BigDecimal?`(기간제는 null), `canceledAt: OffsetDateTime?`, `cancelReason: String?`, `registeredAt: OffsetDateTime`.
    `companion object { fun from(pass: Pass, today: LocalDate): PassResponse }` — `displayStatus`는
    **반드시 `pass.displayStatus(today)`를 호출**해 얻는다(계산 규칙을 DTO에 복제하지 않는다).
    `passId`는 `requireNotNull(pass.id) { "저장되지 않은 Pass는 응답으로 변환할 수 없습니다." }` 형식을 따른다.
    `memberId`는 LAZY 연관 접근이므로 **트랜잭션 안에서만 호출**해야 한다는 경고를 companion object KDoc에 남긴다(RESEARCH Pitfall 3).
    `cancelReason`은 관리자 전용 정보다 — 회원 조회(03-10)는 취소된 이용권을 아예 제외하므로 항상 null이 된다는 사실을 KDoc에 적는다(D-043의 연장).

    (3) `AuthenticatedPrincipal`에 `requireAdminId(): Long`을 `requireMemberId()`와 대칭으로 추가한다
    (`check(principalType == PrincipalType.ADMIN) { "관리자 전용 경로에 회원 주체가 들어왔습니다." }`).
    KDoc도 `requireMemberId`처럼 "여기서 던지는 예외는 사용자 대면 오류가 아니라 인가 규칙과 서비스 코드가
    어긋났다는 프로그래밍 오류 신호"라는 취지를 적는다.

    (4) `src/test/kotlin/com/goldwrestling/auth/AuthenticatedPrincipalTest.kt`를 새로 만들어
    `requireAdminId`의 두 케이스를 순수 단위테스트로 검증한다(기존에 이 클래스 전용 테스트가 없다).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.auth.AuthenticatedPrincipalTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.auth.AuthenticatedPrincipalTest"` 종료 코드 0
    - `AuthenticatedPrincipal.kt`가 `fun requireAdminId()` 를 포함한다
    - `PassResponse.kt`가 `pass.displayStatus(today)` 호출을 포함한다 (규칙 복제 없음)
    - `RegisterPassRequest.kt`에 `@field:DecimalMin`·`remainder`·"0.5" 문자열이 없다 (도메인 규칙은 DTO 밖)
    - 두 DTO의 모든 필드에 `@field:Schema` 가 붙어 있다
  </acceptance_criteria>
  <done>FE 계약이 되는 요청·응답 형태가 확정되고, 관리자 주체 id를 얻는 방법이 회원과 대칭이 된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminPassService.register + AdminPassController</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt,
    src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt,
    docs/decisions.md
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (전체 — 클래스 헤더 `@Transactional(readOnly = true)`, 변경 메서드만 `@Transactional`, 조회→검증→변경→DTO변환 순서)
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt (전체 — 경로·`@Operation`·권한 애노테이션 없음 관례)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt (`register` 시그니처)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt, PassTransactionRepository.kt
    - src/main/kotlin/com/goldwrestling/member/MemberRepository.kt, MemberExceptions.kt
    - src/main/kotlin/com/goldwrestling/admin/Admin.kt 및 AdminRepository (FK 주체 조회)
    - .claude/skills/add-endpoint/SKILL.md §1~§4
    - docs/decisions.md D-020, D-055
  </read_first>
  <behavior>
    - 유효한 요청으로 `POST /api/admin/members/{memberId}/passes` 를 호출하면 201과 `PassResponse`가 반환된다
    - 횟수권 등록 시 `pass_transaction`에 `INITIAL_GRANT` 행이 1건 생기고 `amount`가 초기 횟수와 같다
    - 기간제 등록 시 `pass_transaction` 행이 생기지 않는다 (수량 개념이 없다 — D-057 "PassTransaction은 ±수량 원장에 고정")
    - 존재하지 않는 memberId로 등록하면 404 `MEMBER_NOT_FOUND`
  </behavior>
  <action>
    (1) `AdminPassService` — `@Service @Transactional(readOnly = true)` 클래스에 `@Transactional fun register(memberId: Long, request: RegisterPassRequest, adminId: Long): PassResponse`.
    처리 순서:
    · `memberRepository.findById(memberId).orElseThrow { MemberNotFoundException(memberId) }`
    · `adminRepository.findById(adminId).orElseThrow { ... }` — 주체 Admin 엔티티 확보
    · `val today = LocalDate.now(clock)` / `val now = OffsetDateTime.now(clock)` — **`Clock` 빈을 주입받아 쓴다**
      (CLAUDE.md 기술 규칙, 테스트에서 시각 고정 필요). `LocalDate.now()` 무인자 호출 금지
    · `val startDate = request.startDate ?: today` (D-055 기본값 오늘, 과거 허용 — 과거 검증을 넣지 않는다)
    · `Pass.register(member, member.branch, request.type, startDate, request.term, request.initialCount, admin, now)`
      → `passRepository.save(pass)`. `branch`는 회원의 소속 지점을 그대로 쓴다(D-067)
    · 횟수제(`request.initialCount != null` && 타입이 횟수제)면 **같은 트랜잭션에서**
      `passTransactionRepository.save(PassTransaction(pass, initialCount, TransactionReason.INITIAL_GRANT, note = null, admin, now))`
      (CLAUDE.md 규칙 6). 기간제면 이력을 남기지 않는다
    · `PassResponse.from(pass, today)` 반환 — **트랜잭션 안에서 변환**(RESEARCH Pitfall 3)
    회원 상태(`PENDING`/`ON_LEAVE`/`INACTIVE`)로 등록을 막지 **않는다** — policies·requirements 어디에도 제한이 없고,
    D-034/D-044가 상태 전이에 관리자 재량을 남긴 선례를 따른다. **이 규칙은 계획 단계에서 사용자가 확정했다
    (2026-08-03, plan-phase AskUserQuestion — "제한 없음, 관리자 재량" 선택).** 이 판단을 `docs/decisions.md`에
    **D-068**로 3~4줄 기록한다 (사용자 확정 사실 포함).

    (2) `AdminPassController` — `@RestController`, `@RequestMapping("/api/admin")`.
    `@PostMapping("/members/{memberId}/passes")`, `@ResponseStatus(HttpStatus.CREATED)`,
    시그니처는 `(@PathVariable memberId: Long, @RequestBody @Valid request: RegisterPassRequest, @AuthenticationPrincipal principal: AuthenticatedPrincipal)`.
    `principal.requireAdminId()`로 주체 id를 얻어 서비스에 넘긴다 — **요청 바디로 adminId를 받지 않는다**.
    응답에 `Location` 헤더(`/api/admin/passes/{passId}`)를 포함한다.
    클래스 KDoc에 `AdminMemberController`와 같은 문구를 남긴다 — 권한 애노테이션을 붙이지 않는 이유(D-040),
    `@Transactional`·`try-catch`를 붙이지 않는 이유(D-020·D-017).
    `@Operation(summary = "이용권 등록 (저녁반 회비 / 예약제 횟수권 / 1:1 레슨권)")`를 붙인다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileKotlin` 종료 코드 0
    - `AdminPassService.kt` 클래스에 `@Transactional(readOnly = true)`가, `register` 메서드에 `@Transactional`이 붙어 있다
    - `AdminPassService.kt`가 `TransactionReason.INITIAL_GRANT` 를 참조한다
    - `AdminPassService.kt`에 `LocalDate.now()`·`OffsetDateTime.now()`의 **무인자** 호출이 0건이다 (전부 `(clock)` 인자 전달)
    - `AdminPassController.kt`에 `@Transactional`·`try {` 가 0건이다
    - `AdminPassController.kt`가 `principal.requireAdminId()` 를 호출한다
    - `docs/decisions.md`에 `D-068` 항목이 존재한다
  </acceptance_criteria>
  <done>이용권 생성과 초기 부여 이력이 하나의 트랜잭션으로 묶여, 이력 없는 이용권이 만들어질 수 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminPassControllerTest 통합테스트 + openapi.yaml 재생성</name>
  <files>
    src/test/kotlin/com/goldwrestling/pass/AdminPassControllerTest.kt,
    docs/api/openapi.yaml
  </files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/member/MemberSearchTest.kt (전체 — 애노테이션 조합, `adminAccessToken`/`memberAccessToken` 헬퍼, 인가 401/403 섹션, 픽스처 헬퍼)
    - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt (03-02 산출물 — 이용권 픽스처 헬퍼)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt, AdminPassService.kt (Task 2 산출물)
    - .claude/skills/add-endpoint/SKILL.md §5(테스트), §6(openapi 재생성)
    - docs/conventions.md §10.0 표(컨트롤러 → 통합테스트 1개 이상: 성공 + 대표 실패), §10.1
  </read_first>
  <behavior>
    - 관리자 토큰 + 유효 요청 → 201, 본문 `passId`·`displayStatus`·`endDate` 존재, `Location` 헤더 존재
    - 횟수권 등록 후 `passTransactionRepository`에 `INITIAL_GRANT` 1건, `amount`가 초기 횟수와 같다
    - 저녁반(`EVENING_MEMBERSHIP` + `ONE_MONTH`) 등록 → 201, 응답 `remainingCount`가 null, 이력 0건
    - `startDate` 미지정 → 고정된 테스트 Clock의 오늘로 등록된다
    - `startDate`를 과거로 지정 → 201 (과거 허용, D-055)
    - 횟수권에 `initialCount` 0.3 → 400 `INVALID_ADJUSTMENT_UNIT`
    - 기간제에 `initialCount` 지정 → 409 `PASS_TYPE_NOT_ADJUSTABLE`
    - 존재하지 않는 memberId → 404 `MEMBER_NOT_FOUND`
    - 회원 토큰 → 403 `ACCESS_DENIED` / 토큰 없음 → 401 `UNAUTHENTICATED`
  </behavior>
  <action>
    (1) `AdminPassControllerTest.kt`를 만든다. 애노테이션 조합은 `MemberSearchTest`와 **한 글자도 다르지 않게**
    (`@SpringBootTest`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`, `@Transactional`)
    맞춘다 — 다르면 스프링 컨텍스트가 새로 떠서 스위트가 급격히 느려진다(conventions §10.1).
    `@BeforeEach`의 `(clock as MutableTestClock).setTo(...)` 관례를 따르되, 이 테스트는 날짜 단언이 있으므로
    **고정된 Instant**로 세팅해 `startDate` 기본값 단언이 흔들리지 않게 한다.
    위 `<behavior>` 10건을 각각 `@Test`로 쓴다. 실패 응답은 `jsonPath("$.code").value("...")`로 에러코드를 단언한다
    (상태코드만 보면 어떤 규칙이 걸렸는지 알 수 없다). 하단에 `adminAccessToken()`/`memberAccessToken()`/
    `persistBranch()`/`persistMember()`/`persistAdmin()` 헬퍼를 `MemberSearchTest`에서 그대로 본떠 둔다.
    이 클래스는 이후 03-07·03-08·03-09가 가감·기간수정·취소 통합테스트를 추가할 자리이므로,
    `// ---------- 등록 ----------` 같은 섹션 주석으로 구획을 나눠 둔다.

    (2) `./gradlew generateApiDocs`(D-029)로 `docs/api/openapi.yaml`을 재생성하고 `git diff docs/api/openapi.yaml`로
    새 경로가 들어갔는지 확인한다. 재생성에는 DB가 필요하므로 `docker-compose up -d`가 선행되어야 한다.
    diff에 `POST /api/admin/members/{memberId}/passes`와 `RegisterPassRequest`·`PassResponse` 스키마가 나타나야 한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.pass.AdminPassControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.pass.AdminPassControllerTest"` 종료 코드 0
    - `AdminPassControllerTest.kt`에 `@Test`가 10개 이상 존재한다
    - `AdminPassControllerTest.kt`의 애노테이션 4종이 `MemberSearchTest.kt`와 동일하다
    - `grep -c "/api/admin/members/{memberId}/passes" docs/api/openapi.yaml` >= 1
    - `docs/api/openapi.yaml`이 `RegisterPassRequest` 와 `PassResponse` 스키마명을 포함한다
    - `./gradlew build` 종료 코드 0
  </acceptance_criteria>
  <done>등록 API의 성공·대표 실패·인가가 실제 HTTP 계약으로 증명되고, FE가 쓸 openapi.yaml이 갱신됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `/api/admin/**` | 관리자 전용 경계. `SecurityConfig`의 `hasRole("ADMIN")`이 1차 방어 |
| 요청 바디 → 도메인 | 타입·수량·날짜가 신뢰할 수 없는 입력으로 들어오는 지점 |
| 인증 주체 → 이력 주체 | `PassTransaction.admin`이 감사 근거가 되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Elevation of Privilege | `POST /api/admin/members/{id}/passes` | mitigate | 경로가 `/api/admin/**` 프리픽스 안이라 `SecurityConfig`의 `hasRole("ADMIN")`이 자동 적용된다(D-040). 컨트롤러에 별도 애노테이션을 붙이지 않고, 401/403 응답을 통합테스트로 고정한다 |
| T-03-22 | Spoofing | 이력 주체 위조 | mitigate | `adminId`를 요청 바디·쿼리로 받지 않고 `principal.requireAdminId()`에서만 얻는다. 바디로 받으면 관리자 A가 B 이름으로 이력을 남길 수 있다 |
| T-03-23 | Repudiation | 이력 없는 이용권 생성 | mitigate | `Pass` 저장과 `INITIAL_GRANT` 저장이 같은 `@Transactional` 메서드 안 — 함께 커밋되거나 함께 롤백된다 |
| T-03-24 | Tampering | 비정상 초기 횟수 | mitigate | DTO는 `@Digits`로 형식만, 양수·0.5 배수는 `Pass.register`가 강제(T-03-12), `ck_pass_remaining_count_by_type`이 DB 이중 방어 |
| T-03-25 | Information Disclosure | 다른 회원 정보 노출 | accept | 관리자 전용 경로이므로 회원 스코프 제한이 없는 것이 정상 동작이다. 회원용 경로(03-10)는 별도 방어 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 신규 외부 패키지 설치 없음 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `docker-compose up -d` 후 `./gradlew generateApiDocs` 성공, `git diff docs/api/openapi.yaml`에 새 경로 확인
- CLAUDE.md 규칙 10 준수: 이 플랜에서 만든 프로덕션 코드에 대응하는 테스트가 같은 작업에 포함됨
</verification>

<success_criteria>
- 3종 이용권이 시작일 지정(기본 오늘, 과거 허용)으로 등록된다
- 횟수권 등록이 `INITIAL_GRANT` 이력과 원자적으로 묶인다
- 인가·성공·대표 실패 4종이 통합테스트로 고정된다
- `openapi.yaml`이 재생성되어 커밋 가능한 상태다
</success_criteria>

<output>
Create `.planning/phases/03-pass/03-06-SUMMARY.md` when done.
D-068(회원 상태로 등록을 제한하지 않음)은 계획 단계에서 사용자가 확정했으므로 완료 보고에는 확정 사실만 언급한다.
커밋은 사용자가 명시적으로 요청할 때만 실행한다 (CLAUDE.md 커밋 규칙).
</output>
