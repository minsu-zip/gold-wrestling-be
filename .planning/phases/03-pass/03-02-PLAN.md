---
phase: 03-pass
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/main/resources/db/migration/V4__create_pass_tables.sql
  - src/main/kotlin/com/goldwrestling/pass/PassType.kt
  - src/main/kotlin/com/goldwrestling/pass/PassStatus.kt
  - src/main/kotlin/com/goldwrestling/pass/PassDisplayStatus.kt
  - src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt
  - src/main/kotlin/com/goldwrestling/pass/EveningMembershipTerm.kt
  - src/main/kotlin/com/goldwrestling/pass/Pass.kt
  - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt
  - src/main/kotlin/com/goldwrestling/pass/PassPeriodChange.kt
  - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt
  - src/main/kotlin/com/goldwrestling/pass/PassTransactionRepository.kt
  - src/main/kotlin/com/goldwrestling/pass/PassPeriodChangeRepository.kt
  - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03, PASS-04, PASS-07, PASS-08]

must_haves:
  truths:
    - "pass·pass_transaction·pass_period_change 테이블이 Flyway 마이그레이션으로 생성되고 엔티티 매핑이 ddl-auto=validate를 통과한다"
    - "기간제 이용권에 잔여 횟수를 넣거나 횟수제 이용권의 잔여 횟수를 비우면 DB가 저장을 거부한다"
    - "잔여 횟수는 결과가 음수가 되지 않을 때만 원자적으로 갱신되고, 갱신 실패는 반환 행 수 0으로 드러난다"
    - "한 이용권의 이력 수량 합계를 DB에서 한 번의 질의로 얻을 수 있다"
  artifacts:
    - path: "src/main/resources/db/migration/V4__create_pass_tables.sql"
      provides: "이용권 3테이블 + CHECK 제약 + 인덱스"
      contains: "ck_pass_remaining_count_by_type"
    - path: "src/main/kotlin/com/goldwrestling/pass/Pass.kt"
      provides: "이용권 단일 엔티티 (PassType 판별 컬럼, D-060)"
      contains: "@Enumerated(EnumType.STRING)"
    - path: "src/main/kotlin/com/goldwrestling/pass/PassRepository.kt"
      provides: "조건부 잔여 갱신 쿼리"
      contains: "adjustRemainingCount"
    - path: "src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt"
      provides: "커스텀 쿼리·CHECK 제약 Testcontainers 통합테스트"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/pass/Pass.kt"
      to: "src/main/resources/db/migration/V4__create_pass_tables.sql"
      via: "ddl-auto=validate 기동 시 매핑 검증"
      pattern: "@Table\\(name = \"pass\"\\)"
    - from: "src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/Pass.kt"
      via: "@ManyToOne(fetch = LAZY) pass FK"
      pattern: "JoinColumn\\(name = \"pass_id\""
---

<objective>
이용권 3테이블 스키마(V4)와 엔티티·리포지토리를 만든다. **도메인 판정 메서드는 넣지 않는다** —
그건 03-03~03-05의 TDD 플랜이 테스트를 먼저 쓰고 채운다.

Purpose: 이후 모든 플랜이 이 위에 쌓인다. 특히 `PassRepository.adjustRemainingCount`(조건부 UPDATE)는
Phase 4의 예약 차감이 그대로 재사용할 경로이므로(D-021), 관리자 단독 조작인 이번 phase에서도
처음부터 DB가 마지막 방어선이 되는 형태로 만든다.

Output: `V4__create_pass_tables.sql`, enum 5종, 엔티티 3종, 리포지토리 3종, `PassRepositoryTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-migration/SKILL.md
@.claude/skills/add-domain-test/SKILL.md
@.planning/phases/03-pass/03-PATTERNS.md
@.planning/phases/03-pass/03-RESEARCH.md
@.planning/phases/03-pass/03-CONTEXT.md
@docs/conventions.md
@docs/glossary.md
@docs/decisions.md
</context>

<interfaces>
<!-- 그대로 본뜰 기존 계약. 이 값들로 충분하므로 코드베이스를 더 뒤지지 않는다. -->

`member/Member.kt` — 엔티티 선언 형식의 원본:
- `class Member(@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "branch_id", nullable = false) val branch: Branch, ..., @Column(name = "created_at", nullable = false, updatable = false) val createdAt: OffsetDateTime) { @Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null }`
- `data class` 아님 / `@Id`는 생성자 밖 / `createdAt`은 서비스가 `Clock`으로 채움 (D-039)

`auth/RefreshTokenRepository.kt` 31-37줄 — 조건부 벌크 UPDATE의 원본:
- `@Modifying(flushAutomatically = true, clearAutomatically = true)`
- `@Query("update RefreshToken rt set rt.revokedAt = :now where rt.id = :id and rt.revokedAt is null")`
- `fun revokeIfUsable(@Param("id") id: Long, @Param("now") now: OffsetDateTime): Int` — 반환 0 = 경쟁 패배

`member/MemberRepository.kt`:
- `interface MemberRepository : JpaRepository<Member, Long>, JpaSpecificationExecutor<Member>`

`admin/Admin.kt`, `branch/Branch.kt` — FK 대상 엔티티 (이미 존재)

테스트 애노테이션 조합 (`member/MemberSearchTest.kt` 35-38줄) — **한 글자도 바꾸지 않는다**:
- `@SpringBootTest` / `@AutoConfigureMockMvc` / `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)` / `@Transactional`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: enum 5종 + V4 마이그레이션</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/PassType.kt,
    src/main/kotlin/com/goldwrestling/pass/PassStatus.kt,
    src/main/kotlin/com/goldwrestling/pass/PassDisplayStatus.kt,
    src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt,
    src/main/kotlin/com/goldwrestling/pass/EveningMembershipTerm.kt,
    src/main/resources/db/migration/V4__create_pass_tables.sql
  </files>
  <read_first>
    - .claude/skills/add-migration/SKILL.md (전체 — 금지사항, §2 스키마 규약, §3 동시성 제약, §4 검증)
    - src/main/resources/db/migration/V3__add_auth_credentials_and_refresh_token.sql (전체 — 주석·FK 이름·CHECK·인덱스 관례)
    - src/main/resources/db/migration/V2__create_branch_member_admin.sql (전체 — branch/member/admin 컬럼과 PK 형식)
    - src/main/kotlin/com/goldwrestling/member/MemberStatus.kt (enum KDoc 관례)
    - docs/glossary.md "차감 사유(TransactionReason)" 표 + 03-01이 추가한 "이용권 (Phase 3)" 표
    - docs/conventions.md §5(시간 타입), §9(Flyway)
    - .planning/phases/03-pass/03-RESEARCH.md "Architecture Patterns 3", "Common Pitfalls 5·6"
  </read_first>
  <action>
    (1) `pass/` 패키지에 enum 5개를 만든다. 각 상수에 `/** 한 줄 설명(근거 정책 조항) */` KDoc을 붙이는
    `MemberStatus.kt` 관례를 유지한다. 이름은 glossary 등재값을 그대로 쓴다.
    · `PassType` — `EVENING_MEMBERSHIP`, `SESSION_PASS`, `LESSON_PASS`
    · `PassStatus` — `ACTIVE`, `CANCELED` (저장값 2종만, D-064)
    · `PassDisplayStatus` — `USABLE`, `EXPIRED`, `EXHAUSTED`, `CANCELED` (저장하지 않는 계산값, D-064)
    · `TransactionReason` — glossary 8종 전부 선언한다: `RESERVE`, `CANCEL_REFUND`, `ADMIN_ADJUST`,
      `EVENING_HALF`, `INACTIVITY`, `CLASS_CANCELED_REFUND`, `INITIAL_GRANT`, `REGISTRATION_CANCELED`.
      **이번 phase가 실제로 쓰는 건 `INITIAL_GRANT`·`ADMIN_ADJUST`·`REGISTRATION_CANCELED` 3종뿐**이라는 사실을
      enum KDoc에 적는다(나머지는 Phase 4·5·6에서 사용).
    · `EveningMembershipTerm` — `ONE_MONTH`, `THREE_MONTHS`, `SIX_MONTHS`. 각 상수가 `val months: Long`
      프로퍼티(1/3/6)를 갖게 해 등록 시 `startDate.plusMonths(term.months)` 계산에 그대로 쓰이게 한다.
      **DB에 저장하지 않는다**(D-063) — 이 사실을 KDoc에 명시한다.

    (2) `src/main/resources/db/migration/V4__create_pass_tables.sql`을 만든다. 작성 전에
    `ls src/main/resources/db/migration/`으로 최신 번호가 V3인지 재확인한다(Pitfall 6). 파일 상단에
    이 스키마가 근거한 결정(D-055~D-067)을 한글 주석으로 남기는 V2·V3 관례를 유지한다.

    `pass` 테이블 — 컬럼: `id`(BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY), `member_id`(BIGINT NOT NULL),
    `branch_id`(BIGINT NOT NULL, D-067), `type`(VARCHAR(20) NOT NULL), `status`(VARCHAR(20) NOT NULL),
    `start_date`(DATE NOT NULL), `end_date`(DATE NOT NULL), `remaining_count`(DECIMAL(4,1), 횟수제만 채움),
    `registered_by_admin_id`(BIGINT NOT NULL), `canceled_at`(TIMESTAMPTZ), `cancel_reason`(VARCHAR(500)),
    `canceled_by_admin_id`(BIGINT), `created_at`(TIMESTAMPTZ NOT NULL DEFAULT now()).
    제약 — FK 4개에 명시적 이름(`fk_pass_member`, `fk_pass_branch`, `fk_pass_registered_by_admin`,
    `fk_pass_canceled_by_admin`), 그리고 CHECK 3개:
    · `ck_pass_remaining_count_by_type` — `EVENING_MEMBERSHIP`이면 `remaining_count IS NULL`,
      `SESSION_PASS`/`LESSON_PASS`면 `remaining_count IS NOT NULL`
    · `ck_pass_period` — `end_date >= start_date`
    · `ck_pass_cancellation` — `status = 'CANCELED'`면 `canceled_at`·`cancel_reason`·`canceled_by_admin_id`가
      전부 NOT NULL, 아니면 셋 다 NULL
    인덱스 — `idx_pass_member ON pass (member_id)`.
    **`ck_refresh_token_principal`류의 "주체 중 정확히 하나" CHECK는 넣지 않는다**(Pitfall 5) —
    이번 phase의 사유 코드는 전부 관리자 주체이고, Phase 5의 배치 차감은 주체가 없어 그 제약이 나중에 깨진다.

    `pass_transaction` 테이블 — `id`, `pass_id`(BIGINT NOT NULL), `amount`(DECIMAL(4,1) NOT NULL, 부호 포함),
    `reason`(VARCHAR(30) NOT NULL), `note`(VARCHAR(500), D-061), `admin_id`(BIGINT NOT NULL),
    `occurred_at`(TIMESTAMPTZ NOT NULL). FK `fk_pass_transaction_pass`·`fk_pass_transaction_admin`,
    CHECK `ck_pass_transaction_amount_nonzero`(`amount <> 0`, D-065),
    인덱스 `idx_pass_transaction_pass ON pass_transaction (pass_id)` (이력 필터 D-058이 이 인덱스를 탄다).

    `pass_period_change` 테이블 — `id`, `pass_id`(BIGINT NOT NULL), `previous_start_date`(DATE NOT NULL),
    `previous_end_date`(DATE NOT NULL), `new_start_date`(DATE NOT NULL), `new_end_date`(DATE NOT NULL),
    `reason`(VARCHAR(500) NOT NULL), `admin_id`(BIGINT NOT NULL), `occurred_at`(TIMESTAMPTZ NOT NULL).
    FK 2개 + CHECK `ck_pass_period_change_period`(`new_end_date >= new_start_date`),
    인덱스 `idx_pass_period_change_pass`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"` 종료 코드 0 (빈 DB에서 V1~V4 전부 재생됨)
    - `src/main/resources/db/migration/V4__create_pass_tables.sql`이 `CREATE TABLE pass`, `CREATE TABLE pass_transaction`, `CREATE TABLE pass_period_change` 3개를 모두 포함한다
    - 같은 파일이 `ck_pass_remaining_count_by_type`, `ck_pass_period`, `ck_pass_cancellation`, `ck_pass_transaction_amount_nonzero`, `ck_pass_period_change_period` 5개 제약 이름을 포함한다
    - 같은 파일이 `member_id` 이외의 주체 컬럼(`ck_..._principal` 형태의 "정확히 하나" CHECK)을 포함하지 않는다
    - V1~V3 마이그레이션 파일의 내용이 변경되지 않았다 (`git diff --name-only`에 V1/V2/V3가 없다)
    - enum 5개 파일이 각각 존재하고 `TransactionReason`이 8개 상수를 모두 선언한다
  </acceptance_criteria>
  <done>빈 DB에서 마이그레이션이 전부 적용되고, 타입별 컬럼 규칙이 애플리케이션이 아니라 DB에서 강제된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 엔티티 3종 (필드·매핑만)</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/Pass.kt,
    src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt,
    src/main/kotlin/com/goldwrestling/pass/PassPeriodChange.kt
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/Member.kt (전체 — 엔티티 선언 형식의 원본)
    - src/main/kotlin/com/goldwrestling/auth/RefreshToken.kt (전체 — 이력성 엔티티의 val/var 구분)
    - src/main/resources/db/migration/V4__create_pass_tables.sql (Task 1 산출물 — 컬럼명·nullable을 여기에 맞춘다)
    - docs/conventions.md §3(엔티티), §4(BigDecimal), §5(시간 타입)
    - .planning/phases/03-pass/03-PATTERNS.md "pass/Pass.kt", "pass/PassTransaction.kt, pass/PassPeriodChange.kt"
  </read_first>
  <behavior>
    이 태스크의 검증은 매핑 정합성이다 — `ddl-auto=validate` 기동이 통과하면 컬럼명·타입·nullable이 맞은 것이다.
    - `./gradlew test`가 컨텍스트를 띄우는 순간 Hibernate가 pass·pass_transaction·pass_period_change 매핑을 검증한다
    - Kotlin nullability와 DB NOT NULL이 어긋나면 validate가 잡지 못하는 버그가 되므로(conventions §3) 수동으로 대조한다
  </behavior>
  <action>
    `Pass.kt` — `@Entity @Table(name = "pass")`, `data class` 금지. 생성자 파라미터에 컬럼을 나열하고
    `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null`은 클래스 본문에 둔다.
    · `member: Member` / `branch: Branch` / `registeredBy: Admin` — 전부 `@ManyToOne(fetch = FetchType.LAZY)` +
      `@JoinColumn(name = "member_id" | "branch_id" | "registered_by_admin_id", nullable = false)`, `val`
    · `type: PassType`(val) / `status: PassStatus`(var) — 둘 다 `@Enumerated(EnumType.STRING)` + `@Column(nullable = false, length = 20)`
    · `startDate: LocalDate`(var) / `endDate: LocalDate`(var) — `@Column(name = "start_date" | "end_date", nullable = false)`
    · `remainingCount: BigDecimal?`(var) — `@Column(name = "remaining_count", precision = 4, scale = 1)`
    · `canceledAt: OffsetDateTime? = null`(var) / `cancelReason: String? = null`(var, length = 500) /
      `canceledBy: Admin? = null`(var, `@ManyToOne(fetch = LAZY) @JoinColumn(name = "canceled_by_admin_id")`)
      — `Member.rejectionReason`처럼 기본값 있는 생성자 파라미터로 둔다
    · `createdAt: OffsetDateTime`(val) — `@Column(name = "created_at", nullable = false, updatable = false)`
    클래스 KDoc에 "저장 상태는 ACTIVE/CANCELED 2종뿐이고 만료·소진은 조회 시점 계산이다(D-064)",
    "도메인 판정 메서드는 03-03~03-05에서 테스트를 먼저 쓰고 추가한다"를 적는다.

    `PassTransaction.kt` — `@Entity @Table(name = "pass_transaction")`. **append-only이므로 모든 필드를 `val`로**
    선언해 이력 불변성을 코드로 보장한다(setter 없음). 필드: `pass: Pass`(LAZY, nullable=false),
    `amount: BigDecimal`(precision=4, scale=1, nullable=false), `reason: TransactionReason`(EnumType.STRING, length=30),
    `note: String?`(length=500), `admin: Admin`(LAZY, nullable=false), `occurredAt: OffsetDateTime`(name="occurred_at").

    `PassPeriodChange.kt` — `@Entity @Table(name = "pass_period_change")`. 마찬가지로 전 필드 `val`.
    필드: `pass: Pass`(LAZY), `previousStartDate`/`previousEndDate`/`newStartDate`/`newEndDate`(LocalDate, NOT NULL),
    `reason: String`(length=500, nullable=false), `admin: Admin`(LAZY), `occurredAt: OffsetDateTime`.

    세 엔티티 모두 `LocalDateTime`을 쓰지 않는다 — 타임스탬프는 `OffsetDateTime`(timestamptz), 날짜는 `LocalDate`(date)다(conventions §5).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.db.FlywayMigrationIntegrationTest"` 종료 코드 0 (ddl-auto=validate가 세 엔티티 매핑을 통과)
    - `grep -c "fetch = FetchType.LAZY" src/main/kotlin/com/goldwrestling/pass/Pass.kt` >= 4
    - `Pass.kt`가 `@Enumerated(EnumType.STRING)`을 2회 이상 포함한다 (type, status)
    - `PassTransaction.kt`·`PassPeriodChange.kt`에 `var ` 선언이 0건이다 (append-only)
    - 세 파일 어디에도 `LocalDateTime` import가 없다
    - `Pass.kt`에 `data class` 선언이 없다
  </acceptance_criteria>
  <done>엔티티 매핑이 V4 스키마와 정확히 대응하고, 이력 엔티티는 생성 후 변경할 수 없다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 리포지토리 3종 + 커스텀 쿼리 통합테스트</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/PassRepository.kt,
    src/main/kotlin/com/goldwrestling/pass/PassTransactionRepository.kt,
    src/main/kotlin/com/goldwrestling/pass/PassPeriodChangeRepository.kt,
    src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/auth/RefreshTokenRepository.kt (전체 — 조건부 UPDATE 3종과 KDoc이 설명하는 flush/clear 이유)
    - src/main/kotlin/com/goldwrestling/auth/TokenService.kt 106-152줄 (반환값 0 처리 관례, 준영속 함정 회피)
    - src/main/kotlin/com/goldwrestling/member/MemberRepository.kt (JpaSpecificationExecutor 병행 상속)
    - src/test/kotlin/com/goldwrestling/member/MemberSearchTest.kt 1-70줄 (애노테이션 조합·픽스처 헬퍼·resetClock)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt (Task 2 산출물 — 필드명이 JPQL 경로가 된다)
    - docs/conventions.md §10.0 표("리포지토리 커스텀 쿼리 → Testcontainers 통합테스트 필수"), §10.1
    - .planning/phases/03-pass/03-PATTERNS.md "pass/PassRepository.kt — 조건부 UPDATE"
  </read_first>
  <behavior>
    - `adjustRemainingCount(id, +0.5)`는 1을 반환하고 잔여가 0.5 늘어난다
    - `adjustRemainingCount(id, -1.0)`이 결과를 음수로 만들면 **0을 반환하고 잔여가 그대로**다 (DB가 마지막 방어선)
    - 잔여를 정확히 0으로 만드는 가감(잔여 1.0에 -1.0)은 성공한다 (경계는 `>= 0`이지 `> 0`이 아니다)
    - `zeroRemainingCount(id, expected = 0.5)`는 DB 잔여가 `0.50`으로 저장돼 있어도 1을 반환한다 (SQL 수치 비교, D-016 주의점의 반대편)
    - `zeroRemainingCount(id, expected)`가 실제 잔여와 다르면 0을 반환하고 값이 그대로다
    - `sumAmountByPassId`는 이력이 없으면 0을 반환한다 (null이 아니다)
    - 기간제(`EVENING_MEMBERSHIP`)에 `remainingCount`를 채워 저장하면 DB CHECK가 저장을 거부한다
  </behavior>
  <action>
    (1) `PassRepository.kt` — `interface PassRepository : JpaRepository<Pass, Long>, JpaSpecificationExecutor<Pass>`.
    파생 쿼리 `fun findAllByMemberIdOrderByStartDateDescIdDesc(memberId: Long): List<Pass>` 와
    `fun findAllByMemberIdAndStatusNotOrderByStartDateDescIdDesc(memberId: Long, status: PassStatus): List<Pass>` 를 둔다
    (각각 관리자 목록·회원 목록이 쓴다). 여기에 커스텀 쿼리 2개를 추가한다 —
    둘 다 `@Modifying(flushAutomatically = true, clearAutomatically = true)`를 붙이고,
    `RefreshTokenRepository.revokeIfUsable`의 KDoc이 설명하는 flush/clear 이유를 이 도메인 문장으로 다시 적는다.
    · `adjustRemainingCount(id, amount): Int` — JPQL `update Pass p set p.remainingCount = p.remainingCount + :amount
      where p.id = :id and p.status = ACTIVE and p.remainingCount + :amount >= 0`
      (`status` 조건은 취소된 이용권이 조건부 갱신 경로로 되살아나는 것을 DB에서 막는다)
    · `zeroRemainingCount(id, expectedRemaining): Int` — JPQL `update Pass p set p.remainingCount = 0
      where p.id = :id and p.remainingCount = :expectedRemaining` (등록 취소의 잔여 0 상쇄에 쓴다)
    반환 0의 의미("경쟁에서 졌거나 사전 판정 이후 상태가 바뀜 → 서비스가 재조회해 정확한 예외로 변환")를 KDoc에 남긴다.

    (2) `PassTransactionRepository.kt` — `JpaRepository<PassTransaction, Long>` + `JpaSpecificationExecutor<PassTransaction>`
    (이력 조회 필터가 03-10에서 Specification을 쓴다). 집계 쿼리
    `@Query("select coalesce(sum(t.amount), 0) from PassTransaction t where t.pass.id = :passId")`
    `fun sumAmountByPassId(@Param("passId") passId: Long): BigDecimal` 를 둔다 — `coalesce`가 없으면
    이력이 없는 이용권에서 null이 돌아와 "잔여 = 이력 합계" 검증이 NPE로 죽는다.

    (3) `PassPeriodChangeRepository.kt` — `JpaRepository<PassPeriodChange, Long>` +
    `fun findAllByPassIdOrderByOccurredAtDesc(passId: Long): List<PassPeriodChange>`.

    (4) `src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt` — `MemberSearchTest`와 **동일한 애노테이션
    조합**(`@SpringBootTest`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`,
    `@Transactional`)을 쓴다. 조합이 다르면 스프링이 컨텍스트를 새로 띄워 테스트 전체가 느려진다(conventions §10.1).
    `@BeforeEach`에서 `(clock as MutableTestClock).setTo(Instant.now())`로 되돌리는 관례도 그대로 가져온다.
    위 `<behavior>` 7개를 각각 백틱 한국어 서술문 테스트 메서드로 쓴다. 하단에 `persistBranch()`/`persistMember()`/
    `persistAdmin()`/`persistPass(type, remaining)` 픽스처 팩토리 함수를 둔다.
    BigDecimal 비교는 전부 `isEqualByComparingTo`를 쓴다 — `isEqualTo`는 `0.5 != 0.50`으로 실패한다(D-016).
    CHECK 제약 위반 테스트는 `saveAndFlush`로 즉시 flush시켜 `DataIntegrityViolationException`을 받는다
    (커밋 시점까지 미루면 어느 문장이 터졌는지 알 수 없고, 같은 테스트 메서드에서 이후 단언을 못 한다).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.pass.PassRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.pass.PassRepositoryTest"` 종료 코드 0
    - `PassRepositoryTest.kt`에 `@Test` 메서드가 7개 이상 존재한다
    - `PassRepository.kt`가 `@Modifying(flushAutomatically = true, clearAutomatically = true)`를 2회 포함한다
    - `PassRepository.kt`의 `adjustRemainingCount` JPQL이 `>= 0` 조건과 `status` 조건을 모두 포함한다
    - `PassTransactionRepository.kt`가 `coalesce(sum(t.amount), 0)`를 포함한다
    - `PassRepositoryTest.kt`에 `isEqualTo(BigDecimal` 형태의 단언이 0건이다 (전부 `isEqualByComparingTo`)
    - `./gradlew ktlintCheck` 종료 코드 0
  </acceptance_criteria>
  <done>잔여 갱신이 애플리케이션 조건문이 아니라 DB 술어로 막히고, 그 동작이 실제 PostgreSQL에서 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 → DB | 잔여 횟수·상태 변경이 이 경계를 넘는다. 동시 요청은 애플리케이션 조건문으로 막을 수 없다 |
| 마이그레이션 → 운영 DB | 스키마 제약이 여기서 확정되며, 커밋 후에는 수정할 수 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `PassRepository.adjustRemainingCount` | mitigate | JPQL `where` 절에 `p.remainingCount + :amount >= 0` 술어를 두어 결과 음수를 DB가 거부한다. 애플리케이션 사전 판정은 사용자 안내용일 뿐 방어선이 아니다 (D-021) |
| T-03-05 | Tampering | 취소된 이용권 재조작 | mitigate | `adjustRemainingCount` JPQL에 `p.status = ACTIVE` 조건을 포함해, 서비스 가드를 우회해도 잔여가 바뀌지 않는다 |
| T-03-06 | Tampering | 타입별 컬럼 규칙 우회 | mitigate | `ck_pass_remaining_count_by_type` CHECK로 기간제에 잔여를 넣거나 횟수제 잔여를 비우는 저장을 DB가 거부 |
| T-03-07 | Repudiation | 취소 메타데이터 누락 | mitigate | `ck_pass_cancellation` CHECK로 `status = 'CANCELED'`인 행은 취소 시각·사유·주체가 반드시 채워지게 강제 |
| T-03-08 | Tampering | 의미 없는 0 수량 이력 | mitigate | `ck_pass_transaction_amount_nonzero`로 `amount = 0` 행을 차단 (D-065와 정합) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 신규 외부 패키지 설치 없음 (RESEARCH.md "Package Legitimacy Audit — 해당 없음") |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `git diff --name-only`에 V1·V2·V3 마이그레이션이 나타나지 않는다 (커밋된 마이그레이션 수정 금지)
- `docker compose`가 뜬 상태에서 애플리케이션 기동 시 `ddl-auto=validate`가 실패하지 않는다
</verification>

<success_criteria>
- 이용권 3테이블이 Flyway로 생성되고 엔티티 매핑이 validate를 통과
- 타입별 컬럼 규칙·취소 메타데이터 완전성·0 수량 금지가 DB 제약으로 강제됨
- 잔여 갱신 조건부 UPDATE 2종과 이력 합계 집계가 실제 PostgreSQL 통합테스트로 증명됨
</success_criteria>

<output>
Create `.planning/phases/03-pass/03-02-SUMMARY.md` when done.
커밋은 사용자가 명시적으로 요청할 때만 실행한다 (CLAUDE.md 커밋 규칙).
</output>
