---
phase: 03-pass
plan: 10
type: execute
wave: 9
depends_on: [03-09, 03-06]
files_modified:
  - src/main/kotlin/com/goldwrestling/pass/PassTransactionSpecifications.kt
  - src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionResponse.kt
  - src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionSearchCondition.kt
  - src/main/kotlin/com/goldwrestling/pass/MemberPassService.kt
  - src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt
  - src/test/kotlin/com/goldwrestling/pass/MemberPassControllerTest.kt
  - src/test/kotlin/com/goldwrestling/pass/MemberPassTransactionControllerTest.kt
  - docs/api/openapi.yaml
  - docs/decisions.md
autonomous: true
requirements: [PASS-05, PASS-06]

must_haves:
  truths:
    - "회원이 본인이 보유한 모든 이용권의 잔여 횟수·유효기간을 조회할 수 있다"
    - "만료·소진된 이용권도 목록에 나오고 상태로 구분된다"
    - "취소된 이용권은 회원 목록에서 보이지 않는다"
    - "회원이 본인의 차감/복구 이력을 이용권별 필터와 페이지 단위로 조회할 수 있다"
    - "다른 회원의 이용권·이력은 어떤 파라미터 조작으로도 조회되지 않는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt"
      provides: "GET /api/members/me/passes, GET /api/members/me/pass-transactions"
      contains: "@AuthenticationPrincipal"
    - path: "src/main/kotlin/com/goldwrestling/pass/MemberPassService.kt"
      provides: "본인 스코프 조회 (requireMemberId + MemberStateGate)"
      contains: "requireMemberId()"
    - path: "src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionSearchCondition.kt"
      provides: "@ParameterObject 쿼리 조건 DTO (D-054)"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt"
      to: "src/main/kotlin/com/goldwrestling/auth/AuthenticatedPrincipal.kt"
      via: "경로가 아닌 토큰 주체에서만 memberId를 얻는다 (IDOR 방지)"
      pattern: "@AuthenticationPrincipal principal: AuthenticatedPrincipal"
    - from: "src/main/kotlin/com/goldwrestling/pass/MemberPassService.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/PassTransactionSpecifications.kt"
      via: "ownedByMember 스코프 + hasPassId 필터 조합"
      pattern: "Specification\\.allOf"
---

<objective>
회원 본인 이용권 조회(PASS-05)와 본인 차감/복구 이력 조회(PASS-06)를 만든다.
두 엔드포인트 모두 **경로·쿼리로 memberId를 받지 않고** 토큰 주체에서만 얻는다.

Purpose: 이 phase에서 회원이 직접 호출하는 유일한 경로이고, 다른 회원의 잔여·이력이 새어나갈 수 있는
유일한 표면이다(IDOR). "다른 회원의 것을 조회하는 경로를 아예 만들지 않는" `MemberProfileController`의
방식을 그대로 따른다.

Output: `PassTransactionSpecifications`, 응답·조건 DTO 2종, `MemberPassService`, `MemberPassController`,
통합테스트 2파일, 재생성된 `openapi.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@docs/requirements.md
@docs/conventions.md
@docs/decisions.md
@.planning/phases/03-pass/03-CONTEXT.md
@.planning/phases/03-pass/03-PATTERNS.md
@.planning/phases/03-pass/03-RESEARCH.md
</context>

<interfaces>
`auth/AuthenticatedPrincipal.kt`: `fun requireMemberId(): Long`

`member/MemberStateGate.kt`: `fun requireActive(principal: AuthenticatedPrincipal)` — `ACTIVE`가 아니면 `MemberNotActiveException`(403 `MEMBER_NOT_ACTIVE`)

`member/dto/PageResponse.kt`:
- `data class PageResponse<T>(content, page, size, totalElements, totalPages)`
- `companion object { fun <E : Any, T> from(page: Page<E>, mapper: (E) -> T): PageResponse<T> }`

`member/dto/MemberSearchCondition.kt` — 쿼리 조건 DTO의 원본:
- `@field:Min(0) val page: Int = 0`, `@field:Min(1) @field:Max(100) val size: Int = 20`

`member/AdminMemberController.kt` 37-48줄 — `@ParameterObject @ModelAttribute @Valid` 사용 형태 (D-054)

`member/MemberSpecifications.kt` — `Specification.allOf(listOfNotNull(...))` + 조건 없으면 null 반환 관례

`pass/PassRepository.kt`: `fun findAllByMemberIdAndStatusNotOrderByStartDateDescIdDesc(memberId: Long, status: PassStatus): List<Pass>`

`pass/dto/PassResponse.kt`: `fun from(pass: Pass, today: LocalDate): PassResponse`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 이력 조회용 Specification + 응답·조건 DTO</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/PassTransactionSpecifications.kt,
    src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionResponse.kt,
    src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionSearchCondition.kt,
    docs/decisions.md
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt (전체 — `Specification.allOf` 관례, 조건 없으면 null, `object` 싱글턴)
    - src/main/kotlin/com/goldwrestling/member/dto/MemberSearchCondition.kt (전체 — page/size 제약)
    - src/main/kotlin/com/goldwrestling/member/dto/MemberSummaryResponse.kt (목록용 응답 DTO — 민감정보 최소화 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (필드명이 Specification 경로가 된다)
    - docs/decisions.md D-035, D-043, D-054, D-058
    - docs/requirements.md §3.2 (본인 조회 — "언제, 무슨 사유로 몇 회")
  </read_first>
  <behavior>
    - `PassTransactionSpecifications.ownedByMember(7)`는 `pass.member.id = 7` 조건을 만든다 (조인 경로)
    - `hasPassId(null)`은 null을 반환한다 (조건 없음)
    - `PassTransactionResponse.from(tx)`는 `note`를 담지 않는다 (관리자 메모 비노출)
  </behavior>
  <action>
    (1) `PassTransactionSpecifications.kt` — `object`로 선언한다. **파일명이 `PassSpecifications`가 아닌 이유**를
    KDoc에 한 줄 남긴다: 이 Specification들은 `Pass`가 아니라 `PassTransaction`을 필터한다.
    · `fun ownedByMember(memberId: Long): Specification<PassTransaction>` — **null을 반환하지 않는 필수 조건**이다.
      `root.get<Pass>("pass").get<Member>("member").get<Long>("id")` 경로로 비교한다. 이 조건이 IDOR 방어의 핵심이므로
      "호출부가 이 조건을 빼먹을 수 없도록 nullable로 만들지 않는다"를 KDoc에 명시한다
    · `fun hasPassId(passId: Long?): Specification<PassTransaction>?` — null이면 조건 없음(`MemberSpecifications.hasStatus` 관례 그대로)

    (2) `PassTransactionResponse` — 필드: `transactionId`, `passId`, `passType: PassType`, `amount: BigDecimal`,
    `reason: TransactionReason`, `occurredAt: OffsetDateTime`. `companion object { fun from(transaction: PassTransaction): PassTransactionResponse }`.
    **`note`(관리자 자유 텍스트 사유)를 응답에 담지 않는다** — requirements §3.2의 "무슨 사유로"는 `reason` 코드가 답하고,
    관리자 메모는 내부 정보다(D-043 "거절 사유 원문은 회원에게 노출하지 않는다"의 연장).
    **이 규칙은 계획 단계에서 사용자가 확정했다 (2026-08-03, plan-phase AskUserQuestion — "회원에게 비노출" 선택).**
    이 판단을 `docs/decisions.md`에 **D-070**으로 3~4줄 기록한다 (사용자 확정 사실 포함).
    `pass`·`passType` 접근은 LAZY 연관이므로 변환이 트랜잭션 안에서 일어나야 한다는 경고를 KDoc에 남긴다.
    모든 필드에 `@field:Schema`를 붙인다.

    (3) `PassTransactionSearchCondition` — `passId: Long? = null`, `@field:Min(0) page: Int = 0`,
    `@field:Min(1) @field:Max(100) size: Int = 20` (D-035·D-058, `MemberSearchCondition`과 동일한 제약).
    `passId` 스키마 설명에 "본인 이용권이 아니면 결과가 비어 있다"를 적는다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileKotlin` 종료 코드 0
    - `PassTransactionSpecifications.kt`의 `ownedByMember` 반환 타입이 nullable이 아니다 (`Specification<PassTransaction>`)
    - `PassTransactionResponse.kt`에 `note` 필드가 없다
    - `PassTransactionSearchCondition.kt`가 `@field:Min`·`@field:Max` 를 포함한다
    - `docs/decisions.md`에 `D-070` 항목이 존재한다
  </acceptance_criteria>
  <done>본인 스코프 조건이 빠뜨릴 수 없는 형태로 만들어지고, 회원 응답에 관리자 메모가 섞이지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberPassService + MemberPassController + openapi 재생성</name>
  <files>
    src/main/kotlin/com/goldwrestling/pass/MemberPassService.kt,
    src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt,
    docs/api/openapi.yaml
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/MemberProfileService.kt (전체 — `principal.requireMemberId()`만으로 스코프를 얻는 형태)
    - src/main/kotlin/com/goldwrestling/member/MemberProfileController.kt (전체 — 20-27줄 IDOR 방지 KDoc, `/me` 고정 경로)
    - src/main/kotlin/com/goldwrestling/member/MemberStateGate.kt (`requireActive` 시그니처와 운영 규칙 KDoc)
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt 37-48줄 (`@ParameterObject @ModelAttribute @Valid`)
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt 35-47줄 (Specification + PageRequest + PageResponse 변환)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt, PassTransactionRepository.kt
    - docs/decisions.md D-040, D-054, D-058
    - .claude/skills/add-endpoint/SKILL.md §1, §3, §4, §6
  </read_first>
  <behavior>
    - `GET /api/members/me/passes` → 본인 이용권 배열. `status != CANCELED`만 포함, 만료·소진은 포함하고 `displayStatus`로 구분
    - `GET /api/members/me/pass-transactions` → `PageResponse<PassTransactionResponse>`, `occurredAt` 내림차순
    - `passId` 파라미터가 타인의 이용권을 가리키면 결과가 비어 있다
    - `ACTIVE`가 아닌 회원 → 403 `MEMBER_NOT_ACTIVE`
  </behavior>
  <action>
    (1) `MemberPassService` — `@Service @Transactional(readOnly = true)`. 변경 메서드가 없으므로 `@Transactional` 오버라이드가 없다.
    · `fun getMyPasses(principal: AuthenticatedPrincipal): List<PassResponse>` —
      `memberStateGate.requireActive(principal)` → `val memberId = principal.requireMemberId()` →
      `passRepository.findAllByMemberIdAndStatusNotOrderByStartDateDescIdDesc(memberId, PassStatus.CANCELED)` →
      `PassResponse.from(pass, LocalDate.now(clock))` 변환(트랜잭션 안).
      **취소만 제외하고 만료·소진은 제외하지 않는다**(D-058) — 이 사실을 KDoc에 명시하고,
      관리자 목록(`AdminPassService.getMemberPasses`)은 취소도 포함한다는 대비를 함께 적는다
    · `fun getMyTransactions(principal: AuthenticatedPrincipal, condition: PassTransactionSearchCondition): PageResponse<PassTransactionResponse>` —
      게이트·memberId 획득은 위와 동일. `Specification.allOf(listOfNotNull(
      PassTransactionSpecifications.ownedByMember(memberId), PassTransactionSpecifications.hasPassId(condition.passId)))`,
      `PageRequest.of(condition.page, condition.size, Sort.by(Sort.Direction.DESC, "occurredAt"))`,
      `passTransactionRepository.findAll(spec, pageable)` → `PageResponse.from(page) { PassTransactionResponse.from(it) }`.
      **`condition.passId`를 소유권 검사 없이 그대로 쓰는 것이 안전한 이유**(`ownedByMember`가 항상 AND로 붙는다)를 KDoc에 남긴다

    (2) `MemberPassController` — `@RestController @RequestMapping("/api/members/me")`.
    · `@GetMapping("/passes")` — `(@AuthenticationPrincipal principal: AuthenticatedPrincipal)`,
      `@Operation(summary = "본인 이용권 목록 조회 (만료·소진 포함, 취소 제외)")`
    · `@GetMapping("/pass-transactions")` — `(@AuthenticationPrincipal principal, @ParameterObject @ModelAttribute @Valid condition: PassTransactionSearchCondition)`,
      `@Operation(summary = "본인 차감·복구 이력 조회 (이용권별 필터, 페이지네이션)")`.
      **`@ParameterObject`를 빠뜨리면** openapi.yaml이 객체 1개 파라미터로 생성돼 FE 바인딩이 깨진다(D-054, 실제 재현된 버그 WR-06)
    클래스 KDoc에 `MemberProfileController` 20-27줄과 같은 취지를 적는다 —
    **경로에 `memberId`를 받지 않는다. 다른 회원의 이용권을 조회하는 경로를 아예 만들지 않는 것이 IDOR을 막는 방법이다.**
    권한 애노테이션·`@Transactional`·`try-catch`를 붙이지 않는다(D-040·D-020·D-017).

    (3) `docker-compose up -d` 후 `./gradlew generateApiDocs`로 `openapi.yaml`을 재생성하고,
    **`pass-transactions` 경로의 파라미터가 `passId`·`page`·`size` 개별 쿼리 파라미터로 펼쳐졌는지** diff에서 확인한다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileKotlin` 종료 코드 0
    - `MemberPassController.kt`에 `@PathVariable` 이 0건이다 (memberId를 경로로 받지 않는다)
    - `MemberPassController.kt`가 `@ParameterObject` 와 `@ModelAttribute` 를 함께 사용한다
    - `MemberPassService.kt`가 `memberStateGate.requireActive(` 를 2회 호출한다 (두 조회 모두)
    - `MemberPassService.kt`가 `PassStatus.CANCELED` 를 제외 조건으로 사용한다
    - `docs/api/openapi.yaml`에 `/api/members/me/passes` 와 `/api/members/me/pass-transactions` 경로가 존재한다
    - `openapi.yaml`의 `pass-transactions` 경로에 `name: passId`, `name: page`, `name: size` 파라미터가 각각 존재한다
  </acceptance_criteria>
  <done>회원 본인 조회 두 경로가 열리고, 타인 데이터에 닿을 수 있는 입력 자체가 API 표면에 존재하지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 회원 조회 통합테스트 2파일</name>
  <files>
    src/test/kotlin/com/goldwrestling/pass/MemberPassControllerTest.kt,
    src/test/kotlin/com/goldwrestling/pass/MemberPassTransactionControllerTest.kt
  </files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/member/MemberSearchTest.kt (전체 — 애노테이션 조합, 토큰 헬퍼, 페이지네이션 단언 형태)
    - src/test/kotlin/com/goldwrestling/pass/AdminPassControllerTest.kt (03-06~03-09 산출물 — 이용권 픽스처 헬퍼를 재사용)
    - src/main/kotlin/com/goldwrestling/pass/MemberPassService.kt, MemberPassController.kt (Task 2 산출물)
    - src/main/kotlin/com/goldwrestling/member/MemberStateGate.kt
    - docs/error-codes.md (`MEMBER_NOT_ACTIVE`, `ACCESS_DENIED`, `UNAUTHENTICATED`, `VALIDATION_FAILED`)
    - docs/conventions.md §10.0 표, §10.1, §10.3
  </read_first>
  <behavior>
    `MemberPassControllerTest` (PASS-05):
    - 회원 토큰으로 `GET /api/members/me/passes` → 200, 배열
    - 만료된 이용권이 목록에 포함되고 `displayStatus = EXPIRED`다
    - 잔여 0인 이용권이 포함되고 `displayStatus = EXHAUSTED`다
    - 취소된 이용권은 목록에 **없다**
    - 응답 어디에도 `cancelReason` 값이 채워진 항목이 없다 (취소 제외의 부수 효과)
    - 다른 회원의 이용권이 섞이지 않는다
    - `PENDING` 회원 토큰 → 403 `MEMBER_NOT_ACTIVE`
    - 관리자 토큰 → 403 `ACCESS_DENIED` / 토큰 없음 → 401 `UNAUTHENTICATED`

    `MemberPassTransactionControllerTest` (PASS-06):
    - 200, `content`/`page`/`size`/`totalElements`/`totalPages` 필드가 모두 존재
    - `size=2` → `content.length()`가 2, `totalElements`는 전체 건수
    - `page=1` → 두 번째 페이지가 온다
    - `passId` 필터 → 해당 이용권의 이력만 나온다
    - **타인의 `passId`로 필터 → `totalElements`가 0이다 (IDOR 방어 증명)**
    - 다른 회원의 이력이 섞이지 않는다
    - 정렬이 `occurredAt` 내림차순이다
    - `size=0` 또는 `size=101` → 400 `VALIDATION_FAILED`
    - `PENDING` 회원 → 403 `MEMBER_NOT_ACTIVE` / 관리자 토큰 → 403 / 토큰 없음 → 401
  </behavior>
  <action>
    두 통합테스트 클래스를 만든다. 애노테이션 조합은 `MemberSearchTest`와 동일하게
    (`@SpringBootTest`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`, `@Transactional`)
    맞춘다. 날짜 단언이 있으므로 `@BeforeEach`에서 Clock을 **고정 Instant**로 세팅한다.
    만료·소진 픽스처는 이용권을 직접 저장해 만든다(만료: `endDate`를 과거로, 소진: `remainingCount = 0`).
    이력 픽스처는 여러 건을 만들되 `MutableTestClock.advance`로 `occurredAt`을 벌려 정렬 단언이 성립하게 한다
    (`MemberSearchTest`의 페이지네이션 픽스처가 같은 기법을 쓴다).
    타인 데이터 케이스는 **다른 `kakaoId`의 회원 + 그 회원의 이용권/이력**을 함께 저장해두고,
    본인 토큰으로 조회했을 때 나타나지 않음을 단언한다 — 픽스처가 하나뿐이면 필터가 없어도 테스트가 통과한다.
    실패 케이스는 `jsonPath("$.code")`로 에러코드를 단언한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.pass.MemberPassControllerTest" --tests "com.goldwrestling.pass.MemberPassTransactionControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령 종료 코드 0
    - `MemberPassControllerTest.kt`에 `@Test`가 9개 이상, `MemberPassTransactionControllerTest.kt`에 `@Test`가 11개 이상 존재한다
    - 두 파일 모두 서로 다른 두 회원을 만드는 픽스처를 포함한다 (`kakaoId`가 2개 이상 등장)
    - `MemberPassTransactionControllerTest.kt`에 타인 `passId` 필터 시 `totalElements`가 0임을 단언하는 테스트가 있다
    - 두 파일에 `LocalDate.now()`·`Instant.now()` 기반 기대값 계산이 없다 (고정 Clock 사용)
    - `./gradlew build` 종료 코드 0
  </acceptance_criteria>
  <done>회원 조회의 노출 범위와 스코프 격리가 실제 HTTP 계약으로 증명된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 회원 클라이언트 → `/api/members/me/**` | 인증된 회원이 자신의 데이터만 볼 수 있어야 하는 경계 |
| 쿼리 파라미터 → 조회 조건 | `passId`가 타인 리소스를 가리킬 수 있는 유일한 입력 |
| 저장 데이터 → 회원 응답 | 관리자 전용 정보(취소 사유·가감 메모)가 새어나갈 수 있는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-41 | Information Disclosure | IDOR — 타인 이용권/이력 조회 | mitigate | 경로·바디·쿼리로 `memberId`를 받지 않고 `principal.requireMemberId()`에서만 얻는다. `/me` 고정 경로라 타인을 가리키는 표현 자체가 API에 없다 (ASVS V4) |
| T-03-42 | Information Disclosure | `passId` 파라미터로 타인 이력 열람 | mitigate | `PassTransactionSpecifications.ownedByMember`를 non-nullable 필수 조건으로 만들어 항상 AND로 결합. 타인 `passId`는 빈 결과가 되며, 이를 통합테스트로 고정 |
| T-03-43 | Information Disclosure | 관리자 메모·취소 사유 노출 | mitigate | `PassTransactionResponse`에서 `note`를 제외(D-070). 취소된 이용권은 목록에서 제외하므로 `cancelReason`이 회원 응답에 도달하지 않는다(D-058·D-043) |
| T-03-44 | Elevation of Privilege | 비활성 회원의 데이터 접근 | mitigate | 두 조회 모두 `MemberStateGate.requireActive(principal)`를 호출한다. `SecurityConfig`는 역할만 가르므로 상태 조건은 서비스 계층 책임이다(D-040) |
| T-03-45 | Denial of Service | 과대 페이지 크기 요청 | mitigate | `@Min(1) @Max(100)` size 제약(D-035와 동일). 위반 시 400 `VALIDATION_FAILED` |
| T-03-46 | Tampering | FE 파라미터 바인딩 깨짐으로 인한 무필터 조회 | mitigate | `@ParameterObject`(D-054)를 붙여 openapi.yaml이 개별 쿼리 파라미터를 생성하게 한다. acceptance_criteria가 생성 결과를 직접 검사 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 신규 외부 패키지 설치 없음 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `git diff docs/api/openapi.yaml`에 회원 경로 2개와 개별 쿼리 파라미터 확인
- `./gradlew test --tests "com.goldwrestling.pass.*"` 전부 green
</verification>

<success_criteria>
- 회원이 만료·소진 포함 본인 이용권을 상태 구분과 함께 조회한다 (취소는 제외)
- 회원이 본인 이력을 이용권별 필터 + page/size로 조회한다
- 타인 데이터 접근이 API 표면·쿼리 조건·통합테스트 3중으로 차단된다
</success_criteria>

<output>
Create `.planning/phases/03-pass/03-10-SUMMARY.md` when done.
D-070(가감 메모를 회원에게 노출하지 않음)은 계획 단계에서 사용자가 확정했으므로 완료 보고에는 확정 사실만 언급한다.
커밋은 사용자가 명시적으로 요청할 때만 실행한다 (CLAUDE.md 커밋 규칙).
</output>
