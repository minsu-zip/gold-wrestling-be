---
phase: 07-container-server-setup
plan: 05
type: execute
wave: 3
depends_on: [07-02, 07-03]
chunk: 7b
files_modified:
  - docs/metrics.md
autonomous: false
requirements: [INFRA-02]
user_setup:
  - service: ghcr
    why: "이미지를 GHCR에 push하려면 소유자 계정의 write:packages PAT로 로그인해야 하고, 패키지 가시성(public) 전환은 GitHub UI에서만 가능하다 (D-02, D-03)"
    env_vars:
      - name: CR_PAT
        source: "GitHub > Settings > Developer settings > Personal access tokens (classic) — scope: write:packages, read:packages"
    dashboard_config:
      - task: "Docker Desktop 실행 (buildx 데몬 필요)"
        location: "로컬 Mac — Applications > Docker"
      - task: "패키지 가시성을 Public으로 전환"
        location: "GitHub > minsu-zip > Packages > gold-wrestling-be > Package settings > Danger Zone > Change visibility"

must_haves:
  truths:
    - "`ghcr.io/minsu-zip/gold-wrestling-be:latest`가 amd64+arm64 두 플랫폼을 담은 매니페스트 리스트 1개로 GHCR에 존재한다 (INFRA-02, D-02)"
    - "GHCR 패키지가 public이라 `docker logout ghcr.io` 상태에서도 pull이 성공한다 — 서버에 장기 토큰을 남기지 않는다 (D-03, T-07-04)"
    - "이미지 안에 시크릿이 없다 — 공개 이미지라는 전제가 성립하는 근거다 (D-03, T-07-05)"
    - "레포 자체는 private을 유지한다 — 패키지 가시성과 레포 가시성은 별개다 (D-03)"
  artifacts:
    - path: "docs/metrics.md"
      provides: "게시된 매니페스트 정보(다이제스트·플랫폼·압축 크기) 추가 섹션"
      contains: "ghcr.io/minsu-zip/gold-wrestling-be"
  key_links:
    - from: "deploy/compose.prod.yml"
      to: "ghcr.io/minsu-zip/gold-wrestling-be"
      via: "APP_IMAGE 기본값"
      pattern: "ghcr\\.io/minsu-zip/gold-wrestling-be"
---

<objective>
07-02가 만든 Dockerfile로 **amd64+arm64 매니페스트 1개를 GHCR에 실제로 게시**하고, 인증 없이 pull되는지(=서버에 토큰이 필요 없는지) 확인한다. 서버는 이 이미지를 받아 기동하므로, 07-06이 시작되려면 이 플랜이 먼저 끝나야 한다.

이 플랜은 **청크 7b**의 일부이며 사람의 개입(Docker Desktop 실행, `docker login`, 패키지 가시성 전환)이 필요해 `autonomous: false`다.

Purpose: INFRA-02의 "매니페스트 1개"를 로컬 캐시가 아니라 레지스트리에서 실증하고, Phase 8이 자동화할 push 경로를 손으로 한 번 밟아 둔다 (D-02).
Output: GHCR에 게시된 멀티 아키텍처 이미지, `docs/metrics.md`의 게시 결과 섹션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-container-server-setup/07-CONTEXT.md
@.planning/phases/07-container-server-setup/07-RESEARCH.md
@Dockerfile
@deploy/compose.prod.yml
</context>

<constraints>
- **PAT 실값을 어디에도 기록하지 않는다** — 커밋·문서·SUMMARY·로그 어디에도. 로그인은 사용자가 직접 수행한다 (CLAUDE.md 시크릿 규칙).
- **레포 가시성을 건드리지 않는다.** private 레포 유지가 전제이며, public으로 바꾸는 것은 패키지 가시성뿐이다 (D-03).
- 멀티플랫폼 빌드는 `--load`와 함께 쓸 수 없다 — `--push`가 유일한 경로다.
- 커밋 SHA 태그 자동 부여는 Phase 8(DEPLOY-02) 범위다. 이 phase는 `:latest`와 수동 SHA 태그 1개까지만 다룬다.
</constraints>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: GHCR 로그인 (사람만 가능)</name>
  <read_first>
    - `.planning/phases/07-container-server-setup/07-CONTEXT.md` D-02·D-03 (이미지 전달 경로와 public 결정)
  </read_first>
  <action>
사용자가 아래 `<how-to-do>` 절차를 직접 수행하는 동안 대기한다. 실행자가 할 수 있는 것은 사전 상태 확인(`docker info`가 성공하는지)과 사후 확인(`~/.docker/config.json`의 `auths`에 `ghcr.io` 엔트리가 생겼는지)뿐이다 — **PAT 실값은 어떤 경로로도 다루지 않는다.** Docker Desktop이 꺼져 있으면 실행자가 켤 수 없으므로 그 사실을 알리고 대기한다. 사용자가 "로그인 완료"를 알리면 사후 확인만 실행하고 Task 2로 넘어간다.
  </action>
  <what-needed>
Docker Desktop 실행과 GHCR 로그인은 CLI/API로 대신할 수 없다 — PAT 실값이 필요하고, 그 값은 AI가 다루지 않는다.
  </what-needed>
  <how-to-do>
1. **Docker Desktop을 실행한다** (macOS Applications > Docker). `docker info`가 성공하면 준비 완료다.
2. **write:packages 권한 PAT를 준비한다** — GitHub > Settings > Developer settings > Personal access tokens (classic) > Generate new token. 필요한 scope: `write:packages`, `read:packages`. 기존 토큰이 있으면 재사용해도 된다.
3. **로그인한다** — 토큰을 셸 히스토리에 남기지 않도록 stdin으로 넘긴다:
   `echo $CR_PAT | docker login ghcr.io -u minsu-zip --password-stdin`
   (`CR_PAT`는 셸에 임시로만 export하고 `unset CR_PAT`로 지운다. `~/.zsh_history`에 토큰이 남지 않게 export 앞에 공백을 두는 것을 권장)
4. `docker login ghcr.io` 결과가 `Login Succeeded`인지 확인한다.

**PAT 값을 이 대화에 붙여넣지 말 것.** 로그인 성공 여부만 알려주면 된다.
  </how-to-do>
  <resume-signal>`Login Succeeded`를 확인했으면 "로그인 완료"라고 알려주세요. Docker Desktop이 꺼져 있거나 로그인이 실패하면 그 메시지를 알려주세요.</resume-signal>
  <verify>
    <automated>docker info >/dev/null 2>&1 && python3 -c "import json,os;p=os.path.expanduser('~/.docker/config.json');print('ghcr.io' in json.load(open(p)).get('auths',{}))"</automated>
  </verify>
  <done>Docker 데몬이 응답하고 `~/.docker/config.json`에 `ghcr.io` 인증 엔트리가 존재한다. 토큰 값 자체는 출력하지 않는다.</done>
  <acceptance_criteria>
    - `docker info` exits 0 (데몬 기동 확인)
    - `docker system info --format '{{json .RegistryConfig}}'` 또는 `~/.docker/config.json`에 `ghcr.io` 엔트리가 존재한다 (**토큰 값 자체는 출력하지 않는다**)
    - 사용자가 로그인 성공을 확인했다
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 멀티 아키텍처 이미지 GHCR push + 매니페스트 검증</name>
  <files>docs/metrics.md</files>
  <read_first>
    - `Dockerfile` (07-02 결과 — push할 대상)
    - `deploy/compose.prod.yml` (`APP_IMAGE` 기본값의 이미지 이름 — push 태그와 정확히 일치해야 한다)
    - `.planning/phases/07-container-server-setup/07-RESEARCH.md` §Architecture Patterns Pattern 2 (buildx push 명령 원문, `docker buildx imagetools inspect`로 매니페스트 확인)
    - `docs/metrics.md` (07-02가 만든 현재 상태 — 섹션 번호를 이어 붙인다)
  </read_first>
  <action>
07-02에서 만든 `gw-builder`(docker-container 드라이버) 빌더를 사용해 GHCR에 push한다.

- 태그 2개를 함께 붙인다: `ghcr.io/minsu-zip/gold-wrestling-be:latest`와 `ghcr.io/minsu-zip/gold-wrestling-be:<git short SHA>`. SHA 태그는 07-06에서 롤백 대상을 지목할 수 있게 하고, Phase 8이 자동화할 태깅 방식을 미리 확인하는 목적이다.
- `--platform linux/amd64,linux/arm64 --push`로 빌드한다. `--load`는 멀티플랫폼과 함께 쓸 수 없다.
- push 후 `docker buildx imagetools inspect ghcr.io/minsu-zip/gold-wrestling-be:latest`로 **매니페스트 리스트 안에 linux/amd64와 linux/arm64가 모두 들어 있는지** 확인한다. 이것이 INFRA-02의 "매니페스트 1개" 실증이다.
- **이미지에 시크릿이 없는지 재확인** — public으로 전환하기 직전이 마지막 기회다. `docker run --rm --entrypoint sh <이미지> -c 'ls -a /application'`에 `.env`가 없고, `docker history --no-trunc`에 `ENV`로 박힌 비밀 값이 없는지 본다.

`docs/metrics.md`에 `## 3. GHCR 게시 결과` 섹션을 추가한다: 게시 일자, 이미지 이름·태그 2종, 매니페스트 다이제스트, 플랫폼별 압축 크기(`imagetools inspect --raw` 또는 GHCR 패키지 페이지 값), push 소요 시간. `latest`가 어느 SHA를 가리키는지 함께 적어 나중에 "지금 서버에 뜬 게 어느 커밋인가"를 추적할 수 있게 한다. **PAT·토큰 문자열은 적지 않는다.**
  </action>
  <verify>
    <automated>docker buildx imagetools inspect ghcr.io/minsu-zip/gold-wrestling-be:latest | grep -c 'linux/amd64\|linux/arm64'</automated>
  </verify>
  <acceptance_criteria>
    - `docker buildx build --platform linux/amd64,linux/arm64 -t ghcr.io/minsu-zip/gold-wrestling-be:latest -t ghcr.io/minsu-zip/gold-wrestling-be:$(git rev-parse --short HEAD) --push .` exits 0
    - `docker buildx imagetools inspect ghcr.io/minsu-zip/gold-wrestling-be:latest` 출력에 `linux/amd64`와 `linux/arm64`가 각각 1건 이상 (매니페스트 리스트 1개, INFRA-02)
    - push된 이미지 이름이 `deploy/compose.prod.yml`의 `APP_IMAGE` 기본값과 문자열까지 일치: `grep -q "$(docker buildx imagetools inspect ghcr.io/minsu-zip/gold-wrestling-be:latest --format '{{.Name}}' 2>/dev/null | sed 's/:latest//')" deploy/compose.prod.yml`
    - 이미지에 `.env` 없음: `docker run --rm --entrypoint sh ghcr.io/minsu-zip/gold-wrestling-be:latest -c 'ls -a /application' | grep -c "^\.env$"` prints `0`
    - `docs/metrics.md`에 `## 3.` 섹션과 매니페스트 다이제스트(`sha256:`로 시작하는 문자열)가 존재
    - `grep -ciE 'ghp_[A-Za-z0-9]|github_pat_|password-stdin.*[A-Za-z0-9]{20}' docs/metrics.md` prints `0`
  </acceptance_criteria>
  <done>GHCR에 amd64+arm64 매니페스트 1개가 게시되고, 그 다이제스트·태그·플랫폼이 `docs/metrics.md`에 기록됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: GHCR 패키지 가시성 Public 전환 + 무인증 pull 확인</name>
  <read_first>
    - `.planning/phases/07-container-server-setup/07-CONTEXT.md` D-03 (public 결정과 감수하기로 한 리스크)
    - `docs/decisions.md` D-175 (07-01이 기록한 이 결정의 정본)
  </read_first>
  <action>
사용자가 GitHub UI에서 패키지 가시성을 Public으로 바꾸는 동안 대기한다(API로 대체 불가). 사용자가 "public 전환 완료"를 알리면 실행자가 무인증 접근을 실제로 검증한다: `docker logout ghcr.io` → `docker manifest inspect ghcr.io/minsu-zip/gold-wrestling-be:latest` → `docker pull` 순서로 확인하고, 이어서 `gh repo view minsu-zip/gold-wrestling-be --json visibility`로 **레포는 여전히 PRIVATE인지**를 확인한다(패키지만 public이어야 한다). 무인증 접근이 실패하면 가시성 전환이 반영되지 않은 것이므로 사용자에게 다시 확인을 요청한다 — 임의로 서버에 PAT를 심는 우회를 제안하지 않는다(D-03의 보안 이득이 사라진다).
  </action>
  <what-needed>
GitHub 패키지 가시성 전환은 웹 UI에서만 가능하다 (API로 대체 불가). 이걸 해야 서버가 `docker login` 없이 pull할 수 있고, 그래야 서버에 장기 PAT를 두지 않는다는 D-03의 보안 이득이 실현된다.
  </what-needed>
  <how-to-do>
1. GitHub > `minsu-zip` 프로필 > **Packages** 탭 > `gold-wrestling-be` 패키지로 이동한다 (첫 push 이후에 나타난다).
2. 우측 **Package settings** > 하단 **Danger Zone** > **Change visibility** > **Public** 선택 후 확인 문구 입력.
3. **레포 가시성은 건드리지 않는다** — `gold-wrestling-be` 레포는 private을 유지한다. 바꾸는 것은 패키지 하나뿐이다.
4. 확인: 로컬에서 `docker logout ghcr.io` 후 `docker manifest inspect ghcr.io/minsu-zip/gold-wrestling-be:latest`가 성공하면 무인증 pull이 가능한 상태다.

**주의:** public 전환은 이미지 바이트코드가 누구에게나 열린다는 뜻이다 — 도메인 로직·마이그레이션·API 표면이 디컴파일로 노출될 수 있고, 이는 D-03에서 소유자가 감수하기로 확정한 사항이다. 이미지 안에 시크릿이 없다는 것(Task 2에서 확인)이 이 결정의 전제다.
  </how-to-do>
  <resume-signal>가시성을 Public으로 바꿨으면 "public 전환 완료"라고 알려주세요. 4번 확인이 실패하면 그 에러 메시지를 알려주세요.</resume-signal>
  <verify>
    <automated>docker logout ghcr.io >/dev/null 2>&1; docker manifest inspect ghcr.io/minsu-zip/gold-wrestling-be:latest >/dev/null && test "$(gh repo view minsu-zip/gold-wrestling-be --json visibility -q .visibility)" = "PRIVATE" && echo "public package + private repo OK"</automated>
  </verify>
  <done>패키지가 public이라 로그아웃 상태에서 매니페스트 조회·pull이 성공하고, 레포 가시성은 PRIVATE 그대로다.</done>
  <acceptance_criteria>
    - `docker logout ghcr.io` 후 `docker manifest inspect ghcr.io/minsu-zip/gold-wrestling-be:latest` exits 0 (무인증 조회 성공, D-03/T-07-04)
    - 같은 상태에서 `docker pull ghcr.io/minsu-zip/gold-wrestling-be:latest` exits 0
    - `gh repo view minsu-zip/gold-wrestling-be --json visibility -q .visibility` prints `PRIVATE` (레포 가시성 무변경)
    - 사용자가 public 전환 완료를 확인했다
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 Docker 자격증명 → GHCR | write:packages PAT가 이미지 레지스트리 쓰기 권한을 갖는 지점 |
| GHCR public 패키지 → 인터넷 | 이미지 콘텐츠가 누구에게나 열리는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | public 이미지의 바이트코드(도메인 로직·Flyway 마이그레이션·API 표면) | accept | D-03/D-175에서 소유자가 명시적으로 감수 확정. 완화 전제는 "이미지에 시크릿이 없다"이며 Task 2에서 public 전환 **직전**에 `.env` 부재·`docker history` ENV를 재확인해 그 전제를 실증한다 |
| T-07-04 | Elevation of Privilege | 서버 상주 GHCR PAT | mitigate (설계로 제거) | 패키지를 public으로 두어 서버 `docker login` 자체를 없앤다. `docker logout` 상태에서 pull이 성공함을 acceptance criteria로 실증 (Task 3) |
| T-07-14 | Information Disclosure | PAT 실값이 셸 히스토리·문서·SUMMARY에 남음 | mitigate | 로그인은 사람이 `--password-stdin`으로 수행하고 AI에게 값을 전달하지 않는다. `docs/metrics.md`에 토큰 패턴 0건을 grep으로 강제 (Task 1·2) |
| T-07-15 | Tampering | 레포 가시성을 실수로 public 전환 | mitigate | Task 3 지시에 "레포는 private 유지"를 명시하고, `gh repo view --json visibility`가 `PRIVATE`인지 acceptance criteria로 확인 |
| T-07-SC | Tampering | npm/pip/cargo installs | 해당 없음 | 언어 패키지 설치 0건. 게시 대상은 07-02가 만든 자체 이미지이며 베이스는 Docker 공식 이미지뿐 |
</threat_model>

<verification>
- `docker buildx imagetools inspect ghcr.io/minsu-zip/gold-wrestling-be:latest`에 두 플랫폼 표시
- `docker logout ghcr.io` 상태에서 `docker pull` 성공
- `gh repo view minsu-zip/gold-wrestling-be --json visibility` 가 `PRIVATE`
- `git status --porcelain` 결과가 `docs/metrics.md` 1개 파일 수정만
</verification>

<success_criteria>
- INFRA-02: amd64+arm64 매니페스트 1개가 GHCR에 실제로 존재한다 (로컬 캐시가 아니라 레지스트리 조회로 확인)
- D-02: 로컬 Mac buildx → GHCR push 경로가 손으로 1회 검증됐다
- D-03: 패키지 public, 레포 private, 무인증 pull 성공
- 게시 결과(다이제스트·태그·플랫폼)가 `docs/metrics.md`에 기록됐고 토큰 문자열은 0건
</success_criteria>

<output>
Create `.planning/phases/07-container-server-setup/07-05-SUMMARY.md` when done.
SUMMARY에 (1) 게시된 매니페스트 다이제스트와 `latest`가 가리키는 커밋 SHA, (2) 무인증 pull 확인 결과, (3) 이 플랜에 프로덕션 코드 변경이 없어(레지스트리 조작 + 문서) conventions §10.0 면제 대상이라는 한 줄을 포함한다. **PAT 값은 어떤 형태로도 적지 않는다.**
</output>
