---
phase: 07-container-server-setup
plan: 01
type: execute
wave: 1
depends_on: []
chunk: 7a
files_modified:
  - src/main/resources/application.yml
  - .env.example
  - src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt
  - docs/decisions.md
autonomous: true
requirements: [INFRA-07]
user_setup: []

must_haves:
  truths:
    - "운영자가 `DB_HIKARI_MAX_POOL_SIZE`·`SERVER_TOMCAT_THREADS_MAX` 환경변수만 바꿔 커넥션 풀·스레드 수를 재빌드 없이 조정할 수 있고, 값을 안 주면 각각 5·50이 적용된다 (D-08)"
    - "운영자가 `SWAGGER_ENABLED=false` 하나로 `/v3/api-docs`·`/swagger-ui.html`을 404로 만들 수 있고, `SecurityConfig.kt`는 diff가 0이다 (D-10)"
    - "운영 전용 스프링 프로필이 존재하지 않고, 운영 전용 값은 전부 env 키로 표현된다 (D-09)"
    - "새 운영 키가 실값 없이 `.env.example`에 키 이름·의미 주석과 함께 존재한다 (INFRA-07, CLAUDE.md 시크릿 규칙)"
    - "Phase 7이 확정한 7건의 설계 결정이 `docs/decisions.md`에 D-169 이후 번호로 기록된다 (D-03·D-07·D-08·D-09·D-10·D-12·D-20)"
  artifacts:
    - path: "src/main/resources/application.yml"
      provides: "Hikari/Tomcat/springdoc env 플레이스홀더"
      contains: "DB_HIKARI_MAX_POOL_SIZE"
    - path: ".env.example"
      provides: "운영 배포 env 키 섹션 (실값 없음)"
      contains: "SWAGGER_ENABLED"
    - path: "src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt"
      provides: "springdoc 비활성 시 404 관찰 지점"
      min_lines: 30
    - path: "docs/decisions.md"
      provides: "Phase 7 결정 7건"
      contains: "GHCR 이미지 패키지는 public"
  key_links:
    - from: "src/main/resources/application.yml"
      to: ".env.example"
      via: "동일한 env 키 이름"
      pattern: "SWAGGER_ENABLED|DB_HIKARI_MAX_POOL_SIZE|SERVER_TOMCAT_THREADS_MAX"
    - from: "src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt"
      to: "springdoc.api-docs.enabled"
      via: "@SpringBootTest(properties = [...])"
      pattern: "springdoc\\.api-docs\\.enabled=false"
---

<objective>
운영 배포에 필요한 **애플리케이션 쪽 설정 표면**을 만든다 — 1GB 서버 기준으로 축소한 커넥션 풀·스레드 수, 운영에서 Swagger를 끄는 토글, 그리고 이 phase가 확정한 설계 결정 기록.

이 플랜은 **청크 7a**(`feature/phase-07a-*`)의 일부다. 코드/설정 변경만 있고 인프라 파일은 건드리지 않는다.

Purpose: 뒤 플랜의 compose·Caddyfile·운영 문서가 참조할 **env 키 계약**을 먼저 확정한다. 키 이름이 나중에 바뀌면 compose·`.env.example`·`docs/operations.md` 세 곳이 동시에 틀어진다.
Output: `application.yml` 플레이스홀더 3종, `.env.example` 운영 섹션, `SwaggerDisabledTest.kt`, `docs/decisions.md` D-169~D-175.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-container-server-setup/07-CONTEXT.md
@.planning/phases/07-container-server-setup/07-PATTERNS.md
@.planning/phases/07-container-server-setup/07-RESEARCH.md
@docs/conventions.md
</context>

<constraints>
- **`src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt`에 diff를 만들지 않는다.** Swagger permitAll은 `generateApiDocs`(D-029) 로컬 파이프라인의 전제다. 운영 차단은 springdoc 자체를 비활성화해 404를 유도하는 방식이다 (07-RESEARCH.md §Common Pitfalls Pitfall 5).
- **`.env`/`env_file`은 점(.)이 든 키를 거부한다.** `springdoc.api-docs.enabled`를 env 이름으로 직접 쓰지 않고 평평한 `SWAGGER_ENABLED`로 간접 바인딩한다 (Pitfall 6, D-10).
- `.env.example`에 실값(진짜 도메인·이메일·시크릿)을 넣지 않는다. 안전한 기본값(`5`, `50`, `true`)만 허용한다.
- 새 스프링 프로필(`application-prod.yml` 등)을 만들지 않는다 (D-09).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: application.yml에 Hikari·Tomcat·springdoc env 플레이스홀더 추가</name>
  <files>src/main/resources/application.yml</files>
  <read_first>
    - `src/main/resources/application.yml` (전체 — 현재 `${ENV:default}` 관례와 한국어 주석 스타일을 그대로 따라야 한다. 확장 지점: `spring.datasource.hikari` 블록(현 21-22행), `server:` 블록(현 48-54행), `springdoc:` 블록(현 65-71행))
    - `.planning/phases/07-container-server-setup/07-PATTERNS.md` §`src/main/resources/application.yml` (추가 위치·주석 관례가 행 번호까지 적혀 있다)
    - `.planning/phases/07-container-server-setup/07-RESEARCH.md` §Common Pitfalls Pitfall 6 (점 표기 키를 env 이름으로 쓰면 안 되는 이유)
  </read_first>
  <action>
기존 `${ENV_KEY:안전한기본값}` 패턴 그대로 세 곳을 확장한다. 새 블록을 만들지 말고 **기존 블록 안에 키만 추가**한다.

1. `spring.datasource.hikari` 아래에 `maximum-pool-size: ${DB_HIKARI_MAX_POOL_SIZE:5}` 추가. 기존 `pool-name: gold-wrestling-pool`은 그대로 둔다.
2. `server:` 블록 아래에 `tomcat.threads.max: ${SERVER_TOMCAT_THREADS_MAX:50}` 계층 추가. 기존 `port`·`error` 키는 건드리지 않는다.
3. `springdoc:` 블록의 `api-docs`에 `enabled: ${SWAGGER_ENABLED:true}`, `swagger-ui`에 `enabled: ${SWAGGER_ENABLED:true}` 추가. 기존 `path`·`tags-sorter`·`operations-sorter`는 그대로 둔다. **두 곳 모두 같은 `SWAGGER_ENABLED` 키를 읽는다** — 하나만 꺼면 다른 하나가 살아남는다.

각 새 키 위에 기존 파일 관례대로 한국어 주석 1~3줄을 남긴다. 내용은 "왜 이 기본값인가"여야 한다:
- Hikari 5: RAM 1GB·단일 지점 트래픽, 배치의 `REQUIRES_NEW`가 한 흐름에서 커넥션 2개를 쓰므로 5면 충분 (decisions.md D-170)
- Tomcat 50: 같은 근거, 스레드 스택이 힙 밖 메모리를 먹으므로 200(기본)은 550M 컨테이너에서 과하다 (D-170)
- Swagger 토글: 로컬 기본 `true`, 운영 `.env`에서 `false`. `springdoc.*`를 env 이름으로 쓸 수 없어(점 표기) 평평한 키로 간접 바인딩한다는 사실을 주석에 남긴다 (D-171)

`spring.config.import`·`management`·`goldwrestling` 블록은 무변경이다.
  </action>
  <verify>
    <automated>grep -c 'DB_HIKARI_MAX_POOL_SIZE:5\|SERVER_TOMCAT_THREADS_MAX:50' src/main/resources/application.yml && grep -c 'SWAGGER_ENABLED:true' src/main/resources/application.yml && ./gradlew compileKotlin --console=plain -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'maximum-pool-size: ${DB_HIKARI_MAX_POOL_SIZE:5}' src/main/resources/application.yml` exits 0
    - `grep -q 'max: ${SERVER_TOMCAT_THREADS_MAX:50}' src/main/resources/application.yml` exits 0
    - `grep -c 'enabled: ${SWAGGER_ENABLED:true}' src/main/resources/application.yml` prints `2` (api-docs 1개 + swagger-ui 1개)
    - `grep -c 'springdoc' .env.example` prints `0` — 점 표기 키가 env 파일로 새어나가지 않았다
    - `git diff --name-only` 결과에 `src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt`가 없다
    - `ls src/main/resources/application-*.yml 2>/dev/null | wc -l` prints `0` (D-09: 프로필 파일 미신설)
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>세 개의 새 env 플레이스홀더가 기존 주석 관례를 따라 추가되고, SecurityConfig·프로필 파일에는 변경이 없다.</done>
</task>

<task type="auto">
  <name>Task 2: .env.example에 운영 배포 키 섹션 추가 + SwaggerDisabledTest 신설</name>
  <files>.env.example, src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt</files>
  <read_first>
    - `.env.example` (전체 — `# --- 섹션명 ---` 헤더 + 키 위 한국어 설명 주석 관례, 1-7행의 "실값 커밋 금지" 헤더)
    - `src/test/kotlin/com/goldwrestling/config/SecurityFilterChainTest.kt` (임포트 목록 1-38행, 클래스 KDoc 40-49행, 애노테이션 조합 51-55행, `mockMvc.perform(get(...)).andExpect(status()...)` 체이닝 패턴 — **이 파일이 유일한 analog이며 이미 `/v3/api-docs` GET을 200으로 테스트하고 있다. 새 테스트는 기대값만 뒤집는다**)
    - `src/test/kotlin/com/goldwrestling/config/JwtConfigTest.kt` (`@SpringBootTest(properties = [...])`로 클래스 단위 프로퍼티를 덮어쓰는 선례)
    - `.planning/phases/07-container-server-setup/07-PATTERNS.md` §`.env.example` 및 §`SwaggerDisabledTest.kt` (RESEARCH.md의 Kotlin MockMvc DSL 예제는 이 레포에 사용 사례가 0건이므로 **쓰지 않는다**는 경고 포함)
  </read_first>
  <action>
**(a) `.env.example`** — 파일 맨 끝(`# --- 기본 지점 (D-047) ---` 섹션 뒤)에 새 섹션 `# --- 운영 배포 (Phase 7, decisions.md D-169~D-175) ---`을 추가하고 아래 키를 넣는다. 각 키 위에 의미·기본값·관련 결정 번호를 밝히는 한국어 주석 1~3줄:

- `DOMAIN=` — Caddy가 자동 HTTPS를 발급할 도메인. 비우면 Caddy가 `localhost`로 동작(로컬 검증용). 운영 서버 `.env`에만 실도메인을 넣는다 (D-19)
- `ACME_EMAIL=` — Let's Encrypt 인증서 만료 알림 수신 이메일 (D-19)
- `SWAGGER_ENABLED=true` — 로컬 기본 true, 운영 `.env`에서 false (D-10)
- `DB_HIKARI_MAX_POOL_SIZE=5` (D-08)
- `SERVER_TOMCAT_THREADS_MAX=50` (D-08)
- `JAVA_TOOL_OPTIONS=` — 운영 compose가 JVM 메모리 플래그를 주입하는 통로. 재빌드 없이 힙 비율을 바꾸기 위해 이미지가 아니라 env로 둔다 (D-07)
- `APP_IMAGE=` — 운영 compose가 pull할 이미지 태그. 비우면 `:latest` (Phase 8 SHA 태그 배포 대비)

**실값을 넣지 않는다.** `DOMAIN`·`ACME_EMAIL`·`JAVA_TOOL_OPTIONS`·`APP_IMAGE`는 값 없이 키만 남긴다. 파일 상단 주석(4-7행)이 ".env는 docker-compose와 애플리케이션 두 곳에서 함께 쓰인다"고 설명하므로, 새 섹션 앞에 "운영 서버의 `/opt/gold-wrestling/.env`가 이 목록의 상위집합이다"라는 한 줄을 덧붙인다.

**(b) `SwaggerDisabledTest.kt`** — `src/test/kotlin/com/goldwrestling/config/`에 신설. `SecurityFilterChainTest.kt`의 임포트·체이닝 스타일을 그대로 따른다 (`MockMvcRequestBuilders.get` / `MockMvcResultMatchers.status` 정적 임포트 + `mockMvc.perform(...).andExpect(...)`). **Kotlin MockMvc DSL(`mockMvc.get("...") { }`)은 이 레포에 사용 사례가 0건이므로 쓰지 않는다.**

- 애노테이션: `@SpringBootTest(properties = ["springdoc.api-docs.enabled=false", "springdoc.swagger-ui.enabled=false"])`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfiguration::class)`. `@Transactional`은 붙이지 않는다(DB 픽스처를 쓰지 않는다).
- `@AutoConfigureMockMvc` 임포트는 Boot 4.1 경로 `org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc`다 (Boot 3 경로를 쓰면 컴파일되지 않는다).
- 테스트 2건: `/v3/api-docs` GET → `status().isNotFound`, `/swagger-ui.html` GET → `status().isNotFound`. 메서드명은 백틱 한국어 서술문(conventions §10.2).
- 클래스 KDoc: 이 테스트가 D-10(운영 Swagger 비활성)의 유일한 관찰 지점이라는 것, `SecurityConfig`의 permitAll은 그대로 두고 springdoc 자체를 끄는 방식이라 404가 나온다는 것, `properties=`로 별도 ApplicationContext를 띄우는 비용을 감수하는 이유(전역 springdoc 설정을 바꾸면 다른 모든 컨트롤러 테스트 컨텍스트가 오염된다)를 적는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "*SwaggerDisabledTest*" --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*SwaggerDisabledTest*"` exits 0 with 2 tests executed, 0 failures
    - `grep -c 'DOMAIN=\|ACME_EMAIL=\|SWAGGER_ENABLED=\|DB_HIKARI_MAX_POOL_SIZE=\|SERVER_TOMCAT_THREADS_MAX=\|JAVA_TOOL_OPTIONS=\|APP_IMAGE=' .env.example` prints `7`
    - `grep -E '^(DOMAIN|ACME_EMAIL|JAVA_TOOL_OPTIONS|APP_IMAGE)=.+' .env.example` prints nothing (실값 0건)
    - `grep -q 'org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc' src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt` exits 0
    - `grep -c 'mockMvc.get(' src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt` prints `0` (Kotlin DSL 미사용)
    - `grep -c 'isNotFound' src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt` prints `2`
  </acceptance_criteria>
  <done>운영 키 7개가 실값 없이 `.env.example`에 있고, `SWAGGER_ENABLED=false` 상황에서 두 경로가 404임을 증명하는 테스트 2건이 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: docs/decisions.md에 Phase 7 결정 7건 기록</name>
  <files>docs/decisions.md</files>
  <read_first>
    - `docs/decisions.md` 1640-1760행 (D-152 ~ D-158 — 엔트리 3단 포맷: `## D-XXX. <한 문장 결정>` / `- <날짜> / <무엇을 3~4줄>` / `- 이유:` / `- 기각 대안:`. **마지막 번호를 직접 확인한다**)
    - `.planning/phases/07-container-server-setup/07-CONTEXT.md` §Implementation Decisions (로컬 D-01~D-20 원문 — 여기 근거를 요약해 옮긴다)
    - `.planning/phases/07-container-server-setup/07-RESEARCH.md` §Common Pitfalls Pitfall 1 (`eclipse-temurin:21-jre` 태그가 noble이 아니라 resolute로 롤링돼 있다는 검증 결과 — D-172의 근거)
  </read_first>
  <action>
**번호 확정을 먼저 한다.** `grep -oE '^## D-[0-9]+' docs/decisions.md | tail -1`로 실제 마지막 번호를 읽는다. 2026-09-08 22:29 기준 값은 `D-168`이다 — 플랜 작성 시점(같은 날 22:07)에는 D-158이었으나 그 사이 FE Phase 8 세션이 D-159~D-168 10건을 이 파일에 append했다(Phase 4 때와 같은 FE 선점, STATE.md Accumulated Context). **decisions.md는 BE·FE가 번호를 공유하는 단일 로그**라 실행 시점에 또 밀려 있을 수 있다. 마지막 번호가 D-168이 아니면 **그 다음 번호부터** 7건을 연속 부여하고, 편차 사실을 SUMMARY에 한 줄로 남긴다. 아래는 D-168을 전제한 번호이며, 다른 플랜(07-02~07-06)이 인용하는 D-169~D-175도 같은 폭만큼 밀어 읽는다.

파일 끝에 3단 포맷으로 7건을 append한다. 날짜는 `2026-09-08` 이후 실제 작업일:

- **D-169. 운영 전용 스프링 프로필을 만들지 않고 운영 값을 전부 env 키로 표현한다** — D-011(.env/OS 환경변수 동일 키)의 연장. 프로필을 만들면 "어느 프로필에서 어떤 값이 뜨는가"가 코드와 문서 두 곳에 흩어진다. 기각 대안: `application-prod.yml` 신설.
- **D-170. Hikari `maximum-pool-size` 5, Tomcat `threads.max` 50으로 축소한다** — RAM 1GB(t3.micro)·단일 지점 트래픽. 배치의 `REQUIRES_NEW`(BatchExecutionRecorder)가 한 흐름에서 커넥션 2개를 쓰는 것이 최대 소비 패턴이라 5로 충분하다. 스레드 200개(기본)는 스택 메모리가 힙 밖에서 550M 제한을 압박한다. 둘 다 env 플레이스홀더라 재배포 없이 올릴 수 있다. 기각 대안: 기본값 유지(10/200).
- **D-171. 운영 Swagger 차단은 `springdoc.*.enabled=false`로 하고 `SecurityConfig`는 손대지 않는다** — permitAll 목록은 `generateApiDocs`(D-029)가 401 없이 스펙을 받아오기 위한 전제다. springdoc을 끄면 핸들러 자체가 등록되지 않아 permitAll 경로여도 404가 난다. env_file이 점(.) 포함 키를 거부하므로 평평한 `SWAGGER_ENABLED` 키로 간접 바인딩한다. 기각 대안: SecurityConfig permitAll 제거(= `generateApiDocs` 파괴).
- **D-172. 런타임 베이스 이미지는 `eclipse-temurin:21-jre-noble`로 OS 계열까지 명시 고정한다** — INFRA-01의 "JDK 21 기반 최소 이미지"를 JRE로 충족하는 것으로 해석한다. 접미사 없는 `21-jre` 태그는 현재 noble이 아니라 resolute로 매핑돼 있음을 `docker-library/official-images` 정의로 확인했다 — 짧은 태그는 배급사가 조용히 재매핑하므로 프로덕션에 쓰지 않는다. 기각 대안: Alpine·distroless(musl 호환·디버깅 도구 부재).
- **D-173. 앱 JVM 힙은 `JAVA_TOOL_OPTIONS`의 `-XX:MaxRAMPercentage=60`으로 시작하고 실측으로 확정한다** — 550M 제한에서 힙 ≈330M, 비힙(메타스페이스·스레드 스택·코드캐시·GC 구조체) 여유 ≈220M. 초안이던 70%는 여유가 165M뿐이라 Boot+Hibernate의 일반적 비힙 사용량(150~250M) 상단에서 컨테이너 OOM kill 경계에 걸린다. 이미지가 아니라 env로 주입해 재빌드 없이 조정한다. 기각 대안: 고정 `-Xmx`(컨테이너 limit 변경 때마다 재계산·재빌드 필요), 70%.
- **D-174. `/actuator/*` 외부 차단은 Caddy가 담당하고 `/actuator/health`만 통과시킨다** — 앱 내부 노출 범위(`management.endpoints.web.exposure.include: health,info`)는 이미 좁혀져 있으나 `/actuator/info`도 외부에 열려 있을 이유가 없다. 엣지에서 막으면 앱 설정 실수가 곧바로 외부 노출로 이어지지 않는 2중 방어가 된다. 앱 내부 경계 확정은 Phase 9(OPS-05)가 잇는다. 기각 대안: 앱 설정만으로 차단.
- **D-175. GHCR 이미지 패키지는 public으로 두어 서버 pull 인증을 없앤다** — 레포는 private을 유지한다. 서버에 장기 PAT를 두지 않는 것이 유출 표면을 줄이는 실질적 이득이고, 이미지 안에 시크릿이 없다(전량 env 주입). 바이트코드 디컴파일로 도메인 로직·마이그레이션·API 표면이 노출되는 것은 소유자가 감수하기로 확정했다. 기각 대안: private 패키지 + 서버 상주 PAT.

기존 D-001~D-158 항목은 수정하지 않는다.
  </action>
  <verify>
    <automated>for t in '운영 전용 스프링 프로필을 만들지 않고' 'maximum-pool-size` 5' '운영 Swagger 차단은' 'eclipse-temurin:21-jre-noble' 'MaxRAMPercentage=60' '/actuator/health`만 통과' 'GHCR 이미지 패키지는 public'; do grep -qF -- "$t" docs/decisions.md || { echo "MISSING: $t"; exit 1; }; done; grep -oE '^## D-[0-9]+' docs/decisions.md | tail -7</automated>
  </verify>
  <acceptance_criteria>
    - 7건의 결정 제목 고유 문구(`운영 전용 스프링 프로필을 만들지 않고` / `maximum-pool-size\` 5` / `운영 Swagger 차단은` / `eclipse-temurin:21-jre-noble` / `MaxRAMPercentage=60` / `/actuator/health\`만 통과` / `GHCR 이미지 패키지는 public`)가 각각 `## D-` 헤딩 줄에 존재한다 — **번호가 아니라 제목으로 검증한다.** FE가 번호를 또 선점해도 거짓 통과하지 않는다
    - 새 7건의 번호가 연속이고 첫 번호 = (작업 시작 시점 마지막 번호 + 1)이다: `grep -oE '^## D-[0-9]+' docs/decisions.md | tail -7`이 연속 7개를 출력
    - 각 새 항목이 `- 이유:` 줄을 1개 이상 포함한다: `FIRST=$(grep -oE '^## D-[0-9]+' docs/decisions.md | tail -7 | head -1); awk -v f="$FIRST." 'index($0,f)==1{p=1} p' docs/decisions.md | grep -c '^- 이유:'` prints `7`
    - `grep -n 'eclipse-temurin:21-jre-noble' docs/decisions.md` 출력이 1건 이상 (D-172 근거가 태그 문자열로 고정됨)
    - `git diff docs/decisions.md | grep -c '^-[^-]'` prints `0` (기존 항목 삭제·수정 0건, 순수 append)
    - `./gradlew ktlintCheck --console=plain` exits 0
  </acceptance_criteria>
  <done>Phase 7 결정 7건이 기존 3단 포맷으로 append되고, 기존 결정 항목에는 삭제·수정 diff가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → Caddy → app | 외부 요청이 처음 앱에 닿는 지점. 이 플랜은 그 중 `/v3/api-docs`·`/swagger-ui.html` 노출 여부를 설정으로 제어한다 |
| 레포(공개 대상) → `.env.example` | 커밋되는 파일에 실제 시크릿이 새어나갈 수 있는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | `/v3/api-docs`, `/swagger-ui.html` (운영) | mitigate | `springdoc.api-docs.enabled`·`swagger-ui.enabled`를 `${SWAGGER_ENABLED:true}`로 바인딩하고 운영 `.env`에서 false → 핸들러 미등록으로 404. `SwaggerDisabledTest`가 이 동작을 회귀 테스트로 고정 (Task 1·2) |
| T-07-05 | Information Disclosure | `.env.example` | mitigate | 새 키를 값 없이(또는 안전한 비밀 아닌 기본값만) 추가. acceptance criteria의 `grep -E '^(DOMAIN\|ACME_EMAIL\|JAVA_TOOL_OPTIONS\|APP_IMAGE)=.+'` 0건 검사로 강제 (Task 2) |
| T-07-06 | Elevation of Privilege | `SecurityConfig.kt` 인가 규칙 | accept (변경 금지로 회피) | 이 플랜은 SecurityConfig를 건드리지 않는다. permitAll 제거로 Swagger를 막으려는 시도는 `generateApiDocs`(D-029)를 깨뜨린다 — `git diff --name-only`에 SecurityConfig.kt가 없음을 acceptance criteria로 검사 |
| T-07-SC | Tampering | npm/pip/cargo installs | 해당 없음 | 이 플랜은 언어 패키지를 설치하지 않는다 (Gradle 의존성 추가 0건). 07-RESEARCH.md §Package Legitimacy Audit 참조 |
</threat_model>

<verification>
- `./gradlew ktlintFormat && ./gradlew ktlintCheck build` 전체 그린 (CI와 동일 게이트, D-152)
- `git diff --name-only` 결과가 정확히 4개 파일: `src/main/resources/application.yml`, `.env.example`, `src/test/kotlin/com/goldwrestling/config/SwaggerDisabledTest.kt`, `docs/decisions.md`
- `grep -rn 'SWAGGER_ENABLED' src/main/resources/application.yml .env.example` 두 파일 모두에서 매치 (키 이름 동기화)
</verification>

<success_criteria>
- `DB_HIKARI_MAX_POOL_SIZE`(기본 5) / `SERVER_TOMCAT_THREADS_MAX`(기본 50) / `SWAGGER_ENABLED`(기본 true) 3종 플레이스홀더가 `application.yml`에 존재하고 `.env.example`에 같은 이름으로 문서화됨
- `./gradlew test --tests "*SwaggerDisabledTest*"` 2건 통과 — `SWAGGER_ENABLED=false` 상황에서 `/v3/api-docs`·`/swagger-ui.html`이 404
- `docs/decisions.md`에 D-169~D-175 7건 기록, 기존 항목 무변경
- `SecurityConfig.kt`·`application-*.yml`에 diff 0건
</success_criteria>

<output>
Create `.planning/phases/07-container-server-setup/07-01-SUMMARY.md` when done.
SUMMARY에 실제 부여한 결정 번호 범위와, 테스트를 추가하지 않은 파일(`application.yml`·`.env.example`·`docs/decisions.md`)이 conventions §10.0 면제 대상임을 한 줄로 밝힌다 (CLAUDE.md 규칙 10, §10.5).
</output>
