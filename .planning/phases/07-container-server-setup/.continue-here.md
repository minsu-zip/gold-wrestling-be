---
context: phase
phase: 07-container-server-setup
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-09-09T01:23:56.441Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 자기 지시적 grep 매치 | 검증 grep이 찾는 문자열(`git clone`, CDS 플래그명, 토큰 접두 패턴)을 "없다"고 설명하는 주석·문서 안에 문자 그대로 적어 검증이 주석을 잡음. 07-02·07-04·07-05에서 세 번 재발 | advisory | 금지 대상을 설명할 때는 명령어·패턴을 문자 그대로 쓰지 말고 우회 표현("버전관리 명령", "토큰 접두 패턴")을 쓴다. 커밋 전에 acceptance grep을 실제로 돌린다 |
| bare 로컬 D-번호 인용 | 07-CONTEXT 로컬 번호(D-18 등)를 `docs/decisions.md` 번호처럼 인용. decisions.md D-018은 전혀 다른 결정 | advisory | 로컬은 `07-CONTEXT D-xx`, 전역은 `D-1xx`로만 쓴다. 파일 머리에 표기 안내 줄을 둔다(compose·Dockerfile·operations.md 참조) |

<current_state>
청크 7b 전반(wave 3: 07-04·07-05) 완료. 브랜치 `feature/phase-07b-server-ops`가 origin에 푸시됨(dev 대비 커밋 10개 + 이 핸드오프). **PR은 아직 없다** — wave 4(07-06)까지 끝낸 뒤 청크 7b PR 하나로 낸다. `./gradlew ktlintFormat`·`build` 그린 확인됨(Kotlin 변경 없음). 워킹트리 클린.
</current_state>

<completed_work>

- 07-04: `deploy/server-setup.sh`(멱등 5블록, 100755, bash -n 통과, shellcheck 미설치) / `docs/operations.md`(env 키 29개 전수 표, 메모리 예산표 실측 열 공란, 서버 세팅 절차, 11단계 runbook) / README 링크 2줄. 커밋 `9c2fb4d`~`50e7c30` + 인용 표기 정정 `50d279c`
- 07-05: GHCR push 43초, `latest`·`50d279c` 두 태그 = 인덱스 `sha256:c83a16f27ddc…`(amd64 `d3aba5da…` / arm64 `9fb0b584…`), 압축 전송 amd64 164.5MB / arm64 162.8MB. public 전환 후 `docker logout` 상태에서 manifest inspect·pull 성공, 레포 PRIVATE 유지. 시크릿 부재 3중 확인. `docs/metrics.md` §3. 커밋 `6ba800b`~`2b86f58`
</completed_work>

<remaining_work>

- 07-06 Task 1 (human-verify): `ssh ubuntu@<host> 'bash -s' < deploy/server-setup.sh` 2회 실행 → 종료 요약 출력 비교
- 07-06 Task 2 (human-verify): scp compose.prod.yml·Caddyfile → 서버 .env 작성 → ADMIN_SEED 최초 1회 → `docker compose -f compose.prod.yml pull && up -d` → Flyway V1~V12 로그 → health 200 / info 404 / 80→443 → `restart caddy` 후 재발급 로그 없음
- 07-06 Task 3 (human-verify): `docker stats --no-stream`, `dmesg | grep -i oom` 없음, 몇 시간 뒤 caddy 재확인
- 07-06 Task 4 (auto): `docs/operations.md` §2 실측 열 3행 기입, metrics.md·decisions.md에 힙 비율 확정 기록(decisions.md 마지막 번호 D-177 → 실행 시점 재확인)
- 청크 마감: ktlintFormat → build → `create-pr` 스킬(base dev, 제목 `feat: Phase 7b 서버 세팅·GHCR·실서버 배포 — …`, 본문에 "Phase 7 중 2/2 청크"). 머지는 사용자
</remaining_work>

<decisions_made>

- SHA 태그 = feature 브랜치 HEAD `50d279c`. dev 머지 후 SHA 불일치는 Phase 8 워크플로 태깅이 해소
- 로컬 D-번호 표기 `07-CONTEXT D-xx` 통일(7a 리뷰 Warning 동일 유형 선제 반영)
- PAT는 대화형 `docker login` 프롬프트로 입력(export+공백은 zsh 기본값에서 히스토리에 남음). PAT 값은 어디에도 없음
- buildx provenance attestation(`unknown/unknown` 2건)은 유지 — pull 무관, 소스·env 미포함
- 07-05는 체크포인트가 플랜 양끝이라 오케스트레이터가 인라인 실행(SUMMARY·상태 갱신 규약 동일)
</decisions_made>

<blockers>
- 없음. 단 07-06은 사용자 자산(EC2 SSH 키·퍼블릭 IP·DNS A 레코드·보안그룹 80/443·서버 .env 실값)이 준비돼야 시작 가능
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — 커밋 규칙 예외(D-084: feature/phase-* 브랜치에서만 자동 커밋·푸시·PR, 머지 금지), 시크릿 규칙, 보고 형식
2. `.claude/skills/deliver-phase-chunk/SKILL.md` — 청크 마감 절차 §3
3. `.planning/phases/07-container-server-setup/07-06-PLAN.md` — human-verify 3개의 정확한 명령·기대값
4. `docs/operations.md` §3·§4 — 07-06이 그대로 따라갈 runbook (서버에 git 없음, docker login 없음)
5. `.planning/phases/07-container-server-setup/07-04-SUMMARY.md`, `07-05-SUMMARY.md` — 이번 세션 산출물·다이제스트
6. `.planning/phases/07-container-server-setup/07-03-SUMMARY.md` §로컬 실기동 관측값 — 실서버 대조 기준(로컬 RSS app 365MiB / pg 34MiB / caddy 19MiB)

## Infrastructure State
- GHCR: `ghcr.io/minsu-zip/gold-wrestling-be` public, 태그 `latest`·`50d279c`, 인덱스 `sha256:c83a16f27ddc4781fc633268d9a540222313a0c332c50a5858bb22bb4f5fc35c`
- 로컬 Docker: `docker logout ghcr.io` 상태(다음 push 시 재로그인 필요). `gw-builder` 빌더·`gw-be:test` 이미지 잔존(정상). dev용 `gold-wrestling-postgres` 컨테이너 상시 실행
- git: `origin/HEAD` = `origin/dev` 확인됨. 로컬 7a 브랜치 삭제됨. 원격 7a 브랜치는 미삭제(PR #34 머지됨 — 정리 시 `git push origin --delete feature/phase-07a-container-compose`)
- 실행 중인 백그라운드 프로세스 없음

<context>
07-06 실행 시 주의: 실값(도메인·IP·이메일·시크릿)은 대화·문서·커밋 어디에도 넣지 않는다. 실측값(RSS·소요 시간·로그 문자열)만 기입한다. STATE.md의 "Plan: N of 6" 카운터는 begin-phase가 리셋하는 표시 전용 드리프트로 SUMMARY 존재 여부가 진실이다. GSD 회귀 게이트는 Gradle 레포라 no-op — 청크 마감 때 `./gradlew build`를 직접 돌린다.
</context>

<next_action>
Start with: `git switch feature/phase-07b-server-ops && git pull --ff-only` → `git symbolic-ref --short refs/remotes/origin/HEAD`가 `origin/dev`인지 → `docker info` → 사용자에게 EC2 SSH·DNS·보안그룹·서버 .env 준비 여부 확인 → `/gsd-execute-phase 7 --wave 4`
</next_action>
