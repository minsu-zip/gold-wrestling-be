---
phase: 04-schedule-reservation
plan: 10
type: execute
wave: 10
depends_on: ["04-08", "04-09"]
files_modified:
  - src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt
  - src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt
  - src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/ChangeReservationRequest.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/MyReservationSearchCondition.kt
  - src/test/kotlin/com/goldwrestling/reservation/MemberReservationCancellationTest.kt
  - src/test/kotlin/com/goldwrestling/reservation/MemberReservationControllerTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [RESV-04, RESV-05, NOTIF-01]

must_haves:
  truths:
    - "회원이 당일이 아닌 예약을 취소하면 잔여가 즉시 1 복구되고 CANCEL_REFUND 이력이 남는다"
    - "회원이 예약을 변경하면 하나의 트랜잭션에서 기존 예약이 취소되고 새 예약이 생성된다"
    - "변경 대상 타임 예약이 실패하면 기존 예약이 그대로 남는다"
    - "회원 본인 예약 목록에 취소된 예약이 나오지 않고, 다른 회원의 예약도 나오지 않는다"
    - "다른 회원의 예약 id로 취소를 시도하면 404가 응답된다"
    - "취소·변경마다 관리자 알림이 생성된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      provides: "cancel / change / findMyReservations"
      contains: "fun change"
    - path: "src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt"
      provides: "IDOR 방어용 non-null 소유권 Specification"
      contains: "fun ownedByMember"
    - path: "docs/api/openapi.yaml"
      provides: "청크 B 엔드포인트 계약"
      contains: "/api/members/me/reservations"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      to: "src/main/kotlin/com/goldwrestling/reservation/ReservationRefundPolicy.kt"
      via: "shouldRestore 판정 후에만 adjustRemainingCount 호출"
      pattern: "shouldRestore"
    - from: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt"
      to: "src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt"
      via: "본인 예약 목록 조회 시 ownedByMember 필수 결합"
      pattern: "ownedByMember"
---

<objective>
회원의 **예약 취소·변경 API**와 **본인 예약 목록 조회**를 만든다. 청크 B의 마지막 플랜이므로
`openapi.yaml`도 재생성한다.

Purpose: 취소는 예약 생성의 역방향이지만 대칭이 아니다 — 복구 여부가 이용권 상태에 따라 갈리고(D-091),
변경은 "취소 + 재예약"을 **하나의 트랜잭션**에서 처리해야 한다(D-090). FE가 취소 API와 예약 API를
두 번 부르게 하면 그 사이에 자리를 뺏긴 회원이 양쪽 다 잃는다. 또 본인 예약 목록은 IDOR가 가장 나기 쉬운
지점이라 소유권 조건을 **빠뜨릴 수 없는 형태**로 제공한다.

Output: `MemberReservationService.cancel/change/findMyReservations`, 컨트롤러 3개 엔드포인트,
`ReservationSpecifications`, 통합테스트 2종, 재생성된 `openapi.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/deliver-phase-chunk/SKILL.md
@docs/policies.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`reservation/MemberReservationService.kt`(04-07): `fun reserve(memberId: Long, request: ReserveRequest): ReservationResponse`
  — 이 파일에 `cancel`/`change`/`findMyReservations`를 추가한다
`reservation/Reservation.kt`(04-09): `assertCancelableByMember(today)`, `assertChangeableByMember(today, newClassType)`
`reservation/ReservationRefundPolicy.kt`(04-09): `fun shouldRestore(passStatus: PassStatus, refundRequested: Boolean): Boolean`
`reservation/ReservationRepository.kt`(04-03):
- `fun findByIdAndMemberId(id: Long, memberId: Long): Reservation?`
- `fun cancelByMemberIfActive(id: Long, canceledBy: Member, canceledAt: OffsetDateTime): Int`
- `JpaSpecificationExecutor<Reservation>` 구현
`reservation/ReservationPassPolicy.kt`(04-06): `DEDUCTION_AMOUNT`(= `BigDecimal("1.0")`)
`schedule/ClassSessionRepository.kt`(04-03): `fun decrementReservedCount(id: Long): Int`
`pass/PassRepository.kt`: `fun adjustRemainingCount(id: Long, amount: BigDecimal): Int`
`pass/TransactionReason.kt`: `CANCEL_REFUND`
`notification/NotificationService.kt`(04-07):
- `fun createReservationCanceled(reservation: Reservation, byAdmin: Boolean)`
- `fun createReservationChanged(newReservation: Reservation, byAdmin: Boolean)`
`member/dto/PageResponse.kt`: `PageResponse.from(page, mapper)` — 그대로 재사용(새 DTO 만들지 않음)
`pass/PassTransactionSpecifications.kt`: `fun ownedByMember(memberId: Long): Specification<PassTransaction>`
  — **non-null 반환**으로 호출부가 조건을 빼먹을 수 없게 한 IDOR 방어 관례의 원본
`member/AdminMemberController.kt`: `@ParameterObject @ModelAttribute @Valid condition` + `PageResponse` 반환 관례 (D-054)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 예약 취소·변경 서비스 (단일 트랜잭션)</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/ChangeReservationRequest.kt,
    src/test/kotlin/com/goldwrestling/reservation/MemberReservationCancellationTest.kt
  </files>
  <behavior>
    **취소**
    - 내일 수업 예약 취소 → 예약 `status = CANCELED`, `refunded = true`, `canceledByMember` 세팅,
      `class_session.reserved_count` −1, `pass.remaining_count` +1.0,
      `PassTransaction(CANCEL_REFUND, +1.0, member 주체)` 1행, `Notification(RESERVATION_CANCELED_BY_MEMBER)` 1행
    - 당일 수업 예약 취소 → `SameDayModificationNotAllowedException`, 아무 것도 변하지 않음
    - 이미 취소된 예약 재취소 → `ReservationAlreadyCanceledException`
    - 다른 회원의 예약 id로 취소 → `ReservationNotFoundException` (403이 아니라 404 — 존재 여부를 흘리지 않는다)
    - **차감했던 이용권이 등록 취소(`CANCELED`) 상태** → 예약은 CANCELED로 전환되고 `reserved_count`는 −1 되지만
      `pass.remaining_count`는 그대로이고 `CANCEL_REFUND` 이력이 **생기지 않는다** (D-091, Pitfall 2)
    - 차감했던 이용권이 **유효기간 만료** 상태(ACTIVE) → 정상 복구된다

    **변경**
    - 다른 타임으로 변경 → 기존 예약 CANCELED + 새 예약 ACTIVE 1행,
      `PassTransaction` **2건**(`CANCEL_REFUND` +1.0, `RESERVE` −1.0), 잔여는 순증감 0,
      `Notification(RESERVATION_CHANGED_BY_MEMBER)` 1건(취소·예약 각각이 아니라 변경 1건)
    - 새 타임이 정원 초과 → `ReservationCapacityExceededException`, **기존 예약이 그대로 ACTIVE로 남는다**
    - 새 타임이 예약 창 밖 → `ReservationWindowClosedException`, 기존 예약 유지
    - 수업 종류가 다른 타임으로 변경 → `ReservationTypeMismatchException`, 기존 예약 유지
    - 당일 예약 변경 → `SameDayModificationNotAllowedException`, 기존 예약 유지

    **본인 목록**
    - 활성 예약만 반환하고 취소된 예약은 제외된다 (D-090)
    - 다른 회원의 예약이 절대 포함되지 않는다
    - `classDate` 오름차순, 같은 날이면 `startTime` 오름차순
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt (04-07 산출물 — `reserve`의 조립 순서와 주석)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationRefundPolicy.kt, Reservation.kt (04-09)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt (04-03 — `cancelByMemberIfActive` KDoc의 Pitfall 2 경고)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (`cancel` 메서드 — 조회 → 판정 → 조건부 UPDATE → 이력 저장 순서)
    - src/main/kotlin/com/goldwrestling/notification/NotificationService.kt (04-07)
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "Pitfall 2", "Pitfall 5", "Pattern 5"
    - docs/policies.md §3 (회원 취소·변경 규칙)
  </read_first>
  <action>
    (1) `ChangeReservationRequest(classScheduleId: Long, classDate: LocalDate)` — 새 타임만 받는다.
    `reserve`와 같은 형태로 **세션 id를 받지 않는다**.

    (2) `MemberReservationService.cancel(memberId: Long, reservationId: Long): ReservationResponse` —
    `@Transactional`. 아래 순서 그대로 구현하고 각 단계에 번호 주석을 남긴다.
      ① `reservationRepository.findByIdAndMemberId(reservationId, memberId)` → null이면 `ReservationNotFoundException`.
         **`findById`를 쓰지 않는다** — 소유권 조건이 조회에 포함돼야 IDOR가 구조적으로 막힌다
      ② `reservation.assertCancelableByMember(LocalDate.now(clock))`
      ③ 벌크 UPDATE 전에 `sessionId`·`passId`·`pass.status`를 지역 변수로 미리 꺼낸다
         (`clearAutomatically`로 준영속화되어 이후 LAZY 접근이 실패한다)
      ④ `reservationRepository.cancelByMemberIfActive(reservationId, member, OffsetDateTime.now(clock))` —
         반환 0이면 `ReservationAlreadyCanceledException`(경쟁 패배)
      ⑤ `classSessionRepository.decrementReservedCount(sessionId)` — 반환 0은 이미 0이라는 뜻이라 무시하지 않고
         `IllegalStateException`으로 드러낸다(활성 예약이 있었다면 카운트가 0일 수 없다)
      ⑥ `ReservationRefundPolicy.shouldRestore(passStatus, refundRequested = true)` 판정
      ⑦ 판정이 true일 때만 `passRepository.adjustRemainingCount(passId, DEDUCTION_AMOUNT)` +
         `PassTransaction(CANCEL_REFUND, +1.0, admin = null, member = 회원)` 저장.
         **판정이 false면 두 작업을 모두 건너뛴다** — 반환 0을 사후 해석하지 않는다(Pitfall 2)
      ⑧ `notificationService.createReservationCanceled(reservation, byAdmin = false)`

    (3) `MemberReservationService.change(memberId: Long, reservationId: Long, request: ChangeReservationRequest): ReservationResponse` —
    `@Transactional` **하나**로 처리한다(D-090). 순서:
      ① 기존 예약 조회(소유권 포함) + 새 시간표 조회
      ② `reservation.assertChangeableByMember(today, newSchedule.classType)` — 종류 불일치·당일을 여기서 거부
      ③ 기존 예약 취소 경로(위 cancel의 ④~⑦)를 실행 — `CANCEL_REFUND` 이력이 남는다
      ④ 새 예약 생성 경로(`reserve`의 ③~⑪)를 실행 — `RESERVE` 이력이 남는다
      ⑤ `notificationService.createReservationChanged(newReservation, byAdmin = false)` — **알림은 1건만**
    ③④는 `reserve`/`cancel`과 **같은 private 헬퍼를 공유**하도록 리팩터링한다 — 차감/복구 경로가 두 벌이 되면
    D-021 보장이 흩어진다. 새 예약이 실패하면 트랜잭션 전체가 롤백되어 기존 예약이 그대로 남는다는 사실을
    메서드 KDoc에 명시한다.
    **기존 예약 행을 재사용해 필드만 바꾸지 않는다**(Pitfall 5) — 부분 유니크 인덱스는 INSERT에만 작동하고,
    원장 요구사항(취소 +1 / 예약 −1 두 건)과도 충돌한다.

    (4) `MemberReservationCancellationTest` — `<behavior>`의 취소 6종 + 변경 5종을 각각 `@Test`로 검증한다.
    "등록 취소된 이용권" 픽스처를 **반드시 포함**한다 — 이 분기가 없으면 Pitfall 2가 커버되지 않는다.
    변경 실패 케이스는 매번 "기존 예약이 여전히 ACTIVE"를 함께 단언한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.MemberReservationCancellationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.MemberReservationCancellationTest"` 종료 코드 0, 11개 이상 테스트 실행
    - 테스트가 "등록 취소된 이용권으로 잡힌 예약을 취소하면 잔여가 그대로이고 `CANCEL_REFUND` 이력이 생기지 않는다"를 단언한다
    - 테스트가 "유효기간 만료 이용권도 복구된다"를 단언한다
    - 변경 실패 4종 테스트가 각각 "기존 예약이 여전히 ACTIVE"를 단언한다
    - 변경 성공 테스트가 `PassTransaction` 2건(`CANCEL_REFUND`, `RESERVE`)과 `Notification` 1건을 단언한다
    - `MemberReservationService.kt`가 `findByIdAndMemberId`를 쓰고 `findById(`를 회원 경로에서 쓰지 않는다
    - `MemberReservationService.kt`가 `shouldRestore` 판정 결과로 `adjustRemainingCount` 호출을 감싼다 (반환값 사후 무시가 아님)
  </acceptance_criteria>
  <done>취소·변경이 원장 불변식을 지키며 원자적으로 처리되고, 등록 취소 이용권 분기가 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 회원 취소·변경·본인 목록 엔드포인트 + IDOR 방어 Specification</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt,
    src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/MyReservationSearchCondition.kt,
    src/test/kotlin/com/goldwrestling/reservation/MemberReservationControllerTest.kt
  </files>
  <behavior>
    - `POST /api/members/me/reservations/{reservationId}/cancellation` → 200 + `ReservationResponse`(status=CANCELED)
    - `POST /api/members/me/reservations/{reservationId}/change` → 200 + 새 예약 `ReservationResponse`
    - `GET /api/members/me/reservations` → 200 + `PageResponse<ReservationResponse>`, 활성 예약만
    - 다른 회원의 예약 id로 취소·변경 → 404 ProblemDetail `RESERVATION_NOT_FOUND`
    - 다른 회원의 예약이 목록에 포함되지 않는다 (같은 세션에 두 회원이 예약해도 각자 1건만 본다)
    - 취소된 예약이 목록에 포함되지 않는다
    - `PENDING` 회원 → 403 `MEMBER_NOT_ACTIVE` (세 엔드포인트 모두)
    - `page`/`size` 파라미터가 동작하고 기본값이 적용된다 (D-035)
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt (04-08 산출물 — 여기에 엔드포인트 2개를 추가한다)
    - src/main/kotlin/com/goldwrestling/pass/PassTransactionSpecifications.kt (전체 — `ownedByMember` non-null 반환 관례와 "IDOR 방어의 핵심" KDoc)
    - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt (LIKE 이스케이프·정규화 관례 — 재구현하지 않는다)
    - src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionSearchCondition.kt (검색 조건 DTO + `page`/`size` 기본값 관례, D-035·D-054)
    - src/main/kotlin/com/goldwrestling/member/dto/PageResponse.kt
    - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt (`POST .../cancellation` 하위 리소스 관례)
    - src/test/kotlin/com/goldwrestling/pass/MemberPassControllerTest.kt (컨트롤러 통합테스트 골격)
  </read_first>
  <action>
    (1) `ReservationSpecifications.kt` — `PassTransactionSpecifications` 관례를 그대로 따른다.
    · `fun ownedByMember(memberId: Long): Specification<Reservation>` — **non-null 반환**.
      호출부가 이 조건을 빼먹을 수 없게 만드는 것이 목적이라는 KDoc을 원본과 같은 취지로 남긴다
    · `fun hasStatus(status: ReservationStatus): Specification<Reservation>`
    · `fun classDateBetween(from: LocalDate?, to: LocalDate?): Specification<Reservation>?` (nullable — 04-12 관리자 조회가 재사용)
    · `fun hasClassType(classType: ClassType?): Specification<Reservation>?`
    · `fun memberKeywordContains(keyword: String?): Specification<Reservation>?` — `MemberSpecifications.keywordContains`의
      이스케이프·정규화 로직을 **재구현하지 않고 재사용**한다(같은 방식으로 조인 경로만 다르게)

    (2) `MyReservationSearchCondition(page: Int = 0, size: Int = 20)` — 회원 본인 목록은 필터가 필요 없다.
    `@ParameterObject`로 노출한다(D-054 — 개별 `@RequestParam`으로 펼치지 않는다).

    (3) `MemberReservationController`에 엔드포인트 2개를 추가한다. 세 엔드포인트 모두
    **첫 줄에서 `memberStateGate.requireActive(principal)`를 호출**한다.
    · `@PostMapping("/reservations/{reservationId}/cancellation")` → `service.cancel(...)`
    · `@PostMapping("/reservations/{reservationId}/change")` → `service.change(...)`
    · `@GetMapping("/reservations")` + `@ParameterObject @ModelAttribute @Valid condition` →
      `service.findMyReservations(memberId, condition)` → `PageResponse<ReservationResponse>`
    본인 목록 조회는 `Specification.allOf(ownedByMember(memberId), hasStatus(ACTIVE))` 조합으로만 구성한다 —
    `ownedByMember`를 optional로 다루는 코드를 만들지 않는다.
    정렬은 `Sort.by(ASC, "classDate", "startTime")`.

    (4) `MemberReservationControllerTest`에 `<behavior>` 8개 항목을 추가 `@Test`로 검증한다.
    "다른 회원 예약이 목록에 없다"는 **같은 세션에 두 회원이 예약한 픽스처**로 확인한다 —
    회원이 하나뿐인 픽스처로는 IDOR가 검증되지 않는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.MemberReservationControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.MemberReservationControllerTest"` 종료 코드 0, 17개 이상 테스트 실행(04-08의 9종 + 이번 8종)
    - `ReservationSpecifications.ownedByMember` 반환 타입이 nullable이 아니다 (`Specification<Reservation>`)
    - `MemberReservationController.kt`의 세 엔드포인트 모두에서 `memberStateGate.requireActive`가 호출된다
    - 테스트가 "타인 예약 id 취소 시도 → 404 `RESERVATION_NOT_FOUND`"를 단언한다 (403이 아님)
    - 테스트가 두 회원이 같은 세션에 예약한 상태에서 각자 목록에 1건씩만 보임을 단언한다
    - `MyReservationSearchCondition`이 `@ParameterObject`로 노출되고 개별 `@RequestParam`이 아니다
  </acceptance_criteria>
  <done>회원이 예약을 취소·변경·조회할 수 있고, 소유권 조건이 빠질 수 없는 구조로 결합된다</done>
</task>

<task type="auto">
  <name>Task 3: openapi.yaml 재생성 + 청크 B 마감 점검</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md (openapi 재생성 절차)
    - .claude/skills/deliver-phase-chunk/SKILL.md §3
    - docs/api/openapi.yaml (청크 A 재생성 이후 상태)
  </read_first>
  <action>
    청크 B(wave 6~10)가 끝났으므로 API 계약을 갱신한다.
    · `docker compose up -d` → `./gradlew generateApiDocs`로 `docs/api/openapi.yaml` 재생성 (D-029)
    · 재생성 결과에 `POST /api/members/me/reservations`,
      `POST /api/members/me/reservations/{reservationId}/cancellation`,
      `POST /api/members/me/reservations/{reservationId}/change`,
      `GET /api/members/me/reservations` 4개 경로와
      `ReserveRequest`·`ChangeReservationRequest`·`ReservationResponse` 스키마가 포함됐는지 확인한다
    · `./gradlew ktlintFormat` → `./gradlew build`
    · 동시성 테스트(`ReservationCapacityConcurrencyTest`)를 **2회 연속** 실행해 flaky가 아님을 확인한다
    이후 `deliver-phase-chunk` 절차에 따라 `feature/phase-04b-reservation` → dev PR을 생성한다.
    **PR 머지는 사용자가 한다.**
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` 종료 코드 0
    - `grep -c "/api/members/me/reservations" docs/api/openapi.yaml` >= 1
    - `grep -c "ChangeReservationRequest" docs/api/openapi.yaml` >= 1
    - `./gradlew test --tests "com.goldwrestling.reservation.ReservationCapacityConcurrencyTest"` 2회 연속 종료 코드 0
  </acceptance_criteria>
  <done>회원 예약 기능 전체가 계약과 함께 리뷰 가능한 단위로 마감된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 회원 요청 → 타인 예약 자원 | `reservationId`가 경로 변수라 임의 값이 들어온다 — IDOR의 전형적 진입점 |
| 취소 처리 → 이용권 원장 | 복구 조건을 잘못 잡으면 잔여가 실제와 어긋난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-45 | Tampering / Information Disclosure | 타인 `reservationId`로 취소·변경 (IDOR) | mitigate | 조회를 `findByIdAndMemberId`로만 수행하고 없으면 `RESERVATION_NOT_FOUND`(404). 403은 자원 존재를 알려주므로 쓰지 않는다. 테스트가 404를 단언 |
| T-04-46 | Information Disclosure | 본인 목록에 타 회원 예약 노출 | mitigate | `ReservationSpecifications.ownedByMember` non-null 반환 + 두 회원 픽스처 테스트 |
| T-04-47 | Tampering | 변경을 통한 당일 취소 우회 | mitigate | `assertChangeableByMember`가 당일 검사를 포함(04-09). 컨트롤러 테스트가 409 `SAME_DAY_MODIFICATION_NOT_ALLOWED`를 단언 |
| T-04-48 | Tampering | 변경 실패 시 기존 예약까지 잃음 | mitigate | 취소+재예약을 단일 `@Transactional`로 묶고, 실패 4종 테스트가 "기존 예약 ACTIVE 유지"를 단언 |
| T-04-49 | Repudiation | 복구했는데 이력이 없거나, 복구 안 했는데 이력이 남음 | mitigate | `shouldRestore` 판정 하나로 `adjustRemainingCount`와 `PassTransaction` 저장을 함께 감싼다 |
| T-04-50 | Elevation of Privilege | 비활성 회원의 취소·변경·조회 | mitigate | 세 엔드포인트 모두 `memberStateGate.requireActive` |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `openapi.yaml`이 재생성되어 커밋 가능한 상태
- 청크 B가 wave 6~10으로 닫힌다 — 예약 생성과 그 방어 코드(정원·중복·동시성 테스트)가 같은 PR 안에 있다 (D-084)
</verification>

<success_criteria>
- 회원이 예약을 취소·변경·조회할 수 있고 잔여가 항상 실제와 일치한다
- 변경이 단일 트랜잭션으로 처리되어 실패 시 기존 예약이 보존된다
- IDOR이 조회 메서드 시그니처 수준에서 막힌다
- `openapi.yaml`이 코드와 동기화되어 청크 B PR을 낼 수 있다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-10-SUMMARY.md` when done.
**청크 B 마지막 플랜** — `deliver-phase-chunk` 절차에 따라 `feature/phase-04b-reservation` → dev PR을 생성하고
PR URL을 보고한다. 머지는 사용자가 한다 (D-084).
</output>
</content>
