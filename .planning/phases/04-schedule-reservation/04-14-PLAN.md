---
phase: 04-schedule-reservation
plan: 14
type: execute
wave: 13
depends_on: ["04-13"]
files_modified:
  - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt
  - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt
  - src/main/kotlin/com/goldwrestling/schedule/dto/SuspendClassSessionRequest.kt
  - src/main/kotlin/com/goldwrestling/schedule/dto/ClassSessionResponse.kt
  - src/test/kotlin/com/goldwrestling/schedule/ClassSessionSuspensionTest.kt
autonomous: true
requirements: [RESV-09, NOTIF-01]

must_haves:
  truths:
    - "관리자가 특정 날짜의 수업을 휴강 처리하면 그 수업의 활성 예약이 전부 자동 취소된다"
    - "자동 취소된 예약마다 차감이 CLASS_CANCELED_REFUND로 복구된다"
    - "휴강 알림은 취소 건수와 무관하게 세션당 1건이다"
    - "휴강된 타임은 회원 시간표에서 예약 불가로 표시되고 새 예약이 거부된다"
    - "휴강 해제하면 그 타임이 다시 예약 가능해지고, 취소됐던 예약은 복원되지 않는다"
    - "아직 세션 행이 없는 타임도 휴강 처리할 수 있다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt"
      provides: "휴강 처리 캐스케이드 / 휴강 해제"
      contains: "fun suspend"
    - path: "src/test/kotlin/com/goldwrestling/schedule/ClassSessionSuspensionTest.kt"
      provides: "일괄 취소·복구·알림 1건·재예약 차단 검증"
      contains: "class ClassSessionSuspensionTest"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt"
      to: "src/main/kotlin/com/goldwrestling/notification/NotificationService.kt"
      via: "createClassSessionSuspended를 세션당 정확히 1회 호출"
      pattern: "createClassSessionSuspended"
    - from: "src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt"
      via: "CLASS_CANCELED_REFUND 사유로 복구 이력 기록"
      pattern: "CLASS_CANCELED_REFUND"
---

<objective>
**휴강 처리와 휴강 해제**(RESV-09)를 구현한다 — 활성 예약 일괄 취소 + 차감 복구(`CLASS_CANCELED_REFUND`) +
세션당 1건 요약 알림, 그리고 그 타임의 예약 차단.

Purpose: 휴강은 이 phase에서 유일하게 **한 요청이 N건의 예약을 건드리는** 경로다. N건 각각이 이용권 복구와
이력을 남겨야 하므로 개별 취소와 같은 정확성이 요구되면서, 알림만은 세션당 1건으로 묶어야 한다(D-093) —
정원 10 수업을 휴강하면 알림 10건이 관리자 알림함을 덮고 Phase 6의 미확인 카운트를 왜곡한다.
또 휴강 해제는 **예약을 복원하지 않는다**(CONTEXT.md 확정) — 복원 시점에 그 사이 횟수를 다른 데 쓴 회원은
잔여가 음수가 되고, 이는 Core Value를 정면으로 깬다.

Output: `AdminScheduleService.suspend/resume`, 엔드포인트 2개, 요청·응답 DTO, 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`schedule/ClassSessionService.kt`(04-05): `fun getOrCreate(schedule: ClassSchedule, classDate: LocalDate): ClassSession`
  — 아직 예약이 없어 세션 행이 없는 타임도 휴강할 수 있어야 하므로 이 경로를 쓴다 (D-090)
`schedule/ClassSession.kt`(04-03): `fun assertSuspendable()`, `fun assertResumable()`
`schedule/ClassSessionRepository.kt`(04-03):
- `fun suspendIfScheduled(id: Long, reason: String, canceledBy: Admin, canceledAt: OffsetDateTime): Int`
- `fun resumeIfCanceled(id: Long): Int`
- `fun decrementReservedCount(id: Long): Int`
`reservation/ReservationRepository.kt`(04-03):
- `fun findAllByClassSessionIdAndStatus(classSessionId: Long, status: ReservationStatus): List<Reservation>`
- `fun cancelByAdminIfActive(id: Long, canceledBy: Admin, refunded: Boolean, canceledAt: OffsetDateTime): Int`
`reservation/ReservationRefundPolicy.kt`(04-09): `fun shouldRestore(passStatus: PassStatus, refundRequested: Boolean): Boolean`
`reservation/AdminReservationService.kt`(04-13): 복구 처리 공유 헬퍼 — 휴강도 이 헬퍼를 재사용한다
`notification/NotificationService.kt`(04-07): `fun createClassSessionSuspended(classSession: ClassSession, canceledCount: Int)`
`pass/TransactionReason.kt`: `CLASS_CANCELED_REFUND` (이 phase에서 처음 사용된다)
`schedule/ScheduleExceptions.kt`(04-01): `ClassSessionCanceledException`, `ClassSessionNotCanceledException`

CONTEXT.md 락인: **휴강 해제 시 예약 자동 복원을 만들지 않는다.** 회원이 직접 다시 잡는다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 휴강 처리 캐스케이드 (일괄 취소 + 복구 + 요약 알림 1건)</name>
  <files>
    src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt,
    src/main/kotlin/com/goldwrestling/schedule/dto/SuspendClassSessionRequest.kt,
    src/main/kotlin/com/goldwrestling/schedule/dto/ClassSessionResponse.kt,
    src/test/kotlin/com/goldwrestling/schedule/ClassSessionSuspensionTest.kt
  </files>
  <behavior>
    - 활성 예약 3건이 있는 수업 휴강 → 3건 모두 `status = CANCELED`, `refunded = true`, `canceledByAdmin` 세팅
    - 3건 각각에 대해 `pass.remaining_count` +1.0 및 `PassTransaction(CLASS_CANCELED_REFUND, +1.0, admin 주체)` 1행
      → 이력 총 3건
    - `Notification`은 **정확히 1건**(`type = CLASS_SESSION_SUSPENDED`, `reservation = null`,
      `classSession = 해당 세션`, `message`에 취소 건수 3 포함)
    - 세션 `status = CANCELED`, `canceled_at`·`cancel_reason`·`canceled_by_admin_id`가 모두 채워진다
    - 세션 `reserved_count`가 0이 된다
    - 예약이 0건인 수업 휴강 → 세션만 CANCELED, `Notification` 1건(취소 건수 0), `PassTransaction` 0건
    - **세션 행이 아직 없는 타임 휴강** → 세션이 생성된 뒤 CANCELED로 전환된다
    - 이미 휴강된 수업 재휴강 → `ClassSessionCanceledException`, 아무 것도 변하지 않는다
    - 취소된 예약 중 차감 이용권이 등록 취소 상태인 건 → 예약은 취소되지만 복구·이력이 생기지 않는다 (D-087)
    - 휴강 후 그 타임에 새 예약 시도 → `ClassSessionCanceledException` (409 `CLASS_SESSION_CANCELED`)
    - 휴강 후 회원 시간표 조회 → 그 셀 `suspended = true`, `bookable = false`
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt (04-11 산출물 — 여기에 메서드를 추가한다)
    - src/main/kotlin/com/goldwrestling/reservation/AdminReservationService.kt (04-13 — 대리 취소의 복구 처리 헬퍼. 휴강이 재사용한다)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt, ReservationRefundPolicy.kt
    - src/main/kotlin/com/goldwrestling/schedule/ClassSessionService.kt, ClassSessionRepository.kt, ClassSession.kt
    - src/main/kotlin/com/goldwrestling/notification/NotificationService.kt (04-07)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (벌크 UPDATE 전 스칼라 값 미리 꺼내기 관례)
    - docs/policies.md §7 (휴강 처리)
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "Pitfall 2"(0행 반환의 이중 의미), "휴강 캐스케이드"
  </read_first>
  <action>
    (1) `SuspendClassSessionRequest(classScheduleId: Long, classDate: LocalDate, reason: String)` —
    `reason`은 `@field:NotBlank @field:Size(max = 500)`. 세션 id가 아니라 (시간표, 날짜)로 받는다 —
    아직 세션 행이 없는 타임도 휴강할 수 있어야 한다(D-090).
    `ClassSessionResponse(id, classScheduleId, classDate, classType, startTime, endTime, capacity,
    reservedCount, status, cancelReason, canceledAt, canceledReservationCount)`.

    (2) `AdminScheduleService.suspend(adminId: Long, request: SuspendClassSessionRequest): ClassSessionResponse` —
    `@Transactional`. 순서:
      ① 시간표 조회 → 없으면 `ClassScheduleNotFoundException`. `schedule.dayOfWeek`와 `classDate`의 요일이
         다르면 같은 예외로 거부한다
      ② `classSessionService.getOrCreate(schedule, classDate)` → `session.assertSuspendable()`
      ③ `classSessionRepository.suspendIfScheduled(sessionId, reason, admin, now)` —
         반환 0이면 `ClassSessionCanceledException`(경쟁 패배). **세션을 먼저 휴강 상태로 만든다** —
         그래야 이 트랜잭션이 진행되는 동안 새 예약이 정원 조건부 UPDATE(`status = SCHEDULED` 조건)에서 거부된다
      ④ `reservationRepository.findAllByClassSessionIdAndStatus(sessionId, ACTIVE)`로 활성 예약을 **한 번에** 조회한다
         (건별 조회 금지 — N+1). 복구에 필요한 `pass.status`를 함께 쓰므로 `pass`를 fetch join으로 가져온다
      ⑤ 각 예약에 대해: `cancelByAdminIfActive(id, admin, refunded = true, now)` →
         `ReservationRefundPolicy.shouldRestore(passStatus, refundRequested = true)`가 true일 때만
         `adjustRemainingCount(passId, +1.0)` + `PassTransaction(CLASS_CANCELED_REFUND, +1.0, admin = 관리자, member = null)`.
         **`CANCEL_REFUND`가 아니라 `CLASS_CANCELED_REFUND`를 쓴다** — 회원 취소와 휴강을 원장에서 구분해야 한다
      ⑥ `reserved_count`를 0으로 만든다 — 건별 `decrementReservedCount`를 N번 부르는 대신
         `ClassSessionRepository`에 `resetReservedCount(id: Long): Int`(조건부 UPDATE, `where id = :id`)를 추가해
         한 번에 처리한다. 건별 호출은 N번의 UPDATE와 N번의 영속성 컨텍스트 clear를 유발한다
      ⑦ `notificationService.createClassSessionSuspended(session, canceledCount)` — **정확히 1회**.
         반복문 안에서 부르지 않는다 (D-093 요약형)
      ⑤에서 벌크 UPDATE가 반복되므로 **각 반복 전에 필요한 스칼라 값(reservationId·passId·passStatus)을
      미리 리스트로 뽑아둔다** — `clearAutomatically`로 매 UPDATE마다 영속성 컨텍스트가 비워져
      루프 도중 엔티티 LAZY 접근이 실패한다. 이 함정을 주석으로 남긴다

    (3) `ClassSessionSuspensionTest` — `<behavior>` 10개 항목을 각각 `@Test`로 검증한다.
    "알림 정확히 1건"과 "이력 정확히 3건"을 같은 테스트에서 함께 단언한다 — 이 대비가 D-093의 요지다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.schedule.ClassSessionSuspensionTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.schedule.ClassSessionSuspensionTest"` 종료 코드 0, 10개 이상 테스트 실행
    - 테스트가 "예약 3건 휴강 → `PassTransaction` 3건 + `Notification` 1건"을 한 테스트에서 함께 단언한다
    - 이력의 `reason`이 `CLASS_CANCELED_REFUND`임을 단언한다 (`CANCEL_REFUND`가 아님)
    - 테스트가 "세션 행이 없는 타임도 휴강 가능"을 단언한다
    - 테스트가 "휴강 후 새 예약 시도 → `CLASS_SESSION_CANCELED`"를 단언한다
    - `AdminScheduleService.kt`가 `createClassSessionSuspended`를 반복문 밖에서 1회만 호출한다
    - `AdminScheduleService.kt`가 활성 예약을 건별이 아닌 배치로 조회한다
  </acceptance_criteria>
  <done>휴강 한 번이 N건의 예약을 정확히 정리하고, 관리자 알림함은 1건만 받는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 휴강 해제 + 엔드포인트 2개</name>
  <files>
    src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt,
    src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt,
    src/test/kotlin/com/goldwrestling/schedule/ClassSessionSuspensionTest.kt
  </files>
  <behavior>
    - `POST /api/admin/class-sessions/suspension` → 200 + `ClassSessionResponse`(status=CANCELED, canceledReservationCount)
    - `POST /api/admin/class-sessions/{classSessionId}/resumption` → 200 + `ClassSessionResponse`(status=SCHEDULED)
    - 휴강 해제 후 세션의 `canceled_at`·`cancel_reason`·`canceled_by_admin_id`가 모두 null이 된다
    - 휴강 해제 후 그 타임에 새 예약이 성공한다
    - **휴강으로 취소됐던 예약은 복원되지 않는다** — 해제 후에도 그 예약들은 CANCELED 그대로이고
      `reserved_count`는 0이다
    - 휴강 상태가 아닌 세션에 해제 시도 → `ClassSessionNotCanceledException` (409 `CLASS_SESSION_NOT_CANCELED`)
    - 회원 토큰 → 403, 인증 없음 → 401
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt (04-11 산출물 — 여기에 엔드포인트를 추가한다)
    - src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt (`resumeIfCanceled` — 취소 메타 4개를 함께 되돌린다)
    - src/main/kotlin/com/goldwrestling/schedule/ClassSession.kt (`assertResumable`)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt (POST 하위 리소스 관례)
    - .planning/phases/04-schedule-reservation/04-CONTEXT.md "휴강 (RESV-09)" — 자동 복원 기각 근거
  </read_first>
  <action>
    (1) `AdminScheduleService.resume(adminId: Long, classSessionId: Long): ClassSessionResponse` — `@Transactional`.
    · 세션 조회 → `session.assertResumable()` → `classSessionRepository.resumeIfCanceled(id)` —
      반환 0이면 `ClassSessionNotCanceledException`
    · `reserved_count`는 0 그대로 둔다 — 취소된 예약을 복원하지 않으므로 카운트도 되돌리지 않는다
    · **예약 복원 로직을 만들지 않는다.** 메서드 KDoc에 근거를 남긴다: 복원 시점에 그 사이 횟수를 다른 데
      써버린 회원은 잔여가 음수가 되고, 이는 Core Value("잔여 = 실제 사용 가능 횟수")를 정면으로 깬다.
      회원이 직접 다시 예약한다 (CONTEXT.md 확정)
    · 휴강 해제 알림은 만들지 않는다 — `NotificationType`에 해당 값이 없다(D-093 이벤트 6종에 미포함)

    (2) `AdminScheduleController`에 엔드포인트 2개를 추가한다. 경로는 보드(`/api/admin/schedule/board`)와 분리해
    `@RequestMapping("/api/admin/class-sessions")`를 갖는 별도 컨트롤러로 두거나, 기존 컨트롤러에
    `@PostMapping("/class-sessions/suspension")` 형태로 붙인다 — **경로 문자열은 아래로 고정한다**:
    · `POST /api/admin/class-sessions/suspension` (body: `SuspendClassSessionRequest`)
    · `POST /api/admin/class-sessions/{classSessionId}/resumption`
    `@Transactional`을 붙이지 않는다. springdoc 애노테이션은 `AdminPassController` 형식을 따른다.

    (3) `ClassSessionSuspensionTest`에 `<behavior>` 7개 항목을 추가 `@Test`로 검증한다.
    "해제 후에도 취소된 예약이 복원되지 않는다"를 **명시적으로** 단언한다 — 이후 누군가 "친절하게" 복원 로직을
    넣으면 이 테스트가 실패해 설계 결정을 지켜준다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.schedule.ClassSessionSuspensionTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.schedule.ClassSessionSuspensionTest"` 종료 코드 0, 17개 이상 테스트 실행(Task 1의 10종 + 이번 7종)
    - 테스트가 "휴강 해제 후 취소됐던 예약이 여전히 CANCELED이고 `reserved_count`가 0"임을 단언한다
    - 테스트가 "휴강 해제 후 새 예약 성공"을 단언한다
    - 테스트가 "휴강 상태가 아닌 세션 해제 시도 → 409 `CLASS_SESSION_NOT_CANCELED`"를 단언한다
    - `AdminScheduleService.resume`에 예약 복원 로직이 없다 (`Reservation` INSERT·status 변경 없음)
    - 엔드포인트 경로가 `/api/admin/class-sessions/suspension`과 `/api/admin/class-sessions/{classSessionId}/resumption`이다
  </acceptance_criteria>
  <done>휴강을 되돌릴 수 있고, 되돌려도 잔여가 음수가 되지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 요청 → N건 예약 일괄 조작 | 한 요청이 여러 회원의 이용권 잔여를 바꾸는 유일한 경로 |
| 휴강 처리 중 → 동시 예약 요청 | 휴강 트랜잭션 진행 중에 들어온 예약이 취소 대상에서 빠질 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-66 | Tampering | 휴강 처리 중 새 예약이 끼어들어 취소되지 않은 채 남음 | mitigate | 세션을 **먼저** `CANCELED`로 전환한 뒤 예약을 조회한다. `incrementReservedCountIfCapacityAvailable`의 `status = SCHEDULED` 조건이 이후 예약을 거부한다 |
| T-04-67 | Repudiation | 휴강 복구를 회원 취소와 구분 불가하게 기록 | mitigate | `CLASS_CANCELED_REFUND` 사유 사용. 테스트가 `reason` 값을 단언 |
| T-04-68 | Repudiation | 취소 주체·사유 없이 휴강된 세션 | mitigate | `ck_class_session_cancellation` CHECK가 취소 메타 3개 완전성을 강제 + `suspendIfScheduled`가 함께 기록 |
| T-04-69 | Denial of Service | 휴강 알림 N건 폭증으로 관리자 알림함·Phase 6 미확인 카운트 왜곡 | mitigate | 세션당 1건 요약형(D-093). acceptance criteria가 반복문 밖 1회 호출을 검사 |
| T-04-70 | Tampering | 휴강 해제 시 예약 자동 복원으로 잔여 음수 발생 | mitigate | 복원 로직을 만들지 않는다. 테스트가 "복원되지 않음"을 명시적으로 고정해 이후 추가를 막는다 |
| T-04-71 | Elevation of Privilege | 회원 토큰으로 휴강 처리 | mitigate | `/api/admin/**` → `hasRole("ADMIN")`. 테스트가 403 단언 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- 휴강 후 회원 시간표 조회·예약 시도가 실제로 차단되는지 통합테스트로 확인 (04-05·04-08 경로 회귀)
- `openapi.yaml` 재생성은 다음 플랜(04-15)에서 한다
</verification>

<success_criteria>
- 휴강 한 번이 활성 예약 전부를 취소·복구하고 원장에 `CLASS_CANCELED_REFUND`로 남긴다
- 알림은 취소 건수와 무관하게 세션당 1건이다
- 휴강된 타임의 예약이 차단되고, 해제 시 다시 열리되 예약은 복원되지 않는다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-14-SUMMARY.md` when done.
청크 C(`feature/phase-04c-admin-ops`) 진행 중 — PR은 wave 14(04-15) 완료 후에 낸다 (D-084).
</output>
</content>
