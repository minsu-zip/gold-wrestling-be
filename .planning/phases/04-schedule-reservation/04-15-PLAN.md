---
phase: 04-schedule-reservation
plan: 15
type: execute
wave: 14
depends_on: ["04-14"]
files_modified:
  - docs/api/openapi.yaml
  - docs/decisions.md
  - docs/glossary.md
  - docs/error-codes.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, RESV-01, RESV-02, RESV-03, RESV-04, RESV-05, RESV-06, RESV-07, RESV-08, RESV-09, NOTIF-01]

must_haves:
  truths:
    - "Phase 4의 13개 요구사항이 모두 자동 테스트로 검증된다"
    - "openapi.yaml이 이 phase가 연 12개 엔드포인트를 모두 담고 코드와 일치한다"
    - "docs/의 용어·에러코드·결정 기록이 실제 구현과 어긋나지 않는다"
    - "실제 브라우저·클라이언트로 회원 예약 흐름과 관리자 운영 흐름이 동작함을 사람이 확인했다"
  artifacts:
    - path: "docs/api/openapi.yaml"
      provides: "Phase 4 전체 API 계약"
      contains: "/api/admin/class-sessions/suspension"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 완료 표시와 플랜 목록"
      contains: "04-15-PLAN.md"
  key_links:
    - from: "docs/error-codes.md"
      to: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      via: "ErrorCodeRegistryTest 양방향 검증"
      pattern: "시간표·예약 코드"
---

<objective>
Phase 4를 마감한다 — 전체 검증, 문서 정합 재확인, `openapi.yaml` 최종 재생성, 그리고 **사람이 직접 확인**하는
회원 예약·관리자 운영 흐름 점검.

Purpose: 이 phase는 15개 플랜·3개 청크에 걸쳐 12개 엔드포인트와 4개 테이블을 만들었다. 각 플랜은 자기 범위만
검증했으므로, 마지막에 **요구사항 13개가 전부 실제로 충족됐는지**를 한 번에 확인해야 한다.
특히 동시성 테스트는 타이밍에 민감해 phase 게이트에서 재확인이 필요하고(RESEARCH Sampling Rate),
"회원이 예약하고 관리자가 그것을 보고 취소한다"는 **경로 전체를 이어 붙인 확인**은 자동 테스트만으로는
빠지기 쉽다(각 테스트가 자기 픽스처를 직접 만들기 때문).

Output: 재생성된 `openapi.yaml`, 문서 정합 수정(있다면), ROADMAP 갱신, 사람 확인 결과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/deliver-phase-chunk/SKILL.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/policies.md
@docs/glossary.md
@docs/decisions.md
@docs/error-codes.md
</context>

<interfaces>
이 phase가 연 엔드포인트 12개 (openapi.yaml에 모두 있어야 한다):
- `GET /api/members/me/schedule` (04-05)
- `POST /api/members/me/reservations` (04-08)
- `POST /api/members/me/reservations/{reservationId}/cancellation` (04-10)
- `POST /api/members/me/reservations/{reservationId}/change` (04-10)
- `GET /api/members/me/reservations` (04-10)
- `GET /api/admin/schedule/board` (04-11)
- `GET /api/admin/reservations` (04-12)
- `POST /api/admin/reservations/{reservationId}/cancellation` (04-13)
- `POST /api/admin/reservations/{reservationId}/change` (04-13)
- `POST /api/admin/class-sessions/suspension` (04-14)
- `POST /api/admin/class-sessions/{classSessionId}/resumption` (04-14)
- (기존) `POST /api/admin/passes/{passId}/cancellation` — D-085 선행 검사가 추가되어 409 응답이 늘었다

이 phase가 만든 마이그레이션: V6(4개 테이블) · V7(시간표 시드 52행) · V8(pass_transaction 주체 확장)
`generateApiDocs` 태스크(D-029): `docker compose up -d`로 Postgres가 떠 있어야 한다
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 전체 검증 + 요구사항 13종 커버리지 확인</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (SCHED-01~03, RESV-01~09, NOTIF-01 정의 원문)
    - .planning/ROADMAP.md Phase 4 절 (성공 기준 5항목, Plans: TBD를 실제 목록으로 채운다)
    - .planning/phases/04-schedule-reservation/ 의 04-01~04-14 SUMMARY (각 플랜이 실제로 무엇을 만들었는지)
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "Phase Requirements → Test Map" (요구사항별 테스트 이름 목록)
  </read_first>
  <action>
    (1) `./gradlew clean build`로 전체 스위트를 실행한다. 실패가 있으면 원인을 고친 뒤 재실행한다.
    (2) `./gradlew test --tests "com.goldwrestling.reservation.ReservationCapacityConcurrencyTest"`와
    `./gradlew test --tests "com.goldwrestling.schedule.ClassSessionConcurrencyTest"`를 각각 **2회 연속** 실행해
    flaky가 아님을 확인한다 (동시성 테스트는 CI 환경에 따라 타이밍이 달라진다 — RESEARCH Sampling Rate).
    (3) 요구사항 13종이 각각 어떤 테스트로 검증되는지 대응표를 만든다. 04-RESEARCH.md의
    "Phase Requirements → Test Map"에 적힌 테스트 이름과 **실제로 존재하는 테스트 파일**을 대조한다.
    대응되는 테스트가 없는 요구사항이 있으면 **그 사실을 SUMMARY에 명시**하고, 빠진 검증을 추가한다.
    (4) `.planning/ROADMAP.md`의 Phase 4 절을 갱신한다:
    · `**Plans**: TBD` → `**Plans**: 15 plans (3개 청크로 납품 — 04a 시간표(w1~5) / 04b 예약(w6~10) / 04c 관리자 운영(w11~14))`
    · 플랜 목록 15줄을 `- [x] 04-NN-PLAN.md — {한 줄 요약} ({요구사항 ID})` 형식으로 추가한다
      (Phase 2·3 절의 형식을 그대로 따른다)
    · Progress 표의 `4. 시간표·예약` 행을 `15/15 | Complete | {오늘 날짜}`로 갱신한다
    · 성공 기준 5항목 각각이 어떤 플랜·테스트로 충족됐는지 한 줄 노트를 추가한다
  </action>
  <verify>
    <automated>./gradlew clean build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew clean build` 종료 코드 0
    - 동시성 테스트 2종이 각각 2회 연속 종료 코드 0
    - `.planning/ROADMAP.md` Phase 4 절에 `TBD` 문자열이 없다
    - `.planning/ROADMAP.md`가 `04-01-PLAN.md`부터 `04-15-PLAN.md`까지 15줄을 포함한다
    - SUMMARY에 요구사항 13종 → 테스트 대응표가 포함되고, 대응 테스트가 없는 항목이 0건이다
  </acceptance_criteria>
  <done>13개 요구사항이 각각 실행 가능한 테스트로 뒷받침된다는 것이 확인된다</done>
</task>

<task type="auto">
  <name>Task 2: openapi.yaml 최종 재생성 + docs 정합 재확인</name>
  <files>docs/api/openapi.yaml, docs/decisions.md, docs/glossary.md, docs/error-codes.md</files>
  <read_first>
    - docs/api/openapi.yaml (청크 B 재생성 이후 상태)
    - docs/glossary.md "시간표·예약 (Phase 4)" 표 (04-01이 추가한 것)
    - docs/decisions.md D-085~D-094 (04-01이 기록한 것)
    - docs/error-codes.md "## 시간표·예약 코드 (Phase 4)" 섹션
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (실제 구현된 코드 목록)
    - .planning/phases/04-schedule-reservation/ 의 04-01~04-14 SUMMARY (구현 중 바뀐 결정이 있는지 확인)
  </read_first>
  <action>
    (1) `docker compose up -d` → `./gradlew generateApiDocs`로 `docs/api/openapi.yaml`을 최종 재생성한다.
    `<interfaces>`에 나열한 12개 경로가 전부 포함됐는지 확인한다. 빠진 경로가 있으면 해당 컨트롤러의
    springdoc 애노테이션·매핑을 점검한다.
    (2) 구현 과정에서 04-01이 기록한 결정과 **다르게 구현된 것이 있으면** `docs/decisions.md`를 정정한다
    (새 번호 D-095부터 추가하거나 기존 항목을 수정하되, **무엇을 왜 바꿨는지** 본문에 남긴다).
    각 SUMMARY의 "결정 변경" 기록을 근거로 삼는다. 변경이 없으면 "변경 없음"을 SUMMARY에 적는다.
    (3) `docs/glossary.md`에 등재하지 않고 코드에 들어간 이름이 있는지 확인한다 — 실제 구현된
    enum·주요 클래스명(`ClassSessionStatus`·`ReservationStatus`·`NotificationType` 상수 6종 등)이
    glossary 표와 일치하는지 대조하고, 어긋나면 glossary를 실제 구현에 맞춘다.
    금지어(`Booking`/`Voucher`/`Ticket`/`Course`/`Session` 단독) 사용이 0건인지 코드 전체에서 확인한다.
    (4) `docs/error-codes.md`와 `ErrorCode.kt`의 1:1 대응은 `ErrorCodeRegistryTest`가 이미 강제하지만,
    **"발생 지점" 열이 실제 구현 클래스와 맞는지**는 테스트가 보지 않는다 — 수동으로 대조해 정정한다.
    (5) `./gradlew ktlintFormat` → `./gradlew build`로 마무리한다.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` 종료 코드 0
    - `docs/api/openapi.yaml`이 `<interfaces>`의 12개 경로 문자열을 모두 포함한다
    - `grep -rn "Booking\|Voucher\|class Ticket" src/main/kotlin/` 결과가 0건
    - `docs/glossary.md`의 `NotificationType` 항목이 실제 enum 상수 6종과 일치한다
    - SUMMARY에 "결정 변경 없음" 또는 변경 내역이 명시된다
  </acceptance_criteria>
  <done>FE가 쓸 계약과 스펙 문서가 실제 구현과 일치한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 회원 예약 흐름 · 관리자 운영 흐름 수동 확인</name>
  <files>(없음 — 확인 전용)</files>
  <read_first>
    - docs/api/openapi.yaml (Task 2에서 재생성된 최종본 — 호출할 경로·요청 본문 확인)
    - .planning/phases/04-schedule-reservation/04-CONTEXT.md `<domain>` (이 phase가 약속한 범위)
  </read_first>
  <what-built>
    회원 주간 시간표 조회, 예약 생성·취소·변경·본인 목록, 관리자 주간 스케줄 보드·예약 조회·대리 취소/변경·
    휴강 처리/해제, 그리고 이 모든 이벤트의 관리자 알림 레코드 생성. 자동 테스트는 각각을 개별 픽스처로
    검증했지만, **한 회원의 예약이 관리자 화면에 나타나고 관리자가 그것을 정리하는 연결된 흐름**은
    사람이 한 번 이어서 확인해야 한다.
  </what-built>
  <how-to-verify>
    로컬에서 `docker compose up -d` 후 애플리케이션을 띄우고, 아래를 순서대로 확인한다.
    HTTP 클라이언트(curl·Postman·IntelliJ .http 등)로 호출하며, 토큰은 기존 로그인 경로로 발급받는다.

    **A. 회원 흐름**
    1. `ACTIVE` 회원 토큰으로 `GET /api/members/me/schedule` — 이번 주 요일×타임 그리드가 오는가?
       저녁반 셀의 `reservable`이 false이고 `capacity`가 null인가? 예약 없는 예약제 셀이 `0/10`으로 보이는가?
    2. `GET /api/members/me/schedule?weekStart={다음 주 월요일}` — 조회는 되지만 모든 셀 `bookable`이 false인가?
    3. 그 회원에게 `SESSION_PASS`를 등록한 뒤(관리자 API) `POST /api/members/me/reservations`로
       이번 주 예약제 타임을 예약 — 201이 오고, `GET /api/members/me/passes`의 잔여가 **즉시 1 줄었는가**?
    4. 같은 타임을 한 번 더 예약 — 409 `DUPLICATE_RESERVATION`인가?
    5. `GET /api/members/me/reservations` — 방금 예약 1건만 보이는가?
    6. `POST /api/members/me/reservations/{id}/change`로 같은 주 다른 예약제 타임으로 변경 —
       200이 오고 `GET /api/members/me/pass-transactions`에 `CANCEL_REFUND`와 `RESERVE`가 **2건 모두** 남았는가?
    7. 오늘 날짜의 타임을 예약한 뒤 즉시 취소 시도 — 409 `SAME_DAY_MODIFICATION_NOT_ALLOWED`인가?

    **B. 관리자 흐름**
    8. 관리자 토큰으로 `GET /api/admin/schedule/board` — 위에서 만든 예약이 해당 셀 명단에 **회원 이름과 함께**
       보이는가? 회원 응답(1번)에는 이름이 없었는데 여기엔 있는가?
    9. `GET /api/admin/reservations?keyword={회원명}` — 그 예약이 검색되는가? 취소된 예약도 함께 나오는가?
    10. `POST /api/admin/reservations/{id}/cancellation` body `{"refund": false}` —
        예약이 취소되고 회원 잔여가 **그대로**인가? (`GET /api/admin/members/{id}/passes`로 확인)
    11. 예약이 남아 있는 이용권에 `POST /api/admin/passes/{passId}/cancellation` —
        409 `PASS_HAS_ACTIVE_RESERVATION`인가? 예약을 모두 정리한 뒤 다시 시도하면 성공하는가?
    12. 회원 예약 2~3건을 만든 뒤 `POST /api/admin/class-sessions/suspension`으로 그 타임 휴강 —
        예약이 전부 취소되고 잔여가 전부 복구됐는가? DB `notification` 테이블에
        `type = 'CLASS_SESSION_SUSPENDED'` 행이 **정확히 1건**인가? (`select * from notification order by id desc limit 10`)
    13. 휴강한 타임에 회원이 다시 예약 시도 — 409 `CLASS_SESSION_CANCELED`인가?
    14. `POST /api/admin/class-sessions/{id}/resumption` — 다시 예약이 되는가?
        취소됐던 예약이 **복원되지 않았는가**?

    **C. 데이터 확인 (psql)**
    15. `select reason, count(*) from pass_transaction group by reason;` —
        `RESERVE`/`CANCEL_REFUND`/`CLASS_CANCELED_REFUND`가 위 조작 횟수와 맞는가?
    16. `select admin_id, member_id, reason from pass_transaction order by id desc limit 10;` —
        회원 셀프 조작은 `member_id`, 관리자 대리·휴강은 `admin_id`가 채워졌는가? (둘 다 채워진 행이 없는가?)
  </how-to-verify>
  <action>
    위 16개 확인 항목을 사용자에게 그대로 제시하고 결과를 받는다. 이 태스크는 코드를 작성하지 않는다.
    문제가 보고되면 원인을 진단하고 수정 커밋을 같은 브랜치에 올린 뒤 재확인을 요청한다.
    확인 결과(각 항목 통과/실패와 발견된 이슈)를 SUMMARY에 기록한다.
  </action>
  <verify>
    <automated>test -n "$GSD_CHECKPOINT_ANSWER" || echo "사용자 확인 대기 — 자동 검증 대상 아님"</automated>
  </verify>
  <acceptance_criteria>
    - 사용자가 16개 항목의 확인 결과를 회신했다
    - 실패한 항목이 있으면 수정 커밋이 올라갔고 재확인이 통과했다
    - 확인 결과가 SUMMARY에 항목별로 기록됐다
  </acceptance_criteria>
  <done>회원 예약부터 관리자 정리까지 이어지는 경로가 실제 환경에서 동작함이 확인된다</done>
  <resume-signal>각 항목의 통과 여부를 알려주거나, 문제가 있으면 항목 번호와 증상을 적는다</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서(계약) → FE | `openapi.yaml`이 실제 코드와 어긋나면 FE가 잘못된 타입으로 통신한다 |
| 실제 운영 환경 → 전체 경로 | 자동 테스트가 각자 픽스처를 만들기 때문에 경로 간 연결 결함이 남을 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-72 | Tampering | `openapi.yaml`과 코드 불일치로 FE가 잘못된 계약 사용 | mitigate | `generateApiDocs` 재생성 + 12개 경로 존재 확인을 acceptance criteria로 검사 |
| T-04-73 | Information Disclosure | 회원 응답에 예약자 명단이 새는 회귀 | mitigate | 수동 확인 항목 1·8이 회원 응답과 관리자 응답의 차이를 직접 비교한다 |
| T-04-74 | Repudiation | 이력 주체가 잘못 기록되는 회귀 | mitigate | 수동 확인 항목 16이 `admin_id`/`member_id` 배타성과 행위자 기준을 DB에서 직접 확인 |
| T-04-75 | Repudiation | 문서에 기록되지 않은 설계 변경 | mitigate | Task 2에서 SUMMARY의 결정 변경 기록을 근거로 `decisions.md`를 정정하거나 "변경 없음"을 명시 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 (04-RESEARCH.md Package Legitimacy Audit — 해당 없음) |
</threat_model>

<verification>
- `./gradlew clean build` 그린
- 동시성 테스트 2종 2회 연속 통과
- `openapi.yaml`에 12개 경로 존재
- 사람 확인 16항목 완료
- 이후 `/gsd-verify-work`·`/gsd-audit-uat`로 phase 목표 달성을 별도 확인한다
</verification>

<success_criteria>
- 요구사항 13종이 모두 테스트로 뒷받침되고 대응표가 문서화됨
- API 계약·용어·에러코드·설계 결정이 실제 구현과 일치함
- ROADMAP Phase 4가 완료로 갱신되고 플랜 15개가 목록화됨
- 회원·관리자 전체 흐름이 실제 환경에서 사람에 의해 확인됨
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-15-SUMMARY.md` when done.
**청크 C 마지막 플랜** — `deliver-phase-chunk` 절차에 따라 `feature/phase-04c-admin-ops` → dev PR을 생성하고
PR URL을 보고한다. 머지는 사용자가 한다. **dev → main PR은 마일스톤/배포 시점에만 낸다** (D-084).
</output>
</content>
