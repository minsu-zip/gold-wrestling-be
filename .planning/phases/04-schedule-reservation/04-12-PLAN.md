---
phase: 04-schedule-reservation
plan: 12
type: execute
wave: 11
depends_on: ["04-10"]
files_modified:
  - src/main/kotlin/com/goldwrestling/reservation/AdminReservationService.kt
  - src/main/kotlin/com/goldwrestling/reservation/AdminReservationController.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/ReservationSearchCondition.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/AdminReservationResponse.kt
  - src/test/kotlin/com/goldwrestling/reservation/AdminReservationSearchTest.kt
autonomous: true
requirements: [RESV-07]

must_haves:
  truths:
    - "관리자가 기간·수업 종류·회원 검색어로 모든 회원의 예약을 필터링해 조회할 수 있다"
    - "예약 목록이 page/size 페이지네이션으로 내려온다"
    - "취소된 예약도 조회되고 취소 여부·복구 여부·취소 주체가 구분된다"
    - "회원 토큰으로는 이 API에 접근할 수 없다"
    - "검색어에 LIKE 와일드카드(%, _)를 넣어도 전체 조회로 확장되지 않는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/reservation/AdminReservationController.kt"
      provides: "GET /api/admin/reservations"
      contains: "@ParameterObject"
    - path: "src/main/kotlin/com/goldwrestling/reservation/dto/ReservationSearchCondition.kt"
      provides: "기간·종류·검색어·페이지 조건 (D-054)"
      contains: "class ReservationSearchCondition"
    - path: "src/main/kotlin/com/goldwrestling/reservation/dto/AdminReservationResponse.kt"
      provides: "취소 메타데이터 포함 관리자용 응답"
      contains: "refunded"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/reservation/AdminReservationService.kt"
      to: "src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt"
      via: "Specification.allOf(listOfNotNull(...)) 조합"
      pattern: "Specification.allOf"
---

<objective>
관리자 **전체 예약 조회 API**(RESV-07)를 만든다 — 기간·수업 종류·회원 검색어 필터 + 페이지네이션.

Purpose: 관리자가 "이 회원이 이번 달에 뭘 예약했나", "지난주 1:1 레슨은 몇 건이었나"를 확인하는 경로이고,
대리 취소·변경(04-13)의 대상 예약을 찾는 진입점이다. Phase 2·3이 확립한 목록 API 관례
(`@ParameterObject` + `Specification` + `PageResponse`)를 그대로 따라 FE 계약이 일관되게 유지되도록 한다 —
D-054가 이미 "개별 `@RequestParam`으로 펼치는 방식"의 실패를 근거로 이 형태를 확정했다.

Output: `AdminReservationService.search`, `AdminReservationController`, 검색 조건·응답 DTO, 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@docs/requirements.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`reservation/ReservationSpecifications.kt`(04-10) — 이 플랜이 재사용한다 (수정하지 않는다):
- `fun ownedByMember(memberId: Long): Specification<Reservation>` (non-null)
- `fun hasStatus(status: ReservationStatus): Specification<Reservation>`
- `fun classDateBetween(from: LocalDate?, to: LocalDate?): Specification<Reservation>?`
- `fun hasClassType(classType: ClassType?): Specification<Reservation>?`
- `fun memberKeywordContains(keyword: String?): Specification<Reservation>?`
`reservation/ReservationRepository.kt`(04-03): `JpaSpecificationExecutor<Reservation>` 구현
`member/dto/PageResponse.kt`: `PageResponse.from(page, mapper)`
`member/AdminMemberController.kt` — 이 API의 형태 원본:
- `@GetMapping fun search(@ParameterObject @ModelAttribute @Valid condition: MemberSearchCondition): PageResponse<...>`
`member/AdminMemberService.kt` — Specification 조합 원본:
- `Specification.allOf<Member>(listOfNotNull(spec1, spec2, spec3))` + `PageRequest.of(condition.page, condition.size, Sort...)`
`pass/dto/PassTransactionSearchCondition.kt`: `page`/`size` 기본값·검증 관례 (D-035)
`member/MemberSpecifications.kt`: `escapeLikeWildcards` — LIKE 이스케이프 원본

`SecurityConfig`는 수정하지 않는다 — `/api/admin/**`가 이미 `hasRole("ADMIN")`이다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminReservationService.search + 검색 조건·응답 DTO</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/AdminReservationService.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/ReservationSearchCondition.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/AdminReservationResponse.kt,
    src/test/kotlin/com/goldwrestling/reservation/AdminReservationSearchTest.kt
  </files>
  <behavior>
    - 필터 없음 → 전체 예약이 `reservedAt` 내림차순으로 페이지 단위 반환
    - `from`/`to` 지정 → `classDate`가 그 범위(양 끝 포함) 안인 예약만
    - `classType = LESSON` → 1:1 예약만
    - `keyword = "홍길"` → 회원명에 부분 일치하는 예약만
    - `keyword = "%"` → 전체 조회로 확장되지 않는다 (LIKE 와일드카드 이스케이프)
    - `keyword` 앞뒤 공백 → 정규화 후 매칭된다
    - `status` 필터 없음이 기본 — 취소된 예약도 함께 반환된다
    - 응답에 `status`·`refunded`·`canceledAt`·취소 주체 구분(`canceledBy` 표시 문자열)이 포함된다
    - `page = 1, size = 2` → 두 번째 페이지 2건, `totalElements`가 전체 건수
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (전체 — `search` 메서드의 Specification 조합·정렬·PageResponse 변환)
    - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt (전체 — LIKE 이스케이프·키워드 정규화)
    - src/main/kotlin/com/goldwrestling/pass/dto/PassTransactionSearchCondition.kt (page/size 기본값·`@Min`/`@Max` 검증 관례)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt (04-10 산출물)
    - src/main/kotlin/com/goldwrestling/reservation/Reservation.kt (04-03 — 취소 메타데이터 필드)
    - src/test/kotlin/com/goldwrestling/member/AdminMemberControllerTest.kt 또는 동등한 검색 테스트 (필터 조합 테스트 골격)
  </read_first>
  <action>
    (1) `ReservationSearchCondition` — `@ParameterObject`로 노출될 DTO.
    필드: `from: LocalDate?`, `to: LocalDate?`, `classType: ClassType?`, `keyword: String?`,
    `status: ReservationStatus?`, `page: Int = 0`, `size: Int = 20`.
    `page`/`size` 검증(`@Min(0)`, `@Min(1) @Max(100)`)은 `PassTransactionSearchCondition` 관례를 그대로 따른다.
    `from > to`면 `@AssertTrue` 또는 서비스 판정으로 400을 낸다 (기존 Phase 3 관례를 읽고 같은 방식을 쓴다).

    (2) `AdminReservationResponse` — 관리자용이라 회원 정보를 포함한다:
    `id, memberId, memberName, classSessionId, classType, classDate, startTime, endTime, status,
     reservedAt, canceledAt, refunded, canceledByType`(`MEMBER`/`ADMIN`/null), `canceledByName`, `passId`.
    **전화번호는 넣지 않는다** — 예약 목록의 목적이 아니고, 필요하면 회원 상세 API로 간다.

    (3) `AdminReservationService.kt` — `@Service @Transactional(readOnly = true)`.
    `fun search(condition: ReservationSearchCondition): PageResponse<AdminReservationResponse>`
    · `Specification.allOf<Reservation>(listOfNotNull(classDateBetween(from, to), hasClassType(classType),
      memberKeywordContains(keyword), condition.status?.let { hasStatus(it) }))` — `AdminMemberService.search`와 같은 형태
    · `PageRequest.of(condition.page, condition.size, Sort.by(Sort.Direction.DESC, "reservedAt"))`
    · `PageResponse.from(page) { toResponse(it) }` — 새 페이지네이션 DTO를 만들지 않는다
    · 응답 매핑에서 `member`·`canceledByMember`·`canceledByAdmin` LAZY 접근이 발생하므로
      **`@EntityGraph` 또는 fetch join으로 함께 가져온다** — 안 하면 페이지 크기만큼 N+1이 난다.
      `JpaSpecificationExecutor`와 `@EntityGraph`를 함께 쓰는 방법이 확실하지 않으면
      `verify-boot4-api` 스킬 절차(context7 → 컴파일 검증)로 확인한다. 추측해서 쓰지 않는다

    (4) `AdminReservationSearchTest` — `<behavior>` 9개 항목을 각각 `@Test`로 검증한다.
    `keyword = "%"` 케이스를 **반드시 포함**한다 — 이스케이프가 빠지면 검색이 전체 조회로 바뀐다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.AdminReservationSearchTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.AdminReservationSearchTest"` 종료 코드 0, 9개 이상 테스트 실행
    - 테스트가 `keyword = "%"` 입력 시 결과가 전체 건수보다 적음(또는 0건)을 단언한다
    - `AdminReservationService.kt`가 `Specification.allOf` + `listOfNotNull` 형태를 쓴다
    - `AdminReservationService.kt`가 `member`를 fetch join 또는 `@EntityGraph`로 함께 조회한다
    - `AdminReservationResponse.kt`에 `phoneNumber` 필드가 없다
    - 새 페이지네이션 응답 DTO를 만들지 않고 `member/dto/PageResponse`를 재사용한다
  </acceptance_criteria>
  <done>관리자가 조건을 조합해 예약을 찾을 수 있고, 목록 API 형태가 Phase 2·3과 동일하게 유지된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/admin/reservations 엔드포인트</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/AdminReservationController.kt,
    src/test/kotlin/com/goldwrestling/reservation/AdminReservationSearchTest.kt
  </files>
  <behavior>
    - `GET /api/admin/reservations` → 200 + `PageResponse<AdminReservationResponse>`
    - `GET /api/admin/reservations?from=...&to=...&classType=LESSON&keyword=홍길&page=0&size=10` → 200, 필터 적용
    - `size = 500` → 400 ProblemDetail (상한 검증)
    - `from`이 `to`보다 뒤 → 400 ProblemDetail
    - 회원 토큰 → 403, 인증 없음 → 401
    - openapi 문서에 쿼리 파라미터가 개별 필드로 노출된다 (`@ParameterObject`, D-054)
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt (전체 — `@ParameterObject @ModelAttribute @Valid` 시그니처와 springdoc 애노테이션)
    - src/main/kotlin/com/goldwrestling/reservation/AdminReservationService.kt (Task 1 산출물)
    - src/test/kotlin/com/goldwrestling/pass/AdminPassControllerTest.kt (관리자 principal 주입 방식)
    - .planning/phases/02-auth-member/02-15-PLAN.md 또는 그 SUMMARY (D-054가 왜 개별 @RequestParam을 기각했는지)
  </read_first>
  <action>
    `AdminReservationController.kt` — `@RestController @RequestMapping("/api/admin/reservations")`.
    · `@GetMapping fun search(@ParameterObject @ModelAttribute @Valid condition: ReservationSearchCondition,
       @AuthenticationPrincipal principal: AuthenticatedPrincipal): PageResponse<AdminReservationResponse>`
    · **개별 `@RequestParam`으로 펼치지 않는다** (D-054). `@ParameterObject`가 있어야 openapi.yaml에
      쿼리 파라미터가 개별 필드로 문서화되고 FE 타입 생성이 정상 동작한다
    · 컨트롤러에 `@Transactional`을 붙이지 않는다 (D-020)
    · springdoc `@Operation`/`@ApiResponse`는 `AdminMemberController` 형식을 따른다

    `AdminReservationSearchTest`에 `<behavior>` 6개 항목을 컨트롤러 레벨 `@Test`로 추가한다
    (MockMvc 경로 — 서비스 레벨 테스트와 같은 파일에 두되 `@Nested`로 구분한다).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.AdminReservationSearchTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.AdminReservationSearchTest"` 종료 코드 0, 15개 이상 테스트 실행(Task 1의 9종 + 이번 6종)
    - `AdminReservationController.kt`가 `@ParameterObject`를 사용하고 `@RequestParam`이 0건이다
    - 테스트가 "회원 토큰 → 403", "인증 없음 → 401", "size 상한 초과 → 400"을 각각 단언한다
    - `SecurityConfig.kt`가 이 플랜에서 수정되지 않았다
  </acceptance_criteria>
  <done>관리자 예약 조회가 기존 목록 API와 같은 계약 형태로 열린다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 요청 → 전체 회원 예약 데이터 | 모든 회원의 예약과 이름이 나가는 경로 |
| 검색어 파라미터 → JPA Criteria LIKE | 사용자 입력이 쿼리 조건으로 들어가는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-55 | Information Disclosure | 회원 토큰으로 전체 예약 조회 | mitigate | `/api/admin/**` → `hasRole("ADMIN")`. 테스트가 403을 단언 |
| T-04-56 | Tampering | LIKE 와일드카드(`%`, `_`)로 필터 우회 | mitigate | `MemberSpecifications.escapeLikeWildcards` 재사용(재구현 금지). `keyword = "%"` 테스트로 고정 |
| T-04-57 | Denial of Service | `size` 상한 없는 대량 조회 | mitigate | `@Max(100)` 검증 + 400 응답 테스트 |
| T-04-58 | Denial of Service | 응답 매핑 시 N+1 (페이지 크기만큼 추가 쿼리) | mitigate | `member`·취소 주체를 fetch join / `@EntityGraph`로 함께 조회 |
| T-04-59 | Information Disclosure | 예약 목록에 전화번호까지 포함 | mitigate | `AdminReservationResponse`에 `phoneNumber` 미포함. acceptance criteria로 검사 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `openapi.yaml` 재생성은 청크 C 마지막 플랜(04-15)에서 한 번에 한다
</verification>

<success_criteria>
- 관리자가 기간·종류·회원 검색어로 예약을 조회할 수 있고 페이지네이션이 기존 API와 동일하게 동작한다
- 검색어 이스케이프·페이지 상한·역할 인가가 테스트로 고정된다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-12-SUMMARY.md` when done.
청크 C(`feature/phase-04c-admin-ops`) 진행 중 — 04-11과 같은 wave 11이라 병렬 실행 가능하다.
PR은 wave 14(04-15) 완료 후에 낸다 (D-084).
</output>
</content>
