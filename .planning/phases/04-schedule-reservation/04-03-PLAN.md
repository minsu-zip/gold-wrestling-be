---
phase: 04-schedule-reservation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/main/kotlin/com/goldwrestling/schedule/ClassType.kt
  - src/main/kotlin/com/goldwrestling/schedule/ClassSessionStatus.kt
  - src/main/kotlin/com/goldwrestling/schedule/ClassSchedule.kt
  - src/main/kotlin/com/goldwrestling/schedule/ClassScheduleRepository.kt
  - src/main/kotlin/com/goldwrestling/schedule/ClassSession.kt
  - src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt
  - src/main/kotlin/com/goldwrestling/reservation/ReservationStatus.kt
  - src/main/kotlin/com/goldwrestling/reservation/Reservation.kt
  - src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt
  - src/main/kotlin/com/goldwrestling/notification/NotificationType.kt
  - src/main/kotlin/com/goldwrestling/notification/Notification.kt
  - src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt
  - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt
  - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt
  - src/test/kotlin/com/goldwrestling/schedule/ClassSessionRepositoryTest.kt
  - src/test/kotlin/com/goldwrestling/reservation/ReservationRepositoryTest.kt
autonomous: true
requirements: [SCHED-01, SCHED-02, RESV-01, RESV-02, RESV-06, NOTIF-01]

must_haves:
  truths:
    - "같은 (시간표, 날짜) 조합에 ClassSession을 두 번 만들려 해도 행은 정확히 1개만 생긴다"
    - "정원이 찬 ClassSession에 예약 인원 증가를 요청하면 0행이 갱신되고 값이 변하지 않는다"
    - "ACTIVE가 아닌 예약에 취소 조건부 UPDATE를 걸면 0행이 갱신된다"
    - "PassTransaction이 관리자 주체와 회원 주체 두 형태로 저장된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt"
      provides: "get-or-create 네이티브 upsert + 정원 조건부 UPDATE + 휴강 상태 전환"
      contains: "on conflict (class_schedule_id, class_date) do nothing"
    - path: "src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt"
      provides: "예약 취소 compare-and-swap + 소유·세션·이용권 조회"
      contains: "fun cancelByMemberIfActive"
    - path: "src/main/kotlin/com/goldwrestling/reservation/Reservation.kt"
      provides: "예약 엔티티(비정규화 컬럼 포함) + 취소 주체 nullable 쌍"
      contains: "class Reservation"
    - path: "src/main/kotlin/com/goldwrestling/notification/Notification.kt"
      provides: "append-only 관리자 알림 엔티티"
      contains: "class Notification"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt"
      to: "class_session 테이블 uq_class_session 제약"
      via: "네이티브 INSERT ... ON CONFLICT DO NOTHING"
      pattern: "nativeQuery = true"
    - from: "src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt"
      to: "ck_pass_transaction_subject CHECK"
      via: "admin/member nullable 쌍 매핑"
      pattern: "val member: Member\\?"
---

<objective>
V6~V8 스키마를 JPA 엔티티·리포지토리로 매핑하고, 이 phase의 **동시성 방어 도구 3종**
(네이티브 get-or-create, 정원 조건부 UPDATE, 예약 취소 compare-and-swap)을 리포지토리 계층에 만든다.

Purpose: 서비스 코드(04-07 이후)는 이 메서드들을 조립만 하면 되도록, "원자성을 보장하는 최소 단위"를
먼저 리포지토리에 확정한다. D-072의 역할 분리(도메인 메서드는 판정만, 반영은 조건부 UPDATE)를
Phase 3과 동일하게 유지한다. 또 `PassTransaction`에 회원 주체를 매핑해 Phase 3 코드가 새 스키마에서 계속 동작하게 한다.

Output: schedule/reservation/notification 3개 패키지의 enum·엔티티·리포지토리,
`PassTransaction` 주체 확장 반영, 리포지토리 통합테스트 2개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
@docs/conventions.md
@docs/glossary.md
@src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql
</context>

<interfaces>
<!-- 실행자가 그대로 이식할 기존 계약. 코드베이스 탐색 없이 이 값을 쓴다. -->

`pass/PassRepository.kt` — 조건부 UPDATE 관례의 원본 (이 파일은 이번 플랜에서 수정하지 않는다):
- `@Modifying(flushAutomatically = true, clearAutomatically = true)`
- `@Query("update Pass p set p.remainingCount = p.remainingCount + :amount where p.id = :id and p.status = com.goldwrestling.pass.PassStatus.ACTIVE and p.remainingCount + :amount >= 0")`
- `fun adjustRemainingCount(@Param("id") id: Long, @Param("amount") amount: BigDecimal): Int`
- `fun cancelIfNotCanceled(id: Long, reason: String, canceledBy: Admin, canceledAt: OffsetDateTime): Int`

`pass/PassTransaction.kt` 현재 상태 (수정 대상):
- `class PassTransaction(val pass: Pass, val amount: BigDecimal, val reason: TransactionReason, val note: String?, val admin: Admin, val occurredAt: OffsetDateTime)` — 전 필드 `val`(append-only)
- `admin`이 non-null이며 `@JoinColumn(name = "admin_id", nullable = false)`

`pass/TransactionReason.kt`: `RESERVE, CANCEL_REFUND, ADMIN_ADJUST, EVENING_HALF, INACTIVITY, CLASS_CANCELED_REFUND, INITIAL_GRANT, REGISTRATION_CANCELED`

`pass/AdminPassService.kt` — `PassTransaction(...)` 생성자 호출 4곳이 있다(register/adjust/changePeriod/cancel 경로). `admin`이 nullable이 되면 **명명 인자 추가만** 필요하다.

conventions §3 엔티티 규약: `data class` 금지 / `@ManyToOne`은 `fetch = FetchType.LAZY` 명시 /
`@Enumerated(EnumType.STRING)` 필수 / `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schedule 패키지 — enum·엔티티·리포지토리(get-or-create + 정원 조건부 UPDATE)</name>
  <files>
    src/main/kotlin/com/goldwrestling/schedule/ClassType.kt,
    src/main/kotlin/com/goldwrestling/schedule/ClassSessionStatus.kt,
    src/main/kotlin/com/goldwrestling/schedule/ClassSchedule.kt,
    src/main/kotlin/com/goldwrestling/schedule/ClassScheduleRepository.kt,
    src/main/kotlin/com/goldwrestling/schedule/ClassSession.kt,
    src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt,
    src/test/kotlin/com/goldwrestling/schedule/ClassSessionRepositoryTest.kt
  </files>
  <behavior>
    - `insertIfAbsent`를 같은 (scheduleId, classDate)로 두 번 호출하면 두 번째는 0을 반환하고 `class_session` 행 수는 1이다
    - `incrementReservedCountIfCapacityAvailable`는 `reserved_count < capacity`일 때 1을, 정원이 찼을 때 0을 반환하고 값은 변하지 않는다
    - `incrementReservedCountIfCapacityAvailable`는 `status = CANCELED`(휴강) 세션에 대해 0을 반환한다
    - `decrementReservedCount`는 `reserved_count = 0`인 세션에 대해 0을 반환한다 (음수로 내려가지 않는다)
    - `suspendIfScheduled`는 이미 CANCELED인 세션에 대해 0을 반환한다
    - `resumeIfCanceled`는 SCHEDULED 세션에 대해 0을 반환한다
  </behavior>
  <read_first>
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (전체 — 컬럼명·타입이 엔티티와 정확히 맞아야 ddl-auto=validate가 통과한다)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt (전체 — 엔티티 골격, "판정만 하고 대입하지 않는다" KDoc 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt (전체 — 조건부 UPDATE 형식과 KDoc 내용)
    - src/main/kotlin/com/goldwrestling/pass/PassStatus.kt (enum 선언 형식)
    - src/main/kotlin/com/goldwrestling/branch/Branch.kt (ManyToOne 대상 엔티티)
    - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt (전체 — 리포지토리 통합테스트 골격)
    - .planning/phases/04-schedule-reservation/04-PATTERNS.md "schedule/ClassSchedule.kt"·"schedule/ClassSessionRepository.kt" 절
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "Pattern 1"·"Pattern 2"·"Pitfall 1"
  </read_first>
  <action>
    (1) `ClassType`: `EVENING`, `SESSION`, `LESSON`. 각 상수에 `val reservable: Boolean` 생성자 프로퍼티를 둔다
    (`EVENING`=false, 나머지 true) — D-092의 "예약 대상이 아님을 응답에 표현"과 `CLASS_SESSION_NOT_RESERVABLE` 판정이 이 값을 쓴다.
    `ClassType`→`PassType` 매핑은 여기 두지 않는다 — `schedule` 패키지가 `pass`를 참조하지 않도록,
    그 매핑은 04-06에서 `reservation` 패키지에 만든다.
    (2) `ClassSessionStatus`: `SCHEDULED`, `CANCELED`. KDoc에 "CANCELED = 휴강(policies §7)"임을 명시한다.
    (3) `ClassSchedule` 엔티티: `branch`(LAZY), `dayOfWeek: java.time.DayOfWeek`(`@Enumerated(EnumType.STRING)`,
    새 enum을 만들지 않는다), `classType`, `startTime`/`endTime: LocalTime`, `capacity: Int?`, `createdAt: OffsetDateTime`.
    companion factory를 만들지 않는다 — 이 엔티티는 Flyway 시드로만 채워진다(D-089). 그 사실을 클래스 KDoc에 남긴다.
    (4) `ClassScheduleRepository`: `JpaRepository<ClassSchedule, Long>` +
    `fun findAllByBranchId(branchId: Long): List<ClassSchedule>`,
    `fun findAllByBranchIdAndDayOfWeekIn(branchId: Long, days: Collection<DayOfWeek>): List<ClassSchedule>`.
    (5) `ClassSession` 엔티티: `classSchedule`(LAZY), `classDate: LocalDate`, `classType`, `startTime`/`endTime`,
    `capacity: Int?`, `reservedCount: Int`, `status: ClassSessionStatus`, `canceledAt`/`cancelReason`/`canceledByAdmin`,
    `createdAt`. **판정만 하는 도메인 메서드**를 둔다 — `fun assertReservable()`(휴강이면 `ClassSessionCanceledException`,
    `classType.reservable`이 false면 `ClassSessionNotReservableException`), `fun assertSuspendable()`,
    `fun assertResumable()`. Pass.kt KDoc의 "여기에 대입문을 추가하지 않는다" 경고 문구를 같은 형태로 남긴다.
    (6) `ClassSessionRepository`: `JpaRepository<ClassSession, Long>` +
    · `fun findByClassScheduleIdAndClassDate(scheduleId: Long, classDate: LocalDate): ClassSession?`
    · `fun findAllByClassDateBetween(from: LocalDate, to: LocalDate): List<ClassSession>`
    · `insertIfAbsent(...): Int` — 04-RESEARCH.md Pattern 1의 네이티브 SQL을 그대로 채택
      (`insert into class_session (...) values (...) on conflict (class_schedule_id, class_date) do nothing`,
      `nativeQuery = true`, `@Modifying(flushAutomatically = true, clearAutomatically = true)`).
      **이 저장소 최초의 `nativeQuery` 사용**이므로 KDoc에 이유를 남긴다: `save()` 후
      `DataIntegrityViolationException`을 catch하는 방식은 Hibernate가 flush 시점 제약 위반 이후
      영속성 컨텍스트를 신뢰할 수 없는 상태로 만들 수 있어(Pitfall 1) 예외가 애초에 나지 않는 upsert를 쓴다.
      반환 0은 실패가 아니라 "이미 존재한다"는 상태 정보이므로 호출부는 반환값을 보지 않고 항상 재조회한다.
    · `incrementReservedCountIfCapacityAvailable(id: Long): Int` — JPQL 조건부 UPDATE,
      `where s.id = :id and s.status = ...SCHEDULED and s.capacity is not null and s.reservedCount + 1 <= s.capacity`
    · `decrementReservedCount(id: Long): Int` — `where s.id = :id and s.reservedCount > 0`
    · `suspendIfScheduled(id: Long, reason: String, canceledBy: Admin, canceledAt: OffsetDateTime): Int` —
      `where s.id = :id and s.status = ...SCHEDULED`
    · `resumeIfCanceled(id: Long): Int` — status를 SCHEDULED로 되돌리고 취소 메타 3개를 null로 되돌린다
      (`ck_class_session_cancellation`이 완전성을 강제하므로 반드시 4개를 함께 갱신한다),
      `where s.id = :id and s.status = ...CANCELED`
    조건부 UPDATE 메서드마다 `PassRepository.adjustRemainingCount`와 **동일 형식의 KDoc**을 반복해서 쓴다 —
    반환 0의 의미, `flushAutomatically`/`clearAutomatically`가 왜 필요한지, 호출 전에 필요한 스칼라 값을
    지역 변수로 미리 꺼내둬야 하는 이유(직후 엔티티가 준영속이 되어 LAZY 접근 시 예외).
    (7) `ClassSessionRepositoryTest`: `<behavior>` 6개 항목을 각각 `@Test`로 검증한다.
    골격은 `PassRepositoryTest`를 그대로 따른다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.schedule.ClassSessionRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.schedule.ClassSessionRepositoryTest"` 종료 코드 0, 6개 이상 테스트 실행
    - `ClassSessionRepository.kt`가 `nativeQuery = true`와 `on conflict (class_schedule_id, class_date) do nothing`을 포함한다
    - `ClassSessionRepository.kt`의 조건부 UPDATE 메서드 4개가 각각 `@Modifying(flushAutomatically = true, clearAutomatically = true)`를 갖는다
    - `ClassSession.kt`에 `status =`·`reservedCount =` 같은 필드 대입문이 없다 (판정 전용 — `grep -c "    status =\|    reservedCount =" ClassSession.kt` == 0)
    - `ClassType.kt`가 `EVENING`, `SESSION`, `LESSON` 3개 상수와 `reservable` 프로퍼티를 갖는다
    - `./gradlew compileKotlin` 종료 코드 0
  </acceptance_criteria>
  <done>세션 생성 경쟁·정원 초과·휴강 중복 처리가 전부 리포지토리 한 줄 호출로 원자적으로 결정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reservation 패키지 — 엔티티·리포지토리(취소 compare-and-swap) + 부분 유니크 인덱스 검증</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/ReservationStatus.kt,
    src/main/kotlin/com/goldwrestling/reservation/Reservation.kt,
    src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt,
    src/test/kotlin/com/goldwrestling/reservation/ReservationRepositoryTest.kt
  </files>
  <behavior>
    - 같은 (classSession, member)로 ACTIVE 예약을 두 건 INSERT하면 두 번째가 `DataIntegrityViolationException`으로 실패한다
    - 첫 예약을 CANCELED로 전환한 뒤 같은 (classSession, member)로 다시 INSERT하면 성공한다 (재예약 허용, D-086)
    - `class_type = LESSON` 세션에 서로 다른 회원 2명이 ACTIVE 예약을 INSERT하면 두 번째가 실패한다
    - 같은 회원이 같은 `classDate`·`startTime`의 서로 다른 세션에 ACTIVE 예약 2건을 INSERT하면 두 번째가 실패한다 (D-088)
    - `cancelByMemberIfActive`는 ACTIVE 예약에 1, 이미 CANCELED인 예약에 0을 반환한다
    - `cancelByAdminIfActive(refunded = false)`로 취소한 예약은 `refunded = false`로 저장된다
    - `existsByPassIdAndStatus(passId, ACTIVE)`가 활성 예약 유무를 정확히 반환한다 (D-085 선행 검사용)
  </behavior>
  <read_first>
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (reservation 테이블 + 부분 유니크 인덱스 3종)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt (`cancelIfNotCanceled` — 이식 대상 형식)
    - src/main/kotlin/com/goldwrestling/auth/RefreshToken.kt (member/admin nullable 쌍 매핑 + 주체 판별 메서드 관례)
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt (판정 전용 도메인 메서드 KDoc 관례)
    - src/test/kotlin/com/goldwrestling/pass/PassRepositoryTest.kt (통합테스트 골격)
    - .planning/phases/04-schedule-reservation/04-PATTERNS.md "reservation/Reservation.kt"·"reservation/ReservationRepository.kt" 절
  </read_first>
  <action>
    (1) `ReservationStatus`: `ACTIVE`, `CANCELED`.
    (2) `Reservation` 엔티티: `member`(LAZY), `classSession`(LAZY), `pass`(LAZY, 복구 대상 추적 — D-087),
    비정규화 컬럼 `classType`/`classDate`/`startTime`, `status`, `reservedAt: OffsetDateTime`,
    `canceledAt: OffsetDateTime?`, `canceledByMember: Member?`(LAZY), `canceledByAdmin: Admin?`(LAZY),
    `refunded: Boolean?`, `createdAt`.
    비정규화 컬럼에 KDoc을 단다 — 이 값들은 세션에서 복사한 사본이며, 부분 유니크 인덱스의 `WHERE`/키가
    **같은 테이블 안에서** 나와야 성립하기 때문에 존재한다(조인 기반 방어는 Postgres에서 불가능).
    (3) **취소·변경 판정 메서드는 이 플랜에서 만들지 않는다** — 04-09가 TDD로 추가한다(테스트가 먼저).
    다만 클래스 KDoc에 자리를 예고한다: "취소·변경 가능 여부 판정은 04-09에서 `assertCancelableByMember` 등으로
    이 클래스에 추가된다. 판정 메서드에는 필드 대입문을 넣지 않는다(D-072) — 실제 상태 전환은
    `ReservationRepository`의 조건부 UPDATE가 담당한다."
    (4) `ReservationRepository`: `JpaRepository<Reservation, Long>`, `JpaSpecificationExecutor<Reservation>` +
    · `fun existsByPassIdAndStatus(passId: Long, status: ReservationStatus): Boolean`
    · `fun findAllByClassSessionIdAndStatus(classSessionId: Long, status: ReservationStatus): List<Reservation>`
    · `fun findAllByClassSessionIdInAndStatus(classSessionIds: Collection<Long>, status: ReservationStatus): List<Reservation>`
      — 주간 보드가 셀별 명단을 N+1 없이 한 번에 가져오는 경로
    · `fun findByIdAndMemberId(id: Long, memberId: Long): Reservation?` — IDOR 방어(없으면 404)
    · `cancelByMemberIfActive(id: Long, canceledBy: Member, canceledAt: OffsetDateTime): Int` —
      status를 CANCELED로, `refunded = true`, `canceledByMember` 세팅, `where ... and r.status = ACTIVE`
    · `cancelByAdminIfActive(id: Long, canceledBy: Admin, refunded: Boolean, canceledAt: OffsetDateTime): Int`
    두 메서드 KDoc에 **Pitfall 2를 명시**한다: 이 UPDATE의 반환 0은 "경쟁에서 졌다"는 뜻이지만,
    뒤따르는 `PassRepository.adjustRemainingCount`의 반환 0은 "등록 취소된 이용권이라 복구하지 않는다"는
    **정상 경로**다 — 두 0을 같은 의미로 다루지 않는다.
    (5) `ReservationRepositoryTest`: `<behavior>` 7개 항목을 각각 `@Test`로 검증한다. 인덱스 위반 3건은
    `assertThatThrownBy { ... }.isInstanceOf(DataIntegrityViolationException::class.java)` 형태로 확인하되,
    `@Transactional` 롤백에 기대지 않고 각 케이스를 독립 트랜잭션에서 실행한다(제약 위반 이후 같은
    트랜잭션을 계속 쓰지 않는다).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.ReservationRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.ReservationRepositoryTest"` 종료 코드 0, 7개 이상 테스트 실행
    - 테스트가 부분 유니크 인덱스 3종(세션+회원 / LESSON 슬롯 / 회원+날짜+시각)의 위반을 각각 단언한다
    - 테스트가 "취소 후 같은 타임 재예약 성공"을 단언한다
    - `Reservation.kt`가 `canceledByMember: Member?`와 `canceledByAdmin: Admin?`를 모두 갖는다
    - `Reservation.kt`에 `assertCancelable`/`assertChangeable` 메서드가 아직 없다 (04-09가 TDD로 추가한다)
    - `ReservationRepository.kt`가 `fun cancelByMemberIfActive`와 `fun cancelByAdminIfActive`를 모두 갖고, 각각 `@Modifying(flushAutomatically = true, clearAutomatically = true)`를 갖는다
  </acceptance_criteria>
  <done>중복·초과 예약이 DB에서 실제로 거부되는 것이 테스트로 증명되고, 취소는 경쟁 안전한 한 번의 UPDATE로 끝난다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: notification 패키지 + PassTransaction 회원 주체 매핑</name>
  <files>
    src/main/kotlin/com/goldwrestling/notification/NotificationType.kt,
    src/main/kotlin/com/goldwrestling/notification/Notification.kt,
    src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt,
    src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt,
    src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt
  </files>
  <behavior>
    - 기존 Phase 3 테스트 전체가 그대로 통과한다 (`admin` nullable 완화가 기존 호출부를 깨지 않는다)
    - `PassTransaction`을 `member` 주체로 저장할 수 있다 (`admin = null, member = <회원>`)
    - 주체를 둘 다 넣거나 둘 다 비우면 `ck_pass_transaction_subject`로 저장이 실패한다
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (전체)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (전체 — `PassTransaction(...)` 생성자 호출 지점 전부)
    - src/main/kotlin/com/goldwrestling/auth/RefreshToken.kt (nullable 주체 쌍 + `principalType()` 판별 메서드)
    - src/main/resources/db/migration/V8__extend_pass_transaction_subject.sql
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (notification 테이블)
    - .planning/phases/04-schedule-reservation/04-PATTERNS.md "notification/Notification.kt"·"pass/PassTransaction.kt" 절
  </read_first>
  <action>
    (1) `NotificationType`: `RESERVATION_CREATED`, `RESERVATION_CANCELED_BY_MEMBER`, `RESERVATION_CHANGED_BY_MEMBER`,
    `RESERVATION_CANCELED_BY_ADMIN`, `RESERVATION_CHANGED_BY_ADMIN`, `CLASS_SESSION_SUSPENDED`.
    각 상수에 한 줄 KDoc으로 어떤 이벤트인지 적는다 (D-093의 4종 이벤트를 주체별로 갈라 6개로 표현한 것).
    (2) `Notification` 엔티티: `PassTransaction`과 동일하게 **전 필드 `val`**(append-only, setter 없음).
    `type`, `reservation: Reservation?`(LAZY), `classSession: ClassSession?`(LAZY),
    비정규화 표시 정보 `memberName: String?`·`classType: ClassType?`·`classDate: LocalDate?`·`startTime: LocalTime?`,
    `message: String`, `occurredAt: OffsetDateTime`, `isRead: Boolean = false`, `readAt: OffsetDateTime? = null`, `createdAt`.
    `isRead`/`readAt`은 이 phase에서 쓰지 않고 Phase 6(NOTIF-02)이 갱신한다 — 그 사실과 함께
    "수신자 컬럼이 없는 이유는 수신자가 항상 관리자 전체이기 때문"(D-093)을 클래스 KDoc에 명시한다.
    `isRead`/`readAt`만 `var`로 두어 Phase 6이 갱신할 수 있게 한다.
    (3) `NotificationRepository`: `JpaRepository<Notification, Long>`. 이 phase는 저장만 하므로 조회 메서드를
    추가하지 않는다 — 조회 계약은 Phase 6이 정의한다. 그 사실을 KDoc에 남긴다.
    (4) `PassTransaction` 수정: `admin: Admin`을 `admin: Admin?`으로 완화(`@JoinColumn(name = "admin_id")`,
    `nullable = false` 제거)하고 `member: Member?`(`@JoinColumn(name = "member_id")`, LAZY)를 추가한다.
    클래스 KDoc에 **Pitfall 4 규칙**을 못박는다 — 주체는 "이용권 소유자"가 아니라 **"이 이력을 발생시킨 행위자"**이며,
    회원 셀프 예약/취소/변경은 `member`, 관리자 대리 조작과 휴강 캐스케이드는 `admin`이다.
    "정확히 하나" 불변식의 최종 방어선은 `ck_pass_transaction_subject` CHECK임을 함께 적는다.
    (5) `AdminPassService`의 `PassTransaction(...)` 생성자 호출 전부에 `member = null`을 명시적으로 넘긴다 —
    기본값에 의존하지 않는다. Phase 3 경로는 전부 관리자 주체이므로 동작이 바뀌지 않는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.pass.*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.pass.*"` 종료 코드 0 (Phase 3 회귀 0건)
    - `PassTransaction.kt`가 `val admin: Admin?`과 `val member: Member?`를 모두 갖는다
    - `PassTransaction.kt`의 KDoc이 "행위자" 규칙을 명시한다
    - `Notification.kt`의 `type`·`reservation`·`classSession`·`message`·`occurredAt` 필드가 모두 `val`이다
    - `NotificationType.kt`가 6개 상수를 갖는다
    - `./gradlew build` 종료 코드 0 (ddl-auto=validate가 4개 신규 엔티티 매핑을 검증한다)
  </acceptance_criteria>
  <done>알림 레코드를 저장할 준비가 되고, 회원 주체 이력이 스키마·엔티티 양쪽에서 표현 가능해진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서비스 계층 → 리포지토리 | 상태 전환이 조건부 UPDATE 한 줄로 표현되는 지점 — 여기서 조건을 빠뜨리면 상위 계층의 모든 검사가 무의미해진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | `incrementReservedCountIfCapacityAvailable` WHERE 절 누락 | mitigate | 정원 조건(`reservedCount + 1 <= capacity`)과 휴강 조건(`status = SCHEDULED`)을 같은 WHERE에 두고, `ClassSessionRepositoryTest`가 정원 초과·휴강 두 케이스의 0행 반환을 단언한다 |
| T-04-11 | Tampering | `Reservation` 판정 메서드에 대입문 추가 | mitigate | Pass.kt와 동일한 "여기에 대입문을 추가하지 않는다" KDoc + acceptance criteria의 grep 검사. D-072 위반이 Phase 3의 실제 회귀 버그 원인이었다 |
| T-04-12 | Information Disclosure | `findByIdAndMemberId` 대신 `findById` 사용 | mitigate | 회원 경로 조회 메서드를 소유자 조건이 포함된 형태로만 제공한다 — 소유자 없는 `findById`를 회원 서비스가 쓰지 못하게 KDoc에 명시 |
| T-04-13 | Repudiation | `Notification`을 사후 수정해 이벤트 이력 위조 | mitigate | 표시·연결 필드 전부 `val`(setter 없음). `isRead`/`readAt`만 `var`로 열어 Phase 6 확인 처리에 한정 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- 애플리케이션 부팅 시 `ddl-auto=validate`가 신규 엔티티 4개(ClassSchedule/ClassSession/Reservation/Notification)와
  V6 스키마의 불일치를 잡지 않는다
- Phase 3 테스트 전체 그린 (PassTransaction 시그니처 변경 회귀 없음)
</verification>

<success_criteria>
- 동시성 방어 도구 3종(get-or-create upsert, 정원 조건부 UPDATE, 취소 compare-and-swap)이 리포지토리에 존재하고 각각 통합테스트로 증명됨
- 부분 유니크 인덱스 3종의 실제 거부 동작이 테스트로 증명됨
- 알림 저장 경로와 회원 주체 이력 매핑이 준비됨
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-03-SUMMARY.md` when done.
청크 A(`feature/phase-04a-schedule`) 진행 중 — PR은 wave 5(04-05) 완료 후에 낸다 (D-084).
</output>
</content>
