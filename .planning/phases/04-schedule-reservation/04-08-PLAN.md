---
phase: 04-schedule-reservation
plan: 08
type: execute
wave: 8
depends_on: ["04-07"]
files_modified:
  - src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt
  - src/test/kotlin/com/goldwrestling/reservation/MemberReservationControllerTest.kt
  - src/test/kotlin/com/goldwrestling/reservation/ReservationCapacityConcurrencyTest.kt
autonomous: true
requirements: [RESV-01, RESV-02, RESV-06]

must_haves:
  truths:
    - "회원이 예약 API를 호출하면 예약이 생성되고 잔여가 즉시 줄어든 응답을 받는다"
    - "정원 10명 수업에 20명이 동시에 예약하면 정확히 10건만 성공한다 — 초과 예약 0건"
    - "1:1 슬롯에 여러 명이 동시에 예약하면 정확히 1건만 성공한다"
    - "같은 회원이 같은 타임에 동시에 두 건을 요청하면 1건만 성공한다"
    - "동시성 시나리오에서 성공 건수 = DB 활성 예약 행 수 = PassTransaction RESERVE 이력 건수 = 세션 reserved_count"
    - "ACTIVE가 아닌 회원은 예약 API를 쓸 수 없다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt"
      provides: "POST /api/members/me/reservations"
      contains: "memberStateGate.requireActive"
    - path: "src/test/kotlin/com/goldwrestling/reservation/ReservationCapacityConcurrencyTest.kt"
      provides: "RESV-06 초과 예약 0건 실증 (ExecutorService + CountDownLatch)"
      contains: "CountDownLatch"
  key_links:
    - from: "src/test/kotlin/com/goldwrestling/reservation/ReservationCapacityConcurrencyTest.kt"
      to: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      via: "동시 스레드에서 reserve 호출 후 성공 건수·DB 행 수·이력 건수 3자 일치 검증"
      pattern: "reserve\\("
---

<objective>
예약 생성 API를 열고, 이 phase의 **Core Value를 증명하는 동시성 테스트 3종**을 작성한다.

Purpose: "초과 예약 0건"은 이 시스템의 핵심 가치이고, policies §8이 요구하는 검증이다.
D-094로 검증 방법이 **JVM `ExecutorService`+`CountDownLatch` 통합테스트**로 확정됐다(k6 제외).
단순히 "성공 건수 = 정원"만 보면 부족하다 — 성공 건수·DB 활성 예약 행 수·`PassTransaction` 이력 건수·
세션 `reserved_count`가 **서로 일치**해야 "잔여 = 이력 합계" 원장 불변식까지 증명된다(ROADMAP 성공기준 4).

Output: `MemberReservationController`, 컨트롤러 통합테스트, 동시성 테스트 3종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/add-domain-test/SKILL.md
@docs/policies.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`reservation/MemberReservationService.kt`(04-07): `fun reserve(memberId: Long, request: ReserveRequest): ReservationResponse`
`reservation/dto/ReserveRequest.kt`(04-07): `ReserveRequest(classScheduleId: Long, classDate: LocalDate)`
`reservation/dto/ReservationResponse.kt`(04-07)
`member/MemberStateGate.kt`: `fun requireActive(principal: AuthenticatedPrincipal)`
`auth/AuthenticatedPrincipal.kt`: `fun requireMemberId(): Long`
`pass/MemberPassController.kt`: 회원 컨트롤러 골격(`@RestController @RequestMapping("/api/members/me")`, springdoc 애노테이션) 원본
`pass/AdminPassController.kt`: `POST .../cancellation` 형태의 하위 리소스 POST 관례 원본

`test/support/TestClockConfiguration.kt`, `MutableTestClock.kt`
`test/.../pass/PassCancellationConcurrencyTest.kt`: 동시성 테스트 골격 원본
(`@SpringBootTest` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`,
`@Transactional` 미부착, `ExecutorService`/`CountDownLatch`/`AtomicInteger`/`Collections.synchronizedList`,
`@AfterEach`에서 `JdbcClient`로 직접 정리)

`SecurityConfig`는 수정하지 않는다 — `/api/members/**`가 이미 `hasRole("MEMBER")`다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/members/me/reservations 엔드포인트</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/MemberReservationController.kt,
    src/test/kotlin/com/goldwrestling/reservation/MemberReservationControllerTest.kt
  </files>
  <behavior>
    - `POST /api/members/me/reservations` 정상 요청 → 201 + `ReservationResponse`
    - `classScheduleId` 누락 → 400 ProblemDetail (jakarta validation)
    - 정원 초과 → 409 ProblemDetail, `code = RESERVATION_CAPACITY_EXCEEDED`
    - 잔여 부족 → 409 ProblemDetail, `code = INSUFFICIENT_PASS_COUNT`
    - 중복 예약 → 409 ProblemDetail, `code = DUPLICATE_RESERVATION`
    - 예약 창 밖 → 409 ProblemDetail, `code = RESERVATION_WINDOW_CLOSED`
    - `PENDING` 회원 → 403 ProblemDetail, `code = MEMBER_NOT_ACTIVE`
    - 인증 없음 → 401 ProblemDetail
    - 관리자 토큰으로 호출 → 403 (SecurityConfig 역할 규칙)
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt (전체 — 회원 컨트롤러 골격, `@AuthenticationPrincipal`, springdoc `@Operation`/`@ApiResponse`)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt (POST 201 응답 + `@ResponseStatus` 관례)
    - src/main/kotlin/com/goldwrestling/member/MemberStateGate.kt (D-040 유의사항 KDoc)
    - src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt (04-07)
    - src/test/kotlin/com/goldwrestling/pass/MemberPassControllerTest.kt (컨트롤러 통합테스트 골격 + 인증 principal 주입)
    - .claude/skills/add-endpoint/SKILL.md
  </read_first>
  <action>
    `MemberReservationController.kt`를 `@RestController @RequestMapping("/api/members/me")`로 만든다.
    · `@PostMapping("/reservations")`, `@ResponseStatus(HttpStatus.CREATED)`,
      파라미터 `@AuthenticationPrincipal principal: AuthenticatedPrincipal`, `@RequestBody @Valid request: ReserveRequest`
    · **첫 줄에서 `memberStateGate.requireActive(principal)`를 호출한다** — D-040: 새 회원 엔드포인트마다
      게이트 호출 여부를 확인해야 하고, 빠뜨려도 컴파일·테스트가 잡아주지 않는다
    · `memberReservationService.reserve(principal.requireMemberId(), request)` 반환
    · 컨트롤러에 `@Transactional`을 붙이지 않는다 (D-020)
    · springdoc 애노테이션은 `MemberPassController` 형식을 그대로 따르고, 409 응답의 가능한 `code` 목록을
      `@ApiResponse` 설명에 나열한다 — FE가 분기해야 할 코드가 5종이라 계약에 드러나야 한다

    `MemberReservationControllerTest`는 `<behavior>` 9개 항목을 각각 `@Test`로 검증한다.
    ProblemDetail 검증은 HTTP 상태뿐 아니라 **응답 본문의 `code` 필드**까지 단언한다 —
    상태 코드만 보면 409 5종이 서로 구분되지 않는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.MemberReservationControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.MemberReservationControllerTest"` 종료 코드 0, 9개 이상 테스트 실행
    - `MemberReservationController.kt`가 `memberStateGate.requireActive` 를 호출한다
    - `MemberReservationController.kt`에 `@Transactional` 애노테이션이 0건이다
    - 테스트가 409 응답 5종의 `code` 필드를 각각 다른 값으로 단언한다
    - `SecurityConfig.kt`가 이 플랜에서 수정되지 않았다
  </acceptance_criteria>
  <done>회원이 실제로 예약할 수 있고, 5가지 실패 사유가 FE에서 구분 가능한 형태로 응답된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 동시성 테스트 3종 — 초과 예약 0건 실증 (RESV-06)</name>
  <files>src/test/kotlin/com/goldwrestling/reservation/ReservationCapacityConcurrencyTest.kt</files>
  <behavior>
    **시나리오 A — 예약제 정원 경쟁**
    · 정원 10인 `SESSION` 수업에 서로 다른 회원 20명이 각각 `SESSION_PASS`(잔여 1.0) 보유
    · 20 스레드가 `CountDownLatch`로 동시에 출발해 `reserve` 호출
    · 성공 10건, 실패 10건(전부 `ReservationCapacityExceededException`)
    · `select count(*) from reservation where class_session_id = ? and status = 'ACTIVE'` = 10
    · `select reserved_count from class_session where id = ?` = 10
    · `select count(*) from pass_transaction where reason = 'RESERVE'` = 10
    · 실패한 10명의 `pass.remaining_count`는 1.0 그대로

    **시나리오 B — 1:1 슬롯 경쟁**
    · 정원 1인 `LESSON` 수업에 서로 다른 회원 10명이 각각 `LESSON_PASS`(잔여 1.0) 보유
    · 10 스레드 동시 출발 → 성공 1건, 활성 예약 행 1건, `RESERVE` 이력 1건

    **시나리오 C — 같은 회원의 같은 타임 중복 요청**
    · 회원 1명, 잔여 5.0인 `SESSION_PASS` 1장
    · 같은 (시간표, 날짜)로 10 스레드 동시 요청
    · 성공 1건(나머지는 `DuplicateReservationException` 또는 `ReservationCapacityExceededException`),
      활성 예약 1건, 잔여 4.0 (정확히 1회만 차감)

    **공통**
    · 세션 행이 없는 상태에서 시작한다 — get-or-create 경쟁까지 함께 재현된다
    · 예외 0건이 아니라 "예외가 전부 도메인 예외"인지 확인한다 — `DataIntegrityViolationException`이나
      `LazyInitializationException`이 새어나오면 실패로 본다
  </behavior>
  <read_first>
    - src/test/kotlin/com/goldwrestling/pass/PassCancellationConcurrencyTest.kt (전체 — 골격을 그대로 재사용한다)
    - src/test/kotlin/com/goldwrestling/schedule/ClassSessionConcurrencyTest.kt (04-05 — 같은 phase의 선례)
    - src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt (04-07)
    - docs/conventions.md §10.4 (동시성 테스트 규약)
    - .claude/skills/add-domain-test/SKILL.md §동시성
    - docs/policies.md §8 (04-01이 정정한 검증 방법 문구)
  </read_first>
  <action>
    `ReservationCapacityConcurrencyTest.kt`를 작성한다. `PassCancellationConcurrencyTest` 골격을 그대로 따른다:
    · `@SpringBootTest` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`
    · **클래스·메서드에 `@Transactional`을 붙이지 않는다** — 각 스레드가 별도 트랜잭션이어야 경쟁이 재현된다
    · `startLatch`(1) / `doneLatch`(N), `AtomicInteger successCount`,
      `Collections.synchronizedList<Throwable>`로 실패 수집
    · `ExecutorService`는 `Executors.newFixedThreadPool(N)`, 테스트 끝에 `shutdown()` + `awaitTermination`
    · `@AfterEach`에서 `JdbcClient`로 `notification` → `pass_transaction` → `reservation` → `class_session` →
      `pass` → `member` 순으로 직접 정리한다(FK 역순). 롤백에 기대지 않는다
    · 시각은 `MutableTestClock`으로 **예약 창 안**(이번 주, 수업 시작 전)에 고정한다 — 고정하지 않으면
      테스트를 돌리는 요일에 따라 `ReservationWindowClosedException`으로 전부 실패한다

    `<behavior>`의 시나리오 A·B·C를 각각 `@Test`로 만든다. 각 테스트는 **4자 일치**(성공 건수 = 활성 예약 행 수 =
    `RESERVE` 이력 건수 = 세션 `reserved_count`)를 한 블록에서 함께 단언한다 — 이게 ROADMAP 성공기준 4의 문구다.

    동시성 테스트는 타이밍에 민감하다. flaky를 줄이기 위해 스레드 풀 크기를 요청 수와 같게 두고,
    `startLatch.await()` 직후 곧바로 `reserve`를 부르도록 준비 작업(픽스처 조회 등)을 latch **이전**에 끝낸다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.ReservationCapacityConcurrencyTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.ReservationCapacityConcurrencyTest"` 를 **연속 2회** 실행해 모두 종료 코드 0 (flaky 확인)
    - 테스트 3개(정원 경쟁 / 1:1 슬롯 / 같은 회원 중복)가 모두 실행된다
    - 각 테스트가 성공 건수·활성 예약 행 수·`RESERVE` 이력 건수·`reserved_count` 4자 일치를 단언한다
    - `ReservationCapacityConcurrencyTest.kt`에 `@Transactional` 애노테이션이 0건이다
    - 시나리오 A가 "실패한 회원들의 잔여가 그대로 1.0"임을 단언한다
    - 수집된 예외가 전부 `DomainException` 하위 타입임을 단언한다
  </acceptance_criteria>
  <done>초과 예약 0건이 실제 Postgres·실제 동시 트랜잭션으로 증명되고, 원장 불변식이 함께 확인된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인증된 회원 → 예약 API | 역할은 SecurityConfig가, 회원 상태는 서비스 계층 게이트가 검사한다 |
| 동시 요청 다수 → 단일 세션 정원 | 이 시스템 Core Value가 실제로 성립하는지가 결정되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-34 | Elevation of Privilege | `PENDING`/`ON_LEAVE` 회원의 예약 | mitigate | 컨트롤러 첫 줄 `memberStateGate.requireActive(principal)`. 403 + `MEMBER_NOT_ACTIVE` 테스트 |
| T-04-35 | Elevation of Privilege | 관리자 토큰으로 회원 예약 API 호출 | mitigate | `/api/members/**` → `hasRole("MEMBER")` (기존 SecurityConfig). 403 테스트로 확인 |
| T-04-36 | Tampering | 레이스를 악용한 초과 예약(정원·1:1) | mitigate | 조건부 UPDATE + 부분 유니크 인덱스 이중 방어. `ReservationCapacityConcurrencyTest` 시나리오 A·B가 실증 |
| T-04-37 | Tampering | 같은 회원이 동시 요청으로 이용권 이중 차감 | mitigate | 시나리오 C가 "잔여가 정확히 1회만 줄었다"를 단언. `ux_reservation_member_timeslot_active`가 최종 방어선 |
| T-04-38 | Repudiation | 예약은 됐는데 이력이 없거나 그 반대 | mitigate | 4자 일치 단언(성공 건수 = 예약 행 = 이력 = `reserved_count`) |
| T-04-39 | Information Disclosure | 409 응답이 타 회원 예약 현황을 유추하게 함 | accept | 정원 초과 여부는 시간표 조회로도 이미 공개되는 정보(인원 숫자)이며 개인 식별 정보가 아니다 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- 동시성 테스트는 CI 환경에 따라 타이밍이 달라질 수 있어 **2회 연속 통과**를 게이트로 삼는다
- `openapi.yaml` 재생성은 청크 B 마지막 플랜(04-10)에서 한 번에 한다
</verification>

<success_criteria>
- 회원이 예약 API로 실제 예약을 만들 수 있다
- 정원 경쟁·1:1 슬롯·같은 회원 중복 3종 동시성이 실제 DB로 검증되어 policies §8을 충족한다 (D-094)
- 성공 건수와 DB 상태·원장이 어긋나지 않는다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-08-SUMMARY.md` when done.
청크 B(`feature/phase-04b-reservation`) 진행 중 — PR은 wave 10(04-10) 완료 후에 낸다 (D-084).
</output>
</content>
