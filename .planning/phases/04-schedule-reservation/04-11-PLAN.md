---
phase: 04-schedule-reservation
plan: 11
type: execute
wave: 11
depends_on: ["04-10"]
files_modified:
  - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt
  - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt
  - src/main/kotlin/com/goldwrestling/schedule/dto/AdminWeeklyBoardResponse.kt
  - src/main/kotlin/com/goldwrestling/schedule/dto/AdminBoardCellResponse.kt
  - src/main/kotlin/com/goldwrestling/schedule/dto/BoardReservationResponse.kt
  - src/test/kotlin/com/goldwrestling/schedule/AdminScheduleServiceTest.kt
  - src/test/kotlin/com/goldwrestling/schedule/AdminScheduleControllerTest.kt
autonomous: true
requirements: [SCHED-03]

must_haves:
  truths:
    - "관리자가 주간 스케줄 보드를 조회하면 요일×타임 그리드가 수업 종류·예약 n/정원·1:1 여부와 함께 내려온다"
    - "셀마다 예약자 명단(회원 id·이름)이 포함된다"
    - "예약이 없는 타임도 세션 행 없이 0/정원으로 표시된다"
    - "휴강된 타임은 휴강 상태와 사유가 함께 표시된다"
    - "관리자는 회원과 달리 조회 주 범위 제한 없이 과거·미래 주를 볼 수 있다"
    - "회원 토큰으로는 이 API에 접근할 수 없다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt"
      provides: "GET /api/admin/schedule/board"
      contains: "/api/admin"
    - path: "src/main/kotlin/com/goldwrestling/schedule/dto/AdminBoardCellResponse.kt"
      provides: "셀별 예약자 명단 포함 응답"
      contains: "reservations"
    - path: "src/test/kotlin/com/goldwrestling/schedule/AdminScheduleServiceTest.kt"
      provides: "보드 조립 규칙(빈 셀·명단·휴강·주 정규화) 직접 검증"
      contains: "AdminScheduleService"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt"
      to: "src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt"
      via: "findAllByClassSessionIdInAndStatus 배치 조회로 셀별 명단 구성"
      pattern: "findAllByClassSessionIdInAndStatus"
---

<objective>
관리자 **주간 스케줄 보드**(SCHED-03)를 만든다 — 요일×타임 그리드에 수업 종류, 예약 n/정원,
1:1 여부, **셀별 예약자 명단**을 담아 내려준다.

Purpose: 관리자 대시보드의 핵심 화면이고, 대리 취소(04-13)·휴강 처리(04-14)가 시작되는 진입점이다.
회원용 시간표(04-05)와 데이터 원천은 같지만 두 가지가 다르다 — ① 예약자 명단을 포함하고(D-096:
명단은 관리자만), ② 조회 주 범위 제한이 없다(관리자는 과거 출석 확인·미래 계획을 봐야 한다).
같은 그리드 조립 로직을 두 벌 만들지 않도록, 회원용과 공유 가능한 부분을 먼저 확인하고 재사용한다.

Output: `AdminScheduleService`(+ 서비스 통합테스트), `AdminScheduleController`(+ 컨트롤러 통합테스트),
보드 응답 DTO 3종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/add-domain-test/SKILL.md
@docs/requirements.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`schedule/ScheduleService.kt`(04-05): `fun getWeeklySchedule(memberId, branchId, weekStart): WeeklyScheduleResponse`
  — 그리드 조립 로직의 원본. 공통 부분은 private 헬퍼로 추출해 공유한다
`schedule/ClassScheduleRepository.kt`(04-03): `fun findAllByBranchId(branchId: Long): List<ClassSchedule>`
`schedule/ClassSessionRepository.kt`(04-03): `fun findAllByClassDateBetween(from: LocalDate, to: LocalDate): List<ClassSession>`
`schedule/ReservationWindow.kt`(04-04): `fun bookableWeek(today: LocalDate): WeekRange`
`common/time/WeekRange.kt`(04-04): `WeekRange.of(date)`, `fun dates(): List<LocalDate>`
`reservation/ReservationRepository.kt`(04-03):
- `fun findAllByClassSessionIdInAndStatus(ids: Collection<Long>, status: ReservationStatus): List<Reservation>`
`auth/AuthenticatedPrincipal.kt`: `fun requireAdminId(): Long`
`admin/` 패키지: 관리자와 지점 연결은 `admin_branch`(V2) — 관리자가 여러 지점을 가질 수 있다.
  현재 MVP는 단일 지점이므로 `branchId`를 요청 파라미터로 받거나 관리자의 첫 지점을 쓴다(읽고 기존 방식 확인).
`member/AdminMemberController.kt`: `@RestController @RequestMapping("/api/admin/...")` 골격 원본

`SecurityConfig`는 수정하지 않는다 — `/api/admin/**`가 이미 `hasRole("ADMIN")`이다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminScheduleService — 주간 보드 조립 (셀별 명단 포함) + 서비스 통합테스트</name>
  <files>
    src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt,
    src/main/kotlin/com/goldwrestling/schedule/dto/AdminWeeklyBoardResponse.kt,
    src/main/kotlin/com/goldwrestling/schedule/dto/AdminBoardCellResponse.kt,
    src/main/kotlin/com/goldwrestling/schedule/dto/BoardReservationResponse.kt,
    src/test/kotlin/com/goldwrestling/schedule/AdminScheduleServiceTest.kt
  </files>
  <behavior>
    - `getWeeklyBoard(branchId, weekStart)` → 7일 × 요일별 타임 셀 그리드
    - 예약이 없는 타임 셀 → `classSessionId = null`, `reservedCount = 0`, `reservations = []`
    - 예약 3건이 있는 예약제 셀 → `reservedCount = 3`, `capacity = 10`, `reservations` 3건
    - `reservations`의 각 항목은 `reservationId`·`memberId`·`memberName`만 포함한다 (전화번호·상태 등은 제외)
    - 취소된 예약은 명단에 포함되지 않는다
    - 휴강 셀 → `suspended = true`, `cancelReason` 채워짐
    - 저녁반 셀 → `capacity = null`, `reservable = false`, `reservations = []`
    - 지난 주·다음 다음 주 `weekStart`도 정상 조회된다 (관리자는 범위 제한 없음)
    - `weekStart`가 월요일이 아니면 그 날짜가 속한 주의 월요일로 정규화한다 (거부하지 않는다)
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/schedule/ScheduleService.kt (04-05 전체 — 그리드 조립 로직. 공유 가능한 부분을 판단한다)
    - src/main/kotlin/com/goldwrestling/schedule/dto/ScheduleCellResponse.kt (04-05 — 회원용 셀과 무엇이 다른지 비교)
    - src/main/kotlin/com/goldwrestling/reservation/Reservation.kt, ReservationRepository.kt (04-03)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (관리자 서비스 골격)
    - src/test/kotlin/com/goldwrestling/schedule/MemberScheduleControllerTest.kt (04-05 — 시간표 픽스처 구성 방식. 시간표·세션·예약을 어떻게 심는지 그대로 재사용한다)
    - docs/requirements.md §4.5 (관리자 대시보드 — 주간 스케줄 보드 요구 항목)
    - .planning/phases/04-schedule-reservation/04-CONTEXT.md "회원 시간표 응답 (D-096)" (명단 비공개 경계)
  </read_first>
  <action>
    (1) DTO 3종을 만든다.
    · `AdminWeeklyBoardResponse(weekStart: LocalDate, weekEnd: LocalDate, days: List<AdminBoardDayResponse>)`
      — 일자별 구조는 회원용 `DayScheduleResponse`와 같은 형태를 쓰되 셀 타입만 다르다
    · `AdminBoardCellResponse(classScheduleId, classSessionId: Long?, classType, startTime, endTime,
       capacity: Int?, reservedCount: Int, reservable: Boolean, suspended: Boolean, cancelReason: String?,
       reservations: List<BoardReservationResponse>)`
    · `BoardReservationResponse(reservationId: Long, memberId: Long, memberName: String)`
      — **전화번호·회원 상태·이용권 정보는 넣지 않는다.** 명단의 목적은 "누가 오는가" 확인이고,
      더 필요한 정보는 기존 회원 상세 API로 간다. 이 경계를 클래스 KDoc에 명시한다

    (2) `AdminScheduleService.kt` — `@Service @Transactional(readOnly = true)`.
    `fun getWeeklyBoard(branchId: Long, weekStart: LocalDate?): AdminWeeklyBoardResponse`
    · `weekStart`가 null이면 오늘이 속한 주, 월요일이 아니면 `WeekRange.of(weekStart).monday`로 정규화한다
      — 회원용(`assertViewable`로 거부)과 다르다. 관리자는 범위 제한이 없다는 것을 KDoc에 명시한다
    · 시간표 전량 → 그리드 뼈대 → 세션 배치 조회 1회로 덮어쓰기 → 예약 배치 조회 1회로 명단 채우기.
      **회원용 `ScheduleService`와 동일한 N+1 회피 원칙**을 지킨다(셀 루프 안에서 리포지토리 호출 금지)
    · `memberName` 접근이 LAZY 프록시를 건드리므로, 예약 배치 조회 시 `member`를 함께 가져오도록
      `@EntityGraph` 또는 fetch join을 쓴다 — 안 쓰면 명단 크기만큼 추가 쿼리가 나간다.
      `ReservationRepository`에 fetch join 버전 메서드를 추가해도 된다
    · 회원용 `ScheduleService`와 겹치는 "시간표 → 그리드 뼈대 → 세션 덮어쓰기" 로직은
      한쪽에 private 헬퍼로 두고 재사용하거나, 공통 부분을 `schedule` 패키지의 내부 헬퍼로 추출한다.
      **두 벌로 복제하지 않는다** — 복제하면 시간표 정렬·EVENING 취급이 두 화면에서 어긋난다

    (3) `AdminScheduleServiceTest.kt` — Testcontainers 통합테스트(`add-domain-test` 스킬 골격).
    위 `<behavior>` **9개 항목을 각각 `@Test`로 직접 검증한다.** 서비스 메서드를 직접 호출하므로
    HTTP 레이어 없이 조립 규칙만 고립시켜 확인할 수 있다 — 컨트롤러 테스트(Task 2)는 인가·직렬화·
    파라미터 바인딩을 보고, 이 테스트는 그리드 조립 규칙을 본다. 두 레이어의 관심사를 섞지 않는다.
    · `Clock`을 고정해 "지난 주 / 다음 다음 주" 케이스가 실행 시점에 따라 흔들리지 않게 한다
    · 취소 예약 케이스는 `ACTIVE` 1건 + `CANCELED` 1건을 같은 세션에 심고
      `reservations.size == 1`과 `reservedCount == 1`을 함께 단언한다
    · 명단 필드 경계는 `BoardReservationResponse`의 선언 프로퍼티 수를 단언하는 대신,
      DTO에 회원 이름 외 개인정보 필드가 없음을 acceptance criteria(정적 검사)로 잡는다
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.schedule.AdminScheduleServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.schedule.AdminScheduleServiceTest"` 종료 코드 0, 9개 이상 테스트 실행
    - 테스트가 "예약 없는 셀 → `reservedCount = 0`, `reservations = []`"를 단언한다
    - 테스트가 "취소된 예약은 명단·`reservedCount`에 반영되지 않는다"를 단언한다
    - 테스트가 "휴강 셀 → `suspended = true` + `cancelReason` 존재"를 단언한다
    - 테스트가 "수요일 `weekStart` 입력 → 응답 `weekStart`가 그 주 월요일"과 "지난 주 조회 성공"을 각각 단언한다
    - 테스트가 `Clock` 빈을 고정한다 (실행 날짜에 따라 결과가 바뀌지 않는다)
    - `BoardReservationResponse.kt`에 `phoneNumber`·`memberStatus`·`passId` 필드가 없다
    - `AdminScheduleService.kt`가 셀 루프 안에서 리포지토리 메서드를 호출하지 않는다 (배치 조회 2회 이하)
    - `AdminScheduleService.kt`가 예약 조회 시 `member`를 fetch join 또는 `@EntityGraph`로 함께 가져온다
    - 회원용 `ScheduleService`와 그리드 조립 코드가 통째로 복제되지 않았다 (공통 헬퍼 재사용)
  </acceptance_criteria>
  <done>관리자 화면이 요구하는 그리드 데이터가 쿼리 몇 개로 조립되고, 조립 규칙 9개가 테스트로 고정된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/admin/schedule/board 엔드포인트 + 통합테스트</name>
  <files>
    src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt,
    src/test/kotlin/com/goldwrestling/schedule/AdminScheduleControllerTest.kt
  </files>
  <behavior>
    - `GET /api/admin/schedule/board` (weekStart 생략) → 200, 이번 주 그리드
    - `GET /api/admin/schedule/board?weekStart={지난 주 월요일}` → 200 (관리자는 범위 제한 없음)
    - `GET /api/admin/schedule/board?weekStart={수요일 날짜}` → 200, 응답 `weekStart`는 그 주 월요일로 정규화됨
    - 예약 3건 있는 셀 → `reservations` 3건, 각 항목에 `memberName` 포함
    - 취소된 예약이 있는 셀 → 명단에서 제외되고 `reservedCount`에도 반영되지 않는다
    - 휴강 셀 → `suspended = true`, `cancelReason` 포함
    - 회원 토큰으로 호출 → 403
    - 인증 없음 → 401
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt (전체 — 관리자 컨트롤러 골격, springdoc 애노테이션)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassController.kt (`@RequestMapping("/api/admin")` 형태)
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleService.kt (Task 1 산출물)
    - src/test/kotlin/com/goldwrestling/schedule/AdminScheduleServiceTest.kt (Task 1 산출물 — 픽스처 구성을 그대로 재사용하고 단언만 HTTP 레벨로 바꾼다)
    - src/test/kotlin/com/goldwrestling/pass/AdminPassControllerTest.kt (관리자 컨트롤러 통합테스트 골격 + 관리자 principal 주입)
    - .claude/skills/add-endpoint/SKILL.md
  </read_first>
  <action>
    `AdminScheduleController.kt` — `@RestController @RequestMapping("/api/admin/schedule")`.
    · `@GetMapping("/board")`, 파라미터 `@RequestParam(required = false) weekStart: LocalDate?`
      (`@DateTimeFormat(iso = ISO.DATE)`), `@AuthenticationPrincipal principal: AuthenticatedPrincipal`
    · `branchId`는 관리자에게 연결된 지점에서 얻는다 — `admin_branch` 매핑을 조회하는 기존 경로가 있으면 그것을 쓰고,
      없으면 `@RequestParam(required = false) branchId: Long?`로 받되 **관리자가 소속되지 않은 지점 id면 거부**한다.
      어느 쪽인지는 `admin` 패키지의 기존 코드를 읽고 결정해 SUMMARY에 기록한다
    · `memberStateGate`는 호출하지 않는다 — 회원 상태 게이트는 회원 전용이며 관리자 principal이 들어오면
      `IllegalStateException`을 던진다
    · 컨트롤러에 `@Transactional`을 붙이지 않는다 (D-020)

    `AdminScheduleControllerTest`는 `<behavior>` 8개 항목을 각각 `@Test`로 검증한다.
    "회원 토큰 403"을 반드시 포함한다 — 예약자 명단이 나가는 API라 역할 인가가 실제로 걸리는지 확인해야 한다.
    조립 규칙 자체는 Task 1의 `AdminScheduleServiceTest`가 이미 고정했으므로, 여기서는 **HTTP 계약**
    (상태코드·인가·쿼리 파라미터 바인딩·JSON 직렬화 형태)에 단언을 집중한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.schedule.AdminScheduleControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.schedule.AdminScheduleControllerTest"` 종료 코드 0, 8개 이상 테스트 실행
    - 테스트가 "회원 토큰 → 403"과 "인증 없음 → 401"을 각각 단언한다
    - 테스트가 "취소된 예약은 명단·`reservedCount`에 반영되지 않는다"를 단언한다
    - 테스트가 수요일 `weekStart` 입력 시 응답 `weekStart`가 그 주 월요일임을 단언한다
    - `AdminScheduleController.kt`에 `memberStateGate` 참조가 없다
    - `SecurityConfig.kt`가 이 플랜에서 수정되지 않았다
  </acceptance_criteria>
  <done>관리자가 한 화면에서 주간 운영 현황과 예약자 명단을 볼 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 요청 → 예약자 명단 응답 | 회원 개인정보(이름)가 응답에 포함되는 유일한 조회 경로 |
| 관리자 요청 → `branchId` 파라미터 | 다지점 확장 시 타 지점 데이터 열람 경로가 될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-51 | Information Disclosure | 회원 토큰으로 예약자 명단 조회 | mitigate | `/api/admin/**` → `hasRole("ADMIN")` (기존 SecurityConfig). 테스트가 403을 단언 |
| T-04-52 | Information Disclosure | 명단에 전화번호·이용권 정보까지 노출 | mitigate | `BoardReservationResponse`를 `reservationId`/`memberId`/`memberName` 3필드로 고정. acceptance criteria가 추가 필드 부재를 검사 |
| T-04-53 | Elevation of Privilege | 소속되지 않은 지점의 보드 조회 | mitigate | `branchId`를 요청으로 받는 경우 관리자-지점 매핑(`admin_branch`)을 검증한 뒤에만 조회 |
| T-04-54 | Denial of Service | 넓은 주 범위·셀별 개별 쿼리로 DB 포화 | mitigate | 조회 단위를 1주로 고정하고 배치 조회 2회 이하로 제한. `member` fetch join으로 명단 N+1 제거 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `openapi.yaml` 재생성은 청크 C 마지막 플랜(04-15)에서 한 번에 한다
</verification>

<success_criteria>
- 관리자가 요일×타임 그리드와 셀별 예약자 명단을 조회할 수 있다
- 명단이 관리자 역할에서만 나가고 노출 필드가 최소로 고정된다
- 조회가 배치 쿼리로 처리되어 셀 수에 비례해 쿼리가 늘지 않는다
- 보드 조립 규칙과 HTTP 계약이 각각 별도 테스트 파일로 고정된다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-11-SUMMARY.md` when done.
**청크 C 첫 플랜** — `feature/phase-04c-admin-ops` 브랜치를 최신 dev에서 새로 따고 시작한다 (D-084).
이 플랜은 04-12와 같은 wave 11이므로 병렬 실행 가능하다 (파일 겹침 없음).
</output>
</content>
