---
phase: 02-auth-member
plan: 10
type: execute
wave: 8
depends_on: ["02-04", "02-08", "02-09"]
files_modified:
  - src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt
  - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
  - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt
  - src/main/kotlin/com/goldwrestling/member/dto/RejectMemberRequest.kt
  - src/main/kotlin/com/goldwrestling/member/dto/UpdateMemberStatusRequest.kt
  - src/test/kotlin/com/goldwrestling/member/MemberApprovalTest.kt
  - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [MEMBER-01, MEMBER-03]

must_haves:
  truths:
    - "관리자가 온보딩을 마친 PENDING 회원을 승인하면 ACTIVE로 전환되고 전체 기능을 쓸 수 있다"
    - "온보딩 미완료 회원은 승인할 수 없다 (policies §5.1)"
    - "관리자가 사유를 적어 거절하면 INACTIVE로 전환되고 사유가 기록된다 (D-034)"
    - "거절된 회원이 재로그인하면 거절 안내 화면 대상으로 식별된다"
    - "관리자가 상태를 PENDING으로 되돌리면 거절 사유가 지워져 재신청 상태가 된다"
    - "전환 후 상태가 ACTIVE가 아니면 그 회원의 refresh 토큰이 전부 폐기되어 강제 로그아웃된다 (D-033, D-044)"
    - "상태가 바뀐 회원의 기발급 access 토큰도 즉시 상태 게이트에 막힌다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      provides: "approve / reject / changeStatus (상태 전이 + refresh 무효화)"
      exports: ["approve", "reject", "changeStatus"]
    - path: "src/main/kotlin/com/goldwrestling/member/dto/RejectMemberRequest.kt"
      provides: "거절 사유 필수 입력 계약"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      to: "TokenService.revokeAllForMember"
      via: "ACTIVE가 아니게 되는 전이에서 강제 로그아웃 (D-044)"
      pattern: "revokeAllForMember"
    - from: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      to: "Member.isOnboardingCompleted()"
      via: "승인 가능 여부 판정 (policies §5.1)"
      pattern: "isOnboardingCompleted"
---

<objective>
Phase 2의 마지막 도메인 기능 — **가입 승인·거절(MEMBER-01)과 회원 상태 변경(MEMBER-03)**.
회원이 `PENDING`에서 `ACTIVE`로 넘어가는 유일한 경로가 여기서 생긴다.

D-033이 요구한 강제 로그아웃이 여기서 실제로 발동한다: 상태가 `ACTIVE`가 아니게 되는 전이는
그 회원의 refresh 토큰을 전부 폐기한다(D-044). access 토큰은 최대 30분 남지만, 02-05의 필터가
매 요청 DB 상태를 다시 읽으므로 상태 게이트가 즉시 막는다 — 두 방어선이 함께 있어야 완결된다.

Purpose: 관리자가 회원 생애주기를 통제할 수 있게 하고, 상태 변경이 세션에 즉시 반영되게 한다.
Output: 승인·거절·상태 변경 API + 강제 로그아웃 연동 + openapi.yaml 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<commit_policy>
이 레포는 **사용자가 명시적으로 요청할 때만 커밋한다** (CLAUDE.md 커밋·브랜치 규칙).
execute-plan 워크플로가 자동 커밋을 요구하더라도 `git add`/`git commit`/`git push`를 실행하지 않는다.
파일 저장까지만 하고, 완료 보고에 "커밋 대기 중인 변경 파일 목록"을 나열한 뒤 멈춘다.
</commit_policy>

<context>
@CLAUDE.md
@docs/policies.md
@docs/decisions.md
@docs/conventions.md
@.claude/skills/add-endpoint/SKILL.md
@src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
@src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt
@src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt
@src/main/kotlin/com/goldwrestling/auth/TokenService.kt
</context>

<interfaces>
<!-- 기존 (02-02~02-09 결과) -->
com.goldwrestling.member.Member — status: MemberStatus(var), rejectionReason: String?(var),
  isOnboardingCompleted(), isRejected()
com.goldwrestling.member.MemberStatus — PENDING, ACTIVE, ON_LEAVE, INACTIVE
com.goldwrestling.member.MemberRepository
com.goldwrestling.member.MemberExceptions — MemberNotFoundException, MemberStateConflictException(message)
com.goldwrestling.member.dto.MemberDetailResponse.from(member)
com.goldwrestling.member.AdminMemberService (@Service, @Transactional(readOnly = true)) — search, getDetail
com.goldwrestling.member.AdminMemberController — @RequestMapping("/api/admin/members"), GET 두 개
com.goldwrestling.auth.TokenService.revokeAllForMember(memberId: Long)   // @Transactional

<!-- 이 플랜이 추가하는 계약 -->
com.goldwrestling.member.dto.RejectMemberRequest(@field:NotBlank @field:Size(max = 500) reason: String)
com.goldwrestling.member.dto.UpdateMemberStatusRequest(@field:NotNull status: MemberStatus)
AdminMemberService:
  @Transactional fun approve(memberId: Long): MemberDetailResponse
  @Transactional fun reject(memberId: Long, reason: String): MemberDetailResponse
  @Transactional fun changeStatus(memberId: Long, newStatus: MemberStatus): MemberDetailResponse
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 승인·거절 (MEMBER-01)</name>
  <files>src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt, src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt, src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt, src/main/kotlin/com/goldwrestling/member/dto/RejectMemberRequest.kt, src/test/kotlin/com/goldwrestling/member/MemberApprovalTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (02-09 결과 — 기존 search/getDetail을 지우지 말고 메서드를 추가한다)
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt (02-09 결과 — 기존 GET 두 개 유지)
    - src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt (02-08 결과 — 기존 4개 예외 유지)
    - src/main/kotlin/com/goldwrestling/member/Member.kt (02-02 결과 — isOnboardingCompleted)
    - src/main/kotlin/com/goldwrestling/auth/TokenService.kt (02-04 결과 — revokeAllForMember)
    - docs/policies.md §5, §5.1, §5.2
    - docs/decisions.md D-034, D-043, D-044
    - .claude/skills/add-endpoint/SKILL.md §1, §4
  </read_first>
  <behavior>
    승인 (POST /api/admin/members/{memberId}/approval):
    - 온보딩 완료 PENDING 회원을 승인하면 200 이고 status가 ACTIVE로 바뀐다
    - 승인 후 그 회원은 상태 게이트를 통과한다 (MemberStateGate.requireActive가 통과)
    - 승인은 refresh 토큰을 폐기하지 않는다 (승인 직후 재로그인을 강요하지 않는다 — D-044)
    - 온보딩 미완료 PENDING 회원 승인은 409 + code=MEMBER_STATE_CONFLICT (policies §5.1)
    - 이미 ACTIVE인 회원 승인은 409 + code=MEMBER_STATE_CONFLICT
    - INACTIVE 회원 승인은 409 + code=MEMBER_STATE_CONFLICT (재신청은 PENDING 복귀를 거친다 — D-034)
    - 존재하지 않는 memberId면 404 + code=MEMBER_NOT_FOUND
    - 회원 토큰으로 호출하면 403

    거절 (POST /api/admin/members/{memberId}/rejection):
    - 온보딩 완료 PENDING 회원을 사유와 함께 거절하면 200 이고 status=INACTIVE, rejectionReason이 저장된다
    - 거절된 회원의 상세 응답에 rejectionReason 원문이 보인다
    - 거절 직후 그 회원의 미폐기 refresh 토큰이 전부 폐기된다 (D-044)
    - 거절된 회원이 카카오 재로그인하면 응답의 rejected가 true 다 (D-034)
    - 거절된 회원의 GET /api/members/me 응답에 rejected=true, rejectionReason 없음 (D-043)
    - 사유가 공백이면 400 + code=VALIDATION_FAILED 이고 상태가 바뀌지 않는다
    - 사유가 501자 이상이면 400 + code=VALIDATION_FAILED (컬럼 길이 500)
    - PENDING이 아닌 회원 거절은 409 + code=MEMBER_STATE_CONFLICT
  </behavior>
  <action>
**MemberExceptions.kt** — 기존 4개를 유지하고 필요하면 문구만 추가한다. 새 예외 클래스는 만들지 않는다 —
승인/거절 실패는 전부 `MemberStateConflictException(message)`로 표현하고 상황별 문구를 호출부가 넘긴다
(에러코드를 늘리지 않는다: FE 동작은 "안내 문구 표시"로 동일하다). 이 판단을 파일 KDoc에 한 줄 남긴다.

**AdminMemberService.kt** — 기존 `search`·`getDetail`을 그대로 두고 메서드를 추가한다.
생성자에 `TokenService`를 추가 주입한다.
- `@Transactional fun approve(memberId: Long): MemberDetailResponse`
  1. 회원 조회, 없으면 `MemberNotFoundException`
  2. `member.status != PENDING`이면 `MemberStateConflictException("승인 대기 상태의 회원만 승인할 수 있습니다.")`
  3. `!member.isOnboardingCompleted()`면 `MemberStateConflictException("회원이 이름·전화번호를 등록해야 승인할 수 있습니다.")`
     (policies §5.1 — 승인 목록 노출 필터와 같은 규칙이 서버에서도 강제되어야 한다. 목록 필터만으로는
     API 직접 호출을 막지 못한다. 이 이유를 주석으로 남긴다)
  4. `member.status = MemberStatus.ACTIVE`, `member.rejectionReason = null`
     (이전 거절 이력이 남아 있으면 `isRejected()` 판정이 흐려진다 — D-044의 취지를 따른다)
  5. **refresh를 폐기하지 않는다** — 승인은 권한이 늘어나는 전이다. 이 예외를 주석으로 명시한다
  6. `MemberDetailResponse.from(member)` 반환
- `@Transactional fun reject(memberId: Long, reason: String): MemberDetailResponse`
  1. 회원 조회, 없으면 `MemberNotFoundException`
  2. `member.status != PENDING`이면 `MemberStateConflictException("승인 대기 상태의 회원만 거절할 수 있습니다.")`
  3. `member.status = MemberStatus.INACTIVE`, `member.rejectionReason = reason.trim()` (D-034)
  4. `tokenService.revokeAllForMember(memberId)` — 전환 후 상태가 ACTIVE가 아니므로 강제 로그아웃 (D-044)
  5. `MemberDetailResponse.from(member)` 반환
  `TokenService.revokeAllForMember`도 `@Transactional`이므로 기본 전파(REQUIRED)로 같은 트랜잭션에
  참여한다 — 상태 변경과 토큰 폐기가 함께 커밋되거나 함께 롤백된다. 이 점을 KDoc에 남긴다.

**dto/RejectMemberRequest.kt** — `data class RejectMemberRequest(@field:NotBlank @field:Size(max = 500) val reason: String)`.
`@Schema(description = "거절 사유. 관리자 전용 정보이며 회원에게 원문이 노출되지 않는다 (D-043)")`.

**AdminMemberController.kt** — 기존 GET 두 개 아래에 추가한다.
- `@PostMapping("/{memberId}/approval")` `@Operation(summary = "가입 승인 (온보딩 완료 PENDING 회원만)")`
  `fun approve(@PathVariable memberId: Long): MemberDetailResponse`
- `@PostMapping("/{memberId}/rejection")` `@Operation(summary = "가입 거절 (INACTIVE 전환 + 사유 기록)")`
  `fun reject(@PathVariable memberId: Long, @RequestBody @Valid request: RejectMemberRequest): MemberDetailResponse`
경로를 `/approve`·`/reject`(동사)가 아니라 `/approval`·`/rejection`(명사 리소스)으로 둔다 —
add-endpoint §1의 경로 규약(kebab-case 명사)에 맞춘다. `@Transactional` 금지.

**MemberApprovalTest.kt** — `@SpringBootTest @AutoConfigureMockMvc` +
`@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`.
위 behavior 전부를 검증한다.
- "거절 직후 refresh 폐기"는 거절 전에 `TokenService.issueTokenPair(MEMBER, id)`로 토큰을 만들어 두고,
  거절 후 그 refresh로 `POST /api/auth/refresh`를 호출해 401 + `REFRESH_TOKEN_INVALID`가 나오는지 확인한다
- "거절된 회원 재로그인 시 rejected=true"는 `POST /api/auth/kakao/login`을 호출해 확인한다
  (카카오 클라이언트는 02-06 테스트와 같은 방식으로 목 처리)
- "승인은 refresh를 폐기하지 않는다"는 승인 후 같은 refresh로 갱신이 200인지 확인한다
- "검증 실패 시 상태가 바뀌지 않는다"는 요청 전후 `member.status`를 비교한다
  </action>
  <verify>
    <automated>./gradlew test --tests "*MemberApprovalTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*MemberApprovalTest*"` exits 0
    - `POST /api/admin/members/{id}/approval` 이 온보딩 완료 PENDING 회원에 200 + `$.status == "ACTIVE"` 를 반환한다
    - 온보딩 미완료 PENDING 회원 승인이 409 + `$.code == "MEMBER_STATE_CONFLICT"` 다
    - `POST /api/admin/members/{id}/rejection` 이 200 + `$.status == "INACTIVE"` + `$.rejectionReason` 이 요청 사유와 같다
    - 거절 후 그 회원의 refresh 토큰으로 `/api/auth/refresh` 호출이 401 + `$.code == "REFRESH_TOKEN_INVALID"` 다
    - 승인 후 refresh 토큰으로 `/api/auth/refresh` 호출이 200 이다
    - 거절된 회원의 `GET /api/members/me` 응답에 `$.rejected == true` 이고 `rejectionReason` 키가 없다
    - `grep -c 'isOnboardingCompleted' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 1 이상
    - `grep -c 'revokeAllForMember' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 1 이상
    - 02-09가 만든 `search`·`getDetail` 메서드와 GET 엔드포인트 두 개가 그대로 남아 있다
    - `grep -c '@Transactional' src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt` 가 0
  </acceptance_criteria>
  <done>가입 승인·거절이 policies §5.1·§5.2 규칙대로 동작하고, 거절이 즉시 세션을 끊는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 회원 상태 변경 + 강제 로그아웃 (MEMBER-03, D-044)</name>
  <files>src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt, src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt, src/main/kotlin/com/goldwrestling/member/dto/UpdateMemberStatusRequest.kt, src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (Task 1 결과)
    - src/main/kotlin/com/goldwrestling/auth/TokenService.kt (revokeAllForMember)
    - src/main/kotlin/com/goldwrestling/member/MemberStateGate.kt (02-08 결과 — 상태 게이트가 어떻게 막는지)
    - src/main/kotlin/com/goldwrestling/auth/AuthenticationPrincipalResolver.kt (02-05 결과 — 매 요청 DB 재조회)
    - docs/policies.md §5, §5.2
    - docs/decisions.md D-033, D-034, D-044
  </read_first>
  <behavior>
    PATCH /api/admin/members/{memberId}/status:
    - ACTIVE → ON_LEAVE 로 바꾸면 200 이고 status가 ON_LEAVE 다
    - ACTIVE → ON_LEAVE 전환 시 그 회원의 refresh 토큰이 전부 폐기된다 (D-044)
    - ACTIVE → INACTIVE 전환 시에도 refresh가 폐기된다
    - ON_LEAVE → ACTIVE 전환 시에는 refresh가 폐기되지 않는다
    - INACTIVE → PENDING 전환(재신청 처리) 시 rejectionReason이 null로 초기화된다 (D-044)
    - INACTIVE → PENDING 전환 시 refresh가 폐기된다 (전환 후 상태가 ACTIVE가 아니다)
    - 같은 상태로 바꾸는 요청은 200 이고 부작용이 없다 (멱등)
    - status 값이 enum에 없는 문자열이면 400 + code=MALFORMED_REQUEST
    - status 필드가 없으면 400 + code=VALIDATION_FAILED
    - 존재하지 않는 memberId면 404 + code=MEMBER_NOT_FOUND
    - 회원 토큰으로 호출하면 403
    - 상태를 ON_LEAVE로 바꾼 뒤, 그 회원의 **기존 access 토큰**으로 상태 게이트가 걸린 경로를 호출하면
      403 + code=MEMBER_NOT_ACTIVE 다 (access 잔여 수명 창이 DB 상태 재조회로 막힌다 — D-033 노트)
  </behavior>
  <action>
**dto/UpdateMemberStatusRequest.kt** — `data class UpdateMemberStatusRequest(@field:NotNull val status: MemberStatus)`.
`@Schema(description = "변경할 회원 상태. PENDING으로 되돌리면 거절 사유가 초기화되어 재신청 상태가 된다 (D-034)")`.
MEMBER-03 요구사항 문구는 `ACTIVE`/`ON_LEAVE`/`INACTIVE` 3종이지만 **PENDING도 허용한다** —
D-034의 재신청 처리(관리자가 상태를 PENDING으로 되돌린다)가 이 API로만 가능하기 때문이다.
이 확장 근거를 `@Schema` 설명과 서비스 KDoc 양쪽에 남긴다.

**AdminMemberService.kt** — 메서드 추가.
- `@Transactional fun changeStatus(memberId: Long, newStatus: MemberStatus): MemberDetailResponse`
  1. 회원 조회, 없으면 `MemberNotFoundException`
  2. `member.status = newStatus`
  3. `newStatus == MemberStatus.PENDING`이면 `member.rejectionReason = null` (D-044 재신청 처리)
  4. `if (newStatus != MemberStatus.ACTIVE) tokenService.revokeAllForMember(memberId)` (D-044 강제 로그아웃)
     — 조건을 "전환 후 상태가 ACTIVE가 아니면"으로 쓴다. "ACTIVE에서 벗어날 때"가 아니다:
     PENDING→INACTIVE처럼 원래도 ACTIVE가 아니었던 전이에서도 세션을 끊는 것이 안전하고 규칙이 단순하다.
     이 판단 근거를 주석으로 남긴다
  5. `MemberDetailResponse.from(member)` 반환
  상태 전이 자체에 제약을 두지 않는다(어느 상태에서든 4종 중 하나로 바꿀 수 있다) —
  policies §5.2가 "승인 취소는 별도 기능 없이 상태 변경으로 갈음한다"고 했으므로 관리자 재량을 막지 않는다.
  이 판단을 KDoc에 남긴다.

**AdminMemberController.kt** — 추가.
- `@PatchMapping("/{memberId}/status")` `@Operation(summary = "회원 상태 변경 (ACTIVE가 아니게 되면 해당 회원 강제 로그아웃)")`
  `fun changeStatus(@PathVariable memberId: Long, @RequestBody @Valid request: UpdateMemberStatusRequest): MemberDetailResponse`

**MemberStatusChangeTest.kt** — `@SpringBootTest @AutoConfigureMockMvc` +
`@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`.
위 behavior 전부를 검증한다.
- refresh 폐기 검증은 상태 변경 전에 토큰 쌍을 발급해 두고, 변경 후 그 refresh로 `/api/auth/refresh`를 호출한다
- **"기존 access 토큰이 상태 게이트에 막힌다"** 검증은 이 phase에서 `requireActive`를 실제로 부르는
  엔드포인트가 아직 없으므로, `MemberStateGate.requireActive(principal)`를 호출하는
  **테스트 전용 컨트롤러**를 `@TestConfiguration`으로 등록해(`/api/members/__gated`) 검증한다
  (02-05의 `SecurityFilterChainTest`가 쓴 패턴과 동일). 상태 변경 전에는 200, 변경 후 같은 access 토큰으로는
  403 + `MEMBER_NOT_ACTIVE`가 나와야 한다. 이 테스트가 D-033 노트("상태 게이트는 DB 현재 상태 기준")의
  최종 회귀 방어선이므로 생략하지 않는다
- 멱등 검증은 같은 상태로 두 번 PATCH 한 뒤 `member` 행과 refresh 행 수를 확인한다
  </action>
  <verify>
    <automated>./gradlew test --tests "*MemberStatusChangeTest*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*MemberStatusChangeTest*"` exits 0
    - `PATCH /api/admin/members/{id}/status` 가 200 + `$.status` 를 요청 값으로 반환한다
    - ACTIVE → ON_LEAVE 후 기존 refresh로 `/api/auth/refresh` 호출이 401 이다
    - ON_LEAVE → ACTIVE 후 기존 refresh로 `/api/auth/refresh` 호출이 200 이다
    - INACTIVE → PENDING 후 상세 응답의 `rejectionReason` 이 null 이다
    - 알 수 없는 status 문자열이 400 + `$.code == "MALFORMED_REQUEST"` 다
    - 상태 변경 후 기존 access 토큰으로 게이트 걸린 테스트 경로 호출이 403 + `$.code == "MEMBER_NOT_ACTIVE"` 다
    - `grep -c 'newStatus != MemberStatus.ACTIVE' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 1
    - `grep -c 'rejectionReason = null' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 2 (승인 + PENDING 복귀)
    - 회원 토큰으로 호출 시 403 이다
  </acceptance_criteria>
  <done>관리자가 회원 상태를 통제하고, 그 변경이 refresh 폐기와 상태 게이트로 즉시 세션에 반영된다.</done>
</task>

<task type="auto">
  <name>Task 3: openapi.yaml 재생성 및 확인</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md §6
    - docs/api/openapi.yaml (02-09가 갱신한 현재 상태)
  </read_first>
  <action>
`docker compose up -d` 후 `./gradlew generateApiDocs` 실행.
`git diff docs/api/openapi.yaml`으로 확인한다:
- `/api/admin/members/{memberId}/approval`(POST), `/api/admin/members/{memberId}/rejection`(POST),
  `/api/admin/members/{memberId}/status`(PATCH) 세 경로 추가
- `RejectMemberRequest`·`UpdateMemberStatusRequest` 스키마 생성, `status`가 4종 enum으로 노출
- 기존 경로 전부 유지, `servers:`가 `/`로 유지
  </action>
  <verify>
    <automated>grep -c 'approval\|rejection' docs/api/openapi.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/api/admin/members/{memberId}/approval' docs/api/openapi.yaml` 가 1 이상
    - `grep -c '/api/admin/members/{memberId}/rejection' docs/api/openapi.yaml` 가 1 이상
    - `grep -c '/api/admin/members/{memberId}/status' docs/api/openapi.yaml` 가 1 이상
    - `grep -c 'UpdateMemberStatusRequest' docs/api/openapi.yaml` 가 1 이상
    - openapi.yaml의 `UpdateMemberStatusRequest` 스키마에 PENDING·ACTIVE·ON_LEAVE·INACTIVE 4종이 모두 나온다
    - `grep -c '/api/members/me' docs/api/openapi.yaml` 가 2 이상 (기존 경로 유지)
    - `grep -c '8099\|localhost' docs/api/openapi.yaml` 가 0
    - `./gradlew ktlintFormat && ./gradlew build` exits 0
  </acceptance_criteria>
  <done>FE 관리자 화면이 승인·거절·상태 변경을 계약만 보고 만들 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 API → 회원 생애주기 | 상태를 바꿀 수 있는 유일한 경로. 잘못 열리면 권한 상승 경로가 된다 |
| 상태 변경 → 기존 세션 | 상태가 바뀐 뒤에도 살아 있는 토큰이 만드는 창 |
| 목록 필터 정책 → 서버 검증 | 화면 필터에만 있고 서버에 없는 규칙은 API 직접 호출로 우회된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-37 | Elevation of Privilege | 온보딩 미완료 회원의 승인 우회 | mitigate | 승인 서비스가 `member.isOnboardingCompleted()`를 직접 검사해 409로 거부한다 (Task 1). 목록 필터(02-09)만 믿으면 API를 직접 호출해 우회할 수 있다 — 정책은 서버에서 강제한다 (policies §5.1) |
| T-02-38 | Elevation of Privilege | 상태가 강등된 회원의 기존 세션 유지 | mitigate | 두 겹으로 막는다 — (1) refresh 전량 폐기로 갱신 경로를 끊고(D-044, Task 1·2), (2) 02-05 필터의 DB 상태 재조회 + `MemberStateGate`가 남은 access 토큰을 즉시 403으로 막는다(D-033 노트). 두 번째 방어선을 테스트 전용 게이트 엔드포인트로 실증한다 (Task 2) |
| T-02-39 | Repudiation | 거절 이력 소실 | mitigate | 거절을 데이터 삭제가 아니라 `INACTIVE` 전환 + `rejection_reason` 기록으로 처리한다 (D-034, Task 1). 재로그인 시 신규 가입으로 오인되지 않고, `INACTIVE`의 다른 유래(탈퇴·장기 미이용)와 구분된다 |
| T-02-40 | Tampering | 상태 변경과 토큰 폐기의 부분 실패 | mitigate | `TokenService.revokeAllForMember`가 기본 전파(REQUIRED)로 같은 트랜잭션에 참여해, 상태 변경과 토큰 폐기가 함께 커밋되거나 함께 롤백된다 (Task 1·2). "상태는 바뀌었는데 토큰은 살아 있는" 중간 상태가 생기지 않는다 |
</threat_model>

<verification>
1. `./gradlew ktlintFormat` → `./gradlew build` 통과 (전체 스위트)
2. `./gradlew test --tests "com.goldwrestling.member.*" --tests "com.goldwrestling.auth.*"` 통과
3. `git diff docs/api/openapi.yaml` 에 관리자 회원 관리 경로 3개만 추가되었다
4. ROADMAP Phase 2 성공기준 3번·4번이 이 플랜의 테스트로 커버되는지 확인한다
</verification>

<success_criteria>
- 승인이 온보딩 완료 PENDING 회원에만 허용된다 (MEMBER-01, policies §5.1)
- 거절이 INACTIVE + 사유 기록 + 강제 로그아웃으로 동작하고, 재로그인 시 거절 안내 대상으로 식별된다 (D-034)
- 상태 변경 4종이 동작하고, ACTIVE가 아니게 되는 전이가 refresh를 전량 폐기한다 (MEMBER-03, D-044)
- PENDING 복귀 시 거절 사유가 초기화된다
- 기존 access 토큰이 상태 게이트에 즉시 막힌다 (D-033 노트 — 테스트로 증명)
- `docs/api/openapi.yaml` 갱신
- `./gradlew build` 통과
</success_criteria>

<output>
Create `.planning/phases/02-auth-member/02-10-SUMMARY.md` when done.
완료 보고에 CLAUDE.md 학습 모드의 `이번에 쓴 기술` 섹션을 포함한다 (후보: 트랜잭션 전파(REQUIRED)로 두 서비스의 변경이 원자적으로 묶이는 원리, 무상태 JWT에서 "강제 로그아웃"이 왜 두 겹의 방어를 필요로 하는지, 화면 필터와 서버 검증을 둘 다 둬야 하는 이유, 상태 기계를 4종으로 유지해 얻는 코드 단순성). **커밋하지 않은 채** 변경 파일 목록을 제시한다.
</output>
