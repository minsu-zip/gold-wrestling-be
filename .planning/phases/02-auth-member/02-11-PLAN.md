---
phase: 02-auth-member
plan: 11
type: execute
wave: 9
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08", "02-09", "02-10"]
files_modified:
  - docs/conventions.md
  - docs/error-codes.md
  - docs/api/openapi.yaml
  - .planning/phases/02-auth-member/02-VALIDATION.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, MEMBER-01, MEMBER-02, MEMBER-03, MEMBER-04]
user_setup:
  - service: kakao
    why: "이 플랜의 수동 검증 체크포인트는 실제 카카오 계정으로 로그인해 보는 단계다. 자동화 테스트는 전부 목킹이라, 콘솔 설정(Redirect URI·Client Secret)이 실제로 맞는지는 이 단계에서만 드러난다"
    env_vars:
      - name: KAKAO_REST_API_KEY
        source: "카카오 개발자 콘솔 → 앱 키 → REST API 키"
      - name: KAKAO_CLIENT_SECRET
        source: "카카오 개발자 콘솔 → 카카오 로그인 → 보안 → Client Secret"
      - name: KAKAO_REDIRECT_URI
        source: "카카오 개발자 콘솔에 등록한 Redirect URI 와 정확히 동일한 값"
    dashboard_config:
      - task: "카카오 로그인 ON + Redirect URI 등록 + Client Secret 사용 ON"
        location: "카카오 개발자 콘솔 → 제품 설정 → 카카오 로그인"

must_haves:
  truths:
    - "Phase 2 전체 테스트 스위트가 green 이다"
    - "docs/api/openapi.yaml 이 실제 API와 일치한다 (재생성 후 diff 없음)"
    - "ErrorCode enum과 docs/error-codes.md 표가 1:1로 일치한다"
    - "실제 카카오 계정으로 로그인이 성공하고, 온보딩 → 승인 → 정상 이용까지 사람이 확인했다"
    - "02-VALIDATION.md의 Per-Task Verification Map이 실제 테스트 파일·명령으로 채워져 있다"
  artifacts:
    - path: ".planning/phases/02-auth-member/02-VALIDATION.md"
      provides: "Phase 2 검증 계약의 최종 상태 (TBD 제거, nyquist_compliant: true)"
    - path: "docs/conventions.md"
      provides: "이번 phase에서 새로 확인한 Boot 4 차이 반영 (§11 표)"
  key_links:
    - from: ".planning/phases/02-auth-member/02-VALIDATION.md"
      to: "실제 테스트 파일"
      via: "요구사항 ID → 테스트 명령 매핑"
      pattern: "gradlew test --tests"
---

<objective>
Phase 2를 닫는다. 새 기능을 만들지 않고 **정합성 확인과 사람 검증**만 한다.

이 phase의 자동화 테스트는 카카오 API를 전부 목킹했다. 목킹은 "우리 코드가 카카오 응답을 잘 다루는가"는
증명하지만 "카카오 콘솔 설정이 맞는가"는 증명하지 못한다 — `redirect_uri` 불일치, Client Secret 미사용
설정 같은 문제는 실제 로그인 한 번으로만 드러난다. 그래서 마지막에 사람 검증 체크포인트를 둔다.

Purpose: 계약 파일·문서·테스트 계약이 코드와 어긋난 채로 Phase 3에 넘어가지 않게 한다.
Output: 전체 검증 결과 + 문서 정합 + 수동 E2E 확인 + VALIDATION.md 확정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<commit_policy>
이 레포는 **사용자가 명시적으로 요청할 때만 커밋한다** (CLAUDE.md 커밋·브랜치 규칙).
execute-plan 워크플로가 자동 커밋을 요구하더라도 `git add`/`git commit`/`git push`를 실행하지 않는다.
파일 저장까지만 하고, 완료 보고에 "커밋 대기 중인 변경 파일 목록"을 나열한 뒤 멈춘다.
</commit_policy>

<context>
@CLAUDE.md
@docs/conventions.md
@docs/error-codes.md
@docs/glossary.md
@docs/decisions.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-member/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 검증 + 문서 정합 점검</name>
  <files>docs/conventions.md, docs/error-codes.md, docs/api/openapi.yaml</files>
  <read_first>
    - docs/conventions.md §11 (Boot 3 → 4 차이 표 — "새로 발견한 차이는 이 표에 추가한다"고 적혀 있다)
    - docs/error-codes.md (02-01이 추가한 인증·회원 코드 표)
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
    - docs/glossary.md (02-01이 추가한 인증·회원 섹션 — 코드에 실제로 쓰인 이름과 대조한다)
    - .planning/phases/02-auth-member/02-01-SUMMARY.md ~ 02-10-SUMMARY.md (각 플랜이 남긴 결정·발견 사항)
    - .claude/skills/verify-boot4-api/SKILL.md (마지막 문장: "새로 발견한 차이는 이 표와 conventions.md §11에 추가한다")
  </read_first>
  <action>
**1) 전체 스위트 실행**
`docker compose up -d` 후 `./gradlew ktlintFormat` → `./gradlew build`를 돌린다.
실패가 있으면 원인을 고친다. 플랜 단위 실행 중에는 통과했지만 전체에서 깨지는 경우
(컨텍스트 캐시 충돌, 테스트 간 데이터 오염, 시드된 관리자와의 충돌)가 실제로 있으므로 이 단계를 생략하지 않는다.

**2) openapi.yaml 드리프트 확인**
`./gradlew generateApiDocs`를 실행하고 `git diff docs/api/openapi.yaml`을 본다.
**diff가 비어 있어야 한다** — 02-10까지 각 플랜이 재생성했으므로 이미 최신이어야 한다.
diff가 있으면 어느 플랜이 재생성을 빠뜨렸다는 뜻이다. 그 사실을 완료 보고에 적고 갱신된 파일을 남긴다.
확인 항목: 이 phase에서 만든 11개 경로가 전부 있는지 —
`/api/auth/kakao/login`, `/api/auth/admin/login`, `/api/auth/refresh`, `/api/auth/logout`,
`/api/members/me`(GET), `/api/members/me/onboarding`(POST), `/api/admin/members`(GET),
`/api/admin/members/{memberId}`(GET), `/api/admin/members/{memberId}/approval`(POST),
`/api/admin/members/{memberId}/rejection`(POST), `/api/admin/members/{memberId}/status`(PATCH).
`servers:`가 `/`인지, 테스트 전용 경로(`__test-secured`, `__gated`, `__probe`)가 **새어 나오지 않았는지** 확인한다 —
새어 나왔다면 해당 컨트롤러가 `@TestConfiguration`이 아니라 프로덕션 코드에 있다는 뜻이므로 반드시 고친다.

**3) 에러코드 ↔ 문서 정합**
`./gradlew test --tests "*ErrorCodeRegistry*"`가 통과하는지 확인한다(02-01이 만든 자동 검증).
추가로 이 phase에서 실제로 던져지는 코드가 전부 문서에 있는지 눈으로 대조한다:
`grep -rho 'ErrorCode\.[A-Z_]*' src/main/kotlin | sort -u` 결과의 각 코드가 `docs/error-codes.md`에 있는지.

**4) glossary ↔ 코드 네이밍 대조**
`docs/glossary.md`의 인증·회원 섹션에 등재한 이름이 실제 코드에 그대로 쓰였는지 확인한다.
코드에만 있고 glossary에 없는 새 개념(예: 구현 중 새로 만든 클래스 이름)이 있으면 glossary에 추가한다.
금지어(`Ticket`/`Voucher`/`Coupon`/`Booking`/`Course`)가 이 phase 코드에 쓰이지 않았는지 확인한다.

**5) conventions.md §11 갱신**
이 phase에서 verify-boot4-api 절차로 새로 확인한 Boot 3 → 4 차이가 있으면 §11 표에 행을 추가한다.
후보(각 플랜 SUMMARY에서 확인): Jackson 3 `tools.jackson` 임포트 경로, `RestClient.Builder` 타임아웃 설정 API,
`Specification` 조합 API 변경, `NimbusJwtEncoder`/`ImmutableSecret` 패키지, 목 빈 애노테이션(`@MockitoBean` 등).
**실제로 확인한 것만 적는다** — 추측으로 표를 채우지 않는다. 새로 확인한 것이 없으면 이 단계는 건너뛰고
그 사실을 완료 보고에 적는다.
  </action>
  <verify>
    <automated>./gradlew ktlintFormat && ./gradlew build && ./gradlew test --tests "*ErrorCodeRegistry*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew ktlintFormat && ./gradlew build` exits 0
    - `./gradlew generateApiDocs` 실행 후 `git diff --stat docs/api/openapi.yaml` 가 빈 출력이거나, 변경이 있으면 그 원인이 완료 보고에 기록되어 있다
    - `grep -c '__test-secured\|__gated\|__probe' docs/api/openapi.yaml` 가 0
    - `docs/api/openapi.yaml` 의 `servers:` 블록 url 이 `/` 다
    - 이 phase의 11개 경로가 모두 openapi.yaml에 있다 (위 action의 목록으로 확인)
    - `grep -rho 'ErrorCode\.[A-Z_]*' src/main/kotlin | sort -u` 의 각 코드가 `docs/error-codes.md` 에 존재한다
    - `grep -rn 'Ticket\|Voucher\|Coupon\|Booking\|Course' src/main/kotlin/com/goldwrestling/` 가 아무 출력도 내지 않는다
    - `./gradlew test` 전체가 통과한다 (실패 0)
  </acceptance_criteria>
  <done>코드·계약·문서가 서로 어긋나지 않은 상태로 phase를 닫을 준비가 되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 02-VALIDATION.md 확정 (TBD 제거)</name>
  <files>.planning/phases/02-auth-member/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-auth-member/02-VALIDATION.md (현재 TBD 상태의 Per-Task Verification Map, Wave 0 요구사항 체크박스, Sign-Off 체크박스)
    - .planning/phases/02-auth-member/02-01-PLAN.md ~ 02-10-PLAN.md (각 플랜의 threat ID와 테스트 파일명)
    - src/test/kotlin/com/goldwrestling/ (실제로 만들어진 테스트 파일 목록)
  </read_first>
  <action>
`02-VALIDATION.md`를 실제 결과로 채운다.

**Per-Task Verification Map** — `TBD` 자리를 실제 값으로 바꾼다:
- `Task ID`: `02-04 T1` 처럼 `{플랜번호} T{태스크번호}` 형식
- `Plan`: `02-04` 형식
- `Wave`: 해당 플랜의 wave 값
- `Threat Ref`: 그 태스크를 커버하는 플랜의 threat ID (예: `T-02-10`, `T-02-14`)
- `File Exists`: `❌ W0` → 실제 테스트 파일 경로
- `Status`: 실행 결과에 따라 `✅ green`

요구사항별 매핑(플랜에서 확정된 실제 파일):
- AUTH-01 → `KakaoAuthControllerTest` (02-06 T2)
- AUTH-02 → `TokenControllerTest`, `RefreshTokenRotationTest` (02-04 T1·T2)
- AUTH-03 → `AdminAuthControllerTest`, `AdminSeederTest` (02-07 T1·T2)
- AUTH-04 → `SecurityFilterChainTest`, `MemberStateGateTest`, `MemberStatusChangeTest` (02-05 T3, 02-08 T1, 02-10 T2)
- AUTH-05 → `OnboardingValidationTest`, `MemberProfileTest` (02-08 T1·T2)
- AUTH-06 → `MemberOnboardingStatusTest`, `MemberProfileTest` (02-02 T3, 02-08 T2)
- MEMBER-01 → `MemberSpecificationTest`, `MemberApprovalTest` (02-09 T1, 02-10 T1)
- MEMBER-02 → `MemberSearchTest` (02-09 T2)
- MEMBER-03 → `MemberStatusChangeTest` (02-10 T2)
- MEMBER-04 → `MemberProfileTest` (02-08 T2)
- (D-033 회전) → `RefreshTokenRotationTest` (02-04 T1)
- (D-017 회귀) → `JwtAuthenticationFilterTest` (02-05 T3)
실제 파일명이 위와 다르면 **실제 파일명을 적는다** — 계획이 아니라 결과를 기록하는 문서다.

**Wave 0 Requirements** 4개 체크박스를 실제 파일 경로와 함께 체크한다
(`src/test/kotlin/com/goldwrestling/support/`, `src/test/resources/kakao/`, `MutableTestClock`/`TestClockConfiguration`, `KakaoApiMockSupport`).

**Manual-Only Verifications** 표의 "실제 카카오 개발자 콘솔 연동" 행에 Task 3 체크포인트 결과를 기록한다
(수행 일시와 결과 한 줄).

**Validation Sign-Off** 체크박스를 실제 상태로 채우고, frontmatter의 `nyquist_compliant`를 `true`,
`wave_0_complete`를 `true`, `status`를 `complete`로 바꾼다. **실제로 충족된 항목만 체크한다** —
충족되지 않은 항목이 있으면 체크하지 말고 그 이유를 문서에 한 줄 남긴다.
  </action>
  <verify>
    <automated>grep -c 'TBD' .planning/phases/02-auth-member/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/02-auth-member/02-VALIDATION.md` 가 0
    - `grep -c '❌ W0' .planning/phases/02-auth-member/02-VALIDATION.md` 가 0
    - `grep -c 'nyquist_compliant: true' .planning/phases/02-auth-member/02-VALIDATION.md` 가 1
    - `grep -c 'wave_0_complete: true' .planning/phases/02-auth-member/02-VALIDATION.md` 가 1
    - Per-Task Verification Map의 모든 행에 실제 테스트 파일 경로가 적혀 있고, 그 경로가 실제로 존재한다
    - 각 행의 `Automated Command` 를 실행하면 통과한다 (표본으로 3개 이상 확인)
  </acceptance_criteria>
  <done>Phase 2의 검증 계약이 실제 결과를 담은 문서가 되었다 — Phase 3 이후가 이 매핑을 신뢰할 수 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실제 카카오 로그인 + 승인 플로우 수동 검증</name>
  <files>(코드 변경 없음 — 검증 결과는 Task 2에서 .planning/phases/02-auth-member/02-VALIDATION.md 에 기록한다)</files>
  <read_first>
    - .env.example (사용자가 .env에 채워야 할 키 목록)
    - .gitignore (.env 가 제외되어 있는지 확인)
    - docs/api/openapi.yaml (검증에 쓸 실제 경로·요청 본문 형태)
  </read_first>
  <action>
코드를 수정하지 않는다. 아래 `<how-to-verify>` 절차를 사용자에게 그대로 제시하고 **응답을 기다린다**.

먼저 Claude가 준비를 마친다: `docker compose up -d`로 DB를 띄우고, `.gitignore`에 `.env`가 포함되어
있는지 확인하고, `.env`에서 아직 비어 있는 키(`KAKAO_REST_API_KEY`, `KAKAO_CLIENT_SECRET`,
`KAKAO_REDIRECT_URI`, `JWT_SECRET`, `ADMIN_SEED_LOGIN_ID`, `ADMIN_SEED_PASSWORD`)가 무엇인지
**키 이름만** 사용자에게 알려준다 (값을 읽어서 출력하지 않는다).
그 다음 `./gradlew bootRun`으로 앱이 뜬 상태를 만들고 절차를 안내한 뒤 멈춘다.

사용자가 "approved"라고 답하면 결과를 Task 2의 `02-VALIDATION.md` Manual-Only 표에 기록한다.
어긋난 단계를 보고받으면 그 단계의 상태코드·`code` 값을 근거로 원인을 찾아 고친 뒤 다시 확인을 요청한다.
"카카오 보류"라고 답하면 6~7번을 건너뛴 채 8~12번만 확인하고, 카카오 E2E를 **미확인**으로
`02-VALIDATION.md`에 명시한다 — 확인하지 않은 것을 확인했다고 적지 않는다.
  </action>
  <what-built>
Phase 2의 전 기능이 구현되고 자동화 테스트가 통과한 상태다. 다만 카카오 API는 테스트에서 전부 목킹되어 있어,
**카카오 개발자 콘솔 설정이 실제로 맞는지는 아직 아무도 확인하지 않았다.**

Claude가 자동화한 것: 회원 가입·온보딩·승인·거절·상태 변경·토큰 갱신·강제 로그아웃 전 경로의 통합테스트,
카카오 응답 처리(성공/4xx/5xx)의 목킹 테스트, openapi.yaml 재생성.

Claude가 자동화할 수 없는 것: 실제 카카오 계정으로의 로그인. `redirect_uri` 불일치, Client Secret 미사용
설정, 앱 키 오타 같은 문제는 실제 호출 한 번으로만 드러난다.
  </what-built>
  <how-to-verify>
**사전 준비**
1. 카카오 개발자 콘솔(https://developers.kakao.com)에서 앱을 만들고 다음을 확인한다:
   - 제품 설정 → 카카오 로그인 → **활성화 ON**
   - 제품 설정 → 카카오 로그인 → **Redirect URI 등록** (예: `http://localhost:5180/oauth/kakao/callback`)
   - 제품 설정 → 카카오 로그인 → 보안 → **Client Secret 발급 + 사용 ON**
2. 로컬 `.env`에 실제 값을 채운다 (`KAKAO_REST_API_KEY`, `KAKAO_CLIENT_SECRET`, `KAKAO_REDIRECT_URI`,
   `JWT_SECRET`, `ADMIN_SEED_LOGIN_ID`, `ADMIN_SEED_PASSWORD`).
   **`.env`는 커밋 대상이 아니다 — 실값이 저장소에 들어가지 않았는지 `git status`로 확인한다.**
3. `docker compose up -d && ./gradlew bootRun`

**검증 절차**
4. 브라우저 주소창에 아래를 넣어 카카오 로그인 화면을 띄운다 (값은 본인 것으로 치환):
   `https://kauth.kakao.com/oauth/authorize?client_id=<REST_API_KEY>&redirect_uri=<REDIRECT_URI>&response_type=code`
5. 로그인·동의 후 리다이렉트된 주소의 `code=` 뒤 값을 복사한다 (FE가 아직 없으므로 수동으로 가져온다)
6. `curl -X POST http://localhost:8080/api/auth/kakao/login -H 'Content-Type: application/json' -d '{"code":"<복사한 코드>"}'`
   → **기대**: 200, `member.status`가 `PENDING`, `member.onboardingCompleted`가 `false`,
   `tokens.accessToken`·`tokens.refreshToken`이 들어 있다
   ※ 인가 코드는 1회용이라 재사용하면 실패한다. 다시 하려면 4번부터 반복한다
7. 받은 accessToken으로 온보딩 제출:
   `curl -X POST http://localhost:8080/api/members/me/onboarding -H 'Authorization: Bearer <accessToken>' -H 'Content-Type: application/json' -d '{"name":"홍길동","phoneNumber":"010-1234-5678"}'`
   → **기대**: 200, `onboardingCompleted`가 `true`, `phoneNumber`가 `01012345678`(하이픈 제거)
8. 관리자로 로그인:
   `curl -X POST http://localhost:8080/api/auth/admin/login -H 'Content-Type: application/json' -d '{"loginId":"<ADMIN_SEED_LOGIN_ID>","password":"<ADMIN_SEED_PASSWORD>"}'`
   → **기대**: 200, 토큰 쌍 반환
9. 승인 대기 목록 조회:
   `curl 'http://localhost:8080/api/admin/members?status=PENDING&onboardingCompleted=true' -H 'Authorization: Bearer <관리자 accessToken>'`
   → **기대**: 6번에서 만든 회원이 목록에 보인다
10. 승인:
    `curl -X POST http://localhost:8080/api/admin/members/<memberId>/approval -H 'Authorization: Bearer <관리자 accessToken>'`
    → **기대**: 200, `status`가 `ACTIVE`
11. 회원 프로필 재조회:
    `curl http://localhost:8080/api/members/me -H 'Authorization: Bearer <회원 accessToken>'`
    → **기대**: `status`가 `ACTIVE`, `rejected`가 `false`
12. 강제 로그아웃 확인 — 관리자가 상태를 `ON_LEAVE`로 변경:
    `curl -X PATCH http://localhost:8080/api/admin/members/<memberId>/status -H 'Authorization: Bearer <관리자 accessToken>' -H 'Content-Type: application/json' -d '{"status":"ON_LEAVE"}'`
    그 다음 회원의 refreshToken으로 갱신 시도:
    `curl -X POST http://localhost:8080/api/auth/refresh -H 'Content-Type: application/json' -d '{"refreshToken":"<회원 refreshToken>"}'`
    → **기대**: 401, `Content-Type: application/problem+json`, `code`가 `REFRESH_TOKEN_INVALID`

**실패 시 흔한 원인**
- 6번이 401 `KAKAO_AUTH_FAILED`: `.env`의 `KAKAO_REDIRECT_URI`가 4번에서 쓴 값과 다르거나, 콘솔 등록값과 다르다
- 6번이 502 `KAKAO_UNAVAILABLE`: 네트워크 또는 카카오 장애. 잠시 후 재시도
- 8번이 401: 관리자 시드가 안 돌았다. 기동 로그에서 "관리자 시드" 관련 줄을 확인한다
  </how-to-verify>
  <verify>
    <human-check>위 4~12번 절차를 사용자가 수행하고 각 단계의 기대 결과와 일치하는지 확인한다</human-check>
  </verify>
  <acceptance_criteria>
    - 6번 요청이 200이고 응답의 `member.status` 가 `PENDING` 이다
    - 7번 요청 후 DB `member.phone_number` 값에 `-` 문자가 없다
    - 9번 목록에 해당 회원이 포함된다
    - 10번 후 `status` 가 `ACTIVE` 다
    - 12번 갱신 시도가 401 + `application/problem+json` + `REFRESH_TOKEN_INVALID` 다
    - `git status` 에 `.env` 가 스테이징/추적 대상으로 올라와 있지 않다
  </acceptance_criteria>
  <done>실제 카카오 연동과 승인·강제 로그아웃 플로우가 사람 눈으로 확인되었다 (또는 보류 사유가 기록되었다).</done>
  <resume-signal>
전부 기대대로면 "approved" 라고 알려주세요.
어긋난 단계가 있으면 **몇 번 단계에서 무엇이 나왔는지**(상태코드 + 응답 본문의 `code` 값)를 알려주세요 —
그 지점부터 원인을 찾겠습니다.
카카오 콘솔 설정을 아직 못 했다면 "카카오 보류"라고 알려주세요 — 6~7번을 건너뛰고 8~12번(관리자·상태 변경)만
확인한 뒤, 카카오 E2E는 미확인으로 02-VALIDATION.md에 남기겠습니다.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 목킹된 테스트 → 실제 외부 시스템 | 자동화 테스트가 증명하지 못하는 영역 |
| 로컬 `.env` → 저장소 | 실값이 커밋될 수 있는 마지막 지점 |
| 테스트 전용 코드 → 프로덕션 계약 | 테스트용 엔드포인트가 실제 API 표면에 새어 나갈 수 있는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-41 | Spoofing | 카카오 콘솔 설정 오류로 인증이 실제로는 동작하지 않음 | mitigate | 사람이 실제 카카오 계정으로 전체 플로우를 1회 수행한다 (Task 3). 목킹 테스트가 덮지 못하는 유일한 영역이다 |
| T-02-42 | Information Disclosure | `.env` 실값 커밋 | mitigate | 수동 검증 절차에 `git status`로 `.env` 미추적을 확인하는 단계를 넣는다 (Task 3 acceptance criteria). `.gitignore`에 `.env`가 있는지도 함께 확인한다 |
| T-02-43 | Elevation of Privilege | 테스트 전용 엔드포인트가 프로덕션에 노출 | mitigate | `openapi.yaml`에 `__test-secured`·`__gated`·`__probe` 문자열이 없음을 검증한다 (Task 1). 새어 나왔다면 해당 컨트롤러가 `@TestConfiguration` 밖에 있다는 뜻이므로 즉시 고친다 |
</threat_model>

<verification>
1. `./gradlew ktlintFormat && ./gradlew build` 전체 통과
2. `./gradlew generateApiDocs` 후 openapi.yaml diff 없음
3. `02-VALIDATION.md`에 TBD·❌ 가 남아 있지 않음
4. Task 3 체크포인트에서 사용자가 "approved" 또는 "카카오 보류" 응답
</verification>

<success_criteria>
- Phase 2 전체 테스트가 green 이다
- openapi.yaml이 실제 API와 일치한다 (재생성 diff 없음, 테스트 경로 미노출)
- ErrorCode·glossary·conventions 문서가 코드와 일치한다
- 02-VALIDATION.md가 실제 결과로 확정되었다
- 실제 카카오 로그인 → 온보딩 → 승인 → 강제 로그아웃 플로우를 사람이 확인했다 (또는 보류 사유가 기록되었다)
- ROADMAP Phase 2 성공 기준 5항목이 전부 충족되었음을 근거(테스트 파일·수동 확인)와 함께 정리했다
</success_criteria>

<output>
Create `.planning/phases/02-auth-member/02-11-SUMMARY.md` when done.
SUMMARY에는 다음을 포함한다:
- ROADMAP Phase 2 성공 기준 5항목 각각에 대한 충족 근거 (테스트 파일 또는 수동 검증 단계 번호)
- AUTH-01~06, MEMBER-01~04 요구사항별 구현 위치
- Phase 3(이용권)이 전제로 삼을 수 있는 것 — `MemberStateGate`, `AuthenticatedPrincipal`, `PageResponse`,
  `MutableTestClock`, 인증된 회원/관리자 컨텍스트
- 남은 확인 사항이나 미해결 항목
- CLAUDE.md 학습 모드의 `이번에 쓴 기술` 섹션 (Phase 2 전체를 관통한 3~6가지)
**커밋하지 않은 채** 변경 파일 목록을 제시한다.
</output>
