---
phase: 05-batch
plan: 16
type: execute
wave: 16
gap_closure: true
depends_on: [05-15]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-batch/05-VALIDATION.md
  - .planning/phases/05-batch/05-PATTERNS.md
  - docs/decisions.md
autonomous: false
requirements: [BATCH-01, BATCH-02, BATCH-03, BATCH-04]
must_haves:
  truths:
    - "전체 테스트 스위트가 캐시 없이 실제로 실행돼 통과한다"
    - "코드·문서 어디에도 '동시 실행은 안전하지 않다' 계열 서술이 남아 있지 않다"
    - "사용자가 로컬 실기동으로 202·조회·잔여 변화를 직접 관찰했다"
    - "ROADMAP·REQUIREMENTS의 BATCH-01·02·04 상태가 갭 클로저 결과와 일치한다"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 갭 클로저 결과 반영"
      contains: "05-16-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BATCH-01·02·04 커버리지 갱신"
      contains: "BATCH-04"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/05-batch/05-10~05-16-PLAN.md"
      via: "갭 클로저 청크 목록"
      pattern: "05-1[0-6]-PLAN.md"
---

<objective>
Phase 5 갭 클로저를 마감한다 — 전체 회귀를 캐시 없이 실제로 돌리고, 코드·문서에 남은
"동시 실행은 안전하지 않다" 계열의 사실과 다른 서술을 전수 점검하고, 사용자가 로컬 실기동으로
새 동작(202 접수 → 조회 → 잔여 변화)을 직접 확인한 뒤 로드맵·요구사항 표기를 갱신한다.

Purpose: 05-VERIFICATION.md가 실패로 판정한 세 truth(BATCH-01·02·04)가 실제로 뒤집혔는지를
**주장이 아니라 실행 결과**로 확정한다. GSD 회귀 게이트는 이 Gradle 레포에서 no-op이므로
이 플랜이 그 역할을 직접 수행한다.
Output: 전체 그린 빌드, 문서 정합 점검 결과, 사용자 확인 로그, ROADMAP·REQUIREMENTS 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-batch/05-VERIFICATION.md
@.planning/phases/05-batch/05-GAP-CONTEXT.md
@.claude/skills/deliver-phase-chunk/SKILL.md

**검증 명령 규칙:** 반드시 `./gradlew cleanTest test`를 쓴다. `cleanTest` 없이 `test`만 돌리면
Gradle이 `UP-TO-DATE`로 이전 결과를 재사용해 "돌렸다"는 착각을 준다 — 이 레포에서 실제로 겪은 문제다.

**로컬 DB 보존 데이터:** member 4·5, pass 6·7, pass_transaction 38건, batch_execution 3건.
임의로 지우지 않는다(`docker compose down -v` 금지).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 회귀 실행 + 문서·코드 정합 전수 점검</name>
  <files>docs/decisions.md</files>
  <read_first>
    .planning/phases/05-batch/05-VERIFICATION.md,
    .planning/phases/05-batch/05-REVIEW.md,
    docs/decisions.md,
    docs/api/openapi.yaml,
    docs/policies.md,
    docs/glossary.md
  </read_first>
  <action>
    (a) 캐시 없이 전체 스위트를 돌린다: `./gradlew ktlintFormat` → `./gradlew cleanTest test` →
    `./gradlew build`. 실패가 있으면 원인을 고치고 다시 돌린다. **테스트를 실행하지 않고 통과한다고
    보고하지 않는다**(conventions §10.5). Docker Desktop이 떠 있어야 한다(Testcontainers).

    (b) 사실과 달라진 서술이 남았는지 전수 점검한다. 아래 검색이 모두 0건이어야 한다
    (주석 줄이 스스로 검색어를 만들어 자기 무효화하지 않도록, 검색 결과가 나오면 그 줄이 실제
    잘못된 서술인지 사람이 읽고 판단한다):
    - `grep -rn "동시 실행은 아직 안전하지 않다" src/ docs/`
    - `grep -rn "응답이 오지 않아도 재호출하지 않는다" src/ docs/`
    - `grep -rn "차감 원자성 보장은 갭 클로저에서 정한다" src/ docs/`
    - `grep -rn "ON_LEAVE.*→.*ACTIVE.*복귀일" docs/`
    남아 있으면 그 파일을 고친다. 단 **커밋된 Flyway 마이그레이션(V1~V10 중 이미 커밋된 것)은
    수정하지 않는다** — V9 주석의 정정은 이미 V10 헤더에 남겼다.

    (c) 결정 문서의 정합을 확인한다. 아래가 모두 존재해야 한다:
    D-108 해소 갱신(2026-08-16), D-111 정정, D-113 갱신, D-114 갱신(에러코드 + 202/조회),
    D-117(신규), D-118(신규), D-119(신규).
    빠진 것이 있으면 채운다. 마지막으로 `docs/decisions.md`에 이번 갭 클로저 전체를 한 항목으로
    요약하지 말 것 — 결정은 이미 개별 D 번호로 기록돼 있고 중복 기록은 다음 작업자가 어느 쪽을
    믿어야 할지 모르게 만든다. 대신 D-108의 해소 항목에 "실증 근거: `InactivityBatchRunConcurrencyTest`,
    `AdminBatchRunConcurrencyTest`" 한 줄이 있는지 확인하고 없으면 추가한다.

    (d) 요구사항 추적을 확인한다 — `.planning/phases/05-batch/05-1*-PLAN.md`의 frontmatter
    `requirements` 필드를 모아 BATCH-01·02·03·04가 모두 최소 1회 등장하는지 본다.
  </action>
  <verify>
    <automated>./gradlew ktlintFormat &amp;&amp; ./gradlew cleanTest test &amp;&amp; ./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew cleanTest test` 가 BUILD SUCCESSFUL (스킵·캐시 재사용 없이 실제 실행)
    - `./gradlew build` 가 BUILD SUCCESSFUL (ktlintCheck 포함)
    - `grep -rn "동시 실행은 아직 안전하지 않다" src/ docs/ | wc -l` 이 0
    - `grep -rn "응답이 오지 않아도 재호출하지 않는다" src/ docs/ | wc -l` 이 0
    - `grep -rn "차감 원자성 보장은 갭 클로저에서 정한다" src/ docs/ | wc -l` 이 0
    - `grep -c '^## D-117\|^## D-118\|^## D-119' docs/decisions.md` 가 3
    - `grep -h 'requirements:' .planning/phases/05-batch/05-1*-PLAN.md` 결과에 BATCH-01·02·03·04가 모두 등장한다
    - `git status --porcelain src/main/resources/db/migration/` 에 V1~V9 파일 변경이 없다
  </acceptance_criteria>
  <done>전체 테스트가 캐시 없이 통과하고, 코드·문서에 사실과 다른 안전성 서술이 남아 있지 않다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 로컬 실기동으로 새 실행 흐름을 사용자가 직접 확인한다</name>
  <what-built>
    - `RUNNING` 부분 유니크 인덱스로 실행 전체를 직렬화(중복 실행 409 거부)
    - 수동 실행이 202 접수 + 비동기 실행으로 바뀌고, 실행 상태 조회 API 2종이 열림
    - 정책 시행일 하한(기본 2026-09-01)과 1회 실행당 회원 1명 1회 상한
    - 휴회에서 벗어나는 모든 전이에서 복귀 시각 기록
  </what-built>
  <how-to-verify>
    1. `docker compose up -d` 로 로컬 Postgres를 띄운다(**`down -v` 금지** — Phase 5 검증 데이터
       member 4·5 / pass 6·7 / pass_transaction 38 / batch_execution 3건이 보존돼 있어야 한다).
    2. `./gradlew bootRun` 으로 앱을 띄운다. 기동 로그에 Flyway가 **V10**을 적용했다는 줄이 보이는지
       확인한다(이미 적용돼 있으면 건너뛴다는 로그).
    3. 관리자 토큰으로 `POST /api/admin/batch/inactivity-runs` 를 호출한다.
       기대: **HTTP 202**, 응답 본문의 `status` 가 `RUNNING`, `finishedAt` 이 `null`,
       `Location` 헤더가 `/api/admin/batch/inactivity-runs/{id}` 를 가리킨다.
    4. 곧바로 `GET` 그 `Location` 을 호출한다.
       기대: 잠시 뒤 `status` 가 `SUCCESS`(또는 `PARTIAL_FAILURE`)로 바뀌고 집계 3종이 채워진다.
    5. `GET /api/admin/batch/inactivity-runs?limit=5` 를 호출한다.
       기대: 최근 실행이 `startedAt` **내림차순**으로 오고, 기존 3건 + 이번 실행이 함께 보인다.
    6. 3번을 **연속 두 번 빠르게** 호출한다.
       기대: 두 번째가 **409** + 본문 `code` 가 `BATCH_ALREADY_RUNNING` 이고
       `Content-Type` 이 `application/problem+json` 이다.
       ※ 대상 회원이 적어 배치가 즉시 끝나면 두 번째도 202가 나올 수 있다 — 그 경우
       `deductedCount` 가 0인지만 확인하고 넘어간다(순차 재실행은 원래 안전하다). 동시 호출의
       409는 `AdminBatchRunConcurrencyTest` 가 이미 결정론적으로 증명한다.
    7. `docker compose exec -T postgres psql -U "$DB_USERNAME" -d "$DB_NAME"` 로 다음 두 가지를 본다:
       `select count(*) from batch_execution where status='RUNNING';` → **0**
       `select id, status, finished_at, deducted_count from batch_execution order by id desc limit 5;`
    8. 응답 본문 어디에도 회원 이름·전화번호·회원 id 목록이 없는지 눈으로 확인한다.
  </how-to-verify>
  <read_first>
    docs/api/openapi.yaml,
    src/main/kotlin/com/goldwrestling/batch/AdminBatchController.kt
  </read_first>
  <acceptance_criteria>
    - 사용자가 3~7번의 관찰 결과를 보고했다(각 단계의 실제 상태코드·필드 값)
    - `select count(*) from batch_execution where status='RUNNING'` 결과가 0이다
    - 보존 데이터(member 4·5, pass 6·7)가 그대로 남아 있다
  </acceptance_criteria>
  <files>(없음 — 코드 변경 없이 실행 결과만 관찰한다)</files>
  <action>
    위 `how-to-verify` 1~8을 사용자가 직접 실행하도록 안내하고, 실행에 필요한 준비
    (`docker compose up -d`, `./gradlew bootRun`, 관리자 토큰 발급 방법)를 Claude가 먼저 마련해
    제시한다. 관찰 결과를 받으면 기대와 다른 항목이 있는지 대조하고, 다르면 원인을 찾아 고친 뒤
    다시 확인을 요청한다. 사용자 승인 전에는 다음 태스크로 넘어가지 않는다.
  </action>
  <verify>
    <automated>docker compose exec -T postgres psql -U "$DB_USERNAME" -d "$DB_NAME" -tAc "select count(*) from batch_execution where status='RUNNING'"</automated>
  </verify>
  <done>사용자가 202 접수·상태 조회·목록·잔여 변화를 직접 관찰하고 승인했으며, `RUNNING` 잔존 행이 0건이다.</done>
  <resume-signal>관찰 결과를 붙여 넣고 "승인"이라고 답한다. 기대와 다른 부분이 있으면 그 항목 번호와 실제 값을 알려준다.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ROADMAP·REQUIREMENTS·VALIDATION·PATTERNS를 갭 클로저 결과로 갱신한다</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/05-batch/05-VALIDATION.md, .planning/phases/05-batch/05-PATTERNS.md</files>
  <read_first>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/05-batch/05-VALIDATION.md,
    .planning/phases/05-batch/05-PATTERNS.md,
    .planning/phases/05-batch/05-VERIFICATION.md,
    .planning/phases/05-batch/05-10-PLAN.md,
    .planning/phases/05-batch/05-15-PLAN.md
  </read_first>
  <action>
    `.planning/ROADMAP.md`의 Phase 5 절에는 계획 단계에서 이미 **청크 D**(wave 10~16, 플랜
    05-10 ~ 05-16) 목록과 "16 plans / 4개 청크" 표기가 들어가 있다. 이 태스크는 그 목록의
    체크박스를 실행 결과에 맞춰 `[x]`로 바꾸고, 각 줄의 요약이 실제로 한 일과 다르면 사실에 맞게
    고친다. 목록을 새로 만들지 말고 **갱신**한다(중복 블록을 만들지 않는다).
    기존 "**검증 결과**(05-VERIFICATION.md, 2026-08-15): gaps_found" 블록은 **지우지 않는다** —
    그 아래에 "**갭 클로저**(2026-08-16, 청크 D)" 항목을 덧붙여 (a)(b)(c)와 WR-05가 각각 어느
    플랜에서 닫혔는지, 그리고 **CR-03(출석일 후보 부재)은 범위 밖이며 운영 배포 시
    `BATCH_INACTIVITY_SCHEDULER_ENABLED=false`로 cron을 꺼 둔 채 올린다**는 사실을 적는다.
    "갭 클로저 전까지 dev→main 병합을 하지 않는다"는 문장은 이번 청크가 dev에 머지된 뒤에도
    **CR-03 때문에 cron 활성 배포는 여전히 하지 않는다**는 취지로 정확히 다시 쓴다.

    `.planning/REQUIREMENTS.md`:
    - BATCH-01·BATCH-02·BATCH-04의 체크박스를 완료(`[x]`)로 바꾼다. BATCH-03은 이미 완료다.
    - 하단 커버리지 표의 `Gaps found` 3행을 `Complete`로 바꾸되, BATCH-01 행에는
      "출석일 후보는 Phase 6에서 채운다(CR-03, 범위 밖)"를 한 줄로 덧붙인다 — 조건 없이 Complete로
      바꾸면 다음 작업자가 이 미완을 잃어버린다.

    `.planning/phases/05-batch/05-VALIDATION.md` — 이 문서의 Per-Task Verification Map은 wave 1~9
    (플랜 05-01~05-09)까지만 담고 있어 갭 클로저 태스크 21개가 빠져 있다. 플랜 체커가 지적한
    항목이다. wave 10~16 구간을 표에 추가한다. 각 행은 플랜 파일의 `<verify><automated>` 를 그대로
    옮기고, 특히 **동시성 테스트 2종**(`InactivityBatchRunConcurrencyTest`,
    `AdminBatchRunConcurrencyTest`)이 어느 truth를 실증하는지 명시한다. 문서 전체를 다시 쓰지 말고
    기존 표에 이어 붙인다.

    `.planning/phases/05-batch/05-PATTERNS.md` — File Classification 표에 갭 클로저 신규 파일이
    없다. 다음을 analog와 함께 추가한다: `BatchExecutionRecorder.kt`, `InactivityBatchProperties.kt`,
    `BatchExecutorConfig.kt`, `AdminBatchService.kt`, `BatchExceptions.kt`, 그리고 신규 테스트
    파일 전부. analog는 각 플랜의 `<interfaces>`/`<read_first>`에 이미 지정돼 있으므로 새로 찾지 말고
    거기서 가져온다.

    네 파일 모두 사실만 적는다. 아직 통과하지 않은 검증(`/gsd:verify-work` 재검증)을 통과한 것처럼
    쓰지 않는다 — 재검증은 이 플랜 이후의 별도 단계다.
  </action>
  <verify>
    <automated>grep -c '05-16-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -c 'Gaps found' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] 05-1[0-6]-PLAN.md' .planning/ROADMAP.md` 가 7 (청크 D 7개 플랜이 모두 완료 표기)
    - `grep -c 'phase-05d-gap-closure' .planning/ROADMAP.md` 가 1 이상
    - `grep -c 'CR-03' .planning/ROADMAP.md` 가 1 이상
    - `grep -c 'Gaps found' .planning/REQUIREMENTS.md` 가 0
    - `grep -c '\[x\] \*\*BATCH-01\*\*\|\[x\] \*\*BATCH-02\*\*\|\[x\] \*\*BATCH-04\*\*' .planning/REQUIREMENTS.md` 가 3
    - `grep -c 'InactivityBatchRunConcurrencyTest' .planning/phases/05-batch/05-VALIDATION.md` 가 1 이상
    - `grep -c 'AdminBatchRunConcurrencyTest' .planning/phases/05-batch/05-VALIDATION.md` 가 1 이상
    - `grep -c 'BatchExecutionRecorder.kt' .planning/phases/05-batch/05-PATTERNS.md` 가 1 이상
    - `grep -c 'InactivityBatchProperties.kt' .planning/phases/05-batch/05-PATTERNS.md` 가 1 이상
  </acceptance_criteria>
  <done>로드맵·요구사항이 실제 상태(갭 3건 닫힘, CR-03은 명시적 범위 밖)를 반영하고, VALIDATION·PATTERNS가 갭 클로저 청크를 추적한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 실기동 → 보존된 개발 DB | 실제 회원 잔여 데이터가 실기동 검증으로 변한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05D-16-01 | Tampering | 실기동 검증 중 보존 데이터 손상 | mitigate | `docker compose down -v` 금지를 체크포인트 지시문에 명시하고, 검증 후 `batch_execution`·잔여 상태를 psql로 확인한다. 차감은 모두 `PassTransaction` 이력으로 되돌릴 근거가 남는다 |
| T-05D-16-02 | Repudiation | 검증했다는 주장만 남는 것 | mitigate | `cleanTest`를 강제해 캐시 재사용을 막고, 체크포인트가 사용자의 실제 관찰 결과를 요구한다 |
| T-05D-16-SC | Tampering | 패키지 설치 | n/a | 새 의존성 없음 (npm/pip/cargo 설치 없음) |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew cleanTest test` → `./gradlew build` 모두 BUILD SUCCESSFUL
- 사용자 체크포인트 승인 완료
- `git status`에 커밋되지 않은 산출물이 남지 않는다(재생성된 `docs/api/openapi.yaml` 포함)
</verification>

<success_criteria>
- 05-VERIFICATION.md의 실패 truth 3개에 대응하는 코드·테스트·문서가 모두 존재하고 전체 회귀가 그린이다
- 사용자가 202 접수·조회·잔여 변화를 직접 관찰했다
- 문서에 사실과 다른 안전성 서술이 남아 있지 않다
- ROADMAP·REQUIREMENTS가 갭 클로저 결과와 CR-03 범위 밖 사실을 함께 반영한다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-16-SUMMARY.md` when done.

**납품(D-084):** 이 플랜이 끝나면 `.claude/skills/deliver-phase-chunk/SKILL.md` 절차에 따라
`feature/phase-05d-gap-closure` 브랜치에 커밋·푸시하고 `create-pr` 스킬로 **dev 대상 PR 1개**를
만든다. PR 본문 `## 개요`에 "Phase 5 갭 클로저 — 청크 D(4/4)"와 닫은 결함(CR-01·CR-02·CR-04·
WR-02·WR-04·WR-05), 그리고 **범위 밖인 CR-03과 그 운영 대응(cron 꺼 둔 채 배포)**을 적는다.
**PR 머지는 하지 않는다.** `gh` 명령은 `gh auth switch --user minsu-zip &&` 로 묶어 실행한다.
완료 보고에 `이번에 쓴 기술` 섹션을 포함한다.
</output>
