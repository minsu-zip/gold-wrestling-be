---
phase: 05-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glossary.md
  - docs/decisions.md
  - docs/error-codes.md
  - docs/requirements.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [BATCH-01, BATCH-03, BATCH-04]

must_haves:
  truths:
    - "배치가 쓸 새 개념(BatchExecution·BatchTrigger·BatchExecutionStatus·returnedFromLeaveAt)이 glossary에 등록돼 있어 코드가 그 이름을 그대로 쓸 수 있다"
    - "이번 phase의 Claude 재량 결정 5건(CHECK 완화 방식·복귀 시각 컬럼·트랜잭션 경계·실행 이력 스키마·cron/API 경로)이 docs/decisions.md에 기록돼 있다"
    - "BATCH-03이 '구현물 없이 기존 메커니즘(D-064)으로 충족, 검증 테스트로 실증'으로 REQUIREMENTS·ROADMAP에 기술돼 있다"
    - "기준일 후보 ④의 범위(차감 가능한 장 중 최신 created_at)와 스킵 집계(SUCCESS + 별도 건수)가 D-105·D-108에 반영돼 있다"
  artifacts:
    - path: "docs/glossary.md"
      provides: "배치(Phase 5) 용어 등록"
      contains: "BatchExecution"
    - path: "docs/decisions.md"
      provides: "Phase 5 재량 결정 5건 기록"
      contains: "batch_execution"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BATCH-03 문구 정정"
      contains: "D-107"
  key_links:
    - from: "docs/glossary.md"
      to: "docs/decisions.md D-105~D-109"
      via: "새 용어 정의가 결정 번호를 인용"
      pattern: "D-10[5-9]"
---

<objective>
Phase 5가 만들 새 개념의 **이름과 설계 결정을 코드보다 먼저 문서에 고정**하고, `/gsd:discuss-phase`가
지시한 문서 정합(BATCH-03 문구 정정)을 처리한다.

Purpose: CLAUDE.md 규칙 3("새 개념은 glossary에 추가 후 사용")과 규칙 5("설계 결정은 decisions.md에
기록")를 코드 작성 **전에** 만족시킨다. 특히 CONTEXT.md가 Claude 재량으로 넘긴 5개 항목(V8 CHECK 완화
방식, 회원 상태 전환 시각 컬럼, 배치 트랜잭션 경계, 실행 이력 스키마·네이밍, cron 시각·수동 실행 API
경로)은 05-02 이후 모든 플랜이 전제로 삼기 때문에 여기서 확정하지 않으면 플랜마다 다른 해석이 들어간다.
또한 사용자가 명시적으로 지시한 BATCH-03 문구 정정(D-107 — 구현물 없음)을 REQUIREMENTS·ROADMAP에 반영한다.

Output: `docs/glossary.md`(배치 섹션 신설), `docs/decisions.md`(재량 결정 5건 + D-105·D-108 보강),
`.planning/REQUIREMENTS.md`·`.planning/ROADMAP.md`(BATCH-03 정정), `docs/error-codes.md`(신규 코드
필요 여부 판정 결과).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/glossary.md
@docs/conventions.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-RESEARCH.md
@.planning/phases/05-batch/05-PATTERNS.md
</context>

<interfaces>
현재 `docs/decisions.md`의 마지막 결정 번호는 **D-109**다. 단, Phase 4에서 FE 레포 작업이 D-085~D-088을
선점해 계획이 가정한 번호를 순연시킨 선례가 있다(STATE.md Accumulated Context) — **작업 시작 시
`grep -n "^## D-" docs/decisions.md | tail -3`으로 실제 마지막 번호를 확인하고 그 다음 번호부터
순차 부여한다.** 이 플랜은 아래에서 결정을 번호가 아니라 **결정 A~E**로 부르고, 실제 번호는 실행 시점에 정한다.

`docs/glossary.md` 구조: `## 핵심 엔티티` → `## 이용권 (Phase 3)` → `## 수업 종류` →
`## 시간표·예약 (Phase 4)` → `## 회원 상태` → `## 인증·회원 (Phase 2)` → `## 차감 사유` → `## 기타 규칙`.
각 섹션은 `| 한국어 | 코드 네이밍 | 설명 |` 3열 표다.

`docs/decisions.md` 항목 형식(D-105~D-109 참조):
```
## D-NNN. <한 줄 제목>

- <연월> / <무엇을 정했는가 — 3~4줄>
- 이유: <왜>
- 기각 대안: <무엇을 왜 안 골랐는가>
```

금지어(glossary §기타 규칙): `Ticket`, `Voucher`, `Coupon`, `Booking`, `Course`, `Session`(단독),
`User`, `Account`, `Role`(단독). — `BatchExecution`/`BatchTrigger`/`BatchExecutionStatus`는 이 목록과
충돌하지 않는다(리서치 A4에서 확인).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: glossary에 배치(Phase 5) 용어 등록</name>
  <files>docs/glossary.md</files>
  <read_first>
    - docs/glossary.md (전체 — 섹션 배치와 표 형식, 금지어 목록)
    - .planning/phases/05-batch/05-CONTEXT.md (open_items — "배치 실행 이력 개념 등록 후 사용")
    - .planning/phases/05-batch/05-PATTERNS.md (`batch/BatchExecution.kt`·`BatchExecutionStatus.kt` 절 — 필드 구성)
  </read_first>
  <action>
`## 시간표·예약 (Phase 4)` 섹션 **뒤에** `## 배치 (Phase 5)` 섹션을 신설하고 기존 표와 동일한
3열(`| 한국어 | 코드 네이밍 | 설명 |`) 형식으로 아래 6개 개념을 등록한다. 설명 열에는 근거 결정
번호(D-105·D-106·D-108·D-109)를 인용한다.

- 배치 실행 이력 → `BatchExecution` (DB `batch_execution`) — 배치 1회 실행의 시각·처리 건수·결과를
  남기는 append-only 기록. **관측·복구 판단용이며 멱등성의 근거가 아니다**(D-108)
- 배치 트리거 종류 → `BatchTrigger` — `SCHEDULED`(매일 새벽 cron) / `MANUAL`(관리자 수동 실행 API)
- 배치 실행 결과 → `BatchExecutionStatus` — `SUCCESS` / `PARTIAL_FAILURE`. 경쟁 패배로 인한 스킵은
  정상 경로라 `SUCCESS`이며 스킵 건수만 별도로 센다(D-108 보강, CONTEXT §C)
- 미사용 판정 기준일 → `dueDate` — 5종 후보 중 가장 최근 날짜(D-105). 회원 단위로 계산한다
- 부족분 → `shortfall` — `floor(기준일→오늘 경과일 / 14)` − 기준일 이후 `INACTIVITY` 이력 건수(D-106)
- 휴회 복귀 시각 → `returnedFromLeaveAt` (DB `member.returned_from_leave_at`) — `ON_LEAVE` → `ACTIVE`
  전이 시각. 다른 상태 전이에서는 기록하지 않는다(D-105 기준일 후보 ③ 전용)

`## 기타 규칙`의 금지어 목록은 그대로 두되, 위 이름 중 어느 것도 금지어와 충돌하지 않음을 확인한다
(특히 `Session` 단독 사용 금지 — `BatchExecution`은 `Execution`을 쓰므로 충돌하지 않는다).
  </action>
  <verify>
    <automated>grep -c "BatchExecution\|BatchTrigger\|BatchExecutionStatus\|returnedFromLeaveAt\|shortfall" docs/glossary.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "## 배치 (Phase 5)" docs/glossary.md`가 1행을 반환한다
- `docs/glossary.md`에 `BatchExecution`, `BatchTrigger`, `BatchExecutionStatus`, `dueDate`,
  `shortfall`, `returnedFromLeaveAt` 6개 코드 네이밍이 모두 존재한다
- 새로 추가한 표 행에 `Ticket|Voucher|Coupon|Booking|Course|Session` 단독 사용이 없다
  (`ClassSession` 인용은 예외 — 기존 개념 참조)
- 배치 실행 이력 행의 설명에 "멱등성의 근거가 아니다" 취지의 문구와 `D-108`이 함께 있다
  </acceptance_criteria>
  <done>배치 개념 6종이 glossary에 등록돼 05-02 이후 플랜이 이 이름을 그대로 쓸 수 있다</done>
</task>

<task type="auto">
  <name>Task 2: Claude 재량 결정 5건 기록 + D-105·D-108 보강</name>
  <files>docs/decisions.md</files>
  <read_first>
    - docs/decisions.md (D-105~D-109 전문 — 형식과 이번 phase 확정 내용)
    - .planning/phases/05-batch/05-CONTEXT.md (`### Claude's Discretion`, `### C. 계획 단계 보완 확정`)
    - .planning/phases/05-batch/05-RESEARCH.md (Pitfall 3·Pitfall 5, "회원 상태 전환 시각 컬럼", "배치 실행 이력 테이블 — 마이그레이션 스케치")
    - src/main/resources/db/migration/V8__extend_pass_transaction_subject.sql (완화 대상 CHECK의 현재 정의와 그 근거 주석)
  </read_first>
  <action>
`grep -n "^## D-" docs/decisions.md | tail -3`으로 마지막 번호를 확인한 뒤, 그 다음 번호부터 아래
**결정 A~E**를 순서대로 추가한다(파일 맨 끝에 이어 붙인다). 각 항목은 기존 형식(`- 연월 / 무엇을`,
`- 이유:`, `- 기각 대안:`)을 그대로 따르고 3~4줄로 쓴다.

**결정 A — `pass_transaction` 시스템 주체는 CHECK를 "정확히 하나"에서 "최대 하나"로 완화해 표현한다**
V9에서 `ck_pass_transaction_subject`를 DROP하고
`CHECK (NOT (admin_id IS NOT NULL AND member_id IS NOT NULL))`로 재정의한다. 배치(`INACTIVITY`) 이력은
`admin_id`·`member_id`가 **둘 다 NULL**이며 이것이 "시스템 주체"의 표현이다. 기존 행은 전부 한쪽만
채워져 있어 백필이 필요 없다. 기각 대안: `actor_type` 판별 컬럼 추가(기존 두 컬럼의 의미가 그대로인데
판별 컬럼이 생겨 주체 표현이 이원화된다), 시스템 관리자 계정을 만들어 `admin_id`에 대입(`admin`의
의미가 "이 행위를 한 실제 관리자"에서 오염되고 감사 추적에서 배치와 사람이 구분되지 않는다 — RESEARCH Pitfall 5).

**결정 B — 휴회 복귀 시각은 `member.returned_from_leave_at` 단일 목적 컬럼으로 기록한다**
`ON_LEAVE` → `ACTIVE` 전이에서만 갱신하고 다른 전이(승인 `PENDING`→`ACTIVE`, 재활성화
`INACTIVE`→`ACTIVE`, 휴회 시작)에서는 손대지 않는다. `Reservation.canceledAt`과 같은 "그 사건이
일어난 시각만" 기록하는 관례다. 기각 대안: 범용 `status_changed_at`(마지막 전환 시각만 남고 이전
상태를 알 수 없어 "휴회에서 복귀한 것인지" 판정 불가), 휴회 기간 테이블(D-105가 이미 기각).

**결정 C — 미사용 차감 배치의 트랜잭션 경계는 "회원 1명 = 트랜잭션 1개"다**
루프를 도는 `InactivityBatchRunner`에는 `@Transactional`을 붙이지 않고, 회원 1명분 차감 반영
(`InactivityDeductionService.deductOnce`)만 **별도 스프링 빈**의 `@Transactional` 메서드로 둔다.
회원 단위 예외는 루프가 `try-catch`로 흡수해 실행 이력에 집계하고 나머지 회원은 계속 처리한다.
기각 대안: 배치 전체를 한 트랜잭션으로(150번째 회원의 실패가 앞선 149명의 차감까지 롤백시키고,
같은 클래스 내부 호출은 프록시를 우회해 트랜잭션 경계가 생기지도 않는다 — D-020의 취지는
"차감과 이력이 원자적"이지 "배치 전체가 원자적"이 아니다).

**결정 D — 배치 실행 이력 `batch_execution` 스키마와 스킵 집계**
컬럼: `trigger_type`(SCHEDULED/MANUAL), `triggered_by_admin_id`(MANUAL일 때만 non-null, CHECK로 강제),
`started_at`/`finished_at`, `processed_member_count`, `deducted_count`, `skipped_count`,
`status`(SUCCESS/PARTIAL_FAILURE), `error_summary`. **경쟁 패배·대상 소진으로 인한 스킵은 정상 경로라
`SUCCESS`로 두고 `skipped_count`에만 센다**(CONTEXT §C). 실행 이력은 관측·복구 판단용이며 멱등성
판단에 절대 쓰지 않는다(D-106·D-108). 기각 대안: 스킵을 `PARTIAL_FAILURE`로 집계(상태 기반 설계가
예정한 정상 동작을 실패로 표시하면 운영자가 매일 오탐을 본다), 회원별 처리 상세 저장(원장이 이미
이력을 갖고 있어 이중 기록).

**결정 E — cron은 매일 04:00 `Asia/Seoul`, 수동 실행은 `POST /api/admin/batch/inactivity-runs` 1개**
`@Scheduled(cron = "0 0 4 * * *", zone = SEOUL_ZONE_ID)`. 새벽 4시는 예약·차감 트래픽이 사실상 없는
시간대다. 수동 실행 API는 요청 본문 없이 `BatchExecutionResponse`를 반환하고, 인가는 기존
`/api/admin/**` → `hasRole("ADMIN")` 규칙을 그대로 상속한다(D-040) — SecurityConfig를 수정하지 않는다.
**새 에러코드를 추가하지 않는다** — D-106 상태 기반 설계가 "중복 실행" 같은 실패 경로를 부족분 0으로
흡수하므로 거부할 요청이 없다. 기각 대안: 실행 파라미터(기준일 override 등)를 받는 API(테스트 목적의
소급 차감 경로가 열려 원장이 오염된다).

이어서 **기존 항목 2건을 보강**한다(새 결정을 만들지 말고 해당 항목 본문에 한 줄씩 덧붙인다):
- **D-105**: 기준일 후보 ④(SESSION_PASS 등록일)의 범위는 "**차감 가능한 장**(취소·만료 아님, 잔여 > 0)
  중 최신 `created_at`"이다 — 새 장 등록(충전)이 시계를 리셋하는 +가감일 원리와 일관하며, 이미 못 쓰는
  옛 이용권의 등록일이 기준일을 부당하게 최신으로 만드는 것을 막는다(2026-08-15 사용자 확정).
- **D-108**: 경쟁 패배로 스킵된 회원이 있어도 실행 결과는 `SUCCESS`이며 스킵 건수를 별도 컬럼에
  기록한다 — 스킵은 다음 실행이 상태 기반으로 보정하는 정상 동작이다(2026-08-15 사용자 확정).
  </action>
  <verify>
    <automated>grep -c "^## D-" docs/decisions.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "^## D-" docs/decisions.md | tail -5`가 이번에 추가한 결정 5건을 보여주고, 번호가
  이전 마지막 번호에서 1씩 증가하며 건너뛰지 않는다
- 새 결정 5건이 각각 `- 이유:`와 `- 기각 대안:` 줄을 모두 갖는다
- `grep -n "NOT (admin_id IS NOT NULL AND member_id IS NOT NULL)" docs/decisions.md`가 1행 이상 반환한다
- `grep -n "returned_from_leave_at" docs/decisions.md`가 1행 이상 반환한다
- `grep -n "0 0 4 \* \* \*" docs/decisions.md`와 `grep -n "/api/admin/batch/inactivity-runs" docs/decisions.md`가 각각 1행 이상 반환한다
- D-105 항목 본문에 "차감 가능한" + "created_at" 취지의 보강 문구가, D-108 항목 본문에 "스킵" +
  "SUCCESS" 취지의 보강 문구가 들어 있다
- 기존 D-001~D-109 항목의 **제목 줄은 하나도 변경되지 않았다** (`git diff docs/decisions.md`에서
  `^-## D-` 삭제 라인이 없다)
  </acceptance_criteria>
  <done>05-02 이후 플랜이 참조할 재량 결정 5건이 결정 번호를 갖고 문서에 남았다</done>
</task>

<task type="auto">
  <name>Task 3: BATCH-03 문구 정정 + 에러코드 필요 여부 판정</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/requirements.md, docs/error-codes.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (`### 배치 (M5) — BATCH` 섹션)
    - .planning/ROADMAP.md (`### Phase 5: 배치` 전체 — 성공 기준 4항목과 Note)
    - docs/requirements.md (전체 — 배치 관련 문구 존재 여부 확인)
    - docs/error-codes.md (상단 규칙 + 표 형식)
    - docs/decisions.md D-107 (BATCH-03 충족 근거)
  </read_first>
  <action>
**(1) `.planning/REQUIREMENTS.md`** — BATCH-01·BATCH-03 두 줄을 정정한다.
- BATCH-01: 기준일 설명을 D-105 확정 문구로 교체한다 — "기준일: 마지막 출석일 / 마지막 취소되지 않은
  예약의 수업일 / `ON_LEAVE`→`ACTIVE` 복귀일 / `SESSION_PASS` 등록일(`created_at`) / 마지막 양(+)
  `ADMIN_ADJUST` 일자 중 **가장 최근 날짜**, 판정은 회원 단위 (D-027, D-105)".
- BATCH-03: "유효기간(등록일+1년) 만료 이용권이 사용 불가 처리된다 — **별도 배치 구현물 없이** 기존
  메커니즘(D-064 조회 시점 `displayStatus` 계산 + Phase 4 예약 경로의 수업날 기준 거부)으로 충족하고,
  Phase 5는 이를 **검증 테스트로 실증**한다 (D-107)".

**(2) `.planning/ROADMAP.md` Phase 5 절** — 성공 기준 1·3과 Note를 정정한다.
- 성공 기준 1의 기준일 괄호 설명을 D-105 5종 후보로 교체하고 "판정은 회원 단위"를 명시한다.
- 성공 기준 3을 "등록일로부터 1년이 지난 이용권은 예약에 쓸 수 없다 — 기존 조회 시점 계산(D-064)과
  Phase 4 예약 거부 경로로 충족되며 이 phase는 검증 테스트로 실증한다 (D-107)"로 바꾼다.
- 기존 **Note**(Attendance 선반영 가능) 문단을 "출석(`Attendance`) 스키마는 이 phase에서 선반영하지
  않는다 — 기준일 조회가 출석 테이블을 필요로 하지 않음을 05-CONTEXT에서 확인했고, 기준일 후보 ①은
  Phase 6까지 자연히 부재로 동작한다(의도된 동작)"로 교체한다.
- Phase 5의 `**Plans**:` 줄과 플랜 목록은 이 플랜 작성 시점에 이미 채워져 있다 — 목록의 텍스트를
  건드리지 말고 그대로 둔다.

**(3) `docs/requirements.md`** — `grep -n "배치\|미사용\|만료" docs/requirements.md`로 배치 관련
문구를 찾는다. D-105~D-109와 어긋나는 문장이 있으면 정정하고, 없으면 **파일을 수정하지 않는다**
(SUMMARY에 "배치 관련 문구 없음 — 변경 없음"으로 남긴다).

**(4) `docs/error-codes.md`** — 결정 E대로 이번 phase는 신규 에러코드를 추가하지 않는다. 파일을
수정하지 않고, SUMMARY에 "수동 실행 API는 도메인 거부 경로가 없어(D-106이 흡수) 신규 코드 없음 —
05-08에서 재확인"을 남긴다. 단 05-08에서 새 코드가 필요해지면 그 플랜이 같은 커밋에서 이 표를 갱신한다.
  </action>
  <verify>
    <automated>grep -n "D-107" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/REQUIREMENTS.md`의 BATCH-03 줄에 "구현물" 또는 "구현 없이" 취지의 문구와 `D-107`이 함께 있다
- `.planning/REQUIREMENTS.md`의 BATCH-01 줄에 `D-105`와 "회원 단위"가 있다
- `.planning/ROADMAP.md` Phase 5 성공 기준 3에 `D-107`이 있고 "배치"로 만료 상태를 저장한다는 표현이 없다
- `.planning/ROADMAP.md` Phase 5 Note에 "선반영하지 않는다" 취지의 문장이 있고, 기존 "필요시 선반영할 수
  있다" 문장은 남아 있지 않다
- `git diff --stat docs/requirements.md docs/error-codes.md`가 변경 없음(0 files changed)이거나,
  변경이 있다면 SUMMARY에 그 사유가 적혀 있다
  </acceptance_criteria>
  <done>BATCH-03의 "구현물 없음" 확정이 요구사항·로드맵 양쪽에 반영돼 검증 단계가 존재하지 않는 산출물을 찾지 않는다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서(스펙) → 코드 | 이 phase의 모든 차감 로직이 이 문서들을 근거로 작성된다 — 문구가 틀리면 대량 차감 로직이 틀린 채 구현된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | 기준일 정의가 문서마다 달라 배치가 소급 차감 | mitigate | REQUIREMENTS·ROADMAP의 기준일 문구를 policies §4.3·D-105 확정 문구로 통일. Task 3 acceptance가 `D-105` 인용을 강제 |
| T-05-02 | Repudiation | 재량 결정이 기록되지 않아 이후 리뷰에서 근거를 추적할 수 없음 | mitigate | 결정 A~E를 decisions.md에 기록(이유·기각 대안 포함), acceptance가 두 줄 존재를 검사 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다(RESEARCH Package Legitimacy Audit — 해당 없음) |
</threat_model>

<verification>
- `git diff --stat`에 `src/` 변경이 **없다** (이 플랜은 문서 전용이다)
- `./gradlew build`는 이 플랜에서 돌리지 않는다 (코드 변경 없음 — 규칙 10 §10.0 면제: 문서)
</verification>

<success_criteria>
- 05-02가 마이그레이션·엔티티를 쓸 때 "이 개념을 뭐라고 부를지"·"CHECK를 어떻게 완화할지"를 다시 정하지 않는다
- 검증 단계(`/gsd:verify-work`)가 BATCH-03에 대해 존재하지 않는 구현물을 찾지 않는다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-01-SUMMARY.md` when done.
**청크 A 첫 플랜** — `git symbolic-ref --short refs/remotes/origin/HEAD`가 `origin/dev`인지 먼저 확인하고
(아니면 `git remote set-head origin dev`), 최신 dev에서 `feature/phase-05a-batch-foundation` 브랜치를
새로 따고 시작한다 (D-084).
</output>
