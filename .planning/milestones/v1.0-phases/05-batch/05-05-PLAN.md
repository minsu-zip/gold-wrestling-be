---
phase: 05-batch
plan: 05
type: tdd
wave: 5
depends_on: ["05-04"]
files_modified:
  - src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityDeductionServiceTest.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityDeductionRaceTest.kt
autonomous: true
requirements: [BATCH-01, BATCH-02]

must_haves:
  truths:
    - "미사용 차감 1회는 만료가 가장 임박한 SESSION_PASS 한 장에서만 일어난다"
    - "잔여가 1회보다 적으면 잔여만큼만 차감되고 부족분이 다른 장으로 이월되지 않는다"
    - "차감 1건마다 INACTIVITY 사유의 PassTransaction이 시스템 주체(admin·member 둘 다 null)로 남는다"
    - "차감 후에도 '잔여 = 이력 합계' 불변식이 유지된다"
    - "차감할 이용권이 없거나 조건부 UPDATE가 0행이면 이력 없이 스킵한다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt"
      provides: "회원 1명분 차감 반영 (@Transactional 단위)"
      contains: "fun deductOnce"
    - path: "src/test/kotlin/com/goldwrestling/batch/InactivityDeductionServiceTest.kt"
      provides: "부분 차감·재선택·이력·불변식 통합테스트"
      contains: "class InactivityDeductionServiceTest"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt"
      to: "PassRepository.adjustRemainingCount (D-021 조건부 UPDATE)"
      via: "차감 반영은 조건부 UPDATE로만"
      pattern: "adjustRemainingCount"
    - from: "src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt"
      to: "PassTransaction(reason = INACTIVITY, admin = null, member = null)"
      via: "시스템 주체 원장 기록"
      pattern: "TransactionReason.INACTIVITY"
---

<objective>
**차감 1회를 수행하는 트랜잭션 단위 메서드**를 TDD로 만든다 — 만료 임박 한 장 선택(D-109/D-091),
부분 차감(잔여만큼), 시스템 주체 원장 기록, 경쟁 패배 시 스킵.

Purpose: 이 메서드가 배치에서 실제로 회원 잔여를 바꾸는 유일한 지점이다. 프로젝트의 Core Value
("회원이 보는 잔여 횟수는 항상 실제 사용 가능 횟수와 일치한다")가 여기서 깨지면 나머지가 무의미하다.
또 배치 트랜잭션 경계 결정(05-01 결정 C)의 실현부다 — 이 메서드만 `@Transactional`이고 루프(05-06)는
아니다. 별도 스프링 빈으로 분리해야 자기호출(self-invocation)로 프록시가 우회되지 않고, 회원 1명의
실패가 그 회원만 롤백한다.

Output: `batch/InactivityDeductionService.kt` + 통합테스트(Testcontainers) + 경쟁 패배 단위테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@.claude/skills/add-domain-test/SKILL.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-RESEARCH.md
@.planning/phases/05-batch/05-PATTERNS.md
@.planning/phases/05-batch/05-04-SUMMARY.md
</context>

<interfaces>
policies §4.3 확정 문구:
- "차감은 한 번에 한 장: 차감 가능한 `SESSION_PASS`(취소·만료 아님, 잔여 > 0) 중 유효기간 만료가 가장
  임박한 한 장에서 1회 차감한다(D-091 선택 규칙 준용)"
- "**잔여가 차감량보다 적으면 잔여만큼만 차감한다**(잔여 0.5 → 0) — 부족분을 다른 장에서 마저 차감하지
  않는다(합산 금지, D-091과 일관)"

05-04가 만든 조회(회당 다시 호출한다 — RESEARCH Pitfall 1):
`PassRepository.findDeductibleSessionPasses(memberId: Long, today: LocalDate): List<Pass>`
(`status = ACTIVE`, `type = SESSION_PASS`, `endDate >= today`, `remainingCount > 0`,
`order by endDate asc, id asc`)

기존 조건부 UPDATE(그대로 재사용, 수정 금지):
`PassRepository.adjustRemainingCount(id: Long, amount: BigDecimal): Int`
— `status = ACTIVE` + `remainingCount + amount >= 0`일 때만 반영, 반환값은 갱신 행 수(0 = 경쟁 패배).
`@Modifying(flushAutomatically = true, clearAutomatically = true)`라 **실행 직후 엔티티는 준영속**이다 —
이후 필요한 스칼라 값은 실행 **전에** 지역 변수로 꺼내 두고, 이력 저장용 엔티티는 `findById`로 재조회한다
(`ReservationLedgerSupport.createReservation` 관례).

`PassTransaction` 생성자: `(pass, amount, reason, note, admin, member, occurredAt)`.
배치는 `admin = null, member = null`(시스템 주체 — V9 완화된 CHECK가 허용, 05-02), `note = null`.

`pass/PassExceptions.kt`: `PassNotFoundException(passId)` 등 기존 예외 재사용 — 새 예외를 만들지 않는다.
`TransactionReason.INACTIVITY`는 Phase 3에서 이미 선언돼 있다.

`BigDecimal` 규약(D-016): 비교는 `compareTo`(Kotlin `<`, `>`, `minOf`), 상수는 문자열 생성자.
`ReservationPassPolicy.DEDUCTION_AMOUNT`가 예약용 `BigDecimal("1.0")`을 이미 갖고 있으나 **재사용하지
않는다** — 배치가 `reservation` 패키지에 의존하면 의존 방향이 꼬인다. `batch` 안에 자체 상수를 둔다.

`Clock` 주입(conventions §5): `LocalDate.now(clock)` / `OffsetDateTime.now(clock)`.
테스트 픽스처는 05-04가 만든 `batch/BatchFixtures.kt`를 쓴다.
</interfaces>

<feature>
  <name>미사용 차감 1회 반영 (D-109 · D-021 조건부 UPDATE)</name>
  <files>
    src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityDeductionServiceTest.kt,
    src/test/kotlin/com/goldwrestling/batch/InactivityDeductionRaceTest.kt
  </files>
  <behavior>
    **공개 API**: `@Transactional fun deductOnce(memberId: Long): Boolean` — 차감했으면 `true`,
    스킵했으면 `false`(예외를 던지지 않는다 — 대상 소진은 정상 경로다).

    **통합테스트(`InactivityDeductionServiceTest`, Testcontainers)**
    · 잔여 3.0 한 장 → 잔여 2.0, `INACTIVITY` 이력 1건(amount = -1.0), 반환 true
    · 이력의 `admin`·`member`가 **둘 다 null**이고 `reason = INACTIVITY`, `note = null`, `occurredAt`이 고정 Clock 시각과 같다
    · 잔여 0.5 한 장 → 잔여 0, 이력 amount = **-0.5**(부분 차감), 반환 true
    · 잔여 0.5 한 장 + 잔여 3.0 한 장(0.5 쪽이 만료 임박) → **0.5 쪽에서 0.5만 차감**되고 3.0 장은 그대로다
      (부족분을 다른 장으로 이월하지 않는다 — D-109)
    · 두 장(endDate 다름) → 만료가 임박한 장에서 차감된다
    · 두 장(endDate 동일) → `id`가 작은 장에서 차감된다
    · 잔여 1.0 한 장에 대해 `deductOnce`를 **연속 2회** 호출 → 1회차는 true(잔여 0), 2회차는 false(그 장이
      후보에서 빠졌고 다른 장이 없다) — 이력은 총 1건
    · 잔여 1.0 두 장에 대해 연속 2회 호출 → 첫 장 소진 후 **두 번째 장에서 차감**된다(회당 재선택, Pitfall 1)
    · 차감 가능한 장이 없는 회원 → false, 이력 0건, 어떤 이용권의 잔여도 변하지 않는다
    · 만료된 장만 보유 → false (BATCH-02)
    · 잔여 0인 장만 보유 → false (BATCH-02)
    · 등록 취소(CANCELED)된 장만 보유 → false
    · `LESSON_PASS`만 보유 → false, `LESSON_PASS` 잔여는 변하지 않는다 (미사용 차감은 `SESSION_PASS` 전용)
    · **불변식**: 위 모든 케이스 이후 `pass.remainingCount == sumAmountByPassId(passId)`가 성립한다
      (`PassLedgerInvariantTest` 방식, `isEqualByComparingTo`)

    **경쟁 패배 단위테스트(`InactivityDeductionRaceTest`, 스프링 컨텍스트 없음)**
    · `adjustRemainingCount`가 0을 반환하면(다른 트랜잭션이 먼저 잔여를 바꿈) → 반환 false,
      `passTransactionRepository.save`가 **한 번도 호출되지 않는다**
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/ReservationLedgerSupport.kt (전체 — 조건부 UPDATE → 재조회 → 이력 저장 관례, 준영속 주의사항)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt (`adjustRemainingCount` KDoc + 05-04가 추가한 `findDeductibleSessionPasses`)
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (생성자·주체 규칙 — 05-02가 KDoc을 갱신했다)
    - src/test/kotlin/com/goldwrestling/pass/PassLedgerInvariantTest.kt (불변식 단언 헬퍼와 애노테이션 조합)
    - src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt (05-04가 만든 픽스처)
    - src/test/kotlin/com/goldwrestling/auth/KakaoLoginConcurrencyTest.kt (이 저장소의 Mockito 사용 선례)
    - docs/policies.md §4.3 (차감 대상·부분 차감 문장 — 테스트 메서드명을 여기서 딴다)
  </read_first>
  <implementation>
    RED → GREEN → REFACTOR 사이클을 두 번 돈다.

    **사이클 1 — deductOnce 본체 (Testcontainers 통합테스트)**
    RED: `InactivityDeductionServiceTest`에 `<behavior>`의 통합 케이스 13종을 백틱 한국어 서술문으로
    작성한다. 애노테이션 조합은 `@SpringBootTest` + `@Import(TestcontainersConfiguration::class,
    TestClockConfiguration::class)`. **클래스 레벨 `@Transactional`을 붙이지 않는다** —
    `deductOnce`가 자체 트랜잭션을 여는 서비스 메서드라 테스트가 트랜잭션을 감싸면 조건부 UPDATE의
    flush/clear 동작과 실제 커밋 결과를 검증할 수 없다(`MemberReservationServiceTest` 선례).
    대신 `@AfterEach`에서 이 테스트가 만든 행을 직접 지운다.
    커밋: `test(05-05): 미사용 차감 1회 반영 실패 테스트 추가`

    GREEN: `batch/InactivityDeductionService.kt`를 만든다.
    - `@Service` + 클래스 레벨 `@Transactional(readOnly = true)`, `deductOnce`에만 `@Transactional`
      오버라이드(D-020).
    - `private val ONE_SESSION = BigDecimal("1.0")`을 companion에 둔다(`BigDecimal(1.0)` Double 생성자 금지).
    - 흐름: `today = LocalDate.now(clock)` → `findDeductibleSessionPasses(memberId, today)` →
      `firstOrNull() ?: return false` → `deductAmount = minOf(ONE_SESSION, candidate.remainingCount!!)`
      (`requireNotNull`로 꺼낸다 — 횟수제라 non-null 보장) → `passId`를 지역 변수로 확보 →
      `adjustRemainingCount(passId, deductAmount.negate())`가 0이면 `return false` →
      `findById(passId).orElseThrow { PassNotFoundException(passId) }`로 재조회 →
      `PassTransaction(pass = refreshed, amount = deductAmount.negate(), reason = INACTIVITY,
      note = null, admin = null, member = null, occurredAt = OffsetDateTime.now(clock))` 저장 → `return true`.
    - KDoc에 남길 것: ① "**회당 재선택**이 이 메서드의 계약이다 — 호출부는 부족분만큼 이 메서드를 반복
      호출하고, 매 호출이 후보를 다시 조회한다(D-109, RESEARCH Pitfall 1). 후보 리스트를 호출부가
      캐시해 소비하면 첫 차감으로 잔여가 0이 된 장을 다시 고른다" ② "**부족분을 다른 장으로 이월하지
      않는다** — 한 차감 이벤트는 한 장에서만 일어난다(합산 금지 D-091과 일관)" ③ "반환 false는 실패가
      아니라 정상 경로다 — 대상 소진·경쟁 패배 모두 다음 실행이 상태 기반으로 보정한다(D-106)"
      ④ "`@Transactional`이 이 메서드에만 있는 이유: 배치 루프 전체를 한 트랜잭션으로 감싸면 한 회원의
      실패가 앞선 전원을 롤백하고, 같은 클래스 내부 호출은 프록시를 우회해 경계가 생기지도 않는다(05-01 결정 C)".
    커밋: `feat(05-05): 미사용 차감 1회 반영 InactivityDeductionService 추가`

    **사이클 2 — 경쟁 패배 스킵 (Mockito 단위테스트)**
    RED: `InactivityDeductionRaceTest`를 스프링 없이 만든다 — `PassRepository`·`PassTransactionRepository`를
    Mockito `mock()`으로 만들고 `findDeductibleSessionPasses`가 후보 1장을 반환하되
    `adjustRemainingCount`가 **0**을 반환하도록 스텁한다. 고정 `Clock`은 `Clock.fixed(...)`를 쓴다.
    단언: 반환값 false + `verify(passTransactionRepository, never()).save(any())`.
    커밋: `test(05-05): 조건부 UPDATE 경쟁 패배 시 이력 미기록 실패 테스트 추가`
    GREEN: 사이클 1 구현이 이미 만족하면 코드 변경 없이 통과한다 — 통과하지 않으면 0행 분기를 추가한다.
    커밋(코드 변경이 있을 때만): `fix(05-05): 조건부 UPDATE 0행 시 이력 기록 없이 스킵`

    REFACTOR(필요 시): 통합테스트의 반복되는 `SESSION_PASS` 생성·잔여 단언을 private 헬퍼로 묶는다.
  </implementation>
</feature>

<verify>
  <automated>./gradlew test --tests "com.goldwrestling.batch.InactivityDeductionServiceTest" --tests "com.goldwrestling.batch.InactivityDeductionRaceTest"</automated>
</verify>

<acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.batch.InactivityDeductionServiceTest" --tests "com.goldwrestling.batch.InactivityDeductionRaceTest"` 종료 코드 0이고 14개 이상 테스트가 실행된다
- 테스트가 "잔여 0.5는 0.5만 차감된다"와 "부족분이 다른 장으로 이월되지 않는다"를 각각 단언한다
- 테스트가 "연속 2회 호출 시 두 번째는 다른 장에서 차감된다"(회당 재선택)를 단언한다
- 테스트가 이력의 `admin`·`member`가 둘 다 null임을 단언한다
- 테스트가 차감 후 `잔여 = 이력 합계` 불변식을 `isEqualByComparingTo`로 단언한다
- `grep -c "@Transactional" src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt`가 2
  (클래스 레벨 readOnly + `deductOnce` 오버라이드)
- `grep -c "BigDecimal(\"1.0\")" src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt`가 1이고
  `grep -c "BigDecimal(1" src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt`가 0 (Double 생성자 금지)
- `grep -c "com.goldwrestling.reservation" src/main/kotlin/com/goldwrestling/batch/InactivityDeductionService.kt`가 0
  (batch → reservation 의존을 만들지 않는다)
- `InactivityDeductionServiceTest` 클래스에 `@Transactional`이 붙어 있지 않다
- `git log --oneline`에 이 플랜의 `test(05-05):` 커밋 2건과 `feat(05-05):` 커밋 1건이 있다
- `./gradlew ktlintFormat` 후 `./gradlew build` 통과
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배치(시스템 주체) → 회원 잔여 횟수 | 사람의 확인 없이 잔여를 깎는 유일한 경로다 — 이중 차감·과차감이 조용히 일어날 수 있다 |
| 배치 차감 ↔ 회원 예약 차감(동시) | 같은 이용권을 회원이 예약으로 쓰는 순간 배치가 깎으면 잔여가 음수가 될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering | 배치 차감과 예약 차감이 경쟁해 잔여가 음수가 됨 | mitigate | `adjustRemainingCount`의 `remainingCount + :amount >= 0` 조건부 UPDATE만 사용(D-021). 0행이면 스킵하고 다음 실행이 보정 |
| T-05-16 | Tampering | 후보 리스트 캐시 소비로 이미 소진된 장을 반복 차감(또는 차감 유실) | mitigate | 계약을 "1회 호출 = 1회 재조회"로 고정하고 연속 호출 테스트로 증명(Pitfall 1) |
| T-05-17 | Repudiation | 잔여만 바뀌고 이력이 남지 않아 감사 추적 단절 | mitigate | 같은 트랜잭션에서 `PassTransaction` 저장. 불변식(잔여 = 이력 합계) 테스트가 모든 케이스에서 성립을 요구 |
| T-05-18 | Spoofing | 배치 차감이 특정 관리자 행위로 기록돼 책임이 오귀속 | mitigate | `admin = null, member = null` 시스템 주체 고정 + 테스트 단언(05-02 CHECK 완화가 전제) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `PassLedgerInvariantTest`·`MemberReservationServiceTest` 그린 (예약 차감 경로 회귀 없음 — 이 플랜은 기존 파일을 수정하지 않는다)
</verification>

<success_criteria>
- 05-06 러너는 "몇 번 차감할지"만 정하고, "어느 장에서 얼마나" 는 이 서비스가 전부 책임진다
- 배치 차감 후에도 Core Value("잔여 = 실제 사용 가능 횟수 = 이력 합계")가 테스트로 증명된다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-05-SUMMARY.md` when done.
**청크 B 첫 플랜** — 청크 A PR이 dev에 머지된 것을 확인한 뒤 최신 dev에서
`feature/phase-05b-inactivity-batch` 브랜치를 새로 따고 시작한다 (D-084).
</output>
