---
phase: 05-batch
plan: 07
type: execute
wave: 7
depends_on: ["05-06"]
files_modified:
  - src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityBatchExpiryVerificationTest.kt
autonomous: true
requirements: [BATCH-02, BATCH-03, BATCH-04]

must_haves:
  truths:
    - "같은 날 배치를 두 번 실행해도 차감 건수와 잔여가 1회 실행과 동일하다(이중 차감 0건)"
    - "배치가 며칠 밀린 뒤 실행하면 밀린 주기만큼 몰아서 차감한다"
    - "여러 번 실행한 뒤에도 '잔여 = 이력 합계' 불변식이 성립한다"
    - "유효기간이 지난 이용권은 표시 상태가 EXPIRED이고 예약에 쓸 수 없으며 미사용 차감 대상도 아니다"
  artifacts:
    - path: "src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt"
      provides: "BATCH-04 멱등·캐치업 실증"
      contains: "class InactivityBatchIdempotencyTest"
    - path: "src/test/kotlin/com/goldwrestling/batch/InactivityBatchExpiryVerificationTest.kt"
      provides: "BATCH-03 기존 메커니즘 충족 실증(신규 프로덕션 코드 없음)"
      contains: "class InactivityBatchExpiryVerificationTest"
  key_links:
    - from: "src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt"
      to: "PassTransactionRepository.sumAmountByPassId (잔여 = 이력 합계)"
      via: "매 실행 후 불변식 재검증"
      pattern: "sumAmountByPassId"
---

<objective>
BATCH-04(멱등)와 BATCH-03(만료 사용 불가)을 **테스트로 실증**한다. 두 요구사항 모두 이 플랜에서
새 프로덕션 코드를 만들지 않는다 — 멱등은 D-106 설계가 이미 보장하고, 만료는 D-064 조회 시점 계산과
Phase 4 예약 거부 경로가 이미 충족한다(D-107).

Purpose: conventions §10.0 표가 "배치 → 단위테스트 + **멱등성 테스트**(두 번 실행해도 결과 동일)"를
명시적으로 요구한다. 그리고 D-107로 "구현물 없음"을 확정한 BATCH-03은 **검증 테스트가 유일한 산출물**이라,
이 테스트가 없으면 요구사항이 근거 없이 충족 처리된다. 두 테스트 모두 시각을 이동시켜(`MutableTestClock`)
실제 PostgreSQL 위에서 여러 번 실행한다.

Output: 테스트 2파일. 프로덕션 코드 변경 0줄(변경이 필요해지면 그 자체가 설계 결함 신호다 — SUMMARY에 기록).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@.claude/skills/add-domain-test/SKILL.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-RESEARCH.md
@.planning/phases/05-batch/05-PATTERNS.md
@.planning/phases/05-batch/05-06-SUMMARY.md
</context>

<interfaces>
`InactivityBatchRunner.run(trigger: BatchTrigger, triggeredByAdminId: Long?): BatchExecution` (05-06)
`PassTransactionRepository.sumAmountByPassId(passId): BigDecimal` — 불변식 단언용(기존 메서드)
`Pass.displayStatus(today: LocalDate): PassDisplayStatus` — `USABLE`/`EXPIRED`/`EXHAUSTED`/`CANCELED` (D-064)
`PassRepository.findDeductionCandidates(memberId, type, classDate, requiredAmount)` — 예약용 후보 조회
(만료 판정 기준은 **수업 날짜**, D-091)
`MemberReservationService.reserve(...)` — 예약 생성 경로(Phase 4). 잔여·후보가 없으면
`InsufficientPassCountException`을 던진다
`MutableTestClock.setTo(instant)` — 시각 이동. `TestClockConfiguration` 기본값은 `2026-08-02T10:00:00+09:00`
`BatchFixtures`(05-04) — 회원·이용권·이력 픽스처

테스트 애노테이션 조합: `@SpringBootTest` +
`@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`.
**두 테스트 모두 클래스 레벨 `@Transactional`을 붙이지 않는다** — 배치가 여는 독립 트랜잭션이 실제로
커밋돼야 "두 번째 실행이 첫 실행의 이력을 본다"가 성립한다. `@AfterEach`로 생성 행을 정리한다.

`BigDecimal` 단언은 `isEqualByComparingTo`(D-016).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 멱등성·캐치업 실증 테스트 (BATCH-04)</name>
  <files>src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/pass/PassLedgerInvariantTest.kt (불변식 단언 헬퍼 형태)
    - src/test/kotlin/com/goldwrestling/batch/InactivityBatchRunnerTest.kt (05-06 — 픽스처 구성·정리 방식 재사용)
    - src/test/kotlin/com/goldwrestling/support/MutableTestClock.kt (시각 이동 API)
    - src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt (05-06 — run 반환값)
    - docs/policies.md §4.3 (멱등 요구 문장 — 테스트 메서드명 원본)
    - docs/decisions.md D-106
  </read_first>
  <action>
`InactivityBatchIdempotencyTest`를 만들고 아래 시나리오를 각각 독립 테스트로 작성한다. 모든 시나리오
끝에 `assertLedgerInvariant(passId)`(잔여 == `sumAmountByPassId`)를 호출한다.

1. **같은 날 2회 실행** — 기준일 14일 전 회원 1명, 잔여 3.0. `run()`을 시각 이동 없이 연속 2회 호출한다.
   기대: 총 차감 1건, 잔여 2.0, `INACTIVITY` 이력 1건, 두 번째 실행의 `deductedCount = 0`,
   `batch_execution` 행은 2건(실행 자체는 두 번 기록된다 — 이력은 관측용이므로 정상).
2. **같은 날 5회 실행** — 위와 같은 조건에서 5회 호출해도 이력이 1건인지 확인한다(반복 호출 안전성 —
   수동 실행 API의 근거).
3. **14일 뒤 재실행** — 1번 이후 시각을 14일 앞으로 이동해 `run()`. 기대: 이력 2건, 잔여 1.0.
4. **캐치업(6주 밀린 첫 실행)** — 기준일 42일 전 회원, 잔여 3.0, 이력 0건 상태에서 `run()` 1회.
   기대: 차감 3건, 잔여 0. 이어서 같은 날 재실행하면 추가 차감 0건.
5. **캐치업 + 부분 차감 소진** — 기준일 42일 전, 잔여 1.5인 회원. 기대: 1.0 → 0.5 차감으로 2건 후
   잔여 0, 3회차는 대상 소진으로 스킵되고 `skippedCount >= 1`, `status = SUCCESS`
   (스킵은 실패가 아니다 — 05-01 결정 D).
6. **차감 후 관리자 충전** — 4번 이후 양(+) `ADMIN_ADJUST` 이력을 오늘 날짜로 넣고 `run()`.
   기대: 기준일이 가감일로 리셋돼 추가 차감 0건 (D-105 "충전 시점부터 2주 유예 재시작").
7. **재기동 중복 시뮬레이션** — `run(SCHEDULED, null)`과 `run(MANUAL, adminId)`를 같은 날 섞어 호출해도
   총 차감 건수가 1건이다(트리거 종류가 멱등성에 영향을 주지 않는다).

테스트 메서드명은 policies §4.3·D-106 문장을 딴 백틱 한국어 서술문으로 쓴다
(예: `` `같은 날 배치를 두 번 실행해도 이중 차감이 없다` ``, `` `배치가 6주 밀리면 다음 실행이 밀린 3회를 몰아서 차감한다` ``).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.batch.InactivityBatchIdempotencyTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.batch.InactivityBatchIdempotencyTest"` 종료 코드 0이고 7개 이상 테스트가 실행된다
- "같은 날 2회 실행 → 이력 1건" 테스트가 `PassTransaction` **건수**와 `remainingCount`를 모두 단언한다
- "6주 밀린 실행 → 3건 차감" 테스트가 존재한다
- 모든 테스트가 종료 전에 `잔여 = sumAmountByPassId` 불변식을 `isEqualByComparingTo`로 단언한다
- 스킵 발생 시나리오에서 `status`가 `SUCCESS`이고 `skippedCount >= 1`임을 단언한다
- `git diff --stat src/main/kotlin/`이 이 태스크에서 **변경 없음**이다 (프로덕션 코드 무변경 — 변경이
  필요했다면 SUMMARY에 원인과 함께 기록한다)
- 클래스에 `@Transactional`이 붙어 있지 않다
  </acceptance_criteria>
  <done>BATCH-04(멱등)와 캐치업이 실제 DB 위에서 반복 실행으로 증명된다</done>
</task>

<task type="auto">
  <name>Task 2: 만료 이용권 사용 불가 실증 테스트 (BATCH-03, 구현물 없음)</name>
  <files>src/test/kotlin/com/goldwrestling/batch/InactivityBatchExpiryVerificationTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/Pass.kt (`displayStatus`·`isExpired` — D-064·D-066 비교축)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt (`findDeductionCandidates`의 `endDate >= :classDate`)
    - src/test/kotlin/com/goldwrestling/reservation/MemberReservationServiceTest.kt (예약 거부 경로 단언 방식·픽스처)
    - src/test/kotlin/com/goldwrestling/pass/PassDisplayStatusTest.kt (표시 상태 단위테스트 선례)
    - docs/decisions.md D-064·D-066·D-107
  </read_first>
  <action>
`InactivityBatchExpiryVerificationTest`를 만들고, **BATCH-03이 기존 메커니즘으로 충족됨을 세 축에서**
단언한다. 클래스 KDoc 첫 문단에 "이 파일은 BATCH-03의 **유일한 산출물**이다 — D-107로 `EXPIRED`
영속화 배치를 만들지 않기로 확정했고(진실 원천 이원화 방지), 대신 기존 메커니즘이 실제로 만료
이용권의 사용을 막는지를 여기서 실증한다"를 남긴다.

1. **표시 상태(D-064)** — `endDate`가 어제인 `SESSION_PASS`의 `displayStatus(today)`가 `EXPIRED`다.
   `endDate == today`면 `USABLE`이다(D-066 종료일 포함 경계).
2. **예약 거부(Phase 4 경로)** — 만료된 `SESSION_PASS`만 가진 회원이 예약제 수업을 예약하면
   `InsufficientPassCountException`으로 거부되고, 잔여·예약 인원·이력이 요청 전과 같다.
   추가로 **"오늘은 유효하지만 수업날에는 만료되는 이용권"**으로 미래 수업을 예약하면 거부된다
   (유효기간 우회 차단, D-091 — 수업날 기준 판정).
3. **미사용 차감 제외(BATCH-02와 연결)** — 만료된 `SESSION_PASS`만 가진 회원은 기준일이 아무리
   오래됐어도 `InactivityBatchRunner.run()`에서 차감되지 않고, `batch_execution.deductedCount`에도
   잡히지 않는다(`processedMemberCount`에도 포함되지 않는다 — 대상 조회에서 이미 제외).
4. **만료 후 유효기간 수정으로 되살아남(D-056)** — 만료된 이용권의 `endDate`를 미래로 수정하면
   `displayStatus`가 `USABLE`로 돌아오고 배치 대상에 다시 포함된다. 이 케이스가 "상태를 저장하지
   않는" 설계의 이점(되돌리기 로직 불필요)을 증명한다 — 기존 기간 수정 경로(`AdminPassService`)를
   그대로 호출하고 새 코드를 만들지 않는다.

**이 태스크는 프로덕션 코드를 한 줄도 바꾸지 않는다.** 테스트가 실패하면 그것은 테스트 문제가
아니라 D-107 전제가 깨진 것이므로 — 임의로 프로덕션 코드를 고치지 말고 SUMMARY에 실패 내용을
기록하고 체크포인트로 사용자에게 알린다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.batch.InactivityBatchExpiryVerificationTest"</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew test --tests "com.goldwrestling.batch.InactivityBatchExpiryVerificationTest"` 종료 코드 0이고 5개 이상 테스트가 실행된다
- 테스트가 `displayStatus`·예약 거부·배치 제외 3축을 각각 단언한다
- "오늘은 유효하지만 수업날에는 만료" 케이스가 예약 거부로 단언된다
- 유효기간 수정으로 `USABLE` 복귀 + 배치 대상 재포함을 단언하는 테스트가 있다
- `git diff --stat src/main/kotlin/`이 이 태스크에서 변경 없음이다
- 클래스 KDoc에 `D-107`과 "BATCH-03" 문자열이 함께 등장한다
  </acceptance_criteria>
  <done>BATCH-03이 "구현물 없이 충족"임을 실행 가능한 근거(테스트)로 증명한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 반복 실행(cron 재기동·관리자 수동 실행) → 원장 | 멱등이 깨지면 회원 잔여가 조용히 과차감된다 |
| 만료 이용권 → 예약·차감 경로 | 만료 판정이 한 경로에서만 걸리면 다른 경로로 사용이 새어나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Tampering | 같은 날 중복 실행으로 이중 차감 | mitigate | 같은 날 2회·5회 실행 테스트로 이력 건수 고정. 실패 시 즉시 드러난다 |
| T-05-25 | Tampering | 만료 이용권으로 예약해 유효기간 우회 | mitigate | "오늘 유효·수업날 만료" 예약 거부 테스트(D-091 수업날 기준) |
| T-05-26 | Repudiation | 반복 실행 후 잔여와 이력이 어긋나 감사 불가 | mitigate | 모든 멱등 시나리오 종료 시 `잔여 = 이력 합계` 불변식 단언 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- `git diff --stat src/main/kotlin/`이 이 플랜 전체에서 변경 없음 (테스트 전용 플랜)
</verification>

<success_criteria>
- BATCH-04와 BATCH-03이 각각 실행 가능한 자동 검증으로 뒷받침된다
- 이후 phase가 배치 로직을 건드릴 때 이 테스트가 멱등성 회귀를 잡는다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-07-SUMMARY.md` when done.
청크 B(`feature/phase-05b-inactivity-batch`) 진행 중 — 이 플랜에서는 PR을 내지 않는다.
</output>
