---
phase: 05-batch
plan: 09
type: execute
wave: 9
depends_on: ["05-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/decisions.md
  - docs/glossary.md
  - docs/error-codes.md
autonomous: false
requirements: [BATCH-01, BATCH-02, BATCH-03, BATCH-04]

must_haves:
  truths:
    - "BATCH-01~04 각각에 대해 '무엇이 충족 근거인지'가 테스트 파일·클래스명 단위로 문서에 적혀 있다"
    - "전체 테스트 스위트가 그린이고 ktlint 위반이 없다"
    - "문서(decisions·glossary·error-codes)가 실제 구현과 일치한다"
    - "사람이 로컬에서 실제로 배치를 실행해 차감·이력·멱등을 눈으로 확인했다"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 성공 기준별 충족 근거"
      contains: "충족 근거"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BATCH-01~04 Complete 전환"
      contains: "BATCH-04 | Phase 5 | Complete"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "src/test/kotlin/com/goldwrestling/batch/*"
      via: "성공 기준마다 검증 테스트 클래스명을 인용"
      pattern: "Inactivity"
---

<objective>
Phase 5를 닫는다 — 요구사항 4종의 충족 근거를 테스트 단위로 대응시키고, 문서와 구현의 최종 정합을
확인한 뒤, 사람이 로컬에서 실제 동작을 확인한다.

Purpose: Phase 2~4가 같은 방식으로 마감했다(02-11·03-11·04-15). 자동 테스트는 "코드가 의도대로
동작한다"만 증명하고, **의도 자체가 정책과 맞는지**는 사람이 한 번 봐야 한다. 특히 이번 phase는
사람의 확인 없이 회원 잔여를 깎는 최초의 경로이므로, 실제 DB에서 차감·이력·멱등을 눈으로 확인한 뒤
닫는다.

Output: `.planning/ROADMAP.md`(충족 근거), `.planning/REQUIREMENTS.md`(Complete 전환), 문서 정합 결과,
사용자 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/glossary.md
@docs/decisions.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-batch/05-CONTEXT.md
@.planning/phases/05-batch/05-01-SUMMARY.md
@.planning/phases/05-batch/05-05-SUMMARY.md
@.planning/phases/05-batch/05-06-SUMMARY.md
@.planning/phases/05-batch/05-07-SUMMARY.md
@.planning/phases/05-batch/05-08-SUMMARY.md
</context>

<interfaces>
Phase 4 마감(04-15)이 남긴 형식을 따른다 — ROADMAP의 phase 절에 `**충족 근거** (05-09 phase 마감 검증):`
블록을 추가하고 성공 기준 번호마다 "어느 플랜이 만든 어느 테스트가 무엇을 증명하는지"를 한 줄로 적는다.

Phase 5 성공 기준(ROADMAP, 05-01이 3번을 D-107 문구로 정정했다):
1. `SESSION_PASS`가 기준일 기준 2주 미사용이면 1회 자동 차감, 이후 2주마다 반복, `INACTIVITY` 이력
2. `ON_LEAVE`·잔여 0·만료 이용권은 차감 대상 제외
3. 유효기간 만료 이용권은 예약에 쓸 수 없다 (기존 메커니즘 + 검증 테스트, D-107)
4. 같은 날 두 번 이상 실행해도 이중 차감 없음(멱등)

이번 phase가 만든 테스트 파일:
`batch/InactivityDueDateCalculatorTest`(05-03), `batch/InactivityBatchQueryTest`(05-04),
`batch/InactivityDeductionServiceTest`·`InactivityDeductionRaceTest`(05-05),
`batch/InactivityBatchRunnerTest`(05-06), `batch/InactivityBatchIdempotencyTest`·
`InactivityBatchExpiryVerificationTest`(05-07), `batch/AdminBatchControllerTest`(05-08),
`batch/BatchExecutionRepositoryTest`(05-02).

로컬 실행: `docker compose up -d` → `./gradlew bootRun`. 관리자 토큰은 시드 관리자 계정으로
`POST /api/admin/auth/login`(Phase 2)에서 발급받는다 — 실제 경로·요청 형태는 `docs/api/openapi.yaml`에서 확인한다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 요구사항 대응표 작성 + 전체 스위트 검증</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 절의 `**충족 근거**` 블록 — 형식 원본, Phase 5 절 전체)
    - .planning/REQUIREMENTS.md (배치 섹션 + Traceability 표)
    - .planning/phases/05-batch/05-03~05-08-SUMMARY.md (실제로 만들어진 테스트 클래스·케이스 수)
  </read_first>
  <action>
`./gradlew ktlintFormat` → `./gradlew build`를 실제로 실행해 전체 스위트가 그린인지 확인한다
(결과 요약 — 총 테스트 수·실패 0 — 을 SUMMARY에 남긴다). 실패가 있으면 여기서 멈추고 원인을 보고한다.

`.planning/ROADMAP.md` Phase 5 절에 Phase 4와 같은 형식으로 `**충족 근거** (05-09 phase 마감 검증):`
블록을 추가한다. 성공 기준 1~4 각각에 대해 "플랜 번호 — 테스트 클래스가 무엇을 증명하는지"를 한 줄로 쓴다.
예: "4. 05-07 — `InactivityBatchIdempotencyTest`가 같은 날 2회·5회 실행 시 이력 1건, 6주 캐치업 3건,
매 실행 후 잔여 = 이력 합계를 검증".
또한 Phase 5 절의 체크박스(`- [ ] **Phase 5: 배치**`)를 완료로 바꾸고 플랜 목록의 체크박스도 전부 채운다.
Progress 표의 Phase 5 행을 `9/9 | Complete | <오늘 날짜>`로 갱신한다.

`.planning/REQUIREMENTS.md`에서 BATCH-01~04의 체크박스를 `[x]`로 바꾸고 Traceability 표의 4행을
`Complete`로 갱신한다. BATCH-03 행에는 D-107 근거(검증 테스트로 충족)가 05-01에서 이미 반영돼 있어야
한다 — 없으면 여기서 채운다.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
- `./gradlew build` 종료 코드 0 (ktlint + 컴파일 + 전체 테스트)
- `.planning/ROADMAP.md` Phase 5 절에 `**충족 근거**` 블록이 있고 성공 기준 1~4 각각에 테스트 클래스명이 인용돼 있다
- `.planning/ROADMAP.md` Progress 표의 Phase 5 행이 `9/9`·`Complete`다
- `grep -c "BATCH-0[1-4] | Phase 5 | Complete" .planning/REQUIREMENTS.md`가 4
- `grep -c "\- \[ \] \*\*BATCH-0" .planning/REQUIREMENTS.md`가 0
  </acceptance_criteria>
  <done>요구사항 4종이 각각 "무엇이 증명하는가"와 함께 완료로 기록된다</done>
</task>

<task type="auto">
  <name>Task 2: 문서 ↔ 구현 최종 정합</name>
  <files>docs/decisions.md, docs/glossary.md, docs/error-codes.md</files>
  <read_first>
    - docs/decisions.md (05-01이 기록한 결정 A~E + 05-04가 추가했을 수 있는 프로젝션 결정)
    - docs/glossary.md `## 배치 (Phase 5)` 섹션
    - docs/error-codes.md (신규 코드 추가 여부 확인)
    - src/main/kotlin/com/goldwrestling/batch/ (실제 구현된 클래스·enum 이름 전체)
    - src/main/resources/db/migration/V9__*.sql (실제 컬럼명)
  </read_first>
  <action>
05-01이 **계획으로** 기록한 결정 A~E와 **실제 구현**을 대조해 어긋난 곳을 문서 쪽에서 정정한다
(코드가 정책과 어긋나면 코드를 고치는 것이 원칙이지만, 여기서 대상은 재량 결정의 세부 — 실행 중
더 나은 형태로 바뀐 것이면 문서를 실제에 맞춘다. 정책(policies §4.3)과 어긋나는 변경이면 문서를
고치지 말고 사용자에게 보고한다).

확인 항목:
- `batch_execution` 실제 컬럼(V9) ↔ 결정 D 기술 내용
- `BatchExecutionStatus`·`BatchTrigger` 실제 상수 ↔ glossary 등록 내용
- cron 시각·API 경로 실제 값 ↔ 결정 E
- 05-04가 프로젝션 인터페이스 도입 결정을 기록했는지(안 했으면 여기서 3~4줄로 추가)
- `ErrorCode`에 배치 관련 코드가 추가됐는지 — 추가됐다면 `docs/error-codes.md`에 같은 행이 있는지
  (`grep -c` 로 enum 상수 수와 표 행 수를 비교). 추가되지 않았으면 "신규 코드 없음"을 SUMMARY에 남긴다
- `docs/policies.md` §4.3과 구현이 어긋나는 부분이 있는지 (있으면 **코드가 틀린 것** — 사용자에게 보고)

또한 `docs/decisions.md`에 이번 phase 실행 중 새로 내려진 결정(예: 테스트 구조·프로젝션 문법 대안 채택)이
SUMMARY에는 있는데 decisions에는 없는 것이 있으면 여기서 3~4줄로 추가한다.
  </action>
  <verify>
    <automated>grep -c "batch_execution\|inactivity-runs" docs/decisions.md</automated>
  </verify>
  <acceptance_criteria>
- `docs/decisions.md`에 기록된 `batch_execution` 컬럼 목록이 V9 마이그레이션의 실제 컬럼과 일치한다
- `docs/decisions.md`에 기록된 cron 표현식·API 경로가 `InactivityBatchScheduler`·`AdminBatchController`의 실제 값과 문자열 단위로 일치한다
- `docs/glossary.md`의 `BatchTrigger`·`BatchExecutionStatus` 상수 목록이 실제 enum과 일치한다
- `ErrorCode` enum의 배치 관련 상수 수와 `docs/error-codes.md` 표 행 수가 일치한다(둘 다 0이어도 통과)
- 불일치를 발견해 문서를 고쳤다면 무엇을 왜 고쳤는지가 SUMMARY에 있다
  </acceptance_criteria>
  <done>문서만 읽는 사람과 코드만 읽는 사람이 같은 결론에 도달한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 로컬 실제 실행 확인 (사람)</name>
  <files>(없음 — 확인 전용)</files>
  <read_first>
    - docs/api/openapi.yaml (`/api/admin/batch/inactivity-runs` 요청 형태, 관리자 로그인 경로)
    - .planning/phases/05-batch/05-08-SUMMARY.md (엔드포인트·응답 필드)
  </read_first>
  <action>
오케스트레이터가 아래 `<how-to-verify>` 10단계를 **직접 실행**하고(앱 기동·관리자 로그인·psql 데이터
준비·curl 호출·psql 확인), 각 단계의 실제 응답 본문과 psql 출력을 사용자에게 그대로 보여 준 뒤
승인을 받는다. 사용자가 "승인"으로 답하기 전에는 phase를 닫지 않는다. 정책(policies §4.3)과 다르게
동작하는 지점이 관찰되면 임의로 코드를 고치지 말고 그 사실을 함께 보고한다.
  </action>
  <what-built>
매일 새벽 4시(`Asia/Seoul`) 자동 실행되는 2주 미사용 차감 배치 + 관리자 수동 실행 API
(`POST /api/admin/batch/inactivity-runs`). 차감은 만료 임박한 `SESSION_PASS` 한 장에서 1회씩,
잔여가 모자라면 잔여만큼만. 모든 차감은 `INACTIVITY` 사유의 시스템 주체 이력으로 남고,
실행마다 `batch_execution`에 요약이 기록된다.
  </what-built>
  <how-to-verify>
오케스트레이터가 아래를 실제로 수행하고 결과를 사용자에게 보고한 뒤 승인을 받는다.

1. `docker compose up -d` → `./gradlew bootRun` (앱이 정상 기동하는지 — `@EnableScheduling` 도입 후 첫 기동)
2. 관리자로 로그인해 access 토큰을 얻는다 (`docs/api/openapi.yaml`의 관리자 로그인 경로 사용)
3. psql로 테스트용 데이터를 만든다: `ACTIVE` 회원 1명 + 잔여 3.0인 `SESSION_PASS` 1장
   (`created_at`을 **20일 전**으로, `start_date`/`end_date`는 유효하게). 예약·가감 이력은 만들지 않는다
4. `curl -X POST .../api/admin/batch/inactivity-runs -H "Authorization: Bearer <token>"` 호출
   → 응답의 `deductedCount`가 **1**, `status`가 `SUCCESS`인지 확인
5. psql로 확인: 해당 `pass.remaining_count`가 **2.0**, `pass_transaction`에 `reason='INACTIVITY'`,
   `amount=-1.0`, `admin_id IS NULL AND member_id IS NULL`인 행이 1건
6. **같은 요청을 한 번 더** 호출 → `deductedCount`가 **0**, `pass_transaction` 행 수가 여전히 1건
   (이중 차감 없음 — BATCH-04)
7. 회원 토큰(또는 토큰 없이)으로 같은 엔드포인트를 호출 → 403(또는 401) + `application/problem+json`
8. psql로 `batch_execution` 조회 → 실행 2건이 `trigger_type='MANUAL'`, `triggered_by_admin_id`가
   채워진 상태로 기록됐는지 확인
9. 회원 상태를 `ON_LEAVE`로 바꾼 뒤 다시 호출 → `deductedCount`가 0이고 잔여가 그대로인지 확인 (BATCH-02)
10. 확인이 끝나면 테스트 데이터를 정리할지 사용자에게 묻는다 (04-15에서 사용자가 검증 데이터를
    보존하라고 지시한 선례가 있다 — 임의로 지우지 않는다)

각 단계의 실제 응답·쿼리 결과를 사용자에게 보여 주고, 정책과 다르게 보이는 지점이 있으면 그것을 함께 보고한다.
  </how-to-verify>
  <acceptance_criteria>
- 10단계 전부의 실제 결과(응답 본문·psql 출력)가 사용자에게 보고됐다
- 4·6단계에서 `deductedCount`가 각각 1과 0으로 관찰됐다
- 5단계에서 `admin_id`·`member_id`가 모두 NULL인 `INACTIVITY` 이력이 확인됐다
- 9단계에서 `ON_LEAVE` 회원이 차감되지 않음이 확인됐다
- 사용자가 "승인"으로 응답했다
  </acceptance_criteria>
  <verify>
    <human-check>위 10단계의 실제 결과를 사용자가 확인하고 "승인"으로 답한다 (자동 검증은 Task 1의 `./gradlew build`가 담당)</human-check>
  </verify>
  <resume-signal>"승인"이라고 답하거나, 다르게 동작한 부분을 알려 주세요</resume-signal>
  <done>사용자가 실제 실행 결과를 확인하고 승인해 Phase 5를 닫을 수 있다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자동 테스트 → 정책 충족 판단 | 테스트가 그린이어도 정책 해석이 틀렸을 수 있다 — 사람의 확인이 마지막 방어선이다 |
| 로컬 검증 데이터 → 실제 DB | 검증용 회원·이용권이 정리되지 않으면 이후 개발에 혼선을 준다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-32 | Repudiation | 요구사항이 근거 없이 완료 처리됨 | mitigate | 성공 기준마다 테스트 클래스명을 인용한 충족 근거 블록을 ROADMAP에 남긴다 |
| T-05-33 | Tampering | 문서와 구현이 갈라진 채 phase가 닫혀 다음 phase가 틀린 전제를 밟음 | mitigate | Task 2가 decisions·glossary·error-codes를 실제 코드와 문자열 단위로 대조 |
| T-05-34 | Tampering | 로컬 검증 데이터가 남아 이후 배치 실행에 섞임 | accept | 사용자에게 정리 여부를 확인받는다(04-15 선례 — 임의 삭제 금지) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과 (전체 스위트)
- `docker compose up -d && ./gradlew bootRun` 정상 기동 — 스케줄러 도입 후 첫 실기동 확인
- 사용자 승인 완료
</verification>

<success_criteria>
- BATCH-01~04가 각각 자동 검증 근거와 함께 완료로 기록된다
- 사람이 실제 DB에서 차감·이력·멱등·인가를 눈으로 확인했다
- 문서와 구현이 일치해 Phase 6이 틀린 전제를 밟지 않는다
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-09-SUMMARY.md` when done.
**청크 B 마지막 플랜** — `./gradlew ktlintFormat` → `./gradlew build` 그린 확인 후 `create-pr` 스킬로
dev 대상 PR을 만든다(제목: `feat: Phase 5b 미사용 차감 배치 — 차감 실행·멱등·스케줄러·관리자 API`,
본문 개요에 "Phase 5 중 2/2 청크"). openapi.yaml은 05-08에서 이미 재생성됐다.
**머지 버튼은 누르지 않는다** (D-084).
</output>
