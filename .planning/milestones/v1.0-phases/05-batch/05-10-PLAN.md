---
phase: 05-batch
plan: 10
type: execute
wave: 10
gap_closure: true
depends_on: []
files_modified:
  - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
  - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
  - src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt
  - docs/policies.md
  - docs/glossary.md
  - docs/decisions.md
autonomous: true
requirements: [BATCH-02]
must_haves:
  truths:
    - "휴회(ON_LEAVE)에서 어떤 상태로 벗어나든 그 시각이 returnedFromLeaveAt에 기록된다"
    - "ON_LEAVE→INACTIVE→ACTIVE 경로로 복귀한 회원은 휴회 기간이 소급 차감되지 않는다"
    - "ON_LEAVE가 아닌 상태에서 시작한 전이는 returnedFromLeaveAt을 건드리지 않는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      provides: "휴회 이탈 시각 기록 조건"
      contains: "previousStatus == MemberStatus.ON_LEAVE && newStatus != MemberStatus.ON_LEAVE"
    - path: "src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt"
      provides: "휴회 우회 복귀 경로의 소급 차감 방지 실증"
      min_lines: 120
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      to: "Member.returnedFromLeaveAt"
      via: "changeStatus에서 휴회 이탈 시각 대입"
      pattern: "returnedFromLeaveAt = OffsetDateTime.now\\(clock\\)"
    - from: "src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt"
      to: "InactivityBatchRunner.run"
      via: "복귀 후 배치 실행 결과로 소급 차감 부재를 단언"
      pattern: "inactivityBatchRunner\\.run\\("
---

<objective>
CR-04를 닫는다 — `AdminMemberService.changeStatus`가 `ON_LEAVE → ACTIVE` **직행**에서만
`returnedFromLeaveAt`을 기록해, 실제 운영 경로인 `ON_LEAVE → INACTIVE → ACTIVE`·
`ON_LEAVE → PENDING → ACTIVE`에서는 기준일이 휴회 전으로 되돌아가 휴회 기간 전체가 소급 차감된다
(6개월 휴회 복귀 시 최대 12회). 기록 조건을 "**휴회에서 벗어났을 때**"로 확장한다.

Purpose: BATCH-02("`ON_LEAVE` 기간은 자동 차감 대상에서 제외된다")의 두 번째 축(복귀 시각 리셋)이
특정 전이에서만 동작하는 것을 고쳐 policies §4.3의 명시적 예외를 모든 경로에서 성립시킨다.
Output: 조건 확장 1건 + 상태 전이 회귀 테스트 4종 + 우회 복귀 경로 소급 차감 방지 통합테스트 +
문서 3종(policies §4.3 후보 ③ 문구, glossary "휴회 복귀 시각", decisions D-111) 정정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@docs/glossary.md
@.planning/phases/05-batch/05-GAP-CONTEXT.md
@.claude/skills/add-domain-test/SKILL.md

**확정 설계(재검토 금지):** `05-GAP-CONTEXT.md` §2.3 — 조건을
`previousStatus == ON_LEAVE && newStatus != ON_LEAVE`로 확장한다. 다른 방식(휴회 기간 테이블,
범용 `status_changed_at`)은 D-105·D-111이 이미 기각했다.

**커밋된 V9 마이그레이션은 수정하지 않는다.** V9 13~15행 주석("ON_LEAVE → ACTIVE 전이에서만
기록한다")이 이 변경으로 사실과 달라지지만, 파일을 고치면 Flyway 체크섬 불일치로 기동이 막힌다 —
정정 사실은 05-11이 만드는 V10 헤더 주석에 남긴다(이 플랜에서는 손대지 않는다).

<interfaces>
현재 `AdminMemberService.changeStatus`(137~141행)의 기록 분기와 협력자:
- `member.status`(var, `MemberStatus`), `member.returnedFromLeaveAt`(var, `OffsetDateTime?`)
- `clock: Clock`은 이미 생성자에 주입돼 있다 — 새로 추가할 필요 없다
- `MemberStatus`: `PENDING` / `ACTIVE` / `ON_LEAVE` / `INACTIVE`
- 배치 기준일 후보 ③은 `MemberRepository.findReturnedFromLeaveTimestamps(memberIds)`가
  `MemberTimestampProjection`(`getMemberId()`/`getTimestamp()`)으로 읽어 간다 — 이 플랜은
  쿼리를 바꾸지 않는다
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 휴회 이탈 전이 전체에서 복귀 시각을 기록한다</name>
  <files>
    src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt,
    src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
  </files>
  <read_first>
    src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt,
    src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt,
    src/main/kotlin/com/goldwrestling/member/Member.kt,
    docs/policies.md,
    docs/conventions.md
  </read_first>
  <behavior>
    - `ON_LEAVE → ACTIVE`: `returnedFromLeaveAt`이 `Clock` 기준 현재 시각으로 채워진다 (기존 계약 유지)
    - `ON_LEAVE → INACTIVE`: 채워진다 (신규)
    - `ON_LEAVE → PENDING`: 채워진다 (신규)
    - `ON_LEAVE → ON_LEAVE`(같은 상태 재지정): 갱신되지 않는다 (경계)
    - `INACTIVE → ACTIVE`, `PENDING → ACTIVE`, `ACTIVE → ON_LEAVE`: 여전히 갱신되지 않는다 (회귀)
  </behavior>
  <action>
    `AdminMemberService.changeStatus`의 기록 분기 조건을
    `previousStatus == MemberStatus.ON_LEAVE && newStatus == MemberStatus.ACTIVE` 에서
    `previousStatus == MemberStatus.ON_LEAVE && newStatus != MemberStatus.ON_LEAVE` 로 바꾼다.
    대입식(`member.returnedFromLeaveAt = OffsetDateTime.now(clock)`)과 위치(`member.status = newStatus`
    대입 직후)는 그대로 둔다.

    같은 메서드 KDoc의 "휴회 복귀 시각은 여기서만 기록된다" 문단을 고쳐 쓴다 — 기록 시점이
    "`ACTIVE`로 복귀했을 때"가 아니라 "**`ON_LEAVE`에서 벗어났을 때**"이며, 그 이유
    (휴회가 끝난 시각이 곧 2주 유예가 다시 시작되는 시점이고, `ON_LEAVE→INACTIVE` 시점에 값이
    채워져야 이후 `INACTIVE→ACTIVE` 우회 경로에서도 기준일이 살아 있다는 것)를 명시한다.
    D-111·CR-04를 근거로 인용한다.

    `MemberStatusChangeTest`에 백틱 한국어 테스트 4개를 추가한다(기존 `returnedFromLeaveAt` 테스트
    블록 바로 뒤, 같은 픽스처 헬퍼 재사용):
    `ON_LEAVE에서 INACTIVE로 바꾸면 returnedFromLeaveAt이 채워진다`,
    `ON_LEAVE에서 PENDING으로 바꾸면 returnedFromLeaveAt이 채워진다`,
    `ON_LEAVE에서 다시 ON_LEAVE로 바꾸면 returnedFromLeaveAt은 갱신되지 않는다`,
    `ON_LEAVE에서 INACTIVE를 거쳐 ACTIVE로 돌아와도 최초 이탈 시각이 유지된다`(두 번째 전이가
    값을 덮어쓰지 않음을 단언).
    기존 테스트 `INACTIVE 회원을 ACTIVE로 바꿔도 returnedFromLeaveAt은 여전히 null이다`와
    `ACTIVE 회원을 ON_LEAVE로 바꾸면 returnedFromLeaveAt은 갱신되지 않는다`는 **그대로 통과해야
    한다** — 수정하지 말고 회귀 방어로 남긴다.
  </action>
  <verify>
    <automated>./gradlew cleanTest test --tests 'com.goldwrestling.member.MemberStatusChangeTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'previousStatus == MemberStatus.ON_LEAVE && newStatus != MemberStatus.ON_LEAVE' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 1
    - `grep -c 'newStatus == MemberStatus.ACTIVE) {' src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 결과에 복귀 시각 기록 분기가 남아 있지 않다 (온보딩 검사 분기만 남는다)
    - `grep -c 'fun \`ON_LEAVE에서' src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt` 가 4 이상
    - 위 gradle 명령이 BUILD SUCCESSFUL이고, 기존 `returnedFromLeaveAt` 테스트 6종이 모두 통과한다
  </acceptance_criteria>
  <done>휴회에서 벗어나는 모든 전이에서 복귀 시각이 기록되고, 휴회와 무관한 전이는 값을 건드리지 않는다는 것이 테스트로 고정됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 우회 복귀 경로의 소급 차감 부재를 배치 실행으로 실증하고 문서를 정정한다</name>
  <files>
    src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt,
    docs/policies.md,
    docs/glossary.md,
    docs/decisions.md
  </files>
  <read_first>
    src/test/kotlin/com/goldwrestling/batch/InactivityBatchIdempotencyTest.kt,
    src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt,
    src/main/kotlin/com/goldwrestling/batch/InactivityBatchRunner.kt,
    src/main/kotlin/com/goldwrestling/batch/InactivityDueDateCalculator.kt,
    docs/policies.md,
    docs/glossary.md,
    docs/decisions.md
  </read_first>
  <behavior>
    - 200일 전 등록된 `SESSION_PASS`(잔여 5.0)를 가진 회원이 휴회 → 비활성 → 활성으로 복귀하고
      복귀 13일 뒤 배치를 돌리면 `deductedCount`가 0이고 잔여가 5.0 그대로다
    - 같은 회원을 복귀 15일 뒤로 옮겨 배치를 돌리면 정확히 1회만 차감된다(휴회 이전 200일이
      소급되지 않는다)
    - 대조군: 휴회를 거치지 않고 200일 방치된 회원은 같은 실행에서 차감이 발생한다
      (기준일 리셋이 "휴회 이탈"에서만 일어난다는 것을 보인다)
  </behavior>
  <action>
    `src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt`를 새로 만든다.
    애노테이션 조합은 `InactivityBatchIdempotencyTest`와 동일하게
    `@SpringBootTest` + `@Import(TestcontainersConfiguration::class, TestClockConfiguration::class)`로
    맞춰 컨텍스트 캐시를 공유한다(conventions §10.1). 클래스 레벨 `@Transactional`을 붙이지 않는다 —
    `deductOnce`가 실제로 커밋돼야 배치 결과를 관찰할 수 있다. `@AfterEach`에서 이 클래스가 만든
    데이터만 지운다.

    고유 픽스처 대역을 이 클래스 전용으로 배정한다: `KAKAO_ID_BASE = 9_730_000_000L`,
    `KAKAO_ID_MAX = KAKAO_ID_BASE + 10_000L`, `ADMIN_LOGIN_PREFIX = "admin-inactivity-leave-return-"`.
    정리 쿼리는 하한만 쓰지 말고 **범위**로 좁힌다(`kakao_id >= :base and kakao_id < :max`) —
    05-REVIEW.md WR-07이 지적한 "남의 데이터까지 지우는 `>=` 정리"를 이 신규 클래스에서 반복하지
    않는다. 회원·이용권·이력 픽스처는 `BatchFixtures.member`/`BatchFixtures.sessionPass`를 쓴다.

    시각 이동은 `(clock as MutableTestClock).setTo(...)`로 한다(`Thread.sleep` 금지, conventions §10.3).
    상태 전이는 반드시 `AdminMemberService.changeStatus`를 통해 일으킨다 — 엔티티에 직접
    `returnedFromLeaveAt`을 대입하면 이번 수정의 회귀 방어가 되지 않는다.
    배치 실행은 `InactivityBatchRunner.run(BatchTrigger.MANUAL, adminId)`로 하고, 생성된
    `BatchExecution` id를 모아 `@AfterEach`에서 지운다.
    잔여 검증은 `isEqualByComparingTo`를 쓴다(D-016, `equals` 금지).

    문서 3종을 같은 작업에서 정정한다:
    - `docs/policies.md` §4.3 기준일 후보 목록의 "`휴회(ON_LEAVE)` → `ACTIVE` **복귀일**" 항목을
      "`휴회(ON_LEAVE)`에서 **벗어난 시각**(어떤 상태로 나가든) — 휴회가 끝나면 2주 유예가 새로
      시작된다"로 고친다. 이유(`ON_LEAVE→INACTIVE→ACTIVE` 같은 운영 경로에서 휴회 기간이 소급
      차감되는 것을 막는다)를 한 줄 덧붙인다.
    - `docs/glossary.md` "휴회 복귀 시각 `returnedFromLeaveAt`" 행의 설명 "`ON_LEAVE` → `ACTIVE`
      전이 시각"을 "`ON_LEAVE`에서 벗어난 전이의 시각(대상 상태 무관)"으로 고친다.
    - `docs/decisions.md` D-111에 정정 항목을 덧붙인다: 날짜(2026-08-16), 05-REVIEW.md CR-04를
      근거로 기록 조건을 `newStatus != ON_LEAVE`로 확장했다는 것, 확장 이유, 그리고 커밋된 V9
      주석은 수정하지 않고 V10 주석에 정정을 남긴다는 것.

    `docs/policies.md`는 이 저장소의 스펙 최상위 문서다 — 이 편집은 스펙 문구 변경이므로
    SUMMARY의 완료 보고에 "policies.md §4.3을 고쳤다"는 사실과 그 이유를 반드시 한 줄로 밝힌다
    (CLAUDE.md 문서 우선순위 규칙).
  </action>
  <verify>
    <automated>./gradlew cleanTest test --tests 'com.goldwrestling.batch.InactivityLeaveReturnTest'</automated>
  </verify>
  <acceptance_criteria>
    - 새 파일 `src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt`가 존재하고 테스트 3개 이상을 포함한다
    - `grep -c 'kakao_id < :max' src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt` 가 1 이상 (정리 쿼리가 범위로 한정됨)
    - `grep -c '9_730_000_000L' src/test/kotlin/com/goldwrestling/batch/InactivityLeaveReturnTest.kt` 가 1 (다른 배치 테스트와 겹치지 않는 전용 대역)
    - `grep -v '^#' docs/policies.md | grep -c '벗어난 시각'` 가 1 이상
    - `grep -c 'ON_LEAVE.*ACTIVE.*전이 시각' docs/glossary.md` 가 0
    - `docs/decisions.md`의 D-111 절에 `2026-08-16` 정정 항목이 있다
    - 위 gradle 명령이 BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>휴회 우회 복귀 경로에서 휴회 기간이 소급 차감되지 않는다는 것이 실제 배치 실행으로 실증됐고, policies·glossary·decisions가 코드와 일치한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 HTTP → `AdminMemberService.changeStatus` | 관리자가 임의 상태 전이를 요청한다 (`/api/admin/**` = `hasRole("ADMIN")`) |
| `changeStatus` → 배치 판정(`returnedFromLeaveAt`) | 상태 전이 시각이 회원 잔여 차감의 기준일이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05D-10-01 | Tampering | `changeStatus`의 기준일 리셋 | mitigate | 시각은 요청 값이 아니라 주입된 `Clock`에서만 얻는다(`OffsetDateTime.now(clock)`) — 클라이언트가 기준일을 지정할 경로가 없다 |
| T-05D-10-02 | Elevation of Privilege | 상태 전이 API | mitigate | 기존 `/api/admin/**` URL 인가 규칙(D-040)을 그대로 상속한다. 이 플랜은 SecurityConfig를 건드리지 않는다 |
| T-05D-10-03 | Repudiation | 휴회 이탈 기록 | accept | `returnedFromLeaveAt`은 마지막 이탈 시각만 남기고 전이 이력 테이블을 만들지 않는다(D-111 기각안) — 잔여 변경 자체는 `PassTransaction`이 전량 기록하므로 감사 추적은 유지된다 |
| T-05D-10-SC | Tampering | 패키지 설치 | n/a | 이 플랜은 새 의존성을 추가하지 않는다 (npm/pip/cargo 설치 없음) |
</threat_model>

<verification>
- `./gradlew cleanTest test --tests 'com.goldwrestling.member.MemberStatusChangeTest' --tests 'com.goldwrestling.batch.InactivityLeaveReturnTest'` 통과
  (`cleanTest`를 반드시 붙인다 — 없으면 Gradle이 `UP-TO-DATE`로 이전 결과를 재사용해 "돌렸다"는 착각을 준다)
- `./gradlew ktlintFormat` 후 `./gradlew compileKotlin compileTestKotlin` 통과
- `grep -rn "ON_LEAVE.*→.*ACTIVE.*복귀일" docs/` 결과 0건
</verification>

<success_criteria>
- `ON_LEAVE`에서 벗어나는 4가지 전이(→`ACTIVE`/`INACTIVE`/`PENDING`, 그리고 같은 상태 재지정 제외)의 기록 여부가 테스트로 고정됐다
- `ON_LEAVE → INACTIVE → ACTIVE` 복귀 회원의 배치 실행 `deductedCount`가 0이다
- policies §4.3·glossary·D-111이 새 동작과 일치한다
- 커밋된 V1~V9 마이그레이션 파일이 수정되지 않았다 (`git diff --name-only`에 `db/migration/V9` 없음)
</success_criteria>

<output>
Create `.planning/phases/05-batch/05-10-SUMMARY.md` when done.
완료 보고에는 (1) `이번에 쓴 기술` 섹션(CLAUDE.md 학습 모드)과 (2) `docs/policies.md`를 고쳤다는
사실·이유를 반드시 포함한다.
</output>
