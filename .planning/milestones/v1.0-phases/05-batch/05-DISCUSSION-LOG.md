# Phase 5: 배치 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-15
**Phase:** 5-배치
**Areas discussed:** 휴회 복귀 후 차감 기준, 밀린 차감 캐치업, 만료 처리 방식, 배치 실행·운영 방식, (추가 A) 여러 장 보유 시 판정·차감 단위, (추가 B) 잔여 0.5의 부분 차감

---

## 논의 형식

사용자가 4개 영역 전부 선택 + 추가 의제 A·B를 얹고, 6건 모두에 대한 입장을 선제 제시하며
"반대 근거 없으면 확정"을 요청했다. Claude가 코드·정책 대조 후 2·3·4·B는 반대 근거 없음으로
그대로 확정, 1·2·A에 얽힌 보완 지점 4건을 AskUserQuestion으로 확정했다.

## 사용자 선제 입장 (전부 채택, 1·2·A는 아래 보완 반영)

1. **휴회 복귀**: 기준일 = max(마지막 출석일, 마지막 예약 수업일, 복귀일, 이용권 시작일→**등록일로 보정**), 복귀 시 2주 유예 재시작, 휴회 기간 테이블 대신 상태 전환 시각 기록 (→ D-105)
2. **캐치업**: "존재해야 할 차감 수 − 실제 이력 수" 상태 기반 부족분 채우기 = 멱등성 구현 (→ D-106)
3. **만료**: D-064 유지, EXPIRED 영속화 없음, BATCH-03은 기존 메커니즘 충족으로 정정 (→ D-107)
4. **실행·운영**: @Scheduled + 실행 이력 테이블 + 관리자 수동 실행 API, 외부 트리거 M7 (→ D-108)
- **A**: 판정 회원 단위, 차감은 만료 임박 SESSION_PASS 한 장 1회 (→ D-109)
- **B**: 잔여 < 차감량이면 잔여만큼만 차감 (0.5 → 0) (→ D-109)

---

## 보완 질문 1 — 활동 없는 이용권의 기준일 fallback

| Option | Description | Selected |
|--------|-------------|----------|
| 등록일 (추천) | D-027 원문 유지. 등록일 ≥ 시작일이라 항상 관대, 과거 시작일 등록(D-055) 직후 즉시 차감 구조적 불가능 | ✓ |
| 시작일 유지 | 제시안 그대로. 과거 시작일 등록 시 첫 배치가 밀린 주기를 몰아서 차감하는 것을 의도된 동작으로 봄 | |

**User's choice:** 등록일 (추천)

## 보완 질문 2 — 잔여 0 제외 기간의 부족분 소급

| Option | Description | Selected |
|--------|-------------|----------|
| +가감일도 기준일 후보 (추천) | 양(+) ADMIN_ADJUST 일자를 max()에 추가. 충전 시점부터 유예 재시작 — "제외 기간엔 부채가 안 쌓인다"를 복귀일과 같은 원리로 일관 구현 | ✓ |
| 소급 차감 허용 | 충전하면 밀린 부족분 즉시 회수. 관리자가 서비스로 준 횟수가 곧바로 깎이는 운영상 황당함 | |

**User's choice:** +가감일도 기준일 후보 (추천)

## 보완 질문 3 — 취소된 예약의 활동 인정

| Option | Description | Selected |
|--------|-------------|----------|
| 취소 예약 제외 (추천) | 취소되지 않은 예약만 인정 — D-027의 "차감된 사용" 근거와 일치. 예약-취소 반복 회원은 미사용 판정 | ✓ |
| 취소 예약도 포함 | 예약 행위 자체를 활동으로. 쿼리는 단순하지만 환불받은 예약이 차감을 막는 비대칭 | |

**User's choice:** 취소 예약 제외 (추천)

## 보완 질문 4 — 활동으로 인정하는 예약 종류 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 모든 예약 (추천) | SESSION·LESSON 무관 인정. D-027 문구 그대로, 실제로 체육관에 나오는 회원은 차감하지 않는다는 취지 | ✓ |
| SESSION 예약만 | SESSION_PASS 미사용은 SESSION 수업 사용 여부로만 판정. 1:1만 다니는 회원의 횟수권은 계속 차감 | |

**User's choice:** 모든 예약 (추천)

---

## Claude's Discretion

- PassTransaction 시스템 주체 표현 (V8 CHECK 조정, 새 마이그레이션)
- 회원 상태 전환 시각 컬럼 설계, 배치 트랜잭션 경계, 실행 이력 테이블 스키마·네이밍
- cron 시각, 수동 실행 API 경로·DTO·에러코드
- 차감 조건부 UPDATE 경쟁 패배 시 스킵 (다음 실행이 상태 기반으로 자연 보정)

## Deferred Ideas

- 외부 트리거 (M7 재검토 — 사용자 지정)
- 배치 이벤트 알림 (요구사항 없음, 필요 확인 시 Phase 6 알림 체계에)
- 분산 락 (단일 인스턴스 전제 깨질 때)
- 휴회 토글 유예 리셋 관대함 보정 (악용 관찰 시)
