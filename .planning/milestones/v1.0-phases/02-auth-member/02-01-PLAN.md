---
phase: 02-auth-member
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glossary.md
  - docs/decisions.md
  - docs/error-codes.md
  - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
  - src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04, MEMBER-01, MEMBER-03]

must_haves:
  truths:
    - "이번 phase가 쓸 모든 신규 개념(kakaoId, loginId, passwordHash, rejectionReason, RefreshToken, PrincipalType, onboardingCompleted)이 docs/glossary.md에 등재되어 있다"
    - "인증·인가·회원 상태 실패에 대응하는 에러코드 10종이 ErrorCode enum과 docs/error-codes.md 표에 1:1로 존재한다"
    - "이번 phase에서 planner가 확정한 설계 결정(D-036~D-047)이 docs/decisions.md에 기록되어 있다"
    - "ErrorCode enum에 코드를 추가하고 docs/error-codes.md 갱신을 빠뜨리면 테스트가 실패한다"
  artifacts:
    - path: "docs/glossary.md"
      provides: "인증·회원 신규 개념 네이밍 (CLAUDE.md 규칙 3의 선행 조건)"
      contains: "RefreshToken"
    - path: "docs/error-codes.md"
      provides: "FE 분기 계약 — 인증/인가/회원 에러코드 표"
      contains: "REFRESH_TOKEN_INVALID"
    - path: "docs/decisions.md"
      provides: "D-036~D-047 설계 결정 기록"
      contains: "D-047"
    - path: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      provides: "신규 에러코드 enum 상수 10종 + defaultStatus"
      contains: "MEMBER_NOT_ACTIVE"
    - path: "src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt"
      provides: "enum ↔ 문서 표 동기화 자동 검증"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      to: "docs/error-codes.md"
      via: "ErrorCodeRegistryTest가 enum 이름 전수를 문서 본문에서 찾는다"
      pattern: "ErrorCode\\.entries"
---

<objective>
이번 phase가 코드에서 쓸 **이름과 에러 계약을 먼저 확정**한다. CLAUDE.md 규칙 3(네이밍은 glossary.md만
따른다)과 D-028(에러코드 추가 시 같은 PR에서 error-codes.md 갱신)은 "코드보다 문서가 먼저"를 요구하므로,
이 플랜이 Wave 1에서 그 선행 조건을 만든다.

또한 planner가 CONTEXT.md의 "Claude's Discretion" 범위에서 확정한 설계 결정 12건(D-036~D-047)을
`docs/decisions.md`에 기록해, 이후 모든 플랜이 그 결정을 근거로 구현하게 한다.

Purpose: 이후 9개 플랜이 이름·에러코드를 각자 즉흥적으로 정하는 것을 막는다.
Output: glossary/decisions/error-codes 3개 문서 갱신 + ErrorCode enum 확장 + 동기화 검증 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<commit_policy>
이 레포는 **사용자가 명시적으로 요청할 때만 커밋한다** (CLAUDE.md 커밋·브랜치 규칙).
execute-plan 워크플로가 자동 커밋을 요구하더라도 `git add`/`git commit`/`git push`를 실행하지 않는다.
파일 저장까지만 하고, 완료 보고에 "커밋 대기 중인 변경 파일 목록"을 나열한 뒤 멈춘다.
</commit_policy>

<context>
@CLAUDE.md
@docs/glossary.md
@docs/decisions.md
@docs/error-codes.md
@docs/conventions.md
@.planning/phases/02-auth-member/02-CONTEXT.md
@src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
@src/main/kotlin/com/goldwrestling/common/error/DomainException.kt
</context>

<interfaces>
<!-- 기존 계약 — 그대로 확장한다 -->

src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt:
  enum class ErrorCode(val defaultStatus: HttpStatus) — 현재 7개 상수
  (VALIDATION_FAILED, MALFORMED_REQUEST, RESOURCE_NOT_FOUND, METHOD_NOT_ALLOWED,
   UNSUPPORTED_MEDIA_TYPE, NOT_ACCEPTABLE, INTERNAL_ERROR)

src/main/kotlin/com/goldwrestling/common/error/DomainException.kt:
  abstract class DomainException(errorCode: ErrorCode, message: String, status: HttpStatus = errorCode.defaultStatus)
  → 이후 플랜의 인증·회원 예외가 이 클래스를 상속한다 (이 플랜에서는 예외 클래스를 만들지 않는다)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: glossary.md에 인증·회원 신규 개념 등재</name>
  <files>docs/glossary.md</files>
  <read_first>
    - docs/glossary.md (현재 표 구조·금지어 규칙 확인)
    - docs/policies.md §5, §5.1, §5.2 (회원 상태·온보딩·승인/거절 용어 원문)
    - .planning/phases/02-auth-member/02-CONTEXT.md (D-032~D-035에서 등장한 용어)
  </read_first>
  <action>
`docs/glossary.md`에 "## 인증·회원 (Phase 2)" 섹션을 `## 회원 상태 (MemberStatus)` 섹션 바로 뒤에 추가하고,
아래 개념을 한국어 / 코드 네이밍 / 설명 3열 표로 등재한다. DB 컬럼명이 따로 있는 항목은 설명에 `snake_case`를 병기한다.

- 카카오 사용자 식별자 → `kakaoId` (DB `kakao_id`). 카카오가 발급하는 회원 고유 번호. 회원 1명당 1개, 중복 가입 방지의 기준
- 리프레시 토큰 → `RefreshToken` (DB `refresh_token`). access 토큰 재발급용 자격증명. DB에 해시로 저장
- 액세스 토큰 → `accessToken` / 리프레시 토큰 값 → `refreshToken`
- 토큰 회전 → `rotation`. refresh를 쓸 때마다 새 refresh를 발급하고 기존 것을 폐기하는 방식
- 토큰 주체 종류 → `PrincipalType` (`MEMBER` / `ADMIN`). Member/Admin이 별도 테이블이라 토큰이 어느 쪽을 가리키는지 구분한다
- 인증 주체 → `AuthenticatedPrincipal`. 요청을 수행하는 회원/관리자를 나타내는 값 객체 (엔티티가 아니다)
- 관리자 로그인 ID → `loginId` (DB `login_id`)
- 비밀번호 해시 → `passwordHash` (DB `password_hash`). 평문 비밀번호는 어디에도 저장하지 않는다
- 거절 사유 → `rejectionReason` (DB `rejection_reason`). 가입 거절 시 기록 (D-034)
- 온보딩 → `Onboarding`. 최초 로그인 회원이 실명·전화번호를 입력하는 절차 (policies §5.1)
- 온보딩 완료 여부 → `onboardingCompleted`. 별도 상태 컬럼이 아니라 `name`·`phoneNumber` 입력 여부로 판정한다 (D-025)
- 회원 상태 게이트 → `MemberStateGate`. 엔드포인트가 요구하는 회원 상태를 DB 현재 값 기준으로 검사하는 컴포넌트 (D-033 노트)

"## 기타 규칙"의 금지어 목록에 인증 영역 금지어를 한 줄 추가한다:
`Session`(우리 체계는 STATELESS라 세션 개념이 없다 — `ClassSession`과도 혼동됨), `User`(→ `Member`), `Account`(→ `Member`/`Admin`), `Role` 단독 사용(→ `PrincipalType`).
  </action>
  <verify>
    <automated>grep -c 'RefreshToken\|PrincipalType\|onboardingCompleted\|rejectionReason\|passwordHash\|kakaoId\|loginId\|MemberStateGate\|AuthenticatedPrincipal' docs/glossary.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kakaoId' docs/glossary.md` 가 1 이상
    - `grep -c 'RefreshToken' docs/glossary.md` 가 1 이상
    - `grep -c 'PrincipalType' docs/glossary.md` 가 1 이상
    - `grep -c 'rejectionReason' docs/glossary.md` 가 1 이상
    - `grep -c 'onboardingCompleted' docs/glossary.md` 가 1 이상
    - `grep -c 'passwordHash' docs/glossary.md` 가 1 이상
    - `grep -c 'MemberStateGate' docs/glossary.md` 가 1 이상
    - `docs/glossary.md`에 `## 인증·회원 (Phase 2)` 제목 줄이 존재한다
    - 기존 섹션(핵심 엔티티 / 수업 종류 / 회원 상태 / 차감 사유)의 행이 삭제되거나 변경되지 않았다 (`git diff docs/glossary.md`가 추가 위주여야 한다)
  </acceptance_criteria>
  <done>이후 플랜이 코드에 쓸 인증·회원 이름이 전부 glossary에 있고, 금지어 규칙이 인증 영역까지 확장되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 인증·회원 에러코드 10종 추가 (ErrorCode enum + error-codes.md + 동기화 테스트)</name>
  <files>src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt, docs/error-codes.md, src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (기존 7개 상수·KDoc 스타일)
    - docs/error-codes.md (표 형식·폴백 규칙 문장)
    - src/main/kotlin/com/goldwrestling/common/error/GlobalExceptionHandler.kt (code 주입 지점, 시큐리티 예외 되던지기 규칙)
    - src/test/kotlin/com/goldwrestling/common/error/GlobalExceptionHandlerTest.kt (테스트 스타일·백틱 한국어 메서드명)
    - docs/conventions.md §8, §10.0
  </read_first>
  <action>
`ErrorCode.kt`에 아래 10개 상수를 기존 7개 뒤에 `// --- 인증·회원 (Phase 2) ---` 구분 주석 없이(ktlint import 규칙과 무관하나 enum 사이 주석은 허용) 각 상수마다 KDoc 한 줄을 붙여 추가한다. 괄호 안은 `defaultStatus`:

- `UNAUTHENTICATED(HttpStatus.UNAUTHORIZED)` — 인증이 필요하거나 access 토큰이 없음/만료/위조
- `ACCESS_DENIED(HttpStatus.FORBIDDEN)` — 인증은 됐으나 역할(ROLE_MEMBER/ROLE_ADMIN)이 부족
- `INVALID_CREDENTIALS(HttpStatus.UNAUTHORIZED)` — 관리자 loginId/비밀번호 불일치 (어느 쪽이 틀렸는지 구분해 노출하지 않는다)
- `REFRESH_TOKEN_INVALID(HttpStatus.UNAUTHORIZED)` — refresh 토큰이 없음/만료/폐기됨/재사용 감지됨
- `KAKAO_AUTH_FAILED(HttpStatus.UNAUTHORIZED)` — 카카오가 인가 코드 교환 또는 사용자 조회를 거부함
- `KAKAO_UNAVAILABLE(HttpStatus.BAD_GATEWAY)` — 카카오 API가 응답하지 않거나 5xx를 반환
- `MEMBER_NOT_FOUND(HttpStatus.NOT_FOUND)` — 대상 회원 없음
- `MEMBER_NOT_ACTIVE(HttpStatus.FORBIDDEN)` — 상태 게이트 위반. 회원 상태가 요구 조건(`ACTIVE`)이 아님
- `ONBOARDING_ALREADY_COMPLETED(HttpStatus.CONFLICT)` — 이미 온보딩을 마친 회원의 온보딩 재제출 (프로필 수정은 v2 PROF-01)
- `MEMBER_STATE_CONFLICT(HttpStatus.CONFLICT)` — 승인 대상이 아니거나 허용되지 않는 상태 전이

클래스 KDoc의 "여기 정의된 7개는 …" 문장을 현재 개수·범위에 맞게 고친다 (공통 7개 + 인증·회원 10개).

`docs/error-codes.md`에는 `## 인증·회원 코드 (Phase 2)` 섹션을 "폴백 규칙" 문단 **앞**에 추가하고,
기존 표와 동일한 4열(코드 / HTTP 상태 / 의미 / 발생 지점)로 위 10개를 적는다. "발생 지점"에는 이후 플랜에서
만들 클래스명을 적는다 — `ProblemDetailAuthenticationEntryPoint`, `ProblemDetailAccessDeniedHandler`,
`AdminAuthService`, `TokenService`, `KakaoApiClient`, `AdminMemberService`, `MemberStateGate`, `MemberProfileService`.
문서 맨 아래의 "도메인 코드(예: RESERVATION_FULL …)는 …" 문장은 그대로 둔다.

`ErrorCodeRegistryTest.kt`는 스프링 컨텍스트 없이 도는 순수 단위테스트로 만든다.
`java.io.File("docs/error-codes.md").readText()`로 문서를 읽고(테스트 작업 디렉터리는 프로젝트 루트다),
`ErrorCode.entries` 전수에 대해 문서 본문에 백틱으로 감싼 `` `<NAME>` `` 문자열이 포함되는지 검증한다.
테스트 메서드명은 백틱 한국어 서술문으로 쓴다 — 예: `모든 ErrorCode 는 docs 에러코드 표에 등재되어 있다`.
반대 방향(문서에만 있고 enum에 없는 코드)도 한 테스트로 확인한다: 문서에서 정규식으로 대문자·언더스코어
백틱 토큰을 뽑아 `ErrorCode` 이름 집합에 없는 것이 없어야 한다 — 단 `TZ` 같은 오탐을 피하기 위해
`^[A-Z][A-Z_]{4,}$` 형태만 대상으로 하고, 표 안의 예외 클래스명 열은 백틱을 쓰므로 오탐 후보가 된다.
오탐을 없애기 위해 **역방향 검사는 `## 공통 코드`·`## 인증·회원 코드` 섹션의 표 첫 번째 열만** 파싱한다
(줄이 `| \`` 로 시작하는 행에서 첫 백틱 토큰만 추출).
  </action>
  <verify>
    <automated>./gradlew test --tests "*ErrorCodeRegistry*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "*ErrorCodeRegistry*"` exits 0
    - `grep -c 'UNAUTHENTICATED\|ACCESS_DENIED\|INVALID_CREDENTIALS\|REFRESH_TOKEN_INVALID\|KAKAO_AUTH_FAILED\|KAKAO_UNAVAILABLE\|MEMBER_NOT_FOUND\|MEMBER_NOT_ACTIVE\|ONBOARDING_ALREADY_COMPLETED\|MEMBER_STATE_CONFLICT' src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt` 가 10 이상
    - `grep -c 'KAKAO_UNAVAILABLE' docs/error-codes.md` 가 1 이상
    - `ErrorCode.KAKAO_UNAVAILABLE.defaultStatus` 가 `HttpStatus.BAD_GATEWAY` 다 (소스에 `KAKAO_UNAVAILABLE(HttpStatus.BAD_GATEWAY)` 문자열이 존재)
    - `ErrorCode.MEMBER_STATE_CONFLICT.defaultStatus` 가 `HttpStatus.CONFLICT` 다
    - ErrorCode에 상수를 하나 임시로 추가하고 문서를 갱신하지 않으면 `./gradlew test --tests "*ErrorCodeRegistry*"` 가 실패한다 (실행 후 되돌릴 것)
    - 기존 7개 상수의 이름·defaultStatus가 변경되지 않았다
  </acceptance_criteria>
  <done>인증·회원 에러 계약이 코드와 문서 양쪽에 존재하고, 둘이 어긋나면 빌드가 깨진다.</done>
</task>

<task type="auto">
  <name>Task 3: 이번 phase 설계 결정 D-036~D-047을 decisions.md에 기록</name>
  <files>docs/decisions.md</files>
  <read_first>
    - docs/decisions.md (D-030~D-035 형식: 번호. 제목 / 날짜 / 결정 / 이유 / 기각 대안, 그리고 D-036 플레이스홀더 행)
    - .planning/phases/02-auth-member/02-CONTEXT.md (Claude's Discretion 목록 — 이 결정들의 위임 근거)
    - .planning/phases/02-auth-member/02-RESEARCH.md Open Questions 1~3, Common Pitfalls 2
  </read_first>
  <action>
`docs/decisions.md` 맨 아래의 `## D-036. (예시 — 다음 결정을 여기에 추가)` 플레이스홀더 블록을 **삭제하고**,
그 자리에 아래 12건을 기존과 같은 형식(제목 / `- 2026-08 / 결정` / `- 이유:` / `- 기각 대안:` / 필요 시 `- 유의:`)으로 추가한다.
날짜는 전부 `2026-08`. 마지막에 `## D-048. (예시 — 다음 결정을 여기에 추가)` 플레이스홀더를 다시 남긴다.

- **D-036. refresh 토큰 표현·저장: 랜덤 문자열 + SHA-256 해시 저장, 사용 시 회전**
  결정: refresh 토큰은 JWT가 아니라 256비트 난수를 Base64URL로 인코딩한 문자열이고, DB에는 원문이 아니라 SHA-256 hex(64자)를 `token_hash`에 저장한다. 사용할 때마다 새 토큰을 발급하고 기존 행은 `revoked_at`을 채워 폐기한다. 이미 폐기된 토큰이 다시 제시되면 재사용으로 보고 해당 주체의 모든 refresh를 폐기한다. 회원당 여러 refresh 행을 허용해 멀티 디바이스 로그인을 지원한다.
  이유: refresh는 DB 조회가 전제라 자기기술적 JWT일 이유가 없다. 원문 저장은 DB 유출이 곧 계정 탈취다. 비밀번호와 달리 이미 고엔트로피라 BCrypt 같은 느린 해시가 필요 없다.
  기각 대안: refresh도 JWT(폐기·회전 추적을 위해 어차피 DB 행이 필요해 이점이 없음), 원문 저장(유출 즉시 탈취), 회원당 1개만 허용(모바일·PC 동시 로그인이 서로를 로그아웃시킴).

- **D-037. refresh_token의 주체 참조: nullable FK 쌍 + CHECK 제약**
  결정: `refresh_token`은 `member_id`·`admin_id` 두 FK를 모두 nullable로 두고 `CHECK ((member_id IS NULL) <> (admin_id IS NULL))`로 정확히 하나만 채워지도록 강제한다.
  이유: Member/Admin이 별도 테이블이라 단일 FK로 표현할 수 없는데, `principal_type` 문자열 + `principal_id` 조합은 FK 무결성을 잃는다. 이 레포는 `admin_branch`에서도 FK를 명시해 왔다.
  기각 대안: `principal_type`+`principal_id` 문자열 조합(참조 무결성 없음 — 삭제된 회원의 토큰 행이 남음), 테이블 2개로 분리(`member_refresh_token`/`admin_refresh_token` — 회전·폐기 로직이 통째로 두 벌이 됨).

- **D-038. 관리자 시드: ApplicationRunner 멱등 시드 (Flyway INSERT 금지)**
  결정: 관리자 계정은 `AdminSeeder`(`ApplicationRunner`)가 기동 시 `login_id` 존재 여부를 확인해 없을 때만 INSERT한다. 자격은 `ADMIN_SEED_LOGIN_ID`·`ADMIN_SEED_PASSWORD`·`ADMIN_SEED_NAME` 환경변수로 주입하고, 비어 있으면 시드를 건너뛰고 경고 로그만 남긴다(앱은 정상 기동).
  이유: 비밀번호는 환경마다 달라야 하는 시크릿이다. Branch 시드(D-031)처럼 Flyway INSERT로 넣으면 플레이스홀더가 비어 있는 채로 한 번 적용되는 순간 깨진 해시가 그 환경 DB에 영구히 박히고, 커밋된 마이그레이션은 수정 금지라 새 UPDATE 마이그레이션이 필요해진다.
  기각 대안: Flyway 시드 + `spring.flyway.placeholders`(위 이유), 관리자 가입 API(D-026이 셀프 가입 없음으로 확정).

- **D-039. 감사 시각(created_at): JPA Auditing 미도입, 애플리케이션이 Clock으로 명시 세팅**
  결정: `@EnableJpaAuditing`/`@CreatedDate`를 도입하지 않는다. `Member`·`Admin`·`RefreshToken`의 `createdAt`은 엔티티 생성 시 서비스가 주입받은 `Clock`으로 채운다. DB의 `DEFAULT now()`는 그대로 두어 방어선으로 남긴다. (Phase 1에서 이월된 결정 — D-030 유의 참조)
  이유: 감사 시각이 필요한 필드가 아직 소수이고, `Clock` 주입은 이미 프로젝트 규약(conventions §5)이라 테스트에서 시각을 고정하기 쉽다. Auditing은 커스텀 `DateTimeProvider` 빈 배선이 추가로 필요하고 시각 고정 경로가 한 겹 늘어난다.
  기각 대안: `@CreatedDate` + `dateTimeProviderRef`(배선 추가 대비 이득이 작음), DB DEFAULT만 쓰고 매핑하지 않기(회원 목록에 가입 신청 시각을 못 보여줌).

- **D-040. 인가 구현: URL/역할은 authorizeHttpRequests, 상태 게이트는 MemberStateGate**
  결정: 역할 구분(공개 / `ROLE_MEMBER` / `ROLE_ADMIN`)은 `SecurityConfig`의 `authorizeHttpRequests` URL 규칙으로만 표현한다. "회원 상태가 `ACTIVE`여야 한다" 같은 조건은 `MemberStateGate` 컴포넌트가 서비스 계층에서 DB 현재 상태로 검사하고 `DomainException`을 던진다. `@EnableMethodSecurity`/`@PreAuthorize`는 쓰지 않는다.
  이유: 상태 규칙에는 엔드포인트별 예외가 있다(`GET /api/members/me`와 온보딩 제출은 `PENDING`도 접근 가능해야 한다). 이를 URL 규칙이나 전역 메서드 시큐리티로 표현하면 예외를 또 열어주는 규칙이 겹쳐 오히려 복잡해진다. 서비스 계층 검사는 도메인 예외 경로를 그대로 타서 `ProblemDetail` 형식이 자동으로 통일된다.
  기각 대안: `@PreAuthorize`에 SpEL로 상태 조건 넣기(예외 엔드포인트마다 규칙 중복, 에러 응답이 시큐리티 경로로 빠져 `code` 주입이 갈라짐), 필터에서 상태까지 차단(거절 안내 화면 조회가 401로 막힘).
  유의: 서비스 계층 검사는 "빠뜨릴 수 있는" 방식이다. 회원 대상 엔드포인트를 새로 만들 때마다 `MemberStateGate` 호출이 필요한지 확인한다.

- **D-041. 전화번호: 하이픈 제거 후 숫자만 저장, UNIQUE 제약 없음**
  결정: 온보딩 요청은 `010-1234-5678`·`01012345678` 두 형태를 모두 받고(형식 검증 정규식 `^01[016789]-?\d{3,4}-?\d{4}$`), 서버가 하이픈을 제거해 숫자만 `member.phone_number`에 저장한다. `phone_number`에 DB UNIQUE 제약은 걸지 않는다.
  이유: 저장 형식이 섞이면 "전화번호 부분 일치 검색"(D-035)이 입력 형태에 따라 결과가 달라진다. UNIQUE는 policies §5.1이 요구하지 않고, 운영 규모가 작아 중복은 관리자가 승인 단계에서 걸러낼 수 있다.
  기각 대안: 하이픈 포함 저장(검색 불안정), 입력을 숫자만으로 제한(FE·사용자 입력 관습과 어긋남), UNIQUE 제약(가족 공유폰 등 정당한 중복을 막고, 제거하려면 새 마이그레이션이 필요).
  유의: 중복 가입 방지가 필요해지면 v2에서 UNIQUE 제약을 새 마이그레이션으로 추가한다 — 컬럼 추가보다 싼 변경이다.

- **D-042. 온보딩 재제출 금지**
  결정: 온보딩 제출 API는 회원 상태가 `PENDING`이면서 온보딩 미완료일 때만 허용한다. 이미 완료했으면 `ONBOARDING_ALREADY_COMPLETED`(409)로 거부한다.
  이유: 이 API를 열어두면 회원이 온보딩 경로로 이름·전화번호를 자유롭게 바꿀 수 있어, "프로필 수정은 관리자만"(MVP, 셀프 수정은 v2 PROF-01)이라는 결정이 우회된다. 이름·전화번호는 관리자가 회원을 식별하는 기준(policies §5.1)이라 임의 변경은 운영 사고다.
  기각 대안: 멱등하게 덮어쓰기 허용(PROF-01 우회), 완료 후에도 값이 같을 때만 허용(우회 여지는 남고 규칙만 복잡).

- **D-043. 거절 사유 노출 범위: 회원에게는 rejected 불리언만**
  결정: 회원 대상 응답(카카오 로그인 응답, `GET /api/members/me`)에는 `rejected: Boolean`만 담고 `rejectionReason` 원문은 담지 않는다. 사유 원문은 관리자 회원 상세 응답에서만 반환한다.
  이유: policies §5.2가 요구하는 것은 "거절 안내 화면 대상으로 식별"까지다. 사유는 관리자가 내부 운영 메모로 쓸 수 있어(예: "타 회원 신고 이력") 그대로 노출하면 분쟁 소지가 된다.
  기각 대안: 회원에게 사유 원문 노출(내부 메모 유출), 사유를 아예 기록하지 않음(D-034가 요구하는 `INACTIVE` 유래 구분이 불가능).

- **D-044. 상태 변경 시 refresh 무효화 + PENDING 복귀 시 거절 사유 초기화**
  결정: 관리자의 회원 상태 변경·거절로 **전환 후 상태가 `ACTIVE`가 아니게 되면** 해당 회원의 폐기되지 않은 refresh 토큰을 전부 폐기한다. 승인(`PENDING`→`ACTIVE`)에서는 폐기하지 않는다. 상태를 `PENDING`으로 되돌리는 재신청 처리(D-034)에서는 `rejection_reason`을 `NULL`로 초기화한다.
  이유: D-033이 요구하는 강제 로그아웃의 실현부다. `PENDING` 복귀 시 사유가 남아 있으면 재신청 회원에게 거절 안내 화면이 계속 뜬다.
  기각 대안: 모든 상태 변경에서 폐기(승인 직후 재로그인을 강요), refresh를 두고 access 만료만 기다림(최대 30분 창이 남음 — D-033이 명시적으로 기각).

- **D-045. 비밀번호 해싱: DelegatingPasswordEncoder (기본 bcrypt)**
  결정: `PasswordEncoderFactories.createDelegatingPasswordEncoder()`를 `PasswordEncoder` 빈으로 등록한다. 저장 값에는 `{bcrypt}` 접두가 붙으므로 `admin.password_hash`는 `VARCHAR(100)`으로 잡는다.
  이유: 저장 값 자체가 알고리즘을 기술하므로, 나중에 알고리즘을 바꿔도 기존 해시를 그대로 검증할 수 있다. `spring-boot-starter-security`에 이미 포함돼 추가 비용이 0이다.
  기각 대안: `BCryptPasswordEncoder` 직접 등록(알고리즘 교체 시 전 계정 재설정 필요), 직접 salt+hash 구현(적응형 work factor·타이밍 안전 비교를 직접 다뤄야 함).

- **D-046. 카카오 연동 세부: state는 FE 책임, redirect_uri는 서버 환경변수 고정**
  결정: CSRF 방지용 `state` 파라미터의 생성·검증은 FE가 담당한다. BE는 인가 코드만 받는다. `redirect_uri`는 요청 본문이 아니라 서버 환경변수(`KAKAO_REDIRECT_URI`)에서 읽어 카카오 토큰 교환에 사용한다.
  이유: BE가 STATELESS(세션 없음)라 `state`를 저장해 둘 곳이 없다. `redirect_uri`를 요청에서 받으면 공격자가 자기 서버를 넣어 인가 코드를 유도할 수 있고, 카카오 콘솔 등록값과 서버 설정이 갈라진다.
  기각 대안: BE가 state를 Redis 등에 저장해 검증(인프라 추가), redirect_uri를 요청 파라미터로 수용(오픈 리다이렉트 유사 위험).

- **D-047. 신규 회원 지점 배정: 기본 지점 이름 설정으로 조회 배정**
  결정: 카카오 최초 로그인으로 `Member`를 만들 때 `goldwrestling.default-branch-name`(기본값 `송파점`) 설정으로 `Branch`를 조회해 배정한다. 해당 지점이 없으면 로그인은 서버 오류로 실패한다.
  이유: `member.branch_id`는 NOT NULL인데 회원 스스로 지점을 고르는 UI가 v1에 없다(MVP는 송파점 단일 — D-031). 지점명을 설정으로 빼두면 2호점이 생겨도 코드 수정 없이 환경별로 다르게 둘 수 있다.
  기각 대안: 코드에 `송파점` 하드코딩(지점 추가 시 코드 수정), `branch.id = 1` 가정(identity 시퀀스에 의존하는 취약한 전제), 회원이 가입 시 지점 선택(v1 스코프 밖 — CROSS-01은 v2).
  </action>
  <verify>
    <automated>grep -c '^## D-0' docs/decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## D-036\.' docs/decisions.md` 가 1
    - `grep -c '^## D-047\.' docs/decisions.md` 가 1
    - `grep -c '^## D-048\. (예시' docs/decisions.md` 가 1
    - `grep -c '예시 — 다음 결정을 여기에 추가' docs/decisions.md` 가 1 (플레이스홀더가 하나만 남아 있다)
    - D-036부터 D-047까지 번호가 빠짐없이 존재한다: `for n in $(seq -w 36 47); do grep -q "^## D-0$n\." docs/decisions.md || echo "missing D-0$n"; done` 이 아무 출력도 내지 않는다
    - 기존 D-001~D-035 블록의 본문이 변경되지 않았다
  </acceptance_criteria>
  <done>이후 플랜이 "왜 이렇게 구현하는가"를 docs/decisions.md에서 찾을 수 있고, 사용자가 결정을 한곳에서 검토할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 클라이언트(에러 응답) | 에러 본문에 내부 정보가 실려 나가는 경계 |
| 문서 ↔ 코드 | FE가 신뢰하는 계약(`code` 값)이 코드와 갈라질 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `INVALID_CREDENTIALS` 에러 의미 정의 | mitigate | "아이디가 없음"과 "비밀번호가 틀림"을 구분하지 않는 단일 코드로 정의하고, 그 의도를 error-codes.md 표의 "의미" 열에 명시 (Task 2) — 계정 열거 공격 차단 |
| T-02-02 | Tampering | ErrorCode enum ↔ docs/error-codes.md 드리프트 | mitigate | `ErrorCodeRegistryTest`가 양방향 동기화를 빌드에서 강제 (Task 2). FE가 존재하지 않는 코드로 분기하거나, 새 코드가 문서 없이 배포되는 것을 막는다 |
</threat_model>

<verification>
1. `./gradlew ktlintFormat && ./gradlew build` 통과 (Docker Desktop 실행 필요 — Testcontainers)
2. `./gradlew test --tests "*ErrorCodeRegistry*"` 통과
3. `git diff --stat` 결과가 위 files_modified 5개 파일로만 구성된다 (코드 구현 파일이 섞이지 않았다)
</verification>

<success_criteria>
- glossary.md에 인증·회원 신규 개념 12종 이상이 등재되어 있다
- ErrorCode enum이 17개 상수(공통 7 + 인증·회원 10)를 갖고, 전부 error-codes.md 표에 존재한다
- ErrorCodeRegistryTest가 enum↔문서 드리프트를 실제로 검출한다 (임시 상수 추가 실험으로 확인)
- decisions.md에 D-036~D-047 12건이 기존 형식으로 기록되어 있다
- `./gradlew build` 통과
</success_criteria>

<output>
Create `.planning/phases/02-auth-member/02-01-SUMMARY.md` when done.
완료 보고에 CLAUDE.md 학습 모드의 `이번에 쓴 기술` 섹션을 포함하고, **커밋하지 않은 채** 변경 파일 목록을 제시한다.
</output>
