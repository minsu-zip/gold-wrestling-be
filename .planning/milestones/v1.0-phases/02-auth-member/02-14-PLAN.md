---
phase: 02-auth-member
plan: 14
type: execute
wave: 12
depends_on: ["02-13"]
gap_closure: true
autonomous: true
requirements: [MEMBER-01, MEMBER-02, MEMBER-03]
files_modified:
  - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt
  - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
  - src/test/kotlin/com/goldwrestling/member/MemberSpecificationTest.kt
  - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
  - docs/decisions.md

must_haves:
  truths:
    - "검색어가 하이픈만으로 이루어져도 전화번호가 있는 전 회원이 반환되지 않는다"
    - "이름이 공백만인 회원은 엔티티 판정과 승인 대기 목록 쿼리 양쪽에서 동일하게 온보딩 미완료로 취급된다"
    - "온보딩(실명·전화번호)을 마치지 않은 회원은 상태 변경 API로도 ACTIVE가 될 수 없다"
    - "승인 API가 강제하는 규칙을 상태 변경 API가 우회시키지 않는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt"
      provides: "정규화 결과가 빈 문자열이면 전화번호 술어를 만들지 않는 검색 + trim 기반 온보딩 판정"
      contains: "trim"
    - path: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt"
      provides: "changeStatus의 ACTIVE 전환 시 온보딩 완료 서버측 강제"
      contains: "MemberStateConflictException"
    - path: "src/test/kotlin/com/goldwrestling/member/MemberSpecificationTest.kt"
      provides: "하이픈 전용 검색어 케이스 + 공백 이름 픽스처 정합성 검증"
    - path: "src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt"
      provides: "온보딩 미완료 회원의 ACTIVE 전환 409 검증"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (changeStatus)"
      to: "src/main/kotlin/com/goldwrestling/member/Member.kt (isOnboardingCompleted)"
      via: "ACTIVE 전환 전 서버측 정책 검사"
      pattern: "isOnboardingCompleted"
    - from: "src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt (onboardingCompleted)"
      to: "src/main/kotlin/com/goldwrestling/member/Member.kt (isOnboardingCompleted)"
      via: "동일 판정 규칙 — MemberSpecificationTest의 정합성 테스트가 어긋남을 잡는다"
      pattern: "criteriaBuilder\\.trim"
---

<objective>
02-REVIEW.md의 Warning 3건(WR-03, WR-04, WR-05)을 닫는다. 셋 다 "서버가 강제한다고 문서·주석에
써 둔 규칙이 실제로는 특정 입력에서 무너진다"는 같은 종류의 결함이다.

- **WR-03**: `approve()`는 "온보딩 완료 + PENDING"을 서버에서 강제하며 그 근거로 "목록 필터는 화면
  노출 범위일 뿐이라 API 직접 호출로 우회 가능하므로 정책은 항상 서버에서 강제한다"(T-02-37)를
  명시했다. 그런데 같은 파일의 `changeStatus()`는 `newStatus=ACTIVE`에 아무 검사가 없어, 이름·전화번호가
  없는 회원을 ACTIVE로 만드는 우회 경로가 열려 있다. 관리자가 회원을 이름·전화번호로 식별한다는
  전제(D-025)가 깨진 ACTIVE 회원이 생길 수 있다.
- **WR-04**: `keywordContains`는 와일드카드 이스케이프로 "검색어 하나로 전체 회원 매칭"을 막았다고
  주장하지만, 검색어 `"-"`는 blank가 아니라 통과한 뒤 `PhoneNumberNormalizer.normalize("-")` → `""`가
  되어 전화번호 조건이 `LIKE '%%'`가 된다 — 전화번호가 non-null인 **모든 회원**이 반환된다.
- **WR-05**: `Member.isOnboardingCompleted()`는 `isNullOrBlank()`(공백만 = 미완료)를 쓰지만
  `MemberSpecifications.onboardingCompleted`는 `notEqual(..., "")`(빈 문자열만 미완료)를 쓴다.
  `name = " "` 행이 생기면 승인 대기 목록(policies §5.1)이 엔티티 판정과 어긋난다.
  KDoc은 "반드시 같은 판정 규칙"이라고 명시했지만 실제로는 다르다.

Purpose: 셋 다 정책·주석과 실제 동작의 불일치다. 이 프로젝트에서 "코드와 문서가 다르면 코드가 틀린 것"이다.
Output: 세 결함 수정 + 각각을 재현하는 회귀 테스트 + 결정 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.claude/skills/add-domain-test/SKILL.md
@.planning/phases/02-auth-member/02-REVIEW.md

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않아도 되도록 필요한 계약을 여기 적는다. -->

MemberSpecifications (object, 수정 대상)
- `private const val LIKE_ESCAPE_CHAR = '\\'`
- `fun keywordContains(keyword: String?): Specification<Member>?`
  현재: blank면 null → `trimmed` → `escapedNameKeyword`(lowercase) / `escapedPhoneKeyword`
  → Specification 안에서 `namePredicate`(lower(name) like) 와 `phonePredicate`(phoneNumber like)를
  항상 만들고 `criteriaBuilder.or(namePredicate, phonePredicate)` 반환
- `fun hasStatus(status: MemberStatus?): Specification<Member>?`  (변경 없음)
- `fun onboardingCompleted(flag: Boolean?): Specification<Member>?`
  현재: `isNotNull(name) and notEqual(name, "")` , `isNotNull(phoneNumber) and notEqual(phoneNumber, "")`
- `private fun escapeLikeWildcards(value: String): String`

PhoneNumberNormalizer (object, 변경 없음)
- `const val PHONE_NUMBER_PATTERN = "^01[016789]-?\\d{3,4}-?\\d{4}$"`
- `fun normalize(raw: String): String = raw.trim().replace("-", "")`

Member (엔티티, 변경 없음)
- `fun isOnboardingCompleted(): Boolean = !name.isNullOrBlank() && !phoneNumber.isNullOrBlank()`

AdminMemberService (수정 대상)
- `@Transactional fun approve(memberId: Long): MemberDetailResponse`
  — `status != PENDING` → `MemberStateConflictException("승인 대기 상태의 회원만 승인할 수 있습니다.")`
  — `!isOnboardingCompleted()` → `MemberStateConflictException("회원이 이름·전화번호를 등록해야 승인할 수 있습니다.")`
- `@Transactional fun changeStatus(memberId: Long, newStatus: MemberStatus): MemberDetailResponse`
  — 현재 검사 없음. `PENDING` 복귀 시 `rejectionReason = null`, `newStatus != ACTIVE` 면 `tokenService.revokeAllForMember(memberId)`

MemberStateConflictException(message: String) : DomainException(ErrorCode.MEMBER_STATE_CONFLICT, message)
ErrorCode.MEMBER_STATE_CONFLICT → HttpStatus.CONFLICT (409). 이미 존재하므로 ErrorCode·error-codes.md 변경 불필요.

AdminMemberController.changeStatus → `PATCH /api/admin/members/{memberId}/status`, body `UpdateMemberStatusRequest(status)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 검색·온보딩 판정 술어 교정 (WR-04, WR-05)</name>

  <files>
    src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt
    src/test/kotlin/com/goldwrestling/member/MemberSpecificationTest.kt
  </files>

  <read_first>
    - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt (수정 대상 — `keywordContains` 31~52행, `onboardingCompleted` 71~87행, 두 KDoc의 현재 주장)
    - src/main/kotlin/com/goldwrestling/member/Member.kt (`isOnboardingCompleted`의 `isNullOrBlank` 판정 — 맞춰야 할 기준)
    - src/main/kotlin/com/goldwrestling/member/PhoneNumberNormalizer.kt (`normalize`가 하이픈만 입력에 대해 `""`를 반환한다)
    - src/test/kotlin/com/goldwrestling/member/MemberSpecificationTest.kt (수정 대상 — 픽스처 m1~m6 구성과 기존 단언을 깨지 않고 확장해야 한다)
    - .planning/phases/02-auth-member/02-REVIEW.md WR-04·WR-05 절
  </read_first>

  <behavior>
    - `keywordContains("-")` 로 조회하면 이름에 `-`가 들어간 회원만 나온다. 픽스처 m1~m6(전화번호 전부 non-null,
      이름에 `-` 없음)에서는 결과가 비어 있다.
    - `keywordContains("--")` 도 동일하게 비어 있다.
    - `keywordContains("010-5555")` 는 기존대로 m4를 반환한다 (정규화 매칭 회귀 없음).
    - `name = " "`(공백 한 칸) 픽스처 m7이 `onboardingCompleted(false)` 결과에 포함되고
      `onboardingCompleted(true)` 결과에서는 제외된다.
    - 기존 정합성 테스트(`onboardingCompleted true 결과 == Member.isOnboardingCompleted 필터 결과`)가
      m7을 포함한 상태에서도 통과한다.
  </behavior>

  <action>
    **WR-04 — `keywordContains` 수정.**
    `escapedPhoneKeyword`를 만들기 전에 `PhoneNumberNormalizer.normalize(trimmed)` 결과를 지역 변수
    (예: `normalizedPhone`)에 담고, Specification 람다 안에서 술어 목록을 조건부로 구성한다:
    이름 술어는 항상 넣고, 전화번호 술어는 `normalizedPhone.isNotEmpty()` 일 때만 넣는다.
    최종 반환은 모아 둔 술어들을 `criteriaBuilder.or(*predicates.toTypedArray())`로 합친다.
    `keyword`가 blank일 때 `null`을 반환하는 기존 동작은 그대로 둔다.
    KDoc에 이유를 한 항목 추가한다: 하이픈만으로 된 검색어는 blank가 아니라 통과하지만 정규화하면
    빈 문자열이 되어, 술어를 만들면 `LIKE '%%'`가 되어 전화번호가 있는 전 회원이 매칭된다 —
    이스케이프만으로는 막히지 않는 두 번째 우회 경로다.

    **WR-05 — `onboardingCompleted` 수정.**
    `notEqual(root.get<String>("name"), "")` 를
    `criteriaBuilder.notEqual(criteriaBuilder.trim(root.get<String>("name")), "")` 로 바꾼다.
    `phoneNumber`도 동일하게 `trim`을 적용한다. `isNotNull` 검사는 그대로 둔다.
    KDoc의 "반드시 같은 판정 규칙" 문단을 실제 보장 수준으로 정정한다: SQL `TRIM`은 기본적으로
    **공백 문자만** 제거하므로 코틀린 `isBlank()`(탭·개행 등 유니코드 공백 전부 포함)와 완전히
    동일하지는 않다. 현재 쓰기 경로(온보딩이 trim + `@NotBlank`)에서 도달 가능한 것은 공백 문자
    케이스뿐이므로 그 범위에서 판정이 일치한다는 사실을 **있는 그대로** 적는다 — 없는 보장을
    주장하지 않는다(이 결함 자체가 과장된 주석에서 비롯됐다).

    **테스트 확장.**
    `MemberSpecificationTest`의 공통 픽스처에 `m7`을 추가한다: `name = " "`(공백 한 칸),
    `phoneNumber = "01088887777"`, `status = MemberStatus.PENDING`, kakaoId는 기존과 겹치지 않는 값(예: 9007L).
    m7 추가로 기존 단언(`hasStatus(PENDING)`의 `containsExactlyInAnyOrder`,
    `onboardingCompleted(false)`의 목록)이 깨지므로 **해당 단언들에 m7.id를 함께 추가**한다.
    새 테스트 메서드(백틱 한국어 서술문):
    - `검색어가 하이픈뿐이면 전화번호 조건이 만들어지지 않아 전 회원이 매칭되지 않는다`
    - `공백만 있는 이름은 온보딩 미완료로 취급된다`
    각각 `<behavior>`의 단언을 그대로 옮긴다.
  </action>

  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.member.MemberSpecificationTest" --tests "com.goldwrestling.member.MemberSearchTest" --rerun</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "criteriaBuilder.trim" src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt` 가 2 (name·phoneNumber)
    - `grep -c "isNotEmpty()" src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt` 가 1 이상 (전화번호 술어 조건부 구성)
    - `grep -c "criteriaBuilder.or(namePredicate, phonePredicate)" src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt` 가 0 (무조건 2개 or 하던 코드가 사라짐)
    - `grep -c "m7" src/test/kotlin/com/goldwrestling/member/MemberSpecificationTest.kt` 가 4 이상 (선언·픽스처·기존 단언 2곳 이상 갱신)
    - `./gradlew test --tests "com.goldwrestling.member.MemberSpecificationTest" --tests "com.goldwrestling.member.MemberSearchTest" --rerun` 이 BUILD SUCCESSFUL
    - **회귀 방향 증명:** 프로덕션 수정만 되돌리면 신규 테스트 2건이 실패함을 실측하고 SUMMARY에 기록. 확인 후 복원
  </acceptance_criteria>

  <done>
    하이픈 전용 검색어가 전 회원을 반환하지 않고, 공백 이름의 온보딩 판정이 엔티티와 쿼리에서 일치한다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 상태 변경 API의 승인 규칙 우회 차단 (WR-03)</name>

  <files>
    src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt
    src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt
  </files>

  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt (수정 대상 — `approve` 67~81행이 강제하는 규칙과 `changeStatus` 114~131행의 무검사 상태)
    - src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt (`MemberStateConflictException` — 새 예외를 만들지 말고 재사용한다)
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (`MEMBER_STATE_CONFLICT` → 409, 이미 존재)
    - src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt (수정 대상 — 관리자 토큰 발급·PATCH 호출 헬퍼와 기존 단언 패턴)
    - docs/policies.md §5.1, §5.2 (승인 조건과 상태 전이 정책)
    - .planning/phases/02-auth-member/02-REVIEW.md WR-03 절
  </read_first>

  <behavior>
    - 이름·전화번호가 없는 `PENDING` 회원에게 `PATCH /api/admin/members/{id}/status` 로 `ACTIVE`를 보내면
      409 응답이고 body의 `code`가 `MEMBER_STATE_CONFLICT`다. 회원 상태는 `PENDING` 그대로 남는다.
    - 온보딩을 마친 회원에게 같은 요청을 보내면 기존대로 200이고 상태가 `ACTIVE`가 된다.
    - 온보딩 미완료 회원을 `ON_LEAVE`·`INACTIVE`·`PENDING`으로 바꾸는 것은 기존대로 허용된다(200).
      (막는 것은 ACTIVE 전환 하나뿐이다)
  </behavior>

  <action>
    `AdminMemberService.changeStatus`에서 회원을 조회한 직후, `member.status = newStatus`로 대입하기
    **전에** 정책 검사를 넣는다: `newStatus == MemberStatus.ACTIVE && !member.isOnboardingCompleted()`
    이면 `MemberStateConflictException`을 던진다. 메시지는 `approve()`의 문구와 상황에 맞게 짝을 이루도록
    "회원이 이름·전화번호를 등록해야 활성 상태로 바꿀 수 있습니다." 로 한다 — 사용자 대면 문구이며
    내부 식별자·SQL을 담지 않는다(conventions §8). 새 예외 클래스나 새 `ErrorCode`를 만들지 않는다.

    `changeStatus`의 KDoc을 갱신한다. 현재 KDoc은 "상태 전이 자체에 제약을 두지 않는다"고 단언하는데,
    이제 예외가 하나 생겼다. 다음을 담는다: 상태 전이는 원칙적으로 자유롭게 두되 **ACTIVE 전환만
    유일한 예외**이며, 그 근거는 `approve()`가 서버측에서 강제하는 것과 같은 규칙(policies §5.1,
    T-02-37 "정책은 항상 서버에서 강제한다")을 같은 리소스의 다른 엔드포인트가 우회시키면 안 되기
    때문이다. ACTIVE가 아닌 전이(ON_LEAVE·INACTIVE·PENDING 복귀)는 종전대로 무제한이다.

    `MemberStatusChangeTest`에 `<behavior>`의 3가지 시나리오를 검증하는 테스트를 추가한다.
    기존 테스트 클래스의 애노테이션 조합·관리자 토큰 발급 헬퍼·`mockMvc.perform(patch(...))` 패턴을
    그대로 재사용한다(새 컨텍스트를 만들지 않는다). 메서드명은 백틱 한국어 서술문으로,
    policies 문장에 가깝게 쓴다(conventions §10.2). 409 케이스는 상태 코드만 보지 말고
    `jsonPath("$.code").value("MEMBER_STATE_CONFLICT")` 와, 요청 후 회원 상태가 여전히 `PENDING`인지를
    함께 단언한다 — 예외는 던졌지만 상태가 이미 바뀐 뒤였다면 트랜잭션이 롤백됐는지까지 확인되어야 한다.
  </action>

  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.member.MemberStatusChangeTest" --tests "com.goldwrestling.member.MemberApprovalTest" --rerun</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "isOnboardingCompleted" src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 2 (approve + changeStatus)
    - `grep -c "MemberStateConflictException" src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 4 (approve 2 + reject 1 + changeStatus 1)
    - `grep -c "활성 상태로 바꿀 수 있습니다" src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` 가 1
    - `src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt` 와 `docs/error-codes.md` 가 이 태스크에서 변경되지 않았다 (`git status --short` 로 확인 — 기존 코드 재사용)
    - `grep -c "MEMBER_STATE_CONFLICT" src/test/kotlin/com/goldwrestling/member/MemberStatusChangeTest.kt` 가 1 이상
    - `./gradlew test --tests "com.goldwrestling.member.MemberStatusChangeTest" --tests "com.goldwrestling.member.MemberApprovalTest" --rerun` 이 BUILD SUCCESSFUL
    - **회귀 방향 증명:** `changeStatus`의 새 검사만 제거하면 신규 409 테스트가 실패(200 관측)함을 실측하고 SUMMARY에 기록. 확인 후 복원
  </acceptance_criteria>

  <done>
    온보딩 미완료 회원을 ACTIVE로 만드는 우회 경로가 막히고, 나머지 전이 자유도는 그대로다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 결정 기록 + 포맷·전체 빌드</name>

  <files>
    docs/decisions.md
  </files>

  <read_first>
    - docs/decisions.md (마지막 결정 번호 확인 — 02-12·02-13이 각각 1건씩 추가했을 것이므로 파일 끝을 직접 확인)
    - docs/policies.md §5.1, §5.2 (기록할 결정이 policies와 어긋나지 않는지 대조)
    - CLAUDE.md 규칙 5 (설계 결정 3~4줄 기록), 문서 우선순위 (policies.md > .planning)
  </read_first>

  <action>
    `docs/decisions.md` 끝에 다음 사용 가능한 번호 2건을 추가한다(파일 끝을 직접 확인 — 예상 D-052, D-053).
    기존 3줄 형식(내용 / 이유 / 기각 대안)을 지킨다.

    첫 번째 — "상태 변경 API의 ACTIVE 전환도 온보딩 완료를 서버에서 강제":
    - 내용: `changeStatus`는 `newStatus == ACTIVE`이면서 온보딩(실명·전화번호) 미완료인 회원에 대해
      `MEMBER_STATE_CONFLICT`(409)로 거부한다. ACTIVE가 아닌 전이는 종전대로 제한 없음.
    - 이유: `approve()`가 서버에서 강제하는 policies §5.1 규칙을 같은 리소스의 다른 엔드포인트가
      우회시키면, 관리자가 회원을 이름·전화번호로 식별한다는 전제(D-025)가 깨진 ACTIVE 회원이 생긴다.
    - 기각 대안: 우회를 허용하고 문서에만 명시(정책이 엔드포인트마다 갈라짐), 모든 전이에 전이표를
      도입(policies §5.2가 "승인 취소는 상태 변경으로 갈음"이라 관리자 재량을 남겨 둔 취지와 충돌).

    두 번째 — "통합 검색어는 정규화 결과가 빈 문자열이면 전화번호 술어를 만들지 않는다":
    - 내용: `keywordContains`는 `PhoneNumberNormalizer.normalize` 결과가 빈 문자열이면 전화번호 술어를
      생성하지 않고 이름 술어만 사용한다. 온보딩 완료 판정 쿼리는 SQL `TRIM` 기준으로 엔티티의
      `isNullOrBlank()`와 맞춘다(공백 문자 범위에서 일치).
    - 이유: LIKE 와일드카드 이스케이프만으로는 `"-"` 같은 입력이 `LIKE '%%'`가 되는 경로를 막지 못해
      전화번호가 있는 전 회원이 반환된다. 판정 규칙이 엔티티와 쿼리 두 곳에 존재하는 한 한쪽만 고쳐지면
      승인 대기 목록이 어긋난다.
    - 기각 대안: 검색어에서 하이픈을 제거한 뒤 blank 검사(이름에 하이픈이 든 검색을 못 하게 됨),
      전화번호 컬럼에 함수 인덱스 도입(문제의 원인이 인덱스가 아님).

    마지막에 `./gradlew ktlintFormat` → `./gradlew build` 순서로 실행한다.
  </action>

  <verify>
    <automated>./gradlew ktlintFormat && ./gradlew build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## D-05" docs/decisions.md` 값이 이 플랜 시작 시점보다 2 증가했다
    - 추가된 두 항목이 각각 "이유:" 와 "기각 대안:" 줄을 갖는다 (기존 형식 준수)
    - `git status --short docs/api/openapi.yaml` 이 출력 없음 (이 플랜은 API 계약을 바꾸지 않는다 — 새 응답 코드가 springdoc 스펙에 나타나지 않는다)
    - `./gradlew build` 가 BUILD SUCCESSFUL — 전체 테스트 통과
    - 전체 테스트 수가 이 플랜 이전보다 4건 이상 늘어나 있다 (Task 1의 2건 + Task 2의 3건)
  </acceptance_criteria>

  <done>
    두 결정이 문서화되고 포맷·전체 빌드가 통과한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 → `/api/admin/members/**` | 인증된 ADMIN이지만 화면 필터를 우회한 직접 API 호출이 가능하다 |
| 관리자 입력(검색어) → JPA Criteria → SQL | 사용자 제어 문자열이 LIKE 패턴으로 들어간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14-01 | Elevation of Privilege | `AdminMemberService.changeStatus` | mitigate | ACTIVE 전환 시 `isOnboardingCompleted()` 서버측 강제 — 화면 필터가 아닌 서비스 계층에서 검사(T-02-37 원칙) |
| T-02-14-02 | Information Disclosure | `MemberSpecifications.keywordContains` | mitigate | 정규화 결과가 빈 문자열이면 전화번호 술어를 생성하지 않아 `LIKE '%%'` 전체 매칭을 차단. 이스케이프는 기존대로 유지 |
| T-02-14-03 | Tampering | LIKE 패턴 인젝션 | accept | JPA Criteria 파라미터 바인딩이 SQL 인젝션을 막고, `%`·`_`·이스케이프 문자는 `escapeLikeWildcards`가 처리 — 이번 변경으로 약화되지 않음 |
| T-02-14-SC | Tampering | 패키지 매니저 설치 | accept | 이 플랜은 신규 의존성을 추가하지 않는다 (build.gradle.kts 미변경) |
</threat_model>

<verification>
1. `./gradlew build` — 전체 테스트 통과
2. `grep -n "criteriaBuilder.trim" src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt` — 2건
3. `grep -n "isOnboardingCompleted" src/main/kotlin/com/goldwrestling/member/AdminMemberService.kt` — 2건 (approve, changeStatus)
4. `git status --short docs/api/openapi.yaml` — 출력 없음
5. `git status --short docs/error-codes.md src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt` — 출력 없음 (기존 에러코드 재사용)
</verification>

<success_criteria>
- WR-03·WR-04·WR-05가 각각 재현 테스트와 함께 닫혔다
- 새 에러코드·새 예외 클래스를 만들지 않고 기존 체계를 재사용했다
- 기존 회원 관리 테스트에 회귀가 없다
- `./gradlew build` 통과
</success_criteria>

<output>
Create `.planning/phases/02-auth-member/02-14-SUMMARY.md` when done.
SUMMARY에는 다음을 반드시 포함한다:
- Task 1·Task 2의 회귀 방향 증명 실측 결과
- CLAUDE.md 학습 모드가 요구하는 `이번에 쓴 기술` 섹션
  (JPA Criteria API의 동적 술어 조합, SQL `TRIM`과 코틀린 `isBlank()`의 의미 차이,
  "정책은 서버에서 강제한다"가 왜 화면 필터로 대체될 수 없는지 — 이번 작업에 실제로 등장한 것만)
</output>
</content>
