---
phase: 03-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glossary.md
  - docs/decisions.md
  - docs/policies.md
  - docs/error-codes.md
  - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
  - src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt
  - src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt
autonomous: false
requirements: [PASS-01, PASS-03, PASS-07, PASS-08]

must_haves:
  truths:
    - "이용권 phase에서 새로 쓰는 개념(PassStatus·PassDisplayStatus·EveningMembershipTerm·note·registeredBy)이 docs/glossary.md에 등재되어 있다"
    - "이용권 도메인 실패 7종이 각각 고유한 ErrorCode 값으로 구분되어 응답된다"
    - "Phase 3에서 내린 설계 결정 8건(D-060~D-067)이 docs/decisions.md에 기록되어 있다"
    - "유효기간·회비 기간의 경계 산정 방식이 policies.md에 한 문장으로 명시되어 더 이상 두 갈래로 해석되지 않는다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      provides: "이용권 도메인 에러코드 7종"
      contains: "INSUFFICIENT_PASS_COUNT"
    - path: "src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt"
      provides: "DomainException 상속 이용권 예외 7종"
      contains: "class InsufficientPassCountException"
    - path: "docs/error-codes.md"
      provides: "FE 분기 계약 표에 이용권 코드 섹션"
      contains: "## 이용권 코드 (Phase 3)"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt"
      to: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      via: "DomainException(ErrorCode.XXX, message) 생성자 호출"
      pattern: "DomainException\\(\\s*ErrorCode\\."
    - from: "src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt"
      to: "docs/error-codes.md"
      via: "targetSectionPrefixes에 이용권 섹션 추가"
      pattern: "이용권 코드"
---

<objective>
Phase 3이 쓸 **용어·에러코드·설계 결정 문서를 코드보다 먼저 정합화**한다.

Purpose: CLAUDE.md 규칙 3("새 개념은 glossary에 추가한 뒤 사용")·규칙 5("설계 결정은 decisions.md에 기록")·
D-028("ErrorCode와 error-codes.md는 항상 1:1")을 지키려면 이 작업이 스키마·엔티티보다 **앞서야** 한다.
뒤로 미루면 이후 플랜이 등재되지 않은 이름을 먼저 코드에 박아 넣게 된다.
또 RESEARCH.md Assumption A3(유효기간 경계일 포함 여부)이 회원이 마지막 날 이용권을 쓸 수 있는지를
좌우하므로, 계산 로직(03-04)을 짜기 전에 사용자에게 확정받는다.

Output: glossary 신규 개념 5건, decisions.md D-060~D-067, policies.md §1 경계 문장,
ErrorCode 7종 + error-codes.md 이용권 섹션, `pass/PassExceptions.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03-pass/03-CONTEXT.md
@.planning/phases/03-pass/03-RESEARCH.md
@.planning/phases/03-pass/03-PATTERNS.md
@docs/policies.md
@docs/glossary.md
@docs/decisions.md
@docs/error-codes.md
</context>

<interfaces>
<!-- 실행자가 그대로 상속·확장할 기존 계약. 코드베이스 탐색 없이 이 값을 쓴다. -->

`src/main/kotlin/com/goldwrestling/common/error/DomainException.kt`:
- `abstract class DomainException(val errorCode: ErrorCode, message: String, val status: HttpStatus = errorCode.defaultStatus) : RuntimeException(message)`

`src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt`:
- `enum class ErrorCode(val defaultStatus: HttpStatus)` — 현재 마지막 상수는 `MEMBER_STATE_CONFLICT(HttpStatus.CONFLICT)`

`src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt` (예외 선언 형식의 원본):
- `class MemberNotFoundException(memberId: Long?) : DomainException(ErrorCode.MEMBER_NOT_FOUND, "회원을 찾을 수 없습니다.")`
- `class MemberStateConflictException(message: String) : DomainException(ErrorCode.MEMBER_STATE_CONFLICT, message)`

`src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt`:
- `private val targetSectionPrefixes = listOf("## 공통 코드", "## 인증·회원 코드")` — 역방향 검증이 훑는 섹션 목록
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 유효기간·회비 기간의 경계 산정 방식 확정</name>
  <files>docs/policies.md, docs/decisions.md</files>
  <read_first>
    - docs/policies.md §1 (이용권 3종 — "시작일로부터 1년", "1/3/6개월")
    - .planning/phases/03-pass/03-RESEARCH.md Assumptions Log A3
    - .planning/phases/03-pass/03-CONTEXT.md `<decisions>` 등록 규칙 (D-055)
  </read_first>
  <decision>
    `SESSION_PASS`/`LESSON_PASS`의 "시작일로부터 1년"과 `EVENING_MEMBERSHIP`의 "1/3/6개월"에서
    저장할 `end_date`를 어떻게 계산하고, 그 날짜 **당일**을 사용 가능일로 볼 것인가.
  </decision>
  <context>
    policies.md §1은 기간의 길이만 정하고 경계일 포함 여부를 말하지 않는다. 두 해석이 하루씩 어긋나며,
    회원이 "마지막 날 예약이 되는가"를 직접 체감하는 차이다. Phase 4(예약 가능 판정)와 Phase 5(만료 배치)가
    이 판정을 그대로 재사용하므로 지금 확정하지 않으면 세 phase에 걸쳐 하루 오차가 번진다.
    시작일 2026-01-01 · 1년권 기준으로 두 안의 결과를 비교한다.
  </context>
  <action>
    아래 `<decision>`·`<context>`·`<options>`를 사용자에게 그대로 제시하고 선택을 받는다.
    이 태스크는 코드를 작성하지 않는다 — 선택 결과는 Task 2의 D-066 본문과 policies.md §1 문장으로만 반영된다.
    사용자가 두 안과 다른 방식을 제시하면 그 계산식(`endDate = ...`)과 유효 판정식(`...isAfter/isBefore...`)을
    되짚어 확인한 뒤 그대로 채택한다.
  </action>
  <verify>
    <automated>test -n "$GSD_CHECKPOINT_ANSWER" || echo "사용자 응답 대기 — 자동 검증 대상 아님"</automated>
  </verify>
  <done>사용자가 경계 산정 방식을 하나 선택했고, 그 계산식·판정식이 Task 2에 넘길 수 있는 형태로 문장화됐다</done>
  <options>
    <option id="option-a">
      <name>A. 종료일 포함 + 정확히 1년 (권장)</name>
      <pros>
        `endDate = startDate.plusYears(1).minusDays(1)` → 2026-12-31. 유효 판정은 `!today.isAfter(endDate)`.
        이용 가능 일수가 정확히 1년(366일이 아님)이고, 회원에게 보이는 "만료일 2026-12-31"이
        "그날까지 사용 가능"과 일치해 안내 문구가 단순하다. 저녁반 1개월도 1/1 시작 → 1/31 만료로 자연스럽다.
      </pros>
      <cons>계산식에 `.minusDays(1)`이 붙어 코드만 보면 한 번 더 생각해야 한다.</cons>
    </option>
    <option id="option-b">
      <name>B. 종료일 미포함(경계 배타)</name>
      <pros>`endDate = startDate.plusYears(1)` → 2027-01-01. 계산식이 가장 단순하다.</pros>
      <cons>
        저장된 만료일 당일에는 못 쓰는데 화면에는 그 날짜가 "만료일"로 보인다 — 회원 문의가 생기는 형태다.
        FE가 표시용으로 하루를 빼야 해서 경계 처리가 두 곳으로 흩어진다.
      </cons>
    </option>
  </options>
  <acceptance_criteria>
    - 사용자가 option-a / option-b 중 하나를 선택했다
    - 선택 결과가 Task 2의 D-066 본문과 policies.md §1 문장에 그대로 반영된다
  </acceptance_criteria>
  <resume-signal>option-a 또는 option-b 를 입력하거나, 두 안과 다른 방식을 서술한다</resume-signal>
</task>

<task type="auto">
  <name>Task 2: glossary 신규 개념 5건 + decisions.md D-060~D-067 + policies.md 경계 문장</name>
  <files>docs/glossary.md, docs/decisions.md, docs/policies.md</files>
  <read_first>
    - docs/glossary.md (전체 — "핵심 엔티티" 표, "차감 사유(TransactionReason)" 표, "기타 규칙" 금지어 목록)
    - docs/decisions.md D-055~D-059 (기록 형식과 마지막 번호 확인)
    - docs/policies.md §1, §4.2a
    - .planning/phases/03-pass/03-CONTEXT.md `<decisions>` 전체
    - .planning/phases/03-pass/03-RESEARCH.md "Architecture Patterns 3", "Assumptions Log"
  </read_first>
  <action>
    (1) `docs/glossary.md` "핵심 엔티티" 표 아래에 **이용권 (Phase 3)** 표를 새로 추가하고 5개 행을 넣는다.
    코드 네이밍은 아래 값을 그대로 쓴다 — 이후 모든 플랜이 이 이름만 사용한다.
    · 이용권 저장 상태 → `PassStatus` (`ACTIVE` / `CANCELED`) — DB에 저장되는 값은 이 2종뿐
    · 이용권 표시 상태 → `PassDisplayStatus` (`USABLE` / `EXPIRED` / `EXHAUSTED` / `CANCELED`) — 저장하지 않고 조회 시점 계산 (D-064)
    · 저녁반 회비 기간 단위 → `EveningMembershipTerm` (`ONE_MONTH` / `THREE_MONTHS` / `SIX_MONTHS`) — 등록 요청의 닫힌 집합, 저장하지 않음 (D-063)
    · 가감 사유 메모 → `note` (DB `pass_transaction.note`) — 사유 **코드** `reason`(TransactionReason)과 분리된 자유 텍스트 (D-061)
    · 이용권 등록 주체 → `registeredBy` (DB `pass.registered_by_admin_id`)
    금지어 목록(`Ticket`/`Voucher`/`Coupon`/`Booking`/`Course`/`Session`)에 저촉되지 않는지 확인한다.

    (2) `docs/decisions.md` 끝에 D-060~D-067을 각 3~4줄로 추가한다. 기존 D-055~D-059와 동일한
    `## D-0NN. 한 줄 제목` + 결정/이유/영향 형식을 유지한다. 내용은 다음으로 고정한다.
    · D-060 `Pass`는 JPA `@Inheritance` 없이 **단일 엔티티 + `PassType` 판별 컬럼**. 타입별 컬럼 규칙(횟수제만 `remaining_count` NOT NULL)은 DB CHECK 제약으로 강제. [사용자 확정]
    · D-061 `PassTransaction`은 `reason`(TransactionReason enum)과 `note`(nullable 자유 텍스트)를 분리하고, `ADMIN_ADJUST`일 때만 `note` 필수를 서비스 계층에서 강제. [사용자 확정]
    · D-062 기간·유효기간 수정은 통합 엔드포인트 `PATCH /api/admin/passes/{passId}/period` 하나로 처리. 횟수권은 **종료일만** 수정 가능(시작일 고정), 저녁반은 시작·종료 모두 수정. [사용자 확정]
    · D-063 `EVENING_MEMBERSHIP`의 개월 수(1/3/6)는 **저장하지 않는다** — 등록 시 `end_date` 계산 입력으로만 쓴다. 이후 기간 수정이 날짜 직접 지정(D-057)이라 저장하면 실제 기간과 어긋나 표시가 거짓말이 된다.
    · D-064 이용권 상태는 `ACTIVE`/`CANCELED`만 저장하고 만료·소진은 조회 시점에 `PassDisplayStatus`로 계산한다. 저장하면 배치가 돌기 전까지 저장값과 실제가 갈라진다.
    · D-065 등록 취소 시 **잔여가 0이면 상쇄 `PassTransaction`을 남기지 않는다.** 수량 변화가 0인 행은 원장에서 의미가 없고, `pass_transaction`의 `amount <> 0` CHECK와도 정합이 맞는다. "잔여 = 이력 합계" 불변식은 그대로 유지된다.
    · D-066 유효기간·회비 기간의 경계 산정 — **Task 1에서 사용자가 고른 안**을 계산식(`endDate = ...`)과 유효 판정식(`...isAfter/isBefore...`)까지 적어 기록한다.
    · D-067 `pass` 테이블은 `branch_id`를 보유한다 — requirements §1 "모든 핵심 엔티티에 `branch_id`" 확장 전제와 add-migration §2 규약. 값은 등록 시점 회원의 소속 지점으로 채운다(발급 지점 귀속).

    (3) `docs/policies.md` §1에 경계 산정 문장 1줄을 추가한다 — Task 1 결정을 그대로 옮기고 `(D-066)`을 붙인다.
    **이것은 스펙 보강이므로 완료 보고에 "policies.md를 이렇게 고쳤다"를 명시**한다 (CLAUDE.md 문서 우선순위).
  </action>
  <verify>
    <automated>grep -c "D-06[0-7]" docs/decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' docs/decisions.md | grep -c "## D-06"` 가 8 이상
    - `docs/glossary.md`가 `PassDisplayStatus`, `EveningMembershipTerm`, `PassStatus`, `registeredBy` 문자열을 모두 포함한다
    - `docs/glossary.md`에 추가한 코드 이름 중 금지어(`Ticket`/`Voucher`/`Coupon`/`Booking`/`Course`/`Session`)를 포함하는 것이 없다
    - `docs/policies.md` §1에 `D-066`을 참조하는 경계 산정 문장이 1줄 존재한다
    - D-066 본문이 Task 1에서 선택된 안과 일치한다 (선택하지 않은 안의 계산식이 결정문에 남아 있지 않다)
  </acceptance_criteria>
  <done>이후 플랜이 쓸 모든 신규 이름과 설계 결정이 docs/에 등재되어, 코드가 문서보다 앞서지 않는다</done>
</task>

<task type="auto">
  <name>Task 3: ErrorCode 7종 + error-codes.md 이용권 섹션 + PassExceptions.kt</name>
  <files>
    src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt,
    docs/error-codes.md,
    src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt,
    src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt
  </files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (전체 — 마지막 상수 `MEMBER_STATE_CONFLICT` 뒤에 이어 붙인다)
    - src/main/kotlin/com/goldwrestling/common/error/DomainException.kt (전체)
    - src/main/kotlin/com/goldwrestling/member/MemberExceptions.kt (전체 — 예외 선언 형식의 원본)
    - src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt (전체 — `targetSectionPrefixes`)
    - docs/error-codes.md (전체 — 표 형식과 폴백 규칙 문단)
    - .planning/phases/03-pass/03-PATTERNS.md "Shared Patterns > 에러 응답"
  </read_first>
  <action>
    (1) `ErrorCode.kt`의 `MEMBER_STATE_CONFLICT` 뒤에 이용권 코드 7종을 추가한다. 각 상수 위에 한 줄 KDoc을 붙이는
    기존 관례를 유지한다. 값과 상태는 아래로 고정한다.
    · `PASS_NOT_FOUND`(NOT_FOUND) — 대상 이용권 없음
    · `INVALID_ADJUSTMENT_UNIT`(BAD_REQUEST) — 가감 수량이 0.5 단위가 아니거나 0 (policies §4.2a)
    · `INSUFFICIENT_PASS_COUNT`(CONFLICT) — 가감 결과 잔여가 음수가 됨
    · `PASS_TYPE_NOT_ADJUSTABLE`(CONFLICT) — 기간제(`EVENING_MEMBERSHIP`)에 횟수 가감 시도
    · `PASS_ALREADY_CANCELED`(CONFLICT) — 이미 취소된 이용권을 가감·기간수정·재취소하려 함
    · `INVALID_PASS_PERIOD`(BAD_REQUEST) — 종료일이 시작일보다 앞서거나, 횟수권 시작일 수정 시도 (D-062)
    · `PASS_STATE_CONFLICT`(CONFLICT) — 조건부 갱신 경쟁 패배 등 위 코드로 나뉘지 않는 이용권 상태 충돌

    (2) `docs/error-codes.md`에 `## 이용권 코드 (Phase 3)` 섹션과 표를 추가한다. 열 구성(`코드`/`HTTP 상태`/`의미`/`발생 지점`)과
    `| \`CODE\` | ...` 행 형식을 기존 섹션과 동일하게 맞춘다 — 형식이 다르면 아래 (4)의 역방향 검증이 코드를 못 읽는다.
    발생 지점 열에는 `Pass`(엔티티 도메인 판정) / `AdminPassService` 를 적는다.

    (3) `src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt`를 새로 만든다(패키지 `com.goldwrestling.pass`).
    `MemberExceptions.kt` 형식 그대로 `DomainException`을 상속하는 7개 클래스를 선언한다:
    `PassNotFoundException(passId: Long?)`, `InvalidAdjustmentUnitException`, `InsufficientPassCountException`,
    `PassTypeNotAdjustableException`, `PassAlreadyCanceledException`, `InvalidPassPeriodException(message: String)`,
    `PassStateConflictException(message: String)`.
    메시지는 **사용자 대면 한국어 문구**로 쓰고 내부 식별자·SQL·필드명을 노출하지 않는다.
    `GlobalExceptionHandler`는 건드리지 않는다 — `DomainException` 상속만으로 ProblemDetail 변환이 자동 적용된다.

    (4) `ErrorCodeRegistryTest.kt`의 `targetSectionPrefixes` 리스트에 `"## 이용권 코드"`를 추가한다.
    이 줄을 빠뜨리면 새 섹션의 코드가 역방향(문서 → enum) 검증에서 통째로 제외되어, 표에 오타가 나도 빌드가 통과한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.common.error.ErrorCodeRegistryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.common.error.ErrorCodeRegistryTest"` 종료 코드 0
    - `ErrorCode.kt`가 `PASS_NOT_FOUND`, `INVALID_ADJUSTMENT_UNIT`, `INSUFFICIENT_PASS_COUNT`, `PASS_TYPE_NOT_ADJUSTABLE`, `PASS_ALREADY_CANCELED`, `INVALID_PASS_PERIOD`, `PASS_STATE_CONFLICT` 7개 상수를 모두 포함한다
    - `docs/error-codes.md`가 `## 이용권 코드 (Phase 3)` 헤딩을 포함한다
    - `ErrorCodeRegistryTest.kt`가 `"## 이용권 코드"` 문자열을 포함한다
    - `PassExceptions.kt`의 7개 클래스가 각각 서로 다른 `ErrorCode` 상수를 생성자에 넘긴다 (`grep -c "ErrorCode\." src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt` == 7)
    - `./gradlew ktlintCheck` 종료 코드 0
  </acceptance_criteria>
  <done>이용권 실패 7종이 FE가 구분할 수 있는 고유 코드로 응답되고, enum과 문서가 빌드에서 강제로 맞춰진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 클라이언트(에러 응답) | 도메인 실패 사유가 응답 본문으로 나간다 — 내부 구조가 새어나갈 수 있는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `PassExceptions.kt` 메시지 | mitigate | 예외 message를 사용자 대면 한국어 문구로 고정하고 passId·SQL·엔티티 필드명을 넣지 않는다 (`MemberNotFoundException`이 memberId를 받되 메시지에 쓰지 않는 선례를 따른다) |
| T-03-02 | Repudiation | 설계 결정 미기록 | mitigate | D-060~D-067을 decisions.md에 기록해 이후 phase가 근거 없이 다르게 구현하는 것을 막는다 (CLAUDE.md 규칙 5) |
| T-03-03 | Tampering | error-codes.md ↔ ErrorCode 불일치 | mitigate | `ErrorCodeRegistryTest`의 `targetSectionPrefixes`에 이용권 섹션을 추가해 양방향 검증을 빌드에서 강제 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 이번 phase는 신규 외부 패키지를 설치하지 않는다 (RESEARCH.md "Package Legitimacy Audit — 해당 없음"). 설치 태스크가 생기면 slopcheck + 차단형 휴먼 체크포인트를 먼저 넣는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 순서로 실행해 통과 (conventions §11.5)
- `docs/glossary.md`·`docs/decisions.md`·`docs/policies.md`·`docs/error-codes.md` 4개 문서가 서로 모순되지 않는다
  (특히 D-066 경계 산정 문장이 policies §1과 동일해야 한다)
</verification>

<success_criteria>
- 이후 플랜이 쓸 신규 이름 5건이 glossary에 등재됨
- D-060~D-067 8건이 decisions.md에 기록됨
- 이용권 에러코드 7종이 enum·문서·예외 클래스 세 곳에서 1:1로 일치하고 빌드가 이를 강제함
- 유효기간 경계 산정이 사용자 승인으로 확정되어 03-04의 계산 로직에 모호함이 없음
</success_criteria>

<output>
Create `.planning/phases/03-pass/03-01-SUMMARY.md` when done.
커밋은 사용자가 명시적으로 요청할 때만 실행한다 (CLAUDE.md 커밋 규칙 — GSD 자동 커밋보다 우선).
</output>
