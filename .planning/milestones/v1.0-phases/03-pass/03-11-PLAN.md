---
phase: 03-pass
plan: 11
type: execute
wave: 10
depends_on: [03-10]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-pass/03-VALIDATION.md
  - docs/api/openapi.yaml
  - docs/error-codes.md
  - docs/glossary.md
  - docs/decisions.md
autonomous: false
requirements: [PASS-01, PASS-02, PASS-03, PASS-04, PASS-05, PASS-06, PASS-07, PASS-08]

must_haves:
  truths:
    - "PASS-01~08 여덟 요구사항이 각각 어느 테스트로 증명되는지 한 표에서 확인된다"
    - "코드·docs·openapi.yaml이 서로 어긋나는 곳이 없다"
    - "전체 빌드와 전체 테스트가 통과한다"
    - "관리자가 실제 요청으로 등록→가감→기간수정→취소→조회 흐름을 한 번 통과시켜 확인했다"
  artifacts:
    - path: ".planning/phases/03-pass/03-VALIDATION.md"
      provides: "요구사항↔테스트 매핑이 채워진 검증 계약"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PASS-01~08 상태 갱신"
    - path: "docs/api/openapi.yaml"
      provides: "이 phase의 전 엔드포인트가 반영된 최종 계약"
  key_links:
    - from: ".planning/phases/03-pass/03-VALIDATION.md"
      to: "src/test/kotlin/com/goldwrestling/pass/"
      via: "요구사항별 automated command"
      pattern: "com\\.goldwrestling\\.pass\\."
---

<objective>
Phase 3을 닫는다. 전체 빌드·전체 테스트를 실제로 돌리고, 코드와 문서(glossary·decisions·error-codes·openapi)가
어긋나지 않는지 점검하고, 사람이 한 번 실제 요청으로 흐름 전체를 통과시킨다.

Purpose: 플랜별 검증은 그 플랜 범위만 본다. 이 플랜만이 "여덟 요구사항이 전부 실제로 동작하는가"와
"이 phase가 남긴 문서가 다음 phase에게 정확한가"를 확인한다. Phase 4(예약)가 이 phase의 차감 경로를
그대로 호출하므로, 여기서 남긴 문서가 곧 다음 phase의 입력이다.

Output: 채워진 `03-VALIDATION.md`, 갱신된 `REQUIREMENTS.md`, 최종 `openapi.yaml`, 사람 확인 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pass/03-VALIDATION.md
@.planning/phases/03-pass/03-CONTEXT.md
@docs/policies.md
@docs/glossary.md
@docs/decisions.md
@docs/error-codes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 검증 실행 + 문서 정합 점검</name>
  <files>
    docs/api/openapi.yaml,
    docs/error-codes.md,
    docs/glossary.md,
    docs/decisions.md
  </files>
  <read_first>
    - .planning/phases/03-pass/03-01-SUMMARY.md ~ 03-10-SUMMARY.md (각 플랜이 실제로 만든 것과 남긴 결정)
    - docs/glossary.md "이용권 (Phase 3)" 표 (03-01 산출물)
    - docs/decisions.md D-060~D-070
    - docs/error-codes.md 이용권 코드 섹션
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
    - src/main/kotlin/com/goldwrestling/pass/ 전체 파일 목록 (`ls`로 확인 — 개별 정독은 필요한 파일만)
    - docs/conventions.md §11.5, §12
  </read_first>
  <action>
    (1) `docker-compose up -d` 후 아래를 순서대로 실행하고 결과를 기록한다:
    · `./gradlew ktlintFormat`
    · `./gradlew build` (ktlintCheck + compileKotlin + 전체 test)
    · `./gradlew generateApiDocs` → `git diff docs/api/openapi.yaml`
      **여기서 diff가 나오면 앞선 플랜 중 하나가 재생성을 빠뜨린 것이다** — 그 결과를 커밋 대상에 포함하고
      어느 엔드포인트가 누락됐었는지 SUMMARY에 적는다. diff가 없으면 계약이 이미 최신이다.

    (2) 문서↔코드 정합을 점검하고 어긋난 곳을 고친다:
    · `docs/glossary.md`에 등재된 이용권 이름(`PassStatus`·`PassDisplayStatus`·`EveningMembershipTerm`·`note`·`registeredBy`)이
      실제 코드 식별자와 철자까지 같은가. 코드에만 있고 glossary에 없는 새 이름이 생겼는가
      (`ls src/main/kotlin/com/goldwrestling/pass/`로 대조)
    · `ErrorCode` enum과 `docs/error-codes.md` 표가 1:1인가 —
      `./gradlew test --tests "com.goldwrestling.common.error.ErrorCodeRegistryTest"`로 자동 확인
    · D-060~D-070이 실제 구현과 일치하는가. 구현 중 결정이 바뀌었는데 문서가 안 바뀐 항목이 있으면
      **문서를 실제 구현에 맞춰 고치고** 그 사실을 SUMMARY에 적는다 (반대로 하지 않는다 — 코드가 문서를 따라야 한다면 코드를 고친다)
    · `docs/policies.md`와 구현이 충돌하는 지점이 있으면 **policies.md가 이긴다**(CLAUDE.md 문서 우선순위) —
      코드를 고칠 사안이면 그대로 진행하지 말고 사용자에게 보고한다

    (3) 이 phase에서 프로덕션 코드를 만들고 테스트를 쓰지 않은 항목이 있는지 `conventions.md §10.0` 면제 목록으로 대조하고,
    면제 판단한 것이 있으면 그 이유를 SUMMARY에 한 줄씩 적는다(§10.5).
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` 종료 코드 0
    - `./gradlew test --tests "com.goldwrestling.pass.*"` 종료 코드 0
    - `./gradlew generateApiDocs` 실행 후 `git diff --exit-code docs/api/openapi.yaml` 이 0을 반환한다 (재생성 결과가 커밋 상태와 동일)
    - `docs/api/openapi.yaml`이 다음 7개 경로를 모두 포함한다: `POST /api/admin/members/{memberId}/passes`, `GET /api/admin/members/{memberId}/passes`, `POST /api/admin/passes/{passId}/adjustments`, `PATCH /api/admin/passes/{passId}/period`, `POST /api/admin/passes/{passId}/cancellation`, `GET /api/members/me/passes`, `GET /api/members/me/pass-transactions`
    - `grep -v '^#' docs/error-codes.md | grep -c "PASS_\|INSUFFICIENT_PASS_COUNT\|INVALID_ADJUSTMENT_UNIT"` >= 7
    - `ls src/main/kotlin/com/goldwrestling/pass/` 결과의 모든 신규 개념 이름이 `docs/glossary.md`에 등장한다
  </acceptance_criteria>
  <done>빌드·테스트가 통과하고 코드와 문서가 서로를 정확히 반영한다</done>
</task>

<task type="auto">
  <name>Task 2: 요구사항↔테스트 매핑 표 완성 + REQUIREMENTS 상태 갱신</name>
  <files>
    .planning/phases/03-pass/03-VALIDATION.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/03-pass/03-VALIDATION.md (전체 — `Per-Task Verification Map` 표의 TBD 열과 `Wave 0 Requirements` 체크리스트)
    - .planning/REQUIREMENTS.md 32-39줄(PASS-01~08 정의), 119-126줄(커버리지 표)
    - .planning/phases/03-pass/03-01-SUMMARY.md ~ 03-10-SUMMARY.md
    - `ls src/test/kotlin/com/goldwrestling/pass/` (실제로 만들어진 테스트 파일 목록)
  </read_first>
  <action>
    (1) `03-VALIDATION.md`의 `Per-Task Verification Map` 표를 채운다. 각 행의 `Task ID`·`Plan`·`Wave`·`Threat Ref`·
    `Automated Command`·`File Exists`·`Status`를 실제 산출물로 갱신한다. `Threat Ref`에는 해당 요구사항을 지키는
    위협 ID(예: PASS-02 → T-03-27, PASS-03 → T-03-26, PASS-05/06 → T-03-41·T-03-42)를 적는다.
    실제 테스트 파일명이 원래 계획과 다른 항목(예: 표시 상태는 `PassDisplayStatusTest`, 기간·취소 통합은
    `AdminPassControllerTest`, 조건부 쿼리는 `PassRepositoryTest`)은 **실제 파일명으로 바로잡는다**.
    `Wave 0 Requirements` 체크박스를 실제 존재 여부로 체크하고, 존재하지 않는 항목이 있으면 그 사유를 적는다.
    프론트매터의 `status`를 `complete`로, `nyquist_compliant`·`wave_0_complete`를 `true`로 바꾼다
    (모든 요구사항 행이 automated command를 갖고 실제로 green일 때만).

    (2) `.planning/REQUIREMENTS.md`의 커버리지 표에서 PASS-01~08의 상태를 `Pending`에서 완료 상태로 바꾸고,
    32-39줄의 체크박스 `- [ ]`를 `- [x]`로 바꾼다. 문서 하단 `Last updated` 문구를 갱신한다.
    **요구사항 문장 자체는 고치지 않는다** — 구현이 요구사항과 다르면 그건 갭이지 문서 수정 대상이 아니다.
  </action>
  <verify>
    <automated>grep -c "TBD" .planning/phases/03-pass/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-VALIDATION.md`의 `Per-Task Verification Map` 표에 `TBD` 문자열이 0건이다
    - `03-VALIDATION.md` 프론트매터가 `nyquist_compliant: true` 와 `wave_0_complete: true` 를 포함한다
    - 표의 8개 `Automated Command`가 모두 실제 존재하는 테스트 클래스를 가리킨다 (각 명령을 한 번씩 실행해 종료 코드 0)
    - `.planning/REQUIREMENTS.md`에서 `PASS-0` 로 시작하는 체크박스 8개가 모두 `- [x]` 다
    - `.planning/REQUIREMENTS.md`의 PASS-01~08 커버리지 행에 `Pending` 이 남아 있지 않다
  </acceptance_criteria>
  <done>여덟 요구사항이 각각 어느 명령으로 증명되는지가 표 하나로 추적된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 관리자 흐름 수동 확인 + openapi diff 리뷰</name>
  <files>docs/api/openapi.yaml, docs/decisions.md</files>
  <read_first>
    - docs/api/openapi.yaml (이번 phase에서 추가된 경로들)
    - .planning/phases/03-pass/03-06-SUMMARY.md, 03-10-SUMMARY.md (D-068·D-070 확인 요청 항목)
  </read_first>
  <what-built>
    이용권 3종 등록(시작일 지정·과거 허용, 횟수권 유효기간 1년 자동), 수동 가감(사유 필수·0.5 단위·음수 거부),
    기간·유효기간 수정(통합 엔드포인트), 등록 취소(상쇄 이력), 관리자 이용권 목록, 회원 본인 이용권·이력 조회.
    자동 검증은 전부 통과한 상태다 — 이 체크포인트는 **자동 테스트가 잡지 못하는 것**(응답 형태가 FE가 쓰기에
    적절한가, 확인 요청한 두 결정이 맞는가)만 본다.
  </what-built>
  <how-to-verify>
    1. `docker-compose up -d` 후 애플리케이션을 기동한다.
    2. `git diff docs/api/openapi.yaml`(또는 Swagger UI `/swagger-ui.html`)로 새 경로 7개의 요청·응답 스키마를 훑어본다.
       특히 `GET /api/members/me/pass-transactions`의 파라미터가 `passId`·`page`·`size` **개별 쿼리 파라미터**로
       펼쳐져 있는지 확인한다 (객체 1개로 나오면 FE 타입 생성이 깨진다).
    3. 관리자 토큰으로 다음 순서를 한 번 통과시킨다:
       등록(`SESSION_PASS`, 초기 10회) → 가감(`-0.5`, 사유 입력) → 기간 수정(종료일 연장) → 취소 →
       관리자 목록 조회(취소된 것이 구분되어 보이는지) → 해당 회원 토큰으로 본인 조회(취소된 것이 안 보이는지).
    4. 확인이 필요한 결정 2건에 답한다:
       · **D-068** — 이용권 등록 시 회원 상태(`PENDING`/`ON_LEAVE`/`INACTIVE`)를 제한하지 않는다. 이대로 둘 것인가?
       · **D-070** — 관리자가 남긴 가감 사유 메모(`note`)를 회원 이력 조회 응답에 **넣지 않는다**. 이대로 둘 것인가?
    5. 이 phase가 policies.md와 어긋난다고 느껴지는 동작이 있으면 그대로 적는다 — 문서 우선순위상 policies.md가 기준이다.
  </how-to-verify>
  <action>
    Task 1·2가 끝난 상태에서 위 `<how-to-verify>` 5단계를 사용자에게 제시하고 응답을 기다린다.
    사용자가 D-068·D-070을 바꾸겠다고 하면 해당 결정문을 `docs/decisions.md`에서 수정하고,
    코드 변경이 필요한 범위(가감 메모 노출 등)는 이 플랜에서 즉흥 수정하지 말고 후속 작업으로 분리해 보고한다.
    사용자가 "approved"만 남기면 `docs/decisions.md`의 D-068·D-070에 "사용자 확인 완료(날짜)" 한 줄을 덧붙인다.
  </action>
  <verify>
    <automated>./gradlew build</automated>
  </verify>
  <done>사용자가 실제 요청으로 흐름을 확인했고, 확인 요청한 결정 2건에 답이 기록됐다</done>
  <acceptance_criteria>
    - 사용자가 "approved" 또는 수정이 필요한 항목을 서술했다
    - D-068·D-070 두 결정에 대한 답이 기록되었다
    - openapi.yaml의 `pass-transactions` 파라미터가 개별 쿼리 파라미터로 펼쳐진 것을 사용자가 확인했다
    - 6단계 관리자 흐름이 실제 요청으로 한 번 완주되었다
  </acceptance_criteria>
  <resume-signal>"approved"를 입력하거나, 고쳐야 할 항목과 D-068·D-070에 대한 답을 서술한다</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 이 phase 산출물 → 다음 phase | Phase 4(예약)가 이 phase의 차감 경로와 문서를 그대로 전제한다 |
| 코드 → FE 계약(openapi.yaml) | 재생성을 빠뜨리면 FE가 존재하지 않는 형태로 타입을 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-47 | Tampering | openapi.yaml과 실제 API 불일치 | mitigate | `generateApiDocs` 재실행 후 `git diff --exit-code`로 재생성 결과가 커밋 상태와 같은지 검사한다 |
| T-03-48 | Repudiation | 결정과 구현의 괴리 | mitigate | D-060~D-070을 실제 구현과 1:1 대조하고, 어긋나면 문서를 구현에 맞춰 고치고 SUMMARY에 기록 |
| T-03-49 | Information Disclosure | 미검토 응답 필드 노출 | mitigate | 사람이 openapi 스키마를 눈으로 훑고 회원 응답에 관리자 전용 필드가 없는지 확인(체크포인트 3단계) |
| T-03-50 | Elevation of Privilege | 인가 규칙 회귀 | mitigate | 전 엔드포인트의 401/403 통합테스트가 `./gradlew build`에 포함되어 매 빌드마다 재검증된다 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | 이 phase 전체에서 신규 외부 패키지 설치 없음 (RESEARCH.md Package Legitimacy Audit — 해당 없음) |
</threat_model>

<verification>
- `./gradlew build` 통과
- `./gradlew generateApiDocs` 후 `git diff --exit-code docs/api/openapi.yaml` 통과
- `03-VALIDATION.md` 8개 요구사항 행의 automated command가 각각 실제로 green
- 사람이 관리자 흐름 6단계를 실제 요청으로 완주
</verification>

<success_criteria>
- PASS-01~08 전부가 자동 테스트로 증명되고 그 매핑이 문서로 남는다
- 코드·docs·openapi.yaml이 서로 일치한다
- 확인 요청한 설계 결정 2건(D-068·D-070)에 사용자 답이 기록된다
- Phase 4가 전제할 차감 경로와 문서가 준비된다
</success_criteria>

<output>
Create `.planning/phases/03-pass/03-11-SUMMARY.md` when done.
커밋은 사용자가 명시적으로 요청할 때만 실행한다 (CLAUDE.md 커밋 규칙 — GSD 자동 커밋보다 우선).
</output>
