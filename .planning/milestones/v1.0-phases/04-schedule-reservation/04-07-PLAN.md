---
phase: 04-schedule-reservation
plan: 07
type: execute
wave: 7
depends_on: ["04-05", "04-06"]
files_modified:
  - src/main/kotlin/com/goldwrestling/notification/NotificationService.kt
  - src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/ReserveRequest.kt
  - src/main/kotlin/com/goldwrestling/reservation/dto/ReservationResponse.kt
  - src/test/kotlin/com/goldwrestling/notification/NotificationServiceTest.kt
  - src/test/kotlin/com/goldwrestling/reservation/MemberReservationServiceTest.kt
autonomous: true
requirements: [RESV-01, RESV-02, NOTIF-01]

must_haves:
  truths:
    - "예약제 수업을 예약하면 SESSION_PASS 잔여가 정확히 1 줄고 RESERVE 이력이 회원 주체로 남는다"
    - "1:1 레슨을 예약하면 LESSON_PASS 잔여가 정확히 1 준다"
    - "예약 성공 1건마다 관리자 알림 레코드가 정확히 1건 생성된다"
    - "예약 도중 어느 단계가 실패해도 잔여·예약 인원·예약 행·이력·알림이 하나도 남지 않는다 (전부 롤백)"
    - "휴강된 수업·저녁반·예약 창 밖·중복·잔여 부족은 각각 고유한 에러코드로 거부된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      provides: "예약 생성 트랜잭션 조립 (세션 확보 → 판정 → 정원 → 차감 → 예약 → 이력 → 알림)"
      contains: "fun reserve"
    - path: "src/main/kotlin/com/goldwrestling/notification/NotificationService.kt"
      provides: "관리자 알림 생성 전용 헬퍼 (호출부 트랜잭션에 편승)"
      contains: "fun createReservationCreated"
    - path: "src/test/kotlin/com/goldwrestling/reservation/MemberReservationServiceTest.kt"
      provides: "예약 생성 성공·거부 경로 통합테스트"
      contains: "class MemberReservationServiceTest"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      to: "src/main/kotlin/com/goldwrestling/pass/PassRepository.kt"
      via: "adjustRemainingCount(passId, -1.0) 조건부 UPDATE로 차감"
      pattern: "adjustRemainingCount"
    - from: "src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt"
      to: "src/main/kotlin/com/goldwrestling/schedule/ClassSessionRepository.kt"
      via: "incrementReservedCountIfCapacityAvailable 조건부 UPDATE로 정원 확보"
      pattern: "incrementReservedCountIfCapacityAvailable"
---

<objective>
**예약 생성의 트랜잭션 조립**과 관리자 알림 생성 헬퍼를 만든다. 이 phase에서 가장 중요한 코드다.

Purpose: 예약 1건은 5개 테이블을 건드린다 — `class_session`(정원 +1) · `pass`(잔여 −1) · `reservation`(INSERT) ·
`pass_transaction`(RESERVE) · `notification`(INSERT). 이 다섯이 **하나의 트랜잭션**에서 전부 성공하거나
전부 롤백돼야 Core Value("회원이 보는 잔여 = 실제 사용 가능 횟수")가 성립한다.
각 단계의 원자성은 이미 04-03의 조건부 UPDATE가 보장하므로, 이 플랜의 일은 **순서와 실패 처리**를 정확히 짜는 것이다.

Output: `NotificationService`(생성 전용), `MemberReservationService.reserve`, 요청·응답 DTO, 통합테스트 2종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@docs/conventions.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
</context>

<interfaces>
`schedule/ClassSessionService.kt`(04-05): `fun getOrCreate(schedule: ClassSchedule, classDate: LocalDate): ClassSession`
`schedule/ClassSession.kt`(04-03): `fun assertReservable()` (휴강/저녁반 거부)
`schedule/ClassSessionRepository.kt`(04-03):
- `fun incrementReservedCountIfCapacityAvailable(id: Long): Int` — 0 = 정원 초과 또는 휴강
`schedule/ReservationWindow.kt`(04-04): `fun assertBookable(classDate: LocalDate, startTime: LocalTime, now: LocalDateTime)`
`schedule/ClassScheduleRepository.kt`(04-03): `fun findAllByBranchId(branchId: Long): List<ClassSchedule>`

`reservation/ReservationPassPolicy.kt`(04-06):
- `fun requiredPassType(classType: ClassType): PassType`
- `val DEDUCTION_AMOUNT: BigDecimal` (= `BigDecimal("1.0")`)
- `fun selectCandidate(candidates: List<Pass>): Pass`
`pass/PassRepository.kt`(04-06):
- `fun findDeductionCandidates(memberId: Long, type: PassType, classDate: LocalDate, requiredAmount: BigDecimal): List<Pass>`
- `fun adjustRemainingCount(id: Long, amount: BigDecimal): Int` — 0 = 경쟁 패배 또는 상태 변경
`pass/PassTransaction.kt`(04-03): `PassTransaction(pass, amount, reason, note, admin, member, occurredAt)` — 전 필드 `val`
`pass/TransactionReason.kt`: `RESERVE`, `CANCEL_REFUND`, `CLASS_CANCELED_REFUND`
`notification/Notification.kt`(04-03): 전 필드 `val`, `type`/`reservation`/`classSession`/`memberName`/`classType`/`classDate`/`startTime`/`message`/`occurredAt`
`notification/NotificationType.kt`(04-03): 6개 상수
`member/MemberStateGate.kt`: `fun requireActive(principal: AuthenticatedPrincipal)`
`config/ClockConfig.kt`: `Clock` 빈

기존 조립 관례(`pass/AdminPassService.kt`): 조회 → 판정 → 조건부 UPDATE → (필요 시) 재조회 → 이력 저장.
**벌크 UPDATE 호출 전에 이후 로직에 필요한 스칼라 값을 지역 변수로 미리 꺼내둔다** — `clearAutomatically`로
엔티티가 준영속이 되어 LAZY 연관 접근 시 `LazyInitializationException`이 난다.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationService — 관리자 알림 생성 전용 헬퍼</name>
  <files>
    src/main/kotlin/com/goldwrestling/notification/NotificationService.kt,
    src/test/kotlin/com/goldwrestling/notification/NotificationServiceTest.kt
  </files>
  <behavior>
    - `createReservationCreated(reservation)` → `type = RESERVATION_CREATED`, `reservation` 참조, `memberName`/`classType`/`classDate`/`startTime` 비정규화, `classSession = null`
    - `createReservationCanceled(reservation, byAdmin: Boolean)` → `RESERVATION_CANCELED_BY_ADMIN` 또는 `RESERVATION_CANCELED_BY_MEMBER`
    - `createReservationChanged(newReservation, byAdmin: Boolean)` → `RESERVATION_CHANGED_BY_ADMIN` 또는 `RESERVATION_CHANGED_BY_MEMBER`, 새 예약을 참조
    - `createClassSessionSuspended(classSession, canceledCount: Int)` → `CLASS_SESSION_SUSPENDED`, `reservation = null`, `classSession` 참조, `message`에 취소 건수 포함
    - 생성된 알림의 `isRead`는 false, `readAt`은 null이다
    - `occurredAt`은 주입된 `Clock` 기준이다 (테스트에서 고정한 시각과 일치)
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/notification/Notification.kt, NotificationType.kt, NotificationRepository.kt (04-03 산출물)
    - src/main/kotlin/com/goldwrestling/reservation/Reservation.kt (04-03 — 비정규화할 필드 확인)
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (`PassTransaction`을 같은 트랜잭션 안에서 인라인 저장하는 방식 — 이 서비스의 참고 원형)
    - src/main/kotlin/com/goldwrestling/config/ClockConfig.kt
    - .planning/phases/04-schedule-reservation/04-PATTERNS.md "notification/NotificationService.kt" 절 (No analog — 설계 근거)
    - .planning/phases/04-schedule-reservation/04-CONTEXT.md "알림(Notification) — NOTIF-01 범위"
  </read_first>
  <action>
    `notification/NotificationService.kt`를 `@Service`로 만든다. **클래스·메서드에 `@Transactional`을 붙이지 않는다** —
    이 서비스는 항상 호출부(예약/취소/변경/휴강 서비스)의 트랜잭션 안에서 실행되는 헬퍼이고, 자체 경계를 열면
    "예약은 롤백됐는데 알림은 남는" 상태가 생긴다. 이 이유를 클래스 KDoc에 명시한다.
    또 **수신자 컬럼이 없는 이유**(수신자는 항상 관리자 전체 — D-097)와 **이 phase는 생성만 하고 조회는 Phase 6**임을 적는다.

    `<behavior>`의 4개 팩토리 메서드를 만든다. 비정규화 값은 `Reservation`/`ClassSession`에서 **그 시점 값을 복사**한다 —
    회원이 나중에 이름을 바꿔도 "그때 그 알림"이 그대로 남아야 알림·활동 피드의 의미가 유지된다.
    `memberName` 접근이 LAZY 프록시를 건드리므로, 호출부가 조건부 UPDATE 직후에 부르지 않도록
    "이 메서드는 벌크 UPDATE 이후 준영속 엔티티를 넘기면 안 된다"를 KDoc에 경고로 남긴다.

    `message`는 관리자 화면에 그대로 보일 한국어 한 줄로 만든다. 예:
    · 예약 — `"{회원명}님이 {M/d} {HH:mm} {수업종류 한글} 수업을 예약했습니다."`
    · 휴강 — `"{M/d} {HH:mm} {수업종류 한글} 수업이 휴강 처리되어 예약 {N}건이 자동 취소되었습니다."`
    수업 종류 한글 라벨은 `docs/glossary.md`의 "수업 종류 (ClassType)" 표를 따른다
    (`EVENING`=저녁반, `SESSION`=예약제 수업, `LESSON`=1:1 레슨).

    `NotificationServiceTest`는 `<behavior>` 6개 항목을 각각 `@Test`로 검증한다. `MutableTestClock`으로 시각을 고정한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.notification.NotificationServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.notification.NotificationServiceTest"` 종료 코드 0, 6개 이상 테스트 실행
    - `NotificationService.kt`에 `@Transactional` 애노테이션이 0건이다
    - `NotificationService.kt`가 `createReservationCreated`, `createReservationCanceled`, `createReservationChanged`, `createClassSessionSuspended` 4개 메서드를 갖는다
    - 테스트가 `CLASS_SESSION_SUSPENDED` 알림의 `reservation`이 null이고 `classSession`이 non-null임을 단언한다
    - 테스트가 `occurredAt`이 고정한 테스트 시각과 같음을 단언한다
  </acceptance_criteria>
  <done>4개 호출부가 각자 알림 문구를 만들지 않고 이 헬퍼만 부르면 되는 상태가 된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberReservationService.reserve — 예약 생성 트랜잭션</name>
  <files>
    src/main/kotlin/com/goldwrestling/reservation/MemberReservationService.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/ReserveRequest.kt,
    src/main/kotlin/com/goldwrestling/reservation/dto/ReservationResponse.kt,
    src/test/kotlin/com/goldwrestling/reservation/MemberReservationServiceTest.kt
  </files>
  <behavior>
    - 정상 예약(SESSION) → `Reservation`(ACTIVE) 1행, `class_session.reserved_count` +1, `pass.remaining_count` −1.0,
      `PassTransaction(RESERVE, amount = -1.0, member = 예약자, admin = null)` 1행, `Notification` 1행
    - 정상 예약(LESSON) → `LESSON_PASS`에서 차감된다
    - 저녁반 타임 예약 시도 → `ClassSessionNotReservableException` (409 `CLASS_SESSION_NOT_RESERVABLE`)
    - 휴강된 수업 예약 시도 → `ClassSessionCanceledException` (409 `CLASS_SESSION_CANCELED`)
    - 다음 주 수업 예약 시도 / 이미 시작된 수업 예약 시도 → `ReservationWindowClosedException` (409)
    - 같은 회원이 같은 날짜·시각에 이미 활성 예약 보유 → `DuplicateReservationException` (409)
    - 정원이 찬 수업 → `ReservationCapacityExceededException` (409)
    - 잔여 부족(0.5회만 보유) → `InsufficientPassCountException` (409)
    - 위 실패 케이스 각각에서 **`class_session.reserved_count`·`pass.remaining_count`가 요청 전과 동일**하고
      `reservation`·`pass_transaction`·`notification` 행이 늘지 않는다
    - `PassTransaction.amount`는 `BigDecimal("-1.0")`이고 `compareTo`로 비교했을 때 정확히 일치한다
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (전체 — 조립 순서, 벌크 UPDATE 전 스칼라 값 미리 꺼내기, 0행 반환 처리 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt (전체 — `adjustRemainingCount` KDoc의 준영속 경고)
    - src/main/kotlin/com/goldwrestling/schedule/ClassSessionService.kt, ClassSessionRepository.kt, ReservationWindow.kt
    - src/main/kotlin/com/goldwrestling/reservation/ReservationPassPolicy.kt, Reservation.kt, ReservationRepository.kt
    - src/main/kotlin/com/goldwrestling/notification/NotificationService.kt (Task 1 산출물)
    - src/main/kotlin/com/goldwrestling/pass/dto/RegisterPassRequest.kt (요청 DTO + jakarta validation 관례)
    - src/test/kotlin/com/goldwrestling/pass/AdminPassServiceTest.kt 또는 동등한 서비스 통합테스트 (픽스처·시각 고정 방식)
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "System Architecture Diagram"의 reserve 10단계, "Pitfall 2", "Pitfall 4"
  </read_first>
  <action>
    (1) `ReserveRequest(classScheduleId: Long, classDate: LocalDate)` — `@field:NotNull` 형식 검증만.
    **`classSessionId`를 받지 않는다** — 세션은 아직 없을 수 있고(D-094), 클라이언트가 임의 세션 id를 지정하면
    다른 지점·다른 시간표의 세션에 예약이 붙을 수 있다. 시간표 id + 날짜로만 받고 서버가 세션을 확보한다.
    (2) `ReservationResponse(id, classSessionId, classType, classDate, startTime, endTime, status, reservedAt, passId)` —
    엔티티를 노출하지 않는다(D-019). 다른 회원 정보는 포함하지 않는다.
    (3) `MemberReservationService.kt` — `@Service @Transactional(readOnly = true)`, `reserve`만 `@Transactional` 오버라이드.
    `reserve(memberId: Long, request: ReserveRequest): ReservationResponse`를 **아래 순서 그대로** 구현한다.
    순서를 바꾸면 롤백 범위나 에러코드가 달라진다 — 각 단계에 주석으로 번호를 남긴다.
      ① 회원 조회(`MemberRepository`) — `branchId` 확보. 상태 게이트는 컨트롤러(04-08)가 호출한다
      ② `classScheduleRepository`에서 `classScheduleId` 조회 → 없거나 회원 지점과 다르면 `ClassScheduleNotFoundException`.
         **요청한 시간표의 `dayOfWeek`가 `classDate`의 요일과 다르면** 같은 예외로 거부한다
         (요일이 어긋난 조합으로 존재하지 않는 수업을 만들 수 있다)
      ③ `ReservationWindow.assertBookable(classDate, schedule.startTime, LocalDateTime.now(clock))`
      ④ `ReservationPassPolicy.requiredPassType(schedule.classType)` — 저녁반이면 여기서 거부
      ⑤ `classSessionService.getOrCreate(schedule, classDate)` → `session.assertReservable()` (휴강 거부)
      ⑥ 중복 예약 사전 검사 — 같은 회원의 같은 `classDate`·`startTime` 활성 예약 존재 여부.
         `ReservationRepository`에 `existsByMemberIdAndClassDateAndStartTimeAndStatus(...)`를 추가해 쓴다.
         **사전 검사는 사용자에게 정확한 에러를 주기 위한 것이고 실제 방어는 부분 유니크 인덱스다**(D-021) —
         이 사실을 주석에 남긴다
      ⑦ `classSessionRepository.incrementReservedCountIfCapacityAvailable(sessionId)` — 반환 0이면
         `ReservationCapacityExceededException`. **이 호출 전에 `session.classType`·`startTime`·`endTime`·`capacity`를
         지역 변수로 미리 꺼내둔다**(`clearAutomatically`로 준영속화됨)
      ⑧ `passRepository.findDeductionCandidates(memberId, passType, classDate, DEDUCTION_AMOUNT)` →
         `ReservationPassPolicy.selectCandidate(...)` → 없으면 `InsufficientPassCountException`.
         후보의 `id`를 지역 변수로 꺼내둔다
      ⑨ `passRepository.adjustRemainingCount(passId, DEDUCTION_AMOUNT.negate())` — 반환 0이면
         **재시도 없이 즉시** `InsufficientPassCountException` (CONTEXT.md Claude's Discretion 확정:
         같은 회원이 자기 이용권을 동시에 두 곳에서 쓰는 경우뿐이라 빈도가 극히 낮다). 이 판단을 주석에 남긴다
      ⑩ `Reservation` INSERT — `classType`/`classDate`/`startTime`을 세션에서 복사해 채운다.
         부분 유니크 인덱스 위반 시 `DataIntegrityViolationException`이 나므로 이를 잡아
         `DuplicateReservationException`으로 변환한다. **INSERT 실패이므로 트랜잭션이 여기서 끝나도 안전하다**
         (⑦⑨의 UPDATE는 같은 트랜잭션이라 함께 롤백된다)
      ⑪ `PassTransaction(pass, amount = DEDUCTION_AMOUNT.negate(), reason = RESERVE, note = null,
         admin = null, member = 예약 회원, occurredAt = OffsetDateTime.now(clock))` 저장 — **행위자 기준**(Pitfall 4)
      ⑫ `notificationService.createReservationCreated(reservation)`
    (4) `MemberReservationServiceTest` — `<behavior>` 10개 항목을 각각 `@Test`로 검증한다.
    실패 케이스마다 **"요청 전후로 잔여·예약 인원·행 수가 동일하다"**를 함께 단언한다 — 이게 롤백 검증의 핵심이다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.reservation.MemberReservationServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.reservation.MemberReservationServiceTest"` 종료 코드 0, 10개 이상 테스트 실행
    - 실패 케이스 테스트 6종(저녁반/휴강/창밖/중복/정원초과/잔여부족)이 각각 잔여·`reserved_count` 불변을 단언한다
    - `MemberReservationService.kt`의 `reserve`에 `@Transactional`이 붙어 있고 클래스에는 `@Transactional(readOnly = true)`가 붙어 있다
    - `MemberReservationService.kt`가 `PassTransaction(... admin = null, member = ...)` 형태로 회원 주체를 기록한다
    - `ReserveRequest.kt`에 `classSessionId` 필드가 없다
    - `MemberReservationService.kt`가 `adjustRemainingCount` 반환 0에 대해 재시도 루프를 만들지 않는다 (`while`/`retry` 없음)
    - `./gradlew test --tests "com.goldwrestling.pass.*"` 종료 코드 0 (Phase 3 회귀 없음)
  </acceptance_criteria>
  <done>예약 1건이 5개 테이블을 원자적으로 갱신하고, 8가지 거부 사유가 각각 고유 에러로 구분된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 회원 요청 → 예약 생성 서비스 | 회원이 `classScheduleId`·`classDate`를 임의로 보낼 수 있다 |
| 서비스 → 다중 테이블 쓰기 | 5개 테이블 갱신 중 어느 하나만 남으면 원장 불변식이 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Tampering | 임의 `classSessionId` 지정으로 타 지점·타 시간표 수업에 예약 | mitigate | `ReserveRequest`가 세션 id를 받지 않는다. 시간표 id + 날짜만 받고 서버가 세션을 확보하며, 시간표의 `branchId`가 회원 지점과 같은지 검사 |
| T-04-28 | Tampering | 요일이 맞지 않는 (시간표, 날짜) 조합으로 존재하지 않는 수업 생성 | mitigate | ② 단계에서 `schedule.dayOfWeek == classDate.dayOfWeek` 검사 후 거부 |
| T-04-29 | Tampering | 정원 사전 검사와 커밋 사이의 레이스로 초과 예약 | mitigate | 정원 확보를 조건부 UPDATE(⑦)로만 수행하고 반환 0을 예외로 변환. 부분 유니크 인덱스가 2차 방어선. 실증은 04-08 동시성 테스트 |
| T-04-30 | Repudiation | 잔여만 줄고 이력이 남지 않는 차감 | mitigate | ⑨와 ⑪이 같은 트랜잭션. 실패 케이스 테스트가 "잔여 불변 + 이력 행 수 불변"을 함께 단언 |
| T-04-31 | Repudiation | 회원 셀프 예약을 관리자 주체로 기록 | mitigate | ⑪에서 `admin = null, member = 예약 회원`. `ck_pass_transaction_subject`가 둘 다 채우는 것을 거부 |
| T-04-32 | Information Disclosure | `ReservationResponse`에 타 회원 정보 노출 | mitigate | 응답 DTO에 회원 필드를 두지 않는다 (본인 예약이므로 불필요) |
| T-04-33 | Denial of Service | `adjustRemainingCount` 0행 시 무한 재시도로 DB 과부하 | mitigate | 재시도하지 않고 즉시 실패시킨다 (CONTEXT.md 확정). acceptance criteria로 재시도 루프 부재를 검사 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 통과
- 이 플랜은 컨트롤러를 만들지 않는다 — API 노출과 동시성 실증은 04-08이 같은 청크 안에서 이어서 한다 (D-084: 불변식과 방어 코드는 한 청크)
</verification>

<success_criteria>
- 예약 생성이 5개 테이블을 원자적으로 갱신하고, 8가지 실패 경로 모두에서 아무 흔적도 남지 않는다
- 차감 이력의 주체가 행위자 기준(회원)으로 기록된다
- 알림 생성이 4개 호출부가 공유할 헬퍼로 분리된다
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-07-SUMMARY.md` when done.
청크 B(`feature/phase-04b-reservation`) 진행 중 — PR은 wave 10(04-10) 완료 후에 낸다 (D-084).
</output>
</content>
