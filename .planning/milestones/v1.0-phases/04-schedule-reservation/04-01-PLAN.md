---
phase: 04-schedule-reservation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glossary.md
  - docs/decisions.md
  - docs/policies.md
  - docs/error-codes.md
  - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
  - src/main/kotlin/com/goldwrestling/schedule/ScheduleExceptions.kt
  - src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt
  - src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt
  - src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt
autonomous: true
requirements: [SCHED-02, RESV-03, RESV-04, RESV-06, RESV-09]

must_haves:
  truths:
    - "Phase 4가 새로 쓰는 개념(ClassSessionStatus·ReservationStatus·NotificationType·WeekRange·refunded)이 docs/glossary.md에 등재되어 있다"
    - "Phase 4에서 확정한 설계 결정 10건(D-085~D-094)이 docs/decisions.md에 기록되어 있다"
    - "시간표·예약 도메인 실패 13종이 각각 고유한 ErrorCode 값으로 구분되어 응답된다"
    - "policies.md가 1:1 예약 가능 타임·차감 대상 이용권 선택·중복 예약 금지·예약 창 경계·동시성 검증 방법을 명시한다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      provides: "시간표·예약 도메인 에러코드 13종"
      contains: "RESERVATION_CAPACITY_EXCEEDED"
    - path: "src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt"
      provides: "DomainException 상속 예약 예외 7종"
      contains: "class ReservationCapacityExceededException"
    - path: "src/main/kotlin/com/goldwrestling/schedule/ScheduleExceptions.kt"
      provides: "DomainException 상속 시간표·수업 예외 5종(예약 창 포함)"
      contains: "class ClassSessionCanceledException"
    - path: "docs/error-codes.md"
      provides: "FE 분기 계약 표에 시간표·예약 섹션"
      contains: "## 시간표·예약 코드 (Phase 4)"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt"
      to: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      via: "DomainException(ErrorCode.XXX, message) 생성자 호출"
      pattern: "DomainException\\(\\s*ErrorCode\\."
    - from: "src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt"
      to: "docs/error-codes.md"
      via: "targetSectionPrefixes에 시간표·예약 섹션 추가"
      pattern: "시간표·예약 코드"
---

<objective>
Phase 4가 쓸 **용어·에러코드·설계 결정 문서를 코드보다 먼저 정합화**하고, 도메인 예외 클래스를 선언한다.

Purpose: CLAUDE.md 규칙 3(새 개념은 glossary에 등재 후 사용)·규칙 5(설계 결정은 decisions.md 기록)·
D-028(ErrorCode와 error-codes.md는 항상 1:1)을 지키려면 이 작업이 스키마·엔티티보다 **앞서야** 한다.
뒤로 미루면 이후 12개 플랜이 등재되지 않은 이름을 코드에 먼저 박아 넣는다.
또 CONTEXT.md `<open_items>`가 지정한 policies.md 보강 4건(§2 1:1 타임 범위, §3 이용권 선택·중복 예약·
예약 창, §8 k6 문구 정정)이 이후 플랜의 판정 로직 근거이므로 여기서 먼저 스펙을 확정한다.

Output: glossary 신규 개념 표, decisions.md D-085~D-094, policies.md §2·§3·§8 보강,
ErrorCode 13종 + error-codes.md 섹션, `schedule/ScheduleExceptions.kt`·`reservation/ReservationExceptions.kt`,
`pass/PassExceptions.kt`에 `PassHasActiveReservationException` 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/04-schedule-reservation/04-CONTEXT.md
@.planning/phases/04-schedule-reservation/04-RESEARCH.md
@.planning/phases/04-schedule-reservation/04-PATTERNS.md
@docs/policies.md
@docs/glossary.md
@docs/decisions.md
@docs/error-codes.md
</context>

<interfaces>
<!-- 실행자가 그대로 상속·확장할 기존 계약. 코드베이스 탐색 없이 이 값을 쓴다. -->

`common/error/DomainException.kt`:
- `abstract class DomainException(val errorCode: ErrorCode, message: String, val status: HttpStatus = errorCode.defaultStatus) : RuntimeException(message)`

`common/error/ErrorCode.kt`:
- `enum class ErrorCode(val defaultStatus: HttpStatus)` — 현재 마지막 상수는 `PASS_STATE_CONFLICT(HttpStatus.CONFLICT)`
- 이미 존재해 **재사용할** 코드: `PASS_NOT_FOUND`(NOT_FOUND), `INSUFFICIENT_PASS_COUNT`(CONFLICT, 잔여 부족),
  `PASS_ALREADY_CANCELED`(CONFLICT), `MEMBER_NOT_ACTIVE`(FORBIDDEN), `MEMBER_NOT_FOUND`(NOT_FOUND)

`pass/PassExceptions.kt` (예외 선언 형식의 원본):
- `class PassNotFoundException(passId: Long?) : DomainException(ErrorCode.PASS_NOT_FOUND, "이용권을 찾을 수 없습니다.")`

`test/.../common/error/ErrorCodeRegistryTest.kt`:
- `private val targetSectionPrefixes = listOf("## 공통 코드", "## 인증·회원 코드", "## 이용권 코드")`

`docs/decisions.md`: 현재 마지막 항목은 `## D-084.` — 신규 번호는 D-085부터
`docs/policies.md`: §1에 D-085(등록 취소 선행 조건), §3에 D-086(취소=상태 전환·변경 원자성)이 **이미 반영됨**.
이 플랜은 §2·§3·§8만 추가 보강한다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: glossary 신규 개념 + decisions.md D-085~D-094 + policies.md §2·§3·§8 보강</name>
  <files>docs/glossary.md, docs/decisions.md, docs/policies.md</files>
  <read_first>
    - docs/glossary.md (전체 — "핵심 엔티티" 표, "수업 종류 (ClassType)" 표, "차감 사유(TransactionReason)" 표, "기타 규칙" 금지어 목록)
    - docs/decisions.md D-080~D-084 (기록 형식과 마지막 번호 확인)
    - docs/policies.md §1, §2, §3, §7, §8 (전체 흐름 파악 — §1·§3의 D-085/D-086 문장은 이미 반영되어 있다)
    - .planning/phases/04-schedule-reservation/04-CONTEXT.md `<decisions>`·`<open_items>` 전체
    - .planning/phases/04-schedule-reservation/04-RESEARCH.md "Open Questions"
  </read_first>
  <action>
    (1) `docs/glossary.md` "수업 종류 (ClassType)" 표 아래에 **시간표·예약 (Phase 4)** 표를 새로 추가하고 아래 이름을
    그대로 등재한다. 이후 모든 플랜이 이 이름만 쓴다. 금지어(`Ticket`/`Voucher`/`Coupon`/`Booking`/`Course`/`Session` 단독)
    저촉 여부를 확인한다 — 특히 날짜별 수업은 항상 `ClassSession`이고 `Session` 단독은 금지다.
    · 정기 시간표 → `ClassSchedule` (요일+시각+수업종류+정원, Flyway 시드 고정)
    · 날짜별 수업 → `ClassSession` (`ClassSessionStatus`: `SCHEDULED` / `CANCELED`(휴강))
    · 예약 → `Reservation` (`ReservationStatus`: `ACTIVE` / `CANCELED`)
    · 복구 여부 → `refunded` (DB `reservation.refunded`) — 관리자 대리 취소의 "복구 안 함" 선택 결과
    · 취소 주체 → `canceledByMember` / `canceledByAdmin` (둘 중 정확히 하나)
    · 예약 인원 → `reservedCount`, 정원 → `capacity`
    · 관리자 알림 → `Notification`, 종류 → `NotificationType`
      (`RESERVATION_CREATED` / `RESERVATION_CANCELED_BY_MEMBER` / `RESERVATION_CHANGED_BY_MEMBER` /
       `RESERVATION_CANCELED_BY_ADMIN` / `RESERVATION_CHANGED_BY_ADMIN` / `CLASS_SESSION_SUSPENDED`)
    · 주 범위 → `WeekRange` (월요일 시작 월~일 7일)
    · 휴강 처리 → `suspend`(수업을 휴강으로 전환) / 휴강 해제 → `resume`

    (2) `docs/decisions.md` 끝에 D-085~D-094를 각 3~4줄로 추가한다. 기존 D-080~D-084와 동일한
    `## D-0NN. 한 줄 제목` + 결정/이유/영향 형식을 유지한다. 내용은 다음으로 고정한다.
    · D-085 등록 취소 선행 조건 — 대상 이용권으로 잡힌 활성 예약이 하나라도 있으면 등록 취소를 거부한다.
      관리자가 대리 취소(복구 안 함)로 먼저 정리한 뒤 등록을 취소하는 2단계. 자동 연쇄 취소는 만들지 않는다
      (연쇄 복구가 `REGISTRATION_CANCELED` 상쇄와 충돌). [policies §1 반영 완료]
    · D-086 예약 취소는 물리 삭제가 아니라 상태 전환. 취소한 타임의 재예약을 허용하므로 전체 유니크 제약 대신
      `status = 'ACTIVE'` 부분 유니크 인덱스로 중복을 막는다. 변경은 전용 엔드포인트 1개 + 단일 트랜잭션
      (취소 UPDATE + 새 행 INSERT), 원장에 `CANCEL_REFUND`(+1)와 `RESERVE`(−1) 2건 모두 남긴다.
      같은 수업 종류 안에서만 변경 가능. [policies §3 반영 완료]
    · D-087 차감 대상 이용권 선택 — 같은 종류를 여러 장 보유하면 `end_date` 오름차순(동률 시 `id` 오름차순)
      첫 장에서 차감한다. **합산 금지**(0.5회 두 장으로 1회 예약 불가) → 예약 1건 ↔ 이용권 1장이 항상 대응하고
      예약 행이 `pass_id`를 보유한다. 유효기간 판정 기준일은 예약일이 아니라 **수업날**이다.
      복구는 원래 차감한 그 이용권으로 하되, 만료 상태여도 복구하고 등록 취소(`CANCELED`) 상태면 복구하지 않는다.
    · D-088 중복 예약 금지 — 같은 회원이 같은 날짜·같은 시각에 두 건을 예약할 수 없다. 같은 날 다른 타임 복수
      예약은 제한 없음. 1:1(`LESSON`) 한도는 "날짜 + 시각" 단위로 1명이며 같은 시각의 예약제 정원과 무관하다.
    · D-089 정기 시간표는 Flyway 시드로 고정하고 관리자 CRUD API를 만들지 않는다(MVP 단일 지점).
      정원은 `ClassSchedule`에 둔다(요일+시각 단위, 예약제 10 / 1:1 1). 저녁반(`EVENING`)도 행으로 정의하되
      `capacity`는 NULL이고 예약 대상이 아니다.
    · D-090 `ClassSession`은 사전 배치 생성이 아니라 **필요할 때 생성**한다(첫 예약 또는 휴강 처리 시).
      배치 인프라는 Phase 5 소관이고, 배치가 하루라도 안 돌면 예약 자체가 막히기 때문. 경쟁은
      `(class_schedule_id, class_date)` 유니크 제약 + `INSERT ... ON CONFLICT DO NOTHING`으로 흡수한다.
      시작·종료 시각과 정원을 시간표에서 **복사해 자기 컬럼으로 보유**한다(과거 수업이 시간표 수정에 소급되지 않도록).
    · D-091 예약 창 — 오픈은 해당 주 월요일 00:00(`Asia/Seoul`), 마감은 수업 시작 시각 전까지.
      조회 범위는 이번 주 + 다음 주 2주치이며 다음 주는 조회만 가능하다. 주 시작은 월요일, 판정은 `Clock` 빈 기준.
    · D-092 회원 시간표 응답은 저녁반도 함께 노출하되 예약 대상이 아님을 표현하고, 셀에는 **예약 인원 숫자만**
      내려준다. **예약자 명단은 회원에게 주지 않는다** — 회원 간 개인정보 노출. 명단은 관리자 보드에서만 본다.
    · D-093 알림 수신자는 관리자 하나뿐이다(회원용 알림 없음). 이벤트 6종(`NotificationType`)을 기록하며
      이 phase는 스키마 + 레코드 생성까지만 구현한다(조회·확인·폴링은 Phase 6). 조회 시점 조인이 필요 없도록
      회원명·수업 종류·수업 일시를 비정규화해 담는다. **휴강 알림은 세션당 1건 요약형**이라
      `Notification`이 `reservation_id` 외에 `class_session_id`(nullable) 참조를 갖는다.
    · D-094 "초과 예약 0건" 증명은 JVM `ExecutorService`+`CountDownLatch` 동시성 통합테스트로 한다
      (conventions §10.4 · `add-domain-test` 스킬과 동일 방식). k6 부하테스트는 HTTP 레벨 부하·처리량 도구로
      목적이 다르고 새 인프라를 요구해 이번 phase 범위에서 제외한다. policies §8 문구를 이에 맞춰 정정한다.

    (3) `docs/policies.md`를 아래 3곳 보강한다. **이것은 스펙 변경이므로 완료 보고에 "policies.md를 이렇게 고쳤다"를
    반드시 명시**한다 (CLAUDE.md 문서 우선순위).
    · §2 — "1:1 레슨: 체육관 운영 중인 모든 타임슬롯" 문장을 **"저녁반·예약제 수업이 열리는 모든 타임(주 26타임)"**으로
      구체화한다. 현재 문구는 "운영 중인"의 범위가 두 갈래로 읽힌다.
    · §3 — 아래 4줄을 추가한다. 각 줄 끝에 근거 결정 번호를 단다.
      - 차감 대상 이용권: 같은 종류 여러 장 보유 시 유효기간 만료가 가장 임박한 한 장에서 차감하고,
        여러 장을 합산해 한 건을 예약할 수 없다 (D-087)
      - 유효기간 판정 기준일은 예약일이 아니라 **수업날**이다 (D-087)
      - 같은 회원이 같은 날짜·같은 시각에 두 건을 예약할 수 없다 (D-088)
      - 예약 창: 해당 주 월요일 00:00(`Asia/Seoul`) 오픈, 수업 시작 시각 전 마감, 조회는 이번 주 + 다음 주 2주치 (D-091)
    · §8 — "k6 부하테스트로 검증한다"를 **"JVM `ExecutorService`+`CountDownLatch` 동시성 통합테스트로 검증한다
      (D-094)"**로 정정한다.
  </action>
  <verify>
    <automated>grep -v '^#' docs/decisions.md | grep -c "D-09" </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## D-08[5-9]\|^## D-09[0-4]" docs/decisions.md` 가 10
    - `docs/glossary.md`가 `ClassSchedule`, `ClassSession`, `ClassSessionStatus`, `ReservationStatus`, `NotificationType`, `WeekRange`, `refunded` 문자열을 모두 포함한다
    - `docs/glossary.md`에 새로 추가한 코드 이름 중 `Booking`·`Voucher`·`Ticket`·`Course`를 포함하는 것이 없다
    - `grep -c "k6" docs/policies.md` 가 0
    - `docs/policies.md` §8이 `ExecutorService` 문자열을 포함한다
    - `docs/policies.md` §3이 `D-087`, `D-088`, `D-091` 참조를 각각 포함한다
    - `docs/policies.md` §2가 `26타임` 문자열을 포함한다
  </acceptance_criteria>
  <done>이후 14개 플랜이 쓸 신규 이름과 설계 결정 10건이 docs/에 등재되어, 코드가 문서보다 앞서지 않는다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorCode 13종 + error-codes.md 섹션 + 예외 클래스 3파일</name>
  <files>
    src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt,
    docs/error-codes.md,
    src/main/kotlin/com/goldwrestling/schedule/ScheduleExceptions.kt,
    src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt,
    src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt,
    src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt
  </files>
  <behavior>
    - `ErrorCodeRegistryTest`의 정방향 검증(enum → 문서): 신규 13개 코드가 각각 `docs/error-codes.md` 표에 행으로 존재한다
    - `ErrorCodeRegistryTest`의 역방향 검증(문서 → enum): `## 시간표·예약 코드 (Phase 4)` 섹션의 모든 행이 enum 상수로 존재한다
    - 13개 예외 클래스가 각각 서로 다른 `ErrorCode`를 생성자에 넘긴다
  </behavior>
  <read_first>
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (전체 — 마지막 상수 `PASS_STATE_CONFLICT` 뒤에 이어 붙인다)
    - src/main/kotlin/com/goldwrestling/common/error/DomainException.kt (전체)
    - src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt (전체 — 예외 선언 형식의 원본이자 이번에 1건 추가할 대상)
    - src/test/kotlin/com/goldwrestling/common/error/ErrorCodeRegistryTest.kt (전체 — `targetSectionPrefixes` 위치)
    - docs/error-codes.md (전체 — 표 열 구성과 폴백 규칙 문단)
    - .planning/phases/04-schedule-reservation/04-PATTERNS.md "common/error/ErrorCode.kt — 신규 에러코드 추가"
  </read_first>
  <action>
    (1) `ErrorCode.kt`의 `PASS_STATE_CONFLICT` 뒤에 아래 13종을 추가한다. 각 상수 위 한 줄 KDoc 관례를 유지한다.
    **시간표·수업 (4종)**
    · `CLASS_SCHEDULE_NOT_FOUND`(NOT_FOUND) — 요청한 정기 시간표 행이 없음
    · `CLASS_SESSION_CANCELED`(CONFLICT) — 휴강된 수업에 예약·변경 시도 (policies §7)
    · `CLASS_SESSION_NOT_CANCELED`(CONFLICT) — 휴강 상태가 아닌 수업에 휴강 해제 시도
    · `CLASS_SESSION_NOT_RESERVABLE`(CONFLICT) — 예약 대상이 아닌 수업 종류(`EVENING`) 예약 시도 (D-089)
    **예약 (8종)**
    · `RESERVATION_NOT_FOUND`(NOT_FOUND) — 대상 예약 없음(타인 예약 조회 포함 — 존재 여부를 흘리지 않는다)
    · `RESERVATION_CAPACITY_EXCEEDED`(CONFLICT) — 정원 초과 (RESV-06)
    · `RESERVATION_WINDOW_CLOSED`(CONFLICT) — 예약 창 밖(다음 주 예약, 시작 시각 경과) (D-091)
    · `DUPLICATE_RESERVATION`(CONFLICT) — 같은 회원·같은 날짜·시각 중복 예약 (D-088)
    · `SAME_DAY_MODIFICATION_NOT_ALLOWED`(CONFLICT) — 당일 취소·변경 시도 (policies §3).
      취소와 변경이 동일 규칙이라 코드를 하나로 둔다 — FE 분기가 나뉘지 않는다
    · `RESERVATION_ALREADY_CANCELED`(CONFLICT) — 이미 취소된 예약에 재취소·변경 시도
    · `RESERVATION_TYPE_MISMATCH`(BAD_REQUEST) — 변경 시 수업 종류가 다름 (`SESSION`↔`LESSON` 교차, D-086)
    · `RESERVATION_STATE_CONFLICT`(CONFLICT) — 조건부 갱신 경쟁 패배 등 위 코드로 나뉘지 않는 예약 상태 충돌
    **이용권 (1종)**
    · `PASS_HAS_ACTIVE_RESERVATION`(CONFLICT) — 활성 예약이 있어 등록 취소 거부 (D-085)
    **재사용(추가하지 않음)**: 잔여 부족은 기존 `INSUFFICIENT_PASS_COUNT`, 이용권 없음은 `PASS_NOT_FOUND`,
    비활성 회원 접근은 `MEMBER_NOT_ACTIVE`를 그대로 쓴다.

    (2) `docs/error-codes.md`에 `## 시간표·예약 코드 (Phase 4)` 섹션과 표를 추가한다. 열 구성
    (`코드`/`HTTP 상태`/`의미`/`발생 지점`)과 `| \`CODE\` | ...` 행 형식을 기존 `## 이용권 코드 (Phase 3)` 섹션과
    **완전히 동일**하게 맞춘다 — 형식이 다르면 (4)의 역방향 검증이 코드를 못 읽는다.
    `PASS_HAS_ACTIVE_RESERVATION`은 이용권 코드 섹션이 아니라 이 새 섹션에 넣는다(발생 원인이 예약이므로).
    발생 지점 열에는 `ClassSession` / `Reservation` / `MemberReservationService` / `AdminReservationService` /
    `AdminScheduleService` / `AdminPassService` 중 해당하는 것을 적는다.

    (3) 예외 클래스를 선언한다. 모두 `DomainException`을 상속하고, 메시지는 **사용자 대면 한국어 문구**로만 쓰며
    id·SQL·엔티티 필드명을 노출하지 않는다(`PassNotFoundException`이 passId를 받되 메시지에 쓰지 않는 선례).
    · 신규 `src/main/kotlin/com/goldwrestling/schedule/ScheduleExceptions.kt` (패키지 `com.goldwrestling.schedule`) — **5종**:
      `ClassScheduleNotFoundException(classScheduleId: Long?)`, `ClassSessionCanceledException`,
      `ClassSessionNotCanceledException`, `ClassSessionNotReservableException`,
      `ReservationWindowClosedException(message: String)`
      — `ReservationWindowClosedException`을 여기 두는 이유: 이 예외를 던지는 `ReservationWindow`(04-04)가
      `schedule` 패키지에 있고, `schedule` → `reservation` 역참조를 만들지 않기 위해서다.
      의존 방향은 `reservation → schedule` 한 갈래만 유지한다 (D-018)
    · 신규 `src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt` (패키지 `com.goldwrestling.reservation`) — **7종**:
      `ReservationNotFoundException(reservationId: Long?)`, `ReservationCapacityExceededException`,
      `DuplicateReservationException`, `SameDayModificationNotAllowedException`,
      `ReservationAlreadyCanceledException`, `ReservationTypeMismatchException`,
      `ReservationStateConflictException(message: String)`
    · 기존 `pass/PassExceptions.kt`에 `PassHasActiveReservationException` 1건 추가
    `GlobalExceptionHandler`는 건드리지 않는다 — `DomainException` 상속만으로 ProblemDetail 변환이 자동 적용된다.

    (4) `ErrorCodeRegistryTest.kt`의 `targetSectionPrefixes` 리스트에 `"## 시간표·예약 코드"`를 추가한다.
    이 줄을 빠뜨리면 새 섹션이 역방향 검증에서 통째로 제외되어 표에 오타가 나도 빌드가 통과한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.common.error.ErrorCodeRegistryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.common.error.ErrorCodeRegistryTest"` 종료 코드 0
    - `ErrorCode.kt`가 `CLASS_SCHEDULE_NOT_FOUND`, `CLASS_SESSION_CANCELED`, `CLASS_SESSION_NOT_CANCELED`, `CLASS_SESSION_NOT_RESERVABLE`, `RESERVATION_NOT_FOUND`, `RESERVATION_CAPACITY_EXCEEDED`, `RESERVATION_WINDOW_CLOSED`, `DUPLICATE_RESERVATION`, `SAME_DAY_MODIFICATION_NOT_ALLOWED`, `RESERVATION_ALREADY_CANCELED`, `RESERVATION_TYPE_MISMATCH`, `RESERVATION_STATE_CONFLICT`, `PASS_HAS_ACTIVE_RESERVATION` 13개 상수를 모두 포함한다
    - `docs/error-codes.md`가 `## 시간표·예약 코드 (Phase 4)` 헤딩을 포함한다
    - `ErrorCodeRegistryTest.kt`가 `"## 시간표·예약 코드"` 문자열을 포함한다
    - `grep -c "ErrorCode\." src/main/kotlin/com/goldwrestling/reservation/ReservationExceptions.kt` == 7
    - `grep -c "ErrorCode\." src/main/kotlin/com/goldwrestling/schedule/ScheduleExceptions.kt` == 5
    - `schedule` 패키지 소스가 `com.goldwrestling.reservation` 을 import 하지 않는다 (`grep -rc "import com.goldwrestling.reservation" src/main/kotlin/com/goldwrestling/schedule/` == 0)
    - `./gradlew ktlintCheck` 종료 코드 0
  </acceptance_criteria>
  <done>시간표·예약 실패 13종이 FE가 구분할 수 있는 고유 코드로 응답되고, enum과 문서가 빌드에서 강제로 맞춰진다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 → 클라이언트(에러 응답) | 예약 실패 사유가 응답 본문으로 나간다 — 타인 예약의 존재 여부·내부 구조가 새어나갈 수 있는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `ReservationExceptions.kt` 메시지 | mitigate | 예외 message를 사용자 대면 한국어 문구로 고정하고 reservationId·memberId·SQL·컬럼명을 넣지 않는다 |
| T-04-02 | Information Disclosure | 타인 예약 접근 시 응답 코드 | mitigate | 소유자가 아닌 예약 접근은 403이 아니라 `RESERVATION_NOT_FOUND`(404)로 응답한다 — 403은 "그 id의 예약이 존재한다"를 알려준다. 이 규칙을 `docs/error-codes.md`의 `RESERVATION_NOT_FOUND` 행 "의미" 열에 명시한다 |
| T-04-03 | Repudiation | 설계 결정 미기록 | mitigate | D-085~D-094를 decisions.md에 기록해 이후 플랜·phase가 근거 없이 다르게 구현하는 것을 막는다 (CLAUDE.md 규칙 5) |
| T-04-04 | Tampering | error-codes.md ↔ ErrorCode 불일치 | mitigate | `ErrorCodeRegistryTest.targetSectionPrefixes`에 신규 섹션을 추가해 양방향 검증을 빌드에서 강제 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 신규 외부 패키지를 설치하지 않는다 (04-RESEARCH.md "Package Legitimacy Audit — 해당 없음"). 설치 태스크가 생기면 slopcheck + 차단형 휴먼 체크포인트를 먼저 넣는다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat` → `./gradlew build` 순서로 실행해 통과 (conventions §11.5)
- `docs/glossary.md`·`docs/decisions.md`·`docs/policies.md`·`docs/error-codes.md` 4개 문서가 서로 모순되지 않는다
  (특히 policies §3의 예약 창 문장과 D-091 본문이 같은 경계를 말해야 한다)
</verification>

<success_criteria>
- 이후 플랜이 쓸 신규 이름이 glossary에 등재됨 (금지어 저촉 0건)
- D-085~D-094 10건이 decisions.md에 기록됨
- policies.md §2·§3·§8이 이후 판정 로직의 근거로 쓸 수 있을 만큼 구체화됨 (k6 문구 잔존 0건)
- 시간표·예약 에러코드 13종이 enum·문서·예외 클래스 세 곳에서 1:1로 일치하고 빌드가 이를 강제함
</success_criteria>

<output>
Create `.planning/phases/04-schedule-reservation/04-01-SUMMARY.md` when done.
이 플랜은 청크 A(`feature/phase-04a-schedule`, wave 1~5)의 첫 플랜이다 — 청크가 끝나기 전에는 PR을 내지 않는다 (D-084).
</output>
</content>
</invoke>
