---
phase: 06-operations
plan: 11
type: execute
wave: 9
depends_on: ["06-03", "06-04", "06-08", "06-10"]
files_modified:
  - README.md
  - docs/error-codes.md
  - docs/glossary.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [ATTEND-01, ATTEND-02, NOTICE-01, NOTICE-02, NOTIF-02, NOTIF-03]
must_haves:
  truths:
    - "운영자가 따라 할 수 있는 미사용 차감 cron 켜기 체크리스트가 README에 있다"
    - "6개 요구사항 각각에 대해 '무엇이 증명하는가'가 SUMMARY의 대응표로 남는다"
    - "전체 회귀가 캐시 없이 그린이다"
    - "문서(에러코드·용어)와 실제 코드가 어긋나지 않는다"
    - "사용자가 실제 앱·실제 DB로 출석·차감·복구·공지·알림·피드를 눈으로 확인했다"
  artifacts:
    - path: "README.md"
      provides: "cron 운영 켜기 절차(D-130)"
      contains: "BATCH_INACTIVITY_SCHEDULER_ENABLED"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "환경변수 이름 일치"
      pattern: "BATCH_INACTIVITY_SCHEDULER_ENABLED"
---

<objective>
Phase 6을 마감한다 — (1) Phase 5에서 이월된 **cron 운영 켜기 절차**를 운영자가 따라 할 수 있는
체크리스트로 문서화하고(D-130), (2) 문서와 코드의 최종 정합을 확인하고, (3) 캐시 없는 전체 회귀를
돌리고, (4) 사용자가 실제 앱으로 핵심 흐름을 확인한다.

목적: CR-03이 코드로 닫혔어도(06-03) **켜는 판단은 배포 절차의 일**이다(D-121 fail-safe 유지).
절차가 산문에만 있으면 잊히므로 README 체크리스트로 남긴다 — Phase 5의 "cron은 꺼 둔 채 배포"
전제가 산문에만 있어 잊히기 쉬웠던 문제를 반복하지 않는다.

산출물: README 절차 + 문서 정합 수정 + 회귀 결과 + 사용자 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@.planning/phases/06-operations/06-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README에 미사용 차감 cron 운영 켜기 체크리스트를 추가한다</name>
  <files>README.md, .planning/ROADMAP.md</files>
  <read_first>
    - README.md (기존 섹션 구성·표 스타일 — 어디에 운영 섹션을 넣을지 결정)
    - .env.example 50-62행 (`BATCH_INACTIVITY_SCHEDULER_ENABLED`·`BATCH_INACTIVITY_POLICY_EFFECTIVE_DATE`·`BATCH_INACTIVITY_MAX_DEDUCTIONS_PER_RUN`·`BATCH_INACTIVITY_STALE_RUN_TIMEOUT` 실제 키 이름)
    - docs/decisions.md D-116·D-119·D-121·D-130 (킬 스위치 기본 꺼짐, 정책 시행일 하한, 켜기 절차 3단계)
    - .planning/ROADMAP.md Phase 5 "배포 조건" 블록 (".env.example 기본값을 false로 바꿔 두었다 — Phase 6에서 되돌린다" 문장이 D-130과 충돌한다)
    - src/main/kotlin/com/goldwrestling/batch/InactivityBatchScheduler.kt 및 InactivityBatchProperties.kt (cron 표현식·설정 키 실제 값)
  </read_first>
  <action>
README.md에 "## 미사용 차감 배치 운영 (cron 켜기 절차)" 섹션을 추가한다 — 위치는 "로컬 실행"
다음, 배포 관련 섹션 근처. 내용은 운영자가 순서대로 체크할 수 있는 형태로 쓴다:

1. **전제 확인** — 출석 배선(Phase 6, `InactivityBatchRunner`의 `lastAttendanceDate`)이 배포됐는지
   확인한다. 이 배선 없이 cron을 켜면 저녁반에만 나오는 `SESSION_PASS` 회원이 2주마다 1.0회씩
   부당 차감된다(CR-03의 실제 피해).
2. **수동 실행 1회 검증** — `POST /api/admin/batch/inactivity-runs`(202 접수) → `Location` 헤더의
   실행 상태 조회로 `SUCCESS`와 처리·차감 건수를 확인한다. **정책 시행일(`BATCH_INACTIVITY_POLICY_EFFECTIVE_DATE`,
   기본 `2026-09-01`) 이전에는 차감 0건이 정상**이다 — 0건을 "동작 안 함"으로 오독하지 않는다(D-119).
3. **cron 활성화** — 배포 환경 변수에 `BATCH_INACTIVITY_SCHEDULER_ENABLED=true`를 **명시적으로**
   설정한다. 기본값은 꺼짐이며 코드로 되돌리지 않는다(D-121 fail-safe — 설정을 빠뜨리면 꺼진다).
4. **되돌리기** — 이상 징후(예상보다 많은 `INACTIVITY` 이력) 시 같은 변수를 `false`로 바꿔 재배포하면
   즉시 멈춘다. 이미 발생한 차감은 관리자 수동 가감(`ADMIN_ADJUST`)으로 정정한다 — 배치는 환불하지
   않는다(D-127).

관련 설정 4종(`BATCH_INACTIVITY_SCHEDULER_ENABLED`/`_POLICY_EFFECTIVE_DATE`/`_MAX_DEDUCTIONS_PER_RUN`/
`_STALE_RUN_TIMEOUT`)을 이름·기본값·의미 3열 표로 함께 정리한다. 값은 `.env.example`과
`InactivityBatchProperties`에서 확인한 실제 값을 쓴다.

**`.env.example`의 기본값 `false`를 바꾸지 않는다** — D-130이 기본 꺼짐 유지를 확정했다.
`.planning/ROADMAP.md` Phase 5 배포 조건 블록의 ".env.example 기본값을 false로 바꿔 두었다 —
Phase 6에서 되돌린다" 문장을 "Phase 6에서 CR-03을 닫았고, 기본값은 D-130에 따라 꺼짐으로 유지한다 —
켜는 것은 README의 운영 절차가 담당한다"로 정정한다.

이 태스크는 conventions §10.0 면제 대상이다(문서) — 테스트를 만들지 않는다.
  </action>
  <verify>
    <automated>grep -q "BATCH_INACTIVITY_SCHEDULER_ENABLED=true" README.md && grep -q "2026-09-01" README.md && grep -q "^BATCH_INACTIVITY_SCHEDULER_ENABLED=false" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md에 cron 켜기 4단계와 설정 4종 표가 존재한다
    - README.md에 "차감 0건이 정상" 취지의 문장이 존재한다(시행일 전 오독 방지)
    - `.env.example`의 `BATCH_INACTIVITY_SCHEDULER_ENABLED` 값이 여전히 `false`다
    - `.planning/ROADMAP.md`에 "Phase 6에서 되돌린다" 문자열이 존재하지 않는다
  </acceptance_criteria>
  <done>운영자가 README만 보고 cron을 안전하게 켤 수 있고, 기본 꺼짐 fail-safe는 유지된다</done>
</task>

<task type="auto">
  <name>Task 2: 문서·코드 최종 정합 점검 + 캐시 없는 전체 회귀 + 요구사항 대응표</name>
  <files>docs/error-codes.md, docs/glossary.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/error-codes.md "운영 코드 (Phase 6)" 섹션 (06-01이 만든 표 — 발생 지점 열이 실제 클래스와 맞는지 확인 대상)
    - docs/glossary.md "운영 (Phase 6)" 섹션
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (실제 enum 상수)
    - .planning/REQUIREMENTS.md 60-75행·140-152행 (ATTEND/NOTICE/NOTIF 항목과 Coverage 표)
    - .planning/phases/06-operations/06-01~06-10 SUMMARY 파일들 (각 요구사항을 무엇이 증명했는지 근거 수집)
  </read_first>
  <action>
**정합 점검** — 아래 3가지를 grep으로 확인하고 어긋난 곳만 고친다:
1. `ErrorCode` enum의 Phase 6 상수 6종이 `docs/error-codes.md` 표와 1:1인지, 표의 "발생 지점" 열이
   실제로 그 예외를 던지는 클래스명(`AttendanceService`·`NoticeService`)과 일치하는지 —
   06-01 시점의 예상값과 실제 구현이 다르면 **문서를 실제 코드 기준으로** 고친다
2. `docs/glossary.md`의 Phase 6 용어가 실제 코드 식별자와 일치하는지(`AttendanceStatus`,
   `EVENING_HALF_REFUND`, `unreadCount`, `markAllAsRead`)
3. `docs/policies.md` §4.2·§6에 적힌 규칙 중 코드가 구현하지 않은 것이 없는지 — 있으면
   **코드가 틀린 것**이므로 SUMMARY에 갭으로 기록한다(문서를 낮추지 않는다)

**전체 회귀** — `./gradlew cleanTest test`를 실행한다(캐시 없이. 사용자 메모리 "GSD 회귀 게이트가
Gradle 레포에서 no-op"이므로 여기서 직접 돌린다). 이어 `./gradlew ktlintFormat && ./gradlew build`.
총 테스트 수와 failures를 SUMMARY에 기록한다.

**요구사항 대응표** — SUMMARY에 6개 요구사항 × (구현 위치 / 증명 테스트 / 상태) 표를 만든다.
Phase 4·5 마감 플랜의 "충족 근거" 형식을 따른다. 이월 CR-03도 한 행으로 포함한다.

`.planning/REQUIREMENTS.md`의 ATTEND-01·ATTEND-02·NOTICE-01·NOTICE-02·NOTIF-02·NOTIF-03 체크박스를
완료로 바꾸고 Coverage 표의 Phase 6 행 상태를 `Complete`로 갱신한다.
  </action>
  <verify>
    <automated>./gradlew cleanTest test</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew cleanTest test` 가 0 failures로 끝나고 총 테스트 수가 SUMMARY에 기록된다
    - `./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
    - `docs/error-codes.md`의 Phase 6 코드 6종이 `ErrorCode.kt`와 1:1이다 (양쪽 grep 결과 개수 일치)
    - `.planning/REQUIREMENTS.md`에서 ATTEND-01·02, NOTICE-01·02, NOTIF-02·03가 완료 표시다
    - SUMMARY에 6요구사항 + CR-03 대응표가 포함된다
  </acceptance_criteria>
  <done>문서와 코드가 어긋나지 않고 전체 회귀가 캐시 없이 그린이며 요구사항 충족 근거가 남았다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 로컬 실기동 확인 — 출석·차감·복구·공지·알림·피드</name>
  <files>(확인 전용 — 코드 변경 없음)</files>
  <read_first>
    - .planning/phases/06-operations/06-08-SUMMARY.md, 06-10-SUMMARY.md (실제 엔드포인트 경로·요청 본문 형태)
    - docs/api/openapi.yaml (확인에 쓸 정확한 경로·파라미터)
  </read_first>
  <what-built>
출석 체크(예약제/1:1 명단 프리로드·출석/불참·소급 수정), 저녁반 0.5회 차감(회비 우선·거부·삭제 복구),
공지 CRUD + 회원 열람, 알림 폴링·모두 읽음, 활동 피드, 그리고 미사용 차감 기준일의 출석 배선(CR-03).
전 항목이 자동 테스트로 검증됐고 캐시 없는 전체 회귀가 그린이다.
  </what-built>
  <action>
오케스트레이터가 `docker compose up -d && ./gradlew bootRun`으로 실제 앱을 띄우고, 관리자 로그인 API로
토큰을 발급한 뒤 아래 `<how-to-verify>` 10개 항목을 직접 실행한다. HTTP 응답(상태·본문 요약)과
`psql` 조회 결과(`attendance` 행 수, `pass.remaining_count`, `pass_transaction`의 사유별 건수)를
항목별로 사용자에게 제시하고 승인을 기다린다. 코드는 수정하지 않는다 — 문제가 발견되면 갭으로
기록하고 별도 플랜으로 닫는다. 확인용 데이터는 사용자 지시가 없으면 삭제하지 않는다.
  </action>
  <verify>
    <human-check>10개 항목의 실제 응답·psql 결과를 사용자가 확인하고 승인한다</human-check>
  </verify>
  <how-to-verify>
오케스트레이터가 실제 앱·실제 DB로 아래를 실행해 결과를 제시하고, 사용자가 승인한다
(04-15·05-16 마감 검증과 같은 방식).

준비: `docker compose up -d && ./gradlew bootRun`. 관리자 토큰은 관리자 로그인 API로 발급.

1. **명단 프리로드** — 예약이 있는 예약제 타임에 대해 `GET /api/admin/attendances?classScheduleId=..&classDate=..`
   → 예약자 전원이 나오고 `status`가 전부 null(미체크)인지
2. **출석 체크·정정** — 같은 회원을 `PUT`으로 `ABSENT` → 다시 `ATTENDED`로 정정 →
   `psql`로 `select count(*) from attendance where member_id=..` 결과가 1인지
3. **저녁반 차감** — 회비 없는 `SESSION_PASS` 회원으로 `POST /api/admin/attendances/evening` →
   200이고 `pass.remaining_count`가 0.5 감소, `pass_transaction`에 `EVENING_HALF` 1건인지
4. **회비 우선** — 유효한 `EVENING_MEMBERSHIP` 보유 회원으로 같은 호출 → 잔여 불변·이력 없음인지
5. **차감 불가 거부** — 잔여 0인 회원으로 호출 → 409 `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`이고
   `attendance` 행이 생기지 않았는지
6. **삭제 복구** — 3번에서 만든 출석을 `DELETE` → 잔여 +0.5, `EVENING_HALF_REFUND` 1건,
   `EVENING_HALF` 이력은 그대로 남아 있는지
7. **공지** — 관리자로 등록·수정·삭제, 회원 토큰으로 목록·상세 열람, 삭제 후 404인지
8. **알림** — `GET /api/admin/notifications` 응답의 `unreadCount` → `POST /read-all` →
   재조회 시 `unreadCount`가 0인지
9. **활동 피드** — `GET /api/admin/activity-feed?type=RESERVATION_CREATED` 필터가 걸리고,
   읽음 처리된 알림도 여전히 나오는지
10. **CR-03 배선** — 출석 기록이 있는 회원에 대해 배치 수동 실행(`POST /api/admin/batch/inactivity-runs`)
    → 실행 상태 조회로 `SUCCESS`와 차감 건수를 확인(정책 시행일 이전이면 0건이 정상)

각 항목의 실제 응답·psql 결과를 사용자에게 제시한다. 확인용으로 만든 데이터는 사용자 지시가
없으면 지우지 않는다.
  </how-to-verify>
  <acceptance_criteria>
    - 10개 항목의 실제 응답(HTTP 상태·본문 요약)과 psql 조회 결과가 사용자에게 제시됐다
    - 3·6번에서 잔여 변화와 `pass_transaction` 사유·건수가 함께 제시됐다
    - 사용자가 승인했거나, 발견된 문제가 갭으로 기록됐다
  </acceptance_criteria>
  <resume-signal>"승인" 또는 발견한 문제를 알려 주세요</resume-signal>
  <done>Phase 6의 핵심 흐름이 실제 앱·실제 DB에서 동작함을 사용자가 확인했다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영 배포 → cron 활성화 | 사람 개입 없이 잔여를 깎는 스위치. 절차 오류가 곧 회원 피해다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-38 | Tampering | 출석 배선 확인 없이 cron을 켜 저녁반 전용 회원 부당 차감 | mitigate | README 절차 1단계가 배선 확인을 선행 조건으로 명시하고, 2단계 수동 실행 검증을 강제한다 |
| T-06-39 | Denial of Service | 기본값 복원으로 fail-safe(설정 누락 시 꺼짐)가 사라짐 | mitigate | `.env.example` 기본값 `false` 유지를 acceptance로 고정(D-121·D-130) |
| T-06-40 | Repudiation | 문서와 코드의 에러코드가 갈라져 FE 분기가 깨짐 | mitigate | Task 2가 enum ↔ 표 1:1을 grep으로 검증하고 어긋나면 문서를 코드 기준으로 정정 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 이 phase는 신규 외부 의존성을 추가하지 않았다. Task 2에서 `build.gradle.kts`의 `git diff`가 비어 있음을 확인한다 |
</threat_model>

<verification>
- `./gradlew cleanTest test` 0 failures (총 건수 기록)
- `./gradlew build` BUILD SUCCESSFUL
- `git diff build.gradle.kts` 가 비어 있다(신규 의존성 없음)
- 사용자 확인 완료
</verification>

<success_criteria>
- cron 켜기 절차가 README에 체크리스트로 존재하고 기본값은 꺼짐이다
- 6개 요구사항 + CR-03의 충족 근거표가 SUMMARY에 있다
- 전체 회귀가 캐시 없이 그린이다
- 사용자가 실기동으로 핵심 흐름을 확인했다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-11-SUMMARY.md` when done
</output>
