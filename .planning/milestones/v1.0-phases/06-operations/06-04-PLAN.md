---
phase: 06-operations
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/kotlin/com/goldwrestling/notice/NoticeService.kt
  - src/main/kotlin/com/goldwrestling/notice/NoticeExceptions.kt
  - src/main/kotlin/com/goldwrestling/notice/AdminNoticeController.kt
  - src/main/kotlin/com/goldwrestling/notice/MemberNoticeController.kt
  - src/main/kotlin/com/goldwrestling/notice/dto/NoticeRequests.kt
  - src/main/kotlin/com/goldwrestling/notice/dto/NoticeResponses.kt
  - src/test/kotlin/com/goldwrestling/notice/AdminNoticeControllerTest.kt
  - src/test/kotlin/com/goldwrestling/notice/MemberNoticeControllerTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [NOTICE-01, NOTICE-02]
must_haves:
  truths:
    - "관리자가 공지를 등록·수정·삭제할 수 있고 삭제는 행이 사라지는 hard delete다"
    - "회원이 공지 목록(최신순 페이지)과 상세를 열람할 수 있다"
    - "존재하지 않는 공지 id로 조회·수정·삭제하면 404 NOTICE_NOT_FOUND ProblemDetail이 온다"
    - "회원 토큰으로 관리자 공지 등록을 호출하면 403이다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/notice/AdminNoticeController.kt"
      provides: "공지 CRUD 5 엔드포인트"
      contains: "/api/admin/notices"
    - path: "src/main/kotlin/com/goldwrestling/notice/MemberNoticeController.kt"
      provides: "회원 열람 2 엔드포인트"
      contains: "/api/members/notices"
    - path: "docs/api/openapi.yaml"
      provides: "FE 계약(공지 7 엔드포인트 포함)"
      contains: "/api/members/notices"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/notice/NoticeService.kt"
      to: "notice.NoticeRepository"
      via: "CRUD + 최신순 페이지 조회"
      pattern: "findAllByOrderByCreatedAtDescIdDesc"
---

<objective>
공지사항(NOTICE-01·02)을 완결한다 — 관리자 CRUD 5개 + 회원 열람 2개 엔드포인트, 제목+본문만
(첨부·고정 없음), 삭제는 hard delete(D-131). 같은 청크 안에서 `openapi.yaml`을 재생성해 FE 계약이
코드와 갈라진 상태를 dev에 남기지 않는다(deliver-phase-chunk "엔드포인트와 그 openapi 재생성은
한 청크에").

목적: 공지는 이 phase에서 유일하게 잔여 횟수·예약과 무관한 독립 기능이라, 출석 청크와 병렬로
납품해 리뷰 단위를 작게 유지한다.

산출물: 서비스 1 + 예외 1 + 컨트롤러 2 + DTO 2파일 + 통합테스트 2 + 재생성된 openapi.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.claude/skills/add-endpoint/SKILL.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NoticeService + NoticeExceptions + 요청·응답 DTO</name>
  <files>src/main/kotlin/com/goldwrestling/notice/NoticeService.kt, src/main/kotlin/com/goldwrestling/notice/NoticeExceptions.kt, src/main/kotlin/com/goldwrestling/notice/dto/NoticeRequests.kt, src/main/kotlin/com/goldwrestling/notice/dto/NoticeResponses.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt (클래스 레벨 `@Transactional(readOnly = true)` + 쓰기 메서드 오버라이드, `Clock` 주입, 관리자 엔티티 조회 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassExceptions.kt (`DomainException` 상속 + id를 메시지에 보간하지 않는 관례)
    - src/main/kotlin/com/goldwrestling/member/dto/PageResponse.kt (`PageResponse.from(page, mapper)`)
    - src/main/kotlin/com/goldwrestling/pass/dto/AdjustPassRequest.kt (`@field:` validation 관례 + `@Schema`)
    - src/main/kotlin/com/goldwrestling/notice/Notice.kt (06-02이 만든 엔티티 — `var title`/`var content`/`var updatedAt`)
    - docs/conventions.md §6·§7
  </read_first>
  <behavior>
    - `create(adminId, request)`가 `createdAt`·`updatedAt`을 주입된 `Clock` 기준 같은 시각으로 채운다
    - `update(noticeId, request)`가 `title`/`content`를 바꾸고 `updatedAt`만 갱신하며 `createdAt`은 불변이다
    - `delete(noticeId)`가 행을 실제로 삭제한다(이후 `findById`가 empty)
    - 없는 id로 `getDetail`/`update`/`delete`를 호출하면 `NoticeNotFoundException`이 난다
    - `getList(page, size)`가 최신순 `PageResponse<NoticeSummaryResponse>`를 반환한다
  </behavior>
  <action>
`NoticeExceptions.kt` — `class NoticeNotFoundException(noticeId: Long?) : DomainException(ErrorCode.NOTICE_NOT_FOUND, "공지를 찾을 수 없습니다.")`.
`PassNotFoundException` 관례대로 `@Suppress("UNUSED_PARAMETER")`를 붙이고 **메시지에 id를 보간하지 않는다**.

`dto/NoticeRequests.kt` — `CreateNoticeRequest(title, content)`, `UpdateNoticeRequest(title, content)`.
두 DTO 모두 `@field:NotBlank @field:Size(max = 200) val title: String`,
`@field:NotBlank @field:Size(max = 10000) val content: String`. 형식 검증만 한다(도메인 규칙 없음).
`content`의 `@Schema(description = ...)`에 **"이스케이프되지 않은 원문 텍스트다 — 렌더링 시 이스케이프는
FE 책임"**을 명시한다(06-RESEARCH Security Domain, XSS 계약).

`dto/NoticeResponses.kt` — `NoticeSummaryResponse(id, title, createdAt, updatedAt)`와
`NoticeDetailResponse(id, title, content, createdAt, updatedAt)`. 각각 `companion object`에
`from(notice: Notice)`를 둔다. **`createdByAdmin`을 응답에 노출하지 않는다** — 회원 응답에 관리자
식별 정보가 섞이지 않게 하고, 관리자 화면도 작성자 표시 요구가 없다(D-131 "제목+본문만").

`NoticeService` — `@Service @Transactional(readOnly = true)`, 생성자 주입은
`noticeRepository`, `adminRepository`, `clock`. 메서드 5개:
`getList(page: Int, size: Int): PageResponse<NoticeSummaryResponse>`(`PageRequest.of(page, size)` +
`findAllByOrderByCreatedAtDescIdDesc`), `getDetail(noticeId): NoticeDetailResponse`,
`@Transactional create(adminId: Long, request: CreateNoticeRequest): NoticeDetailResponse`,
`@Transactional update(noticeId: Long, request: UpdateNoticeRequest): NoticeDetailResponse`,
`@Transactional delete(noticeId: Long)`.
현재 시각은 반드시 `OffsetDateTime.now(clock)`으로 얻는다(직접 `now()` 호출 금지, conventions §5).
`update`는 엔티티의 `title`/`content`/`updatedAt`을 직접 세팅하고 JPA 더티 체킹에 맡긴다.
`delete`는 존재 확인 후 `noticeRepository.delete(notice)` — soft delete 컬럼을 만들지 않는다(D-131).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "NOTICE_NOT_FOUND" src/main/kotlin/com/goldwrestling/notice/NoticeExceptions.kt && grep -q "readOnly = true" src/main/kotlin/com/goldwrestling/notice/NoticeService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NoticeService.kt`에 클래스 레벨 `@Transactional(readOnly = true)`가 있고 `create`/`update`/`delete`에만 `@Transactional`이 붙어 있다
    - `NoticeService.kt`에 `OffsetDateTime.now()`(clock 없는 호출)가 존재하지 않는다
    - `NoticeResponses.kt`에 `createdByAdmin` 또는 `adminId` 필드가 존재하지 않는다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>공지 CRUD 도메인 계층이 트랜잭션·시각·예외 관례를 지키며 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminNoticeController(CRUD 5) + MemberNoticeController(열람 2) + 통합테스트 2종</name>
  <files>src/main/kotlin/com/goldwrestling/notice/AdminNoticeController.kt, src/main/kotlin/com/goldwrestling/notice/MemberNoticeController.kt, src/test/kotlin/com/goldwrestling/notice/AdminNoticeControllerTest.kt, src/test/kotlin/com/goldwrestling/notice/MemberNoticeControllerTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/member/AdminMemberController.kt (관리자 컨트롤러 골격: 권한 애노테이션 없음(D-040), `@Tag`, `@Operation`, 서비스 호출 1줄)
    - src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt (회원 컨트롤러 골격 + `@ParameterObject @ModelAttribute` 힌트가 필요한 이유(D-054·WR-06))
    - src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt (`/api/admin/**` = hasRole ADMIN, `/api/members/**` = hasRole MEMBER)
    - src/test/kotlin/com/goldwrestling/batch/AdminBatchControllerTest.kt 1-60행 (MockMvc 통합테스트 애노테이션 조합 + 토큰 발급 관례 + clock 리셋 주의사항)
    - .claude/skills/add-endpoint/SKILL.md
  </read_first>
  <behavior>
    - `POST /api/admin/notices`가 201 또는 200으로 `NoticeDetailResponse`를 반환한다
    - `GET /api/admin/notices?page=0&size=20`이 최신순 `PageResponse`를 반환한다
    - `PATCH /api/admin/notices/{id}`가 본문을 바꾸고 `updatedAt`이 `createdAt`보다 크거나 같다
    - `DELETE /api/admin/notices/{id}`가 204를 반환하고 이후 `GET`이 404 `NOTICE_NOT_FOUND`다
    - `GET /api/members/notices`·`/api/members/notices/{id}`가 회원 토큰으로 200이다
    - 회원 토큰으로 `POST /api/admin/notices` 호출 시 403 `ACCESS_DENIED` ProblemDetail이다
    - 제목이 빈 문자열이면 400 `VALIDATION_FAILED`다
  </behavior>
  <action>
`AdminNoticeController` — `@RestController @RequestMapping("/api/admin/notices")`,
`@Tag(name = "admin-notice", description = "관리자 공지 관리")`. 엔드포인트 5개:
`GET ""`(목록, `@RequestParam(defaultValue = "0") page`, `@RequestParam(defaultValue = "20") size`),
`GET "/{noticeId}"`, `POST ""`(`@AuthenticationPrincipal principal` + `@Valid @RequestBody CreateNoticeRequest`,
`principal.requireAdminId()`로 작성자 결정), `PATCH "/{noticeId}"`, `DELETE "/{noticeId}"`
(`@ResponseStatus(HttpStatus.NO_CONTENT)`).
권한 애노테이션·`@Transactional`·`try-catch`를 붙이지 않는다(D-040·D-020·D-017).

`MemberNoticeController` — `@RestController @RequestMapping("/api/members/notices")`,
`@Tag(name = "member-notice", ...)`. `GET ""`(목록), `GET "/{noticeId}"`(상세) 2개.
**`@AuthenticationPrincipal`을 받지 않고 `MemberStateGate`도 호출하지 않는다**(D-134) — 공지는
회원 소유 데이터가 아니고, 휴회 회원의 열람을 막을 이유가 없다. 이 근거를 클래스 KDoc에 남긴다.
경로가 `/api/members/me`가 아니라 `/api/members/notices`인 이유(본인 소유 리소스가 아님)도 함께 적는다.

테스트 2종은 `AdminBatchControllerTest`와 **동일한 애노테이션 조합**을 쓴다(`@SpringBootTest` +
`@AutoConfigureMockMvc` + `@Import(TestcontainersConfiguration::class)`, 컨텍스트 캐시 재사용).
토큰은 기존 테스트가 쓰는 `TokenService.issueTokenPair` 경로를 그대로 쓰고, **clock을 과거로
고정하지 않는다**(고정하면 발급 토큰이 즉시 만료돼 401이 된다 — 05-15 선례).
`AdminNoticeControllerTest`: 등록→목록→상세→수정→삭제 성공 경로 + 실패 3종(없는 id 404,
빈 제목 400, 회원 토큰 403). `MemberNoticeControllerTest`: 목록·상세 200 + 없는 id 404.
ProblemDetail 본문은 `jsonPath("$.code")`로 코드 값을 단언한다(FE 계약이 code이므로).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.notice.*"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.notice.*"` 가 0 failures로 끝난다
    - `AdminNoticeController.kt`에 `@PreAuthorize`가 존재하지 않는다
    - `MemberNoticeController.kt`에 `MemberStateGate` 문자열이 존재하지 않는다
    - `AdminNoticeControllerTest`가 `jsonPath("$.code")`로 `NOTICE_NOT_FOUND`와 `ACCESS_DENIED`를 각각 단언한다
  </acceptance_criteria>
  <done>공지 7개 엔드포인트가 성공 경로와 대표 실패 경로(404·400·403)까지 통합테스트로 검증된다</done>
</task>

<task type="auto">
  <name>Task 3: openapi.yaml 재생성 및 diff 확인</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md §6 (재생성 절차와 `servers:` 확인 항목)
    - docs/conventions.md §6 (API 변경 커밋에는 재생성된 openapi.yaml이 포함돼야 한다)
  </read_first>
  <action>
`docker compose up -d`로 Postgres를 띄운 뒤 `./gradlew generateApiDocs`를 실행해
`docs/api/openapi.yaml`을 재생성한다(앱 기동을 포함해 최대 1분 소요, 태스크가 8099에서 기동·정리한다).

`git diff docs/api/openapi.yaml`을 확인해 (1) 이번에 추가한 7개 공지 경로만 늘었는지,
(2) `servers:`가 `/`로 유지되는지, (3) 기존 경로의 스키마가 의도치 않게 바뀌지 않았는지 점검한다.
의도하지 않은 변경이 있으면 그 원인을 SUMMARY에 기록한다.

이 태스크는 conventions §10.0 면제 대상이다(생성 산출물) — 별도 테스트를 만들지 않으며,
계약 검증은 Task 2의 컨트롤러 통합테스트가 담당한다.
  </action>
  <verify>
    <automated>grep -q "/api/members/notices" docs/api/openapi.yaml && grep -q "/api/admin/notices" docs/api/openapi.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/openapi.yaml`에 `/api/admin/notices`, `/api/admin/notices/{noticeId}`, `/api/members/notices`, `/api/members/notices/{noticeId}` 경로가 모두 존재한다
    - `git diff docs/api/openapi.yaml`에 `servers:` 항목 변경이 포함되지 않는다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>FE가 공지 API 타입을 생성할 수 있는 계약이 커밋 가능한 상태로 갱신됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 클라이언트 → `/api/admin/notices` | 쓰기 권한이 있는 경로. 인가는 `SecurityConfig` URL 규칙이 담당 |
| 회원 클라이언트 → `/api/members/notices` | 읽기 전용 경로 |
| BE 응답 → FE 렌더링 | 공지 본문이 원문 텍스트로 전달된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Elevation of Privilege | 회원이 공지 등록·수정·삭제 호출 | mitigate | `/api/admin/**` = `hasRole("ADMIN")` 전역 규칙(D-040). Task 2가 회원 토큰 403 테스트로 실증 |
| T-06-10 | Information Disclosure | 없는 공지 id 조회 시 존재 여부 노출 | mitigate | 404 `NOTICE_NOT_FOUND`로 통일하고 메시지에 id를 보간하지 않는다 |
| T-06-11 | Tampering | 공지 본문 XSS | transfer | BE는 원문 저장·전달만 담당하고 이스케이프는 FE 책임 — `@Schema` description에 계약으로 명시(Task 1) |
| T-06-12 | Denial of Service | 초대형 본문 저장으로 응답·저장소 압박 | mitigate | `@field:Size(max = 10000)` 형식 검증으로 입력 상한을 건다 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew ktlintFormat && ./gradlew build` BUILD SUCCESSFUL
- `git diff --stat docs/api/openapi.yaml` 이 공지 경로 추가만 반영한다
</verification>

<success_criteria>
- 관리자 CRUD 5 + 회원 열람 2 엔드포인트가 동작하고 통합테스트가 통과한다
- 삭제가 hard delete이며 soft delete 컬럼이 없다
- openapi.yaml이 재생성돼 커밋 가능하다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-04-SUMMARY.md` when done
</output>
