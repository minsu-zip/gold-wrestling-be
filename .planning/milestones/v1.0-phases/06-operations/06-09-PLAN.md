---
phase: 06-operations
plan: 09
type: execute
wave: 7
depends_on: ["06-02", "06-08"]
files_modified:
  - src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt
  - src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt
  - src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt
  - src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt
  - src/test/kotlin/com/goldwrestling/notification/AdminNotificationControllerTest.kt
autonomous: true
requirements: [NOTIF-02]
must_haves:
  truths:
    - "관리자가 알림 목록을 페이지로 조회하고 같은 응답에서 미확인 건수를 함께 받는다"
    - "미확인만 보기 필터로 확인하지 않은 알림만 조회할 수 있다"
    - "모두 읽음을 호출하면 미확인 알림이 전부 읽음 처리되고 이후 조회에서 unreadCount가 0이다"
    - "알림 조회가 예약·수업 연관을 로딩하지 않고 비정규화 표시 필드만으로 응답한다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt"
      provides: "폴링 조회 + 미확인 카운트 + 벌크 읽음 처리"
      exports: ["markAllAsRead", "countByIsReadFalse"]
    - path: "src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt"
      provides: "알림 목록·모두읽음 엔드포인트"
      contains: "/api/admin/notifications"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt"
      to: "notification.NotificationRepository.markAllAsRead"
      via: "벌크 UPDATE 후 재조회(준영속 stale 값 사용 금지)"
      pattern: "markAllAsRead"
---

<objective>
관리자 알림 폴링(NOTIF-02)을 만든다 — 목록 조회(미확인 카운트 포함) + "모두 읽음" 벌크 처리.
알림 **레코드 생성은 Phase 4가 이미 끝냈고**(NOTIF-01), 이 플랜은 조회·확인 계층만 추가한다.

목적: FE가 30초마다 호출할 단일 엔드포인트를 만든다. 카운트를 목록 응답에 실어 폴링 1회가
API 1호출로 끝나게 한다(D-129).

산출물: 리포지토리 확장 + 조회 서비스 + 컨트롤러 + 응답 DTO + 통합테스트.

**의존 이유(wave 7)**: 코드상으로는 출석 청크에 의존하지 않지만, `docs/api/openapi.yaml`을
재생성하는 06-10이 06-08과 같은 wave에 놓이면 같은 파일을 두 플랜이 동시에 건드리게 되고,
청크 경계를 wave 경계로만 끊을 수 있다는 제약(D-084, deliver-phase-chunk)상 알림 기능이 출석
청크 PR에 섞인다. 그래서 알림·피드를 출석 청크 뒤 wave로 배치했다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationRepository 확장(폴링 조회·미확인 카운트·모두읽음) + 응답 DTO</name>
  <files>src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt, src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/notification/Notification.kt (비정규화 표시 필드와 `isRead`/`readAt`만 `var`인 이유, `reservation`/`classSession`이 LAZY nullable인 점)
    - src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt (현재는 `JpaRepository` 상속 1줄 + "조회 계약은 Phase 6이 정의한다" KDoc)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt 40-60행 (`@Modifying(flushAutomatically = true, clearAutomatically = true)` 벌크 UPDATE 관례와 그 KDoc 경고)
    - src/main/kotlin/com/goldwrestling/member/dto/PageResponse.kt
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (`idx_notification_unread (is_read, occurred_at DESC)`)
  </read_first>
  <behavior>
    - `findAllByOrderByOccurredAtDescIdDesc(pageable)`가 전체 알림을 최신순 페이지로 반환한다
    - `findAllByIsReadFalseOrderByOccurredAtDescIdDesc(pageable)`가 미확인 알림만 최신순으로 반환한다
    - `countByIsReadFalse()`가 미확인 건수를 반환한다
    - `markAllAsRead(now)`가 미확인 행만 갱신하고 갱신된 건수를 반환하며, 이미 읽은 행의 `readAt`을 덮어쓰지 않는다
  </behavior>
  <action>
`NotificationRepository`를 `interface NotificationRepository : JpaRepository<Notification, Long>, JpaSpecificationExecutor<Notification>`로
바꾼다(`JpaSpecificationExecutor`는 06-10의 활동 피드 필터가 쓴다 — 여기서 함께 붙여 둔다).
기존 KDoc의 "조회 계약은 Phase 6이 정의한다" 문장을 실제 계약 설명으로 교체한다.

메서드 4개를 추가한다:
- `findAllByOrderByOccurredAtDescIdDesc(pageable: Pageable): Page<Notification>`
- `findAllByIsReadFalseOrderByOccurredAtDescIdDesc(pageable: Pageable): Page<Notification>`
- `countByIsReadFalse(): Long`
- `markAllAsRead(now: OffsetDateTime): Int` — `@Modifying(flushAutomatically = true, clearAutomatically = true)` +
  `@Query("update Notification n set n.isRead = true, n.readAt = :now where n.isRead = false")`.
  KDoc에 **"이 벌크 UPDATE는 영속성 컨텍스트를 우회한다 — 호출 전에 로드한 `Notification`을 그대로
  응답에 쓰지 말고 항상 재조회한다"**(`PassRepository.adjustRemainingCount` KDoc과 동일 경고)를 적는다.
  `where n.isRead = false` 조건이 이미 읽은 알림의 `readAt`을 덮어쓰지 않게 하는 장치임을 명시한다.

`dto/NotificationResponses.kt`:
- `NotificationResponse(id, type, message, memberName, classType, classDate, startTime, occurredAt, isRead, readAt)` +
  `companion object { fun from(notification: Notification) }`.
  **`reservation`·`classSession` 연관을 건드리지 않는다** — 두 필드는 LAZY이고, 표시에 필요한 정보는
  이미 비정규화 필드로 들어 있다(D-097). 이 이유를 KDoc에 남긴다. 응답에 `reservationId`·
  `classSessionId`를 넣지 않는다(FE가 알림에서 예약 상세로 이동하는 요구가 요구사항에 없다).
- `NotificationListResponse(notifications: PageResponse<NotificationResponse>, unreadCount: Long)` —
  D-129의 "미확인 카운트는 목록 응답에 포함, 별도 엔드포인트 없음"의 실현부.
- `MarkAllReadResponse(updatedCount: Int, unreadCount: Long)` — 처리 후 재조회한 값으로 채운다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "clearAutomatically = true" src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt && grep -q "JpaSpecificationExecutor" src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NotificationRepository.kt`가 `JpaSpecificationExecutor<Notification>`을 상속하고 `markAllAsRead`에 `@Modifying(flushAutomatically = true, clearAutomatically = true)`가 붙어 있다
    - `markAllAsRead` 쿼리에 `where n.isRead = false`가 포함된다
    - `NotificationResponses.kt`에 `reservationId`·`classSessionId` 필드가 존재하지 않는다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>폴링 조회·카운트·벌크 읽음 처리 쿼리와 LAZY를 건드리지 않는 응답 계약이 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationQueryService + AdminNotificationController</name>
  <files>src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt, src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt 30-60행 (클래스 readOnly + 쓰기 메서드 오버라이드, `Clock` 주입)
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt (관리자 컨트롤러 골격)
    - src/main/kotlin/com/goldwrestling/notification/NotificationService.kt (Phase 4가 만든 생성 전용 서비스 — **이 파일을 수정하지 않는다**, 조회는 새 서비스가 담당)
  </read_first>
  <behavior>
    - `getNotifications(unreadOnly, page, size)`가 `unreadOnly=true`면 미확인만, false면 전체를 반환하고 두 경우 모두 `unreadCount`를 채운다
    - `markAllAsRead()`가 벌크 UPDATE 실행 후 `countByIsReadFalse()`를 **다시 조회**해 `unreadCount = 0`을 응답한다
    - 알림이 0건이어도 빈 목록 + `unreadCount = 0`으로 정상 응답한다
  </behavior>
  <action>
`NotificationQueryService`를 `@Service @Transactional(readOnly = true)`로 만든다. 생성자 주입은
`notificationRepository`, `clock`.
- `getNotifications(unreadOnly: Boolean, page: Int, size: Int): NotificationListResponse` —
  `PageRequest.of(page, size)`로 두 파생 쿼리 중 하나를 고르고 `PageResponse.from(page) { NotificationResponse.from(it) }`로
  변환한 뒤 `countByIsReadFalse()`를 합쳐 반환한다.
- `@Transactional fun markAllAsRead(): MarkAllReadResponse` — `markAllAsRead(OffsetDateTime.now(clock))`
  결과를 `updatedCount`로 쓰고, **그 뒤에 `countByIsReadFalse()`를 다시 호출**해 `unreadCount`를 채운다.
  벌크 UPDATE 이전에 로드한 엔티티나 이전 카운트를 재사용하지 않는다(Pitfall 5). 이 이유를 KDoc에 적는다.

Phase 4의 `NotificationService`(생성 전용)는 **수정하지 않는다** — 생성 책임과 조회 책임을 분리해
두 phase의 변경이 서로를 깨지 않게 한다. 이 판단을 새 서비스 KDoc에 남긴다.

`AdminNotificationController` — `@RestController @RequestMapping("/api/admin/notifications")`,
`@Tag(name = "admin-notification", description = "관리자 알림 폴링·확인 처리")`.
- `@GetMapping("") list(@RequestParam(defaultValue = "false") unreadOnly: Boolean, @RequestParam(defaultValue = "0") page: Int, @RequestParam(defaultValue = "20") size: Int): NotificationListResponse` —
  `@Operation` description에 "FE는 30초 주기로 이 엔드포인트를 폴링한다. 미확인 건수가 응답에
  포함되므로 별도 카운트 호출이 필요 없다(D-129)"를 적는다.
- `@PostMapping("/read-all") markAllAsRead(): MarkAllReadResponse` — description에 "개별 읽음 처리는
  제공하지 않는다(D-129)"를 적는다.
권한 애노테이션·트랜잭션·`try-catch`를 붙이지 않는다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "read-all" src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt && git diff --quiet src/main/kotlin/com/goldwrestling/notification/NotificationService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NotificationQueryService.markAllAsRead`가 `countByIsReadFalse`를 벌크 UPDATE **이후에** 호출한다
    - `git diff src/main/kotlin/com/goldwrestling/notification/NotificationService.kt` 가 비어 있다
    - `AdminNotificationController.kt`에 `@Transactional`·`@PreAuthorize`가 없다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>폴링 1호출로 목록과 미확인 건수를 받고 모두 읽음을 처리할 수 있다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminNotificationControllerTest — 목록·필터·카운트·모두읽음·권한</name>
  <files>src/test/kotlin/com/goldwrestling/notification/AdminNotificationControllerTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/batch/AdminBatchControllerTest.kt 1-70행 (MockMvc 조합·토큰·clock 리셋)
    - src/main/kotlin/com/goldwrestling/notification/Notification.kt (테스트 픽스처가 채워야 할 필수 필드 — `ck_notification_target`이 reservation·classSession 중 최소 하나를 요구한다)
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (`ck_notification_target` 제약 확인)
  </read_first>
  <behavior>
    - 알림 3건(미확인 2·확인 1)을 넣고 `GET`하면 `notifications.totalElements = 3`, `unreadCount = 2`다
    - `GET ?unreadOnly=true`면 `totalElements = 2`다
    - `POST /read-all` 응답의 `updatedCount = 2`, `unreadCount = 0`이고, 직후 `GET`의 `unreadCount`도 0이다
    - 목록 응답 항목에 `memberName`·`classDate` 등 비정규화 필드가 채워져 있다
    - 회원 토큰으로 `GET` 호출 시 403 `ACCESS_DENIED`다
  </behavior>
  <action>
`AdminNotificationControllerTest`를 `AdminBatchControllerTest`와 동일한 애노테이션 조합으로 만든다.
알림 픽스처는 `ck_notification_target`을 만족하도록 `classSession`을 채워 생성한다
(예약 없이도 만들 수 있는 최소 형태).

"모두 읽음 직후 재조회" 테스트는 **벌크 UPDATE 이후 stale 값이 응답에 새지 않는지**를 확인하는
회귀 테스트다(Pitfall 5) — 응답의 `unreadCount`와 재조회 `GET`의 `unreadCount`를 모두 0으로 단언한다.
클래스 레벨 `@Transactional`을 붙이지 않고 `@AfterEach` + `JdbcClient`로 정리한다 — 벌크 UPDATE가
실제로 커밋돼야 재조회가 의미를 갖는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.notification.AdminNotificationControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.notification.AdminNotificationControllerTest"` 가 0 failures로 끝난다
    - 모두 읽음 이후 재조회 `unreadCount`를 0으로 단언하는 테스트가 존재한다
    - `ACCESS_DENIED` 단언이 존재한다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>폴링·필터·카운트·모두읽음 계약이 HTTP 레벨에서 검증되고 stale 값 회귀가 막혔다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `/api/admin/notifications/**` | 관리자 전용. 알림에는 다른 회원의 이름·수업 정보가 비정규화돼 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-30 | Information Disclosure | 회원이 알림 목록을 조회해 타 회원 이름·수업 정보 열람 | mitigate | `/api/admin/**` 전역 `hasRole("ADMIN")`(D-040). Task 3이 회원 토큰 403을 실증. 회원용 알림 경로는 만들지 않는다(D-097) |
| T-06-31 | Tampering | 벌크 UPDATE 직후 stale `isRead` 값이 응답에 노출돼 관리자가 미확인을 놓침 | mitigate | `markAllAsRead` 이후 반드시 재조회(Task 2) + 재조회 회귀 테스트(Task 3) |
| T-06-32 | Denial of Service | 30초 폴링 × 대량 알림으로 전체 스캔 | mitigate | 페이지네이션 상한(`size` 기본 20) + `idx_notification_unread`(미확인 필터) 활용. 전체 시간순 조회는 06-10이 `idx_notification_occurred_at`를 쓴다 |
| T-06-33 | Repudiation | 확인 시각 미기록으로 "언제 봤는지" 추적 불가 | mitigate | `readAt`을 `Clock` 기준 시각으로 함께 기록 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.goldwrestling.notification.*"` 0 failures
- `NotificationService.kt`(Phase 4)와 `Notification.kt`의 `git diff`가 비어 있다
</verification>

<success_criteria>
- 폴링 1호출로 목록 + 미확인 건수를 받는다
- 모두 읽음 후 미확인이 0이 되고 재조회에서도 0이다
- Phase 4의 알림 생성 경로는 무변경이다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-09-SUMMARY.md` when done
</output>
