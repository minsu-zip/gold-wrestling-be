---
phase: 06-operations
plan: 10
type: execute
wave: 8
depends_on: ["06-09"]
files_modified:
  - src/main/kotlin/com/goldwrestling/notification/NotificationSpecifications.kt
  - src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt
  - src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt
  - src/main/kotlin/com/goldwrestling/notification/dto/ActivityFeedSearchCondition.kt
  - src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt
  - src/test/kotlin/com/goldwrestling/notification/ActivityFeedTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [NOTIF-03]
must_haves:
  truths:
    - "관리자가 최근 예약 이벤트 타임라인을 읽음 여부와 무관하게 시간순으로 조회할 수 있다"
    - "기간(from·to)과 종류(type) 필터를 각각·함께 적용할 수 있고 생략하면 전체가 나온다"
    - "피드는 알림과 같은 notification 테이블을 읽는다 — 별도 저장 경로가 없다"
    - "FE가 openapi.yaml로 알림·피드 API 타입을 생성할 수 있다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/notification/NotificationSpecifications.kt"
      provides: "기간·종류 동적 필터"
      exports: ["occurredBetween", "hasType"]
    - path: "docs/api/openapi.yaml"
      provides: "FE 계약(알림 2 + 피드 1 엔드포인트 포함)"
      contains: "/api/admin/activity-feed"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt"
      to: "notification.NotificationSpecifications"
      via: "Specification.allOf(listOfNotNull(...)) 합성"
      pattern: "allOf"
---

<objective>
활동 피드(NOTIF-03)를 만든다 — 알림과 **동일한 `notification` 테이블의 다른 뷰**(읽음 여부 무관
시간순, 기간+종류 필터, 페이지네이션)다(D-129). 이어서 `openapi.yaml`을 재생성해 알림 청크를 마감한다.

목적: 피드 전용 테이블을 만들면 같은 이벤트를 두 번 쓰게 되고 두 뷰가 어긋날 수 있다 — D-129가
그 대안을 명시적으로 기각했다. 이 플랜은 새 저장 경로를 만들지 않고 쿼리만 추가한다.

산출물: Specification 1 + 서비스 메서드 1 + 조건 DTO 1 + 응답 DTO 1 + 엔드포인트 1 + 통합테스트 1 +
재생성된 openapi.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.claude/skills/add-endpoint/SKILL.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationSpecifications(기간·종류) + ActivityFeedSearchCondition + 피드 응답 DTO</name>
  <files>src/main/kotlin/com/goldwrestling/notification/NotificationSpecifications.kt, src/main/kotlin/com/goldwrestling/notification/dto/ActivityFeedSearchCondition.kt, src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/ReservationSpecifications.kt (전문 — 조건 없으면 `null` 반환, `Predicate` 리스트 조합 관례)
    - src/main/kotlin/com/goldwrestling/member/MemberSpecifications.kt 1-20행 (`Specification.allOf(listOfNotNull(...))` 합성 관례 KDoc, Spring Data JPA 4.x 정적 팩토리)
    - src/main/kotlin/com/goldwrestling/reservation/dto/ (관리자 예약 검색 조건 DTO — `from`이 `to`보다 뒤일 때의 검증 관례와 `@ParameterObject` 사용법)
    - src/main/kotlin/com/goldwrestling/notification/NotificationType.kt (종류 필터의 값 집합 6종)
    - src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt (06-09이 만든 `NotificationResponse`)
  </read_first>
  <behavior>
    - `occurredBetween(null, null)`이 `null`을 반환한다(조건 없음)
    - `occurredBetween(from, null)`이 `occurredAt >= from`만 건다
    - `occurredBetween(null, to)`가 `occurredAt <= to`만 건다
    - `hasType(null)`이 `null`을 반환하고, 값이 있으면 `type = :type`을 건다
    - 조건이 전부 없으면 필터 없이 전체가 조회된다
  </behavior>
  <action>
`NotificationSpecifications`를 `object`로 만들고 `ReservationSpecifications` 관례를 그대로 따른다:
- `fun occurredBetween(from: OffsetDateTime?, to: OffsetDateTime?): Specification<Notification>?` —
  둘 다 null이면 `null` 반환. 그 외에는 `Predicate` 리스트를 만들어 `criteriaBuilder.and(*...)`로 합친다.
  비교 대상 필드명은 `occurredAt`이다(`createdAt`이 아니다 — 이벤트 발생 시각이 타임라인의 축이다).
- `fun hasType(type: NotificationType?): Specification<Notification>?` — null이면 `null`.

`dto/ActivityFeedSearchCondition.kt` —
`ActivityFeedSearchCondition(from: OffsetDateTime? = null, to: OffsetDateTime? = null,
type: NotificationType? = null, page: Int = 0, size: Int = 20)`.
`@field:Min(0)`을 `page`에, `@field:Min(1) @field:Max(100)`을 `size`에 건다(배치 API의 `limit`
검증 관례와 동일 급). 각 필드에 `@Schema(description = ...)`을 붙인다.
**`from`이 `to`보다 뒤인 경우의 처리**: `AdminReservationService`가 쓰는
`INVALID_RESERVATION_SEARCH_RANGE`는 예약 전용 코드이므로 재사용하지 않고, 이 조건은 결과가 빈
목록이 되는 것으로 자연 처리한다 — 새 에러코드를 만들지 않는다. 이 판단을 조건 DTO KDoc에 남긴다.

`NotificationResponses.kt`에 `ActivityFeedItemResponse(id, type, message, memberName, classType,
classDate, startTime, occurredAt)`를 추가한다 — `NotificationResponse`와 달리 `isRead`/`readAt`을
담지 않는다(피드는 읽음 여부와 무관한 뷰다, D-129). `companion object { fun from(notification) }`.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "occurredAt" src/main/kotlin/com/goldwrestling/notification/NotificationSpecifications.kt && grep -q "ActivityFeedItemResponse" src/main/kotlin/com/goldwrestling/notification/dto/NotificationResponses.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `NotificationSpecifications.kt`의 두 함수가 조건 부재 시 `null`을 반환한다(반환 타입이 `Specification<Notification>?`)
    - `ActivityFeedItemResponse`에 `isRead`·`readAt` 필드가 존재하지 않는다
    - `ActivityFeedSearchCondition`의 `size`에 최대값 제약이 붙어 있다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>피드 필터와 응답 계약이 기존 Specification 관례를 그대로 따르는 형태로 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 피드 조회 서비스 메서드 + GET /api/admin/activity-feed + ActivityFeedTest</name>
  <files>src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt, src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt, src/test/kotlin/com/goldwrestling/notification/ActivityFeedTest.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/notification/NotificationQueryService.kt (06-09이 만든 현재 상태)
    - src/main/kotlin/com/goldwrestling/notification/AdminNotificationController.kt (06-09이 만든 현재 상태 — 여기에 엔드포인트 하나를 더한다)
    - src/main/kotlin/com/goldwrestling/pass/MemberPassController.kt 35-60행 (`@ParameterObject @ModelAttribute @Valid condition` 힌트가 필요한 이유 — 이게 없으면 openapi가 객체 파라미터 1개로 기술돼 FE 생성기가 바인딩하지 못한다, D-054·WR-06)
    - src/test/kotlin/com/goldwrestling/notification/AdminNotificationControllerTest.kt (06-09 테스트의 픽스처·정리 관례 재사용)
  </read_first>
  <behavior>
    - 필터 없이 호출하면 읽음·미읽음 알림이 모두 최신순으로 나온다
    - `from`/`to`로 기간을 좁히면 그 범위 밖 알림이 제외된다
    - `type`을 지정하면 해당 종류만 나온다
    - 기간+종류를 함께 주면 두 조건이 AND로 적용된다
    - `size`가 101이면 400 `VALIDATION_FAILED`다
    - 회원 토큰으로 호출하면 403이다
  </behavior>
  <action>
`NotificationQueryService`에 메서드를 하나 추가한다 —
`fun getActivityFeed(condition: ActivityFeedSearchCondition): PageResponse<ActivityFeedItemResponse>`.
`Specification.allOf(listOfNotNull(NotificationSpecifications.occurredBetween(condition.from, condition.to),
NotificationSpecifications.hasType(condition.type)))`로 조건을 합성하고,
`PageRequest.of(condition.page, condition.size, Sort.by(Sort.Direction.DESC, "occurredAt").and(Sort.by(Sort.Direction.DESC, "id")))`로
정렬을 준 뒤 `notificationRepository.findAll(spec, pageable)`을 호출한다.
`isRead` 조건을 절대 걸지 않는다 — 피드는 읽음 여부와 무관한 뷰다(D-129). 이 이유를 KDoc에 적는다.
클래스 레벨 `readOnly = true`가 이미 적용되므로 추가 애노테이션은 없다.

`AdminNotificationController`에 엔드포인트를 하나 추가한다. **경로는 `/api/admin/activity-feed`이며
`/api/admin/notifications` 하위가 아니다** — 알림과 피드는 같은 데이터의 다른 뷰라 FE 화면이 서로
다르고, 하위 경로로 두면 "알림의 부분 리소스"로 오해된다. 클래스 레벨 매핑이
`/api/admin/notifications`이므로, `AdminScheduleController`가 `/api/admin`으로 넓게 잡고 메서드마다
하위 경로를 붙인 선례를 따라 **클래스 매핑을 `/api/admin`으로 넓히고 기존 두 메서드에
`/notifications`, `/notifications/read-all` 하위 경로를 붙인 뒤** 새 메서드에 `/activity-feed`를 준다.
경로 문자열은 06-09이 노출한 값과 동일하게 유지된다(FE 계약 불변).
시그니처는 `@GetMapping("/activity-feed") fun activityFeed(@ParameterObject @ModelAttribute @Valid condition: ActivityFeedSearchCondition): PageResponse<ActivityFeedItemResponse>`.
`@ParameterObject`를 빠뜨리지 않는다(D-054 — 빠지면 FE가 파라미터를 바인딩하지 못한다).

`ActivityFeedTest`를 `AdminNotificationControllerTest`와 동일한 애노테이션 조합으로 만들고 위
behavior 6개를 각각 하나의 테스트로 작성한다. 알림 픽스처는 `occurredAt`을 서로 다른 날짜·종류로
여러 건 만들어 필터 효과를 검증한다. 읽음 처리된 알림도 피드에 나오는지 반드시 확인한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.notification.ActivityFeedTest" --tests "com.goldwrestling.notification.AdminNotificationControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.notification.*"` 가 0 failures로 끝난다 (06-09 테스트도 경로 변경 후 그대로 통과한다)
    - `AdminNotificationController.kt`에 `@ParameterObject`가 존재한다
    - `NotificationQueryService.getActivityFeed`에 `isRead` 문자열이 존재하지 않는다
    - 읽음 처리된 알림이 피드에 포함됨을 단언하는 테스트가 존재한다
  </acceptance_criteria>
  <done>활동 피드가 기간·종류 필터와 읽음 무관 시간순으로 조회되고 기존 알림 경로는 그대로다</done>
</task>

<task type="auto">
  <name>Task 3: openapi.yaml 재생성 및 알림·피드 경로 확인</name>
  <files>docs/api/openapi.yaml</files>
  <read_first>
    - .claude/skills/add-endpoint/SKILL.md §6
    - docs/api/openapi.yaml (06-08이 재생성한 현재 상태 — 공지·출석 경로가 이미 들어 있어야 한다)
  </read_first>
  <action>
`docker compose up -d` 후 `./gradlew generateApiDocs`로 `docs/api/openapi.yaml`을 재생성한다.

`git diff docs/api/openapi.yaml`으로 확인한다: (1) `/api/admin/notifications`,
`/api/admin/notifications/read-all`, `/api/admin/activity-feed` 3개 경로가 추가됐는지,
(2) 활동 피드의 쿼리 파라미터가 `from`/`to`/`type`/`page`/`size` **개별 파라미터로** 기술됐는지
(객체 1개로 기술됐다면 `@ParameterObject`가 빠진 것이다 — D-054/WR-06 재발),
(3) 06-04·06-08이 넣은 공지·출석 경로가 그대로 남아 있는지, (4) `servers:`가 `/`로 유지되는지.

이 태스크는 conventions §10.0 면제 대상이다(생성 산출물) — 계약 검증은 Task 2의 통합테스트가 담당한다.
  </action>
  <verify>
    <automated>grep -q "/api/admin/activity-feed" docs/api/openapi.yaml && grep -q "/api/admin/notifications/read-all" docs/api/openapi.yaml && grep -q "/api/admin/attendances" docs/api/openapi.yaml && grep -q "/api/members/notices" docs/api/openapi.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/openapi.yaml`에 공지 4경로 + 출석 3경로 + 알림 2경로 + 피드 1경로가 모두 존재한다
    - 활동 피드 경로의 파라미터 목록에 `from`, `to`, `type`, `page`, `size`가 개별 항목으로 존재한다
    - `git diff docs/api/openapi.yaml`에 `servers:` 변경이 없다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>Phase 6이 추가한 모든 엔드포인트가 FE 계약 파일에 반영됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `/api/admin/activity-feed` | 관리자 전용. 응답에 회원명·수업 정보가 포함된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-34 | Information Disclosure | 회원이 피드로 전체 회원의 예약 활동 열람 | mitigate | `/api/admin/**` 전역 `hasRole("ADMIN")`. Task 2가 회원 토큰 403을 실증 |
| T-06-35 | Denial of Service | `size` 무제한 요청으로 전체 이력 로드 | mitigate | `@field:Max(100)` 상한 + 400 `VALIDATION_FAILED` 테스트 |
| T-06-36 | Denial of Service | 기간 필터 없는 전체 스캔 | mitigate | `idx_notification_occurred_at`(V11, D-135)로 시간순 정렬을 인덱스가 지원 + 페이지네이션 강제 |
| T-06-37 | Tampering | 피드 전용 저장 경로가 생겨 알림과 데이터가 어긋남 | mitigate | 새 테이블·새 write 경로를 만들지 않고 같은 `notification` 테이블을 읽기만 한다(D-129) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.goldwrestling.notification.*"` 0 failures
- `docs/api/openapi.yaml`에 Phase 6의 10개 경로가 모두 존재한다
</verification>

<success_criteria>
- 활동 피드가 기간·종류 필터와 페이지네이션으로 동작한다
- 읽음 여부가 피드 결과에 영향을 주지 않는다
- openapi.yaml이 Phase 6 전체 API를 담은 상태로 갱신됐다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-10-SUMMARY.md` when done
</output>
