---
phase: 06-operations
plan: 08
type: execute
wave: 6
depends_on: ["06-06", "06-07"]
files_modified:
  - src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt
  - src/test/kotlin/com/goldwrestling/attendance/AdminAttendanceControllerTest.kt
  - src/test/kotlin/com/goldwrestling/attendance/AttendanceConcurrencyTest.kt
  - docs/api/openapi.yaml
autonomous: true
requirements: [ATTEND-01, ATTEND-02]
must_haves:
  truths:
    - "관리자가 HTTP로 명단 조회·출석 체크·저녁반 추가·출석 삭제 4가지를 할 수 있다"
    - "회원 토큰으로는 어떤 출석 API도 호출할 수 없다(403)"
    - "같은 회원·같은 저녁반 수업에 동시에 여러 요청이 와도 출석 1건·차감 0.5회만 반영된다"
    - "FE가 openapi.yaml로 출석 API 타입을 생성할 수 있다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt"
      provides: "출석 관리자 API 4종"
      contains: "/api/admin/attendances"
    - path: "src/test/kotlin/com/goldwrestling/attendance/AttendanceConcurrencyTest.kt"
      provides: "이중 차감 0건 실증"
      contains: "CountDownLatch"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt"
      to: "attendance.AttendanceService"
      via: "principal.requireAdminId() 위임 1줄"
      pattern: "requireAdminId"
---

<objective>
출석 기능을 HTTP 표면으로 노출하고(ATTEND-01·02), **초과/이중 차감 0건을 동시성 테스트로 실증**한
뒤 `openapi.yaml`을 재생성해 출석 청크를 마감한다.

목적: policies §8은 경쟁이 있는 경로에 동시성 테스트를 요구한다. 저녁반 출석 추가는 "출석 1건과
차감 1건을 함께 고정"하는 경로라 예약 정원 경쟁과 같은 급의 검증이 필요하다 —
`ReservationCapacityConcurrencyTest`가 그 선례다.

산출물: 컨트롤러 1(엔드포인트 4) + 컨트롤러 통합테스트 1 + 동시성 테스트 1 + 재생성된 openapi.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.claude/skills/add-endpoint/SKILL.md
@.claude/skills/add-domain-test/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminAttendanceController — 명단 조회·출석 체크·저녁반 추가·삭제 4 엔드포인트</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/schedule/AdminScheduleController.kt (전문 — 클래스 KDoc이 담는 4가지(권한 애노테이션 없음/트랜잭션 없음/try-catch 없음/경로 계층 선택 이유), `@RequestParam` + `@DateTimeFormat` 관례, `principal.requireAdminId()` 위임)
    - src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt (06-06·06-07이 만든 4개 public 메서드 시그니처)
    - src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceRequests.kt (06-06이 만든 요청 DTO 2종)
    - src/main/kotlin/com/goldwrestling/config/SecurityConfig.kt (`/api/admin/**` 전역 인가)
    - .claude/skills/add-endpoint/SKILL.md §1·§3
  </read_first>
  <behavior>
    - `GET /api/admin/attendances?classScheduleId=&classDate=`가 명단 응답을 반환한다
    - `PUT /api/admin/attendances`가 예약자 출석 upsert 결과를 반환한다
    - `POST /api/admin/attendances/evening`이 저녁반 출석 추가 결과를 반환한다
    - `DELETE /api/admin/attendances/{attendanceId}`가 204를 반환한다
    - 컨트롤러 어디에도 `@Transactional`·`try-catch`·`@PreAuthorize`가 없다
  </behavior>
  <action>
`AdminAttendanceController`를 `@RestController @RequestMapping("/api/admin/attendances")`,
`@Tag(name = "admin-attendance", description = "관리자 출석 체크·저녁반 0.5회 차감")`로 만든다.
경로를 `/api/admin/attendances` 한 계층으로 잡는 이유(출석이 단일 리소스 계층이라
`AdminScheduleController`처럼 넓은 매핑이 필요 없다)를 클래스 KDoc에 남긴다.

엔드포인트 4개:
1. `@GetMapping("") getRoster(@RequestParam classScheduleId: Long, @RequestParam @DateTimeFormat(iso = ISO.DATE) classDate: LocalDate)` →
   `ClassSessionAttendanceRosterResponse`. `@Operation(summary = "타임별 출석 명단 조회 (예약자 프리로드, 미체크는 status=null)")`
2. `@PutMapping("") check(@AuthenticationPrincipal principal, @Valid @RequestBody CheckAttendanceRequest)` →
   `AttendanceResponse`. `@Operation`의 description에 "예약제·1:1 전용이다. 저녁반은
   `POST /api/admin/attendances/evening`을 쓴다 — 저녁반은 출석 추가가 0.5회 차감을 유발하기 때문"을 적는다
3. `@PostMapping("/evening") addEvening(@AuthenticationPrincipal principal, @Valid @RequestBody AddEveningAttendanceRequest)` →
   `AttendanceResponse`. description에 "유효한 저녁반 회비가 있으면 차감 없이 출석만 기록하고,
   없으면 만료 임박순 횟수권에서 0.5회를 차감한다. 둘 다 불가하면 409
   `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`로 거부한다(관리자가 수동 가감으로 충전 후 재시도)"를 적는다
4. `@DeleteMapping("/{attendanceId}") @ResponseStatus(HttpStatus.NO_CONTENT) delete(@AuthenticationPrincipal principal, @PathVariable attendanceId: Long)`.
   description에 "저녁반 출석이면 연결된 0.5회 차감을 `EVENING_HALF_REFUND`로 복구한다"를 적는다

각 메서드 본문은 `principal.requireAdminId()`를 꺼내 서비스로 위임하는 1줄이다.
권한 애노테이션·트랜잭션·`try-catch`를 붙이지 않는다(D-040·D-020·D-017).

**저녁반 "회원 검색 → 추가" 흐름을 위한 새 검색 API를 만들지 않는다** — FE는 기존
`GET /api/admin/members?keyword=`(02-09)를 그대로 호출해 `memberId`를 얻고, 이 컨트롤러의
`POST /evening`에 그 id를 넘긴다(Don't Hand-Roll, 06-RESEARCH Open Question 2 확정).
`POST /evening`의 `@Operation` description에 이 사용 흐름을 한 줄로 적어 FE가 검색 경로를
찾아 헤매지 않게 한다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "/evening" src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt && ! grep -qE "@Transactional|@PreAuthorize|try \{" src/main/kotlin/com/goldwrestling/attendance/AdminAttendanceController.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AdminAttendanceController.kt`에 `@GetMapping`, `@PutMapping`, `@PostMapping("/evening")`, `@DeleteMapping("/{attendanceId}")`가 각각 존재한다
    - 파일에 `@Transactional`·`@PreAuthorize`·`try {` 문자열이 존재하지 않는다
    - 모든 메서드에 `@Operation`이 붙어 있다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>출석 4개 엔드포인트가 관리자 전용 URL 인가 아래 노출된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminAttendanceControllerTest — 성공 경로 4종 + 대표 실패 5종(HTTP 계약)</name>
  <files>src/test/kotlin/com/goldwrestling/attendance/AdminAttendanceControllerTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/batch/AdminBatchControllerTest.kt 1-70행 (MockMvc 조합·토큰 발급·clock 리셋 주의사항·`jsonPath("$.code")` 단언 관례)
    - src/test/kotlin/com/goldwrestling/attendance/AttendanceFixtures.kt
    - docs/error-codes.md "운영 코드 (Phase 6)" (기대할 code 값)
  </read_first>
  <behavior>
    - `GET`이 200과 명단 JSON을 반환하고 미체크 회원의 `status`가 null이다
    - `PUT`이 200과 `status = ATTENDED`를 반환한다
    - `POST /evening`이 200을 반환하고 응답의 `deducted`가 true다(회비 없는 회원)
    - `DELETE`가 204를 반환한다
    - 예약자가 아닌 회원 `PUT` → 409 `ATTENDANCE_MEMBER_NOT_RESERVED`
    - 저녁반 세션에 `PUT` → 409 `ATTENDANCE_CLASS_TYPE_MISMATCH`
    - 잔여 부족 회원 `POST /evening` → 409 `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`
    - 없는 attendanceId `DELETE` → 404 `ATTENDANCE_NOT_FOUND`
    - 회원 토큰으로 아무 엔드포인트나 호출 → 403 `ACCESS_DENIED`
  </behavior>
  <action>
`AdminAttendanceControllerTest`를 `AdminBatchControllerTest`와 **동일한 애노테이션 조합**으로 만든다
(`@SpringBootTest` + `@AutoConfigureMockMvc` + `@Import(TestcontainersConfiguration::class)`).
clock은 실제 `Instant.now()` 기준으로 리셋한다 — 과거로 고정하면 발급 토큰이 즉시 만료돼 401이 된다
(05-15 선례). 상대 날짜가 필요하면 `LocalDate.now()` 기준 오프셋으로 만든다.

응답 본문 단언은 `jsonPath("$.code")`로 에러코드를, 성공 응답은 `jsonPath("$.status")`·
`jsonPath("$.deducted")` 등 계약 필드를 확인한다. ProblemDetail 응답의 `content().contentType`이
`application/problem+json`인지도 최소 1개 케이스에서 확인한다(D-017 계약).

데이터 정리는 `@AfterEach` + `JdbcClient`로 하고 클래스 레벨 `@Transactional`은 붙이지 않는다 —
저녁반 차감이 실제로 커밋돼야 삭제·복구 케이스가 의미를 갖는다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.AdminAttendanceControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.AdminAttendanceControllerTest"` 가 0 failures로 끝난다
    - 테스트가 `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`, `ATTENDANCE_MEMBER_NOT_RESERVED`, `ATTENDANCE_CLASS_TYPE_MISMATCH`, `ATTENDANCE_NOT_FOUND`, `ACCESS_DENIED` 5개 코드를 각각 단언한다
    - `application/problem+json` 문자열이 테스트에 최소 1회 등장한다
  </acceptance_criteria>
  <done>출석 API의 성공·실패 HTTP 계약이 FE가 의존할 수 있는 형태로 검증된다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AttendanceConcurrencyTest(이중 차감 0건) + openapi.yaml 재생성</name>
  <files>src/test/kotlin/com/goldwrestling/attendance/AttendanceConcurrencyTest.kt, docs/api/openapi.yaml</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/reservation/ReservationCapacityConcurrencyTest.kt (동시성 테스트 골격: `ExecutorService` + `CountDownLatch`, `@Transactional` 금지, 성공 건수·DB 행 수·이력 건수 3중 단언, 테스트 후 수동 정리)
    - .claude/skills/add-domain-test/SKILL.md §4
    - .claude/skills/add-endpoint/SKILL.md §6 (openapi 재생성 절차)
    - docs/policies.md §8
  </read_first>
  <behavior>
    - 같은 회원·같은 저녁반 세션에 10개 스레드가 동시에 `addEveningAttendance`를 호출하면 성공은 정확히 1건이다
    - `attendance` 행 수가 1이다
    - `EVENING_HALF` 이력 건수가 1이고 `remainingCount`가 정확히 0.5만 줄었다
    - 실패한 9건은 `DuplicateAttendanceException` 계열이며 잔여를 건드리지 않았다
  </behavior>
  <action>
`AttendanceConcurrencyTest`를 `ReservationCapacityConcurrencyTest` 골격으로 만든다.
**테스트 메서드에 `@Transactional`을 붙이지 않는다** — 각 스레드가 별도 트랜잭션이어야 경쟁이
재현된다. 데이터는 `@AfterEach`에서 직접 정리한다.

시나리오: 잔여 5.0인 `SESSION_PASS`를 가진 회원 1명, `EVENING` 세션 1개, 스레드 10개가
`CountDownLatch`로 동시에 `attendanceService.addEveningAttendance`를 호출한다.
단언 3중: 성공 카운트 == 1, `attendance` 행 수 == 1, `EVENING_HALF` 이력 == 1 이고
`remainingCount`가 4.5(=5.0 - 0.5)임을 `compareTo`로 확인한다.
**성공 건수만 보지 않는다** — 이력 누락은 조용한 버그다(add-domain-test §4).

이어서 `docker compose up -d` 후 `./gradlew generateApiDocs`로 `docs/api/openapi.yaml`을 재생성한다.
`git diff docs/api/openapi.yaml`으로 (1) 출석 4개 경로만 추가됐는지, (2) `servers:`가 `/`로
유지되는지, (3) 06-04에서 추가된 공지 경로가 그대로 남아 있는지 확인한다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.AttendanceConcurrencyTest" && grep -q "/api/admin/attendances" docs/api/openapi.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.AttendanceConcurrencyTest"` 가 0 failures로 끝난다
    - 테스트 클래스·메서드에 `@Transactional`이 없다
    - 테스트가 성공 건수, `attendance` 행 수, `EVENING_HALF` 이력 건수를 각각 단언한다
    - `docs/api/openapi.yaml`에 `/api/admin/attendances`, `/api/admin/attendances/evening`, `/api/admin/attendances/{attendanceId}` 경로와 기존 `/api/admin/notices` 경로가 모두 존재한다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>동시 요청에서도 출석 1건·차감 0.5회만 반영됨이 실증되고 FE 계약이 갱신됐다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `/api/admin/attendances/**` | 신뢰 경계. JWT 인증 + `hasRole("ADMIN")` URL 규칙이 유일한 관문 |
| 동시 요청 → 잔여 횟수 | 경쟁 경계. DB 유니크 + 조건부 UPDATE가 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Elevation of Privilege | 회원이 출석 API로 자가 차감·자가 복구 | mitigate | `/api/admin/**` 전역 `hasRole("ADMIN")`. Task 2가 회원 토큰 403을 실증 |
| T-06-26 | Tampering | 관리자 더블클릭·재시도로 저녁반 이중 차감 | mitigate | 회원×세션 유니크 + 트랜잭션 롤백. Task 3의 10스레드 동시성 테스트가 성공 1건·이력 1건을 실증 |
| T-06-27 | Information Disclosure | 실패 응답에 회원·이용권 식별자 노출 | mitigate | 도메인 예외 메시지 무보간(06-05) + ProblemDetail 전역 변환(D-017) |
| T-06-28 | Information Disclosure | 명단 응답으로 타 회원 개인정보(전화번호 등) 과다 노출 | accept | 관리자 전용 화면이며 명단은 `memberId`·`memberName`만 담는다(06-06 DTO 설계). 회원 principal에게는 이 API가 노출되지 않는다 |
| T-06-29 | Repudiation | 누가 출석을 확인했는지 추적 불가 | mitigate | `checked_by_admin_id`·`checked_at`을 매 호출마다 기록(06-06) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew cleanTest test --tests "com.goldwrestling.attendance.*"` 0 failures
- `git diff docs/api/openapi.yaml` 에 출석 경로 추가만 반영된다
</verification>

<success_criteria>
- 출석 4개 엔드포인트가 성공·실패 계약과 함께 검증된다
- 동시 저녁반 출석에서 이중 차감 0건이 실증된다
- openapi.yaml이 공지+출석 경로를 모두 담은 상태로 재생성됐다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-08-SUMMARY.md` when done
</output>
