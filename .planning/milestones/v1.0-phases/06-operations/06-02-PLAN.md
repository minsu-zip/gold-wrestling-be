---
phase: 06-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V11__create_attendance_and_notice.sql
  - src/main/kotlin/com/goldwrestling/attendance/Attendance.kt
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceStatus.kt
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt
  - src/main/kotlin/com/goldwrestling/notice/Notice.kt
  - src/main/kotlin/com/goldwrestling/notice/NoticeRepository.kt
  - src/test/kotlin/com/goldwrestling/attendance/AttendanceFixtures.kt
  - src/test/kotlin/com/goldwrestling/attendance/AttendanceRepositoryTest.kt
  - src/test/kotlin/com/goldwrestling/notice/NoticeRepositoryTest.kt
autonomous: true
requirements: [ATTEND-01, ATTEND-02, NOTICE-01, NOTICE-02, NOTIF-03]
must_haves:
  truths:
    - "같은 회원을 같은 ClassSession에 두 번 출석 기록할 수 없다 — DB 유니크가 거부한다"
    - "출석 상태를 ABSENT에서 ATTENDED로 바꿔도 새 행이 생기지 않고 기존 행의 status만 바뀐다"
    - "회원별 마지막 ATTENDED 수업일을 한 번의 쿼리로 벌크 조회할 수 있고, ABSENT는 그 결과에 섞이지 않는다"
    - "공지를 최신순으로 페이지 조회할 수 있다"
  artifacts:
    - path: "src/main/resources/db/migration/V11__create_attendance_and_notice.sql"
      provides: "attendance·notice 테이블 + 활동 피드 인덱스"
      contains: "uq_attendance_member_session"
    - path: "src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt"
      provides: "CR-03 벌크 조회 + 세션별 출석 조회"
      exports: ["findLastAttendedClassDates", "findAllByClassSessionId"]
    - path: "src/main/kotlin/com/goldwrestling/notice/Notice.kt"
      provides: "공지 엔티티(제목·본문 가변)"
      contains: "var title"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/attendance/Attendance.kt"
      to: "attendance 테이블"
      via: "JPA 매핑 (ddl-auto=validate가 기동 시 검증)"
      pattern: "@Table\\(name = \"attendance\"\\)"
    - from: "src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt"
      to: "common.projection.MemberDateProjection"
      via: "벌크 스칼라 프로젝션 (D-115)"
      pattern: "MemberDateProjection"
---

<objective>
Phase 6의 두 신규 테이블(`attendance`·`notice`)과 그 JPA 매핑·리포지토리를 만들고, **제약이
실제로 동작하는지 Testcontainers로 실증**한다. 활동 피드용 인덱스(D-135)도 같은 마이그레이션에 넣는다
— 커밋된 마이그레이션은 수정할 수 없으므로 V11이 유일하게 저렴한 시점이다.

목적: 이후 6개 플랜(출석 서비스·공지 API·CR-03 배선)이 전제하는 저장 계층을 먼저 확정한다.
특히 **회원×세션 조건 없는 일반 유니크**(예약의 부분 유니크를 복사하면 안 된다)와
**`ATTENDED`만 인정하는 CR-03 벌크 조회**는 이 phase의 두 대표 함정이라 여기서 테스트로 못 박는다.

산출물: V11 마이그레이션, 엔티티 2 + enum 1, 리포지토리 2, 통합테스트 2 + 픽스처 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/conventions.md
@.claude/skills/add-migration/SKILL.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: V11 마이그레이션 — attendance·notice 테이블 + 활동 피드 인덱스</name>
  <files>src/main/resources/db/migration/V11__create_attendance_and_notice.sql</files>
  <read_first>
    - src/main/resources/db/migration/V6__create_schedule_reservation_notification.sql (notification 테이블 정의와 `idx_notification_unread`, 예약의 **부분** 유니크 `ux_reservation_*`가 왜 조건부인지 확인 — 출석은 이 관례를 따르지 않는다)
    - src/main/resources/db/migration/V9__relax_pass_transaction_subject_and_add_batch_execution.sql (신규 테이블 + FK 이름 규칙 + 헤더 주석 스타일)
    - .claude/skills/add-migration/SKILL.md (금지사항·PK·타입 규약)
    - .planning/phases/06-operations/06-PATTERNS.md (V11 골격 섹션)
  </read_first>
  <action>
`src/main/resources/db/migration/V11__create_attendance_and_notice.sql`을 새로 만든다. 기존 V1~V10은
절대 수정하지 않는다.

파일 상단에 V9 스타일의 헤더 주석으로 이 마이그레이션이 담는 3가지(출석 테이블, 공지 테이블,
활동 피드 인덱스)와 근거 결정(D-127·D-128·D-131·D-135)을 적는다.

**attendance 테이블** — 컬럼: `id`(BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY),
`class_session_id`(BIGINT NOT NULL), `member_id`(BIGINT NOT NULL), `status`(VARCHAR(20) NOT NULL),
`pass_transaction_id`(BIGINT, nullable), `checked_by_admin_id`(BIGINT NOT NULL),
`checked_at`(TIMESTAMPTZ NOT NULL), `created_at`(TIMESTAMPTZ NOT NULL DEFAULT now()).
FK 4개: `fk_attendance_class_session`→class_session(id), `fk_attendance_member`→member(id),
`fk_attendance_pass_transaction`→pass_transaction(id), `fk_attendance_checked_by`→admin(id).
유니크: `uq_attendance_member_session UNIQUE (class_session_id, member_id)` — **조건(WHERE) 없는 일반
유니크다.** 예약의 `ux_reservation_session_member_active ... WHERE status='ACTIVE'`를 복사하지 않는다:
예약은 취소 후 재예약이 가능해야 해서 부분 유니크가 맞지만, 출석은 레코드가 있으면 이미 체크된
것이고 `ABSENT`↔`ATTENDED` 정정은 새 행이 아니라 같은 행의 `status` UPDATE로 처리한다(D-127).
이 이유를 SQL 주석으로 남긴다.
인덱스: `idx_attendance_member ON attendance (member_id, checked_at)` — CR-03 벌크 조회 대비.

**notice 테이블** — 컬럼: `id`(동일 PK 관례), `title`(VARCHAR(200) NOT NULL), `content`(TEXT NOT NULL),
`created_by_admin_id`(BIGINT NOT NULL), `created_at`(TIMESTAMPTZ NOT NULL DEFAULT now()),
`updated_at`(TIMESTAMPTZ NOT NULL DEFAULT now()). FK: `fk_notice_created_by`→admin(id).
인덱스: `idx_notice_created_at ON notice (created_at DESC)`.
**soft delete 컬럼(`deleted_at` 등)을 만들지 않는다** — D-131이 hard delete로 확정했다.

**활동 피드 인덱스** — `CREATE INDEX idx_notification_occurred_at ON notification (occurred_at DESC);`
(D-135). notification 테이블 자체는 건드리지 않는다 — 수신자 컬럼 추가 금지(D-097).

`branch_id`는 attendance·notice 어디에도 두지 않는다 — 출석은 `class_session`을 통해,
공지는 지점 구분 요구가 없어(requirements NOTICE-01/02) 지점 스코프가 필요하지 않다. 이 판단을
헤더 주석에 한 줄로 남긴다.
  </action>
  <verify>
    <automated>grep -q "uq_attendance_member_session UNIQUE (class_session_id, member_id)" src/main/resources/db/migration/V11__create_attendance_and_notice.sql && ! grep -iE "^\s*(CONSTRAINT )?uq_attendance.*WHERE" src/main/resources/db/migration/V11__create_attendance_and_notice.sql && grep -q "idx_notification_occurred_at" src/main/resources/db/migration/V11__create_attendance_and_notice.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/resources/db/migration/V11__create_attendance_and_notice.sql`이 존재하고 V1~V10 파일의 `git diff`가 비어 있다
    - attendance 유니크 제약에 `WHERE` 절이 없다
    - `deleted_at` 문자열이 파일에 존재하지 않는다
    - `idx_notification_occurred_at`, `idx_attendance_member`, `idx_notice_created_at` 세 인덱스가 모두 존재한다
  </acceptance_criteria>
  <done>attendance·notice 테이블과 활동 피드 인덱스가 Flyway 마이그레이션으로 정의됐고, 출석 유니크는 조건 없는 일반 유니크다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attendance·AttendanceStatus·Notice 엔티티와 리포지토리 2종</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/Attendance.kt, src/main/kotlin/com/goldwrestling/attendance/AttendanceStatus.kt, src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt, src/main/kotlin/com/goldwrestling/notice/Notice.kt, src/main/kotlin/com/goldwrestling/notice/NoticeRepository.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/reservation/Reservation.kt (엔티티 관례: LAZY 명시, `var status`, `@Id` 블록 분리)
    - src/main/kotlin/com/goldwrestling/notification/Notification.kt (nullable `@ManyToOne` 관례 + KDoc으로 설계 의도를 남기는 관례)
    - src/main/kotlin/com/goldwrestling/pass/PassRepository.kt 145-211행 (벌크 스칼라 프로젝션 `@Query` 관례 — `findLastDeductibleSessionPassRegistrationDates`)
    - src/main/kotlin/com/goldwrestling/common/projection/MemberDateProjection.kt (`getMemberId()`/`getDate()` 시그니처)
    - src/main/kotlin/com/goldwrestling/notification/NotificationRepository.kt (가장 단순한 JpaRepository 상속 관례)
    - docs/conventions.md §3 (엔티티 규약)
  </read_first>
  <behavior>
    - `AttendanceStatus`는 `ATTENDED`/`ABSENT` 2값이며 KDoc에 "저녁반은 ATTENDED만 쓴다"가 명시된다
    - `Attendance`는 `member`/`classSession`/`checkedBy`를 non-null LAZY `@ManyToOne`으로, `passTransaction`을 nullable LAZY `@ManyToOne`으로 갖는다
    - `Attendance.status`만 `var`, 나머지는 `val`
    - `AttendanceRepository.findLastAttendedClassDates(memberIds)`가 `ATTENDED` 행만 집계해 회원별 최대 `classDate`를 반환한다
    - `NoticeRepository.findAllByOrderByCreatedAtDescIdDesc(pageable)`가 최신순 페이지를 반환한다
  </behavior>
  <action>
`attendance` 패키지에 3개 파일을 만든다.

`AttendanceStatus` — `ATTENDED`/`ABSENT` enum. 각 값에 KDoc 한 줄(예약제/1:1은 둘 다 사용, 저녁반은
`ATTENDED`만 사용 — D-127).

`Attendance` — `@Entity @Table(name = "attendance")`, `data class` 금지. 생성자 파라미터 순서는
`member`, `classSession`, `status`, `passTransaction`, `checkedBy`, `checkedAt`, `createdAt`.
연관은 전부 `@ManyToOne(fetch = FetchType.LAZY)` 명시 + `@JoinColumn` 이름 지정
(`member_id`, `class_session_id`, `pass_transaction_id`, `checked_by_admin_id`).
`status`는 `@Enumerated(EnumType.STRING) @Column(nullable = false, length = 20)`이며 **`var`**다
(D-127 소급 수정 허용). `passTransaction`만 nullable(`PassTransaction?`) — 예약제/1:1은 항상 null.
`id`는 `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) val id: Long? = null`로 클래스 본문에 둔다.
클래스 KDoc에 (1) 출석은 차감과 무관한 참고 데이터(policies §6), (2) 저녁반만 `passTransaction`이
채워진다, (3) 회원×세션 유니크가 이중 출석·이중 차감의 구조적 방지선이라는 3가지를 적는다.

`AttendanceRepository : JpaRepository<Attendance, Long>` — 메서드 4개:
- `findAllByClassSessionId(classSessionId: Long): List<Attendance>` — 명단 조회용(파생 쿼리)
- `findByClassSessionIdAndMemberId(classSessionId: Long, memberId: Long): Attendance?` — upsert 판정용
- `existsByClassSessionIdAndMemberId(classSessionId: Long, memberId: Long): Boolean` — 사전 검사용
  (KDoc에 "이 검사는 사용자에게 친절한 메시지를 주기 위한 것이고 실제 방어선은 `uq_attendance_member_session`"이라고 명시 — `ReservationRepository.existsByMemberIdAndClassDateAndStartTimeAndStatus` KDoc과 같은 관례)
- `findLastAttendedClassDates(memberIds: Collection<Long>): List<MemberDateProjection>` — `@Query`로
  `select a.member.id as memberId, max(a.classSession.classDate) as date from Attendance a
   where a.member.id in :memberIds and a.status = com.goldwrestling.attendance.AttendanceStatus.ATTENDED
   group by a.member.id`. **`status` 필터를 빼면 불참이 "마지막 참여일"로 잡혀 부당하게 유예가
  갱신된다** — 이 경고를 KDoc에 남긴다(policies §6 "출석(ATTENDED)만 기준으로 한다").

`notice` 패키지에 2개 파일을 만든다.
`Notice` — `title`(`var`, length 200)·`content`(`var`, `columnDefinition = "TEXT"`)·`createdByAdmin`
(LAZY `@ManyToOne`, `created_by_admin_id`)·`createdAt`(`val`, `updatable = false`)·`updatedAt`(`var`).
수정이 가능한 도메인이라 `Notification`(전부 `val`)이 아니라 `Reservation`의 가변 필드 관례를 따른다.
`@PreUpdate`를 쓰지 않는다 — 이 저장소에 선례가 없고 시각은 `Clock` 주입으로 서비스가 명시 세팅한다.
`NoticeRepository : JpaRepository<Notice, Long>` — `findAllByOrderByCreatedAtDescIdDesc(pageable: Pageable): Page<Notice>`
하나만 둔다(같은 시각 동률에서도 순서가 결정적이어야 페이지 경계가 흔들리지 않는다).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "AttendanceStatus.ATTENDED" src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt && grep -c "FetchType.LAZY" src/main/kotlin/com/goldwrestling/attendance/Attendance.kt</automated>
  </verify>
  <acceptance_criteria>
    - `Attendance.kt`에 `FetchType.LAZY`가 4회 이상 등장하고 `data class`가 없다
    - `AttendanceRepository.findLastAttendedClassDates`의 `@Query` 문자열에 `AttendanceStatus.ATTENDED`가 포함된다
    - `Notice.kt`에 `var title`과 `var content`가 있고 `deletedAt` 필드가 없다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>두 신규 테이블의 JPA 매핑과 리포지토리가 존재하고, CR-03 벌크 조회가 ATTENDED만 집계한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 리포지토리 통합테스트 2종 + AttendanceFixtures — 유니크 제약·ATTENDED 필터·최신순 페이지를 실증</name>
  <files>src/test/kotlin/com/goldwrestling/attendance/AttendanceFixtures.kt, src/test/kotlin/com/goldwrestling/attendance/AttendanceRepositoryTest.kt, src/test/kotlin/com/goldwrestling/notice/NoticeRepositoryTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/batch/BatchFixtures.kt (픽스처 object 관례: 고정 시각 상수 + 최소 생성 함수)
    - src/test/kotlin/com/goldwrestling/batch/InactivityBatchRunnerTest.kt 1-60행 (통합테스트 애노테이션 조합: `@SpringBootTest` + `@Import(TestcontainersConfiguration::class)` + `TestClockConfiguration`, `@AfterEach` 정리 관례)
    - .claude/skills/add-domain-test/SKILL.md §2 (애노테이션 조합 통일 이유)
    - docs/conventions.md §10.0 (리포지토리 커스텀 쿼리 = Testcontainers 통합테스트 필수)
  </read_first>
  <behavior>
    - 같은 회원·같은 세션에 두 번째 `Attendance`를 저장하면 `DataIntegrityViolationException`이 난다
    - `ABSENT`로 저장한 행의 `status`를 `ATTENDED`로 바꿔 저장해도 attendance 행 수가 1로 유지된다
    - `findLastAttendedClassDates`가 `ATTENDED`가 있는 회원만 반환하고, 여러 날짜 중 가장 늦은 수업일을 준다
    - 불참(`ABSENT`)만 있는 회원은 `findLastAttendedClassDates` 결과에 아예 나타나지 않는다
    - `findAllByOrderByCreatedAtDescIdDesc`가 최신 공지를 첫 페이지 첫 항목으로 준다
  </behavior>
  <action>
`AttendanceFixtures`(테스트 object)를 `BatchFixtures` 관례로 만든다 — 고정 시각 상수
(`FIXED_TIME`/`FIXED_TODAY`)와 `branch`/`admin`/`member`/`classSchedule`/`classSession`/`attendance`
최소 생성 함수. 이 픽스처는 06-03(배치 테스트)·06-06~06-08(출석 서비스·컨트롤러·동시성)이 함께 쓴다.
`ClassSession` 생성 시 `reservedCount`·`status`는 기본값 없는 필수 파라미터이므로 명시한다.

`AttendanceRepositoryTest` — `@SpringBootTest` + `@Import(TestcontainersConfiguration::class)` 조합을
기존 배치 테스트와 동일하게 맞춘다. 테스트 메서드명은 백틱 한국어 서술문으로 policies §6 문장을
그대로 쓴다. 최소 5개:
1. `같은 회원을 같은 수업에 두 번 출석 기록할 수 없다` — 두 번째 `saveAndFlush`에서
   `DataIntegrityViolationException`을 기대
2. `불참에서 출석으로 정정해도 새 행이 생기지 않는다` — 같은 엔티티 `status` 변경 후 저장,
   `findAllByClassSessionId` 크기가 1
3. `마지막 출석일은 출석(ATTENDED) 기록만 기준으로 한다` — 같은 회원에 `ATTENDED`(이른 날짜)와
   `ABSENT`(늦은 날짜)를 넣고 결과가 이른 날짜(=ATTENDED 쪽)임을 단언
4. `불참만 있는 회원은 마지막 출석일 후보에 나타나지 않는다` — 결과 목록이 그 memberId를 포함하지 않음
5. `회원별로 가장 최근 출석 수업일을 반환한다` — 회원 2명 × 각 2건으로 그룹별 max 검증
정리는 `@AfterEach`에서 `JdbcClient`로 이 클래스가 만든 데이터만 지운다(FK 역순:
attendance → reservation 없음 → class_session → pass_transaction 없음 → pass → member → admin → branch).

`NoticeRepositoryTest` — 동일 애노테이션 조합. 최소 2개: `공지 목록은 최신순으로 조회된다`(3건 저장 후
첫 항목이 가장 늦은 `createdAt`), `삭제한 공지는 조회되지 않는다`(hard delete 후 `findById`가 empty).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.AttendanceRepositoryTest" --tests "com.goldwrestling.notice.NoticeRepositoryTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.AttendanceRepositoryTest"` 가 0 failures로 끝난다
    - `AttendanceRepositoryTest`에 `불참만 있는 회원` 문자열을 포함한 테스트 메서드가 존재한다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다 (V11이 빈 DB에서 재생되고 엔티티 매핑 validate 통과)
  </acceptance_criteria>
  <done>유니크 제약·상태 정정·ATTENDED 필터가 실제 PostgreSQL에서 검증됐고, 이후 플랜이 쓸 출석 픽스처가 존재한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 애플리케이션 → PostgreSQL | 잔여 횟수·출석 정합성의 최종 방어선이 DB 제약이다 (D-021) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | `attendance` 이중 행으로 인한 저녁반 이중 차감 | mitigate | `uq_attendance_member_session`(조건 없는 일반 유니크)로 DB에서 거부. Task 3이 `DataIntegrityViolationException`으로 실증 |
| T-06-04 | Tampering | `findLastAttendedClassDates`에서 `ABSENT` 포함 시 미사용 차감 유예가 부당 갱신 | mitigate | `@Query`에 `status = ATTENDED` 필터 고정 + "불참만 있는 회원" 테스트로 회귀 방지 |
| T-06-05 | Information Disclosure | `notice` 본문에 삽입된 스크립트가 회원 화면에서 실행(XSS) | transfer | BE는 원문 저장·전달만 하고 이스케이프는 FE 렌더링 책임(별도 레포). 06-04가 이 계약을 `@Schema` description에 명시한다 |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음(06-RESEARCH.md 감사 N/A) |
</threat_model>

<verification>
- `./gradlew ktlintFormat && ./gradlew build` BUILD SUCCESSFUL (Testcontainers가 V1~V11을 빈 DB에서 재생)
- `ls src/main/resources/db/migration | wc -l` 결과가 11
- `git diff --name-only src/main/resources/db/migration/` 에 V11만 등장한다
</verification>

<success_criteria>
- attendance·notice 테이블이 생성되고 엔티티 매핑이 `ddl-auto=validate`를 통과한다
- 회원×세션 유니크가 실제로 두 번째 INSERT를 거부한다
- `findLastAttendedClassDates`가 `ATTENDED`만 집계한다
- 공지 최신순 페이지 조회가 동작한다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-02-SUMMARY.md` when done
</output>
