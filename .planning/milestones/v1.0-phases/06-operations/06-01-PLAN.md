---
phase: 06-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/glossary.md
  - docs/policies.md
  - docs/decisions.md
  - docs/error-codes.md
  - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt
  - src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt
autonomous: true
requirements: [ATTEND-01, ATTEND-02, NOTICE-01, NOTIF-03]
must_haves:
  truths:
    - "출석 상태·저녁반 복구 사유·활동 피드 용어가 glossary에 정의돼 있어 이후 플랜이 이름을 새로 짓지 않는다"
    - "policies §4.2가 저녁반 출석 추가 = 차감 원자 결합·회비 우선·거부·삭제 복구를 명문화한다"
    - "이 phase가 던질 6종 도메인 예외의 에러코드가 ErrorCode enum과 error-codes.md에 1:1로 존재한다"
    - "TransactionReason에 EVENING_HALF_REFUND가 존재해 저녁반 복구 이력이 예약 복구와 구분된다"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      provides: "출석·공지 신규 에러코드 6종"
      contains: "EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE"
    - path: "src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt"
      provides: "저녁반 0.5회 복구 사유"
      contains: "EVENING_HALF_REFUND"
    - path: "docs/error-codes.md"
      provides: "FE 분기 계약 표 (운영 코드 섹션)"
      contains: "ATTENDANCE_NOT_FOUND"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt"
      to: "docs/error-codes.md"
      via: "enum 상수 ↔ 표 행 1:1"
      pattern: "EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE"
---

<objective>
Phase 6이 쓸 **이름과 계약**을 코드보다 먼저 확정한다 — 용어(glossary), 도메인 규칙(policies §4.2),
설계 결정(decisions D-132~D-135), 에러코드 레지스트리(error-codes.md + `ErrorCode` enum),
차감 사유(`TransactionReason.EVENING_HALF_REFUND`).

목적: CLAUDE.md 규칙 3(네이밍은 glossary만 따른다)·규칙 5(설계 결정 기록)를 지키기 위해,
이후 9개 플랜이 이름·에러코드를 각자 지어내는 상황을 원천 차단한다. `docs/`가 스펙의 원본이므로
(문서 우선순위) 코드가 먼저 나가면 문서가 코드를 뒤따라가는 역전이 발생한다.

산출물: 문서 4종 갱신 + enum 2개 확장. 새 로직 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/glossary.md
@docs/error-codes.md
@.planning/phases/06-operations/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: glossary·policies §4.2·decisions에 Phase 6 용어와 재량 결정 4건을 기록한다</name>
  <files>docs/glossary.md, docs/policies.md, docs/decisions.md</files>
  <read_first>
    - docs/glossary.md (표 형식·기존 섹션 구성 확인 — "차감 사유(TransactionReason)" 표와 phase별 섹션 관례)
    - docs/policies.md (§4.2 저녁반 0.5회 참여, §6 출석 체크 — §6은 이미 D-127 반영 완료이므로 건드리지 않는다)
    - docs/decisions.md 마지막 40줄 (D-131까지 기록됨. 제목·이유·기각 대안 3단 구성 확인)
    - .planning/phases/06-operations/06-CONTEXT.md (D-127~D-131 확정 내용, Claude's Discretion 범위)
  </read_first>
  <action>
docs/glossary.md에 "## 운영 (Phase 6)" 섹션을 "## 배치 (Phase 5)" 다음에 추가하고 아래 용어를 표로 정의한다
(기존 표와 동일한 3열 구조: 한국어 / 코드 네이밍 / 설명):
- 출석 상태 → `AttendanceStatus` — `ATTENDED`(출석) / `ABSENT`(불참). **레코드 부재 = 미체크**이며
  저녁반은 `ATTENDED`만 쓴다(불참 상태 없음, D-127)
- 출석 확인 주체 → `checkedBy` — DB `attendance.checked_by_admin_id`
- 출석 확인 시각 → `checkedAt` — DB `attendance.checked_at`
- 저녁반 차감 연결 → `passTransaction` — DB `attendance.pass_transaction_id`(nullable).
  저녁반 출석이 유발한 0.5회 차감 이력. 예약제/1:1은 항상 null
- 활동 피드 → `ActivityFeed` — `notification` 테이블의 다른 뷰(읽음 여부 무관 시간순, 기간·종류 필터, D-129)
- 미확인 알림 수 → `unreadCount` — 알림 목록 응답에 포함되는 미확인 건수(별도 엔드포인트 없음, D-129)
- 모두 읽음 → `markAllAsRead` — 개별 읽음은 만들지 않는다(D-129)
- 공지사항 → `Notice`(이미 핵심 엔티티 표에 있음)의 하위 용어로 `title`/`content`만 존재(D-131)

같은 파일 "## 차감 사유 (TransactionReason)" 표에 행을 하나 추가한다:
`EVENING_HALF_REFUND` | 저녁반 출석 삭제로 인한 0.5회 복구 (D-128)

docs/policies.md §4.2(저녁반 0.5회 참여)에 아래 규칙을 추가한다 — §6은 이미 반영됐으므로 수정하지 않는다:
- 저녁반 출석 추가와 0.5회 차감은 **한 트랜잭션**이며 서버가 판정한다(D-128)
- 수업날 기준 유효한 `EVENING_MEMBERSHIP`을 보유하면 **차감하지 않는다**(회비 우선)
- 회비가 없으면 `SESSION_PASS`에서 0.5 차감한다. 차감 대상 선택은 D-091(만료 임박순·합산 금지)을
  그대로 따르고, **유효기간 판정 기준일은 오늘이 아니라 수업날**이다(소급 입력 대응)
- 회비도 없고 `SESSION_PASS` 잔여가 0.5 미만이면 **출석 추가 자체를 거부**한다 — 관리자가
  수동 가감(§4.2a)으로 충전한 뒤 재시도한다
- 같은 회원이 같은 `ClassSession`에 두 번 출석할 수 없다(회원×세션 유니크) — 이중 차감의 구조적 방지
- 저녁반 출석을 삭제하면 연결된 차감을 `EVENING_HALF_REFUND` 사유로 **자동 복구**한다

docs/decisions.md 끝에 D-132~D-135를 기존 3단 구성(제목 / 날짜·내용 / 이유 / 기각 대안)으로 추가한다:
- **D-132. 출석 체크 API는 타임별 일괄 저장이 아니라 회원 건별 upsert로 한다** — 이유: 저녁반 추가는
  차감 트랜잭션을 유발해 회원마다 성공/409가 갈린다. 일괄 저장이면 한 명의 잔여 부족이 다른 회원의
  출석까지 롤백시키거나, 부분 성공을 응답으로 표현하는 별도 계약이 필요해진다. 기각 대안: 타임별 일괄
  저장(부분 실패 계약 필요), 저녁반만 건별·예약제만 일괄(같은 화면에 두 계약 공존)
- **D-133. 저녁반 차감 불가는 신규 코드 `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE`(409)로 응답한다** —
  `INSUFFICIENT_PASS_COUNT` 재사용을 기각한다. 이유: 이 실패는 "잔여가 음수가 된다"가 아니라
  "회비도 없고 잔여도 0.5 미만"이라는 복합 사유이고, FE 안내 문구가 예약 실패("다른 시간을 고르세요")와
  완전히 다르다(관리자에게 "수동 가감으로 충전 후 재시도"를 안내해야 한다). 06-CONTEXT.md 문서 정합
  항목도 신규 코드 등록을 지시했다. 기각 대안: `INSUFFICIENT_PASS_COUNT` 재사용(FE가 맥락 구분 불가)
- **D-134. 공지 열람에는 회원 상태 게이트를 걸지 않는다(D-071 연장)** — 이유: 공지는 개인 데이터가
  아니라 지점 전체 공지이고, `MemberPassService`가 이미 같은 이유로 `MemberStateGate`를 호출하지
  않는다(D-071). `requireActive`를 걸면 휴회(`ON_LEAVE`) 회원이 운영 공지를 못 보게 되는데 이는
  policies §5의 취지(휴회는 차감 정지이지 정보 차단이 아니다)와 어긋난다. 기각 대안:
  `requireActive` 적용(휴회 회원 차단), 새 게이트 메서드 신설(상태 게이트 축이 두 갈래로 갈라짐)
- **D-135. 활동 피드용 `idx_notification_occurred_at (occurred_at DESC)`를 V11에 함께 만든다** —
  이유: 기존 `idx_notification_unread`는 선두 컬럼이 `is_read`라 읽음 여부 무관 시간순 조회(NOTIF-03)에
  온전히 쓰이지 못한다. 인덱스 하나는 값싼 대비책이고, 커밋된 마이그레이션은 수정할 수 없으므로
  attendance/notice와 같은 V11에 함께 넣는 것이 유일하게 저렴한 시점이다. 기각 대안: 미도입(운영
  데이터가 쌓인 뒤 별도 마이그레이션 필요), `(type, occurred_at DESC)` 복합(종류 필터가 선택적이라
  단일 컬럼 인덱스가 더 넓게 쓰인다)
  </action>
  <verify>
    <automated>grep -c "EVENING_HALF_REFUND" docs/glossary.md | grep -qv '^0$' && grep -q "D-135" docs/decisions.md && grep -q "EVENING_MEMBERSHIP" docs/policies.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/glossary.md에 `AttendanceStatus`, `ActivityFeed`, `unreadCount`, `EVENING_HALF_REFUND` 문자열이 모두 존재한다
    - docs/policies.md §4.2에 "회비 우선", "수업날", "EVENING_HALF_REFUND" 문자열이 존재한다
    - docs/decisions.md에 `## D-132.`, `## D-133.`, `## D-134.`, `## D-135.` 제목 4개가 존재하고 각 항목에 "기각 대안" 줄이 있다
    - docs/policies.md §6은 변경되지 않았다 (`git diff docs/policies.md` 결과에 §6 영역 변경 없음)
  </acceptance_criteria>
  <done>Phase 6 용어·저녁반 차감 규칙·재량 결정 4건이 docs/에 기록돼 이후 플랜이 이름과 판정 규칙을 새로 만들 필요가 없다</done>
</task>

<task type="auto">
  <name>Task 2: ErrorCode 6종 + TransactionReason.EVENING_HALF_REFUND를 추가하고 error-codes.md와 1:1로 맞춘다</name>
  <files>src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt, src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt, docs/error-codes.md</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt (enum 상수 + KDoc 1줄 + defaultStatus 관례. 마지막 상수는 `BATCH_EXECUTION_NOT_FOUND`)
    - src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt (`EVENING_HALF`·`CLASS_CANCELED_REFUND` KDoc 스타일)
    - docs/error-codes.md (phase별 섹션 + 4열 표: 코드 / HTTP 상태 / 의미 / 발생 지점, 마지막 섹션은 "## 배치 코드 (Phase 5)")
    - src/main/kotlin/com/goldwrestling/pass/PassTransaction.kt (reason 컬럼이 `@Column(nullable = false, length = 30)`이고 DB CHECK가 없어 마이그레이션 없이 enum 확장만으로 저장 가능함을 확인)
  </read_first>
  <action>
`ErrorCode` enum 끝(`BATCH_EXECUTION_NOT_FOUND` 다음)에 아래 6개 상수를 각각 KDoc 한 줄과 함께 추가한다:
- `ATTENDANCE_NOT_FOUND(HttpStatus.NOT_FOUND)` — 대상 출석 기록 없음(삭제·수정 대상). 메시지에 id를 보간하지 않는다
- `ATTENDANCE_MEMBER_NOT_RESERVED(HttpStatus.CONFLICT)` — 예약제/1:1 출석 체크 대상이 그 세션의 활성 예약자가 아님(D-127 "예약자 명단에 대해 체크")
- `ATTENDANCE_CLASS_TYPE_MISMATCH(HttpStatus.CONFLICT)` — 저녁반 전용 API를 `SESSION`/`LESSON` 세션에 호출했거나, 예약자 체크 API를 `EVENING` 세션에 호출함
- `DUPLICATE_ATTENDANCE(HttpStatus.CONFLICT)` — 같은 회원·같은 세션에 출석 기록이 이미 존재(회원×세션 유니크 위반)
- `EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE(HttpStatus.CONFLICT)` — 유효한 저녁반 회비도 없고 `SESSION_PASS` 잔여도 0.5 미만이라 저녁반 출석 추가를 거부(D-128·D-133)
- `NOTICE_NOT_FOUND(HttpStatus.NOT_FOUND)` — 대상 공지 없음

`TransactionReason`에 `EVENING_HALF_REFUND`를 `EVENING_HALF` 옆이 아니라 enum 끝에 추가한다 —
KDoc은 "저녁반 출석 삭제로 인한 0.5회 복구 (D-128, EVENING_HALF의 반대)". **마이그레이션은 추가하지
않는다**(`pass_transaction.reason`은 VARCHAR(30)이고 DB CHECK가 없다).

docs/error-codes.md에 "## 운영 코드 (Phase 6)" 섹션을 "## 배치 코드 (Phase 5)" 다음, "**폴백 규칙**"
문단 앞에 추가하고 위 6개 코드를 4열 표로 기술한다. "발생 지점" 열은 이후 플랜이 만들 클래스명으로
채운다: `AttendanceService`(ATTENDANCE_MEMBER_NOT_RESERVED·ATTENDANCE_CLASS_TYPE_MISMATCH·
DUPLICATE_ATTENDANCE·ATTENDANCE_NOT_FOUND·EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE),
`NoticeService`(NOTICE_NOT_FOUND).

이 태스크는 conventions.md §10.0 면제 대상이다 — enum 상수 추가와 문서 표는 "필드 나열"·"문서"에
해당해 별도 테스트를 만들지 않는다. 각 코드가 실제로 던져지는 경로의 테스트는 06-04~06-08이 담당한다.
이 판단 근거를 SUMMARY에 한 줄로 남긴다(CLAUDE.md 규칙 10).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && for c in ATTENDANCE_NOT_FOUND ATTENDANCE_MEMBER_NOT_RESERVED ATTENDANCE_CLASS_TYPE_MISMATCH DUPLICATE_ATTENDANCE EVENING_ATTENDANCE_DEDUCTION_UNAVAILABLE NOTICE_NOT_FOUND; do grep -q "$c" src/main/kotlin/com/goldwrestling/common/error/ErrorCode.kt && grep -q "$c" docs/error-codes.md || exit 1; done && grep -q "EVENING_HALF_REFUND" src/main/kotlin/com/goldwrestling/pass/TransactionReason.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorCode.kt`에 신규 상수 6종이 존재하고 각각 `HttpStatus.NOT_FOUND` 또는 `HttpStatus.CONFLICT`가 명시돼 있다
    - `docs/error-codes.md`에 "## 운영 코드 (Phase 6)" 섹션이 있고 위 6개 코드가 각각 표의 행으로 존재한다
    - `TransactionReason.kt`에 `EVENING_HALF_REFUND`가 존재한다
    - `src/main/resources/db/migration/` 아래에 새 파일이 생기지 않았다 (`ls src/main/resources/db/migration | wc -l` 결과가 10)
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>이 phase가 던질 모든 도메인 예외의 코드가 enum과 FE 계약 문서에 1:1로 존재하고, 저녁반 복구 사유가 예약 복구와 구분 가능하다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 이 플랜은 문서와 enum 상수만 다룬다 — 요청 처리 경로가 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | 신규 `ErrorCode` 6종의 응답 메시지 | mitigate | 예외 메시지에 id·회원명 등 식별자를 보간하지 않는다(`PassNotFoundException` 관례) — 06-05가 예외 클래스를 만들 때 이 계약을 상속한다 |
| T-06-02 | Repudiation | `EVENING_HALF_REFUND` 미도입 시 저녁반 복구가 예약 복구와 구분 불가 | mitigate | 사유 코드를 분리해 원장에서 복구 원인을 식별 가능하게 한다(CLAUDE.md 규칙 6) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 이 phase는 신규 외부 의존성을 추가하지 않는다(06-RESEARCH.md Package Legitimacy Audit = N/A). 새 `implementation` 라인이 등장하면 그 시점에 게이트를 재수행한다 |
</threat_model>

<verification>
- `./gradlew ktlintFormat && ./gradlew build` BUILD SUCCESSFUL
- `git diff --stat` 결과가 위 6개 파일로만 구성된다 (코드 로직 변경 0건)
</verification>

<success_criteria>
- glossary·policies §4.2·decisions D-132~D-135·error-codes.md가 갱신됐다
- `ErrorCode` 6종 + `TransactionReason.EVENING_HALF_REFUND`가 컴파일된다
- 마이그레이션 파일이 추가되지 않았다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-01-SUMMARY.md` when done
</output>

