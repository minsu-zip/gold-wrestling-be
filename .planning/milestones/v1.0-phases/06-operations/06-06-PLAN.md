---
phase: 06-operations
plan: 06
type: execute
wave: 4
depends_on: ["06-02", "06-05"]
files_modified:
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt
  - src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt
  - src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceRequests.kt
  - src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceResponses.kt
  - src/test/kotlin/com/goldwrestling/attendance/AttendanceServiceTest.kt
autonomous: true
requirements: [ATTEND-01]
must_haves:
  truths:
    - "관리자가 특정 시간표·날짜의 예약자 명단을 출석 상태와 함께 한 번에 조회할 수 있다"
    - "체크 전 회원은 상태가 비어 있고(미체크), 체크하면 ATTENDED 또는 ABSENT가 된다"
    - "같은 회원을 다시 체크하면 새 행이 아니라 기존 행의 상태가 바뀐다(소급 수정 허용)"
    - "그 수업의 활성 예약자가 아닌 회원은 예약제/1:1 출석 체크 대상이 될 수 없다"
    - "저녁반 세션에는 이 체크 경로를 쓸 수 없다(전용 경로는 06-07)"
  artifacts:
    - path: "src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt"
      provides: "명단 프리로드 + 예약자 출석 upsert"
      exports: ["getRoster", "check"]
    - path: "src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceResponses.kt"
      provides: "명단·단건 응답 DTO"
      contains: "ClassSessionAttendanceRosterResponse"
  key_links:
    - from: "src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt"
      to: "reservation.ReservationRepository"
      via: "활성 예약자 명단 join fetch 재사용(N+1 방지)"
      pattern: "findAllByClassSessionIdInAndStatusWithMember"
---

<objective>
출석 체크(ATTEND-01)의 **예약제/1:1 경로**를 만든다 — 예약자 명단 프리로드 조회와 회원 건별
출석/불참 upsert. 차감이 전혀 개입하지 않는 순수 참고 데이터 경로다(policies §6).

목적: 저녁반 경로(차감 트랜잭션 포함, 06-07)와 분리해 먼저 완성한다. 두 경로를 한 플랜에 담으면
"명단 조회 + 상태 정정 + 회비 판정 + 조건부 차감 + 원장 기록"이 한 컨텍스트에 몰려 품질이 떨어진다.

산출물: 서비스 2 메서드 + 리포지토리 조회 1 추가 + 요청·응답 DTO + 통합테스트 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/policies.md
@.planning/phases/06-operations/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 명단·단건 DTO와 AttendanceRepository 명단 조회(join fetch)를 추가한다</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceRequests.kt, src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceResponses.kt, src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt (06-02이 만든 메서드 4종)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt 86-125행 (`join fetch` 관례와 그것이 필요한 이유 KDoc — 명단에 `member.name`을 담는 순간 LAZY 프록시를 건드린다)
    - src/main/kotlin/com/goldwrestling/schedule/dto/ (기존 관리자 보드 응답 DTO의 명단 표현 방식 — 셀별 예약자 명단 구조를 참고해 필드 이름을 맞춘다)
    - src/main/kotlin/com/goldwrestling/pass/dto/AdjustPassRequest.kt (`@field:` validation + `@Schema` 관례)
    - docs/glossary.md (06-01이 추가한 `AttendanceStatus`·`checkedAt` 명칭)
  </read_first>
  <behavior>
    - `findAllByClassSessionIdWithMember(classSessionId)`가 `member`를 함께 로딩해 명단 변환 시 추가 쿼리가 발생하지 않는다
    - `CheckAttendanceRequest`는 `classScheduleId`·`classDate`·`memberId`·`status` 4필드이며 전부 필수다
    - `AttendanceRosterEntryResponse.status`는 미체크 회원에서 null이다
  </behavior>
  <action>
`AttendanceRepository`에 메서드 하나를 추가한다 —
`@Query("select a from Attendance a join fetch a.member where a.classSession.id = :classSessionId")`
`fun findAllByClassSessionIdWithMember(@Param("classSessionId") classSessionId: Long): List<Attendance>`.
KDoc에 "명단 응답이 `member.name`을 담으므로 join fetch가 없으면 명단 크기만큼 추가 쿼리가 나간다"
(`ReservationRepository.findAllByClassSessionIdInAndStatusWithMember` KDoc과 동일 논리)를 적는다.
06-02이 만든 `findAllByClassSessionId`는 그대로 두되, 명단 조회 경로는 새 메서드를 쓴다.

`dto/AttendanceRequests.kt`:
- `CheckAttendanceRequest(classScheduleId: Long, classDate: LocalDate, memberId: Long, status: AttendanceStatus)` —
  전부 non-null Kotlin 타입. `@field:NotNull`은 non-null 타입이라 Jackson 역직렬화가 먼저 실패해
  `MALFORMED_REQUEST`가 나온다는 점을 KDoc에 남긴다(04-08 선례). `@Schema(description = ...)`로
  각 필드 의미를 적고 `classDate`는 ISO 날짜 형식임을 명시한다.
- `AddEveningAttendanceRequest(classScheduleId: Long, classDate: LocalDate, memberId: Long)` — 06-07이 쓸
  요청 DTO도 여기 함께 선언한다(같은 파일에 모으는 conventions §1 "DTO는 관련된 것끼리 한 파일" 허용).

`dto/AttendanceResponses.kt`:
- `AttendanceResponse(id, memberId, memberName, classSessionId, classDate, startTime, classType, status, deducted: Boolean, checkedAt)` —
  `deducted`는 `passTransaction != null`로 계산한다. `companion object { fun from(attendance: Attendance): AttendanceResponse }`.
  **`passTransaction`의 다른 필드(금액·사유)를 응답에 노출하지 않는다** — 차감 상세는 이용권 이력
  API가 담당한다.
- `AttendanceRosterEntryResponse(memberId, memberName, reservationId: Long?, attendanceId: Long?, status: AttendanceStatus?, deducted: Boolean)` —
  `status`가 null이면 미체크(D-127 "레코드 부재 = 미체크")임을 `@Schema`에 명시한다.
- `ClassSessionAttendanceRosterResponse(classScheduleId, classDate, classSessionId: Long?, classType, startTime, endTime, capacity: Int?, entries: List<AttendanceRosterEntryResponse>)` —
  `classSessionId`가 null이면 아직 그 날짜의 세션 행이 없다는 뜻(예약도 출석도 없음)이다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "join fetch a.member" src/main/kotlin/com/goldwrestling/attendance/AttendanceRepository.kt && grep -q "ClassSessionAttendanceRosterResponse" src/main/kotlin/com/goldwrestling/attendance/dto/AttendanceResponses.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AttendanceRepository.kt`에 `findAllByClassSessionIdWithMember`가 존재하고 쿼리에 `join fetch a.member`가 포함된다
    - `AttendanceRosterEntryResponse.status`의 타입이 nullable(`AttendanceStatus?`)이다
    - `AttendanceResponse`에 `amount`·`reason` 필드가 존재하지 않는다
    - `./gradlew compileKotlin` 성공
  </acceptance_criteria>
  <done>명단·단건 응답 계약과 N+1 없는 명단 조회 경로가 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AttendanceService.getRoster + check(upsert) — 예약자 검증과 소급 수정</name>
  <files>src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt</files>
  <read_first>
    - src/main/kotlin/com/goldwrestling/pass/AdminPassService.kt 30-60행 (`@Transactional(readOnly = true)` 클래스 + 쓰기 메서드 오버라이드, 관리자 조회 관례)
    - src/main/kotlin/com/goldwrestling/schedule/ClassSessionService.kt (`getOrCreate(schedule, classDate)`와 `findExisting(scheduleId, classDate)`의 차이 — 조회 경로는 세션을 만들지 않는다)
    - src/main/kotlin/com/goldwrestling/reservation/ReservationRepository.kt (`findAllByClassSessionIdInAndStatusWithMember` 시그니처)
    - src/main/kotlin/com/goldwrestling/schedule/ClassScheduleRepository.kt (시간표 단건 조회 + 없을 때 던지는 예외 `ClassScheduleNotFoundException`)
    - src/main/kotlin/com/goldwrestling/attendance/EveningHalfDeductionPolicy.kt (06-05의 `requireEveningSession` — 여기서는 반대 판정이 필요하다)
    - docs/policies.md §6
  </read_first>
  <behavior>
    - `getRoster(classScheduleId, classDate)`가 세션이 없으면 `entries` 빈 목록과 `classSessionId = null`을 반환한다(세션을 생성하지 않는다)
    - 예약제/1:1 세션의 `entries`는 활성 예약자 전원이며, 출석 레코드가 있는 회원만 `status`가 채워진다
    - 저녁반 세션의 `entries`는 출석 레코드가 있는 회원만(예약이 없으므로)이다
    - `check(adminId, request)`가 첫 호출에서 `Attendance` 행을 만들고, 두 번째 호출에서는 같은 행의 `status`만 바꾼다
    - 활성 예약자가 아닌 회원을 `check`하면 `AttendanceMemberNotReservedException`이 난다
    - 저녁반(`EVENING`) 세션에 `check`를 호출하면 `AttendanceClassTypeMismatchException`이 난다
    - `checkedBy`/`checkedAt`이 매 호출마다 갱신된다(마지막으로 확인한 관리자·시각)
  </behavior>
  <action>
`AttendanceService`를 `@Service @Transactional(readOnly = true)`로 만든다. 생성자 주입:
`attendanceRepository`, `reservationRepository`, `classScheduleRepository`, `classSessionService`,
`memberRepository`, `adminRepository`, `clock`. (저녁반 차감에 필요한 `passRepository`·
`passTransactionRepository`는 06-07이 추가한다 — 이 플랜에서는 넣지 않는다.)

`getRoster(classScheduleId: Long, classDate: LocalDate): ClassSessionAttendanceRosterResponse`
- `classScheduleRepository`로 시간표를 조회하고 없으면 `ClassScheduleNotFoundException`
- `classSessionService.findExisting(classScheduleId, classDate)`로 세션을 조회한다 —
  **`getOrCreate`를 쓰지 않는다.** 조회가 세션 행을 만들면 관리자가 화면을 열기만 해도 빈 세션이
  쌓인다(`ScheduleService`가 조회 경로에서 `findExisting`을 쓰는 것과 같은 이유).
- 세션이 없으면 시간표에서 복사한 표시 정보(`classType`/`startTime`/`endTime`/`capacity`)와
  빈 `entries`로 응답한다.
- 세션이 있으면: `classType`이 `EVENING`이면 `attendanceRepository.findAllByClassSessionIdWithMember(sessionId)`만으로
  entries를 만든다. `SESSION`/`LESSON`이면 `reservationRepository.findAllByClassSessionIdInAndStatusWithMember(listOf(sessionId), ReservationStatus.ACTIVE)`로
  활성 예약자를 가져오고, 출석 레코드를 `memberId` 키 맵으로 만들어 병합한다 — 예약자 전원이
  entries에 나오고 출석 레코드가 없는 회원은 `status = null`(미체크)이다.
  **새 쿼리를 만들지 않고 기존 join fetch 메서드를 단일 id 리스트로 호출한다**(Don't Hand-Roll).

`@Transactional fun check(adminId: Long, request: CheckAttendanceRequest): AttendanceResponse`
- 시간표 조회 → `classSessionService.getOrCreate(schedule, classDate)`로 세션 확보
  (여기는 쓰기 경로라 세션을 만들어도 된다 — 예약이 하나도 없던 타임에 출석만 기록하는 경우가 있다)
- 세션 `classType`이 `EVENING`이면 `AttendanceClassTypeMismatchException` — 저녁반은 06-07의 전용
  경로가 담당한다(차감 판정이 필요하기 때문)
- `reservationRepository.findAllByClassSessionIdInAndStatusWithMember(listOf(sessionId), ACTIVE)` 결과에
  `request.memberId`가 없으면 `AttendanceMemberNotReservedException`
- `attendanceRepository.findByClassSessionIdAndMemberId(sessionId, memberId)`가 있으면 그 엔티티의
  `status`/`checkedBy`/`checkedAt`을 갱신하고, 없으면 새 `Attendance`를 `save`한다
  (`passTransaction = null` — 예약제/1:1은 차감이 없다, policies §6)
- 시각은 `OffsetDateTime.now(clock)`으로만 얻는다
- **`PassTransaction`·`INACTIVITY` 이력을 조회하거나 수정하는 코드를 어떤 형태로도 넣지 않는다**
  (D-127: 소급 출석이 이미 실행된 차감을 되돌리지 않는다). 이 금지를 클래스 KDoc에 policies §6
  문구를 인용해 남긴다.
  </action>
  <verify>
    <automated>./gradlew compileKotlin -q && grep -q "findExisting" src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt && ! grep -q "PassTransactionRepository" src/main/kotlin/com/goldwrestling/attendance/AttendanceService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AttendanceService.kt`의 `getRoster`가 `classSessionService.findExisting`을 호출하고 `getOrCreate`를 호출하지 않는다
    - `check`가 `getOrCreate`를 호출한다
    - 파일 전체에 `PassTransactionRepository`·`INACTIVITY` 문자열이 존재하지 않는다
    - 클래스 레벨 `@Transactional(readOnly = true)`가 있고 `check`에만 `@Transactional`이 붙어 있다
  </acceptance_criteria>
  <done>예약자 명단 프리로드와 건별 출석 upsert가 차감을 전혀 건드리지 않고 동작한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AttendanceServiceTest — 명단·미체크·소급 수정·예약자 아님·저녁반 거부</name>
  <files>src/test/kotlin/com/goldwrestling/attendance/AttendanceServiceTest.kt</files>
  <read_first>
    - src/test/kotlin/com/goldwrestling/attendance/AttendanceFixtures.kt (06-02 픽스처)
    - src/test/kotlin/com/goldwrestling/batch/InactivityBatchRunnerTest.kt 1-60행 (통합테스트 애노테이션 조합·정리 관례)
    - .claude/skills/add-domain-test/SKILL.md §2·§3
    - docs/policies.md §6 (테스트 메서드명으로 그대로 쓸 문장)
  </read_first>
  <behavior>
    - `세션이 없으면 명단은 비어 있고 세션을 만들지 않는다`: `getRoster` 후 `class_session` 행 수가 그대로다
    - `예약제 수업 명단에는 활성 예약자가 전원 나오고 체크 전에는 상태가 비어 있다`
    - `취소된 예약자는 명단에 나오지 않는다`
    - `불참으로 체크했다가 출석으로 정정할 수 있다`: 두 번째 `check` 후 attendance 행 수 1, status ATTENDED
    - `예약자가 아닌 회원은 출석 체크할 수 없다`: `AttendanceMemberNotReservedException`
    - `저녁반 수업에는 예약자 출석 체크 경로를 쓸 수 없다`: `AttendanceClassTypeMismatchException`
    - `출석을 기록해도 이용권 잔여와 차감 이력은 변하지 않는다`: `pass_transaction` 건수·`remainingCount` 불변
  </behavior>
  <action>
`AttendanceServiceTest`를 `@SpringBootTest` + `@Import(TestcontainersConfiguration::class)`
(+ 시각 고정이 필요하면 `TestClockConfiguration`) 조합으로 만든다 — 기존 통합테스트와 동일 조합을
유지해 컨텍스트 캐시를 재사용한다.

테스트 메서드명은 policies §6 문장을 그대로 쓴 백틱 한국어 서술문으로 한다.
위 behavior 7개를 각각 하나의 테스트로 작성한다. 마지막 테스트("잔여와 이력이 변하지 않는다")가
policies §6 "출석 기록은 차감과 무관한 참고용 데이터다"의 직접 증명이므로 반드시 포함한다.

데이터 정리는 기존 배치 테스트와 같은 방식(`@AfterEach` + `JdbcClient`)을 쓰고, 삭제 순서에서
`attendance`를 `reservation`·`class_session`보다 먼저 지운다.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.goldwrestling.attendance.AttendanceServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew test --tests "com.goldwrestling.attendance.AttendanceServiceTest"` 가 0 failures로 끝난다
    - 테스트 파일에 `차감과 무관한 참고용 데이터` 또는 동등한 문장을 담은 테스트 메서드가 존재한다
    - `./gradlew ktlintFormat && ./gradlew build` 가 BUILD SUCCESSFUL로 끝난다
  </acceptance_criteria>
  <done>예약제/1:1 출석 경로의 성공·정정·거부 시나리오와 "차감 무관" 불변식이 실제 DB에서 검증된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 관리자 요청 → 출석 데이터 | 관리자 전용 경로이며 회원 principal은 URL 인가에서 차단된다 |
| 출석 서비스 → 이용권 원장 | 이 경로는 원장을 **읽지도 쓰지도 않는다**(설계상 격리) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering | 예약자가 아닌 회원을 출석 처리해 명단·통계 왜곡 | mitigate | `check`가 활성 예약자 목록에 없는 memberId를 `AttendanceMemberNotReservedException`으로 거부 |
| T-06-17 | Tampering | 출석 경로가 `INACTIVITY` 이력을 되돌려 멱등(D-106) 파괴 | mitigate | 서비스에서 원장 리포지토리 주입 자체를 배제하고 grep acceptance로 고정 |
| T-06-18 | Denial of Service | 조회만 해도 `class_session` 행이 생성돼 무한 증식 | mitigate | `getRoster`는 `findExisting`만 쓰고 세션을 만들지 않는다(테스트로 실증) |
| T-06-19 | Information Disclosure | 명단 응답에 전화번호 등 불필요한 회원 정보 노출 | mitigate | entries에 `memberId`·`memberName`만 담는다(DTO 설계) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 해당 없음 — 신규 외부 의존성 없음 |
</threat_model>

<verification>
- `./gradlew test --tests "com.goldwrestling.attendance.*"` 0 failures
- `AttendanceService.kt`에 이용권 원장 참조가 없다
</verification>

<success_criteria>
- 명단 프리로드가 예약자 전원 + 미체크 표현을 제공한다
- 출석 upsert가 새 행을 만들지 않고 상태만 정정한다
- 예약자 아님·저녁반 오용이 각각 전용 409로 거부된다
</success_criteria>

<output>
Create `.planning/phases/06-operations/06-06-SUMMARY.md` when done
</output>
